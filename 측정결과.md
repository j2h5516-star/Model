# 측정결과.md — 사전 등록 측정의 실행 기록

전략.md 7장에 **데이터를 보기 전에** 박제한 설계를 그대로 실행한 결과입니다.
설계는 손대지 않았고, 여기에는 결과만 기록합니다.

- 1차 측정일: 2026-08-11
- 데이터: `data/measure/snapshot.json` (앱 저장 2026-08-11 03:53 UTC)
- 실행 코드: `measurement.py` (검증 테스트 9개 + 일부러 깨뜨려 확인)
- 표본: 13종목 · 발표 사건 98건 · 창 60거래일 · 폭등 = SPY 대비 +20%p

---

## 결과 요약 — 한 줄

> **H1(가속→폭등)은 이 표본에서 미채택. 가속 판정 뒤에 사면 아무 발표나
> 사는 것보다 오히려 낮았다. 대신 H2(신고점 돌파)가 유망하나 아직 미확정.**

---

## H1 — "가속이 클수록 폭등하는가": ❌ 미채택

| 그룹 | 사건 | 폭등률(+20%p) | 95% 구간 | 중앙 초과수익 |
|---|---|---|---|---|
| **가속(강+약)** | 23 | **34.8%** | 18.8~55.1 | +5.0%p |
| 유지 | 5 | 100.0% | 56.6~100 | +55.5%p |
| 혼조 | 10 | 60.0% | 31.3~83.2 | +36.5%p |
| 감속(강+약) | 10 | 60.0% | 31.3~83.2 | +21.4%p |
| 판단불가 | 50 | 36.0% | 24.1~49.9 | +8.6%p |
| **기준선② 아무 발표나 사기** | 98 | **43.9%** | 34.5~53.7 | +12.3%p |
| 기준선① 아무 날이나 사기 | 2,908 | 33.0% | 31.3~34.7 | — |

- 사전 등록된 채택 기준(신뢰구간이 기준선②와 분리)을 **못 넘었고**,
  방향조차 열세입니다 — 가속 34.8% < 모든 발표 43.9%.
- 가속폭 3등분(단조 검사): 하 28.6% / 중 28.6% / 상 44.4% —
  단조 증가라 할 수 없고 구간이 전부 겹칩니다.
- 종목별 가속 사건: MU 67%(3건) · ICHR 100%(1건) · LITE 50%(2건) ·
  STRL 50%(2건) · MXL 33%(6건) · TTMI 25%(4건) · PLTR 0%(4건) · AMD 0%(1건)
  — 한두 종목이 끌어올린 결과가 아니라, 신호 자체가 고르지 않습니다.

### 이 결과가 말하는 것 (해석은 조심스럽게)

유지·혼조·감속 시점의 성적이 가속보다 높았다는 그림은, 사전 등록해 둔
반대 가설 **H0(시장 선반영)** 및 이미 측정돼 있던 사실
**"델타는 사이클 전환점에서 늦다"**(마이크론 4분기, 크레도 2분기 지각)와
맞아떨어집니다. 즉 — **모델이 "가속"이라 확인해 줄 때는 주가가 이미
달린 뒤**일 가능성이 큽니다. 다만 유지 5건(100%)은 표본이 5개뿐이라
결론에 쓰지 않습니다.

---

## H2 — "TTM 신고점 돌파가 폭등에 선행하는가": ⏳ 유망하나 미확정

| 그룹 | 사건 | 폭등률 | 95% 구간 | 중앙 초과수익 |
|---|---|---|---|---|
| **신고점 돌파** | 27 | **59.3%** | 40.7~75.5 | +21.6%p |
| 신고점 아님 | 71 | 38.0% | 27.6~49.7 | +9.5%p |
| 기준선② | 98 | 43.9% | 34.5~53.7 | +12.3%p |

- 비율 +15.4%p 우세, 중앙 초과수익 거의 2배 — **방향은 좋습니다.**
- 그러나 신뢰구간(40.7~)이 기준선②(~53.7)와 **겹치므로 채택 기준 미달.**
  약속대로 점수에 반영하지 않고, **표본을 늘려 재측정**합니다.
- 이익 쪽에서는 이미 97.0%로 측정된 신호(config.py 국면)라는 점과
  방향이 일치합니다.

---

## 정직하게 밝히는 한계

1. **표본 편향이 큽니다.** 13종목 전부 사용자가 고른 관심 성장주이고
   부진 종목이 섞이지 않았습니다. "아무 날이나 사도" 폭등률이 33%나
   되는 것이 그 증거입니다(강세장 + 고베타 유니버스). 따라서 **절대
   수치는 부풀려져 있고, 그룹 간 비교(내부 대조)만 믿을 수 있습니다.**
2. 판단불가 50건 — 대부분 수집 초기 분기라 역사가 모자란 사건입니다.
   파서 커버리지가 오르면(현재 110분기) 판정 가능 사건이 늘어납니다.
3. 우측검열 11건(창이 안 끝난 최근 발표) · 구간끊김 11회(분기 누락) ·
   중복발표 1건은 세지 않고 개수만 기록했습니다.
4. 2021~2026 사이 단일 시장 국면입니다. 약세장 표본이 없습니다.
5. ZETA(조정 EPS 미발표)·COHR(3분기뿐)는 사건에 기여하지 못했습니다.

## 다음 측정 과제 (우선순위 순)

1. **부진 종목 혼합** — 표본 편향을 깨는 것이 급선무. 부진/횡보 종목을
   목록에 추가해 재수집·재측정.
2. **H2 표본 확대** — 신고점 돌파 사건을 늘려 신뢰구간을 좁힌다.
3. **"가속 이전" 창 검사** — 폭등이 가속 판정보다 먼저 온다면, 신고점
   돌파(H2)나 혼조→가속 전환 같은 **더 이른 신호**가 포착 지점이라는
   뜻. 새 가설로 번호를 붙여 사전 등록 후 측정.
4. ZETA류를 위한 조정 EBITDA 자(尺) 추가 후 H1-b 별도 측정.

---
---

# 2차 측정 — 2026-08-11 (부진 종목 혼합 후)

- 데이터: 앱 저장 2026-08-11 04:30 UTC — **19종목** (기존 13 +
  INTC·ENPH·SWKS·PYPL·QRVO 부진/횡보 5 + APP)
- 발표 사건 **139건** (98 → 139). APP은 ZETA처럼 조정 EPS 미발표(0건이 정답).
- 설계는 1차와 동일 (전략.md 7장 그대로). 기준선② = 34.5% [27.1~42.8],
  기준선①(아무 날이나, 4,342창) = 27.1%.

## 사전 등록 가설 재확인

| 그룹 | n | 폭등률 | 95% 구간 | 중앙 초과 |
|---|---|---|---|---|
| **H1 가속(강+약)** | 32 | **25.0%** | 13.3~42.1 | +4.3%p |
| **H2 신고점 돌파** | 36 | **50.0%** | 34.5~65.5 | +20.0%p |
| H2 신고점 아님 | 103 | 29.1% | 21.2~38.5 | +3.7%p |
| 기준선② 모든 발표 | 139 | 34.5% | 27.1~42.8 | +9.5%p |

- **H1: 재확인 — 미채택.** 표본을 넓히니 오히려 더 낮아짐(34.8→25.0%).
  가속폭 단조성도 여전히 없음(20.0/30.0/25.0). 이 가설은 이제 두 번
  연속 기준선에 졌습니다.
- **H2: 우세가 더 벌어짐**(+15.5%p, 중앙 초과 5배)이나 구간이 아직
  겹쳐 미채택 유지. 표본이 늘수록 좋아지는 방향은 고무적.

## 탐색 측정 — 어떤 공시 데이터가 상승과 같이 움직이나

⚠️ **탐색은 채택 근거가 아닙니다** (요인 8개를 동시에 재면 우연한 승자가
반드시 나옵니다). 잰 것을 좋든 나쁘든 전부 기록하고, 시간 분할(앞시기
2023-05~2025-03 / 뒤시기 2025-03~2026-05)로 걸렀습니다.

**중대한 교란 발견**: 뒤시기는 기준선 자체가 ~50%인 초강세장이라
(앞시기 ~17.9%), 시기 구분 없이 보면 모든 요인이 좋아 보입니다.

| 요인 | 폭등률 (n) | 앞시기 | 뒤시기 | 판정 |
|---|---|---|---|---|
| **TTM 신고점 돌파** | 50.0% (36) | 37.5% | 53.6% | ✅ 두 시기 모두 우세 — 유일 |
| (비교: 신고점 아님) | 29.1% (103) | 14.8% | 50.0% | |
| **가속 확인 후 매수** | 23.3% (30) | **6.2%** | 42.9% | ❌ 두 시기 모두 열세 |
| 유지 | 85.7% (7) | 100% | 80% | 표본 7 — 참고만 |
| TTM YoY ≥40% | 66.7% (9) | 표본 0 | 66.7% | 뒤시기에만 존재 — 판단 불가 |
| 분기 YoY ≥40% | 50.0% (24) | **0%** | 57.1% | ❌ 앞시기에서 무너짐 |
| 분기 QoQ ≥10% | 42.9% (49) | 25.0% | 55.2% | 약한 우세, 확정 불가 |
| 매출 TTM YoY ≥40% | 55.6% (9) | 표본 0 | 55.6% | 뒤시기에만 존재 |
| TTM 흑자전환 | 33.3% (3) | — | — | 표본 3 — 판단 불가 |

### 탐색의 결론

1. **시간 분할을 통과한 양(+) 신호는 TTM 신고점 돌파 하나뿐**입니다.
   두 시기 모두에서 "신고점 아님"보다 높았습니다 (앞 +22.7%p / 뒤 +3.6%p).
2. **가속 확인 후 매수는 두 시기 모두 기준선 이하** — 특히 정상 장세였던
   앞시기에 6.2%(16건 중 1건)로 사실상 최악의 매수 시점이었습니다.
   "모델이 가속을 확인해 줄 때는 이미 늦다"가 재확인됩니다.
3. 성장률 수준 요인들(TTM YoY 높음 등)은 뒤시기 초강세장에만 표본이
   있어 **장세 효과와 분리할 수 없습니다.** 데이터가 더 쌓여야 합니다.

## 결정 기록 (2026-08-11)

- **월가 컨센서스 제거 — 확정.** 근거: ① 제3자 추정이라 새 모델의 창작
  금지 원칙에 이미 저촉(신뢰도 40%/30% 등급이 전부 추정 경로) ② 측정으로
  검증된 적이 한 번도 없음 ③ 수집도 불안정. 새 모델의 전망 입력은
  **회사가 직접 발표한 가이던스뿐**이며, 없으면 "없음"으로 둔다.
  (구모델은 비교 대상으로 당분간 그대로 둔다 — 전략.md 5장)
- **H3 사전 등록 (가이던스 델타)**: 데이터 수집 전에 등록. 발표 시점에
  회사가 준 **다음 분기 가이던스 EPS(범위면 중간값)가 이번 실제 분기
  EPS보다 +10% 이상**이면 "가이던스 가속"으로 보고, 이후 60거래일
  초과수익을 7장과 같은 규칙(기준선·윌슨·채택 기준)으로 잰다.
  ※ 현재 스냅샷에는 분기별 가이던스가 없어, 앱쪽 저장 확장(가이던스
  EPS 파서 포함) 후 측정한다.

---
---

# 3차 측정 — 2026-08-11 (추세 → EPS 역방향 + 합동 신호)

사용자 제안: "예측 방향만 보지 말고, **주봉 지속 상승추세일 때 조정
EPS가 어떻게 움직였는지**를 보자." 오른 구간만 보면 뭐든 좋아 보이므로
**비상승 구간과 나란히** 봤고, 이어서 앞방향(조합별 이후 수익)도 쟀습니다.

- 추세 정의(실행 전 고정): 주봉 종가 > 26주선, 13주선 > 26주선이
  **13주 연속** = 지속상승. 발표일까지의 주봉만 사용(미래 차단).
- 표본: 발표 사건 139건 중 지속상승 40 · 비상승 96 · 판단불가 3

## 역방향 — 상승추세일 때 조정 EPS는 어떤 상태였나

| | 지속상승 구간 (40건) | 비상승 구간 (96건) |
|---|---|---|
| TTM 신고점 비율 | **37.5%** | 21.9% |
| 분기 QoQ 상승 비율 | **62.5%** | 32.3% |
| 분기 YoY 중앙값 | **+156.8%** | +48.7% |
| 가속 판정 비율 | 27.5% | 21.9% |

**추세는 EPS 개선과 확실히 동행합니다** — QoQ 상승 비율 2배, YoY 3배.
주가 추세가 펀더멘털의 그림자라는 전제가 실측으로 확인됐습니다.

## 앞방향 — 발표 시점 [추세 × 신고점] 조합별 이후 60거래일

| 조합 | n | 폭등률 | 95% 구간 | 중앙 초과 |
|---|---|---|---|---|
| **상승추세 + 신고점 돌파** | 15 | **60.0%** | 35.7~80.2 | **+51.5%p** |
| 상승추세 + 신고점 아님 | 25 | 28.0% | 14.3~47.6 | **−7.0%p** |
| 비추세 + 신고점 돌파 | 21 | 42.9% | 24.5~63.5 | +16.9%p |
| 비추세 + 신고점 아님 | 75 | 29.3% | 20.2~40.4 | +9.5%p |
| (기준선② 모든 발표) | 139 | 34.5% | 27.1~42.8 | +9.5%p |

### 읽는 법 — 이번 측정의 핵심 발견

1. **추세만으로는 부족합니다.** 상승추세 전체는 40.0%로 기준선과 구별
   안 됩니다. 그런데 그 안을 신고점으로 가르면 60.0% vs 28.0%로
   갈라집니다.
2. **"상승추세인데 신고점이 아닌" 칸이 전체에서 가장 나쁩니다**
   (중앙 −7.0%p, 유일한 음수). 이익 신기록이 받쳐 주지 않는 추세는
   김이 빠진다 — 는 해석과 부합합니다.
3. 조합(60.0%)은 신고점 단독(50.0%)보다도 높습니다. 두 정보가 서로
   보완적이라는 뜻입니다.

## 결정 — H4 등록 (표본 확대 후 채택 판정)

- **H4**: 발표 시점에 [주봉 지속 상승추세] AND [TTM 조정 EPS 신고점
  돌파]이면, 이후 60거래일 시장 대비 초과수익. 창·문턱·기준선·채택
  기준은 7장과 동일.
- 현재 n=15, 구간 [35.7~80.2]가 기준선 [27.1~42.8]와 아직 겹침 →
  **미채택.** 탐색에서 나온 조합이므로 다중 비교 위험이 있고,
  새로 쌓이는 발표들로 확인한 뒤에만 채택한다.
- 지금까지의 서열 (측정된 것만): 추세×신고점 60.0% > 신고점 단독
  50.0% > 기준선 34.5% > 가속 25.0%.

---
---

# 표본 확대 결정 — 2026-08-11 (4차 측정 준비)

H2·H4 채택 판정과 H3 첫 측정에 표본이 부족해, 다음과 같이 확대합니다
(사용자 위임: "표본이 부족하다면 너가 샘플로 채택해서 뽑아").

1. **수집 기간 3년 → 약 5년** (`HISTORY_START_DATE` 2021-09-15).
   지금까지의 최대 약점 = 표본이 2023년 이후 강세장뿐. **2021~2022
   약세장**이 들어와야 신호가 장세를 가리는지 알 수 있습니다.
   (8-K 스캔 100건·조기 종료 24건으로 상향, 국면 라벨 "3년내 최고" →
   "5년내 최고"로 정정)
2. **종목 19 → 29** — 성장·부진 균형으로 직접 채택:
   NVDA·ANET·FN·SMCI (성장) + LSCC·SYNA·UCTT·IPGP·CGNX (부진·횡보)
   + ONTO (중간). SMCI는 급등과 급락을 모두 가진 종목이라 특히 값짐.
3. **가이던스 EPS 파서 신설** — 분기 문장만 읽고 연간·GAAP 전용은
   거름 (실물 검증: 샌디스크 $44~46 ✓ · CRDO EPS 가이던스 없음 ✓).
   스냅샷 분기 행에 guid_eps_low/high/mid 로 담겨 **다음 저장부터
   H3(가이던스 델타 ±10%)가 측정 가능**해집니다.
4. 주가 이력 시작 전의 옛 발표는 '주가시작전'으로 제외하는 가드 추가
   (5년 rolling 주가 창 밖의 발표에 몇 달 뒤 가격으로 진입하는 오류 방지).

다음 저장 후 4차 측정에서 재는 것: H1(다중 장세 재확인) · H2·H4(채택
판정) · H3(첫 측정). 예상 사건 수 ~29종목 × 최대 19분기 ≈ 400건 안팎.

---
---

# 4차 측정 — 2026-08-11 (29종목 · 약 5년 · 약세장 포함)

- 데이터: 앱 저장 2026-08-11 05:44 UTC. 분기 542개 · 조정 EPS 406개.
- 발표 사건 **380건** (2021-10 ~ 2026-05 — 2021~22 약세장 포함).
- 기준선② 모든 발표: **21.8%** [18.0~26.3] · 기준선① 아무 날이나:
  25.7% [24.7~26.7] (6,732창)

## 판정 결과 — 전 가설

| 가설 | n | 폭등률 | 기준선② 대비 | 판정 |
|---|---|---|---|---|
| H1 가속(강+약) | 67 | 19.4% [11.7~30.4] | −2.4%p | **기각 확정 (3연속)** |
| H2 신고점 돌파 | 59 | 25.4% [16.1~37.8] | +3.6%p | 미채택 (우세 축소) |
| H4 추세×신고점 | 19 | 26.3% [11.8~48.8] | +4.5%p | 미채택 (우세 붕괴) |
| H3 가이던스 델타 | 13 | — | — | 표본 부족 (판정 불가) |

- **H1은 세 번의 독립된 표본(98→139→380건)에서 모두 기준선 이하.**
  "가속 확인 후 매수"는 이 유니버스에서 매수 신호가 아닙니다 — 확정.
  가속폭 단조성도 3회 연속 없음 (18.2/9.1/30.4).
- **H2·H4의 강세장 우위는 장세 효과였습니다.** 3차(강세장 위주)에서
  50%/60%였던 것이 5년 전체에서 25.4%/26.3%로 내려앉았습니다.
  사전 등록(신뢰구간 분리)을 크게 못 미침 — 미채택.
- **H3**: EPS 가이던스를 주는 회사 자체가 적어(542분기 중 17건 —
  CRDO·NVDA·AMD 등 대부분 매출·GM만 제공) 사건 13건. "+10% 가속"
  구간은 0건. 판정 불가로 기록.
- **기준선①(25.7%) > 기준선②(21.8%)**: 이 유니버스에서 발표일은
  특별히 좋은 진입 시점이 아니었습니다.

## 유지되는 사실 하나 (역방향)

지속 상승추세 구간은 비상승 구간보다 EPS 상태가 일관되게 좋았습니다 —
신고점 비율 24.1% vs 14.8%, QoQ 상승 55.7% vs 36.7%, 분기 YoY 중앙값
+46.3% vs 0.0%. **추세와 펀더멘털은 동행합니다. 그러나 발표 시점에
그것을 확인하고 사면 이미 수익 우위가 없습니다** — 4번의 측정이
일관되게 가리키는 그림입니다.

## 결론 (이 프로젝트의 핵심 질문에 대한 현재 답)

> "조정 EPS 가속이 커질수록 주가가 폭등하는가?"
> → **아니오.** 세 표본 모두에서 가속 확인 후 매수는 기준선을 밑돌았다.
>
> "폭등을 발표 신호로 포착할 수 있는가?"
> → 5년 전체 표본에서는 **아직 채택 가능한 신호가 없다.** 강세장
> 구간에서만 신고점·추세×신고점이 통했다(장세 의존). 새 분기가
> 쌓이면 H2·H4는 재판정한다.

## 화면에 반영할 것 (다음 작업)

채택된 신호가 없으므로, 화면 개편의 방향은 "새 신호 올리기"가 아니라
**정직화**입니다: 델타·신고점·추세는 사실로만 표시하고, 각 신호 옆에
측정된 폭등률과 기준선(21.8%)을 함께 보여 줍니다. "가속 = 매수"로
읽히는 요소는 제거합니다.

---
---

# 5차 관찰 — 2026-08-11 (장세 폭 게이지, 사이클 질문)

사용자 질문: "AI·반도체 슈퍼사이클 같은 장세를 맞추고 싶다."
4차까지의 교훈(개별 신호 우위 미미, **장세 변수가 지배적**)에 따라
사이클을 조작적으로 정의했습니다: **한 무리가 동시에 좋아지는 현상 = 폭.**

- 도구: `breadth.py` — 전부 걸어가며(미래 미리보기 없음) 계산
  · 주가 폭 = 지속 상승추세 종목 비율 (주간)
  · 실적 폭 = 최근 발표가 TTM 신고점이었던 종목 비율 (발표 140일 이내만)
  · 개선 폭 = 최근 발표 분기 YoY 플러스 비율
- 시계열: 2023-05 ~ 2026-08 (172주). ⚠️ 2021~22 약세장은 실적 폭의
  역사 요건(TTM 2개 + 신선도) 때문에 게이지가 못 미칩니다 — 한계.

## 눈으로 확인된 사례 두 개 (실적 폭이 먼저 움직임)

1. **2024년 봄**: 주가 폭 40%대인데 실적 폭이 25%→8%로 붕괴 →
   이후 주가 폭이 가을까지 7%로 추락.
2. **2025년 봄**: 주가 폭 0%(조정)인데 실적 폭은 24%→44%로 상승 →
   5개월 뒤 주가 폭 60%로 폭발 (차트의 노란 구간).

시차 상관의 최대값(+24주, r=0.826)은 구간 끝자락이라 추세 겹침의
착시일 수 있어 **참고만** 합니다. 위 두 사례가 더 믿을 만한 증거입니다.

## 조건부 탐색 — 발표 시점 게이지 상태별 폭등률

⚠️ 게이지 설계가 4차 이후에 나왔으므로 이 표는 **탐색**입니다(채택 아님).

| 발표 시점 상태 | n | 폭등률 | 95% 구간 |
|---|---|---|---|
| **실적 폭 ≥ 20% (ON)** | 199 | **30.7%** | 24.7~37.4 |
| 실적 폭 < 20% (OFF) | 73 | **12.3%** | 6.6~21.8 |
| 주가 폭 ≥ 30% (ON) | 125 | 24.8% | 18.1~33.0 |
| 주가 폭 < 30% (OFF) | 147 | 26.5% | 20.1~34.2 |
| 기준선 (모든 발표) | 380 | 21.8% | 18.0~26.3 |

**이 프로그램 전체에서 가장 깨끗한 분리입니다** — ON/OFF 신뢰구간이
겹치지 않고(24.7 > 21.8), 중앙 초과수익도 +7.8 vs −3.1%p.
그리고 흥미롭게도 **주가 폭은 아무것도 못 가릅니다** — 시장 자신의
추세 폭은 예측력이 없고, **실적 신기록의 폭**이 정보를 담고 있습니다.

주의: 같은 시기 발표들은 같은 장세를 공유하므로(군집 효과) 실제
불확실성은 구간보다 넓습니다. 사이클 전환 표본도 사실상 2번뿐입니다.

## H5 사전 등록 (게이지 신호)

- **H5**: 발표 시점의 **실적 폭(신고점 종목 비율) ≥ 20%** 이면 이후
  60거래일 폭등률이 기준선을 웃돈다. 창·기준선·판정 규칙은 7장과 동일.
- 문턱 20%와 신선도 140일은 이 문서 시점에 고정. **채택 판정은
  앞으로 쌓이는 발표들로만** 합니다 (탐색 표본 재사용 금지).
- 화면에는 게이지를 **사실**로 상시 표시: "실적 폭 N% · 이 상태의
  과거 폭등률 30.7%(ON)/12.3%(OFF), 탐색치" — 정직화 원칙대로.

---
---

# 6차 — 2026-08-11 (v2 재건축 완료 + 수집 로봇 데이터로 배터리 1차)

## v2 재건축 실행 기록

- v1 전체를 `v1-final` 브랜치에 봉인 (3cae127)
- **수집 로봇 자립 확인**: 깃허브 액션 2차 가동이 스스로 수집하고
  스스로 커밋 (`e0699bf 자동 수집 — 2026-08-11`). 조정 EPS 29종목 ·
  일봉 30종목 · 실패 원문 41건 · 약 9분. 1차 가동의 실패(.gitignore 가
  data 전체를 막음)를 수리한 뒤의 결과. **저장 버튼 폐지 완료.**
- 얇은 계기판 배포: 점수제·신뢰도 등급·컨센서스 철거 (~6,900줄),
  검증된 장기만 이식 (파서·측정 장치·status). 전체 테스트 161개.

## 배터리 1차 — 수집 로봇 데이터 (사건 380건, 기준선 21.8%)

경계는 실행 전 고정 (exploration.py: 꾸준<10 / 들쭉≥30 · 0/1/2+ ·
첫돌파/연속2+). 전 요인 기록:

| 요인 | 구간 | n | 폭등률 | 앞/뒤 |
|---|---|---|---|---|
| 델타 변동성 | 꾸준(<10) | 55 | **16.4%** [8.9~28.3] | 12.5/17.9 |
| | 중간 | 40 | 27.5% | 25.0/28.6 |
| | 들쭉(≥30) | 27 | 25.9% | 10.0/35.3 |
| 연속 가속 | 0 / 1 / 2+ | 291/65/24 | 22.0 / 21.5 / 20.8% | 평평 |
| 신고점 연속 | **첫돌파** | 20 | **35.0%** [18.1~56.7] | **42.9/30.8** |
| | 연속2+ | 39 | 20.5% | 0.0/27.6 |
| | 아님 | 321 | 21.2% | — |

### 판정

1. **델타 변동성 가설(성장의 꾸준함) — 우위 없음.** 방향이 오히려
   반대 기미(꾸준 16.4% < 들쭉 25.9%)이나 구간이 전부 겹칩니다.
   "꾸준한 성장주가 유리"는 이 표본에서 지지되지 않습니다.
2. **연속 가속 — 기각.** 0회와 2회 이상이 사실상 같습니다 (22.0 vs
   20.8%). 가속이 여러 분기 이어져도 발표 후 우위는 없습니다.
3. **신고점 "첫" 돌파 — 정제 후보 (H2b 등록).** H2(신고점 전체
   25.4%)를 가르면: 첫 돌파 35.0%, 연속 돌파 20.5%. **처음 기록을
   깨는 발표만 새 정보**라는 기전과 부합하고, 앞/뒤 시기 모두 그
   시기 기준선을 웃돕니다. 단 n=20으로 구간이 기준선과 겹침 —
   **H2b: "TTM 신고점 첫 돌파(직전 발표는 신고점 아님)" 를 등록**하고
   새 데이터로 판정합니다.

## 남은 로드맵 (전략.md v2 7장)

5단계 유니버스 확장(50~100종목 — 로봇이 처리하므로 사람 대기 없음),
6단계 시즌 운영(분기마다 H2·H2b·H5 자동 재판정). 로봇이 매일 돌므로
개발 세션은 언제든 최신 데이터로 측정만 하면 됩니다.

---
---

# 7차 사전 등록 — 교차 검증 판정 (새 종목 데이터를 보기 **전에** 박제)

작성 시각: 2026-08-11, 확장 유니버스 수집 **이전**.

## 왜 이 방법인가

사용자 목표: "다음 실적 시즌을 기다리지 않고 미리 판정한다."
규율(5장 3조)은 탐색 표본 재사용을 금지하지만, **신호가 본 적 없는
종목의 과거**는 새 데이터입니다. H2·H2b·H5 는 기존 29종목에서
발견됐으므로, 새로 추가하는 종목들의 2021~2026 발표 사건은
교차(종목 방향) 검증 표본이 됩니다 — 오늘 바로 판정 가능.

⚠️ 한계를 미리 명시: 새 종목도 **같은 시장 장세**(2022 약세 · 2023~
강세)를 공유하므로, 장세가 만든 착시라면 여기서도 반복될 수 있습니다.
시간 방향 검증(미래 분기)은 이것과 **별개로 계속** 쌓습니다.

## 판정 규칙 (고정)

- **판정 표본**: 확장 유니버스 중 **신규 종목의 발표 사건만**
  (발견에 쓰인 29종목 — config.MEASURE_DISCOVERY_TICKERS — 은 제외).
- **가설과 문턱 (변경 없음)**:
  · H2 = TTM 신고점 돌파 · H2b = 신고점 **첫** 돌파(newhigh_streak==1)
  · H5 = 발표 시점 실적 폭 게이지 ≥ 20% (게이지는 전체 유니버스로 계산)
- **창·표적·폭등 정의**: 60거래일 · SPY 대비 +20%p (기존 그대로)
- **채택 기준**: 신호 그룹 윌슨 **하한** > 같은 표본 기준선②(모든 발표)
  윌슨 **상한** — 즉 신뢰구간 완전 분리. 아니면 미채택 유지.
- **판정 주체**: 수집 로봇이 매일 verdict.json 으로 자동 산출·커밋하고
  계기판이 표시한다 (6단계 — 사람 개입 없음).
- 신규 종목 표본에서도 앞/뒤 시간 분할을 함께 보고한다.

## 확장 유니버스 (+51종목, 제가 채택 — 성장·부진·횡보 균형)

반도체·장비(20): AVGO QCOM TXN ADI MCHP NXPI MRVL KLAC LRCX AMAT
TER ENTG MKSI FORM ACLS AMBA POWI DIOD SLAB MPWR
하드웨어·네트워크·광학(10): CIEN COMM EXTR NTAP PSTG WDC STX DELL
VIAV AAOI
소프트웨어(16): TWLO DDOG NET MDB SNOW OKTA ZS CRWD S HUBS BILL U
ROKU SNAP ZM DOCU
기타 성장·산업(5): TSLA TTD FSLR SEDG GNRC

선정 기준: ① 조정 EPS 를 보도자료로 발표할 가능성이 큰 업종
② 급등주(CRWD·AVGO·TSLA)와 급락·부진주(ZM·DOCU·SNAP·SEDG·MKSI·
ACLS 등)를 섞음 ③ 5년 이력 보유 (최근 상장·피인수 종목 제외).
조정 EPS 미발표로 판명되는 종목은 "없음" 처리되며 그대로 둔다.

## 7차 결과 — 첫 교차 판정 (로봇 자동 산출, 2026-08-11 09:57 UTC)

수집: 80종목 중 **78종목**에서 조정 EPS 시계열 확보 (일봉 79종목).
- 실패 2: COMM·PSTG — SEC 회사 조회 실패(CompanyNotFoundError) → "없음" 처리
- 조정 EPS 0건 9종목(ZETA·APP·TXN·AMBA·COMM·PSTG·ROKU·TSLA·FSLR) —
  조정 EPS 미발표 회사이거나 조회 실패. 창작하지 않고 그대로 둠.
- 판정 표본: **신규 종목 발표 사건 602건**(42종목) / 전체 참고 982건

### 판정 (verdict.json 에서 복사 — 채택 = 신호 하한 > 기준선 상한, n≥10)

| 가설 | 신호 (신규) | 기준선 (신규 전체) | 판정 |
|---|---|---|---|
| H2 TTM 신고점 | 23.0% [17.5, 29.6] n=183 | 18.4% [15.5, 21.7] n=602 | **미채택** (겹침) |
| H2b 신고점 첫돌파 | 27.1% [16.6, 41.0] n=48 | 18.4% [15.5, 21.7] | **미채택** (겹침) |
| H5 실적 폭 ON | 19.7% [16.3, 23.5] n=467 | 18.4% [15.5, 21.7] | **미채택** (아래 참조) |

시간 분할(신규 표본): H2 앞 19.7% [11.9, 30.8] / 뒤 24.8% [17.8, 33.3] ·
H2b 앞 26.1% [12.5, 46.5] / 뒤 28.0% [14.3, 47.6] — 뒤시기(강세장)가
높다는 국면 지배 현상이 신규 표본에서도 반복됨.

### H5 의 구조적 문제 발견 (중요)

80종목 유니버스로 계산한 실적 폭 게이지는 시계열 198주(2022-11~2026-08)
동안 **한 번도 20% 아래로 내려가지 않았다** (최소 34.2%, 최대 67.5%).
즉 사전 등록한 문턱 20%는 이 유니버스에서 **항상 ON** 이라 조건이
변별력을 잃는다. 5차의 ON 30.7% vs OFF 12.3% 분리는 29종목 게이지에서
측정된 것으로, **게이지 문턱은 유니버스 구성이 바뀌면 이식되지 않는다.**
게이지 시계열 시작(2022-11) 이전 사건 135건은 14.1% [9.2, 20.9]로 낮았다
(2021~22 약세 국면과 겹침 — 게이지 무관하게 국면 효과일 수 있음).

문턱 재보정(예: 백분위 기준)은 지금 고치면 사후 조정이 되므로 하지 않는다.
재보정안은 **새 후보로 다음 사전 등록을 거친 뒤** 시간 방향 데이터로만
판정한다.

### 해석 (숫자가 말하는 것만)

1. **채택된 신호는 없다.** 세 후보 모두 신뢰구간 완전 분리에 실패.
2. 방향은 일관: H2 +4.6%p, H2b +8.7%p — 발견 표본(H2b 35.0%)보다
   줄었지만(평균 회귀) 기준선 위는 유지. H2b가 가장 유망하나 n=48로
   구간이 넓어 판정 불가 — **표본이 쌓이면 로봇이 자동 재판정한다.**
3. 계기판에는 "채택된 신호 없음"이 그대로 표시된다. 없는 우위를
   있는 것처럼 만들지 않는다.


---

# 8차 — 측정 장치 정합성·신뢰성 감사 (2026-08-11, 코드 검증)

가설 판정이 아니라 **장치 자체를 검증**한 라운드. 모든 수치는 실행
출력에서 복사.

## A. 통계 도구 (윌슨 구간) — 합격

- 독립 구현(Decimal 고정밀)과 대조: 최대 오차 **0.000903%p**
- 거울 대칭 위반 0건
- 난수 모의실험 커버리지(참값 포함율): **92.1~96.6%** (95% 목표의 정상 진동)

## B. 사건 982건 전수 독립 재계산 — 합격

원시 배열에서 진입일·창·초과수익을 다시 계산해 대조:

- 선견(진입일 ≤ 발표일): **0건** · 창 60거래일 미확보: **0건**
- 초과수익 불일치: **1/982** (SNDK 2025-02-03, 2.82%p) — 분사 직후 거래일
  어긋남 사례로, SPY 비교 구간을 종목의 실제 보유일에 맞추는 장치 방식이
  정확함. 결함 아님.
- 발표 수지 완결: **1,362 = 사건 982 + 우측검열 59 + 중복 1 + 조정EPS없음 320**
- 분할 오염 없음 (하루 ±40% 초과 13건 모두 실적발 실제 급등락, ±100%급 없음)
- 같은 종목 55일 미만 간격 쌍 2건(STRL 42일·ONTO 53일) — 연간보고+1분기
  보고가 붙은 정상 사례, 창 겹침은 감수

## C. 발견된 결함 2건 — 수정 완료

**결함 1 — 신고점 망각** (측정·게이지·앱 3곳 공통):
이력이 끊기면(eps_runs) 끊긴 구간 안에서만 TTM 최고를 비교해, **옛 정점보다
낮은 값이 '신고점'** 이 되고 있었다. 문서 정의("수집 기간 안의 최고")와
구현이 어긋난 것. 실데이터에서 깃발 27건이 갈림 (장치→전체이력:
True→False 18 + False→True 9 · 8종목: AVGO EXTR LITE NXPI SEDG STX SYNA TER).
→ 구간이 끊겨도 TTM 정점을 기억하도록 수정 (measurement·breadth·app).

**결함 2 — 가짜 TTM 이어붙임** (status.is_ttm_new_high, 잠재):
결측 분기를 건너뛰어 4분기 합을 이어붙일 수 있는 구조 (예: [1,1,1,1,결측,10]
→ 가짜 TTM 13 이 '돌파'). 실호출 경로에서는 미발현이었으나 수정
(segment_ttms — 진짜 4분기 합만).

**수정 전후 판정 (신규 표본) — 결론 불변, 전부 미채택:**

| 가설 | 수정 전 | 수정 후 |
|---|---|---|
| H2 | 23.0% [17.5, 29.6] n=183 | 21.6% [16.2, 28.2] n=176 |
| H2b | 27.1% [16.6, 41.0] n=48 | 27.3% [16.3, 41.8] n=44 |
| H5 | 19.7% [16.3, 23.5] n=467 | 19.7% [16.3, 23.5] n=467 |

게이지도 재계산: 최대 **67.5→54.8%** (가짜 신고점이 폭을 부풀리고 있었음),
최소 34.1% — 여전히 20% 미만 주 0/198 → 7차의 "H5 문턱 무변별" 소견 유지.
⚠️ 발견 시대 수치(5차 게이지 ON/OFF, 6차 H2b 35.0%)는 옛 깃발 기준 측정치로
장부 원기록을 유지하며, 이후 라운드는 수정된 깃발로 계산된다.

## D. 신뢰성 (강건성) — H2b 우위는 조건 의존적

등록 조건(60거래일·+20%p)에서 신호 27.1% vs 기준선 18.4% 였던 우위가:

| 조건 변경 | 신호 | 기준선 | 우위 |
|---|---|---|---|
| 창 40일 | 12.5% | 9.8% | +2.7%p 유지 |
| **창 80일** | 17.8% | 23.4% | **역전** |
| 문턱 15%p | 31.2% | 24.3% | +6.9%p 유지 |
| **문턱 25%p** | 12.5% | 13.6% | **역전** |
| 진입 1일 지연 | 20.8% | 17.9% | +2.9%p 로 축소 |

우위가 창·문턱·진입시점에 따라 뒤집히거나 쪼그라듦 — **미채택 판정의
타당성을 보강**하는 결과. 추가 소견: H2b 적중 13건은 11종목에 분산(몰빵
아님) · 연도별 기준선 2021년 0% ~ 2026년 45.6% (국면 지배 재확인) ·
발표 군집(같은 주 최대 28건, 연속 창 겹침 136쌍/982)으로 사건은 완전
독립이 아님 — 윌슨 구간은 실제보다 좁게 나올 수 있다.

## E. 검증 장치

새 회귀 시험 2개(정점 기억·이어붙임 금지)는 결함을 일부러 되살려
**빨간 불을 확인한 뒤** 복원. 전체 시험 167개 통과 (종료코드 0).

---

# 9차 — 계기판·파서 감사 (2026-08-11, 사용자 화면 제보에서 출발)

계기: CRDO 상세 차트에 분기가 1개만 표시됨 (사용자 스크린샷).
추적 결과 화면 1건 + 수집기 5건의 결함을 찾아 수정. 숫자는 실행 출력에서 복사.

## 발견·수정 6건

1. **차트가 마지막 연속 구간만 표시** — 중간 분기 추출이 실패한 종목
   (감사 결과 **23/80개**)은 차트가 1~2개 막대가 됨. → 전체 이력 표시,
   추출 실패 분기는 구멍으로 노출, 최신 가이던스는 별색 막대, 판정
   구간이 짧으면 이유를 화면에 명시.
2. **짝짓기: 예비 공지가 실적 발표를 밀어냄** — CRDO 26Q3 실물: 예비
   매출 공지(02-09, 이익 숫자 없음)가 분기 종료일에 더 가깝다고 분기를
   차지, 실적 발표(03-02)의 조정 EPS $1.07 이 통째로 버려짐.
   → 이익 숫자(조정EPS·영업이익·조정EBITDA)를 실은 발표가 항상 이김.
3. **조정 EBITDA 전달 누락** — 파서는 뽑는데 분기 행으로 복사되지 않아
   버려짐 (실행 불가능 위치의 구제 코드 — 루멘텀 사건 재발 유형).
   → 배관 연결 + 스냅샷 필드 추가 + **대체 잣대 도입**: 조정 EPS 미발표
   회사(ZETA·APP·ROKU·SNAP·U·TTD 등, 실패 원문에서 16건 확인)는
   계기판에서 회사 발표 조정 EBITDA 로 델타를 봄. 측정·판정(H2 등)은
   등록대로 조정 EPS 만 사용 (변경 없음).
4. **EPS 라벨 4형식 보강** — 실패 원문 130건 전수 파싱으로 확인:
   · SEDG "net diluted loss per share of $0.55" → **−0.55** (적자 어순)
   · STX "non-GAAP (loss) per share of $(0.22)" → **−0.22** (괄호 적자)
   · AMBA "earnings per diluted ordinary share of $0.13" (케이맨 어순,
     여백 패딩 포함 — 1년 전 값 0.11 이 아닌 이번 분기 0.13 을 집는지 검산)
   · MPWR 표제목 아래 Basic/Diluted 행 → Diluted **6.50**
   → 실패 코퍼스에서 조정 EPS 회수 **6건 → 13건** (AMD·FORM·MXL·SNDK 포함.
   SNDK $39.25 는 원문 검산 결과 실제 값).
5. **EBITDA 값 검사 2규칙** — ① $10만 미만은 오파싱(실물: FSLR 1.0)
   ② EBITDA > 매출이면 산수 모순 → 매출 쪽 기각(실물: ZETA 가이던스
   상향 폭 $33M 이 매출로 잡힘. 매출은 XBRL 로 대체되지만 EBITDA 는
   대체재가 없음).
6. **TSLA 자동 추출 차단** — 슬라이드 표가 과거→현재 열 순서라 첫 숫자
   규칙이 **1년 전 분기 값**을 최신으로 가져옴 (실물: 조정 EBITDA 3,401
   을 집음, 최신은 3,273). 열 방향 파서가 생기기 전까지 config 로 차단.
   "틀림"은 "없음"보다 위험하다.

## 유니버스 정정 (사명 변경 반영)

- COMM 제외: 2026-01 CommScope 사업 매각·사명 변경(VISN) — 실체 단절
- PSTG → P: Everpure 로 사명 변경, 같은 회사·같은 사업 (티커만 교체)
- 유니버스 79종목 = 발견 29 + 신규 50. 두 종목 모두 기존 판정 표본에
  사건 0건이라 7·8차 수치에는 영향 없음.

## 정직 기록

- 다중 에이전트 분류(워크플로 13배치)는 구조화 출력 한도로 **전멸**
  (58.6만 토큰 소모, 결과 0건). 결정적 전수 파싱으로 대체했고 그쪽이
  검증 가능해서 더 나았다.
- 파서 수정의 실효(스냅샷 회복 규모)는 **다음 로봇 실행에서 확인**된다
  — 새 분기 사건이 늘고 CRDO 26Q3 발표일이 02-09→03-02 로 정정되므로
  판정 표본이 커지고 verdict 숫자가 바뀔 수 있다 (규칙은 불변, 데이터
  정정). 미회수 11건(CGNX 다열 표·MU 가이던스 표·투자자 데크 등)과
  EBITDA 가이던스 미지원은 남은 한계.
- 검증 175개 통과 (신규 9개는 결함을 되살려 빨간 불 확인 후 복원).
  412px 실렌더 — 가로 넘침 없음, CRDO 17분기 이력 표시 확인.

## 9차 실효 확인 (2026-08-12 로봇 재수집, 커밋 b7fdfed — 숫자는 실행 출력에서 복사)

- **수집**: 79/79종목 전부 조정 EPS 시계열 확보 (수정 전 78/80).
  P(구 PSTG) 첫 수집 성공. 파싱 실패 원문 130→107건.
- **회수 규모** (adj_eps 분기): MPWR 0→21 · FORM 3→20 · SEDG 15→20 ·
  STX 18→22 · AMBA 0→6 · CRDO 14→18. 전체 조정 EPS 분기 1,160개.
- **CRDO 26Q3 완전 복구**: adj_eps 1.07 · 발표일 2026-03-02(정정) · 직접공시.
- **대체 잣대 유입**: adjusted_ebitda 분기 — ZETA 7 · APP 9 · ROKU 13.
  TSLA 0 (설계대로 차단). 마지막 구간 조각 종목 23→15개.
- **자동 판정** (표본 확대: 신규 602→665 · 전체 982→1,072):
  · H2 신고점: 22.2% [16.9, 28.5] (n=194) vs 기준선 18.9% [16.2, 22.1] — 미채택
  · H2b 첫돌파: **32.6% [20.9, 47.0]** (n=46) vs 18.9% [16.2, 22.1] — 미채택
    (하한 20.9 vs 상한 22.1 — 간격이 좁혀졌으나 아직 겹침. 규칙대로 관찰 계속)
  · H5 실적폭: 20.2% [17.0, 23.9] (n=520) vs 18.9% — 미채택
- 검증 175개 통과 (새 스냅샷 기준, 종료코드 0).

## 10차 — 장치 감사 (2026-08-13, v3 4단계 작업 중 발견. 숫자는 실행 출력에서 복사)

- **발견**: snapshot(2026-08-12 수집)의 조정 EPS 에 **202.0 이 정확히 30건**
  — FN 15개 분기 전부 · CLS 5 · AMD 4 · FORM 2 · TER · STRL · COHR · CGNX.
  GAAP EPS 202.0 도 23건. 별도로 MCHP 조정 EPS 31.0 이 세 해 연속(23·24·25
  회계연도 Q4), IPGP 31.0 — 분기 종료일 "March 31" 의 **31** 을 문 것.
  회사마다 달라야 할 값이 여러 회사에서 똑같다 = 장치 결함의 지문.
- **원인**: `find_eps_value` 가 이름 뒤 숫자를 훑을 때 소수점 없는 정수
  (날짜·연도류)를 거르지 않았다. 실제 EPS 는 보도자료에서 항상 소수점
  표기($1.66, $39.25)로 나온다.
- **수리**: "소수점 없는 숫자는 EPS 후보가 아니다" 규칙을 AMD 묶음표
  한정에서 **모든 EPS 후보**로 확대 (`sec_fundamentals.py`). 새 시험 2개는
  고치기 전 **빨간 불을 확인**한 뒤 채택 (31.0·202.0 재현 → 수리 후 통과).
- **2차 방어**: 새 데이터 계층(`dataset.py`)에 주당 금액 절대 상한 100 —
  검증된 정상 최대 39.25(SNDK) 의 2.5배 여유, 실측 오류 202.0 전부 차단.
  한계(정직 기록): 상한 아래의 오류(31.0)는 2차 방어로 못 잡는다 —
  그건 파서의 소수점 규칙(1차 방어)만 막는다.
- **판정 영향 주의**: 이 30건은 9차 판정 표본 안에 있었다. 202.0 은 TTM
  신고점을 영구히 가려 H2/H2b(신고점 계열) 판정을 왜곡할 수 있는 값이다.
- **실효 확인은 다음 로봇 실행에서**: 개발 환경은 SEC 차단이라 재수집
  불가. 다음 자동 수집 후 202.0·31.0 소멸을 확인해야 하고, 신고점 계열
  측정·재판정은 그 뒤의 데이터로만 의미가 있다 (규칙 불변, 데이터 정정).

---
---

# 11차 사전 등록 — v3 측정·판정 장치 (2026-08-13, 새 장치로 재기 **전에** 박제)

v3 5단계. 아래 규칙을 적어 머지한 **뒤에만** 측정 장치(`measure_engine.py`)와
자동 판정(`judge.py`)의 코드를 씁니다.

## 유지 (7차 등록에서 변경 없음 — 옮겨 적음)

- **창·표적·폭등**: 발표 **다음 거래일** 종가 진입 · 60거래일 보유 ·
  폭등 = SPY 대비 초과수익 +20%p (보조 +30%p)
- **기준선②** = 같은 표본의 모든 발표 사건
- **채택** = 신호 그룹 윌슨 95% **하한** > 같은 표본 기준선 윌슨 **상한**
  (구간 완전 분리), n≥10
- **판정 표본** = 신규 종목(발견 29종목 = config.MEASURE_DISCOVERY_TICKERS
  제외)의 발표 사건. 전체 표본은 참고로 병기. 앞/뒤 시간 분할(발표일순
  반분)을 항상 함께 보고
- **TTM** = 연속 4분기 합. 연속 = 인접 분기 간격 55~150일. 끊기면 구간을
  나누고, **정점은 구간이 끊겨도 기억**한다 (8차 감사)
- **H2** = TTM 조정 EPS 가 수집 이력 내 과거 정점을 넘은 발표
- **H2b** = 그 첫 돌파 (newhigh_streak == 1)
- **H5** = 발표 시점 실적 폭 게이지 ≥ 20% (기존 등록 그대로 관찰 유지.
  7차에서 확인된 한계 — 79종목에선 항상 ON — 도 그대로 병기)
- 중복 발표(같은 날짜 정정 공시)는 한 번만 센다. 발표일 없는 분기는
  사건이 아니되 TTM 정점 기억에는 참여한다

## 새 등록 ① — H5b (실적 폭 게이지 재보정: 유니버스 이식 가능한 정의)

- **게이지 값** (7차와 동일한 계산): 주간 격자에서, 최근 발표가 140일
  이내로 신선하고 판단 가능한 종목 중 **최근 발표가 TTM 신고점 상태였던
  종목의 비율**(%)
- **H5b ON** = 발표 시점의 게이지 값이 **그 시점 직전까지 쌓인 게이지
  이력의 중앙값을 초과** (자기 역사 대비 상대 위치 — 고정 문턱 20%와
  달리 유니버스 크기가 바뀌어도 이식된다)
- **워밍업**: 직전 게이지 이력이 52주 미만이면 그 사건은 판단 불가 —
  H5b·H6 표본에서 제외 (억지로 판정하지 않는다)
- **정직 명시**: 게이지 시계열 자체는 5차 관찰에서 이미 본 데이터다.
  따라서 H5b 의 채택 판정은 **신규 종목 표본(교차)과 앞으로 쌓이는
  시간 방향 데이터로만** 하고, 발견 29종목의 과거는 참고로만 둔다

## 새 등록 ② — H6 (결합: 헌법 0장의 두 층을 겹침)

- **H6** = H5b ON **그리고** H2b(첫 돌파) 인 발표 사건
- 판정 규칙·창·기준선은 위와 동일. 표본이 작을 것으로 예상되므로
  (과거 유사 조합 n=15~19) **판정을 서두르지 않는다** — n<10 이면
  "판정 불가"로 기록하고 기다린다

## 판정 주체와 기록

- 수집 로봇이 매일 수집 성공 시 `judge.py` 를 돌려
  `data/measure/verdict.json` 으로 자동 산출·커밋 (사람 개입 없음)
- 대상: H2 · H2b · H5 · H5b · H6 전부 (미채택 가설도 관찰 목록으로 계속)
- 지금 있는 verdict.json 은 v2 유물 — 새 장치의 첫 실행이 대체한다

## 등록 시점의 데이터 상태 (정직 기록)

- 이 등록 시점의 snapshot(2026-08-12)에는 10차 감사의 202.0 오염
  30건이 남아 있다. 등록 규칙은 데이터 정정과 무관하게 유효하며(규칙
  불변), **보고에 쓸 판정 수치는 다음 로봇 재수집(깨끗한 데이터) 이후의
  verdict.json 에서 복사**한다. 개발 중 현재 snapshot 으로 돌린 수치는
  장치 작동 확인용일 뿐 판정으로 인용하지 않는다

## 11차 장치 첫 실행 확인 (2026-08-13, 숫자는 실행 출력에서 복사)

새 장치(dataset → measure_engine → judge)가 현재 snapshot 전체에서
끝까지 돎을 확인했다: 사건 1,044건 · 우측검열 62건 · 0.5초.

⚠️ **이 실행의 판정 수치는 인용하지 않는다** (11차 등록의 명시 조항).
현재 snapshot 에는 10차 감사의 오염이 남아 있다 — 202.0 류는 데이터
계층 상한이 걸러냈지만 31.0 류(상한 아래 오류)는 파서 수정 후
재수집되어야 사라진다. **정식 수치는 다음 로봇 실행의 verdict.json 에서
복사한다.** 여기 적는 것은 장치가 작동한다는 사실뿐이다.

## 10차 후속 — 열 방향 표 파서 추가 (2026-08-13, 숫자는 실행 출력에서 복사)

- **B유형(문구는 있는데 값 못 뽑음) 15건의 전수 원인 분류**:
  열 제목이 여러 줄에 쌓인 위치 정렬 표(CGNX 2건) · 값이 애초에 없는
  문서(파트너십·M&A·이미지 데크·가이던스 갱신 공지 등 대부분) ·
  다른 표 유형(P/Everpure — 논갭 선언이 표 제목에만 있는 형식, 2건).
- **수리**: 열 방향 표 파서(`find_eps_in_column_table`) 신설 — 열 제목을
  세로로 쌓아 "Non-GAAP … per share" 열을 찾고 "Current quarter" 행의
  같은 순번 칸을 읽는다. 확신 조건 4개를 전부 만족할 때만 값을 내고
  아니면 "없음" (틀림보다 없음). 실물 CGNX 원문으로 빨간 불 확인 후 채택.
- **효과 실측**: 실패 원문 137건 중 파싱 성공 13 → **15건**
  (CGNX 23Q4 0.27/0.32 · 23Q3 0.16/0.11 회수). 이상값 유입 0건.
- **남은 한계 (정직 기록)**: P/Everpure형(표 제목이 논갭 선언) 2건 ·
  MU형(가이던스 갱신 공지 — 실적 아님이라 회수 대상 아님) ·
  LITE형(이미지 슬라이드 — 텍스트 없음, 회수 불가). 실효(스냅샷 회복
  규모)는 다음 로봇 실행에서 확인된다.

---
---

# 12차 사전 등록 — 잣대 사다리 (2026-08-13, 사다리 측정 코드 작성 **전에** 박제)

배경: 조정 EPS 를 발표하지 않는 종목이 6개(ZETA·APP·ROKU·FSLR·TXN·TSLA),
조정 EBITDA 만 내는 회사가 다수. 이들을 측정에 넣기 위한 규칙.

## 사다리 규칙 (고정)

- 종목의 측정 잣대는 **하나로 고정**하며 아래 순서로 자동 배정한다:
  ① 조정 EPS 분기 ≥ 8개 → **조정 EPS**
  ② 아니고 조정 EBITDA 분기 ≥ 8개 → **조정 EBITDA**
  ③ 아니고 GAAP EPS 분기 ≥ 8개 → **GAAP EPS** (XBRL 구조화 + 8-K)
  ④ 전부 미달 → 그 종목은 측정 제외 (없음으로 둔다)
- 배정은 매 실행 시점의 보유 분기 수로 자동 재계산한다.
  한 종목 안에서 잣대를 절대 섞지 않는다 (끊긴 이력 잇기 사고 방지).
- 문턱 8분기의 근거: TTM 형성에 4분기 + 신고점 비교가 가능해지는 5분기째
  이후로 최소한의 판단 횟수를 확보하는 **커버리지 기준**이다.
  폭등률(결과)을 보고 정한 값이 아니다.

## 기존 가설 불변

- H2 · H2b · H5 · H5b · H6 는 **조정 EPS 잣대 종목만** 대상 (11차 그대로).
- 게이지(H5·H5b) 정의도 불변 — 사다리 종목을 게이지 분모에 넣는 재정의는
  하지 않는다 (하려면 새 사전 등록).

## 새 가설 (관찰 목록에 추가)

- **H7_EBITDA_첫돌파**: 잣대가 조정 EBITDA 인 종목의 TTM 조정 EBITDA
  신고점 **첫** 돌파. 창 60거래일 · +20%p · 기준선 = **같은 잣대 그룹**의
  모든 발표 · 윌슨 분리 · n<10 판정 불가 — 전부 11차와 동일.
- **H8_GAAPEPS_첫돌파**: 잣대가 GAAP EPS 인 종목의 TTM GAAP EPS 신고점
  첫 돌파. 규칙 동일.
- 두 그룹 모두 표본이 작을 것으로 예상 — 억지 판정 없이 기다린다.

## 함께 기록 (같은 날 수집 인프라 사고)

- 로봇 push 거부 사고: 로봇 실행 30분 사이 main 에 PR 머지가 들어와
  수집 17개 파일 커밋이 push 거부로 버려짐 (2026-08-13 08:36 실행).
  워크플로에 "push 실패 시 rebase 후 재시도(3회)"를 추가해 수리.

## 10차 후속 2 — 날짜 열 표 파서, TSLA 부분 해제 (2026-08-13)

- 실물 TSLA 2026-07-22 원문으로 확인: 슬라이드 표가 한 줄로 눌리며
  "Q2-2025 … Q2-2026 [행이름] 값×5 YoY" 형태. 열 순서가 과거→현재라
  일반 파서는 1년 전 값을 문다 (사고 9의 원인 형식).
- 수리: 날짜 열 파서(find_eps_in_date_column_table) — 분기 표기 3개
  이상의 열 제목을 찾아 **가장 최신 분기 열**을 고름 (열 순서 무관).
  실물로 빨간 불 확인 후 채택 (논갭 0.33 · GAAP 0.32 정확).
- PRESS_PARSE_SKIP 의미 변경: 전체 건너뛰기 → **EPS 전용 부분 파싱**
  (매출 등 다른 항목은 첫 열을 물 위험이 남아 계속 차단).
- 실효(TSLA 조정 EPS 시계열 회수)는 다음 로봇 실행에서 확인.

## 13차 실효 확인 — 파서 수리·사다리 전체 가동 (2026-08-13 로봇 2회 재수집, 숫자는 실행 출력에서 복사)

### 오염 소멸 (10차 감사의 실효)

- 202.0 오염 30건 → **0건**. 정수 물기 의심 76건 → **5건**.
- 가짜가 진짜로: FN 26Q3 202.0→3.72 · MCHP 25Q4 31.0→1.31 ·
  CGNX 23Q4 열 표 파서로 0.11 회수 · CRDO 26Q3 1.07 유지.
- **TSLA 조정 EPS 13분기 첫 회수** (날짜 열 파서 + 부분 파싱 —
  사고 9 차단 이후 처음으로 사다리 1순위 편입).
- 새 유입: 매출 가이던스 46분기 · EBITDA 가이던스 20분기 ·
  XBRL GAAP EPS (근사 행 284개에 실림, FSLR 20분기 전부).
- 로봇 push 경합 수리 검증: PR 머지 위에 rebase 후 정상 커밋 2회.

### 판정 (verdict.json 에서 복사 — 신규 표본 762건, 전체 1,177건)

- H2 신고점: 미채택 — 21.3% [16.2, 27.6] (n=197) vs 19.4% [16.6, 22.7]
- H2b 첫돌파: 미채택 — 25.0% [14.9, 38.8] (n=48) vs 19.4%
- H5 고정 20%: 미채택 — 항상 ON (변별력 없음 재확인)
- **H5b 시장 게이지: 채택** — 43.0% [33.1, 53.6] (n=86) vs 23.0%
  [19.0, 27.4]. ⚠️ 단 앞시기 신호 0건(워밍업 특성) — 우위가 전부
  뒤시기(강세장)에서 나옴. 헌법 "강세장 구간만의 우위는 믿지 않는다"
  에 해당하므로 **확정 지식 승격은 앞시기 표본이 쌓인 뒤에만**.
- H6 결합: 판정 불가 — 44.4% (n=9, 사다리 도입으로 표본 구성 변경)
- H7 EBITDA 첫돌파: 판정 불가 — n=2 (기준선 그룹 46건, 축적 시작)
- H8 GAAP EPS 첫돌파: 판정 불가 — 50.0% (n=6, 기준선 그룹 73건)

### 남은 의심 목록 (다음 감사 과제)

- TXN: XBRL GAAP EPS 0건 (다른 종목은 284행 성공 — TXN 특이 원인 미상)
- 잔여 정수 의심 5건: TTMI 26Q2 5.0 · QRVO 26Q4/26Q1 7.0 ·
  TER 21Q4 8.0 · MKSI 25Q4 8.0 — "$7.0" 처럼 소수점이 있어 새 규칙을
  통과한 부류. 원문 확보 후 개별 감사 필요.

## 14차 — 구멍 분기 원문 감사 (2026-08-13, 숫자는 실행 출력에서 복사)

구멍(마지막 연속 구간 4분기 미만) 종목 18개 전수 조사. 원문 보유 13건 감사.

### 판명된 원인과 조치

- **합병 발표 오인**: SWKS·QRVO 2025-10-28 원문은 실적이 아니라 두 회사의
  **합병 발표문** ("accretive to non-GAAP EPS ... $500 million"). 실적으로
  오인되어 원문 보관함(종목당 2건)을 차지 → 진짜 실패 원문이 못 담김.
  ACLS 2025-10-01 인수 발표의 EBITDA $387M 유령 행도 같은 부류.
  → **수리**: 실적 신호 없이 합병 신호만 있는 문서는 실적 아님 판정
  (_looks_like_earnings 게이트, 실물로 빨간 불 확인).
- **예비 공지**: TTD 2026-01·ONTO 2026-04·DIOD 2026-07·MDB 2025-11 원문은
  숫자 없는 예비 발표 — 값 없음이 정답. 잘림 아님(원문 끝까지 확인).
- **원문 미보유 구멍**: COHR·SLAB·STX·CRWD 등의 진짜 실패 원문은 보관
  상한에 밀려 없음 — 합병 게이트 수리로 다음 실행부터 슬롯이 확보됨.
- **WDC 다열 표** (실물 2025-04-07): 날짜 헤더가 행과 떨어져 있는 또 다른
  표 유형 — 다음 파서 과제로 기록.

### 잔여 정수 5건 (TTMI 5.0 등) — 원문 확보 장치 설치

출처 원문이 보관되지 않아 감사 불가(수집 성공으로 분류됐던 값).
percent·million 류 가설은 실험으로 기각 (기존 가드가 이미 막음 — 실행 확인).
→ **진단 장치**: 소수점 규칙을 통과한 정수 EPS(|값|≥5)가 나오면 값은
그대로 두되 원문을 자동 보관하도록 로봇에 설치. 다음 실행 후 실물 감사.
TTMI 는 이 가짜 행이 연속성까지 끊고 있음을 확인 (구멍의 실제 원인).

---
---

# 15차 탐색 — "시장 ON × 주도 섹터" (2026-08-13, 숫자는 실행 출력에서 복사)

저장소 주인 가설: "시장 장세가 ON일 때 주도 섹터의 수익률이 월등하다."
탐색 정의(둘 다 H5b와 같은 규칙): 시장 ON = 시장 게이지 > 자기 이력
중앙값 · 섹터 주도 = 그 섹터 게이지 > 자기 이력 중앙값 (워밍업 52주).
표본: 신규 종목 사건 중 둘 다 판단 가능한 452건. ⚠️ **탐색치 — 채택
근거 아님** (5장 규율). 판정하려면 새 H번호로 등록 후 새 데이터로만.

| 그룹 | 폭등률 | 앞시기 | 뒤시기 |
|---|---|---|---|
| 시장ON × 섹터주도 | 43.3% [32.1,55.2] n=67 | **60.6%** n=33 | 26.5% n=34 |
| 시장ON × 섹터비주도 | 43.8% [28.2,60.7] n=32 | 25.0% n=16 | 62.5% n=16 |
| 시장OFF × 섹터주도 | 19.6% n=107 | 9.4% | 29.6% |
| 시장OFF × 섹터비주도 | 17.1% n=246 | 16.3% | 17.9% |
| 기준선(전체) | 23.5% [19.8,27.6] n=452 | 12.8% | 34.1% |
| [참고] ON×주도×첫신기록 | 66.7% n=6 | — | — |

## 정직한 해석

1. **시장 ON 자체가 크다**: ON 그룹(43%대)이 기준선(23.5%)의 거의 2배 —
   가설의 "시장 ON" 부분은 이 표본에서 강하게 성립.
2. **ON 안에서 '섹터 주도'의 추가 우위는 평균적으로 안 보임**
   (43.3% vs 43.8%). 다만 시기별로 정반대로 갈림: 주도는 앞시기가
   압도적(60.6%), 비주도는 뒤시기가 좋음 — 이 정의로는 후보 자격
   (앞/뒤 모두 우세)을 **못 넘는다**.
3. 삼중 겹침(ON×주도×첫신기록) 66.7%는 눈에 띄나 n=6 — 판정 불가 영역.
4. 한계: 섹터 게이지는 종목 수가 적어(6~22) 계단 잡음이 크고, 같은
   시기 사건들은 장세를 공유(군집)한다. '주도'의 다른 정의(순위 상위
   등)는 우연 승자 위험 때문에 이번에 재지 않았다 — 정의 하나만 쟀음.

## 다음 단계 (등록 없이는 판정 없음)

- 유니버스 확장(신규 종목의 과거 = 교차 새 데이터)이 끝나면 같은 탐색을
  확장 표본에서 1회 재실행해 안정성만 확인.
- 앞/뒤 모두 우세가 확인되는 정의가 나오면 그때 새 H번호로 사전 등록.

## 16차 — 유니버스 2차 확장 +27종목 (2026-08-13, 데이터 수집 전에 명단 박제)

섹터 표본 확충 (인터넷 6→15 · 전력에너지 6→15 · 소프트웨어 15→21 ·
하드웨어 17→20). 총 79 → **106종목**. 후보 27개 전부를 병렬 웹조사
(에이전트 4개, 실패 0)로 ① 2021-08 이전 상장 ② 조정지표 발표 여부를
검증한 뒤 편입 — 조정 EPS 18 · 조정 EBITDA 4 (ETSY·ABNB·DASH·VST) ·
GAAP만 5 (GOOGL·META·AMZN·NFLX·RUN → 사다리 GAAP 잣대 예정).

- 인터넷: GOOGL META AMZN NFLX PINS EBAY ETSY ABNB DASH
- 전력에너지: VRT ETN PWR VST NRG NEE SO RUN BE
- 소프트웨어: ADBE CRM NOW ORCL TEAM WDAY
- 하드웨어: CSCO HPQ HPE

원칙 확인: 신규 종목의 과거 = 신호가 본 적 없는 **교차 방향 새 데이터**
(7차 등록 논리). ⚠️ 같은 장세를 공유하므로 시간 방향 검증은 별개로 계속.
발견 29종목 목록(MEASURE_DISCOVERY_TICKERS)은 불변. 성장·부진 혼합:
급등(NOW·VRT·ABNB 등)과 부진·횡보(PINS·ETSY·RUN·HPQ·SO 등)를 섞음.

## 17차 — 의심 정수 감사 완료 + 확장 표본 재판정 (2026-08-13, 숫자는 실행 출력에서 복사)

### 의심 정수 4건 — 원문 확보로 전원 판명 (사고 백서 16번)

진단 장치가 확보한 실물 원문으로 확정: 전부 **분기 실적이 아닌 값**.
- QRVO 7.0 = "expect … non-GAAP diluted EPS **approaching $7.00**" (연간 전망)
- TER 8.0 = "**2024 earnings model** to … $8.00" (중기 목표)
- TTMI 5.0 = "to **approach** $5.00" (연간 전망 — 표의 진짜 분기 값 $0.99 존재)
- MKSI 8.00 = 이미지 슬라이드의 **차트 값** 나열
수리(전망 문맥 가드 + 슬라이드 줄 제외) 후 재파싱: QRVO **$1.69** ·
TER **$0.76** · TTMI **$0.99** (진짜 분기 값 회수) · MKSI 없음(정답).
실데이터 반영은 다음 로봇 실행에서.

### ⚠️ H5b 채택 → 미채택 (교차 새 데이터의 심판)

유니버스 확장(+27종목의 과거 = 신호가 본 적 없는 교차 표본)을 넣자
아침의 채택이 **하루 만에 뒤집혔다**:
- 아침(79종목): 43.0% [33.1, 53.6] vs 20.6~23.0 → 채택
- 확장(106종목): **27.4% [21.4, 34.5] (n=175) vs 20.6% [17.4, 24.3] — 겹침 → 미채택**
대형주(GOOGL·NEE 류) 편입으로 +20%p 폭등 자체가 드물어져 신호·기준선이
함께 내려앉았고 구간이 겹쳤다. **아침 채택을 확정 지식으로 승격하지
않은 판단이 옳았음을 데이터가 확인해 준 사례.** H5b 는 관찰 목록으로.

### 15차 탐색의 확장 재실행 (사건 671건 판단 가능)

- 시장ON×섹터주도 26.8% [19.8,35.3] vs ON×비주도 25.8% — 섹터 주도의
  추가 우위 이번에도 없음. 시간 분할은 표본 구성에 따라 앞/뒤가
  뒤집힘(79종목: 앞 60.6 우세 → 106종목: 뒤 38.7 우세) — 불안정.
- 시장 ON 자체는 유지되나 축소 (26~27% vs 기준선 19.8%).
- ON×주도×첫신기록 44.4% (n=9) — 계속 관찰 (판정 불가 영역).
- 결론: 현재 정의(섹터 게이지>자기 중앙값)는 등록 후보 요건(앞/뒤 모두
  우세) 미달. 유망 관찰은 삼중 겹침뿐 — 표본 축적 대기.

### 신규 27종목 수집 성적

24/27 잣대 배정 (조정EPS 15 · EBITDA 2 · GAAP 5 등), 수집 오류 0.
미달 3(NFLX·VST·RUN — 조정지표 이력 부족, 측정 제외가 정답).
매출 가이던스 56분기로 증가. 전망 델타의 이상쌍(+1650% 1건)은
가이던스-실적 짝 정합 검사 필요 — 다음 과제.

## 18차 — APP 백테스트 · 매출 가속 탐색 · 사이클 테마 도입 (2026-08-13, 숫자는 실행 출력에서 복사)

### APP 1,000% 상승(2023-01 $10 → 2024-12 $402 → 정점 $510) 사후 분석

- **우리 파이프라인의 잣대(EBITDA)로는 포착 불가였다** — 2022~2024 APP
  8-K 파싱 실패로 발표일·EBITDA가 통째로 비어, 첫돌파가 2026-02에야 점등
  (2년 늦음). 원문은 보관 상한에 밀려 없음 — 회수는 파서 개선 과제.
- **XBRL 매출 이력은 온전**했고, 그것으로 재구성하면: 2023Q4 매출
  QoQ **+34% 대가속**(713→953M)이 주가 ~$47에서 점등 → 이후 TTM YoY
  25→36→48% 확장, 정점까지 ~10배. 단 매출 TTM "신고점"은 2023-06부터
  내내 켜져 있었음(성장주는 항상 신고점 — 변별력 없음).
- 오염 발견: APP 매출 3건이 3.55달러 같은 자릿수 붕괴 → 데이터 계층에
  매출 하한(1만 달러) 가드 신설 (실물 시험 포함).

### 매출 QoQ 가속의 전 종목 일반화 — 우위 없음 (⚠️ 문턱은 APP 사후값)

신규 표본 1,070건: QoQ ≥+15% 18.4% [12.9,25.6] · ≥+25% 17.5% ·
≥+34% 20.7% [12.3,32.8] vs 기준선 16.6% [14.5,19.0] — 전부 겹침,
시간 분할 어느 쪽도 분리 안 됨. **APP 사례는 승자를 골라 되돌아본
n=1 이야기였고, 같은 신호가 전 종목에선 평범했다** — "가속 확인 후
매수 기각"(확정 지식 1)이 매출 축에서도 재확인. 등록하지 않는다.

### 사이클 테마 분류 도입 (저장소 주인 요청)

업종(SECTORS)과 별개로 **현재 사이클 기준 테마**(config.THEMES)를 도입:
AI-GPU가속기·AI-메모리·AI-광통신네트워크·AI-데이터센터전력·
AI-반도체장비·AI-소프트웨어클라우드 / 비AI-범용반도체·소비인터넷·
업무SW·태양광EV·PC프린터. 표시·관찰용(측정 미사용), 사이클이 바뀌면
재정의. 분류는 사람 판단임을 화면에 명시.

## 19차 — 유니버스 3차 확장 +54종목 (2026-08-13, 데이터 수집 전에 명단 박제)

비기술 업종 편입 (총 106 → **160종목**). 54종목 전부 병렬 웹조사
(6배치, 실패 0)로 ① 2021-08 이전 상장 ② 조정지표 발표 여부 검증 —
조정 EPS 43 · GAAP만 10 (JPM·BAC·WFC·C·GS·MS·AXP 은행 + COST·NKE·DE
→ 사다리 GAAP 잣대) · 조정 EBITDA 1 (TMUS).

- 금융(12): JPM BAC WFC C GS MS SCHW BLK V MA AXP COF
- 헬스케어(12): LLY UNH JNJ ABBV MRK PFE AMGN GILD BMY REGN VRTX ISRG
- 소비재(12): WMT COST PG KO PEP MCD SBUX NKE HD LOW TGT EL
- 산업재(8): CAT DE HON GE UNP UPS FDX BA
- 통신미디어(5): T VZ TMUS CMCSA DIS
- 석유가스(5): XOM CVX COP SLB OXY

⚠️ 새 업종의 보도자료 문법(comparable/core EPS, 은행 서식, notable
items 등)은 파서가 처음 만나는 형식 — **첫 수집 후 오염 감사
(정수·자릿수·합병 게이트·의심 원문)를 반드시 재실행**한다 (실패
기록 감안 원칙). 신규 종목의 과거 = 교차 새 데이터, 같은 장세 공유
한계 병기. 로봇 시간 한도 180분으로 상향.

## 20차 탐색 — APP 전 지표 백테스트 (2026-08-13, 숫자는 실행 출력에서 복사)

### 지표별 포착 시점 (APP 내부 걸음)

- **가장 이른 신호 = XBRL 영업이익의 마진 연쇄 확장**: 매출 정체 속
  13%→20%→28%→42%→51% (2023-03~2023-12 분기), 발표 시점 주가
  $23→$40→$43→$47 (정점 $510의 5~9%). 이 데이터는 당시에도 파이프라인에
  구멍 없이 존재 — "볼 수 있었던" 신호.
- 매출 QoQ +34% 는 6개월 늦은 2023Q4(주가 $47). 조정 EPS·EBITDA·
  GAAP EPS 는 그 구간 수집 구멍/미발표로 무용.

### 전 종목 일반화 (신규 표본 882건 · ⚠️ 문턱은 APP 사후값)

- 마진 YoY +10%p: 17.2% · +15%p: 19.2% · +20%p: 21.8% [14.1,32.2]
  vs 기준선 18.5% [16.1,21.2] — 전부 겹침
- APP 패턴(+10%p 확장 2분기 연속): **15.8% — 기준선 이하**
- 앞시기(약세·정상장)에서는 전 문턱이 더 약함

### 결론 (기록)

APP 의 마진 폭발은 사후 식별로만 완벽해 보이는 신호 — 같은 신호를
가진 종목 전체를 사면 기준선 수준. "개별 확인 신호 기각·장세 지배"가
영업이익 축(4번째)에서 재확인됨. 등록하지 않는다. 유망 방향은 결합
조건(H6 류)의 표본 축적뿐.

---
---

# 21차 — 주봉 이평선 탐색 + **H9 사전 등록** (2026-08-13 22:5x UTC, 3차 확장 54종목 데이터 도착 **전에** 박제)

저장소 주인 요청: 주봉 4·13·26·52주 이동평균을 판단에 합류.
탐색 표본: 신규 종목 사건 954건 (이력 52주 미만은 판단 불가 제외).
숫자는 실행 출력에서 복사.

## 탐색 결과 (전부 기록)

| 상태 | 폭등률 | 앞시기 | 뒤시기 |
|---|---|---|---|
| 완전 정배열(주가>4>13>26>52) | 15.8% n=190 | 6.3% | 25.3% |
| 주가 > 52주선 | 17.8% n=557 | 10.1% | 25.4% |
| 주가 < 52주선 | 19.4% n=397 | 18.7% | 20.1% |
| 완전 역배열 | 29.4% n=68 | 44.1% | 14.7% |
| **첫신기록 × 주가<52주선** | **33.3% [20.6,49.0] n=39** | **31.6%** | **35.0%** |
| 첫신기록 × 정배열 | 18.8% n=16 | 12.5% | 25.0% |
| 기준선(전체) | 18.4% [16.1,21.0] | 13.0% | 23.9% |

- 정배열·주가>52주선은 기준선 이하 — **"추세 확인 후 매수는 늦다"**
  (확정 지식 3·5)가 4·13·26·52 체계에서도 재확인.
- 완전 역배열 29.4%는 앞시기(약세장 바닥)에만 우세 — 앞뒤 반전, 후보 미달.
- **유일하게 앞/뒤 모두 우세: 첫신기록 × 주가<52주선** (31.6/35.0 vs
  13.0/23.9). 해석: 이익은 첫 신기록인데 주가는 아직 장기선 아래 =
  "가격이 아직 반영 못 한 새 정보" — 0장 질문의 순수형. 단 전체 구간은
  기준선과 아슬하게 겹침(하한 20.6 vs 상한 21.0) → 등록 후 판정.

## H9 사전 등록 (고정)

- **H9_저평가_첫신기록**: 발표 사건 중 ① 잣대 TTM **첫** 신고점
  (newhigh_streak==1) **그리고** ② 발표 시점 주봉 종가가 **52주 이동평균
  아래** (주봉 = 각 주 마지막 거래일 종가, 이동평균은 발표일까지의
  주봉 52개 평균. 이력 52주 미만이면 판단 불가 — H9 표본 제외).
- 창 60거래일 · +20%p · 기준선 = H9 판단 가능 사건 전체 · 윌슨 95%
  완전 분리 · n≥10 · 앞/뒤 시간 분할 병기 — 전부 11차와 동일.
- **판정 표본 (탐색 재사용 금지)**: ① 이 등록 시각 이후 발표(시간 방향)
  ② **3차 확장 54종목의 과거** — 이 탐색이 본 적 없는 교차 새 데이터
  (지금 로봇이 첫 수집 중, 이 등록은 그 도착 전에 박제됨).
  기존 신규 표본(2차 확장까지)은 탐색에 쓰였으므로 참고로만 병기.
- 판정 주체: 로봇 자동 (judge.py, verdict.json).

## 21차 부록 — 분기 영업이익 델타(% 상승폭) 탐색 (신규 1,035건)

- QoQ +30/50/100%: 18.3 / 17.5 / **14.7%** — 기준선(18.3%) 이하~동급,
  상승폭이 클수록 오히려 나쁨. 흑자전환 17.5% — 평범.
- YoY +100%: 24.1% [17.3,32.7] — 기준선 위지만 구간 겹침, 앞시기
  15.5 vs 13.9 사실상 동급 → 후보 미달 (신고점 H2와 겹치는 정보).
- "영업이익 델타 확인 후 매수"도 우위 없음 — 5번째 축 재확인. 기록만.

## 21차 부록 2 — 조정 EPS 델타(% 상승폭) 탐색 (신규 782건)

저장소 주인 요청("조정eps도 포함"): 부록 1(영업이익)과 같은 전지를
조정 EPS 에 적용. 숫자는 실행 출력에서 복사 (2026-08-13).

**표본 정의 (이 기록으로 고정)**: 신규 종목(발견 29종목 제외)의 발표
사건 중 ① 조정 EPS 값이 직전 **연속** 분기에도 있고(분기 구멍이면 제외)
② 60거래일 초과수익이 계산되는 사건 = 782건. QoQ % 는 직전 분기가
양수일 때만 계산(716건), 흑자전환 = 직전 음수 → 이번 양수.
YoY % 는 정확히 4분기 전 값이 양수일 때만.

※ 같은 날 앞서 돌린 1차 실행(표본 669건)은 스크립트가 보존되지 않아
표본 정의를 재현할 수 없었다. 그래서 정의를 위처럼 박제하고 다시
돌린 이 결과를 기록으로 삼는다. 두 실행의 결론은 같다(전 문턱
기준선과 구간 겹침).

| 신호 | 폭등률 [윌슨95%] | 앞시기 | 뒤시기 |
|---|---|---|---|
| QoQ +30%↑ | 21.5% [15.1,29.6] n=121 | 14.8% n=54 | 26.9% n=67 |
| QoQ +50%↑ | 19.7% [12.1,30.4] n=71 | 18.2% n=33 | 21.1% n=38 |
| QoQ +100%↑ | 17.9% [9.0,32.7] n=39 | 21.1% n=19 | 15.0% n=20 |
| YoY +50%↑ | 22.7% [15.5,32.0] n=97 | 13.6% n=44 | 30.2% n=53 |
| YoY +100%↑ | 20.4% [11.8,32.9] n=54 | 12.5% n=24 | 26.7% n=30 |
| 흑자전환(QoQ) | 16.7% [4.7,44.8] n=12 | 33.3% n=6 | 0.0% n=6 |
| 기준선(전체) | 17.8% [15.3,20.6] n=782 | 13.3% n=391 | 22.3% n=391 |

### 결론 (기록 — 등록하지 않음)

- **전 문턱이 기준선과 윌슨 구간 겹침.** 가장 좋은 YoY+50 도
  하한 15.5 < 기준선 상한 20.6 — 완전 분리 근처에도 못 감.
- 뒤시기 우세(QoQ+30, YoY+50/100)는 기준선 자체의 뒤시기 상승
  (13.3→22.3)과 같은 모양 — 장세 지배(원칙 4), 신호 고유 우위 아님.
- 상승폭이 클수록 오히려 나쁨(QoQ +30→+100: 21.5→17.9%) — 부록 1
  영업이익과 같은 무늬.
- 흑자전환은 앞 33.3/뒤 0.0 반전에 n=12 — 판단 근거 없음.
- "조정 EPS 델타 확인 후 매수"도 우위 없음 — 확정 지식 1(조정 EPS
  가속 3회 기각)의 **델타 % 판** 재확인. 남는 유망 방향은 여전히
  결합 조건(H6·H9 류)뿐.

---
---

# 22차 — 정배열 완성 신호, 긴 창(60/120/250거래일) 탐색 (2026-08-13)

저장소 주인 질문: "APP 는 정배열 완성 후 24년까지 1000% 상승 아니야?
기간을 길게 해서 비교해봐. 셀레스티카도 동일할 것." — 21차의 정배열
15.8%는 60거래일 창이라 긴 추세를 못 볼 수 있다는 지적. 숫자는 전부
실행 출력에서 복사.

**정의 (고정)**: 주봉 = 각 주 마지막 거래일 종가, 이동평균 4·13·26·52주.
완전 정배열 = 종가>4주>13주>26주>52주. **"완성" 사건** = 지난주는
아니었는데 이번 주에 정배열이 된 주봉 (전이). 진입 = 그 주봉 종가.
창이 데이터 끝을 넘는 사건은 제외(우측 검열). 문턱은 60일 +20p 에
비례해 120일 +40p·250일 +80p.

## 1) 개별 확인 — 저장소 주인 관찰은 사실

- **APP**: 첫 완성 2023-07-21 종가 28.10 → 24년말 323.83 = **+1052%**.
  2023~24년의 완성 8건 전부 250일 초과 +168~+714p.
  단 **2025-01 완성 2건(종가 ~350)은 60일 -13~-19p·120일 -1~-5p**,
  2025-10·12 완성 2건도 마이너스 — 같은 종목, 같은 신호, 다른 시기.
- **CLS**: 첫 완성 2023-07-14 종가 15.84 → 24년말 **+483%**
  (250일 +249p). 모양 동일.

## 2) 전 종목 같은 신호 — 긴 창에서도 기준선과 동급

| 창 | 완성 사건 | 기준선(모든 주봉) |
|---|---|---|
| 60일 (+20p) | 22.4% [20.0,24.9] n=1,118 · 중앙 +1.3p | 21.1% [20.6,21.7] n=20,651 · 중앙 +1.2p |
| 120일 (+40p) | 16.3% [14.2,18.7] n=1,029 · 중앙 +3.4p | 16.5% [16.0,17.1] n=19,367 · 중앙 +1.9p |
| 250일 (+80p) | 13.9% [11.7,16.4] n=827 · 중앙 +0.2p | 14.5% [14.0,15.0] n=16,461 · 중앙 +1.2p |

## 3) 결합 판 — 발표 사건에서 첫신기록 × 정배열, 긴 창

| 창 | 첫신기록×정배열 | 첫신기록×**비**정배열 | 기준선 |
|---|---|---|---|
| 120일 (+40p) | 6.7% n=15 · 중앙 -4.5p | 22.6% [14.0,34.4] n=62 · 중앙 +2.1p | 11.7% n=1,052 |
| 250일 (+80p) | **0.0% n=11** · 중앙 -8.3p | 16.1% [8.7,27.8] n=56 · 중앙 +8.2p | 8.9% n=925 |

## 결론 (기록 — 등록하지 않음)

- 관찰은 사실이나 **일반화가 안 된다**: 같은 신호를 전 종목에 걸면
  긴 창에서도 기준선과 동급(250일 13.9 vs 14.5). APP·CLS 는 사후에
  고른 생존자이고, APP 자신도 2025년 완성 사건들은 손실.
- 정배열은 "어느 종목이 가는가"를 알려주지 않는다. 이미 가는 종목을
  들고 있을 때의 **보유 근거**는 될 수 있으나, 그건 출구 규칙의
  문제이지 진입 신호가 아니다.
- 결합 판은 방향이 반대로 확인됨: 첫신기록이어도 **이미 정배열이면
  늦고**(250일 0/11), **아직 비정배열일 때**가 우세(중앙 +8.2p vs
  기준선 -5.9p) — H9(첫신기록×52주선 아래)와 같은 방향. 표본이
  차면 H9 판정이 이 방향을 공식 검증한다.

---
---

# 23차 — 잣대 3종 × 긴 창(60/250/500거래일) 비교 + **H10 사전 등록** (2026-08-13, 3차 확장 데이터 도착 전에 박제)

저장소 주인 요청: "논갭영업이익·에비타·조정eps로 비교해서 최소 2년
정도로 거래일 창을 늘려 데이터분석." 숫자는 실행 출력에서 복사.

**정의 (고정)**: 잣대별로 값이 8분기 이상인 신규 종목. 신호 = 그 잣대
TTM **첫** 신기록. 기준선 = 같은 잣대 그룹 전 발표. 진입 = 발표 다음
거래일. 창 60/250/500거래일, 문턱 +20p/+80p/+160p (60일에 비례).
우측 검열 제외 → 500일 창은 약 2024-08 이전 발표만 남음(표본 축소).
논갭 영업이익 출처 구성: 직접공시 1,124 · 역산 155 · 근사치(GAAP 대용) 704.

## 결과 (전부 기록)

**논갭 영업이익** (첫 신기록 / ×52주선아래 / 기준선):
- 60일: 23.1% n=104 / **34.7% [22.9,48.7] n=49 · 앞46/뒤24** / 15.7% [13.8,17.8] n=1,297 · 앞12/뒤20
- 250일: 10.0% · 중앙 +5.0p / 12.5% · 중앙 +10.5p / 9.2% · 중앙 -5.3p
- 500일: 11.4% · 중앙 +9.0p / 7.1% / 8.3% · 중앙 -10.9p

**조정 EBITDA**: 신호 n=9~13 — 전 창 판단 불가 수준. 기록만.

**조정 EPS** (첫 신기록 / ×52주선아래 / 기준선):
- 60일: 23.2% n=69 / 31.4% n=35 / 17.0% n=888
- 250일: 17.9% [10.0,29.8] / 20.0% · **중앙 +26.3p** / 9.2% [7.3,11.5] · 중앙 -5.1p
- 500일: 5.0% · 중앙 +6.5p / 4.2% · 중앙 +10.0p / 8.2% · 중앙 -11.0p

## 읽기 (정직)

- **창을 2년으로 늘리면 기준선의 중앙값이 점점 나빠진다**(-1.3 →
  -5.3 → -10.9p): 아무 발표나 사서 2년 들고 있으면 보통 SPY에 진다.
  첫 신기록 그룹만 전 창에서 중앙값이 플러스(논갭 영업이익 +2.5 →
  +5.0 → +9.0p) — **신호의 우위가 긴 창에서도 중앙값 기준으로 유지·확대**.
- 단 비례 문턱(+80p·+160p)의 "폭등률"로는 완전 분리가 없고, 조정 EPS
  500일은 문턱 기준 오히려 기준선 아래(5.0 vs 8.2%) — 문턱이 매우
  높아진 탓과 표본 축소가 겹침. 과신 금지.
- 잣대 비교: **조정 EPS ≈ 논갭 영업이익 > EBITDA(표본 부족)**.
  둘은 정보가 겹치되 상호 보완적이고, EBITDA는 아직 판단 불가.
- 유일한 완전 분리: **논갭 영업이익 첫신기록 × 52주선 아래, 60일**
  (하한 22.9 > 기준선 상한 17.8), 앞/뒤 모두 우세(46/24 vs 12/20).
  H9(잣대 사다리 판)와 같은 방향의 독립 확인 — 아래에 등록한다.

## H10 사전 등록 (고정)

- **H10_논갭영업이익_저평가_첫신기록**: 논갭 영업이익 값이 8분기 이상인
  종목의 발표 사건 중 ① 논갭 영업이익 TTM **첫** 신고점
  (newhigh_streak==1) ∧ ② 발표 시점 주봉 종가 < 52주 이동평균
  (H9와 동일 정의. 이력 52주 미만 판단 불가 — 표본 제외).
- 창 60거래일 · +20%p · 기준선 = H10 판단 가능 사건 전체(같은 잣대
  그룹) · 윌슨 95% 완전 분리 · n≥10 · 앞/뒤 병기 — 11차와 동일.
- **판정 표본 (탐색 재사용 금지)**: ① 이 등록 시각 이후 발표
  ② 3차 확장 54종목의 과거 (로봇 첫 수집이 아직 도착하지 않았음 —
  이 등록은 그 도착 전에 박제됨). 기존 표본은 참고로만 병기.
- 판정 주체: 로봇 자동 (judge.py — 구현은 등록 후 추가, 기준은 이 문서가 소유).

---
---

# 24차 — "정배열 시작 × 실적 델타" 검증 (2026-08-13, 숫자는 실행 출력에서 복사)

저장소 주인 가설 (APP 주봉 2023-08-05 정배열 시작 차트와 함께):
"이 시점 기준 실적지표 3종 + 같은 지표의 전망으로 1~2년 창 비교.
나는 분기 영업이익의 델타 상승이 잡았다고 생각한다."

## 1) APP 사례 — 2023-08-05 시점에 각 지표가 보여준 것

- **조정 EPS**: APP 는 미발표 회사 (0건이 정답) — 이 축은 원천 무용.
- **조정 EBITDA**: 2022~2023 보도자료 파싱 구멍 — 당시 우리 파이프라인
  데이터로는 무용 (원문 감사 대상으로 이미 기록).
- **논갭 영업이익(XBRL)**: 유일하게 구멍 없이 존재. 20차 기록대로
  마진 연쇄 확장 13%→20%(5월 발표)까지 이미 보였고, 28% 걸음은
  정배열 시작 4일 뒤(8/9) 발표. **델타(절대액 QoQ)도 당시 컸음** —
  사례 수준에서는 저장소 주인 관찰과 일치.
- **전망(가이던스)**: APP 값 없음. 전 종목 커버리지도 17종목·분기값
  54~56건뿐 — **전망 축 비교는 표본 부족으로 불가** (수집 확대 과제).

## 2) 델타 × 긴 창 일반화 — 영업이익 델타는 기각

창 250일(+80p)/500일(+160p), 연속 분기·직전 양수 조건 (부록 2와 동일):

- **논갭 영업이익 QoQ +30/50/100%**: 250일 6.6/4.6/5.8% — **전부
  기준선(9.0%) 이하**, 중앙값도 더 나쁨. 500일도 동급(겹침).
  **"영업이익 델타 상승 확인 후 매수"는 긴 창에서도 우위 없음.**
- **조정 EPS QoQ**: 기준선 위지만 전부 구간 겹침.
- 유일한 완전 분리: **조정 EPS YoY+100% × 250일** 26.2% [15.3,41.1]
  vs 기준선 9.5% [7.5,12.0]. 단 앞시기 5% ≈ 기준선 앞 4%(우위 전부
  뒤시기), 500일에서는 7.4 vs 7.3%로 소멸 — 원칙 4(강세장 구간만의
  우위 불신)에 걸려 **등록하지 않음**. 기록만.

## 3) 정배열 시작 사건 × 최신 분기 델타 (결합)

정배열 시작(22차 정의) 사건마다 140일 내 최신 발표의 QoQ 델타를 부착:

- 250일: 델타+30%↑ 그룹이 델타<+30% 그룹보다 나음 (영업이익 8.8 vs
  4.5% · 조정EPS 9.9 vs 3.9%) — 단 두 그룹 다 전체 주봉 기준선 이하.
- 500일 조정EPS: 델타+30%↑ 17.0% · 중앙 **+34.3p** vs 델타<+30%
  5.6% · 중앙 -13.0p — 그룹 정렬은 뚜렷하나 구간 겹침(9.2 vs 10.0),
  앞시기 8%로 약함.

## 결론 (기록)

- 사례로서의 APP 는 관찰대로다: 정배열 시작 시점에 영업이익 델타가
  실제로 컸다. 그러나 **전 종목 규칙으로는 영업이익 델타가 오히려
  세 축 중 제일 약하고**(기준선 이하), 델타류의 우위는 조정 EPS 쪽에서
  뒤시기(강세장)에만 나타난다 — 장세 지배 재확인.
- 정배열 시작 안에서는 델타가 좋은 시작과 나쁜 시작을 **정렬**해 주지만
  (APP·CLS 류가 델타+30 그룹), 채택 기준(완전 분리·앞뒤 우세)에는 미달.
- 이 방향의 정식 검증은 이미 등록된 **H9·H10**(첫 신기록 × 52주선 아래)
  경로로 진행 중 — 새 등록 없음.

## 24차 부록 — APP 단독 정밀 분석: 완성 시점별 QoQ 델타 (2026-08-14, 숫자는 실행 출력에서 복사)

저장소 주인 지적: "25년 1월은 QoQ 델타가 하락했지. APP 데이터만 가져와
제대로 분석해. 23년과 25년의 델타 차이값부터 비교해." — **지적이 맞다.**
앞선 대화에서 "2025-01 완성에도 같은 조합(완성+강한 델타)이 떴다"고
말한 것은 **오류였다**: 실측 델타는 +25.9%로 24차의 '강한 델타'(+30%↑)
문턱에 못 미친다. 여기 정정하고 박제한다.

**자료**: XBRL 논갭 영업이익(근사치 계열) 15개 분기 — 구멍 없음.
발표일이 없는 XBRL 분기는 분기말+45일을 "그 시점까지 알려짐" 기준으로
사용(실측 발표일 패턴: Q1→5월 초·Q2→8월 초·Q3→11월 초·Q4→2월 중과
부합). 조정 EPS 는 APP 미발표, GAAP EPS 는 XBRL 25Q2 이전 구멍 —
**EPS 축 델타 비교는 불가**(정직). 영업이익 축만.

### 완성 시점 × 최신 QoQ 델타 × 이후 성과 (22차 실측과 결합)

| 완성일 | 최신 델타 | 이후 |
|---|---|---|
| 2023-07-21 | **+470.5%** | 250일 +168p · →24말 +1052% |
| 2023-10-06 | +47.6% | +211p · +702% |
| 2023-12-29 / 24-01-12 | +39.8% | +714p/+633p · +713%/+676% |
| 2024-04-26 | +62.1% | +276p · +339% |
| 2024-06-28 | **-9.7%** | +306p · +289% ← 델타 규칙의 반례 |
| 2024-08-30 / 09-13 | +12.6% | +404p/+408p · +249%/+188% ← 반례 |
| 2025-01-03 / 01-17 | +25.9% | 60일 -13p/-19p · 250일 +63p/+49p |
| 2025-08-01 | +15.7% | 250일 -17p |
| 2025-10-24 / 12-19 | +15.1%/+12.0% | 60일 -18p/-36p |

### 읽기

- **23 vs 25 델타 차이값**: 2023 완성들 +39.8~+470.5% vs 2025 완성들
  +12.0~+25.9% — 한 자릿수 배 차이. 저장소 주인 관찰 그대로.
- 단 **2024년이 반례**: 6월 완성은 델타 -9.7%(최악)인데 +289%,
  8~9월 완성도 +12.6%(2025년 수준)인데 +188~249%. "델타 낮음→실패"는
  2025년에만 맞고 2024년엔 틀림 — 델타 절대 수준 단독으론 부족.
- 2024와 2025를 가른 건 델타가 아니라 **주가가 이익을 앞지른 정도**:
  2024-06-28→2025-01-03 사이 주가 4.2배 vs 알려진 TTM 영업이익 1.42배.
  2025년 완성들은 이익이 계속 늘는 중에도(+12~22% QoQ) 주가가 이미
  다 반영한 상태에서 찍힘.
- 결론: 델타는 2023류(대폭발)와 2025류(둔화)를 가르는 **정렬 변수**로
  유효하나, 가격-이익 관계(H9·H10의 "눌림")와 결합해야 2024류 반례를
  설명할 수 있다. 등록 변경 없음 — H9·H10 판정 경로 유지.

---
---

# 25차 — 3차 확장(160종목) 도착 감사 + 영업이익 단위 오염 발견·차단 (2026-08-14, 숫자는 실행 출력에서 복사)

로봇 160종목 첫 수집(00:51 UTC, 자동 수집 커밋 114파일) 도착 직후의
19차 의무 감사 실행 기록.

## ① 3차 확장 54종목

- 54종목 전부 분기 데이터 수집됨. 잣대 확보 38종목
  (조정 EPS 27 · GAAP EPS 10 · EBITDA 1). 잣대 없음 16종목 —
  대부분 은행·보험·소비재의 조정 EPS/발표 형식 문제, 원문 감사 대상.

## ② 오염 감사

- 202.0 오염 0건 · |주당값|>100 0건 — 기존 방어 유지 확인.
- 정수 주당값(≥2) 23건 — 의심 목록에 기록 (2.00 같은 실제값 가능성
  있어 원문 확인 전 판단 보류).
- **새 사고(백서 17): 논갭 영업이익 단위 미환산 571건.** 보도자료
  표가 '천/백만 달러' 단위인데 달러로 저장 (실물: CRDO 216,722 ↔ 매출
  4.37억 = 마진 0.05% · MU 3,955 ↔ 매출 113억). 파서의 마진 검사는
  같은 보도자료 안 매출과 비교하니 통과 — **병합에서 매출만 XBRL
  달러값으로 바뀌며 어긋남**. 원칙 6(장치 먼저 의심)이 H10 첫 판정
  전에 작동한 사례.
- **차단**: 데이터 계층에 병합 후 단위 검사 추가 — 매출(달러 확정,
  1천만 이상) 대비 마진 0.1% 미만·90% 초과·-500% 미만이면 영업이익
  없음 처리 (버림, 고치지 않음). 새 테스트 2개 빨간 불 확인 후 통과
  (전체 204개, 종료코드 0). 단위 환산 파서(천/백만 헤더 인식)는 후속
  과제 — 지금은 "없음이 안전".

## ③ 정화 후 H10 탐색 우위 재확인 (전체 신규 표본)

- 신호 27.8% [17.6,40.9] n=54 vs 기준선 13.9% [12.0,16.0] n=1,164 —
  **완전 분리 유지** (하한 17.6 > 상한 16.0). 앞 29%(n=38)/뒤 25%(n=16).

## ④ 진짜 교차 검증 — 탐색에 안 쓴 3차 확장 54종목만

- H9: 신호 9.1% [1.6,37.7] n=11 vs 기준선 2.6% [1.4,4.9] n=341
- H10: 신호 7.7% [1.4,33.3] n=13 vs 기준선 2.7% [1.5,5.1] n=328
- **방향은 맞다**(신호가 기준선의 3~3.5배)만 n=11~13으로 구간이 넓어
  결론 불가 — 로봇이 표본을 쌓는 대로 자동 재판정.
- 별건 발견: 신규 업종(금융·헬스케어·소비재 등)의 기준선 폭등률은
  2.6~2.7%로 반도체권(13.9%)의 1/5 — 업종별 기준선 차이가 크므로
  잣대 그룹별 기준선 원칙(12차)이 여기서도 중요.

## 공식 판정 관련 주의 (박제)

- 로봇 verdict 의 H9·H10 "신규(판정)" 표본에는 탐색에 쓴 106종목
  사건이 섞여 있다. 첫 "채택"이 떠도 그것만으로 독립 확인이 아니며,
  독립 확인은 ④의 교차 표본(3차 확장 + 등록 이후 발표)이 쌓여야 한다.
  이 구분을 매 보고에 병기한다.

---
---

# 27차 — 전망 축 확장: 헌법 개정(컨센서스 허용) + 원장 도입 + 포착 설계 (2026-08-14)

저장소 주인 결정: "B(헌법 개정)로 진행하되 데이터를 실수 없이 정확하게.
가이던스와 컨센서스를 동시에 전망. 평균이 좋을지 다른 포착 방법을
추천해달라."

## 개정 (전략.md 2장 제1조 — 본문이 원본)

- 전망 입력 ①회사 가이던스 ②야후 컨센서스. 컨센서스 안전 조건:
  수집 시점(as_of) 기록 · **추가 전용 원장**(과거 항목 불변 — 소급
  수정 방어) · 사건에는 발표일 **이전** 마지막 스냅샷만(미래 엿보기
  금지) · 판정 편입은 새 H번호 사전 등록 후 새 데이터로만. 그 전까지
  관찰·기록 전용.
- 시킹알파는 채택하지 않음 (공식 API 없음·봇 차단·약관 위반 경로뿐).
  수집 창구는 이미 쓰는 야후(yfinance)로 고정.

## 구현 (이 커밋)

- `consensus_feed.py`: 야후 EPS 추정(이번 분기·다음 분기 avg/low/high/
  분석가 수/전년 실적) → `data/measure/consensus.json` 추가 전용 원장.
  오염 가드: 주당 상한 100 · low≤avg≤high 위반 행 폐기 · NaN→없음.
  하루 한 번 · 값 무변화 시 생략 · 시간 역행 금지.
- 로봇(collect_job)에 연결 — 실패해도 그날 데이터 커밋은 막지 않음.
- 새 테스트 5개: 추가 전용·오염 가드를 각각 일부러 깨서 빨간 불 확인
  후 통과 (전체 211개, 종료코드 0). 실수집 검증은 다음 로봇 런 로그의
  "컨센서스: 확보 n종목" 줄로 증명 (실행 증명 규칙).

## 포착 설계 (기록 — 아직 등록 아님)

**평균은 쓰지 않는다.** 가이던스(회사의 보수적 약속)와 컨센서스(시장
기대)는 성질이 다른 두 값이고, 정보는 **둘의 차이**에 있다. 평균 내면
그 차이가 소거된다. 관찰할 세 가지 (표본이 쌓이면 각각 새 H번호로
사전 등록해 새 데이터로만 판정):

1. **전망 스프레드** = (차기 분기 가이던스 중간값 − 같은 분기 컨센서스
   평균) / |컨센서스 평균|. 회사가 시장 기대보다 높게 부르는 순간 —
   "시장이 아직 안 믿는 이익 개선"의 전망판 (H9·H10과 같은 방향).
2. **서프라이즈** = (실적 조정 EPS − 발표일 직전 컨센서스 평균) /
   |컨센서스 평균|. 첫 신기록과 결합 후보.
3. **개정 방향** = 컨센서스 평균의 원장 내 변화(예: 30일 전 대비).
   상향 개정 흐름 관찰.

한계 (정직): 야후 추정 표는 분기 라벨(0q/+1q)만 주고 회계 분기말을
명시하지 않음 — 가이던스와 짝지을 때 발표 주기로 정렬해야 하며, 그
절차는 등록 시점에 함께 박제한다. 원장 축적 전에는 아무것도 판정하지
않는다.

---
---

# 28차 — 가이던스 파서 커버리지 확대 (2026-08-14, 숫자는 실행 출력에서 복사)

저장소 주인 요청: "가이던스 파서 커버리지도 확대해줘" (전망 스프레드
게이지의 재료 확충).

## 확대 내용 (전부 실물 보도자료 발췌로 검증)

- **분기 선언 구획 상속**: "guidance for the second quarter ...:" 선언
  아래 불릿(분기 낱말 없음)이 분기 문맥을 상속 (AMBA·CIEN·U 실물).
- **± 형**: "$11.2 billion, plus or minus $300 million" (AMD) ·
  "$1.260 billion plus or minus $20.0 million" (MCHP) · ±% 형.
- **between 형 EPS**: "between $0.44 and $0.60" (UCTT) — GAAP 나란히
  있어도 non-GAAP 쪽만.
- **월명 분기**: "March quarter" (MCHP). guidance_text 창 6,000→12,000자.
- **밴드 우선**: 지난번 가이던스 재인용(단일값)보다 새 범위가 이김.

## 오탐 3종 발견·차단 (전부 실물, 테스트로 박제)

- UNH: 제목("...Results; Revises Full Year Guidance")·자사주 매입
  문장("expects to complete by the end of the second quarter")이 구획을
  열어 실적 하이라이트($63.7B)를 가이던스로 오인 → 가짜 선언 필터.
- AXP: 구두점 없는 머리말 뭉치가 수백 자 "문장"이 되어 오인 →
  문장·구획 줄 500자 상한.
- 앞보는 말(expect·outlook 등) 필수 — 과거 실적 문장 원천 차단.

## 결과 (실패 원문 316건 코퍼스 — 편향 표본임을 명시)

- 매출 가이던스 **0 → 60건(35종목)** · EPS 0 → 5건(AMAT·HPQ·SNDK·
  UCTT×2) · EBITDA 9건. KLAC 슬라이드 표 뭉치 1건은 안전 상한이
  배제 — "없음이 안전" 쪽을 택함.
- 무작위 표본 검증: CGNX $235~255M·U $405~415M·AMZN $194~199B·
  META $61~64B — 전부 원문과 대조 확인.
- ⚠️ 이 코퍼스는 "조정 EPS 파싱 실패" 원문 모음(편향)이라 실제
  커버리지는 다음 로봇 런의 guid_* 채움 수로 확인한다 (실행 증명).
- 테스트: 새 7개 각각 빨간 불 확인(사보타주 중 실제 구멍 2개 추가
  발견·수리 — 구획 줄 상한, 지문 부정확) 후 전체 221개 통과, 종료코드 0.

---
---

# 29차 — 가이던스 파서 회귀 발견·수리 (2026-08-14, 숫자는 실행 출력에서 복사)

28차 배포 직후 첫 실전 런(08:40 커밋)에서 가이던스 채움이 구 파서
24종목/143분기 → **7종목/24분기로 격감** — 원칙 6(장치 먼저 의심)에
따라 즉시 감사.

## 원인 (실물 재현으로 확정)

- 28차에서 항목 필터로 쓴 넓은 연간 표지(_ANNUAL_HINTS)에 "fiscal
  year 20XX"가 포함 → **"first quarter of fiscal year 2026" 같은 진짜
  분기 문장이 연간으로 오인돼 통째로 폐기** (INTC·NTAP·TTMI류 전멸).
- 수리 과정에서 추가 발견: 문장 분리가 줄바꿈을 경계로 안 봐서
  마침표 없는 제목+불릿 여러 줄이 한 "문장"으로 뭉침 (연간 수치 혼입
  경로) → 줄바꿈을 문장 경계에 추가.

## 수리 (고정 규칙)

- 항목 필터는 **좁은 연간 표지만** 사용 (full year · for the year ·
  annual guidance/outlook/revenue). "fiscal year 20XX" 단독은 연간
  판정에 쓰지 않는다.
- 좁은 표지는 분기 낱말이 함께 있어도 버린다 — "full year … for the
  fourth quarter update" 혼합 문장은 어느 분기 값인지 확신 불가
  ("없음"이 안전).
- 검증: 회귀 지문 red→green, 실물 양성 5종(AMBA·AMD·MCHP·CIEN·ABNB)
  ·음성 2종(UNH·혼합문장) 유지, 실패 원문 코퍼스 매출 56건/32종목,
  전체 테스트 222개 통과. 실전 회복치는 다음 로봇 런으로 증명.

## 함께 처리 (운영)

- 컨테이너 재생성 시 승인 설정이 사라지던 문제: `.claude/settings.json`
  을 저장소에 커밋해 영구화 (저장소 주인 지시 — 자동 승인).
- 사용량 한도 후 자동 재개: 매시간 자기 호출 장치(서버 예약)를 설치 —
  한도로 끊겨도 다음 정시에 스스로 깨어나 이어서 한다 (CLAUDE.md 1장).

---
---

# 30차 — 3축 주도 섹터 첫 분석 + 컨센서스 매출 축 추가 (2026-08-14, 숫자는 실행 출력에서 복사)

저장소 주인 질문: "SEC 실적·가이던스·컨센서스의 스프레드 주도 섹터를
분석해. 데이터 기준은 조정 EPS → EPS → 논갭 EBITDA → EBITDA 순이
낫겠지?"

## 축 우선순위 결정 (기록)

- **1순위 조정 EPS — 제안대로.** 유일하게 3축(실적·가이던스·컨센서스)
  이 전부 존재. 야후 컨센서스는 관례상 조정(non-GAAP) 기준이라 정의가
  맞음.
- **2순위는 GAAP EPS 가 아니라 매출** — 매출은 가이던스가 가장 풍부
  (실패 코퍼스 32종목분)하고 컨센서스도 존재. 이번 차수에 원장에
  매출 추정(rev_avg/low/high/analysts) 수집을 추가함.
- GAAP EPS: 실적 축 보조로만. **컨센서스와 짝짓기 금지** — 컨센서스가
  조정 기준이라 정의 혼재 오염이 됨 (사고 백서의 정의 혼재류).
- 조정 EBITDA: 관찰만 (실적 15종목·가이던스 20값 — 표본 부족).
  "그냥 EBITDA"는 수집 항목에 없음 — 회사들이 조정 EBITDA 를 발표.

## 첫 실측 (구 파서 스냅샷 a2cd3fc + 8/14 원장 — 29차 회복 전 과도기)

**B축 — 가이던스 vs 컨센서스 (조정 EPS, 4종목)**: ACLS +0.4% ·
NTAP -0.5% · TTMI -20.5% · INTC -20.7%. 전 종목 표본 부족 — 순위 판단
아직 불가.

**C축 — 실적 vs 직전 가이던스 (달성 스프레드, 최근 180일 9건)**:
- 반도체장비 중앙 **+17.8%** (3건: ONTO +54.3, ACLS +17.8/+1.4)
- 소프트웨어 +3.2% (2건 ⚠️) · 하드웨어 +0.0% (3건)
- 반도체 +425% (INTC 0.08→0.42 — 1건 ⚠️, 저기저 효과)
- 읽기: **장비 무리가 회사 스스로 부른 숫자를 큰 폭으로 넘기는 중** —
  실적 게이지(반도체장비 50% 1위)와 방향 일치. 단 표본이 얇아 관찰만.

**A축 — 실적 vs 컨센서스 (서프라이즈)**: 원장이 8/14 시작이라 짝지을
발표 0건 — 다음 실적 시즌부터 자동 축적.

## 한계 (박제)

- B·C 축 가이던스는 29차 수리 전 스냅샷 기준 — 재실행 런 도착 후
  갱신치가 정본. A축은 원장 나이만큼만 자란다.

## 30차 부록 — A축 소급: 야후 보관 서프라이즈 기록 (31차 구현, 2026-08-14)

저장소 주인 제안: "A축을 다음 실적 말고 이번(이미 나온) 실적으로 먼저
쌓아서 미리 짝짓자" — **채택.** 야후 earnings_history 가 지난 4개
분기의 "발표 당시 추정 vs 실제"를 보관 제공하므로 과거 소급이 가능.

- **출처 구분 박제**: 이 값은 야후의 사후 보관 기록이라 "그날 우리가
  직접 본 값"이 아님 → 원장(consensus.json)과 섞지 않고 별도 파일
  (surprise.json)에 담고 출처 명시. **원장 시작(8/14) 이후의 발표는
  원장이 정본**, 보관 기록은 그 이전의 관찰용.
- 검사: 주당 상한 100 · 추정 0(서프라이즈% 정의 불가) 제외 ·
  (종목, 분기) 추가 전용 — 최초 기록 유지.
- 규모 예상: 160종목 × 최근 4분기 ≈ 최대 ~640건 (다음 로봇 런부터).
- 테스트: 중복 방지를 일부러 깨서 빨간 불 확인 후 통과 (전체 224개).

## 32차 — 가이던스 창 제한 제거 (2026-08-14, 숫자는 실행 출력에서 복사)

29차 수리 후에도 실전 채움이 8종목에 그침(10:39 런) — 장치 재감사.
**원인 확정**: 파이프라인이 가이던스 탐색 입력을 원문 "끝 12,000자"로
제한하는데, 전망 문단을 앞쪽(결과 요약 직후)에 두는 회사가 많음.
코퍼스 실측: 전체 원문 56건 vs 끝 12,000자만 18건 — **창이 21종목을
자름** (NVDA·AMD·AMZN·META·SNDK 포함). 전체 원문을 넘기도록 수리.
과거 서술 오인은 29차의 앞보는 말·선언 가드가 담당 (테스트 224개 유지).

---
---

# 33차 — **H11 사전 등록**: 섹터 정배열 폭 → 1년 상승 섹터 포착 (2026-08-14, 측정 전 박제)

저장소 주인 지시: "주가가 정배열 이후에 포텐이 터지는 것 같으니 이것을
무조건 메인으로 삼고, 정배열 이후 최소 1년 이상을 분석하여 앞으로
상승될 섹터를 포착하는 모델링을 설계하라."

**먼저 밝힐 것 (정직)**: 22차에서 **종목 단위** 정배열 완성은 창
60/120/250일 모두 기준선과 동급이었다(250일 13.9% vs 14.5%). 그러나
이번 지시는 **섹터 단위**(무리 전체가 함께 정배열되는 폭) — 22차가
재지 않은 다른 질문이다. 그래서 새 가설로 정식 등록해 측정한다.
결과가 미채택이면 화면에 미채택으로 표시하며, 그래도 저장소 주인
지시대로 **관찰 지표로서 메인 화면에 둔다**(판정 상태를 함께 적어).

## H11 정의 (이 등록으로 고정 — 데이터를 보기 전에 적음)

- **섹터 정배열 폭** = 그 주 마지막 거래일 기준, 섹터 종목 중
  완전 정배열(주봉 종가 > 4주선 > 13주선 > 26주선 > 52주선)인 비율(%).
  52주 이력 미만 종목은 분모에서 제외. 유효 종목 3개 미만인 주는
  **판단 불가**(측정 제외).
- **신호(H11)** = 그 주 폭이 **60% 이상**이고, 직전 주는 60% 미만
  (= 폭이 처음 60%를 넘긴 주 — "무리가 함께 정배열에 들어선 순간").
- **표적** = 신호 주의 다음 거래일부터 **250거래일**(약 1년) 동안,
  그 섹터 종목 **동일가중 평균 수익률 − SPY 수익률**.
  · 폭등 문턱 = **+20%p** (60일 창의 +20%p 와 같은 숫자를 쓰되,
    1년 창이므로 더 엄격한 기준이 아님을 명시 — 이 문턱은 "1년 뒤
    시장을 20%p 이겼는가"를 묻는다)
- **기준선** = 같은 방식으로 잰 **모든 주 × 모든 섹터**(판단 가능
  주 전체). 신호 주도 기준선에 포함된다.
- **채택 기준** = 신호 윌슨 95% 하한 > 기준선 윌슨 95% 상한, n≥10.
  n<10 이면 판정 불가. 앞/뒤 시간 분할 병기 (11차 규칙 그대로).
- **우측 검열**: 250거래일 창이 데이터 끝을 넘는 신호는 세지 않는다
  (약 2025-08 이후 신호는 아직 판정 대상 아님 — 표본이 얇을 것이
  예상되며, 얇으면 얇다고 보고한다).
- **H11b (보조 등록)**: 같은 정의에서 문턱만 **80%**. 더 강한 합의가
  더 나은지 함께 잰다. 채택 기준은 동일.

## 화면 규칙 (저장소 주인 지시 4의 이행)

- 메인 화면 최상단에 **"앞으로 상승할 섹터 후보"**: 각 섹터의 **현재
  정배열 폭**과 **그 상태의 과거 실측 1년 폭등률·기준선·판정 상태**를
  나란히 적는다 (정직화 — 전략.md 2장 제3조).
- 채택 전에는 "이 신호는 아직 채택되지 않았습니다"를 같은 칸에 명시.

## 기간 규칙 (지시 3의 이행)

- 과거: 주가 5년(2021-08~) · 실적 5년 — 3년 요건 충족(이미 확보).
- 전망: 다음 1분기(가이던스·컨센서스 축)를 계기판에 병기.

## 33차 결과 — H11 판정: **미채택** (신호가 기준선보다 나쁨)

측정 (신규 표본 = 판단 가능한 모든 (섹터, 주) 1,872건, 창 250거래일):

| 가설 | 신호 | 기준선 | 판정 |
|---|---|---|---|
| H11 (폭 60% 첫 돌파) | **9.4%** [3.2,24.2] n=32 | 26.8% [24.9,28.9] | **미채택** |
| H11b (폭 80% 첫 돌파) | 14.3% [2.6,51.3] n=7 | 26.8% | 판정 불가 (n<10) |

**H11 은 기준선보다 나쁘다** (9.4 vs 26.8%). 앞시기 8.3% · 뒤시기 10.0%
— 시기를 갈라도 마찬가지. 22차(종목 단위 무우위)에 이어 **섹터 단위로도
"정배열 완성 후 진입"은 우위가 없고, 오히려 열세**임이 확인됐다.

---

# 34차 — 폭 구간 전수 탐색 + **H12 사전 등록** (2026-08-14)

H11 이 왜 나쁜지 알기 위해 폭 구간을 전부 갈라 봤다 (탐색 — 등록 아님):

| 섹터 정배열 폭 | 1년 뒤 +20%p 비율 | 초과 중앙 | 앞/뒤 |
|---|---|---|---|
| 0~9% | 27.3% [24.4,30.4] n=831 | +1.6p | 23/31 |
| 10~24% | 26.3% n=438 | -0.9p | 24/29 |
| 25~39% | 25.8% n=329 | -3.0p | 27/25 |
| **40~59%** | **33.3% [27.3,40.0] n=210** | **+5.4p** | 27/40 |
| 60~79% | **8.2%** [3.2,19.2] n=49 | +6.5p | 8/8 |
| 80~100% | **6.7%** n=15 | +6.4p | 14/0 |
| 기준선(전체) | 26.8% [24.9,28.9] n=1,872 | +0.8p | 24/30 |

### 읽기 (오늘 하루의 세 축이 같은 곳을 가리킨다)

- **폭이 다 차면 늦는다**: 60% 이상 구간의 1년 폭등률은 6~8%로
  기준선(26.8%)의 3분의 1 이하. "무리 전체가 정배열"은 이미 시장이
  다 아는 상태 — 그 뒤 1년은 오히려 부진.
- **가장 좋은 구간은 40~59% "쌓이는 중"**: 33.3%로 유일하게 기준선
  위. 다만 하한 27.3 < 기준선 상한 28.9 로 **완전 분리는 아님**.
- 이는 H9·H10(첫 신기록 × 주가는 아직 52주선 아래)과 **같은 방향**이다:
  이익·주가 모두 "아직 다 안 간 상태"가 유리하다. 오늘 세 번째 독립 확인.
- 60%+ 구간의 초과 **중앙값**은 +6.5p 로 플러스다 — 즉 정배열 완성 뒤
  "완만히 이기는" 경향은 있으나 **폭발(+20%p)은 드물다**. 저장소 주인
  관찰("정배열 후 포텐이 터진다")은 APP·CLS 같은 소수 사례에서는
  사실이나, 무리 전체로는 폭발이 아니라 완만함이다.

## H12 사전 등록 (탐색 재사용 금지 — 새 데이터로만 판정)

- **H12_섹터정배열폭_쌓이는중**: 섹터 정배열 폭이 직전 주 40% 미만에서
  이번 주 **40% 이상 60% 미만** 구간에 들어선 주.
- 표적·기준선·채택 기준·창은 33차(H11)와 완전히 동일.
- **판정 표본**: 이 등록 시각(2026-08-14) **이후 발생한 신호만**.
  위 탐색 표는 근거가 아니며, 화면에는 탐색값임을 명시해 병기한다.

## 34차 부록 — 계기판 재설계 (저장소 주인 지시 1·2·3 이행, 2026-08-14)

- **지시 1 (채택=메인, 미채택=접기)**: ①앞으로 상승할 섹터 후보 →
  ②채택된 신호(H9·H10 + 지금 켜진 종목) → ③섹터 차트 → ④미채택·대기
  신호를 접기 칸으로 이동, 각 신호마다 "무엇을 재봤고 왜 안 쓰는가"를
  한국어 한 줄로 병기 (HYPOTHESIS_DETAILS).
- **지시 2 (한눈에 보는 그래프)**: 정렬된 가로 막대 그래프 4종
  (정배열 폭 · 신기록 폭 · 서프라이즈 · 전망 스프레드). st.bar_chart 는
  축을 가나다순으로 재정렬해 순위가 안 보여 **알테어로 정렬 고정**.
  정배열 폭은 **구간별 색**(초록=쌓이는 중 최적 · 빨강=완성 구간 최저 ·
  파랑=초기 · 회색=약함)으로 한눈에 구분.
- **지시 3 (기간)**: 과거 5년(2021-08~) 이미 확보 — 3년 요건 충족.
  화면 상단에 측정 기간과 "전망: 다음 1분기"를 명시.
- 412px 실렌더 확인 (전 섹션 잘림 없음), 테스트 227개 통과.

---
---

# 35차 — **H13 사전 등록**: 차기 주도 섹터 포착 (이익 델타 ∧ 정배열) (2026-08-14, 측정 전 박제)

저장소 주인 지시: "내가 포착하려는 것은 **현재 주도 섹터에서 다음 주도
섹터로 넘어가는 것**이다. 조건은 이익의 델타가 상승하면서 각 종목이
정배열인 것. 이에 대한 백테스팅을 진행하라."

지금까지의 측정(22·33·34차)은 "정배열이면 오르는가"였다. 이번 질문은
**"어느 무리가 다음 주도가 되는가"** — 순위 교대를 맞히는 문제이므로
표적 자체가 다르다. 새 가설로 등록한다.

## H13 정의 (이 등록으로 고정 — 데이터를 보기 전에 적음)

**시점 격자**: 매월 마지막 거래일 (2021-08 ~ 데이터 끝).

**종목 상태** (그 시점까지 **알려진 것만** — 미래 엿보기 금지):
- (a) **이익 델타 상승** = 발표일이 그 시점 이전인 최신 분기의 잣대값이
  직전 **연속** 분기보다 큼. 잣대는 12차 사다리(조정 EPS → 조정 EBITDA
  → GAAP EPS). 최신 발표가 140일보다 오래됐으면 **판단 불가**(김빠짐 —
  게이지와 같은 기준).
- (b) **정배열** = 그 시점 직전 주봉 종가 > 4주선 > 13주선 > 26주선 >
  52주선. 이력 52주 미만이면 판단 불가.
- 두 조건 중 하나라도 판단 불가면 그 종목은 **분모에서 제외**.

**섹터 신호값** = 판단 가능 종목 중 **(a)∧(b) 둘 다 참인 비율(%)**.
유효 종목 3개 미만인 섹터·시점은 판단 불가(측정 제외).

**표적** = 그 시점 다음 거래일부터 **125거래일**(약 6개월) 동안의
섹터 동일가중 평균 수익 − SPY 수익 (%p).

**판정 지표 두 가지** (둘 다 보고한다):
1. **주도 적중**: 그 시점 신호값 **1위 섹터**가, 이후 6개월 초과수익
   **1위**가 되는 비율. 기준선 = 무작위 선택(1/섹터수, 실측으로 병기).
2. **초과수익 우위**: 신호값 상위 3개 섹터의 6개월 초과수익 중앙값
   vs 전 섹터 중앙값.

**순환(교대) 조건**: 위 두 지표를 ① 전체 ② **현재 주도 섹터를 제외**한
경우(=직전 6개월 초과수익 1위 섹터를 후보에서 뺀 경우) 로 나눠 잰다.
저장소 주인의 질문은 ②다 — "지금 주도가 아닌 것 중 다음 주도".

**채택 기준** = 지표 1의 신호 윌슨 95% 하한 > 기준선 윌슨 95% 상한,
관측 시점 n≥10. n<10 이면 판정 불가. 앞/뒤 시간 분할 병기 (11차 규칙).

**우측 검열**: 125거래일 창이 데이터 끝을 넘는 시점은 세지 않는다.

**보조 등록 H13b**: 같은 정의에서 (a) 이익 델타 상승만 (정배열 조건
제거). 두 조건 중 어느 쪽이 일을 하는지 가르기 위함.
**보조 등록 H13c**: (b) 정배열만. 같은 이유.

## 35차 결과 — H13·H13b·H13c 전부 **미채택** (2026-08-15, 숫자는 실행 출력에서 복사)

**표본**: 판단 가능 시점 **42개월** (2022-08-31 ~ 2026-01-30, 우측 검열
제외) · 시점당 평균 9.7개 섹터 · 섹터-시점 관측 407건.

### 지표 1 — 신호 1위 섹터가 이후 6개월 초과수익 1위가 되는가

| 가설 | 전체 섹터 | 현재 주도 제외 (교대 질문) | 무작위 기준선 |
|---|---|---|---|
| H13 (델타↑ ∧ 정배열) | 11.9% [5.2,25.0] 5/42 | **4.8%** [1.3,15.8] 2/42 | 10.3% [3.8,22.1] |
| H13b (델타↑만) | 4.8% 2/42 | 11.9% [5.2,25.0] 5/42 | 10.3% |
| H13c (정배열만) | 9.5% 4/42 | 4.8% 2/42 | 10.3% |

**전부 무작위 기준선과 구간이 겹친다 → 미채택.** 저장소 주인의 핵심
질문인 "현재 주도 제외" 판에서 H13 은 오히려 무작위의 절반(4.8%)이다.

### 지표 2 — 1위 섹터의 6개월 초과수익 중앙값

H13 +0.2%p (교대판 +0.3) · H13b +1.9%p (교대판 +3.3) · H13c -1.8%p
(교대판 -0.2) vs 전체 섹터 중앙 +0.5%p. 상위 3개 그룹은 H13 +3.5~4.7%p
로 약간 나으나 폭등률로는 차이가 없다.

---

# 36차 — 조건 분해 탐색: 두 조각 중 무엇이 일하는가 (2026-08-15)

35차가 왜 실패했는지 보기 위해 신호값 구간별로 갈랐다 (탐색 — 등록 아님).
표적은 동일(이후 6개월 섹터 동일가중 초과수익), 기준선 중앙 +0.5%p ·
폭등률 17.4% (n=407).

| 구간 | **H13** 델타∧정배열 | **H13b** 델타만 | **H13c** 정배열만 |
|---|---|---|---|
| 0~19% | +0.5%p (n=292) | **-4.4%p** (n=14) | **+1.8%p** (n=229) |
| 20~39% | +1.1%p (n=90) | -1.9%p (n=63) | -1.0%p (n=105) |
| 40~59% | -2.0%p (n=23) | +0.1%p (n=130) | -1.1%p (n=59) |
| 60~79% | (n=1) | **+4.3%p** (n=116) | -2.0%p (n=11) |
| 80%+ | (n=1) | +1.1%p (n=84) | -5.9%p (n=3) |

### 읽기 — 두 조각이 서로 반대로 움직인다

1. **이익 델타(H13b)는 약한 순방향**: 0~19% → 60~79% 로 갈수록 중앙값이
   -4.4 → +4.3%p 로 단조 상승. 다만 **폭등률(+20%p 비율)은 전 구간
   15~20%로 기준선(17.4%)과 차이 없음** — 중앙값을 조금 밀 뿐 폭발을
   만들지 않는다.
2. **정배열(H13c)은 약한 역방향**: 폭이 **낮을수록** 좋다(0~19%가
   +1.8%p로 최고, 60%+ 는 -2.0~-5.9%p). 22·33·34차와 같은 방향의
   네 번째 확인.
3. **그래서 둘을 곱한 H13 은 상쇄된다.** 게다가 두 조건을 동시에
   만족하는 종목이 40%를 넘는 섹터-시점이 **407건 중 25건뿐** —
   신호가 거의 켜지지 않는다.

### 반대 조합도 확인 (탐색): 델타↑ ∧ **비**정배열(눌림)

구간별 중앙: 0~19% -5.9%p · 20~39% -1.1 · 40~59% **+3.3** ·
60~79% +0.6 · 80%+ **+3.6** (기준선 +0.5). 방향은 맞지만 폭등률은
15~20%로 차이 없고, 지표 1(1위 적중)은 4.8%로 무작위 이하. **미달**.

## 결론 (정직)

- **섹터 교대(로테이션) 포착은 이 정의·이 데이터로는 되지 않는다.**
  세 가설 모두 무작위와 구분되지 않으며, 저장소 주인 가설의 두 조각 중
  **정배열은 오히려 방향이 반대**다.
- 살아 있는 조각은 **이익 델타의 약한 순방향**뿐이고, 그것도 "누가 1위가
  되는가"는 못 맞힌다(중앙값만 조금 좋아짐).
- **한계 (반드시 함께 읽을 것)**: ① 42개월은 사실상 **한 사이클**
  (2022 약세 → 2023~25 강세)뿐이다. 로테이션은 여러 사이클을 거쳐야
  검증된다. ② 섹터당 종목이 5~22개로 얇아 섹터 수익률 자체가 튄다.
  ③ 6개월 창은 등록된 값이며 다른 창은 재보지 않았다(탐색 규율 —
  창을 바꿔 가며 좋은 값을 찾는 것은 금지).
- 새 등록은 하지 않는다. 기존 방향(H9·H10·H12 — "아직 다 안 간 상태")
  의 표본 축적을 계속한다.

---
---

# 37차 — AI 사이클 국면 실측: 순서가 반대였다 (2026-08-15, 숫자는 실행 출력에서 복사)

저장소 주인 관찰: "현재 주도인 AI 사이클이 25년 4~6월쯤 정배열을
형성하면서 이익 델타가 급상승했고 주가가 지금까지 폭등 중이다.
이 조건을 기준으로 삼아라."

**AI 사이클 = config.THEMES 의 AI-* 60종목**, 비AI 100종목.
상대수익 = 2024-12-31 기준 동일가중 누적 − SPY (%p). 미래 엿보기 금지
(델타는 발표일 기준, 140일 넘으면 제외).

| 월 | AI 정배열폭 | AI 델타폭 | AI 상대수익 | 비AI 상대 |
|---|---|---|---|---|
| 2025-01 | 17.0% | 68.1% | +1.5 | +2.5 |
| 2025-02 | 4.1% | 63.3% | -6.0 | +1.8 |
| 2025-03 | 4.0% | 66.0% | **-12.6** | +0.3 |
| **2025-04** | **2.0%** ← 최저 | 59.2% | -9.4 | -2.6 |
| 2025-05 | 6.1% | **42.9%** | -2.7 | -3.6 |
| **2025-06** | 18.8% | **39.6%** ← 최저 | **+8.3** ← 전환 | -3.8 |
| 2025-07 | 31.9% | 55.3% | +13.7 | -4.5 |
| 2025-09 | 26.5% | 71.4% | +26.2 | -1.8 |
| 2025-10 | **65.3%** ← 정점 | 73.5% | +43.6 | -1.2 |
| 2026-01 | 39.6% | 85.4% | +56.8 | -0.3 |
| 2026-06 | 33.3% | 72.5% | **+206.9** | +13.2 |
| 2026-08 | 19.6% | 86.3% | +165.1 | +6.5 |

## 결정적 발견 — 순서가 관찰과 반대다

- **2025년 4~6월은 정배열이 "형성된" 때가 아니라 바닥이었다**:
  정배열 폭 최저 **2.0%(4월)**, 이익 델타 폭 최저 **39.6%(6월)**,
  상대수익 최저 **-12.6%(3월)**. 세 지표 모두 그 구간이 최악.
- **주가가 먼저 돌아섰다**: 6월 상대수익 +8.3%p 로 전환 → 그 뒤
  정배열 폭이 7월 31.9% → 10월 65.3% 로 따라 올랐고, 이익 델타 폭도
  7월 55% → 11월 84% 로 **뒤늦게** 회복했다.
- 즉 실제 순서는 **① 바닥(정배열·델타 모두 최저) → ② 주가 반등 →
  ③ 정배열 완성 → ④ 델타 회복**. 저장소 주인이 기억한
  "정배열·델타 먼저, 주가 나중"과 **반대**다.
- 이 국면에서 사서 대박이 난 진입 시점은 **정배열 폭이 2~6%로 바닥이던
  2025년 4~6월** — 34차의 "약함" 구간이자 H9·H10 의 "아직 안 간 상태"와
  같은 자리다. 오늘 다섯 번째 같은 방향 확인.
- 반대로 정배열이 완성된 2025-10(65.3%)에 샀다면 이후 상승분의
  대부분을 놓쳤다(그 시점 이미 +43.6%p 진행).

## 화면 반영

계기판에 **"①-2 사이클 추적"** 두 그래프를 상설 배치: (1) AI 정배열 폭 ·
이익 델타 폭, (2) AI vs 비AI 상대수익. 세 선의 **순서**를 눈으로 볼 수
있게 하고, 실측 순서 요약을 함께 적었다.

## 등록 변경 없음

"이 조건을 기준으로 삼으라"는 지시였으나, **실측이 그 조건의 순서를
부정**하므로 새 등록을 하지 않는다. 이미 등록된 H12(폭 40~59% 진입)와
H9·H10 이 이 국면이 가리키는 방향("아직 다 안 간 상태")을 이미 담고 있다.

---
---

# 38차 — **H14 사전 등록**: 주도 교체 "확인" 신호 (2026-08-15, 측정 전 박제)

저장소 주인 정정: "내가 원하는 것은 **주도주로 넘어가는 섹터가 무엇인지
확신할 수 있는 단계·현상**을 포착하는 모델이다. 주가가 어느 정도
상승했더라도 **정배열과 델타 상승이 동시에** 나와야 한다. 정배열·델타
이전에 주가가 오르고 있더라도 다시 하락하는 **단순 섹터 순환**일 수
있고, 그건 주도 섹터 변경이 아니다."

**37차의 오독을 정정한다**: 37차는 "가장 싸게 사는 자리"를 답했으나
질문은 "**진짜 교체인지 확신하는 자리**"였다. 둘은 다른 질문이며,
바닥에서는 그것이 진짜 주도 교체인지 **알 수 없다**는 것이 요지다.
따라서 표적도 "누가 1위인가"(H13)가 아니라 **"주도가 유지되는가 vs
되돌아가는가"** 여야 한다.

## H14 정의 (이 등록으로 고정 — 데이터를 보기 전에 적음)

**시점**: 매월 마지막 거래일. **대상**: 섹터(12개) 및 테마 묶음.

**전제 조건 (a) — 주가가 이미 돌아섰다**:
그 섹터의 직전 63거래일(3개월) 동일가중 수익 − SPY > 0.
→ (a)를 만족하는 섹터-시점 전체가 **비교 모집단**이다.

**확인 조건 (b)·(c) — 동시 충족**:
- (b) **정배열 확인**: 정배열 폭 ≥ **40%** 이면서 **직전월보다 상승**
- (c) **델타 확인**: 이익 델타 폭(직전 분기보다 이익이 는 종목 비율)
  ≥ **50%** 이면서 **직전월보다 상승**
  (델타는 발표일 기준, 140일 넘은 발표는 판단 불가 — 미래 엿보기 금지)

**신호군(H14)** = (a) ∧ (b) ∧ (c) 인 섹터-시점.
**대조군** = (a) 만 만족하고 (b)·(c) 중 하나라도 빠진 섹터-시점
  ("주가만 오른" 무리 — 저장소 주인이 말한 단순 순환 후보).

**표적** = 그 시점 다음 거래일부터 **250거래일(약 1년)** 동안의
섹터 동일가중 수익 − SPY (%p).
- **주도 유지(성공)** = **+10%p 이상**
- **되돌림(실패)** = **0 미만**

**판정 지표**:
1. 주도 유지 비율 (신호군 vs 대조군)
2. 되돌림 비율 (신호군 vs 대조군) — 낮을수록 좋다
3. 1년 상대수익 중앙값 (신호군 vs 대조군)

**채택 기준** = 지표 1의 신호군 윌슨 95% 하한 > 대조군 윌슨 95% 상한,
신호군 n≥10. n<10 이면 판정 불가. 앞/뒤 시간 분할 병기.

**보조 등록**:
- **H14b**: (a) ∧ (b) 만 (정배열 확인만) — 델타의 기여를 가른다.
- **H14c**: (a) ∧ (c) 만 (델타 확인만) — 정배열의 기여를 가른다.

**우측 검열**: 250거래일 창이 데이터 끝을 넘는 시점은 세지 않는다.
**한계 예고**: 표본이 얇을 것이 확실하다(5년·12섹터). n<10 이면 억지로
결론 내지 않고 "판정 불가"로 두고 표본을 쌓는다.

## 38차 결과 — H14 **미채택이나 지금까지 중 가장 강한 방향** (2026-08-15)

**표본**: 전제(a) 충족 = 직전 3개월 상대수익 > 0 인 섹터·테마-시점
**359건** (2022-09-30 ~ 2025-07-31, 1년 창 우측 검열로 2025-08 이후 제외).

| | 주도 유지(+10%p↑) | 되돌림(<0) | 1년 상대수익 중앙 | n |
|---|---|---|---|---|
| **H14 (정배열 확인 ∧ 델타 확인)** | **70.0%** [48.1,85.5] | **25.0%** | **+42.4%p** | 20 |
| H14c (델타 확인만) | 64.6% [54.6,73.4] | 27.1% | +27.5%p | 96 |
| H14b (정배열 확인만) | 58.5% [46.3,69.6] | 35.4% | +18.1%p | 65 |
| **대조군 (주가만 오른 무리)** | 53.7% [48.4,58.9] | 35.7% | +13.4%p | 339 |

- **판정: 미채택** — 신호군 윌슨 하한 48.1 < 대조군 상한 58.9 (겹침).
  n=20 으로 표본이 얇아 구간이 넓다. 등록 기준대로 채택하지 않는다.
- **그러나 방향은 지금까지 잰 것 중 가장 강하다**: 1년 상대수익 중앙값이
  대조군의 **3배**(+42.4 vs +13.4%p), 되돌림은 25% vs 35.7%.
  **앞시기 70% · 뒤시기 70%** 로 시기에 따라 흔들리지 않는다.
- **두 조건이 각각 기여한다**: 델타 확인만 +27.5%p · 정배열 확인만
  +18.1%p · 둘 다 +42.4%p. 저장소 주인의 "동시에 나와야 한다"가
  데이터와 일치한다. 기여는 **델타 쪽이 더 크다**.

### H13 과 무엇이 달랐나 (프레이밍의 차이)

- H13(35차)은 "**누가 1위가 되는가**"를 물었고 실패했다(무작위 수준).
- H14 는 "**이 상승이 진짜 주도 교체인가, 되돌아갈 순환인가**"를 물었다.
  저장소 주인의 정정이 정확했다 — **순위 맞히기가 아니라 확신(지속성)의
  문제**로 세우자 신호가 살아났다.

### AI 테마에서 신호가 켜졌던 시점 (사례 — 9건 중 8건 적중)

| 시점 | 묶음 | 정배열 | 델타 | 이후 1년 |
|---|---|---|---|---|
| 2023-05 | AI-GPU가속기 | 75% | 75% | +54%p |
| 2023-11 | AI-메모리 | 80% | 60% | **+280%p** |
| 2023-12 | AI-GPU가속기 | 67% | 100% | +71%p |
| 2024-01 | AI-메모리 | 100% | 100% | **+276%p** |
| 2024-03 | AI-반도체장비 | 62% | 50% | **-43%p** ← 유일한 실패 |
| 2024-08 | AI-광통신네트워크 | 50% | 70% | +63%p |
| 2024-11 | AI-광통신네트워크 | 40% | 60% | +74%p |
| 2022-11 | AI-데이터센터전력 | 40% | 100% | +50%p |
| 2023-11 | AI-소프트웨어클라우드 | 50% | 83% | +14%p |

## 다음 단계 (등록 유지)

H14 는 **등록된 채로 표본을 쌓는다**. 로봇이 매일 재판정하며, n 이
늘면 자동으로 채택/미채택이 갱신된다. 계기판에는 **현재 신호가 켜진
묶음**을 표시하되 "미채택 — 아직 매수 근거 아님"을 함께 적는다(정직화).

---
---

# 39차 — 정배열 정의 오류 발견·수정 + **H15 사전 등록** (2026-08-15, 측정 전 박제)

저장소 주인 질문: "정배열 몇 프로 상승이 정확히 무슨 뜻이냐? 나는
**주봉 4·13·26·52선 정배열이 차트상 완성된 직후**를 말하는 것이다.
그리고 AI 광통신이 24.11이 아니라 **25.4가 완성 시점** 아니냐?
코히런트·시에나·루멘텀·마벨 같은 종목이고 지금까지 상승 지속 중이다."

## ① 용어 혼선의 실체 (사과와 정정)

- 지금까지 화면·측정에 쓴 **"정배열 폭 40%"**는 "그 섹터 종목 중 40%가
  정배열 상태" 라는 **비율**이었다. 저장소 주인이 말한 "차트상 정배열
  완성"과는 다른 것이며, 이 용어가 혼선을 일으켰다.
- 더 중요한 **정의 오류**: 지금까지의 정배열 조건에 **종가 > 4주선**이
  들어 있었다. 이러면 주가가 잠깐만 눌려도 상태가 깨져 **"완성"이
  수십 번 반복**된다. 실측: COHR 은 2025년 이후 이 정의로 완성이
  **8번**, 이평선만 보는 정의로는 **1번**(2025-10-10).
- 사람이 차트에서 보는 정배열은 **이동평균선의 배열**
  (4주선 > 13주선 > 26주선 > 52주선)이다. 종가는 그 위아래를 오간다.

## ② "24.11 vs 25.4" — 저장소 주인이 맞다

이평선만 보는 정의로 다시 재면:

| 종목 | 정배열 완성 | 유지 기간 |
|---|---|---|
| **LITE(루멘텀)** | **2025-07-11** | 2026-06-18까지 (11개월) |
| **CIEN(시에나)** | **2025-07-18** | 2026-06-18까지 (11개월) |
| **COHR(코히런트)** | **2025-10-10** | 2026-07-10까지 (9개월) |
| MRVL(마벨) | 2025-12-26 · 2026-04-10 | — |

- 앞서 보고한 "2024-11"은 **섹터 정배열 폭(비율) 기준**이었고,
  그 비율 계산이 위의 깜빡이는 정의를 쓰고 있었다. **차트 기준으로는
  2025년 7~10월이 완성 시점**이며, 저장소 주인의 "25년 4~6월" 감각과
  가깝다(4~6월은 바닥·전환, 7월에 정배열 완성).
- **유지 기간 9~11개월**은 "완성 후 지금까지 상승 지속"이라는 관찰과
  일치한다. 네 종목 모두 2026-06~07 조정에서 정배열이 깨졌다.
- 참고: **MRVL(마벨)은 config 상 AI-GPU가속기 테마**로 분류돼 있어
  광통신 묶음에 없었다. 저장소 주인의 체감 묶음(광통신 4대장)과
  분류가 달랐던 것도 시점 차이의 한 원인이다.

## ③ H15 사전 등록 (데이터를 보기 전에 적음)

**정배열 완성(신규 정의)**: 주봉에서 **4주선 > 13주선 > 26주선 > 52주선**
이 성립하는 첫 주 (직전 주는 미성립). **종가 조건 없음.**

- **신호(H15)** = 종목의 정배열 완성 주에, 그 시점까지 발표된 최신 분기
  **이익 델타 상승**(직전 연속 분기 대비 증가, 잣대 사다리, 140일 신선도)
  이 **동반**된 경우.
- **대조군** = 정배열 완성했으나 델타 상승이 없거나 판단 불가인 경우.
- **표적** = 완성 다음 거래일부터 **250거래일(1년)** SPY 대비 초과수익.
  · 성공 = **+20%p 이상** · 실패(되돌림) = 0 미만
- **보조 지표**: 정배열 **유지 주수**(깨질 때까지) — 신호군이 더 오래
  유지되는가.
- **채택 기준** = 신호군 윌슨 95% 하한 > 대조군 윌슨 95% 상한, n≥10.
- **섹터 판정(H15s)**: 같은 조건을 만족한 종목이 그 섹터에서 **2개 이상
  동시에**(같은 분기 내, 90일 창) 나오면 "섹터 전환 신호". 표적·기준
  동일하되 대상은 섹터 동일가중.
- **우측 검열**: 1년 창이 데이터 끝을 넘는 완성은 세지 않는다.

---
---

# 40차 — **H16 사전 등록**: 바닥 다지기 후 최초 정배열 완성 × 델타 (2026-08-15, 측정 전 박제)

저장소 주인 정정: "**주가가 바닥을 최소 30주 이상 다지고 최초 정배열
완성 시점에 델타 상승이 동반되고 주가가 상승할 때** 주가 상승이 일어난다.
이때 기존 주도 섹터의 정배열이 깨지더라도 다른 섹터들에 저 두 현상이
없다면, 주도 섹터 전환이 아니라 **기존 섹터의 추세 유지**로 판단한다."

**39차가 놓친 것**: 39차는 정배열 완성 **658건 전부**를 셌다. 여기에는
이미 추세 중인 종목이 잠깐 깨졌다 다시 붙는 **재완성**이 대량 섞여 있다.
저장소 주인의 조건은 **긴 바닥을 깨고 나오는 첫 완성**만을 가리킨다 —
전혀 다른 표본이다. 새 가설로 등록한다.

## H16 정의 (이 등록으로 고정)

**정배열 완성** = 주봉 4주선 > 13주선 > 26주선 > 52주선 이 성립하는 첫 주
(직전 주 미성립). 종가 조건 없음 (39차 정정 그대로).

- **바닥 다지기** = 그 완성 **직전 30주 이상 연속으로 정배열이 아니었음**.
  (데이터 시작 직후는 52주 워밍업 뒤부터 세며, 워밍업 이후 계속
  미성립이었다면 그 기간을 그대로 센다.)
- **신호(H16)** = 바닥 다지기 ∧ 그 시점 **이익 델타 상승 동반**
  (그때까지 발표된 최신 분기가 직전 연속 분기보다 큼, 잣대 사다리,
  신선도 140일).
- **대조군 A** = 바닥 다지기 O · 델타 X (델타의 기여를 가름)
- **대조군 B** = 바닥 다지기 X (30주 미만) · 델타 O (바닥의 기여를 가름)
- **기준선** = 정배열 완성 전체 (39차의 658건)
- **표적** = 완성 다음 거래일부터 **250거래일(1년)** SPY 대비 초과수익.
  성공 = **+20%p 이상** · 되돌림 = 0 미만.
- **보조 지표** = 정배열 **유지 주수**(신호군이 더 오래 가는가 — 저장소
  주인의 "지속" 관찰 검증).
- **채택 기준** = 신호군 윌슨 95% 하한 > 기준선 윌슨 95% 상한, n≥10.
- **보조 등록 H16b**: 바닥 문턱을 **52주**로 (더 긴 바닥이 더 나은가).

## H17 — 주도 전환 vs 추세 유지 판정 규칙 (동시 등록)

- **전환 신호** = 어떤 섹터에서 H16 신호가 **같은 분기(90일 창) 안에
  2종목 이상** 발생.
- **판정 규칙** (저장소 주인 정의 그대로): 기존 주도 섹터(직전 6개월
  초과수익 1위)의 정배열 폭이 깨져도, **다른 어느 섹터에도 전환 신호가
  없으면 "추세 유지"로 판단**하고 기존 섹터를 유지한다.
- **검증 방법**: "추세 유지" 판정을 내린 시점들에서, 이후 6개월 동안
  기존 주도 섹터가 실제로 1위를 지켰는지 / 다른 섹터에 추월당했는지.
  그리고 "전환" 판정 시점에서 새 후보 섹터가 실제로 앞섰는지.
- **채택 기준**: 판정 적중률의 윌슨 하한 > 무작위(50%) 상한, n≥10.

## 40차 결과 — H16 미채택이나 **30주 문턱은 실측으로 확인**, 그리고 사례 정정

**측정 장치 정정 먼저 (41차 검증단 발견 — 사고 7 재발)**: `sector_model`
의 연속 분기 간격이 55~**200**일로, 측정 장치(`measure_engine`
SPACING_MAX_DAYS=**150**)와 어긋나 있었다. 200일 창은 반년(182일) 점프를
연속 분기로 통과시켜 **가짜 델타 72쌍**(그중 37쌍은 사이 분기가 존재하는데
잣대값이 비어서 생김)을 만들었다. **150으로 정정 후 아래 숫자를 다시 냈다.**

### 결과 (정정 후, 정배열 완성 658건)

| 그룹 | 성공(+20%p) | 되돌림 | 1년 중앙 | 유지 중앙 | n |
|---|---|---|---|---|---|
| **H16 (바닥 30주+ ∧ 델타↑)** | **36.7%** [25.6,49.3] | 56.7% | -11.6%p | 7주 | 60 |
| 대조A 바닥O·델타X | 30.1% [22.1,39.5] | 58.3% | -4.3%p | 6주 | 103 |
| 대조B 바닥X·델타O | 23.4% [17.8,30.2] | 62.9% | -9.0%p | 9주 | 175 |
| 기준선 (완성 전체) | 27.7% [24.4,31.2] | 59.3% | -8.3%p | 9주 | 658 |
| **무작위 진입 (전 주봉)** | **30.1%** [29.5,30.6] | 54.1% | -3.9%p | — | 24,782 |
| H16b (바닥 52주+) | 38.9% [20.3,61.4] | 55.6% | -13.3%p | 7주 | 18 |

**판정: 미채택** (신호 하한 25.6 < 기준선 상한 31.2). 앞시기 40% ·
뒤시기 33% 로 시기에는 안정적.

### 바닥 길이 구간별 — **30주에서 실제로 문턱이 관측된다**

| 바닥 길이 | 성공률 | n |
|---|---|---|
| 0~9주 | 25.2% | 111 |
| 10~19주 | 23.3% | 43 |
| **20~29주** | **14.3%** ← 최저 | 21 |
| **30~51주** | **35.7%** ← 2.5배 도약 | 42 |
| 52주+ | 38.9% | 18 |

저장소 주인이 지정한 **30주**가 데이터의 실제 분기점과 일치한다.
다만 각 구간 n 이 18~43 으로 얇아 구간이 넓다 — 확정 근거는 아니다.

### 정직하게 남길 세 가지

1. **성공률은 오르지만 중앙값은 마이너스**(-11.6%p)다. 즉 H16 은 "대부분
   이기는 신호"가 아니라 **소수의 큰 성공이 끌어올리는 신호**다
   (상위: AAOI +710 · BE +276 · LRCX +236 · CRDO +107%p).
2. **무작위 진입(30.1%)을 확실히 넘지 못한다.** H16(36.7%)이 점추정으로는
   높지만 구간이 겹친다.
3. **저장소 주인이 지목한 두 종목은 자기 규칙을 만족하지 않는다** (아래).

### 지목 종목 실측 — 규칙과 사례의 불일치

| 종목 | 완성일 | 바닥 | 델타 | H16? | 1년 |
|---|---|---|---|---|---|
| **LITE(루멘텀)** | 2025-07-11 | **21주** | O | **아니오** | **+711.5%p** |
| **CIEN(시에나)** | 2025-07-18 | **19주** | **X** | **아니오** | **+323.8%p** |
| LRCX | 2025-07-03 | 48주 | O | **예** | +236.4%p |
| CRDO | 2025-07-03 | 19주 | O | 아니오 | +164.0%p |

**바닥 30주 규칙을 적용하면 LITE·CIEN 은 걸러진다.** 두 종목의 바닥은
19~21주였고 CIEN 은 델타 상승도 없었다. 규칙이 사례를 설명하지 못한다.

### 지금 신호가 켜져 있으나 1년 창이 미완인 건 (측정 불가 — 관찰만)

- **MU(메모리) 2025-08-15** 바닥 55주 · 델타 O · 유지 **33주** 진행
- **AMAT(반도체장비) 2025-10-03** 바닥 61주 · 델타 O · 유지 **44주** 진행
- **MRVL 2025-12-26** 바닥 44주 · 델타 O · 유지 1주 (곧 깨짐)
- COHR 2025-10-10 바닥 39주 · 델타 **판단 불가**(발표일 없음) · 유지 39주

## H17 (섹터 전환 신호) 결과 — 미달

같은 달 한 섹터에서 H16 2종목 이상 = 10건(24종목). 성공 29.2% ·
중앙 -17.5%p 로 **기준선과 차이 없음**. 성공 사례(2023-03 반도체장비
+59.4%p · 2023-03 하드웨어 +338.8%p)와 실패 사례(2023-06·09 소프트웨어
-26.6·-29.1%p)가 섞인다. **섹터 동시 발생만으로는 전환을 확신할 수 없다.**

---
---

# 41차 — 검증단 반박: 39·40차 보고 숫자 폐기 및 정정 (2026-08-15)

원칙 6(장치 먼저 의심)에 따라 8개 에이전트로 39·40차 백테스트를 독립
재구현·반박 시도했다. **계산 오류·미래 엿보기는 없었으나 보고와 해석에
결함 6건**이 나왔다. 아래는 전부 재실행 출력에서 복사.

## 통과한 것

- 658건·27.7%·되돌림 59.3%·중앙 -8.3%p·유지 중앙 9주가 **4개 팀에서
  독립 재현**. COHR 2025-10-10 / CIEN 2025-07-18 / LITE 2025-07-11
  완성일도 일치.
- **미래 엿보기 0건**(델타 판정 440건 전수 확인) · 가격 구멍 0/160종목 ·
  SPY 정렬 658건 전부 정확히 250거래일 · ISO 주 경계·휴장주·52주 워밍업
  정상 · 워밍업 53주/60주로 바꿔도 0.2%p 이내.

## 폐기·정정해야 할 것

**① 보고 숫자가 고장난 장치의 값이었다.** 39차의 `241/168 ·
27.8%/25.0% · -9.0/-8.4`는 연속분기 간격 **200일**(사고 7 재발 상태)
에서만 재현된다. 150일로 정정하면:

| | n | +20%p | 되돌림 | 중앙 |
|---|---|---|---|---|
| 신호(델타↑) | **235** | **26.8%** [21.6,32.8] | 61.3% | -9.3%p |
| 대조(델타↓) | **156** | **26.3%** [20.0,33.7] | 59.6% | -5.8%p |

**델타 격차 2.8%p → 0.5%p 로 소멸**하고 중앙값 순서는 뒤집힌다.

**② 사전 등록 위반 — 표본의 41%가 보고에서 빠졌다.** 등록문의 대조군은
"델타 없음 **또는 판단 불가**"인데 보고는 '없음'만 썼다. 등록대로면
235 / **423**. 빠진 판단불가군이 셋 중 성적이 가장 좋다(29.3% ·
평균 +21.4%p) — 등록대로 재면 신호군이 **평균에서 진다**(+13.1 vs +16.3%p).

**③ 판단불가군은 무작위가 아니라 종목 편향이다.** 델타를 못 재는 22종목
= `AXP BAC BMY C COF COP CVX ETN GE GS JNJ JPM KLAC LLY MRK MS NKE OXY
PFE SCHW WFC XOM` — **은행·제약·에너지·산업재가 통째로** 빠졌다.
특히 **KLAC 은 adj_eps 21분기가 다 있는데 announced_date 가 0건**
(순수 수집 결함 — 후속 과제). 즉 그 비교는 **기술주 부분우주 안**의
비교였다.

**④ 중앙값만 말하고 평균을 뺐다.** 658건 중앙 -8.3%p 이지만 **평균은
+15.2%p** (5%=-59.3 / 95%=+164.0 / 99%=+491.4). 극단 우편향이라
동일가중 바구니는 SPY를 15%p 이겼다. "지는 쪽이 많다"는 건수로만 맞다.

**⑤ 기준선을 재지 않고 결론을 냈다 (원칙 2 위반).**

| 진입 방식 | n | +20%p | 되돌림 | 중앙 | 평균 |
|---|---|---|---|---|---|
| 무작위(모든 종목·주) | 24,940 | 30.0% | 54.1% | -3.9%p | +17.4%p |
| 정배열 유지 중인 주 | 7,077 | 31.1% | 52.4% | -2.4%p | +17.9%p |
| 정배열 아닌 주 | 17,863 | 29.6% | 54.7% | -4.6%p | +17.2%p |
| **정배열 완성 주** | 658 | 27.7% | 59.3% | -8.3%p | +15.2%p |

개별 종목이 SPY(시총가중) 중앙값에 지는 것은 **정배열과 무관한 상시
현상**이다. "-9.0%p라서 시장에 진다"는 읽기는 틀렸다.

**⑥ 가장 단단한 숫자 — 짝지어 빼면 차이가 0이다.**

| 비교 | 완성 | 대조 | 차이 |
|---|---|---|---|
| 같은 **달** 무작위 | 27.7% | 29.9% | **-2.2%p** |
| 같은 **달·같은 섹터** 무작위 | 27.7% | 27.7% | **-0.0%p** |

**정배열 완성 = 같은 달·같은 섹터에서 아무 종목이나 사는 것과 동일.**
신호 몫은 0에 가깝고 나머지는 전부 기간·유니버스·섹터 효과였다.

## 40차(H16 바닥 30주)도 재평가 — 우연일 가능성이 높다

- 검증단 순위상관: **바닥 길이 vs 1년 초과수익 rho = -0.032**,
  바닥 길이 vs 유지 기간 rho = **-0.014** — 사실상 무관.
- 전체 658건을 바닥 길이로만 가르면 0~9주 26.3% · 10~19주 26.7% 로
  **평평**하다. 40차가 본 30주 도약은 **델타 동반 부분군(n=21~42)** 에서만
  나타났고, 그 크기의 표본에서는 우연이 흔하다.
- **결론: 30주 문턱은 근거로 삼지 않는다.** 관찰로만 남긴다.

---

# 42차 — 검증단이 찾아낸 **실제로 갈리는 변수** (탐색, 등록 아님)

완성 시점에 **미리 알 수 있는** 변수만 전수 검사한 결과 (기준선:
완성 전체 27.7% [24.4,31.2]):

### ① 완성 시점 종가가 52주선 위로 얼마나 떨어져 있나 — 가장 크게 갈림

| 이격도 | n | +20%p | 되돌림 | 중앙 | 평균 |
|---|---|---|---|---|---|
| 0~5% | 39 | 17.9% | 71.8% | -9.7%p | +7.7%p |
| **5~10%** | 100 | **13.0%** | **81.0%** | -15.3%p | -7.6%p |
| 10~20% | 236 | 24.6% | 59.7% | -8.3%p | +1.7%p |
| 20~30% | 155 | 29.7% | 55.5% | -8.3%p | +8.6%p |
| **30% 이상** | 123 | **46.3%** [37.8,55.1] | 41.5% | **+9.1%p** | **+70.5%p** |

5분위 단조: 16.0 / 18.9 / 28.2 / 31.1 / **43.9%**. 판단 불가 0건.

### ② 완성 직전 26주 상대수익 (①과 같은 얘기)

-20~0%p 22.4% · 0~20 27.1% · 20~40 35.7% · **40%p+ 41.8%**.
①과의 교차표: 둘 다 낮은 칸 **13.2%**(n=68) vs 둘 다 높은 칸
**48.8%**(n=86).

### ③ 갈리지 않는 것 (전부 기록)

- **이익 델타의 크기**: 5분위 32.4/15.9/23.1/23.9/25.3% — **비단조**.
  가장 큰 구간이 기준선 아래. 이격도 30%+ 안에서는 델타↑ 33.3% <
  **델타↓ 46.4%** 로 방향이 뒤집힘.
- **바닥 길이**: 위 41차대로 무관.
- **섹터·테마 동시성**: 1개 21.7% → 4개+ 31.1% 로 단조이나 **전부
  기준선 구간 안**. 미래 엿보기 없는 판으로 바꾸면 더 밋밋(28.9%).
- 예외 관찰: **직전 분기값이 적자(≤0)였던 경우** 65.0% [43.3,81.9]
  n=20 · 중앙 +23.3%p. 단 20건 중 13건이 2023년 메모리·광통신 바닥으로
  **단일 국면 표본**이다.

### 창·문턱 격자 24조합 (창 60/125/250/500 × 문턱 +10/20/30 × 초과/절대)

신호 점추정 승 **19/24**, 채택 기준 통과 **0/24**. 짧은 창에서 이기지만
(a) "정배열 무시하고 델타만 보고 아무 주나 사기"에 **네 창 모두 진다**
(b) 종목 군집 부트스트랩하면 0을 포함 (c) 종목당 첫 완성 1건만 쓰면 소멸.

## 저장소 주인 사례 — 반박되지 않는다 (숨기지 않음)

| 종목 | 완성 | 오늘까지 절대 | 초과 | 1년창 |
|---|---|---|---|---|
| LITE | 2025-07-11 | **+854.5%** | +828.6%p | +711.5%p (658건 중 4위) |
| CIEN | 2025-07-18 | +418.1% | +393.0%p | +323.8%p |
| COHR | 2025-10-10 | +184.2% | +165.9%p | **표본 밖(검열)** |
| MRVL | 2025-12-26 | +159.5% | +145.8%p | **표본 밖(검열)** |

- **1년 표적은 주인이 말하는 국면을 구조적으로 못 본다**: 2025-07-01
  이후 완성 273건 중 250일 창이 끝난 것은 **37건**, 500일 창은 **0건**.
- 유지 기간이 결과를 완전히 가르나(4주 이하 17.0% / 27주+ **71.2%**,
  중앙 +36.2%p) **유지 주수는 사후 정보**라 매매 규칙이 못 된다.
  LITE 49주는 **99분위**, CIEN 48주 98.8분위, COHR 39주 97분위 —
  주인의 사례는 전체의 **상위 1~3%** 다. 같은 목록의 MRVL 2025-12-26 은
  **9.6분위(1주 만에 깨짐)** 로 이미 반례가 섞여 있다.

## 다음에 할 것

1. **KLAC 등 22종목 announced_date 수집 결함 복구** (판단불가 편향 제거)
2. 이격도(①)를 새 H번호로 **사전 등록** 후 새 데이터로만 판정
3. 1년 창 결과는 2026-07 이후에야 최근 국면을 포함하기 시작한다

---

# 43차 — 발표일 수집 결함 복구 + **H18 사전 등록** (2026-08-15, 측정 전 박제)

41차가 남긴 두 과제를 처리했다.

## ① 발표일 누락 22종목 — 원인 규명과 수리

**증상**: `AXP BAC BMY C COF COP CVX ETN GE GS JNJ JPM KLAC LLY MRK MS
NKE OXY PFE SCHW WFC XOM` 22종목은 분기 자료가 11~24건씩 있는데
`announced_date`(발표일)가 **0건**이었다. 측정은 발표일로만 전 종목을
줄 세우므로(전략.md 7장) 이 종목들은 측정에서 통째로 빠졌고,
41차가 지적한 "판단 불가군 = 업종 편향"의 정체가 이것이었다.

**원인**(재현 확인): `sec_fundamentals.merge_quarters` 는 XBRL 뼈대가
**비어 있으면** 보도자료 행을 그대로 돌려주는 지름길이 있는데, 그
지름길에서만 **발표일 도장을 찍지 않았다.** 짝짓기 경로와 승격 경로에는
도장이 있었다. 22종목은 전부 XBRL 근사치 행이 0건 —
즉 항상 이 지름길로만 나갔다.

```
재현: merge_quarters([], [8-K 1건]) → announced_date = None   (수리 전)
                                    → announced_date = 8-K 제출일 (수리 후)
```

**수리**: 지름길에서도 8-K 제출일을 발표일로 찍는다.
**시험**: `test_merge_without_xbrl_still_stamps_announced_date` —
수리 전 코드에서 `KeyError: 'announced_date'` 로 **빨간 불 확인 후** 채택.

**남은 것**(다음 로봇 실행에서 확인): 이 22종목은 XBRL 뼈대 자체가 0건이다.
은행·보험은 매출/영업이익 개념 자체가 다르고, KLAC 은 반도체장비인데도
0건이라 별개 원인일 수 있다. 발표일 복구로 **측정 참여는 되지만**,
XBRL 결손은 그대로다 — 고치기 전까지 이 종목들의 잣대는 보도자료에만
의존한다고 적어 둔다.

## ② 정배열 장치가 두 개인 이유 (혼동 방지 기록)

- `aligned_flags` — **33차 등록**(H11·H12·H14 정배열 **폭**).
  조건에 `주봉 종가 > 4주선` 포함. 39차가 이 조건을 "차트 완성"의
  뜻으로는 오류라고 판정했지만, **33차 등록의 장치를 사후에 바꾸면
  사전 등록이 무너지므로 그대로 둔다.**
- `aligned_flags_chart` — **39차 등록**(H15·H16·H18 정배열 **완성**).
  이평선 배열만 본다. 종가 조건 없음.
- 두 장치가 실제로 다르게 움직인다는 것을 시험으로 고정했다
  (`test_chart_device_ignores_close_condition`).
- 완성 사건 전수는 `sector_model.completion_events` 하나로 통일했다 —
  H15·H16·H18 을 같은 잣대로 비교하기 위해서다.

## ③ H18 사전 등록 (데이터를 보기 전에 적음)

42차 탐색에서 **유일하게 크게 갈린 변수**는 완성 시점의 52주선 이격도였다.
탐색은 채택 근거가 아니므로(원칙 5) 새 번호로 등록하고 **새 데이터로만**
판정한다.

- **사건** = 종목의 정배열 **완성** 주 (39차 정의: 4주선 > 13주선 >
  26주선 > 52주선 이 성립한 첫 주, 직전 주 미성립, 종가 조건 없음)
- **신호(H18)** = 그 주 종가가 52주선 위로 **30.0% 이상** 떨어져 있는 경우
- **기준선** = 같은 기간의 완성 사건 **전체**
- **표적** = 완성 다음 거래일부터 **60거래일**(측정 기본형),
  SPY 대비 초과수익. **폭등 = +20%p 이상**
- **채택 기준** = 신호군 윌슨 95% 하한 > 기준선 윌슨 95% 상한, n≥10
- **판정 표본** = **2026-08-15 뒤**에 발생한 완성 사건만.
  그 전 것은 `탐색표본(참고)` 칸에만 넣고 판정에 쓰지 않는다.
- 이격도를 못 잰 사건(52주 이력 부족)과 표적 창이 안 끝난 사건은
  표본에서 뺀다 — 값을 만들지 않는다.

**등록 시점의 예상**: 새 표본은 0건에서 시작한다. n≥10 이 쌓이려면
완성 사건이 10건 이상 생기고 각각 60거래일이 지나야 하므로, 첫 판정은
빨라도 **2026-11 이후**다. 그때까지 화면에는 "판정 불가"로 표시된다.

**참고로 남기는 탐색값**(판정 아님, 42차와 같은 표본을 60거래일 창으로
다시 잰 것 — 실행 출력에서 복사):

| 이격도 | n | +20%p | 중앙 |
|---|---|---|---|
| 0~5% | 54 | 7.4% | -3.1%p |
| 5~10% | 120 | 9.2% | -3.6%p |
| 10~20% | 287 | 5.6% | -1.3%p |
| 20~30% | 193 | 13.0% | -1.1%p |
| **30% 이상** | 192 | **28.6%** [22.7,35.4] | +1.4%p |
| 전체(기준선) | 854 | 13.1% [11.0,15.5] | -1.6%p |

1년 창(42차)과 60거래일 창에서 **방향이 같다**는 것만 확인했다.
같은 표본이므로 독립 증거가 아니다.

## ④ 자동 판정 연결

로봇이 매일 `verdict.json` 에 `H18_완성시_52주선이격도` 를 함께 쓴다.
사람이 다시 재지 않아도 표본이 쌓이는 대로 판정이 바뀐다.

**검증**: `python3 run_tests.py` 종료코드 0 · 238개 통과 0개 실패
(정배열 장치 검증 9개 신규 — 변이 4건으로 빨간 불 확인 후 채택).

---

# 44차 — **주도섹터 모델 사전 등록** (H19·H20·H21, 2026-08-15, 측정 전 박제)

저장소 주인 정의(원문 그대로 옮김):

> "같은 섹터에서 정배열 완성의 종목이 최대한 많이 나오면서 그 시점에
> 이익의 델타 상승이 동반되면 해당 시장의 주도섹터는 이 섹터이고,
> 이 섹터가 정배열이 깨지더라도 다른 섹터가 저 현상이 나타나지 않는다면
> 주도섹터 전환은 아니다."
> "델타 이익의 하락이 발생되면 주도섹터의 주가 상승의 힘이 점점 떨어지며
> 주도섹터가 다른 섹터로 주도를 넘겨줄지 분기점에 다다른다."

이 세 문장을 **기계가 실행할 수 있는 형태**로 옮긴다. 문턱은 데이터를
보기 전에 정하며, 아래 값은 **추론으로만** 고른 것이다 (탐색으로 고르면
그 순간 이 등록은 무효다).

## ① 공통 장치 (기존 등록에서 그대로 가져옴 — 여기서 새로 만들지 않는다)

- **정배열 완성** = 39차 정의. 주봉 4주선 > 13주선 > 26주선 > 52주선이
  성립한 첫 주(직전 주 미성립). 종가 조건 없음. 52주 이력 미만 제외.
- **이익 델타 상승** = 잣대 사다리(조정EPS → 조정EBITDA → GAAP EPS)로
  직전 **연속** 분기 대비 증가. 간격 55~150일. 발표 신선도 140일.
- **묶음**(섹터) = 45차에서 확정하는 새 분류. 4종목 미만 묶음은 판단 불가.

## ② 주도섹터 판정 — H19 (매주 계산)

묶음 g, 주 t 에 대해:

| 값 | 정의 |
|---|---|
| 완성수 C | 최근 **13주**(한 분기) 안에 정배열을 완성한 종목 수 |
| 완성밀도 A | C ÷ (그 묶음의 판단 가능 종목 수) × 100 (%) |
| 델타동반 B | 그 13주 완성 종목 중 **델타 상승을 동반**한 비율 (%) |
| 주도점수 L | A × B ÷ 100 |

**주도 조건** (셋 모두):

1. C ≥ **3종목** — "여러 종목이 함께"라는 말의 최소 조건
2. A ≥ **30.0%**
3. B ≥ **50.0%** — 완성 종목의 과반이 델타 상승 동반

그 주의 **주도섹터** = 조건 셋을 모두 만족하는 묶음 중 **L 이 가장 큰**
묶음. 하나도 없으면 **"주도 없음"** (만들지 않는다).

## ③ 전환 규칙 — H20 (상태 기계, 저장소 주인 규칙 그대로)

- 상태 S = 현재 주도섹터. 초기값 = "없음".
- **S 의 정배열이 깨져도 S 를 내리지 않는다.** 주인 규칙의 핵심이다.
- 전환은 **도전자가 나타날 때만** 일어난다:
  다른 묶음 g 가 ②의 조건 셋을 모두 만족하고
  **L(g) > L(S) 의 직전 13주 최댓값** 이면 S ← g.
- 도전자가 없으면 S 유지 = **"기존 섹터 추세 유지"** 로 판정한다.
- S 가 "없음"일 때는 조건 셋을 만족하는 첫 묶음이 S 가 된다.

**H20 의 표적**: 전환 판정 이후 **60거래일**,
새 주도섹터 동일가중 초과수익 − 옛 주도섹터 동일가중 초과수익 (%p).
주인 규칙이 맞다면 이 값이 **0보다 크게** 나와야 한다.
**성공 = +10%p 이상** (전환이 실제로 주도권 이동이었다는 뜻).

## ④ 분기점 — H21 (델타 하락 = 힘 빠짐)

주도섹터 S 가 유지되는 동안:

- **델타폭 D(S,t)** = S 묶음에서 최근 발표(신선도 140일) 기준
  델타 상승 종목 비율(%). 판단 가능 종목 4개 미만이면 판단 불가.
- **분기점** = D(S,t) 가 그 주도 국면 안의 **직전 최댓값 대비 20.0%p 이상**
  떨어진 **첫** 주.

**H21 의 표적**: 분기점 **직전** 60거래일 S 초과수익 vs
분기점 **직후** 60거래일 S 초과수익.
주인 가설이 맞다면 직후가 직전보다 **낮아야** 한다.
**성공 = 직후 − 직전 ≤ −10%p**.

## ⑤ 채택 기준과 **표본의 한계를 미리 적는다**

- 채택 기준은 헌법 그대로: 신호 윌슨 95% 하한 > 기준선 윌슨 95% 상한, n≥10.
- **그러나 이 등록은 n≥10 에 도달하지 못할 가능성이 매우 높다.**
  주가 자료가 2021-08 부터라 52주 워밍업 뒤 **2022-08 ~ 2026-08 의 4년**만
  볼 수 있고, 그 안의 주도섹터 **국면 전환은 많아야 5~8회**로 예상된다.
  이것은 데이터를 보기 전에 산수만으로 알 수 있는 사실이라 미리 적는다.
- 따라서 H19~H21 은 당분간 **"판정 불가"**로 남을 것이며, 화면에도
  그렇게 적는다. 국면 수가 적다는 이유로 문턱을 낮추거나 표본 단위를
  주(週)로 바꿔 n 을 부풀리는 것은 **금지**한다.
- 주 단위 집계는 참고로만 병기하되, **자기상관 때문에 독립 표본이 아님**을
  반드시 함께 적는다.

## ⑥ 이 등록이 무효가 되는 조건 (미리 적는다)

- 위 문턱(13주 · 3종목 · 30% · 50% · 20%p · ±10%p) 중 **하나라도**
  결과를 보고 바꾸면 이 등록은 무효다. 바꾸려면 새 H번호로 다시 등록한다.
- 분류(묶음)를 결과를 보고 바꿔도 무효다. 분류는 45차에서
  **결과를 보기 전에** 확정한다.

---

# 45차 — **섹터 분류 재설계 확정** (2026-08-15, 측정 전 박제)

저장소 주인 지시: "같은 섹터 내에도 하드웨어와 소프트웨어가 혼동되어 있는
것은 아닌지? 현재 시장은 하드웨어 및 인프라 주도 중이다. 철저하게 섹터
분류의 정의부터 명확하게 하고 분석을 해."

## ① 옛 분류가 실제로 무너져 있었다 (실측)

지금까지 모든 섹터 분석(H11~H14, H17)이 쓴 `SECTORS` 12묶음의 실태:

| 옛 묶음 | 실제 내용물 |
|---|---|
| 하드웨어 20 | 광통신 9 + 데이터센터 5 + 스토리지 5 + PC 1 — **세 수요 사이클** |
| 반도체 22 | AI 가속기 4 + AI 메모리 1 + AI 광통신 2 + **비AI 범용 15** |
| 전력에너지 15 | 데이터센터 전력 8 + 태양광·EV 7 — **2022~2024 정반대로 움직임** |
| 인터넷 15 | AI 인프라 투자자 3 + 소비 인터넷 12 |
| 소프트웨어 21 | 클라우드·데이터 9 + 업무 SW 12 |

이 분류로 44차 모델을 돌리면 **전환 4건 전부 실패**했고, 2025-04 에
"하드웨어 → 산업재" 전환이 잡혔는데 이후 60거래일 성적은
**하드웨어 +45.4%p vs 산업재 +5.7%p** 로 정반대였다.

원인 두 가지:
- **묶음 크기 불균형**: 산업재 8종목은 3종목만 완성해도 밀도 37.5%,
  하드웨어 20종목은 6종목이 필요하다. **작은 묶음이 자동으로 이긴다.**
- **판단불가 편향**: 은행은 발표일 결함(43차에서 수리)으로 델타를 못 재고,
  재는 3~4종목(V·MA·BLK)만으로 100%가 나와 금융이 계속 주도로 뽑혔다.

## ② 재설계 절차 (다중 에이전트, 24개 · 143만 토큰)

CLAUDE.md 5장 규칙대로, **결정적 산수·측정은 한 세션 전수 처리**하고
**분류(도메인 지식·종목별 독립)만** 병렬로 돌렸다.

1. **세 축 독립 설계** — 가치사슬 / 수요동인 / 매출형태. 서로 안 보게 함.
2. **3인 심사** — 통계 검정력 / 미래엿보기 감시 / 실무 투자자 눈.
   각 축 0~25점 + "진 축에서 살릴 것" + "이긴 축의 치명적 결함".
3. **합성** — 이긴 축을 뼈대로, 살릴 것을 접붙이고 결함을 고침.
4. **8조각 배정 + 조각마다 적대적 감사** — 감사관은 기본값이 반박.
   이의 11건이 실제로 반영됐다.
5. **최종 확정** — 4종목 미만 묶음 흡수, 160종목 전수 검산.

## ③ 확정 분류 — 3개 층 · 17묶음

**층을 먼저 가른다**(주인 지시의 핵심). 물건을 인도하고 받는 돈과,
기간·사용량으로 반복 청구하는 돈은 실적 델타의 리듬이 다르다.

| 층 | 묶음 | n | 종목 |
|---|---|---|---|
| 하드웨어·인프라 | **반도체 팹 장비·소재** | 11 | AMAT LRCX KLAC TER ENTG MKSI FORM ACLS ONTO ICHR UCTT |
| 하드웨어·인프라 | **현물가 연동 메모리·저장장치** | 4 | MU SNDK WDC STX |
| 하드웨어·인프라 | **광통신·대역폭 장비·부품** | 7 | CIEN COHR LITE FN AAOI VIAV TTMI |
| 하드웨어·인프라 | **데이터센터 시스템·전력설비 납품** | 5 | ANET SMCI CLS VRT PWR |
| 하드웨어·인프라 | **교체주기 기업 IT 하드웨어** | 8 | INTC CSCO HPQ HPE NTAP EXTR P DELL |
| 하드웨어·인프라 | **기기 OEM 반도체** | 20 | TXN ADI MCHP NXPI MPWR POWI DIOD SLAB LSCC SYNA AMBA MXL SWKS QRVO QCOM NVDA AMD AVGO MRVL CRDO |
| 소프트웨어·서비스 | **좌석·계약 정액 구독 소프트웨어** | 20 | ADBE CRM NOW ORCL TEAM WDAY HUBS BILL DOCU ZM PLTR CRWD ZS OKTA S NET MDB SNOW DDOG TWLO |
| 소프트웨어·서비스 | **광고 플랫폼** | 8 | GOOGL META APP TTD PINS SNAP ZETA U |
| 소프트웨어·서비스 | **소비 거래·결제 플랫폼** | 9 | EBAY ETSY ABNB DASH NFLX V MA AXP PYPL |
| 실물·전통 | **발전·전력 판매** | 4 | VST NRG NEE SO |
| 실물·전통 | **분산형 에너지·전동화 완제품** | 6 | ENPH SEDG RUN BE GNRC TSLA |
| 실물·전통 | **통신·미디어 회선** | 4 | T VZ TMUS CMCSA |
| 실물·전통 | **금융 중개·자본시장** | 9 | JPM BAC WFC C GS MS SCHW BLK COF |
| 실물·전통 | **의약·의료** | 12 | LLY UNH JNJ ABBV MRK PFE AMGN GILD BMY REGN VRTX ISRG |
| 실물·전통 | **소비 최종재·유통** | 15 | WMT COST PG KO PEP MCD SBUX NKE HD LOW TGT EL ROKU AMZN DIS |
| 실물·전통 | **산업 설비·자동화·수송** | 13 | CAT DE HON GE BA UNP UPS FDX IPGP CGNX STRL FSLR ETN |
| 실물·전통 | **에너지 원자재·유전 서비스** | 5 | XOM CVX COP SLB OXY |
검산: 160종목 · 중복 0 · 누락 0 · 최소 4 · 최대 20. `config.GROUPS` 로 동결.

**옛 `SECTORS` 는 지우지 않는다.** 33차 등록(H11~H14)의 장치가 그것을
쓰고 있어, 사후에 바꾸면 사전 등록이 무너진다. 화면의 업종 표시용으로 남긴다.

## ④ 이 분류의 **약점을 미리 적는다** (감추지 않음)

1. **묶음 두 개가 20종목까지 커지면서 성격이 다른 하위 묶음을 품었다.**
   - `기기 OEM 반도체 20` 안에 **데이터센터 실리콘 5**(NVDA AMD AVGO MRVL CRDO)
   - `좌석·계약 정액 구독 20` 안에 **사용량 과금 5**(SNOW DDOG MDB NET TWLO)

   감사관이 "시간 안정성" 규칙(2016년에 배정해도 같은 답이어야 한다)을
   들어 NVDA·AMD 를 기기 OEM 으로 옮긴 결과다. **규칙상 맞지만, 지금
   시장에서 데이터센터 수요와 스마트폰·자동차 수요는 완전히 다른 동인**
   이라는 사실은 그대로 남는다.

   → **처리**: 확정표(17묶음)를 **주 분류**로 쓰고 H19~H21 은 이것으로만
   판정한다. 하위 태그를 갈라 낸 19묶음(`GROUPS_FINE`)은 **감도 분석**으로
   병기하되 **채택 근거로 쓰지 않는다.** 어느 쪽이 좋아 보이는지 보고
   고르는 것은 금지다 — 그래서 **지금, 측정 전에** 주/보조를 못 박는다.

2. **경계 표시 60종목.** 감사에서 이의가 남았거나 매출 비중을 공시로
   확인하지 못한 종목이다. 지어내지 않고 표시만 한다.

3. **시간 안정성이 실제로 깨지는 종목이 있다**: ONTO(2019 합병 신설)
   APP(2025 게임 매각) HPE(2017 분사) T(2018·2021) TMUS(2020 합병)
   VST(2016 회생) JNJ(2023 켄뷰 분사), 그리고 PINS·ABNB·DASH·S 는
   상장 자체가 늦다. 과거 구간의 묶음 크기가 그만큼 틀린다.
   지금은 **판단 가능 종목 수를 주마다 다시 세는 것**으로만 완화하고 있다
   (52주 이력이 없으면 분모에서 빠진다). 완전한 해결은 아니다.

4. **감사관이 요구한 "일부 종목 분모 제외"는 구현하지 않았다.**
   근거가 "매출 비중 50% 미확인"인데, 확인 못 한 것을 빼면 남는 것만으로
   비율을 재게 되어 오히려 편향이 커진다. 확인될 때까지 **전부 분모에
   넣고 경계로 표시**한다.

5. **재배정 규칙이 아직 없다.** 회사 사업 구성이 실제로 바뀌었을 때 언제
   옮길지는 비어 있다. 사후에 정하면 미래 엿보기이므로, 다음 등록에서
   "연 1회, 직전 회계연도 매출 비중이 2년 연속 50%를 넘긴 경우에만,
   그 다음 해 첫 거래일부터 적용"처럼 기계적으로 정한다.

---

# 46차 — 주도섹터 모델 **첫 측정** + 장치 결함 2건 수리 (2026-08-15)

44차 등록(H19·H20·H21)을 45차 분류로 처음 돌렸다. **문턱은 하나도
바꾸지 않았다.** 대신 원칙 6에 따라 결과가 이상한 곳에서 장치를 먼저
의심했고, 실제 결함 2건이 나왔다.

## ① 수리 1 — 조정 EBITDA 단위 미환산 **85건 · 10종목**

`BE CIEN CMCSA ENTG ETSY GNRC PINS PWR SNAP TTMI`.
25차 감사에서 영업이익 칸에 넣은 단위 가드가 **EBITDA 칸에는 없었다.**

가장 아픈 실물 — CIEN(시에나):

| 발표일 | 저장된 조정 EBITDA | 실제 |
|---|---|---|
| 2025-09-04 | 157,962,000 | 1.58억 |
| 2025-12-11 | **205,536** | 2.06억 (천 달러 표기) |
| 2026-03-05 | **287,276** | 2.87억 |

저장값으로 읽으면 **99.9% 급감**이지만 실제로는 **증가**다. 이 오염이
광통신 묶음의 "이익 델타"를 통째로 뒤집어 주도섹터 판정을 틀리게
만들고 있었다. 고치지 않고 버린다(창작 금지).
시험 `test_adjusted_ebitda_unit_mismatch_is_dropped` — 변이 2건으로
빨간 불 확인 후 채택.

## ② 수리 2 — 델타동반 비율의 **분모 붕괴**

판단 불가를 분모에서 빼는 것은 맞다. 그런데 **뺀 뒤 남은 것이 너무
적으면 그것도 없는 사실을 만드는 것**이다. 실물:

> 금융 묶음 2025-07-11: 완성 **9종목**, 그중 델타를 잴 수 있는 것은
> **3종목**뿐(BAC·WFC·C·GS·BLK·COF 는 EPS 값 자체가 수집 안 됨).
> 그 3종목이 전부 상승 → **"완성 종목의 100%가 델타 동반"**으로 찍혔고,
> 그 힘으로 금융이 주도섹터를 차지했다.

→ 판단 가능한 완성이 `MIN_COMPLETIONS`(3) 미만이면 **판단 불가**로 둔다.
이 파일의 다른 모든 곳과 같은 규칙이며, 조건을 **더 엄격하게만** 만든다.
시험 `test_share_needs_enough_decidable_completions` — 빨간 불 확인 후 채택.

**남은 결함(수리 못함)**: 잣대 사다리를 못 넘는 종목이 **24개**다.
`AXP BAC BLK C CAT COF COST DE GS ISRG NFLX PEP PFE PG RUN SNDK SNOW
STRL TXN VRTX VST WFC XOM CLS`. 발표일은 43차에서 복구했지만 **EPS 값
자체가 없다**(BAC 24분기 중 0건, GS 0건). 금융 9종목 중 6종목이 여기
해당한다. 이 종목들이 든 묶음의 델타는 소수로만 재는 셈이다.

## ③ H19 — 주도섹터 흐름 (주 분류 17묶음, 문턱 변경 없음)

| 주 | 주도섹터 | 점수 | 완성 |
|---|---|---|---|
| 2022-11-04 | 의약·의료 | 43.8 | 7종목 |
| 2023-06-16 | 교체주기 기업 IT 하드웨어 | 50.0 | 4종목 |
| 2024-05-03 | 통신·미디어 회선 | 75.0 | 3종목 |
| 2025-07-11 | **금융 중개·자본시장** | 100.0 | 9종목 |
| (2026-08-14 현재) | **금융 중개·자본시장** | — | — |

## ④ H20 — 전환 3건, **성공 0건**

| 전환 | 새 주도 60일 | 옛 주도 60일 | 차이 | 성공(≥+10%p) |
|---|---|---|---|---|
| 2023-06-16 의약→교체주기 IT | +5.5 | +0.5 | +4.9 | ✗ |
| 2024-05-03 교체주기 IT→통신회선 | +5.3 | +1.9 | +3.4 | ✗ |
| 2025-07-11 통신회선→금융 | −1.9 | −11.7 | +9.8 | ✗ |

**0/3 · 판정 불가(n<10)** — 44차에서 미리 적은 대로다. 다만 **세 번 모두
차이가 양수**였다(새 주도가 옛 주도보다 나았다). 방향은 맞았고 크기가
문턱에 못 미쳤다. 표본 3건이라 아무 의미도 부여하지 않는다.

## ⑤ H21 — 분기점 3건, 성공 1건

| 분기점 | 묶음 | 델타폭 | 직전 60일 | 직후 60일 | 차이 |
|---|---|---|---|---|---|
| 2023-02-03 | 의약·의료 | 57.1→25.0 | −11.5 | +4.6 | +16.2 ✗ |
| 2024-03-01 | 교체주기 IT | 85.7→37.5 | +5.3 | +2.8 | −2.5 ✗ |
| 2025-04-25 | 통신·미디어 회선 | 75.0→50.0 | +10.3 | −11.2 | **−21.5 ✓** |

**1/3 · 판정 불가(n<10).**

## ⑥ **가장 중요한 발견** — 정배열이 먼저, 델타가 한 분기 뒤였다

저장소 주인이 지목한 광통신 국면(2025-07~10)을 그대로 재보면:

- **완성 조건은 넘치게 만족했다**: 완성 3~5종목, 밀도 42.9~71.4%
- **델타 조건에서 계속 탈락했다**: 동반 33.3% → 25% → 20% → 0%

왜인지 종목별로 파고들었다. **완성 시점의 직전 발표는 대부분 QoQ 감소였고,
델타 상승은 완성 뒤에 왔다**:

| 종목 | 정배열 완성 | 완성 시 델타 | 델타 상승 발표 | 시차 |
|---|---|---|---|---|
| TTMI | 2025-06-13 | 하락 | 2025-07-30 | **+47일** |
| LITE | 2025-07-11 | 상승 | (이미 상승) | 0 |
| CIEN | 2025-07-18 | 하락 | 2025-09-04 | **+48일** |
| FN | 2025-08-08 | 하락 | 2025-08-18 | **+10일** |
| VIAV | 2025-08-29 | 하락 | 2025-10-29 | **+61일** |
| VIAV | 2025-10-24 | 하락 | 2025-10-29 | +5일 |
| TTMI | 2026-04-10 | 상승 | (이미 상승) | 0 |

**즉 주인의 "동시에"라는 요구가 이 국면에서는 성립하지 않았다.**
주가의 정배열이 **먼저** 완성되고, 이익 델타는 **5~61일 뒤** 다음 발표에서
돌아섰다. 시장이 실적을 선반영한 것으로 읽힌다.

이것은 주인 가설의 **반증이 아니라 시점 수정**이다. "완성과 델타가 동시"가
아니라 "완성 → 다음 실적에서 델타 확인"이 실제 순서였다.

**이 관찰은 채택 근거가 아니다**(원칙 5 — 탐색). 새 번호로 등록한다.

## ⑦ H19b 사전 등록 — **완성 후 확인형** (데이터를 더 보기 전에 적음)

- **신호** = 묶음의 최근 13주 정배열 완성이 3종목 이상 ∧ 밀도 30% 이상인
  상태에서, 그 완성 종목들의 **완성 이후 첫 발표**에서 델타 상승이
  과반이 된 **첫 주**.
- **진입 시점** = 그 주의 다음 거래일 (완성일이 아니라 **확인일**).
  사후 정보가 아니다 — 확인일에 실제로 알 수 있다.
- **표적** = 60거래일 묶음 동일가중 초과수익. 성공 = +10%p 이상.
- **기준선** = 같은 주에 조건을 만족하지 못한 모든 (묶음, 주).
- **채택 기준** = 헌법 그대로(윌슨 하한 > 상한, n≥10).
- **판정 표본** = 2026-08-15 **뒤**에 생기는 확인만. 그 전은 참고.
- H19 는 폐기하지 않는다. 둘을 나란히 재서 어느 시점이 맞는지 본다.

## ⑧ 이번 측정이 **말할 수 없는 것** (미리 적음)

- 전환 3건·분기점 3건으로는 아무것도 채택할 수 없다. 44차에서 예고한 대로다.
- 지금 모델은 **금융을 주도로 지목**하지만, 그 금융 9종목 중 6종목은
  델타를 잴 수 없다. 이 판정은 **데이터 결함 위에 서 있다.**
  EPS 수집을 고치기 전까지 이 결론을 믿어서는 안 된다 — **다음 과제 1순위.**
- 하드웨어·인프라 층은 이 4년 창에서 한 번도 주도로 잡히지 않았다
  (교체주기 기업 IT 가 2023년에 한 번). 광통신은 **완성은 했으나 델타
  조건에서 탈락**했다. 이것이 규칙의 결과이며, 규칙을 바꿔 맞추지 않는다.

**검증**: `python3 run_tests.py` 종료코드 0 · **252개 통과 0개 실패**
(신규 3개 — 변이 3건으로 빨간 불 확인 후 채택).

---

# 47차 — EPS 파서 구멍 수리 + 주도섹터 판정 로봇 연결 (2026-08-15)

46차가 "다음 과제 1순위"로 남긴 것을 처리했다.

## ① 잣대 사다리를 못 넘던 24종목 — 원인은 **라벨 한 줄**

`AXP BAC BLK C CAT CLS COF COST DE GS ISRG NFLX PEP PFE PG RUN SNDK
SNOW STRL TXN VRTX VST WFC XOM`.

발표일은 43차에서 복구했는데 **EPS 값 자체가 없었다.** 원문을 열어 보니:

> TXN 2026-07-22: "…reported second quarter revenue of $5.46 billion,
> net income of $1.98 billion and **earnings per share of $2.14**."

GAAP EPS 라벨 다섯 개는 전부 **GAAP · net · diluted 중 하나를 요구**했다.
`earnings per share` 를 **아무 수식어 없이** 쓰는 회사(TXN·은행권 등)의
문장이 통째로 빠져나가고 있었다.

**수리**: 수식어 없는 `{이익낱말} per share` 라벨을 목록 **맨 뒤**에 추가.
`adjusted / non-GAAP earnings per share` 는 기존의 같은 줄 되돌아보기가
막아 준다(이 목록은 항상 그 옵션과 함께 쓰인다).

**효과 (실행 출력에서 복사)**: 저장된 **파싱 실패 원문 316건 중 98건**에서
이제 EPS 를 읽는다 — **62종목**. 그중 46차가 지목한 24종목의 상당수가
들어 있다(AXP C JPM MS SCHW WFC XOM PG PFE TXN SNDK SNOW …).

**왜 맨 뒤여야 하는가 — 전수 비교로 확인**: 라벨 순서만 바꿔 원문 120건을
다시 파싱했더니 **3건에서 값이 달라졌다**. 결정적 실물은 HD 2023-11-14:

```
Basic earnings per share       $3.83     ← 앞에 두면 이걸 문다 (오답)
Diluted earnings per share     $3.81     ← 뒤에 두면 이걸 문다 (정답)
```

실적표는 Basic 이 Diluted **위**에 오므로, 수식어 없는 라벨이 앞서면
Basic 을 먼저 물어 버린다.

**시험 3개 · 변이 3건으로 빨간 불 확인 후 채택**:
`test_bare_earnings_per_share_is_read`(TXN 실물 문장 2.14) ·
`test_bare_pattern_does_not_steal_nongaap`(adjusted·non-GAAP 은 안 잡힘) ·
`test_specific_labels_still_win_first`(위 HD 실물 — 처음 쓴 판은 변이를
넣어도 빨간 불이 안 떠서 **가짜 초록불**이었고, 실물로 다시 썼다).

**남은 오독 1건 (숨기지 않음)**: AMZN 은 순서와 무관하게 틀린다.
2026-04-29 원문의 정답은 희석 EPS **$2.78** 인데 파서는 1.86(추세표의
다른 열)을 집는다. 여러 분기가 가로로 늘어선 표는 아직 못 읽는다.

> **주의**: 이 수리는 다음 로봇 실행부터 적용된다. 지금 저장된 snapshot 은
> 아직 옛 파서로 만든 것이므로, 46차의 주도섹터 결과는 **여전히 결함 위에
> 서 있다.** 다음 수집이 끝난 뒤 다시 재야 한다.

## ② H19·H20·H21 을 로봇에 연결 (원칙 3의 이행)

45차 확정 분류(`config.GROUPS`)로 매일 `verdict.json` 에 함께 쓴다.
사람이 다시 재지 않아도 국면이 쌓이는 대로 판정이 바뀐다.

판정 규칙은 44차 등록 그대로다. **국면 수가 적다는 이유로 문턱을 낮추거나
표본 단위를 주(週)로 바꿔 n 을 부풀리지 않는다** — 이것을 시험으로 고정했다
(`test_leadership_records_facts_without_forcing_a_verdict`).

현재 기록되는 값 (실행 출력에서 복사):

| 가설 | 값 | 판정 |
|---|---|---|
| H19 현재 주도 | 금융 중개·자본시장 (2026-08-14 기준) · 국면 4개 | 판정 불가 |
| H20 전환 | n=3 · 성공 0 · 0.0% [0.0, 56.2] | 판정 불가 |
| H21 분기점 | n=3 · 성공 1 · 33.3% [6.1, 79.2] | 판정 불가 |

채택 조건은 **윌슨 하한 > 50%** 이며 점추정이 아니다. 12건 중 7건 성공
(점추정 58.3%, 하한 32.0%)이 **미채택**임을 시험으로 박았다
(`test_leadership_can_adopt_only_with_enough_episodes`).

## ③ 화면 (정직화)

H19·H20·H21 을 계기판의 미채택·대기 칸에 쉬운 말 설명과 함께 넣었다.
H19 설명에는 **"지금 지목된 묶음도 델타를 못 재는 종목이 섞여 있어 그대로
믿으면 안 된다"** 를 그대로 적었다.

## ④ 남은 것

1. 다음 로봇 실행 뒤 **46차 측정을 다시** 한다 (파서 수리가 반영된 자료로).
2. H19b(완성 후 확인형, 46차 ⑦ 등록)는 아직 구현 전이다.
3. 컨테이너가 두 번 재생성되며 작업본이 옛 커밋으로 돌아간 일이 있었다.
   원격(브랜치)에 커밋해 둔 덕에 손실은 없었다 — **작업 단위마다 커밋·푸시**
   규칙(CLAUDE.md 1장)이 실제로 작동했다.

---

# 48차 — 파서 수리 후 재측정 + H19b 구현 (2026-08-15)

47차가 "다음 로봇 실행 뒤 다시 재라"고 남긴 것을 처리했다.

## ① 파서 수리의 실제 효과

| | 47차 전 | 47차 후 |
|---|---|---|
| 잣대 사다리 미달 종목 | 24개 | **20개** (AXP·C·PEP·TXN 회수) |
| 금융 묶음 델타폭 | **판단 불가** | **100.0** |

## ② 재측정 — **결론은 바뀌지 않았다**

| | 46차 (결함 자료) | 48차 (수리 자료) |
|---|---|---|
| H19 현재 주도 | 금융 중개·자본시장 | **금융 중개·자본시장** (동일) |
| H19 국면 | 4개 (의약→교체주기IT→통신회선→금융) | **동일** |
| H20 전환 | 3건 · 성공 0 | **3건 · 성공 0** (동일) |
| H21 분기점 | 3건 · 성공 1 | 4건 · 성공 1 |

새로 잡힌 분기점 1건: **2025-07-18 금융** (델타폭 100.0→50.0,
직전 +9.7 → 직후 +0.2, 차이 −9.5%p) — 성공 문턱(−10%p)에 0.5%p 못 미쳤다.

46차에서 "이 결론은 데이터 결함 위에 서 있다"고 적었는데, **수리 후에도
같은 결론이 나왔다.** 경고는 유지할 필요가 없어졌다. 다만 잣대를 못 넘는
종목이 아직 20개이므로 완전히 깨끗한 것은 아니다.

## ③ H19b 구현 — **완성 후 확인형** (46차 ⑦ 등록의 이행)

한 묶음이 완성 무리(최근 13주 완성 3종목+ ∧ 밀도 30%+)일 때, 그 완성
종목들의 **완성 이후 첫 발표**에서 델타 상승이 과반이 되는 **첫 주**를
신호로 잡는다. 완성일에는 몰라도 **확인일에는 실제로 알 수 있는** 값만 쓴다.

**저장소 주인이 지목한 국면을 실제로 잡는다**:

| 확인 주 | 묶음 | 완성 | 확인 | 60거래일 |
|---|---|---|---|---|
| 2025-08-22 | **광통신·대역폭 장비·부품** | 4종목(57.1%) | 3/3 | **+49.8%p** |
| 2024-11-08 | 광통신·대역폭 장비·부품 | 5종목(71.4%) | 3/4 | +0.8%p |

**그러나 전체로 보면 기준선보다 나쁘다** (탐색 표본, 판정 아님):

| | +10%p 이상 | 중앙 | n |
|---|---|---|---|
| 신호(확인이 선 주) | **14.0%** [6.6, 27.3] | −2.4%p | 43 |
| 기준선(그 외 모든 주) | **24.2%** [22.7, 25.7] | +0.8%p | 3,123 |

광통신 +49.8%p 는 **43건 중 6건뿐인 성공 사례**다. 눈에 띄는 사례를
근거로 쓰면 안 된다는 것을 이 숫자가 그대로 보여 준다.

**판정 표본**(2026-08-15 뒤) = **0건 → 판정 불가.** 등록대로다.

## ④ 지금까지의 종합 — 저장소 주인 가설의 현재 상태

세 판을 모두 재봤고, **어느 것도 기준선을 이기지 못했다**:

| 판 | 무엇을 쟀나 | 탐색 결과 |
|---|---|---|
| H19 (동시) | 완성 무리 ∧ 델타 동반 | 전환 0/3 |
| H19b (확인 후) | 완성 무리 → 다음 실적에서 델타 확인 | 14.0% vs 기준선 24.2% |
| H21 (분기점) | 델타 꺾임 뒤 힘 빠짐 | 1/4 |

이것은 **가설이 틀렸다는 증명이 아니다.** 표본이 국면 단위라 아무것도
채택할 수 없는 상태이며(44차 ⑤에서 미리 적음), 잣대를 못 넘는 종목이
20개 남아 있다. 다만 **지금 자료로는 이 규칙을 쓸 근거가 없다**는 것이
정직한 현재 상태다. 로봇이 매일 다시 세므로 표본이 쌓이면 바뀔 수 있다.

## ⑤ 구현 검증

시험 3개 · **변이 4건으로 빨간 불 확인 후 채택**:
`test_confirmation_needs_announcement_after_completion`(완성 전 발표를
세면 실패) · `test_confirmation_fires_once_per_cluster` ·
`test_confirmation_requires_majority_and_enough_decidable`.

로봇이 매일 `verdict.json` 에 H19b 를 기준선과 함께 기록한다.
화면에는 **"지난 자료에서는 신호 14.0%로 기준선 24.2%보다 나빴다"**를
그대로 적었다(정직화).

---

# 49차 — 화면 412px 실렌더 검증 + 미채택 칸 오표시 수리 (2026-08-15)

CLAUDE.md 2장은 **화면 관련 작업은 모바일 폭 412px 실렌더로 확인하고
커밋**하라고 정한다. 46~48차에서 화면 글(H19·H19b·H20·H21 설명)을
넣고도 이 확인을 건너뛰었다. 뒤늦게 이행하면서 결함 1건을 찾았다.

## ① 찾은 결함 — 국면 가설이 "표본이 아직 없습니다"로 뭉개졌다

미채택 칸은 `신규(판정) → {신호, 기준선}` 모양만 읽는다. 그런데
가설마다 결과의 모양이 다르다:

| 가설 | 결과 모양 | 옛 화면 표시 |
|---|---|---|
| H2~H18 | `신규(판정)` 안에 신호·기준선 | 정상 |
| H19 | 현재 주도·국면 수 (성공률이 **없는** 가설) | **"표본이 아직 없습니다"** ✗ |
| H20·H21 | 사건 성공/전체 (n=3) | **"표본이 아직 없습니다"** ✗ |
| H19b | 판정 표본 0 + 탐색 표본 43 | **"표본이 아직 없습니다"** ✗ |

n=3 이 있는데 "표본이 아직 없다"고 적는 것은 사실과 다르다.
`signal_summary` 를 만들어 모양별로 그대로 적게 했다:

- H19 → `현재 지목: 금융 중개·자본시장 · 국면 4개 (성공률 아님)`
- H20 → `실측 0/3건 = 0.0%`
- H19b → `판정 표본 0건 — 탐색 표본에서는 14.0% (n=43)` + 기준선 병기

시험 4개 · **변이 4건으로 빨간 불 확인 후 채택**.

## ② 412px 실렌더 확인 (실행 출력에서 복사)

```
가로폭: {doc: 412, win: 412}        ← 가로 스크롤 없음
가로로 넘친 말단 요소: 없음
details 개수: 3 · 펼친 칸: 3 · 본문 4,662자 → 7,449자
'H19)': 1회 · 'H20': 1회 · 'H21': 1회 · '현재 지목': 1회
```

- 가로 넘침·열 가림 **없음**.
- 화면 캡처로 글자 잘림 없음 확인(상단 + 미채택 칸).
- **Streamlit 은 아래쪽을 늦게 그린다**: 처음 캡처에서 H19~H21 이
  0회로 나와 "화면에 안 나온다"고 오해할 뻔했다. 휠로 끝까지 내려
  그리게 한 뒤에야 나타났다. 다음 세션을 위해 적어 둔다.
- H19b 는 아직 화면에 없다 — 지금 저장된 `verdict.json` 은 47차 코드로
  만든 것이라 H19b 가 들어 있지 않다. **다음 로봇 실행부터** 나온다.

## ③ 도구 사고 1건 (다음 세션이 다시 겪지 않도록)

`pkill -f "streamlit run"` 은 **자기 자신을 죽인다.** 그 명령을 실행하는
셸의 명령줄에도 "streamlit run" 이 들어 있어 패턴에 걸린다(종료코드 144
로 뒤따르는 명령이 통째로 사라졌다). `pkill -f "streamlit[ ]run"` 처럼
자기 자신과 안 겹치게 쓴다.

---

# 50차 — 은행·금융 EPS 표기 3종 수리 (2026-08-15)

47차가 24종목 → 20종목으로 줄였지만, 남은 20종목의 절반이 대형 은행이었다.
48차 재측정에서 **금융이 현재 주도섹터로 지목**되는데 그 9종목 중 5곳의
EPS 값이 없는 상태라 원문을 열어 봤다.

## ① 못 읽던 표기 세 가지 (전부 실물)

| 회사 | 원문 | 왜 못 읽었나 |
|---|---|---|
| **GS** | `Diluted earnings per common share (EPS)1 was $20.98` | `common` 이 라벨에 없음 |
| **WFC** | `Diluted earnings per common share  2.00` | 〃 |
| **COF** | `net income of $3.0 billion, or $4.73 per diluted common share` | 숫자가 이름 **앞** |
| **BLK** | `EPS of $12.19, or $13.91 as adjusted` | 이름 없이 `EPS of` |

은행·보험은 우선주 배당을 뺀 **보통주** 기준으로 적기 때문에
"per **common** share" 를 쓴다. 이 한 낱말이 없어서 대형 은행이 통째로
측정에서 빠져 있었다.

## ② 수리

1. **`common` 허용** — 라벨의 `share` 앞에 `(?:common\s+|ordinary\s+)?` 를
   둔다. GS·WFC·COF 를 한 번에 덮는다.
2. **`EPS of` 라벨 추가** — 맨 `EPS` 는 각주 표시(GS `EPS1`)나
   `EPS Impact`(COF) 까지 물어 엉뚱한 숫자를 집으므로 **`of` 가 붙은
   형태로만** 좁혔다.
3. **`find_eps_before_per_share`** — "…or $4.73 per diluted common share"
   처럼 **숫자가 이름 앞**에 오는 문장 전용. 이름 뒤 탐색이 실패한
   뒤에만 쓴다. 희석을 먼저 쓰고 `basic` 은 쓰지 않으며, 논갭·전망
   문맥은 기존 규칙 그대로 막는다. 괄호 `$(8.58)` 는 음수로 읽는다.

## ③ 효과 (실행 출력에서 복사)

```
실패 원문 316건 중 EPS 회수: 145건 · 87종목   (47차 직후 98건·62종목)
잣대 미달 20종목 중 회수: BAC 2 · BLK 2 · COF 2 · COST 2 · DE 2 · GS 2 ·
  ISRG 2 · PFE 1 · PG 1 · RUN 2 · SNDK 1 · SNOW 1 · STRL 3 · VRTX 1 ·
  WFC 2 · XOM 1   → 16/20 종목에서 회수
아직 0건: CAT · CLS · NFLX · VST
```

**다음 로봇 실행부터 반영된다.** 그 뒤 48차 측정을 다시 해야 한다 —
금융 9종목 중 5곳의 델타가 새로 잡히면 주도섹터 판정이 바뀔 수 있다.

## ④ 검증

시험 5개 · **변이 5건으로 빨간 불 확인 후 채택**:
`test_per_common_share_is_read` · `test_bare_eps_of_is_read_but_footnotes_are_not`
(각주 `EPS1`·`EPS Impact` 를 안 무는지도 함께) ·
`test_eps_before_per_share_phrase`(적자 괄호 포함) ·
`test_eps_before_prefers_diluted_and_skips_basic` ·
`test_eps_before_blocks_nongaap_and_forecast`.

`python3 run_tests.py` 종료코드 0 · **269개 통과 0개 실패**.

---

# 51차 — 은행 EPS 회수 후 재측정 + 상태기계 창 정정 (2026-08-15)

50차 수리가 로봇에 반영된 자료로 48차를 다시 쟀다. **결과가 크게 바뀌었다.**

## ① 잣대 미달 20종목 → **9종목**

`CAT CLS DE NFLX PG SNDK SNOW VRTX VST` 만 남았다.
**금융 9종목은 전부 델타를 잴 수 있게 됐다.**

## ② 48차의 "금융이 주도"는 **데이터 결함이 만든 것이었다**

| 금융 2025-07-11 | 48차 (은행 5곳 EPS 없음) | 51차 (전부 회수) |
|---|---|---|
| 완성 | 9종목 | 9종목 |
| 델타동반 | **100.0%** (잴 수 있는 3종목만) | **44.4%** (9종목 전부) |
| 조건충족 | **참** | **거짓** (과반 미달) |

즉 은행 9곳 중 실제로 델타가 오른 곳은 **4곳**이었는데, 잴 수 있는 3곳이
전부 상승이라 100%로 찍혔다. 46차에 넣은 "판단 가능 3건 미만이면 판단
불가" 규칙도 이 경우는 못 막았다(잴 수 있는 것이 정확히 3건이었다).
**결함을 고치니 금융은 주도 조건을 못 넘는다.**

## ③ 장치 정정 — "직전 13주"는 **시간**이다

전환 규칙의 `L(S) 직전 13주 최댓값`을 구현이 "최근 **판단 가능한** 13개
점수"로 세고 있었다. 판단 불가 주에 자리를 안 채워서, 현 주도가 오래
판단 불가여도 옛 점수가 만료되지 않았다.

**실측**: 데이터센터 묶음이 10주 넘게 완성 0건인데도 옛 점수 60.0 이
살아남아 도전자(의약 33.3)를 계속 막고 있었다. 44차 등록문의
"직전 13주"는 시간 표현이므로 **판단 불가 주도 자리를 채우도록** 고쳤다.

- 시험 `test_incumbent_peak_expires_after_13_weeks_of_no_data` —
  옛 구현으로 되돌리는 변이에서 빨간 불 확인 후 채택.
- **정정 전 결과도 남긴다**: 전환 1건(0/1) · 국면 2개.
  정정 후: 전환 18건 · 국면 19개. 차이가 크므로 숨기지 않는다.
- 이 정정은 "주도는 스스로 내려오지 않는다"는 주인 규칙을 **약화시키는**
  방향이다(오래 잴 수 없으면 문턱이 0이 된다). 결과를 좋게 만들려고
  고른 방향이 아님을 밝혀 둔다.

## ④ H19 — 주도섹터 흐름 (19국면)

| 시점 | 주도섹터 |
|---|---|
| 2022-10-28 | 에너지 원자재·유전 서비스 |
| 2023-06-30 | 교체주기 기업 IT 하드웨어 |
| 2023-11-24 | 좌석·계약 정액 구독 소프트웨어 |
| 2024-02-09 | 금융 중개·자본시장 |
| 2024-08-30 | 데이터센터 시스템·전력설비 납품 |
| 2024-12-27 | 교체주기 기업 IT 하드웨어 |
| 2025-09-26 | 통신·미디어 회선 |
| 2026-01-30 | 기기 OEM 반도체 |
| **2026-04-17** | **데이터센터 시스템·전력설비 납품** |
| **2026-07-17** | **기기 OEM 반도체** ← 현재 |

(중간 국면 생략 — 전체는 실행 출력에 있다)

## ⑤ H20 — 전환 18건 중 표적 잴 수 있는 17건, **성공 6건**

**35.3% [17.3, 58.7] · n=17 → 판정: 미채택** (하한 17.3 < 50).

n≥10 을 처음으로 넘었다. 44차 ⑤에서 "n≥10 에 도달하지 못할 가능성이
매우 높다"고 적었는데, 창 정정으로 국면이 잘게 쪼개져 도달했다.
**표본이 늘어난 것이지 신호가 좋아진 것이 아니다** — 판정은 미채택이다.

성공한 6건: 2023-04 팹장비(+31.7) · 2023-11 구독SW(+15.7) ·
2024-02 금융(+25.7) · 2024-08 데이터센터(+27.3) · 2025-08 산업설비(+19.0) ·
2026-01 기기OEM반도체(+43.1).
가장 크게 틀린 것: 2026-02 기기OEM→에너지 전환(옛 주도가 +66.3,
새 주도 −8.2 → **−74.5%p**).

## ⑥ H21 — 분기점 3건 중 1건 성공, **판정 불가**(n=3)

그런데 **가장 최근 사례가 주인 가설대로 움직였다**:

| 분기점 | 묶음 | 델타폭 | 직전 60일 | 직후 60일 |
|---|---|---|---|---|
| **2026-05-08** | 데이터센터 시스템·전력설비 | 75.0 → 50.0 | **+16.6** | **−9.9** (−26.5%p) |

그리고 **그 뒤 2026-07-17 에 실제로 주도가 바뀌었다**(데이터센터 →
기기 OEM 반도체). 주인이 말한 "델타가 꺾이면 힘이 빠지고 주도를 넘겨줄
분기점에 다다른다"는 순서가 **최근 국면에서 그대로 나타났다.**
다만 **표본 3건이므로 이것은 사례이지 근거가 아니다.**

## ⑦ 검증

`python3 run_tests.py` 종료코드 0 · **270개 통과 0개 실패**
(신규 1개는 변이 1건으로 빨간 불 확인 후 채택).

---

# 52차 — **데이터 신뢰도 전면 감사** (2026-08-16)

저장소 주인 지시: "다중 에이전트를 이용하여 데이터에 대한 신뢰도를 극도로
올려. 우선 데이터에 대한 신뢰도가 우선이야."

## ① 감사 방법과 규모

8축 독립 감사 → 발견마다 **적대적 반박** → 종합.
에이전트 **60개 · 실패 0 · 462만 토큰 · 2시간 42분.**
발견 51건 중 **반박을 통과한 것 28건.**

감사관에게 못 박은 것: "모든 주장은 네가 실제로 돌린 코드의 출력이어야
한다 · **없는 문제를 만드는 것이 이 감사에서 가장 큰 죄다.**"
반박자에게: "**기본값은 반박이다** · 반드시 `dataset.build()` 를 돌려
손질 **후에도** 남는지 확인하라."

**첫 판은 전부 실패했다**(9개 전멸 · 41만 토큰 · 결과 0건). 원인은 데이터가
아니라 **내 워크플로 설계 결함** — 결과 형식에 실행 출력 전문과 재현 코드를
담게 해서 제출 JSON 이 잘렸다. 긴 증거는 파일로 쓰게 하고 제출은 요약만
받도록 고쳐 재실행해 성공했다. (CLAUDE.md 5장에 재실행 규칙으로 박음)

## ② 종합 판정 — **신뢰도 30 / 100**

> "지금 데이터로 주도섹터 판정을 내려도 되는가?" → **안 된다.**

### 깨끗한 것 (감사단이 명시)

- **가격**: 분할·중복날짜·정렬·복붙·하루밀림 **전부 0건**.
  `sector_model` 에 잣대 칸 등장 0회 = 완성수는 순수 가격 계산.
- **미래 엿보기 0건**: 진입일 ≤ 발표일 0/2,366 · 절단 재계산 4,150건 불일치 0.
- **원문 파싱**: 보도자료 **257건 대조**에서 확정 오류 **1건**(매출 128/128 일치).
- 묶음 160종목 6개 집합 완전 일치 · notes 1,106건에 버린 값 전부 기록(조용한 삭제 없음).

### 판정을 막는 결함

| # | 결함 | 건수 | 근거 |
|---|---|---|---|
| 1 | **누적(YTD·연간)값이 분기 칸에 혼입** | 62 | QCOM 10.22 vs 직전4분기합 9.77 · GS 30.06 vs 29.24 |
| 2 | **장치 불안정** — 데이터를 고쳐도 남음 | 210주 | 아래 ③ |
| 3 | 오염이 **주도 경쟁 묶음에 집중** | 11 | 주도 1위 통신·미디어 회선은 4종목 중 **3종목** 오염 의심 |
| 4 | verdict.json 이 낡은 코드 산물 | — | 화면 "데이터센터" vs 현재 코드 "기기 OEM 반도체" |
| 5 | 잣대 칸에 **배당금·주식수·ROTCE** 혼입 | 21 | JPM 배당금 10행(원문 확증) · UCTT 희석주식수 3행 |
| 6 | **주식분할 EPS 미조정** 7종목 | 55 | 가격은 조정, EPS 는 미조정(NVDA 6.12→0.68) |

## ③ 가장 무거운 결론 — **데이터를 완벽히 고쳐도 판정이 재현되지 않는다**

감사단의 결정적 실험(실행 출력에서 복사):

> 깨끗한 잣대값 2,514개 중 **무작위 152개(6%)만 지워도** 주도섹터가
> 210주 중 **중앙값 54주(25.7%)** 바뀐다. 8회 반복 범위 6.2~37.6%.

원인은 **표본 두께**다:

| | 값 |
|---|---|
| 주당 자격 묶음 | 중앙값 **3개** |
| 주도로 뽑힌 묶음의 완성수 | 중앙값 **3종목** = 문턱(MIN_COMPLETIONS)과 동일 |
| 1위−2위 점수차 | 중앙값 **7.9점** |
| 완전 동점인 주 | **15주** |

즉 지금 "주도섹터"는 **한두 종목이 흔들리면 바뀌는 판정**이다.
이것은 데이터 수리로 없앨 수 없고, **판정에 안정성 지표를 상시 병기**해야
정직해진다.

## ④ 수리 1 — 누적값 가드 (이번 차수에 구현)

감사단은 "같은 회계 월에 반복되며 중앙값의 3~5배"라는 휴리스틱을 냈지만,
나는 **산술로 증명되는 부분집합만** 잡는 규칙으로 좁혔다:

> 잣대값이 ⑴ **지난 값들의 중앙값 × 2.5** 를 넘고
> ⑵ **직전 4분기 합과 ±20% 안으로 같으면** 누적값으로 보아 없음 처리

근거: 누적값은 정의상 직전 4분기 합과 비슷하다(사업이 평평하거나 완만히
자라면 연간 ≈ 최근 4분기 합). 이 지문이 없으면 **빠르게 크는 회사의 정상
분기를 오인**한다 — 그래서 배수만으로 자르지 않는다.

**미래를 보지 않는다**: 중앙값도 직전 4분기 합도 **그 행보다 앞선 값들만** 쓴다.
음수(적자) 분기는 누적일 수 없으므로 걸리지 않는다.

**실측 38칸 제거** — MU 12.20·25.11(반기·3분기 누적) · CRDO 0.44·0.82 ·
QCOM 10.22 · GS 30.06 · SMCI 22.09 · ETN 11.97 · LLY 22.50 · FDX 17.80 등.

**주도섹터 변화** (실행 출력에서 복사):

| | 51차 | 52차(가드 후) |
|---|---|---|
| 국면 수 | 19 | **18** |
| H20 전환 | 18건 · 성공 6/17 | **17건 · 성공 6/16** |
| H21 분기점 | 3건 · 성공 1/3 | **4건 · 성공 1/4** |
| 현재 주도 | 기기 OEM 반도체 | **기기 OEM 반도체** (동일) |
| 2023-11 국면 | 좌석·계약 구독 SW(점수 58.5) | 같은 묶음, **11-17 로 1주 앞당겨짐**(54.0) |

**시험 4개 · 변이 4건으로 빨간 불 확인 후 채택**:
누적값 제거 · **성장주 오인 방지** · 적자 분기 보호 · **미래 미참조**.
(처음 쓴 두 시험은 변이를 넣어도 빨간 불이 안 떠 **가짜 초록불**이었고,
뒤에 큰 값을 붙여 앞엣값 판정이 바뀌는지 보는 판으로 다시 썼다.)

## ⑤ 남은 수리 순서 (감사단이 정한 대로)

1. ✅ 누적값 가드 (이번 차수)
2. 잣대 칸 국소 오염 21칸 (JPM 배당금·UCTT 주식수·GNRC 천단위·BAC ROTCE·IPGP)
3. 주식분할 EPS 미조정 7종목
4. **판정에 안정성 지표 상시 병기** (지금 가장 없는 정보)
5. verdict.json 에 코드 버전 기록 + 화면 경고

**문턱(MIN_COMPLETIONS·MIN_MEMBERS)은 맨 마지막까지 건드리지 않는다.**
결과를 보고 문턱을 먼저 만지면 사전 등록 규율 위반이다 — 감사단도 같은
이유로 그것을 맨 뒤에 놓았다.

축별 감사 전문은 `docs/감사/52차/` 에 그대로 보존했다(9개 파일).

---

# 53차 — 수리 2: 파싱 잔해 행 (배당금·주식수 혼입) (2026-08-16)

52차 감사가 지목한 두 번째 결함을 처리했다.

## ① 증상 — 같은 발표일에 진짜와 잔해가 나란히 있다

실물 JPM (52차 감사가 원문으로 확증):

| 발표일 | 매출 | gaap_eps | 정체 |
|---|---|---|---|
| 2024-04-12 | 17,653 | **4.45** | 진짜 분기 실적 |
| 2024-04-12 | **19.3** | **1.15** | **분기 배당금**이 EPS 칸에 들어옴 |

파서가 배당금·주식수 표를 실적으로 오인하면 행이 하나 더 생기는데,
그 행은 **매출까지 함께 무너져** 있다. 이 잔해가 남으면 JPM 델타 9쌍이
전부 "하락"으로 읽힌다.

## ② 처음 세운 규칙은 **틀렸다** (실측으로 잡아냄)

"매출이 자릿수 붕괴(<10,000)면 그 행의 잣대값도 버린다" — 이 규칙을
실제로 돌려 보니 **172칸**이 걸렸다. 열어 보니:

```
AMD  2021-10-26  매출 4,313.0  adj 0.73  gaap 0.75    ← 정상
MCHP 2021-11-04  매출 1,649.8  adj 1.02  gaap 0.47    ← 정상
CRWD 2021-12-01  매출   406.5  adj 0.17  gaap -0.22   ← 정상
```

**매출을 백만 달러 단위로 적는 회사의 정상 행**이었다. 이 규칙을 넣었으면
멀쩡한 172칸을 지울 뻔했다. 규칙을 세운 뒤 **반드시 실물에 돌려 눈으로
확인**해야 한다는 것을 다시 확인했다.

## ③ 채택한 규칙 — 형제 행과 비교한다

> 같은 발표일에 두 행이 있고, 한 행의 매출이 다른 행의 **1/100 이하**면
> 그 행의 잣대값을 없음 처리한다.

단위 차이(백만 vs 달러)는 형제가 없으면 걸리지 않는다. 고치지 않고 버린다.

**구현 함정 하나**: 매출 하한 가드(<10,000 → None)가 **먼저** 돌아서 비교할
값이 이미 사라져 있었다. JPM 이 안 잡히길래 찾아냈다. 가드가 지우기 **전**
값을 내부 표시로 기억했다가 잔해 판정에 쓰고, 끝나면 지운다
(내부 표시가 밖으로 새지 않는지도 시험으로 고정).

**실측 7행 제거**: JPM 5행(배당금) · TTMI 1행 · GE 1행.

## ④ 효과

- **JPM 델타가 정상으로 돌아왔다**: 9쌍 전부 "하락" → 최근 2건 상승
  (`2026-04-14 True · 2026-07-14 True`)
- 주도섹터 흐름·전환·분기점은 **변화 없음** (18국면 · 전환 17건 성공 6/16 ·
  분기점 4건 성공 1/4). 감사단이 "이 항목 단독으로는 판정을 뒤집지 않는다
  (4/210주 = 1.9%)"고 예측한 그대로다.

## ⑤ 검증

시험 4개 · **변이 4건으로 빨간 불 확인 후 채택**:
잔해 제거 · **백만 단위 정상 행 보호** · 비슷한 크기 형제는 둘 다 유지 ·
내부 표시 누출 금지.

---

# 54차 — 수리 3(주식분할)은 **채택하지 않는다** (2026-08-16)

52차 감사의 세 번째 결함(분할이 가격에만 반영되고 EPS 에는 미반영)을
고치려다 **위험해서 되돌렸다.** 못 한 것은 못 했다고 적는다.

## ① 현상은 확실하다 (실제 분할일과 정확히 일치)

| 종목 | 잣대값 단차 | 실제 분할 |
|---|---|---|
| NVDA | 6.12 → 0.68 (**9.0배**) | 10:1 · 2024-06-10 |
| AVGO | 10.96 → 1.24 (**8.8배**) | 10:1 · 2024-07-15 |
| LRCX | 8.60 → 0.91 (**9.5배**) | 10:1 · 2024-10-03 |
| ANET | 2.40 → 0.65 (**3.7배**) | 4:1 · 2024-12-04 |
| SMCI | 7.62 → 0.56 (13.6배) | 10:1 · 2024-10-01 |

주가는 분할조정된 값을 받는데 보도자료 EPS 는 그때그때 발표된 값이라
분할일에 영구 단차가 생긴다. 감사단 지적대로 이 7종목은 분할 뒤
**신고점을 물리적으로 낼 수 없다.**

## ② 시도한 수리와 **왜 버렸는가**

분할 계수를 데이터에서 추정해 과거 값을 나누는 것은 **창작**이므로 금지다.
대신 "단차 자리에서 시계열을 끊는다"(값은 그대로, 이어 붙이지 않음)를
구현했다 — 규칙: 직전 값의 1/3 아래로 떨어지고 앞선 4분기가 꾸준했으면 끊음.

**실제로 돌려 보니 71곳을 끊었고, 대부분이 분할이 아니었다**:

```
LITE 0.29 → 0.06   ICHR 0.38 → 0.02   AMD 0.27 → 0.01
XOM  8.89 → 2.06   OXY  0.63 → 0.14   NKE 0.54 → 0.14
```

전부 **정상적인 이익 급감**이다. 광통신 바닥(LITE)·반도체 장비 불황(ICHR)·
2022 AMD 하강·유가 하락(XOM) — 이것들은 **신호이지 오염이 아니다.**
끊어 버리면 진짜 정보를 잃는다.

분할과 이익 급감을 가를 지문을 찾아봤으나 없었다:
- **배율이 둥글지 않다**: NVDA 9.0 · AVGO 8.8 · LRCX 9.5 — 분할 사이
  이익도 변해서 10.0 이 안 된다. "10:1 이면 정확히 10배"가 성립하지 않는다.
- 주가로는 못 가른다: 가격은 이미 분할조정돼 그날 아무 일도 없어 보인다.

**결론: 지금 가진 자료만으로는 안전하게 못 고친다. 되돌렸다.**

## ③ 한계로 적어 둔다 (감추지 않음)

- **NVDA·GOOGL·AVGO·LRCX·SMCI·ANET·WMT 7종목은 분할 뒤 신고점을 못 낸다.**
  이 종목들이 든 묶음(기기 OEM 반도체·데이터센터·반도체 팹 장비·광고
  플랫폼)은 전부 주도 경쟁 묶음이다 — 주도섹터 판정이 이만큼 왜곡돼 있다.
- 이익 델타는 분할선 **1쌍씩(총 7쌍)** 만 다친다. 그 자리에서 가짜 "하락"이
  한 번 생긴다.

## ④ 제대로 된 수리 (다음 과제)

**수집 로봇이 분할 효력일과 계수를 함께 받아 와야 한다.** 로봇은 깃허브
서버에서 돌아 주가 제공처에 접속할 수 있으므로 가능하다. 계수를 받으면
과거 EPS 를 나누는 것은 창작이 아니라 **사실의 적용**이다.

개발 환경에서는 주가·분할 자료를 받을 수 없으므로(CLAUDE.md 6장)
이 작업은 로봇 쪽 코드로 해야 한다.

---

# 55차 — 수리 4: **안정성 지표 상시 병기** (2026-08-16)

52차 감사가 "지금 가장 없는 정보"라고 지목한 것을 만들었다.

## ① 왜 필요한가

감사단의 가장 무거운 결론은 **데이터를 완벽히 고쳐도 판정이 재현되지
않는다**는 것이었다. 그런데 화면은 "현재 주도: 기기 OEM 반도체"만 적는다.
사람은 그것을 **확정된 사실로 읽는다.** 흔들림을 함께 적지 않으면 정직화가
아니다.

## ② 만든 것 — `leadership.stability_report`

잣대값을 **무작위로 6% 지운 사본**으로 주도섹터를 다시 뽑아 원본과
비교한다. 8번 반복하고, 씨앗을 고정해 **실행마다 같은 답**이 나오게 했다.
원본 자료는 건드리지 않는다(사본에서만 지움).

돌려주는 것은 전부 사실이고 판단이 아니다:

| 항목 | 뜻 |
|---|---|
| 바뀐주_중앙값 / 최소 / 최대 | 210주 중 몇 주의 주도가 달라졌나 |
| 마지막주_불일치 | **지금 지목한 주도**가 몇 번이나 달라졌나 |
| 완성수_중앙값 · 점수차_중앙값 · 동점주 | 왜 흔들리는지의 재료 |

## ③ 실측 (실행 출력에서 복사)

```
판정주수 210 · 지운비율 0.06 · 반복 8
바뀐주 중앙값 54 (최소 16 · 최대 104) = 25.7%
마지막주_불일치 4/8      ← 지금 지목한 주도가 절반에서 달라진다
완성수_중앙값 6 · 점수차_중앙값 7.8 · 동점주 17
```

**감사단이 낸 25.7% 를 독립 구현으로 그대로 재현했다.** 그리고 감사단이
안 잰 것 하나가 더 나왔다 — **지금 지목한 주도가 8번 중 4번 달라진다.**

## ④ 화면에 어떻게 나오나

> 현재 지목: 기기 OEM 반도체 · 국면 18개 (성공률 아님)
> **⚠️ 흔들림: 잣대값을 6%만 지워도 주도가 54/210주(25.7%) 바뀝니다 ·
> 지금 지목도 8번 중 4번 달라짐**

로봇이 매일 `verdict.json` 에 함께 기록하므로 사람이 따로 돌릴 필요가 없다.
안정성 계산이 실패해도 판정 자체는 계속되게 따로 감쌌다.

## ⑤ 검증 — 가짜 초록불 2건 또 적발

시험 6개 · **변이 4건으로 빨간 불 확인 후 채택**.

처음 쓴 안정성 시험 셋은 **한 묶음짜리 가짜 자료**를 썼는데, 그 판은
아무리 지워도 주도가 안 바뀌어 **씨앗을 랜덤화해도·아무것도 안 지워도
초록불**이었다. 주도를 다투는 **두 묶음** 자료(`_competing_ds`)로 다시 썼다.

그 뒤에도 재현성 시험이 안 갈렸다 — 50%를 지우면 **전 주가 다 바뀌어
포화**되는 바람에 씨앗이 달라도 답이 같았기 때문이다. 시행끼리 결과가
갈리는 비율(30%)로 낮추고 `최소 != 최대` 를 함께 검사하도록 고쳤다.

**"흔들리지 않는 판으로는 흔들림 지표를 시험할 수 없다"** — 이번에 배운 것.

---

# 56차 — 수리 5: **판정에 코드 판번호를 남긴다** (2026-08-16)

52차 감사 수리 목록의 **마지막 칸**이다. 이로써 감사가 낸 수리 5개 중
채택 4개(①②④⑤)와 미채택 1건(③ 주식분할, 54차에 이유 기록)이 끝났다.

## ① 고친 사고

52차 감사가 찾은 것 중 **가장 조용한 사고**였다.

| | 판정 파일(verdict.json) | 같은 원자료로 현재 코드 실행 |
|---|---|---|
| H19 현재 주도 | 데이터센터 시스템·전력설비 납품 | 기기 OEM 반도체 |

**데이터는 같았다. 코드가 달랐다.** 51차 수리(은행 EPS 회수)가 그 판정
계산보다 **뒤**였기 때문이다. 그런데 판정 파일에는 계산 **시각**만 있고
어느 판 **코드**로 계산했는지가 없어서, 화면을 보는 사람이 "이 숫자는
이미 낡았다"를 알아챌 방법이 전혀 없었다.

## ② 고친 방법 (두 군데)

**(가) `judge.code_revision()` — 판정에 판번호를 적는다.**
`git rev-parse --short HEAD` 로 지금 돌고 있는 코드의 짧은 해시를 얻어
`verdict.json` 의 `computed_at` 바로 옆에 `code_rev` 로 적는다.
**못 알아내면 `"알수없음"`** 을 적는다 — 그럴듯한 가짜 해시를 지어내지
않는다(원칙 1).

**(나) `app.verdict_code_warning()` — 다르면 화면 맨 위에서 알린다.**

| 판정 파일의 code_rev | 화면 |
|---|---|
| 지금 코드와 같다 | 조용 (늘 뜨는 경고는 아무도 안 본다) |
| 지금 코드와 다르다 | ⚠️ "옛 코드로 계산됐습니다 (판정 xxx · 지금 yyy)" |
| `"알수없음"` 이 적혀 있다 | 조용 — 비교 자체가 불가능하므로 근거 없이 겁주지 않는다 |
| **칸이 아예 없다** | ⚠️ "코드 판번호가 없습니다 — 이 수리보다 먼저 계산됐다는 뜻" |

마지막 줄이 중요하다. 처음 만들었을 때는 칸이 없으면 조용히 넘어가게
했는데, **정작 52차를 일으킨 실제 판정 파일이 바로 그 모양**이었다.
칸이 없다는 것은 "모른다"가 아니라 **"이 수리 이전에 계산됐다"는 확실한
증거**이므로 알리는 쪽이 맞다.

## ③ 실측 (실행 출력에서 복사)

지금 저장소에 있는 실제 판정 파일에 걸어 본 결과:

```
verdict computed_at: 2026-08-15T13:33:32.177837+00:00
verdict code_rev  : None
현재 코드         : d72b115
배너: ⚠️ 이 판정에는 코드 판번호가 없습니다 — 판번호를 적기 시작한
      수리(52차)보다 먼저 계산됐다는 뜻입니다. …
```

즉 **감사가 잡아낸 그 파일을 이 장치가 실제로 잡아낸다.** 다음 로봇
수집부터는 `code_rev` 가 들어가므로 이 배너는 저절로 사라지고, 그 뒤로는
"판정 이후에 코드를 고친 경우"에만 뜬다.

휴대폰 폭 412px 에서 실제로 띄워 확인했다 — 글자 잘림 없음, 가로 스크롤
없음(문서 폭 412 = 화면 폭 412).

## ④ 검증 — 가짜 초록불 1건 적발

시험 7개(계기판 5 · 판정 2) · **변이 8건으로 빨간 불 확인 후 채택**.

| 변이 | 빨간 불 |
|---|---|
| 1 경고를 아예 안 띄운다 | ✅ 2건 |
| 2 판번호를 못 알아내도 경고한다 | ✅ 1건 |
| 3 같은 판번호인데도 경고한다 | ✅ 3건 |
| 4 판정 파일에 판번호를 안 적는다 | ✅ 1건 |
| 5 git 실패 때 가짜 해시를 지어낸다 | ✅ 1건 (**두 번째 판 시험에서**) |
| 6 예외 때 가짜 해시를 지어낸다 | ✅ 1건 |
| 7 판번호 없는 판정을 그냥 넘긴다 | ✅ 1건 |
| 8 "알수없음"까지 옛 코드로 몰아붙인다 | ✅ 1건 |

**변이 5가 처음에 초록불이었다.** "지어내지 않는다"를 확인한다면서
**성공한 결과만** 보고 "16진수 7자리 모양이면 통과"로 검사했는데,
가짜로 박아 넣은 `"0000000"` 도 정확히 그 모양이라 그대로 통과했다.
`subprocess.run` 을 **실제로 실패하게 바꿔치기**해서(오류 종료 · 빈 출력 ·
git 없음 세 가지) 세 경우 모두 `"알수없음"` 이 나오는지 검사하도록 다시
썼다. 그러자 변이 5·6이 둘 다 빨간 불이 됐다.

**"실패하지 않는 자료로는 실패 처리를 시험할 수 없다"** — 55차의
"흔들리지 않는 판으로는 흔들림 지표를 시험할 수 없다"와 같은 종류의
실수다. 두 번 연속 나왔으므로 규칙으로 적어 둔다:

> **막으려는 상황을 실제로 만들어 놓고 시험한다.** 정상 경로만 보고
> "모양이 맞으니 통과"로 검사하면 가짜 초록불이 된다.

## ⑤ 전체 시험

`python3 run_tests.py` 종료코드 0 · **291개 통과, 0개 실패**
(파싱 55 · 주가 14 · 숫자검사 31 · EPS 47 · 측정저장 17 · 로봇 7 ·
컨센서스 7 · 데이터계층 24 · 측정장치 19 · 정배열 9 · 주도섹터 20 ·
판정 15 · 계기판 26)

---

# 57차 — 주도섹터 흔들림의 **진짜 원인** 규명 (2026-08-16)

55차에서 "잣대값 6%를 지우면 210주 중 54주(25.7%)의 주도섹터가 바뀐다"를
실측했다. 이번에는 **왜** 그런지를 다중 에이전트 감사로 캤다.
검증단 4명이 내 진단을 **일부러 반박**했고, 설계단 3명이 대책을 독립
설계했으며, 심사 1명이 합쳤다. 8명 전원 완료, 오류 0건.

## ① 내 첫 진단은 틀렸다 — 원인은 두께가 아니다

처음에 "묶음이 얇아서 흔들린다"고 적었다. **결정적 반증**:

| 6% 삭제 전후로 바뀐 칸 (3,388칸 × 3회) | |
|---|---|
| 완성수 | **0칸** |
| 완성밀도 | **0칸** |
| 판단가능 종목수 | **0칸** |
| 델타동반 | 1,413칸 |
| 조건충족 | 368칸 |

삭제 전후 두 세계의 **묶음 구성이 완전히 같다.** 완성은 주가 정배열로만
정해지므로 잣대 삭제가 밀도에 닿을 길이 없다. 그러니 54주를 "얇음" 탓으로
돌리는 것은 논리적으로 불가능하다.

**분모를 두 개 섞어 쓴 것이 내 실수였다:**

| 분모 | 무엇 | 6% 삭제에 흔들리나 |
|---|---|---|
| 완성밀도의 분모 | 묶음 종목 수 | **아니오** (0칸) |
| 델타동반의 분모 | 델타를 잴 수 있는 완성 수 | **예** (여기가 전부) |

델타동반 분모는 중앙 4, **실제 주도 묶음은 3**이다. 한 건이 빠지면
델타동반이 중앙 33.3%p 움직이고, 분모 3이면 2가 되어 아예 판단 불가로
탈락한다. 취약 칸의 취약도는 종목 수와 거의 무관하다 — 분모 3이면
얇은 묶음 190/190, 두꺼운 묶음 48/48 **둘 다 100% 취약**하다.

## ② "얇으면 점수가 부풀려진다"도 틀렸다 — 산수의 그림자

조건(완성≥3·밀도≥30%·델타동반≥50%)을 걸면 4종목 묶음에서 점수가 가질 수
있는 값은 {50.0, 66.7, 75.0, 100.0} 네 개뿐이고 **바닥이 정확히 50.0**이다.
내가 "부풀려진 중앙값"이라 한 50.0은 **가능한 최솟값 그 자체**였다.
20종목이면 바닥이 15.0이다. 크기별 점수 차이는 대부분 문턱이 만든 바닥의
차이다.

무작위 부분표본의 밀도는 초기하분포라 **평균이 원본과 같아야** 하고 실제로
같았다(33.11 vs 33.77). 진짜 17묶음에서 종목 수 ↔ 밀도 중앙값 상관은
**+0.56(클수록 높음)** 으로 내 주장과 반대다.

## ③ "종목 43%인데 주도 78%"는 잘못된 비교 기준

160종목을 섹터와 **무관하게** 같은 크기 묶음에 무작위 재배치해 돌렸다:

```
실제: 얇은 묶음(9종목 이하)이 주도 주의 82.4%
무작위 40회: 중앙 76.6% · 범위 37.4~96.4%
실제 82.4% 이상이 나온 횟수 11/40 (p≈0.28)
```

(감사단 독립 실행: 200회 중앙 75.6%, p≈0.31 — 같은 답)

**78%라는 값 자체는 이상하지 않다. 이상한 것은 귀무선이 43%가 아니라
76%라는 사실이다.** 편향은 실재하지만 **전부 기계적**이고, 크기는
43→82가 아니라 **43→약 76**이다. 앞으로 이 구분 없이 적으면 편향을
2배 가까이 과장하게 된다.

## ④ 진짜 기제 — 최댓값 뽑기 × 큰 분산

얇은 묶음은 자격을 **덜** 얻는다(스피어만 +0.90):
에너지 8.6% · 통신 11.0% vs 기기 OEM 46.2% · 구독SW 41.9%.
그런데 **얻을 때는 높은 점수로** 얻는다(자격시 점수 ↔ 크기 상관 −0.53).
장치가 매주 **최고점 하나**를 뽑으므로 평균이 높은 쪽이 아니라 **잘 튀는
쪽**이 이긴다. 얇음은 후보 칸의 42.2%인데 주별 최고점의 65.8%를 가져간다.

## ⑤ 그리고 그 지목은 실제로 성과가 나빴다

| | 주도한 주 | 그 묶음 초과수익 | 그 주 17묶음 평균 | 상대 | 평균을 이긴 주 |
|---|---|---|---|---|---|
| 얇음(≤9종목) | 156주 | **−1.6%p** | +3.5%p | **−3.6%p** | 35% |
| 두꺼움(≥10) | 30주 | +7.9%p | +7.1%p | +3.3%p | 53% |

에너지 원자재: 주도 지정 **직전** 60일 +39.1%p → **직후** −4.3%p.
통신·미디어 2025-04-17 국면: 직전 +25.6%p → 직후 −23.9%p.
**이미 오른 뒤에 지목되고, 지목 후에는 이어지지 않는다.**

⚠️ 단 "두꺼우면 좋다"는 강건하지 않다 — 기기 OEM 반도체 한 국면(11주,
+42.0%p)을 빼면 +3.3%p → −4.0%p로 뒤집힌다(n=19). **확실한 것은
"얇으면 나쁘다" 하나뿐이다.**

## ⑥ 장치 감사 (원칙 6) — 결함 3건

1. **`leadership.py` 주석의 숫자가 실행값과 달랐다.**
   주석 "잣대값 2,514개 · 주도 완성수 중앙 3 · 점수차 7.9"
   → 실행값 **2,490 · 6 · 7.8**. 52차 감사단의 숫자를 그대로 옮겨 적고
   우리 장치 출력으로 검산하지 않은 탓. 주석을 고치고, 원인 설명도
   ①의 정정으로 바꿨다.
2. **"284칸 삭제"는 오해를 부르는 표현.** 잣대 3종을 모두 지우지만
   종목마다 실제로 쓰는 잣대는 사다리로 하나뿐이라, 판정에 닿는 것은
   **152칸**이다. 다만 삭제가 무작위라 분자·분모가 같은 비율로 줄어
   **실효 선량은 2,490칸의 6.10%** 로 의도한 6%와 같다.
   (감사단은 "2배 부풀림"이라 했으나 검산 결과 **아니었다** — 감사단
   지적도 그대로 믿지 않고 재 봐야 한다.)
3. **`DENSITY_MIN=30%` 는 10종목 이하 묶음에서 한 번도 작동한 적이 없다.**
   탈락 사유 집계(3,388칸): 완성수<3 **1,718칸** · 밀도<30% 262칸(전부
   11종목 이상) · 델타동반 미달 649칸 · 조건충족 759칸.
   진짜 병목은 밀도 문턱이 아니라 **완성수 문턱**이다.
   ⚠️ 결과를 본 지금 문턱을 만지는 것은 금지다(원칙 2). 사실만 적는다.

## ⑦ 듣지 않는 대책 — 미리 기각한 것들 (실측)

설계단이 실제로 돌려 보고 셋 다 기각했다. **기록해 두어 다시 시도하지
않게 한다.**

| 대책 | 왜 안 되나 (실측) |
|---|---|
| 창을 13→26→52주로 늘린다 | 완성수는 "사건 수"가 아니라 **서로 다른 종목 수**다. 천장이 묶음 크기라 창을 넓혀도 분모가 두꺼워지지 않는다. 오히려 얇은 묶음이 밀도 100%에 눌어붙어 **편향이 커진다** — 9종목 이하 1위 비중 66%→72%→81% |
| 주도 지목에 2·4주 연속 확인을 요구 | 1~2주짜리 주도 국면이 **0개**다(최단 3주). 이미 "도전자가 직전 13주 최고점을 넘어야 한다"가 한 칸 흔들림을 막고 있다. 없앨 것이 없다 — 바뀐주 54→57→87→57 |
| 역사를 5년보다 길게 | 한 주의 분모를 두껍게 하지 못한다. 주(週) 개수만 는다 |

## ⑧ 오늘 한 것 — 코드 변경 (기본 동작 불변)

`leadership.py` 에 **주도점수 방식 갈래**를 넣었다. 관문(조건충족)·
`MIN_MEMBERS`·교체 규칙은 **손대지 않았다.** 바뀌는 것은 순위를 매기는
점수뿐이다.

| 방식 | 식 |
|---|---|
| `raw` (**기본값**) | 완성밀도 × 델타동반 ÷ 100 — 지금까지와 같음 |
| `wilson_product` | 두 비율을 각각 윌슨 95% 하한으로 깎아 곱함 |
| `wilson_single` | 점수는 실은 비율 **하나**(실효적중수÷판단가능)라, 거기에만 윌슨 |

윌슨 하한은 이 저장소가 **채택 기준에서 이미 쓰는 계산**(`judge.wilson_interval`)
이다. 새 문턱·새 손잡이를 만들지 않았다. 주인에게는 한 줄로 설명된다 —
**"표본이 얇으면 잘한 것을 곧이곧대로 믿지 않고 깎아서 본다."**

**기본값이 안 바뀌었음을 증명했다**: 옛 코드(git HEAD)를 따로 불러와
3,388칸 × 7항목 대조 → **불일치 0칸**, 타임라인 210주 **불일치 0주**.

### 탐색 결과 (⚠️ 채택 근거 아님 — 원칙 5)

```
얇음 주도지분:  raw 82.4%  ·  wilson_single 48.2%  ·  wilson_product 38.7%
(무작위 재배치 귀무선 76.6% · 종목 비중 43.1%)
현재 주도: 세 방식 모두 기기 OEM 반도체 (같음)
```

**⚠️ 가장 중요한 경고**: 두 윌슨판 모두 탐색 표본에서 **표적 성적이
나빠졌다** — 전환 후 60거래일 성공 6/16 → 2/15. **"흔들림이 준다"가
"더 잘 맞힌다"를 뜻하지 않는다.** 안정성만 보고 채택하면 원칙 5를
형식만 지키고 정신은 어기는 것이다.

## ⑨ 검증

주도섹터 시험 26개(6개 추가) · **변이 8건으로 빨간 불 확인 후 채택**.

**가짜 초록불 2건 적발** — 처음 쓴 시험은 아래 둘을 못 잡았다:

| 변이 | 처음 | 고친 뒤 |
|---|---|---|
| ① 기본값을 몰래 `wilson_product` 로 바꾼다 | 🟢 초록 | ❌ "기본값이 raw 가 아닙니다" |
| ⑤ 관문 문턱을 점수 방식에 딸려 바꾼다 | 🟢 초록 | ❌ "델타동반 33.3%인데 관문 통과" |

①은 `_score("raw", …)` **공식만** 검사하고 "인자를 안 줬을 때 무엇이
쓰이는가"를 안 봤다. ⑤는 쓰던 가짜 자료의 델타동반이 늘 0 아니면 100이라
문턱을 50→10으로 낮춰도 아무것도 안 달라졌다. **문턱 근처에 걸친 값
(33.3%)이 있는 자료**로 바꾸니 갈렸다.

> **배운 것: 문턱을 검사하려면 문턱에 걸친 자료가 있어야 한다.**
> 55차("흔들리지 않는 판으로는 흔들림을 못 잰다"),
> 56차("실패하지 않는 자료로는 실패 처리를 못 잰다")와 같은 종류다.
> 세 번 연속 나왔으므로 규칙으로 굳힌다 —
> **막으려는 상황을 자료 안에 실제로 만들어 놓고 시험한다.**

`python3 run_tests.py` 종료코드 0 · **297개 통과, 0개 실패**
(파싱 55 · 주가 14 · 숫자검사 31 · EPS 47 · 측정저장 17 · 로봇 7 ·
컨센서스 7 · 데이터계층 24 · 측정장치 19 · 정배열 9 · 주도섹터 26 ·
판정 15 · 계기판 26)

## ⑩ 아직 **사전 등록하지 않았다** — 주인 결정 대기

윌슨판을 H22·H23으로 등록하려면 먼저 정해야 할 것이 있다.
**"맞히는 힘과 흔들리지 않음 중 무엇을 앞에 두는가"** 를 결과를 본
지금 고르면 원칙 5 위반이다. 등록문에 미리 박아야 한다.
주인이 고르기 전까지 갈래는 코드에만 있고 **판정에는 쓰이지 않는다**
(기본값 raw 그대로).

---

# 58차 — 흔들림 202건의 **단일 뿌리**와 수리 가능 범위 (2026-08-16)

57차가 "흔들림은 델타동반의 분모 문제"까지 밝혔다. 이번에는 그 분모를
깎는 것이 **무엇인지**를 끝까지 쪼갰다.

## ① 202건을 원인별로 쪼개면 하나로 모인다

완성 894건 중 델타를 못 재는 것이 **202건(22.6%)**이다.

| 원인 | 건수 | 비율 |
|---|---|---|
| ④ 신선도 140일 초과 | 82건 | 40.6% |
| ③ 완성이 첫 델타쌍보다 앞섬 | 65건 | 32.2% |
| ① 잣대 미달 9종목 (8분기 못 채움) | 55건 | 27.2% |

그런데 ④를 한 겹 더 벗기면 이렇게 갈린다:

```
신선도 초과 82건 · 실제 간격 중앙 247일 (최소 141 · 최대 967)
  ├ 발표는 있었는데 잣대값이 비어 델타쌍이 안 생김   67건 (82%)
  └ 그 기간에 발표 자체가 수집 안 됨                15건 (18%)
```

③도 같은 성질이다 — 초기 잣대값이 비어서 첫 델타쌍이 늦게 생긴 것이다.

> **202건 중 187건(93%)이 "잣대값 결측" 하나로 수렴한다.**
> 발표 자체가 없는 것은 **15건(7%)** 뿐이다.

즉 장치를 손볼 곳이 아니라 **채울 곳**이 문제다.

## ② 구멍을 메우면 얼마나 나아지나 (천장 측정)

판단 불가 완성에 델타를 **관측된 비율대로 배분**해 "구멍이 없었다면"의
세계를 만들어 봤다. 값을 지어낸 것이 아니라 **분모가 얼마였을까**만 본다.

| | 바뀐주 중앙 | 마지막주 불일치 |
|---|---|---|
| 지금 (구멍 22.6%) | 68주 (32.4%) | **3/4** |
| 구멍이 메워진 세계 | **40주 (19.3%)** | **0/4** |

(둘 다 `trials=4` 로 같은 조건에서 잰 값이다. 55차에 적은 54주·25.7%는
`trials=8` 이라 숫자가 다르다 — 비교는 같은 조건끼리만 한다.)

**흔들림이 3분의 1 줄고, "지금 지목한 주도가 뒤집히는 일"은 사라진다.**
잣대 결측을 메우는 것이 이 모델에서 가장 값어치 있는 데이터 작업이다.

## ③ 그런데 이 수리는 **개발 환경에서 못 한다** — 로봇 몫이다

막힌 9종목을 직접 열어 확인했다:

| 종목 | 분기행 | adj_eps | EBITDA | gaap_eps | (문턱 8) |
|---|---|---|---|---|---|
| VRTX | 20 | 4 | 0 | 5 | 합쳐야 9인데 섞을 수 없음 |
| NFLX | 20 | 0 | 5 | 5 | 〃 |
| CAT | 20 | 0 | 0 | 4 | |
| PG | 20 | 0 | 0 | 4 | |
| SNOW | 19 | 0 | 0 | 4 | |
| VST | 20 | 0 | 1 | 1 | |
| CLS | 10 | 6 | 1 | 6 | |
| SNDK | 9 | 6 | 0 | 6 | |
| DE | 7 | 0 | 0 | 6 | 분기행 자체가 부족 |

**분기행은 있는데 잣대값만 비어 있다.** 그런데 저장된 파싱 실패 원문은
종목당 **2~3건뿐**인데 빠진 분기는 **14~16개**다 — 고칠 원문이 손에 없다.

CAT·PG는 XBRL 근사치 행에 매출·영업이익은 있고 **gaap_eps 만 없다**.
확인해 보니 `_series_for_key` 는 4분기 EPS 를 **일부러 역산하지 않는다** —
EPS 는 주당 금액이라 연간에서 분기 합을 빼도 4분기 EPS 가 아니기 때문이다.
**이것은 결함이 아니라 옳은 설계다**(창작 금지). 그러므로 여기서 고칠 것이
없고, SEC 에서 3개월 EPS 를 더 받아오는 수밖에 없다.

> **결론: 잣대 결측 수리는 SEC 접속이 되는 로봇에서만 가능하다.**
> 개발 환경에서 재시도하지 않는다 (CLAUDE.md 6장).

## ④ 로봇이 할 일 (다음 수집 때)

1. 막힌 9종목(CAT·CLS·DE·NFLX·PG·SNDK·SNOW·VRTX·VST)의 3개월 GAAP EPS 를
   XBRL 에서 더 훑는다 — 지금 4~6분기만 잡히는 이유를 로그로 남긴다.
2. 잣대값이 빈 분기의 보도자료 원문을 **실패로 기록되지 않아도** 보관한다.
   지금은 매출이 파싱되면 성공으로 쳐서 원문이 안 남아, 나중에 고칠
   재료가 사라진다. (이번에 9종목을 못 고친 직접적 이유다.)
3. 발표 자체가 없는 15건의 분기를 다시 받아 본다.

## ⑤ 이번에 코드는 고치지 않았다

측정과 진단만 했다. 고칠 곳이 로봇 쪽이고, 그 검증은 실데이터가
들어와야 가능하기 때문이다. 없는 것을 있는 것처럼 만들지 않는다.

---

# 59차 — "6% 지우기"가 무엇을 재는 시험인지 (2026-08-16)

주인 질문: **"잣대값 테스트의 본질은 실적을 6% 못 긁어올 경우의 수를
예상하고 테스트하는 건가?"**

## ① 아니다 — 예측이 아니라 흔들어 보기다

예측이었다면 실제 실패율을 썼어야 한다. **실제 실패율은 이미 22.6%**다
(완성 894건 중 202건이 델타 판단 불가, 58차). 6%는 그 근처도 아니다.

이 시험이 재는 것은 **"답이 데이터 위에 얼마나 단단히 앉아 있는가"**다.
책상이 흔들리는지 보려고 밀어 보는 것이지, "누가 6% 밀 것 같다"는
예상이 아니다. 6%라는 값 자체에 뜻은 없다.

## ② 그래서 의심했다 — 무작위 삭제가 실제 실패를 대신할 수 있나

실제 수집 실패는 무작위가 아니다. **특정 회사(CAT·PG·VRTX…)가 계속
실패하고, 특정 분기(4분기 EPS)가 통째로 빈다.** 몰려서 나는 구멍을
고르게 흩뿌린 구멍으로 흉내 내면 피해를 **과소평가**할 것이라 봤다.

**같은 283칸을 지우되 방식만 바꿔 재봤다:**

| 지우는 방식 | 바뀐주 중앙 | 4번의 값 |
|---|---|---|
| ① 무작위 (지금 쓰는 방식) | 71 / 210 (33.8%) | 49 · 60 · 82 · 113 |
| ② 종목 통째로 (실제 실패 양상) | 50 / 210 (23.8%) | 4 · 43 · 57 · 69 |
| ③ 특정 발표일 통째로 | 52 / 210 (24.8%) | 46 · 52 · 52 · 87 |

**내 예상이 틀렸다.** 몰아서 지우면 오히려 **덜** 흔들린다. 셋 다
24~34% 같은 동네이고, 지금 쓰는 무작위 방식이 셋 중 가장 혹독한 편이다.
(범위가 크게 겹치므로 "무작위가 더 나쁘다"고 단정하지는 않는다 —
말할 수 있는 것은 **"과소평가하지는 않는다"** 까지다.)

한 묶음의 종목을 통째로 지우면 그 묶음 하나만 조용해지지만, 고르게
흩뿌리면 여러 묶음·여러 시기의 델타쌍이 하나씩 끊겨 **더 많은 서로 다른
주**를 건드리기 때문으로 보인다.

## ③ 대신 다른 것이 드러났다 — 자 자체가 흔들린다

위 표의 오른쪽 열을 보라. 같은 6%를 4번 지웠는데 **49 · 60 · 82 · 113주**가
나왔다. 55차에 적은 8회 실측도 최소 16 · 최대 104였다.

> **흔들림을 재는 자 자체가 심하게 흔들린다.**
> "54주 25.7%"처럼 소수점까지 적은 것은 정밀도를 과장한 것이다.

52차 감사단도 같은 지적을 했었다("소수점 한 자리로 적으면 정밀도를
과장한다"). 그때는 중앙값만 화면에 올렸다.

## ④ 고친 것 — 화면에 범위를 함께 적는다

```
⚠️ 흔들림: 잣대값을 6%만 지워도 주도가 54/210주(25.7%) 바뀝니다
   (뽑기마다 16~104주로 갈림) · 지금 지목도 8번 중 4번 달라짐
```

뽑기마다 답이 같았으면 범위를 **적지 않는다** — 없는 폭을 만들어 보이면
그것도 창작이다(원칙 1).

⚠️ 문턱(6%·반복 8회)은 **건드리지 않았다.** 결과를 보고 흔드는 양을
바꾸면 사전 등록 규율 위반이다. 바꾼 것은 **적는 방식**뿐이다.

## ⑤ 검증

계기판 시험 28개(2개 추가) · **변이 3건으로 빨간 불 확인 후 채택**:
①범위를 아예 안 적음 ②뽑기가 다 같아도 가짜 범위를 만듦
③범위만 적고 중앙값을 지움 — 셋 다 빨간 불.

`python3 run_tests.py` 종료코드 0 · **299개 통과, 0개 실패**

## ⑥ 남는 말

6% 시험은 "이 답을 믿어도 되나"를 재는 자이고, 58차가 잰
**"구멍을 메우면 32.4% → 19.3%"** 가 "무엇을 고쳐야 하나"를 재는 자다.
주인의 질문 방향(실제 실패를 예상해서 재자)에 답하는 것은 **후자**이며,
그 답은 이미 나와 있다 — **잣대 결측 187건을 메우는 것**이다.

---

# 60차 — 고칠 재료가 안 모이던 이유 (2026-08-16)

58차에서 잣대 결측 187건이 흔들림의 뿌리임을 밝혔는데, **왜 그동안
못 고쳤는지**는 안 캤다. 이번에 캤더니 원인이 둘 다 우리 쪽에 있었다.

## ① 보관 상한이 2건이었다

잣대가 막힌 9종목은 분기가 **14~16개** 비어 있는데, 고칠 재료로 보관된
보도자료 원문은 **종목당 2~3건뿐**이었다. `MEASURE_RAW_MAX = 2` 였기
때문이다. 재료가 손에 없으니 파서를 고칠 방법이 없었다.

**2 → 12 로 올렸다** (3년치 분기).

## ② 그 2칸마저 엉뚱한 것이 차지하고 있었다

보관 조건이 `조정 EPS 를 못 읽었으면 보관`이었다. 그런데 잣대 사다리는
**셋(조정EPS·EBITDA·GAAP EPS) 중 아무거나** 8분기면 된다. 그래서 GAAP EPS
를 멀쩡히 읽은 원문까지 "실패"로 보관하고 있었다.

**보관된 153건을 지금 파서로 다시 읽어 봤다:**

| | 건수 |
|---|---|
| 잣대값을 하나도 못 읽음 (**진짜 실패**) | 106건 (69%) |
| 이미 잣대값을 읽을 수 있음 (자리만 차지) | **47건 (31%)** |

내역: gaap_eps 15 · adj_eps+gaap_eps 14 · EBITDA+gaap_eps 9 ·
EBITDA 8 · adj_eps 1.

조건을 **"잣대값을 하나도 못 읽음"** 으로 좁혔다. 판단을
`_should_keep_raw()` 로 떼어내 시험할 수 있게 했다 — 예전에는 네트워크가
필요한 함수 안에 파묻혀 있어 검증이 불가능했다.

## ③ 상한을 올리면서 생긴 위험을 함께 막았다

건수만 12로 올리면 최악의 경우 **12건 × 120,000자 = 1.4MB** 가 한 종목에
쌓이고, 실패 종목이 66개면 저장소가 **93MB** 까지 부푼다.

실측 평균은 23KB(현재 153건 3.5MB)라 보통은 문제가 안 된다. 그러나
**보통을 믿고 상한을 안 두면 언젠가 터진다.** 그래서 종목당 총량 상한
`MEASURE_RAW_TOTAL_CAP = 400,000자` 를 새로 뒀다. 보통 크기면 12건이 다
들어가고, 저장소는 26MB 안쪽으로 묶인다.

이제 상한이 셋이다 — **건수(12) · 1건당 글자수(120,000) · 종목당 총량(400,000).**

## ④ 검증

측정 저장 시험 21개(4개 추가) · **변이 8건으로 빨간 불 확인 후 채택**:

| 변이 | 빨간 불 |
|---|---|
| ① 옛 조건(조정 EPS 만 보고 판단) | ✅ |
| ② 실적발표 여부를 안 봄 | ✅ |
| ③ 첨부 없어도 보관 | ✅ |
| ④ 건수 상한을 옛 값 2로 | ✅ 2건 |
| ⑤ 총량 상한 없앰 | ✅ (1.44MB 쌓임) |
| ⑥ 총량 상한이 너무 빡빡 | ✅ 2건 |
| ⑦ 건수 상한 없앰 | ✅ |
| ⑧ 글자 수 자르기 없앰 | ✅ |

⑥은 **반대 방향 실수**를 잡는 변이다 — 상한을 너무 조이면 보통 크기
원문 12건이 안 들어간다. 한쪽만 막으면 반대로 넘어진다.

`python3 run_tests.py` 종료코드 0 · **303개 통과, 0개 실패**

## ⑤ 아직 안 한 것

- 이미 보관된 47건(지금 파서로 읽히는 것)은 **지우지 않았다.** 지워도
  로봇의 보관 자리가 늘지는 않고(자리는 실행마다 새로 셈), 저장소가
  3.5MB 라 해로울 것이 없다. 지우는 것은 되돌리기 어려우니 그대로 둔다.
- 잣대 결측 자체의 수리는 여전히 **로봇 몫**이다(58차 ③). 이번 수리는
  "다음번에는 고칠 재료가 손에 남게" 만든 것이다.

---

# 61차 — 발표일 점검: 지금 자료에서는 결함을 못 찾음 (2026-08-16)

52차 감사가 남긴 미해결 항목 중 **"실적이 아닌 8-K(합병·CEO선임·
투자자설명회) 6건이 발표일로 쓰임"** 을 개발 환경에서 확인해 봤다.

## ① 감사 원본이 저장소에 없다

52차 감사단이 쓴 보고서(`docs/감사/52차/`)는 **커밋된 적이 없다.**
작업 폴더에만 있다가 사라졌다. 그래서 그 6건이 어느 종목이었는지
되짚을 수 없다. 남은 것은 `측정결과.md` 52차 항목의 요약뿐이다.

> **배운 것: 감사 보고서는 반드시 커밋한다.** 요약만 남기면 나중에
> 재현·검산을 못 한다. 이번에 실제로 못 했다.

## ② 그래서 직접 다시 뒤졌다 — 지금 자료는 깨끗하다

분기종료일 → 발표일 간격을 전수 조사했다 (실행 출력에서 복사):

```
분기행 3,068 · 발표일 있음 2,903 · 없음 165
짝짓기 창 120일 · 간격 중앙 29일 · 최소 0 · 최대 83
음수(발표가 분기종료보다 앞): 0건
91일 초과: 0건
```

가장 긴 것이 83일로 짝짓기 창(120일) 안이고, 음수도 없다. 실적발표가
아닌 8-K 를 잘못 물었다면 간격이 튀어야 하는데 **튀는 것이 없다.**
다만 SEC 접속이 막힌 환경이라 "그 8-K 가 정말 실적발표였나"를
원문으로 확인할 수는 없다 — **깨끗해 보인다까지만 말할 수 있다.**

## ③ 발표일 없는 165건은 올바르게 처리되고 있다

발표일이 없으면 그 분기를 **측정에서 제외**하는지, 아니면 분기종료일로
슬쩍 대신하는지가 중요하다. 대신하면 실제보다 이른 시점에 안 사실을
아는 것이 되어 미래 엿보기가 된다. 두 곳 다 확인했다:

| 장치 | 처리 |
|---|---|
| `measure_engine` 사건 생성 | 발표일 없으면 `continue` — 건너뜀 ✅ |
| `sector_model._delta_series` | `r.get("announced_date")` 인 행만 씀 ✅ |

**둘 다 대체하지 않고 버린다.** 옳은 처리다(창작 금지).

## ④ 결론 — 고칠 것이 없어서 코드를 안 고쳤다

이 점검은 **아무것도 못 찾은 점검**이다. 그래도 기록한다 —
원칙 5("잰 것은 좋든 나쁘든 전부 기록한다"). 다음에 같은 항목을
다시 파헤치지 않게 하려는 것이다.

남은 확인은 SEC 원문 대조뿐이고, 그것은 로봇 몫이다.

## ⑤ 곁가지 — 개발 환경이 세 번 초기화됐다

이 세션 동안 작업 폴더가 옛 커밋(`ccbe870`)으로 **세 번** 되돌아갔다.
매번 `git fetch` + `git checkout -B` 로 복구했고, 자주 커밋·푸시해 둔
덕에 잃은 작업은 없다.

⚠️ 그런데 한 번은 **되돌아간 것을 모르고 숫자를 쟀다.** 이 문서의
②번 수치도 처음엔 "분기행 1,481 · 발표일 2,903 아닌 1,375 · 중앙 32일"
로 나왔는데, 옛 코드로 잰 값이었다. 올바른 판에서 다시 재니 위의 값이
나왔다.

> **규칙에 더한다: 숫자를 재기 전에 `git log --oneline -1` 로 어느 판에서
> 재는지 확인한다.** "보고에 적는 숫자는 실행 출력에서 복사한다"만으로는
> 부족하다 — **어느 판의 실행 출력인지**까지 맞아야 한다.

---

# 62차 — 사전 등록: H22·H23(점수 방식) · H24(종목 명부 규칙) (2026-08-16)

주인이 두 갈림길에 답했다. **이 문서를 데이터를 더 보기 전에 머지한다**(원칙 2).

| 물음 | 주인 결정 |
|---|---|
| 맞히는 힘 vs 흔들리지 않음 | **맞히는 힘** |
| 로봇 수집을 1.4배로 늘릴 것인가 | **늘린다** |
| 통신 50주 과거 서술 | 1번 답이 정함 (아래 ③) |

## ① H22·H23 — 주도점수 방식

| | 방식 |
|---|---|
| **현행(본판)** `raw` | 완성밀도 × 델타동반 ÷ 100 |
| **H22** `wilson_product` | 두 비율을 각각 윌슨 95% 하한으로 깎아 곱함 |
| **H23** `wilson_single` | 점수는 실은 비율 하나(실효적중수÷판단가능) → 거기에만 윌슨 |

**채택 기준 = 맞히는 힘.** 표적·기준선·채택은 H20 과 **완전히 동일**하다:
창 60거래일 · 성공 = 새 주도 − 옛 주도 ≥ +10%p ·
채택 = 신호 윌슨 하한 > 기준선 상한 · n≥10.

> ⚠️ **흔들림(안정성)은 채택 근거가 아니다.** 장치의 성질이므로 관찰
> 지표로만 계속 기록한다.

**왜 지금 못 박는가**: 탐색 표본에서 윌슨판은 흔들림이 줄었지만
(25.7% → 16.5%) **표적 성적은 오히려 나빴다**(전환 후 60거래일 성공
6/16 → 2/15). 지금 정해 두지 않으면 나중에 결과를 보고 유리한 쪽을
고르게 된다(원칙 5).

- 판정 표본 = `H22_START_DAY = 2026-08-16` **뒤**에 생기는 전환만.
  지난 5년 결과는 전부 **탐색**이며 채택 근거로 인용 금지.
- 기본값은 `raw` 그대로. 윌슨판이 **표적에서 raw 를 이겨야** 본판이 된다.
- H20(raw) 폐기 금지 — H19b 선례대로 나란히 잰다.
- 앞/뒤 시간 분할을 함께 보고한다(원칙 4).
- **판정 가능 시점을 미리 정직하게 적는다**: 전환은 연 4.2건이므로
  n≥10 에 약 2.4년, 채택 기준까지는 5년 이상 걸리거나 **영영 안 될 수도
  있다.**

등록값은 문서뿐 아니라 **코드 상수**로 박았다(`H22_START_DAY` ·
`H22_SCORE_MODE` · `H23_SCORE_MODE`). 문서에만 적으면 판정할 때가 됐을 때
슬쩍 달라져도 아무도 모른다. 시험 2개 · 변이 3건으로 확인했다.

## ② H24 — 종목 명부 규칙 (종목을 고르기 **전에** 적는다)

지금 묶음별 종목 수가 4~20개로 들쭉날쭉하다. 이것이 장치의 크기 편향을
키운다(57차: 섹터 정보를 지워도 얇은 묶음이 주도 주의 76.6%를 가져감).

**규칙 (사람이 티커를 고르지 않는다):**

1. **모집단** — 매년 1월 첫 거래일 t 기준, 그날 미국 거래소에 상장돼 있고
   국내 양식(10-K/10-Q/8-K)을 제출하는 회사. **상폐·인수된 회사도 그날
   명부에는 남긴다**(생존 편향 차단). 외국 양식(20-F/6-K)만 내는 회사는
   이 저장소 파서가 못 읽으므로 모집단에서 사전 제외.
2. **정렬키** — 직전 4분기 누적 매출(TTM) 내림차순.
   *시총이 아니라 매출을 쓰는 이유*: 시총 순위는 "그해 이미 오른 종목"을
   뽑는 순환성이 있다.
3. **배정** — 45차 분류 규칙 그대로(층 먼저, 무엇을 팔아 버는가).
   수익률을 보지 않는 조건에서 하고 티커마다 근거 한 줄.
   **종목 식별은 티커가 아니라 CIK 로** 한다 (SQ→XYZ, FISV→FI,
   SGH→PENG, NCR→VYX 처럼 티커가 바뀌면 과거가 통째로 빈다).
4. **정원 K = 12.** 각 묶음 상위 12종목.
5. **못 채우면 억지로 채우지 않고 비운다.** 정원 미달 묶음은 화면에
   계속 표시하되 **"두께 미달"** 로 적는다.
   예: 현물가 연동 메모리는 미국 국내 양식을 내는 순수 메모리사가
   MU·SNDK·STX·WDC 넷뿐이라 **영구 미달**이다.
6. **편입 문턱** — 로봇이 8분기 이상 연속 잣대값을 뽑은 뒤 편입.
   탈락 대기목록도 보관한다.
   ⚠️ 이 문턱 자체가 2차 편향이다("실적을 깔끔히 공시하는 회사"로 표본이
   쏠린다). 크기는 아직 모르며, 대기목록을 남겨 나중에 재기로 한다.
7. **명부는 1년 고정.** 주 t 의 판정은 t 직전 1월 명부로 한다.

**하지 않기로 한 것**: "두께 게이트"(10종목 미만 묶음을 주도 후보에서
제외). 감사단이 제안했으나, **그것을 걸면 현재 주도가 바뀐다는 것을 이미
보고 나서** 나온 제안이라 원칙 5 위반 소지가 있다. 채택하지 않는다.

**예상 비용**: 얇은 11묶음을 정원 12로 채우면 +63종목(160 → 223),
로봇 수집량 약 1.4배.

**한계를 미리 적는다**: 이 환경의 주가는 5년 rolling 이라 이미 상폐된
종목(SIVB·PXD·SPWR 등)의 과거 주가를 받을 수 있다는 보장이 없다.
못 받으면 **과거 구간의 생존 편향은 그대로 남는다.** 명부 규칙은 앞으로
생기는 자료부터 온전히 작동한다.

## ③ 통신·미디어 회선 50주를 어떻게 할 것인가

1번 답(맞히는 힘 우선)이 이것을 정한다. 윌슨판은 표적에서 더 나빴으므로
본판으로 올릴 근거가 없고, **현행판이 그대로 본판**이다.
→ 통신 50주 지목은 **그대로 남는다.**

다만 그 국면의 **실제 성과를 함께 적는다**(정직화):

> 통신·미디어 회선(CMCSA·T·TMUS·VZ) — 210주 중 50주 주도(최장).
> 지목의 실체는 완성 3/4종목 · 델타 2/3 = **사실상 2종목**.
> 그 기간 실현 성과는 같은 주 17묶음 평균 대비 **−4.4%p**.

지목은 남기되 "그래서 어땠는지"를 숨기지 않는다.

## ④ 검증

주도섹터 시험 28개(2개 추가) · **변이 3건으로 빨간 불 확인**:
①시작일을 과거로 ②등록된 방식 이름을 바꿈 ③기본값을 raw 가 아니게 함.

`python3 run_tests.py` 종료코드 0 · **305개 통과, 0개 실패**

## ⑤ 다음 할 일

- H24 명부를 실제로 만드는 것은 **로봇 몫**이다(SEC company_tickers ·
  submissions 조회 필요). 이 규칙이 머지된 **뒤에** 명부를 뽑는다 —
  순서를 커밋 시각으로 증명한다.
- 지금 돌고 있는 수집 로봇(#31943912607)이 60차 수리를 실물 검증한다.

---

# 63차 — 로봇 실물 검증 + **연간값 오염 발견** (2026-08-16)

수집 로봇을 손으로 돌려(#31943912607, 13:09 완료) 56·60차 수리를
실데이터로 처음 검증했다. **재는 판: `1810829`**

## ① 56차(코드 판번호) — 통과, 그리고 배너가 실제로 켜졌다

```
computed_at : 2026-08-16T13:08:03Z
code_rev    : 2e2f9e6      ← 기록됨
현재 코드    : 1810829
배너: ⚠️ 이 판정은 옛 코드로 계산됐습니다 (판정 2e2f9e6 · 지금 1810829)
```

배너가 켜진 것은 **우연이 아니라 정확히 52차가 잡아낸 그 상황**이다.
로봇이 `2e2f9e6` 을 받아 90분간 도는 사이에 62차(`c143b33`)를 푸시했고,
로봇은 계산이 끝난 뒤 최신 가지 위에 커밋했다. 그래서 **판정은 옛 코드,
저장소는 새 코드**가 됐다. 52차 때는 사람이 알아챌 방법이 없었다.

(이번 건은 62차가 상수만 추가한 것이라 판정 결과가 실제로 달라지진
않았다. 배너도 "바꿨을 수 있습니다"라고만 말하지 단정하지 않는다.)

## ② 60차(원문 보관) — 둘 다 통과. **처음엔 실패로 오독했다**

전체 533건을 재니 진짜 실패 비율이 69% → **70%**, 거의 그대로였다.
그래서 "②(조건 좁히기)는 효과가 없다"고 보고했다. **틀렸다.**

533건에는 **옛 조건으로 저장된 파일 320건이 섞여 있었다.** 이번 로봇이
새로 넣은 것만 갈라내니 답이 정반대였다:

| | 전체 | 진짜 실패 | 비율 | 이미 읽힘 |
|---|---|---|---|---|
| **새로 온 것** | 213 | 213 | **100%** | **0** |
| 옛 것 | 320 | 161 | 50% | 159 |

**새 조건에서는 헛것이 한 건도 안 들어왔다.** 수리는 완벽히 작동했다.

> **배운 것: 옛 자료가 섞인 통을 통째로 재면 새 수리의 효과가 묻힌다.**
> 61차가 "어느 **판**에서 재는지 확인하라"였다면, 이번은
> **"어느 **부분**을 재는지 확인하라"** 다. 같은 종류의 실수다.

수리 ①(상한 2→12)도 작동: 원문 153 → 533건(18MB, 예상 26MB 안쪽).

## ③ 잣대 미달 9종목 — 그대로. 다만 재료는 왔다

| 종목 | 원문 | | 종목 | 원문 |
|---|---|---|---|---|
| VST | 2 → **10** | | CLS | 3 → 3 |
| PG | 1 → **9** | | DE | 2 → 2 |
| VRTX | 2 → **9** | | SNDK | 2 → 2 |
| SNOW | 3 → **5** | | **NFLX** | **0 → 0** |
| CAT | 2 → 3 | | 합계 | 17 → **43** |

60차는 파서를 고친 것이 아니라 **재료가 남게** 만든 수리이므로 9종목이
그대로인 것은 예상된 결과다. 재료가 종목마다 고르지 않게 온 것이 새 사실이다
(NFLX 는 0건 — 첨부가 없거나 잣대값이 읽히거나. 다음 단계에서 종목별로 본다).

## ④ 델타 구멍·흔들림 — 변화 없음 (당연)

```
완성 894건 · 델타 판단 가능 692건(77.4%) · 구멍 22.6%   (수리 전과 동일)
흔들림 바뀐주 중앙 54(25.7%) · 범위 16~104 · 마지막주 불일치 4/8
현재 주도: 기기 OEM 반도체
```

하루치(08-15 → 08-16)만 늘었으니 같은 것이 맞다. 두 번 독립 실행이
정확히 일치했다.

## ⑤ ⚠️ 새로 찾은 결함 — **연간값이 분기값 자리에 들어와 있다**

검증 중 곁가지로 |EPS| > 40 인 칸을 훑다가 찾았다. **7칸이 그 종목이
실제로 쓰는 잣대**에 들어 있다:

| 종목 | 발표일 | 값 | 정체 |
|---|---|---|---|
| GS | 2022-01-18 | 59.45 | **FY2021 연간 EPS** |
| GS | 2025-01-15 | 40.54 | **FY2024 연간 EPS** |
| GS | 2026-01-15 | 51.32 | **FY2025 연간 EPS** |
| SCHW | 2024-10-15 | 95.30 | 나머지가 1.0~8.1 인데 혼자 95 |
| IPGP | 2025-02-11 | 99.10 | 분기 EPS 는 ~$1 수준 |
| FDX | 2026-05-13 | 68.22 | 분기 ~$4~6, 연간 ~$18 |
| XOM | 2023-07-13 | 89.45 | 나머지가 1.5~13 |

GS 는 **1월 발표(4분기)마다 연간 수치가 들어온다**. 원자료를 펼쳐 보면
분기값이 8~15인데 1월만 59.45 · 30.06 · 22.87 · 40.54 · 51.32 다.
앞의 셋은 각각 GS 의 FY2021·FY2022·FY2023 연간 EPS 와 정확히 일치한다.

**54차의 누적값 가드가 왜 못 잡았나 (원칙 6):**
가드는 `앞선 4개 이상의 이력`을 요구한다(`_CUMULATIVE_MIN_HISTORY = 4`).
GS 의 첫 오염값(2022-01-18)은 시계열의 **두 번째** 값이라 검사 자체를
건너뛴다. 그리고 그 뒤로는 **오염된 값이 기준(중앙값·직전 4분기 합)에
섞여** 다음 오염값을 정상으로 보이게 만든다.

XOM 은 성격이 다르다 — 2023-07-13(89.45)과 2023-07-28(4.73)처럼
**같은 달에 두 행**이 있다. 실적발표가 아닌 문서가 섞였을 가능성이 있다.
61차에서 "발표일은 깨끗해 보인다"고 했는데, 그때는 **간격만** 봤고
**값의 크기**는 안 봤다. 간격만으로는 이 결함이 안 보인다.

**⚠️ 이 표는 |EPS| > 40 이라는 임의의 선을 넘은 것만이다.** GS 의
30.06·22.87 도 연간값인데 40 아래라 이 표에 없다. **진짜 오염 규모는
이보다 크다.** 문턱 없이 다시 훑어야 한다.

이번에는 **고치지 않았다** — 원인이 둘 이상이고(가드의 워밍업 구멍 ·
발표일에 섞인 비실적 문서), 고치면 GS·XOM·금융 묶음의 과거 판정이
바뀔 수 있어 따로 다뤄야 한다. 다음 차수의 첫 일감이다.

## ⑥ 시험

`python3 run_tests.py` 종료코드 0 · **305개 통과, 0개 실패**
(코드는 안 고쳤다 — 이번 차수는 검증과 발견이다.)

---

# 64차 — H24 정렬키 확정: 거래대금 (2026-08-16)

주인 지시: *"섹터별로 정의 및 전략대로 그대로 분류하고, 섹터의 종목을
고를 땐 **이익 델타의 상승과 거래량**으로 순위를 매겨서 가져오자.
그게 그 섹터의 주도종목들이니깐."*

말로 답하지 않고 실제로 재서 갈랐다. **재는 판: `a8c8cbf`**

## ① 섹터 분류 — 그대로 (이미 그렇게 등록돼 있음)

45차 분류(3층 × 17묶음)와 배정 규칙("무엇을 팔아 버는가")을 손대지
않는다. H24 에 이미 그렇게 적혀 있다. 바뀌는 것은 **정렬키**뿐이다.

## ② 이익 델타로 순위를 매기면 — **답이 바뀐다**

델타 상승률 상위 k종목만 남겨 묶음을 다시 짜고 주도를 다시 뽑았다:

| 종목 고르는 규칙 | 가장 오래 주도한 묶음 |
|---|---|
| **현행 (매출·규모)** | 통신·미디어 회선 (50주) |
| 델타 상위 12 | **좌석·계약 정액 구독 소프트웨어 (54주)** |
| 델타 상위 8 | **좌석·계약 정액 구독 소프트웨어 (51주)** |
| 델타 상위 6 | **좌석·계약 정액 구독 소프트웨어 (48주)** |

**같은 자료·같은 장치인데 종목 고르는 규칙만 바꿨더니 주도 섹터가
통째로 바뀐다.** "어느 섹터가 주도인가"를 시장이 아니라 **우리의 선택**이
정하게 되는 것이다. 그러면 무엇을 재고 있는지 알 수 없다.

이것이 순환인 이유는 단순하다 — **이익 델타는 주도점수의 두 인수 중
하나**다(주도점수 = 완성밀도 × 델타동반). 재려는 것으로 표본을 고르면
답을 미리 넣는 셈이다.

### ⚠️ 내가 먼저 한 예측은 틀렸다

나는 "델타로 고르면 **델타동반이 천장에 붙어** 섹터 구분이 안 된다"고
했다. 실측은 다르다:

```
델타동반 중앙값:  현행 66.7% → k=12 66.7% → k=8 66.7% → k=6 66.7%  (불변)
100%인 칸 비율 :  9% → 14% → 14% → 17%                          (꼬리만 포화)
```

**중앙값은 전혀 안 움직였다.** 꼬리만 약간 포화될 뿐이다. 델타동반은
분모가 3~5인 이산값이라 2/3=66.7% 가 어떤 표본에서도 최빈값으로 남는다.

기각 근거는 "포화"가 아니라 **"답이 바뀐다"** 다. 근거를 바꿔 적는다.

## ③ 그럼 거래량은? — 내 제안도 같은 잣대로 시험했다

거래량은 **아직 수집조차 하지 않는다**(주가에 `dates`·`close` 뿐).
그래서 거래량과 같이 움직이는 것들로 대용해 시험했다.

| 순위 기준 | 상위 12 | 상위 8 |
|---|---|---|
| 이익 델타 | 구독SW ❌ | 구독SW ❌ |
| 정배열 완성 횟수 | 통신 ✅ | 통신 ✅ |
| 5년 누적 상승률 | 통신 ✅ | 구독SW ❌ |
| 주가 변동성 | 통신 ✅ | 통신 ✅ |

**이익 델타는 3/3 답을 바꿨고, 가격·추세 기준은 6번 중 5번 답을 지켰다.**
같은 정도로 종목을 줄였는데 결과가 다르니, 차이는 "몇 개로 줄였나"가
아니라 **"무엇으로 골랐나"** 에서 온다.

### ⚠️ 이 시험의 한계 (미리 적는다)

지금 주도인 통신·미디어 회선은 **4종목**이라 상위 12든 8이든 전부 남는다.
그래서 "안 바뀌었다"가 **규칙이 안전해서인지 통신이 안 변해서인지**
완전히 가려지지 않는다. 민감도가 낮은 시험이다.
거래량을 실제로 수집한 뒤 직접 다시 재야 한다.

## ④ 확정 — H24 정렬키 개정

| | 옛 등록(62차) | **새 등록(64차)** |
|---|---|---|
| 1순위 | TTM 매출 | **직전 1년 거래대금 중앙값** |
| 2순위(동률·결측) | 시점고정 시총 | **TTM 매출** |
| 제외 | — | **이익 델타·정배열 등 신호 관련 값 일체** |

- **거래대금 = 종가 × 거래량**. 거래량이 아니라 거래대금을 쓰는 이유:
  저가주는 주식 수가 많아 거래량이 부풀려진다. "실제로 얼마어치가
  오갔나"가 주인이 말한 *주도종목*에 더 가깝다.
- **시점 고정**: 매년 1월 명부를 만들 때 **그 직전 1년**의 거래대금만
  쓴다. 오늘 거래대금으로 5년 전 명부를 만들면 "지금 뜬 종목"을 과거에
  심는 것이 된다.
- **신호와 관련된 값은 순위 기준에서 일체 뺀다** — 이익 델타(주도점수의
  인수)·정배열(다른 인수)·초과수익(표적). 뽑힌 **뒤에** 그것들이
  어땠는지를 재는 것이 원래 하려던 측정이다.
- 두 명부(거래대금 / 매출)를 **둘 다 만들어 얼마나 다른지 보고한다.**
  많이 다르면 그 사실 자체가 발견이다.

나머지(모집단·CIK 식별·정원 12·못 채우면 비움·8분기 편입 문턱·1년 고정)는
62차 등록 그대로다.

## ⑤ 로봇에 필요한 것

**거래량 수집을 추가해야 한다.** 지금 스냅샷의 주가는 `dates`·`close`
뿐이다. 이것 없이는 새 정렬키를 쓸 수 없다. 다음 코드 작업의 첫 일감.

## ⑥ 시험

코드는 안 고쳤다(등록문 개정). `python3 run_tests.py` 종료코드 0 ·
**305개 통과, 0개 실패**

---

# 65차 — 연간값 오염의 해부 (고치지 않고 범위만 정함) (2026-08-16)

63차에서 찾은 "연간 실적이 분기 자리에 들어와 있다"를 파고들었다.
고치기 전에 **정체부터** 확인했다. **재는 판: `721a8a3`**

## ① 오염은 한 종류가 아니다 — 셋으로 갈린다

| 종목 | 발표일 | 라벨 | 값 | 정체 |
|---|---|---|---|---|
| GS | 2022-01-18 | 21 Q4 | 59.45 | **연간값** (라벨·발표일 정상) |
| GS | 2023-01-17 | 22 Q4 | 30.06 | 〃 |
| GS | 2024-01-16 | 23 Q4 | 22.87 | 〃 |
| GS | 2025-01-15 | 24 Q4 | 40.54 | 〃 |
| GS | 2026-01-15 | 25 Q4 | 51.32 | 〃 |
| SCHW | 2024-10-15 | **23 Q4** | 95.30 | **라벨이 발표일보다 1년 앞섬** |
| XOM | 2023-07-13 | **23/07** | 89.45 | **라벨 형식이 다름**(월 스타일) |
| XOM | 2024-05-10 | **24/05** | 39.29 | 〃 |

- **GS 형(연간값)**: 라벨·발표일이 다 정상인데 **값만 연간**이다. 1월
  발표(4분기)마다 반복된다. 겉으로는 아무 이상이 없어 **크기로만**
  잡을 수 있다.
- **SCHW 형(라벨 어긋남)**: 2024년 10월에 발표된 행의 라벨이 `23 Q4`다.
  1년 넘게 어긋났다. **라벨 vs 발표일 정합성 검사**로 잡힌다.
- **XOM 형(라벨 형식이 다름)**: 정상 행은 `23 Q2` 인데 이 둘만 `23/07`
  같은 월 스타일이다. 그리고 2주 뒤(07-28)에 진짜 Q2 행이 따로 있다.
  **같은 분기에 두 행**이 생겼다는 뜻이다.

## ② 출처만으로는 못 가른다

| 출처 | 행 수 | 발표일 = 분기종료일 |
|---|---|---|
| 직접공시 | 1,343 | 38행 (3%) |
| 근사치 | 1,100 | 0행 (0%) |
| **역산** | 625 | **459행 (73%)** |

오염된 행은 전부 `역산` 이지만, **역산 625행 대부분이 정상**이다
(GS 의 7.73·3.08·8.62 도 역산이다). 출처는 단서일 뿐 판별 기준이 못 된다.

`역산` 행의 73%가 **발표일 = 분기종료일** 인 것도 확인했다. 이 경로에서는
분기 종료일을 따로 알아내지 못해 발표일로 채운다는 뜻이다. 이것도
정상 행에도 널리 있으므로 판별 기준이 아니다.

## ③ 54차 누적값 가드가 왜 못 잡았나 (원칙 6)

가드는 `앞선 4개 이상의 이력`을 요구한다(`_CUMULATIVE_MIN_HISTORY = 4`).

- GS 의 첫 오염값(2022-01-18)은 시계열의 **두 번째** 값이라 검사 자체를
  건너뛴다.
- 그리고 그 뒤로는 **오염값이 기준(중앙값·직전 4분기 합)에 섞여**
  다음 오염값을 정상으로 보이게 만든다. 한 번 뚫리면 계속 뚫린다.

## ④ 이번에도 **고치지 않았다** — 이유를 적는다

고치려면 문턱을 만져야 하는데, 54차에 같은 자리에서 **정반대 사고**가
있었다: 주식분할 규칙을 넣었다가 **실적이 진짜로 꺾인 71건을 지웠다**
(LITE 0.29→0.06, ICHR, AMD, XOM, OXY, NKE). 그래서 되돌렸다.

원칙 1은 "없는 값은 없음으로 — **틀림보다 없음이 안전**"이지만,
**멀쩡한 값을 지우는 것도 창작만큼 위험하다**는 것을 그때 배웠다.
지금 워밍업 문턱만 낮추면(4 → 1) GS 는 잡히지만, 적자에서 회복하는
회사의 진짜 4배 도약도 함께 지워진다.

그래서 다음 차수에서 이렇게 나눠 다루기로 범위를 정한다:

1. **SCHW 형·XOM 형 먼저** — 라벨 정합성(라벨 연도 vs 발표일 연도)과
   라벨 형식 검사는 **크기와 무관**해서 멀쩡한 값을 지울 위험이 없다.
   같은 분기에 두 행이 생긴 것도 함께 본다.
2. **GS 형은 그 다음** — 크기 기반이라 위험하다. 지우는 대신 **표시**부터
   한다(의심 칸을 화면·로그에 적고 값은 그대로 둔다). 실제로 몇 건이
   걸리는지 보고 나서 지울지 정한다.
3. **문턱을 만지기 전에 반드시 전수 검사** — 54차의 71건 사고가
   "실데이터에 걸어 보고 나서 채택한다"로 막혔다. 같은 절차를 쓴다.

## ⑤ 규모는 아직 모른다

63차의 7칸은 `|EPS| > 40` 이라는 **임의의 선**을 넘은 것만이다.
GS 의 30.06·22.87 도 연간값인데 그 선 아래라 빠졌다. 실제 규모는 더 크다.
다음 차수의 첫 일감은 **문턱 없이 전수로 세는 것**이다.

## ⑥ 시험

코드는 안 고쳤다. `python3 run_tests.py` 종료코드 0 ·
**305개 통과, 0개 실패**

---

# 66차 — 오염 전수 조사: **내 검사 하나가 틀렸다** (2026-08-16)

65차에서 정한 대로 "문턱 없이 구조로만" 오염을 세려 했다.
구조 검사 셋을 만들어 전수로 돌렸다. **재는 판: `001b8df`**

## ① 결과 — 그리고 곧바로 드러난 문제

| 검사 | 걸린 행 | 그중 쓰는 잣대 |
|---|---|---|
| ① 라벨 연도가 발표일과 어긋남 | **345** | **307** |
| ② 같은 라벨이 두 번 이상 | 254 | 189 |
| ③ 월 스타일 라벨인데 출처가 역산 | 59 | 19 |

(전체 분기행 3,068)

①이 3,068행 중 345행(11%)을 잡았다. **너무 많다.** 걸린 행을 열어 봤다:

```
CRDO  2022-08-31 라벨=23 Q1  ★쓰는잣대  (라벨 2023 vs 발표 2022)
CRDO  2022-11-30 라벨=23 Q2  ★쓰는잣대  (라벨 2023 vs 발표 2022)
```

## ② 검사 ①은 **오염이 아니라 회계연도를 잡고 있었다**

종목별로 걸린 비율을 보면 답이 나온다:

```
①에 걸린 종목 56개 · 걸린 행 345
  AMBA 14/19행(74%) · P 14/19(74%) · MDB 14/19(74%) · SNOW 14/19(74%)
  S 14/19(74%) · ZM 14/19(74%) · DOCU 14/19(74%) · CRM 14/19(74%)
  NVDA 13/17(76%) · MRVL 13/16(81%)
```

**한 종목의 행이 74~81%나 걸린다면 그건 오염이 아니다.** 이 회사들은
회계연도가 1~2월에 끝난다 — 그래서 **회계연도 라벨이 달력보다 앞선다.**
엔비디아의 "FY2025 1분기"는 달력으로 2024년 4~6월이다. 정상이다.

> **검사 ①을 그대로 수리에 썼다면 멀쩡한 잣대값 307칸을,
> 그것도 NVDA·MRVL·SNOW·CRM 같은 주요 종목에서 지울 뻔했다.**

54차에 주식분할 규칙으로 **진짜 실적 하락 71건을 지운** 사고와 **똑같은
종류**다. 그때 만든 규칙("실데이터에 전수로 걸어 보고 나서 채택한다")이
이번에 실제로 사고를 막았다.

## ③ 남은 두 검사도 **아직 검증되지 않았다**

②·③을 "확인된 결함"으로 부르지 않는다. ①이 이렇게 틀렸으니 나머지도
같은 의심을 받아야 한다.

- **②(같은 라벨 두 번, 254행)**: COHR 은 `22 Q2` 가 2022-03-24 와
  2023-02-08 두 번 있다(11개월 차이) — 진짜 중복으로 보인다. 그러나
  STRL `25 Q4` 2026-02-25 / 2026-04-08 은 잠정치와 확정치일 수 있다.
  **개별 확인이 필요하다.**
- **③(월 스타일 + 역산, 59행)**: KLAC `24/03` 은 발표일이 2024-03-18 로
  분기 종료(3월 말)보다 **앞선다**. 그런데 역산 행은 원래 분기 종료일을
  못 알아내 발표일로 채운다(65차 실측: 역산 행의 73%). 그렇다면 이
  라벨은 **분기가 아니라 발표 달**을 적은 것일 뿐 결함이 아닐 수 있다.

## ④ 이번 차수에서 **고친 것은 없다** — 그것이 성과다

세 검사 중 하나는 확실히 틀렸고 둘은 미검증이다. 지금 어느 것도
수리에 쓸 수 없다. 대신 **307칸을 지울 뻔한 것을 막았다.**

> **규칙에 더한다: 구조 검사도 검사가 필요하다.**
> "값의 크기를 안 보니 안전하다"는 것은 착각이었다. 크기를 안 봐도
> **다른 정상 사유**(회계연도·발표 관행)를 오염으로 오인할 수 있다.
> 새 검사는 반드시 **종목별로 걸린 비율**을 먼저 본다 — 한 종목의
> 대부분이 걸리면 그것은 결함이 아니라 그 회사의 성질이다.

## ⑤ 다음 순서

1. ②의 254행을 **종목별 비율**로 먼저 훑는다(①과 같은 함정 확인).
2. 남은 것만 개별로 본다.
3. ③은 역산 경로의 라벨 규칙부터 코드로 확인한 뒤 판단한다.
4. GS 형(연간값)은 여전히 크기로만 잡히므로 **지우지 말고 표시**부터.

## ⑥ 시험

코드는 안 고쳤다. `python3 run_tests.py` 종료코드 0 ·
**305개 통과, 0개 실패**

---

# 67차 — JPM 이익 시계열이 **배당금과 톱니**였다 (수리함) (2026-08-16)

주인 지시: *"모델링보다는 수집한 데이터부터 정확한 게 중요해 보인다."*
그 방향으로 오염을 계속 캐다가 **실제 결함을 찾아 고쳤다.**
**재는 판: `538dd4a` + 새 가드**

## ① 찾은 것 — JPM 의 이익이 배당금과 번갈아 나온다

손질을 **통과한** JPM 잣대 시계열:

```
2025-04-11  1.4      2025-04-11  5.08
2025-07-15  1.4      2025-07-15  5.25
2025-10-14  1.5      2025-10-14  5.08
2026-01-13  1.5      2026-01-13  4.64
```

같은 발표일에 두 값이 있고, 작은 쪽은 **JPM 의 분기 배당금**이다 —
1.15 → 1.25 → 1.40 → 1.50 은 JPM 의 실제 배당 인상 일정과 값·시점이
정확히 일치한다. 매출도 20.3·22.6 처럼 말이 안 된다(실제 17,653 백만).

**TTM·신고점·이익 델타가 전부 무의미해진다.** 46차에서 "금융이 주도"가
데이터 결함이었다고 밝힌 것과 같은 뿌리다.

## ② 기존 가드는 왜 못 잡았나 (원칙 6)

이 저장소는 **이미 이 결함을 알고 있었다.** `_drop_parse_debris` 가
"같은 발표일 형제 행보다 매출이 100배 작으면 배당금 표 잔해"로 버린다
(54차 등록, 시험 문구에 "**분기 배당금**이 EPS 칸에 들어옴"이라고 적혀 있다).

못 잡는 경우가 하나 있었다 — **양쪽 매출이 다 무너진 날**이다:

| JPM 2025-01-15 | 매출 | gaap_eps | |
|---|---|---|---|
| 가짜(배당금) | 20.3 | 1.4 | |
| 진짜(실적) | 13.0 | 4.82 | |

비율이 1.56배뿐이라 100배 규칙이 안 걸리고, **게다가 가짜 쪽 매출이 더
크다.** 매출로는 가릴 방법이 없다.

## ③ 고친 방법 — 가릴 수 없으면 **둘 다** 버린다

`_drop_same_day_siblings`: 같은 발표일에 형제 행이 있고 **어느 쪽도 매출이
멀쩡하지 않으면** 양쪽의 잣대값을 없음 처리한다.

**왜 골라서 남기지 않는가**: "큰 쪽이 진짜"는 크기 규칙이고, 54차에
크기 규칙으로 **진짜 실적 하락 71건을 지운** 사고가 있었다. 가릴 방법이
없으면 없음으로 둔다 — 없음은 안전하고 틀림은 위험하다(원칙 1).

**매출이 하나라도 멀쩡한 날은 건드리지 않는다.** 그 경우는 기존 100배
규칙의 영역이고, 54차가 "비슷한 크기면 둘 다 남긴다"고 이미 정해 뒀다.
새 가드가 옛 결정을 덮어쓰지 않게 시험으로 못 박았다.

## ④ 실측 — 무엇이 달라졌나

```
이 가드가 지운 칸: 14건  (JPM 10 · JNJ 4)
잣대 미달: 9종목 → 10종목  (JPM 이 추가됨)
완성 894건 · 델타 구멍 22.6% → 23.2%
현재 주도: 기기 OEM 반도체 (변화 없음)
```

**JPM 이 잣대 미달로 떨어진 것은 후퇴가 아니라 정직해진 것이다.**
톱니 시계열로 잘못 재는 것보다 "못 잼"이 낫다. 델타 구멍이 0.6%p 늘어난
것도 같은 이유다 — 없던 정확성이 생긴 게 아니라, **없었다는 사실이
드러난 것**이다.

## ⑤ 처음 만든 가드는 **너무 넓었다** — 기존 시험 6개가 막았다

처음에는 "같은 발표일 형제면 무조건 둘 다 버린다"로 만들었다.
기존 시험 6개가 빨간 불이 됐고, 그중 하나는 **의도적으로 반대를
요구**하는 시험이었다(`test_sibling_rows_of_similar_size_are_both_kept`,
54차 등록). 나머지 5개는 시험 자료가 발표일을 공유해 생긴 것이었다.

> **기존 시험이 내 과잉 수리를 막았다.** 시험을 고쳐 통과시키는 대신
> **가드를 좁혔다.** 시험이 옳고 내 규칙이 넓었기 때문이다.

## ⑥ 검증

데이터 계층 시험 29개(5개 추가) · **변이 5건으로 빨간 불 확인**:
①가드 끄기 ②매출 멀쩡해도 지우기 ③형제 하나뿐인 날도 지우기
④발표일 없는 행끼리 묶기 ⑤조용히 지우기.

**가짜 초록불 1건 적발**: ④가 처음엔 초록불이었다. 시험 자료의 매출이
멀쩡해서 가드가 매출 검사에서 먼저 넘어가 버려, "발표일로 묶는가"를
가리지 못했다. 매출도 함께 무너뜨리니 갈렸다.

> 66차에 배운 것과 같은 자리다 — **막으려는 상황을 자료 안에 실제로
> 만들어야 한다.** 이번엔 조건이 둘(발표일 없음 + 매출 무너짐)이라
> 둘 다 갖춰야 시험이 작동했다.

`python3 run_tests.py` 종료코드 0 · **310개 통과, 0개 실패**

## ⑦ 아직 남은 것 — 금융 종목 전반이 의심스럽다

같은 조사에서 손질 후에도 이상한 값들이 보였다(이번 가드로는 안 잡힘):

```
BAC  12.76 · 13.02 · 13.97 · 13.61   (실제 분기 EPS 는 ~0.9)
BAC   0.28 · 0.32                    (배당금으로 보임)
COF  17.72 · -8.58                   (다른 분기는 3~5)
GS   59.45 · 40.54 · 51.32           (연간값 — 63차)
SCHW 95.30                           (다른 분기는 1.0~8.1)
```

**금융 묶음의 잣대를 지금 상태로 믿으면 안 된다.** 다음 일감이다.

---

# 68차 — 매출 자리채움 값 제거 (2026-08-16)

67차 끝에 "금융 종목 전반이 의심스럽다"고 적어 둔 것을 이어서 팠다.
**재는 판: `0dbceb7` + 새 가드**

## ① 찾은 것 — 매출이 지어낸 값이었다

한 종목 안에서 **소수점까지 똑같은 매출값**이 여러 분기에 반복되는지 셌다.
진짜 매출은 분기마다 다르므로, 반복은 곧 자리채움이다.

| 종목 | 반복 | 반복된 값 | 묶음 |
|---|---|---|---|
| OXY | 20/23분기 | **1** | 에너지 원자재 |
| BAC | 17/24분기 | **2,000,000** | 금융 중개 |
| COF | 8/24분기 | **−5,000,000** | 금융 중개 |
| PG | 6/20분기 | **2** | 소비 최종재 |
| ETN | 4/24분기 | 23,200,000,000 (연간값으로 보임) | 산업 설비 |
| BE | 4/18분기 | 1,000 | 분산형 에너지 |
| MRK | 4/8분기 | **−9** | 의약·의료 |

**COF 와 MRK 는 음수 매출이다.** 매출은 음수가 될 수 없다.
BAC 의 실제 분기 매출은 250억 달러대인데 2,000,000 이 17분기에 박혀 있다.

## ② 왜 이게 중요한가 — 매출은 **다른 가드들의 잣대**다

매출 자체는 잣대(측정값)가 아니다. 그런데:

- 형제 행 잔해 판정(`_drop_parse_debris`)은 **매출 100배 비율**로 가른다
- 영업이익 마진 검사도 매출 기준
- EBITDA 단위 검사도 매출 기준
- 67차의 같은날 형제 가드도 "매출이 멀쩡한가"로 갈린다

**가짜 매출이 통행하면 이 가드들이 틀린 기준으로 판단한다.**
실물: BAC 은 형제 행 매출이 둘 다 2,000,000 으로 **같아서** 100배 규칙이
아무것도 못 걸렀다. 67차 가드도 "매출이 있으니 가릴 수 있다"고 판단해
그냥 넘어갔다.

## ③ 고친 방법

`_drop_repeated_revenue`: 한 종목의 매출값이 **4개 분기 이상 똑같으면**
그 값을 전부 없음 처리한다. 고치지 않고 버린다(창작 금지).
매출이 없으면 위 가드들은 그냥 넘어간다 — 틀린 기준으로 판단하는 것보다 낫다.

**문턱 4의 근거**: 3개까지는 우연일 수 있다. 실측된 자리채움은 전부
4개 이상(4·4·4·6·8·17·20)이다. 반대 방향(너무 예민해서 진짜 매출을
지우는 것)도 시험으로 막았다.

이 가드는 형제 판정보다 **먼저** 돈다 — 그래야 뒤의 가드들이 올바른
기준을 쓴다.

## ④ 실측

```
자리채움 매출로 지운 칸: 21건  (BAC 17 · ETN 4)
잣대 미달: 10종목 (변화 없음)
델타 구멍: 23.2% (변화 없음)
현재 주도: 기기 OEM 반도체 (변화 없음)
```

OXY·COF·PG·BE·MRK 는 **이미 매출 하한 검사(1만 미만)에서 걸러지고
있었다** — 이 가드가 새로 잡은 것은 하한을 통과하던 BAC 2,000,000 과
ETN 232억뿐이다. 판정 결과는 안 바뀌었지만, **BAC 의 형제 판정이 이제야
올바른 기준으로 돈다.**

## ⑤ 시험 자료가 현실과 달라 가드가 연쇄했다

새 가드를 넣자 기존 시험 3개가 깨졌다. 원인은 시험 자료가 **매출과
발표일을 8행 모두 공유**한 것이었다 — 그래서 ①자리채움 판정 → 매출 없음
→ ②같은날 형제 판정까지 연쇄로 걸렸다.

진짜 데이터에서는 이 연쇄가 안 난다(BAC 은 같은날 형제가 없고, JPM 은
매출이 분기마다 다르다). **시험 자료를 현실처럼 고쳤다** — 분기마다 매출과
발표일이 다르게. 가드를 약하게 만들지 않았다.

> **배운 것: 시험 자료가 현실과 다르면 가드끼리의 연쇄를 과장한다.**
> 기본값을 그대로 쓴 여러 행은 현실에 없는 모양이다.

## ⑥ 검증

데이터 계층 시험 32개(3개 추가) · **변이 3건으로 빨간 불 확인**:
①가드 끄기 ②문턱을 2로(너무 예민 — 진짜 매출을 지움) ③조용히 지우기.

`python3 run_tests.py` 종료코드 0 · **313개 통과, 0개 실패**

## ⑦ 아직 안 고친 것

BAC 의 EPS 자체는 여전히 이상하다 — 12.76·13.02·13.97·13.61(실제 분기
EPS 는 ~0.9). 라벨도 `25 Q1` 이 5번 연속으로 멈춰 있다. SCHW 95.30,
COF 17.72·−8.58, GS 연간값도 그대로다.

이들은 **크기로만 갈리는** 종류라 위험하다(54차 71건 사고). 다음 차수에서
"지우지 말고 표시부터" 방식으로 다룬다.

---

# 69차 — 매출총이익률 확보 + 판가형/물량형 가설 사전 확인 (2026-08-16)

주인 제안: *"매출과 매출총이익률로 물량상승과 판가상승을 분류하자.
마이크론·샌디스크가 판가형, 크리도·맥스리니어가 물량형. 두 개를 가르는
기준은 백테스팅을 많이 돌려서 적절한 지점을 고정하면 될 것 같다."*

**재는 판: `adfcbf9` + 새 코드**

## ① 매출총이익률은 **이미 수집하고 있었다** — 저장만 안 했다

`gross_margin_pct` 를 보도자료에서 파싱하고 XBRL `GrossProfit` 도 뽑고
있었는데, **스냅샷으로 내보내는 칸 목록에 없어서** 개발 환경에서 쓸 수가
없었다. 수집 비용은 늘지 않는다 — 저장만 하면 된다.

- `measure_store.EPS_FIELDS` 에 `gross_margin_pct` 추가
- `dataset` 의 숫자 검사 대상에 넣고 **범위 검사**(−100 ~ 100%)도 붙였다.
  이익률은 100%를 넘을 수 없고(매출총이익 ≤ 매출), −100% 아래면 원가가
  매출의 2배를 넘는 것이라 표의 다른 숫자를 집었을 가능성이 크다.

## ② 매출만으로는 두 유형이 **안 갈린다** (실측)

| 종목 | 분기변화 중앙 | 변화폭 σ | 최대낙폭 |
|---|---|---|---|
| MU (판가형) | +13.1% | **26.9%** | −57% |
| SNDK (판가형) | +11.3% | **34.7%** | −10% |
| **CRDO (물량형)** | **+19.3%** | **27.2%** | −41% |
| MXL (물량형) | +2.0% | 14.0% | −72% |
| MPWR (참고) | +4.2% | 8.5% | −11% |

4번 비교 중 3번은 가설대로인데 **MU vs CRDO 가 똑같다(σ 27%)**.
이유는 분명하다 — CRDO 는 분기 매출이 중앙 **+19.3%씩 크는 고성장주**라
매출이 크게 출렁인다.

> **변화폭은 "사이클"과 "성장"을 구분하지 못한다.**
> 그래서 매출총이익률이 반드시 필요하다. 주인 제안의 핵심이 맞다 —
> 판가형은 가격이 오르면 원가는 그대로라 GM 이 같이 오르고,
> 물량형은 아무리 커도 GM 이 대체로 유지된다.

## ③ 매출은 EPS 의 대안이 **못 된다** (68차 이어서)

주인이 "실적 데이터가 어려우면 매출로 바꾸자"고 했으나, 실측은 반대다:

| | 매출 | 잣대(EPS 등) |
|---|---|---|
| 값이 있는 분기행 | 2,548 (83.1%) | 2,471 (80.5%) |
| **8분기 이상 확보 종목** | **140/160** | **150/160** |

게다가 매출에 직전 분기 대비 5배 이상 튀는 칸이 **85건** 있다:
AMD 67억 → 10,270 · INTC 143억 → 4,330억(30배) · ZETA 10,348 ↔ 3억.

**잣대를 바꿀 게 아니라 수집을 고쳐야 한다.** 다만 매출 결함은 단위
붕괴라 배수로 잡혀 **EPS 결함(배당금 혼입·연간값)보다 고치기 쉽다.**

## ④ "백테스팅 많이 돌려 지점 고정"은 그대로 하면 안 된다

이 저장소가 가장 크게 데인 자리다:
H5b 는 채택됐다 데이터가 늘자 뒤집혔고, H11 은 좋아 보였는데 실측
9.4% vs 기준선 26.8% 로 **오히려 나빴다**. 54차는 그럴듯한 규칙으로
진짜 실적 하락 71건을 지웠고, 66차는 내 검사가 멀쩡한 307칸을 지울 뻔했다.

**순서만 바꾸면 주인 방식대로 갈 수 있다:**

| 단계 | 무엇 | 규율 |
|---|---|---|
| 1 | 매출총이익률 확보 | (이번 차수에 완료) |
| 2 | **백테스팅 많이 돌리기** | 탐색 — 전부 기록, 채택 근거 아님 |
| 3 | 나온 문턱을 **새 H번호로 등록** | 데이터 더 보기 전에 |
| 4 | **등록 이후 새 자료로만** 판정 | 원칙 5 |

즉 "많이 돌려 지점 찾기"는 하되, 그 결과를 **"맞다"의 근거가 아니라
"이제부터 이걸 시험한다"의 근거**로 쓴다. H18·H19b 가 이미 그렇게 돈다.

## ⑤ 검증

측정 저장 23개(2개 추가) · 데이터 계층 34개(2개 추가) ·
**변이 3건으로 빨간 불 확인**: ①스냅샷에서 칸 빼기 ②범위 검사 끄기
③범위를 너무 좁혀 정상 적자 마진을 지우기.

`python3 run_tests.py` 종료코드 0 · **317개 통과, 0개 실패**

## ⑥ 다음

로봇을 돌려 `gross_margin_pct` 를 채운다. 채워진 뒤에야 커버리지를 알 수
있고, 그때부터 ④의 2단계(탐색)를 시작할 수 있다.

---

# 70차 — **전략: 판가형/물량형과 주도섹터 분기점** (H25 사전 등록) (2026-08-16)

주인 지시: *"너가 모든 방법과 작업을 진행해서 전략을 세워보고 보고해."*

69차에서 재료(매출총이익률)를 확보했고, 매출만으로는 두 유형이 안 갈린다는
것도 실측했다. 이제 **무엇을 왜 재는지**를 데이터를 더 보기 전에 적는다.

## ① 정의 — 무엇이 판가형이고 무엇이 물량형인가

| | 판가형 | 물량형 |
|---|---|---|
| 매출이 느는 이유 | **단가**가 오른다 | **개수**가 는다 |
| 원가 | 대체로 그대로 | 개수만큼 는다 |
| **그래서 매출총이익률은** | **매출과 같이 움직인다** | **대체로 일정하다** |
| 보기 | 메모리(MU·SNDK) · 에너지 | 신제품 침투(CRDO·MXL) |

핵심은 마지막 줄이다. 가격이 올라 매출이 늘면 원가는 그대로라 **마진이
벌어진다.** 개수가 늘어 매출이 늘면 원가도 같이 늘어 **마진은 제자리다.**

## ② 지표 — 변동성이 아니라 **상관**이다

주인은 "매출과 매출총이익률로 가르자"고 했다. 그런데 **매출의 변동성**으로
가르면 안 된다는 것을 69차에 실측했다:

```
MU(판가형)   σ26.9%      CRDO(물량형) σ27.2%   ← 똑같다
```

CRDO 는 분기 매출이 중앙 +19.3%씩 크는 **고성장주**라 매출이 크게 출렁인다.
**변동성은 "사이클"과 "성장"을 구분하지 못한다.**

그래서 지표를 이렇게 정한다:

> **유형점수 = 같은 종목의 시계열에서 corr(Δ매출, Δ매출총이익률)**
>
> · 판가형: 상관이 **양수이고 크다** (매출이 늘 때 마진도 벌어짐)
> · 물량형: 상관이 **0 근처** (매출이 늘어도 마진은 제자리)

이러면 CRDO 는 매출이 아무리 출렁여도 마진이 따라 움직이지 않으므로
물량형으로 갈린다. 변동성으로는 못 하던 구분이다.

- 창: **최근 12분기**(3년). 사이클 한 바퀴를 담되 옛 사업 구조를 안 섞음
- 최소 표본: **8분기** (잣대 사다리와 같은 문턱)
- 못 재면 **"판단 불가"** — 억지로 한쪽에 넣지 않는다

## ③ 오분류 위험 — 미리 적는다

세 갈래가 **물량형인데 마진을 올려** 판가형으로 오분류시킨다:

1. **규모의 경제** — 물량이 늘면 고정비가 분산돼 마진이 오른다.
   반도체가 특히 심하다. 가장 아픈 오분류 경로다.
2. **제품 믹스** — 고마진 제품 비중이 늘면 판가도 물량도 아닌데 마진이 오른다.
3. **원가 하락** — 판가와 무관하게 마진이 오른다.

셋 다 **같은 방향(판가형으로 치우침)** 이므로, 문턱은 판가형 쪽을
**엄격하게** 잡아야 한다. 이것을 탐색 전에 적어 둔다 — 나중에 결과를 보고
"판가형이 많이 나왔으니 문턱을 낮추자"고 하면 규율 위반이다.

## ④ 왜 이것이 주도섹터 모델에 필요한가 — **분기점이 유형마다 다르다**

이것이 이 전략의 값어치다. 주인의 원래 물음은 "주도섹터 교체를 언제
알아채는가"였다. 지금 H21 은 **이익 델타 꺾임 하나**만 본다.

| 유형 | 주도를 잃는 방식 | **먼저 꺾이는 것** |
|---|---|---|
| **판가형** | 가격 사이클이 돈다 | **마진이 먼저** 꺾인다 — 매출은 아직 높은데 마진이 준다 |
| **물량형** | 침투가 포화된다 | **매출 증가율이** 꺾인다 — 마진은 그대로인 채 |

**유형을 모르면 엉뚱한 곳을 본다.** 판가형 주도에서 매출만 보고 있으면
늦고, 물량형 주도에서 마진만 보고 있으면 영영 신호가 안 온다.

지금 주도인 **기기 OEM 반도체**가 어느 쪽인지에 따라 "언제 주도가
끝나는가"의 답이 달라진다. 그것이 이 분류가 모델에 기여하는 지점이다.

## ⑤ H25 사전 등록 — **지금 정할 수 있는 것은 지금 정한다**

원칙 2 대로, 데이터를 더 보기 전에 적는다.

**지금 고정 (탐색 결과와 무관):**
- 정의 = ①의 표
- 지표 = corr(Δ매출, Δ매출총이익률), 창 12분기, 최소 8분기, 못 재면 판단 불가
- **표적·기준선·채택 기준은 H20·H22 와 완전히 동일**:
  창 60거래일 · 성공 = 새 주도 − 옛 주도 ≥ +10%p ·
  채택 = 신호 윌슨 하한 > 기준선 상한 · n≥10
- 오분류가 **판가형 쪽으로 치우친다**는 것(③)을 미리 적었으므로,
  탐색에서 판가형이 많이 나와도 그것을 근거로 문턱을 낮추지 않는다
- H21(현행 분기점)을 **폐기하지 않는다** — H19b·H22 선례대로 나란히 잰다

**탐색 뒤에 정할 것 (그리고 그때 다시 등록):**
- 판가형/물량형을 가르는 상관 문턱
- 유형별 분기점 신호의 구체적 문턱

**판정 표본:** 문턱을 등록한 날 **뒤**에 생기는 전환만.
지금까지의 자료로 잰 것은 전부 **탐색**이며 채택 근거로 인용 금지.

## ⑥ 단계 — 지금 어디에 있나

| 단계 | 무엇 | 상태 |
|---|---|---|
| 1 | 매출총이익률을 스냅샷에 담기 | ✅ 69차 완료 |
| 2 | 로봇을 돌려 값을 채우기 | ⏳ 실행 중 (`31959694187`) |
| 3 | **커버리지 측정** — 몇 종목이 8분기 이상 GM 을 갖나 | 대기 |
| 4 | **탐색** — 전 종목 유형점수를 재고 분포를 본다 | 대기 |
| 5 | 문턱 등록 (H25 확정) | 대기 |
| 6 | 등록 이후 새 자료로만 판정 | 몇 년 |

**3단계가 관문이다.** GM 커버리지가 낮으면(예: 절반 이하) 이 전략은
표본 부족으로 시작할 수 없다. 그때는 "GM 파서 보강"이 먼저다.
보관된 실패 원문에서 잰 하한은 18%였다 — 실제 커버리지는 그보다 높겠지만
얼마인지는 로봇이 끝나야 안다.

## ⑦ 이 전략이 **틀릴 수 있는 지점**을 미리 적는다

- **GM 커버리지가 낮으면** 시작 못 한다 (3단계 관문)
- **오분류가 한쪽으로 치우친다**(③) — 판가형이 과대 집계될 것이다
- **유형은 변한다** — 회사가 사업 구조를 바꾸면 유형도 바뀐다.
  창 12분기는 그것을 따라가되, 전환기에는 둘 다 아닌 값이 나온다
- **분기점 가설(④)은 아직 아무 근거가 없다** — 그럴듯한 이야기일 뿐이고,
  H21 이 이미 3건 중 1건만 맞았다. 유형을 나눈다고 나아진다는 보장은 없다
- 전환이 연 4.2건이라 **n≥10 에 2년 이상**, 채택까지는 더 걸린다

## ⑧ 이번 차수에 코드는 안 고쳤다

전략 수립과 사전 등록이다. `python3 run_tests.py` 종료코드 0 ·
**317개 통과, 0개 실패**

---

# 71차 — H25 탐색: **등록한 지표가 작동하지 않는다** (2026-08-16)

70차에 등록한 유형점수를 로봇이 채운 자료로 실제로 재봤다.
**재는 판: `19253c3`** ⚠️ 전부 **탐색**이며 채택 근거가 아니다(원칙 5).

## ① 3단계 관문 — 통과

로봇이 매출총이익률을 채웠다(실행 `31959694187`, 18:25 완료).

```
분기행 3,070 중 GM 있는 행 1,878 (61.2%)
8분기 이상 GM 확보 종목: 103/160 (64%)
MU 19/19 · SNDK 9/9 · CRDO 17/17 · MXL 20/20  ← 지목 종목은 전부 완비
```

70차가 정한 관문(절반 이상)을 넘었다. 탐색 시작 가능.

## ② 그런데 지표가 **가설대로 갈리지 않는다**

유형점수 = corr(Δ매출, Δ매출총이익률), 창 12분기:

| 종목 | 주인 지목 | 유형점수 |
|---|---|---|
| MU | 판가형 | +0.42 |
| SNDK | 판가형 | +0.63 |
| CRDO | 물량형 | +0.18 ✅ |
| **MXL** | **물량형** | **+0.49** ❌ |

MXL 이 물량형인데 MU 보다 높다. 그리고 전체 순위를 보면 더 이상하다:

```
판가형 쪽 상위: MKSI +0.93 · TXN +0.91 · HUBS +0.89 · TEAM +0.80 · WMT +0.65
물량형 쪽 극단: U -0.83 · ADI -0.77 · EL -0.72 · AVGO -0.59 · ADBE -0.55
```

**HUBS·TEAM 은 구독 소프트웨어**이고 **WMT 는 월마트**다. 상품 가격
사이클과 아무 상관이 없다. 반대로 **ADI·AVGO** 는 가격 결정력이 강한
아날로그·인프라 반도체인데 물량형 극단에 있다.

> **지표가 재려던 것을 재고 있지 않다.**

## ③ 진짜 이유 — **표본 12개로는 상관이 잡음과 구분되지 않는다**

이것이 결정적이다. 표본 n 개의 상관은 우연만으로도 ±1/√n 쯤 나온다.
95%로 0 과 다르다고 말하려면 대략 **|r| > 2/√n** 이어야 한다.

```
종목 93 · 변화 표본 중앙 12개 → 잡음 띠 ±0.58
그 띠 밖에 있는 종목: 18개 (19%)
→ 나머지 75개(81%)는 0 과 구분되지 않는다
```

**주인이 지목한 네 종목이 전부 띠 안이다:**

| 종목 | r | 잡음 띠 | 판정 |
|---|---|---|---|
| MU | +0.42 | ±0.58 | ⚠️ 구분 안 됨 |
| SNDK | +0.63 | ±0.71 | ⚠️ 구분 안 됨 |
| CRDO | +0.18 | ±0.58 | ⚠️ 구분 안 됨 |
| MXL | +0.49 | ±0.58 | ⚠️ 구분 안 됨 |

**MU 와 MXL 의 차이(+0.42 vs +0.49)는 잡음이다.** 순위표의 상위·하위도
대부분 우연이다. 그래서 월마트가 판가형 상위에 오른다.

## ④ 이것이 70차 등록의 값어치다

70차에 문턱을 **미리 안 정한 것**이 옳았다. 만약 "많이 돌려서 좋아 보이는
지점을 고정"했다면, **잡음 위에 문턱을 세우고** 그것을 모른 채 몇 년을
갔을 것이다. 순위표가 그럴듯해 보이기 때문에 더 위험하다.

70차 ③에 적어 둔 오분류 위험(규모의 경제·제품 믹스·원가 하락)보다
**표본 부족이 먼저 걸렸다.** 그건 미리 못 짚었다 — 배운 것으로 적는다:

> **새 지표를 등록할 때는 "그 지표가 잡음과 구분되려면 표본이 몇 개
> 필요한가"를 함께 적는다.** 상관은 ±2/√n, 비율은 윌슨 구간 —
> 문턱을 정하기 전에 **잡을 수 있는 크기**부터 계산한다.

## ⑤ 남은 길 — 셋 중 하나

| 길 | 내용 | 걸림돌 |
|---|---|---|
| ㉮ 창을 늘린다 | 12 → 20분기 이상 | 5년치뿐이고 사업 구조가 바뀐다 |
| ㉯ **묶음 단위로 합친다** | 종목이 아니라 17묶음의 유형을 잰다 | 표본이 종목 수만큼 늘어난다 |
| ㉰ 지표를 바꾼다 | 상관 대신 **마진의 진폭** 등 | 새로 등록해야 한다 |

**㉯가 가장 유망하다.** 애초에 주인의 물음은 "이 **섹터**가 언제 주도를
잃는가"였지 개별 종목이 아니다. 묶음 하나에 12종목 × 12분기 = 144개
변화가 모이면 잡음 띠가 ±0.58 → ±0.17 로 줄어든다.

다만 이것은 **새 지표**이므로 다시 등록해야 한다. 이번 탐색 결과를 보고
정하는 것이라, 판정은 등록 이후 자료로만 한다.

## ⑥ H25 는 지금 상태로 **채택 불가**

문턱을 정하지 않는다. 종목 단위 유형점수는 표본 부족으로 판정할 수 없다.
묶음 단위(㉯)를 다음 차수에 등록하고, 그때 잡음 띠도 함께 적는다.

## ⑦ 시험

코드는 안 고쳤다(탐색). `python3 run_tests.py` 종료코드 0 ·
**317개 통과, 0개 실패**

---

# 72차 — H26 등록: **묶음 단위 유형점수** (잡음 띠를 먼저 적는다) (2026-08-16)

71차에서 종목 단위가 표본 부족으로 깨졌다. 묶음 단위로 옮긴다.
71차에 배운 대로 **"잡음과 구분되려면 얼마나 커야 하는가"를 점수를 보기
전에** 적는다. **재는 판: `0722eaf`**

## ① 표본 크기 — 구조적 사실 (점수와 무관하므로 지금 적어도 된다)

| 묶음 | 변화 표본 | 잡음 띠 ±2/√n |
|---|---|---|
| 기기 OEM 반도체 | 206 | **±0.14** |
| 좌석·계약 정액 구독 소프트웨어 | 192 | **±0.14** |
| 반도체 팹 장비·소재 | 132 | ±0.17 |
| 소비 최종재·유통 | 96 | ±0.20 |
| 교체주기 기업 IT 하드웨어 | 85 | ±0.22 |
| 광통신·대역폭 장비·부품 | 75 | ±0.23 |
| 산업 설비·자동화·수송 | 67 | ±0.24 |
| 의약·의료 | 64 | ±0.25 |
| 분산형 에너지·전동화 완제품 | 60 | ±0.26 |
| 데이터센터 시스템·전력설비 납품 | 45 | ±0.30 |
| 소비 거래·결제 플랫폼 | 36 | ±0.33 |
| 현물가 연동 메모리·저장장치 | 35 | ±0.34 |
| 광고 플랫폼 | 12 | ±0.58 ⚠️ |
| **발전·전력 판매** | **0** | **잴 수 없음** |
| **금융 중개·자본시장** | **0** | **잴 수 없음** |
| **통신·미디어 회선** | **0** | **잴 수 없음** |
| **에너지 원자재·유전 서비스** | **0** | **잴 수 없음** |

종목 단위 띠 중앙 ±0.58 → **묶음 단위 ±0.26**. 표본이 12 → 1,105 로 늘었다.

## ② 그런데 **4묶음은 영영 못 잰다** — 지표의 근본 한계

은행·유틸리티·통신·에너지는 **매출총이익률이라는 개념이 없다.**
매출원가가 그런 식으로 잡히지 않는 업종이다. 이것은 수집을 고쳐서 될
일이 아니라 **지표의 성질**이다.

> ⚠️ **통신·미디어 회선은 210주 중 50주를 주도한 묶음인데(최장)
> 이 지표로는 영영 판정할 수 없다.**

에너지 원자재도 24주 주도한 묶음이다. **지난 5년 주도 주의 상당 부분이
이 지표의 사각지대에 있다.** 이것을 채택 전에 못 박아 둔다 — 나중에
"대부분 묶음이 잘 갈린다"고 말하면 안 된다. **갈린 것은 잴 수 있는
묶음뿐이다.**

## ③ H26 사전 등록

- **지표**: 묶음 유형점수 = corr(Δ매출, Δ매출총이익률), 그 묶음 **모든
  종목의 변화를 합쳐서** 잰다. 창 12분기.
- **판정 가능 조건**: 변화 표본 n ≥ 30 **그리고** |r| > 2/√n.
  둘 중 하나라도 못 넘으면 **판단 불가**. 억지로 유형을 붙이지 않는다.
  (n≥30 은 띠가 ±0.37 아래로 내려가는 지점이다 — 표를 보기 전에 정했다)
- **유형**: 띠를 넘고 **양수**면 판가형, 띠를 넘고 **음수**면 물량형.
  띠 안이면 **"섞임"** — 이것도 사실이므로 그대로 적는다.
- **표적·기준선·채택 기준**: H20·H22·H25 와 **완전 동일**
  (창 60거래일 · +10%p · 윌슨 하한 > 기준선 상한 · n≥10)
- **판정 표본**: `2026-08-16` **뒤**에 생기는 전환만. 오늘까지는 전부 탐색.
- H21(현행 분기점) 폐기 금지 — 나란히 잰다.

## ④ 미리 적는 한계

- **4묶음(발전·금융·통신·에너지)은 영영 판단 불가** — ②
- 광고 플랫폼은 표본 12로 지금은 판단 불가
- 오분류는 **판가형 쪽으로 치우친다**(70차 ③: 규모의 경제·제품 믹스·
  원가 하락). 결과를 보고 문턱을 낮추지 않는다
- 묶음의 유형이 **한 가지가 아닐 수 있다** — 12종목을 합치면 서로 다른
  성질이 상쇄돼 "섞임"으로 나올 수 있다. 그것도 사실로 적는다

## ⑤ 탐색 결과 (⚠️ 채택 근거 아님 — 원칙 5)

| 묶음 | 표본 | 유형점수 | 띠 | 판정 |
|---|---|---|---|---|
| 광통신·대역폭 장비·부품 | 75 | **+0.41** | ±0.23 | **판가형** |
| 현물가 연동 메모리·저장장치 | 35 | **+0.38** | ±0.34 | **판가형** |
| 소비 거래·결제 플랫폼 | 36 | +0.30 | ±0.33 | 섞임 |
| 의약·의료 | 64 | +0.11 | ±0.25 | 섞임 |
| 반도체 팹 장비·소재 | 132 | +0.07 | ±0.17 | 섞임 |
| 데이터센터 시스템·전력설비 납품 | 45 | +0.02 | ±0.30 | 섞임 |
| **기기 OEM 반도체 (현재 주도)** | **206** | **+0.00** | **±0.14** | **섞임** |
| 소비 최종재·유통 | 96 | −0.01 | ±0.20 | 섞임 |
| 교체주기 기업 IT 하드웨어 | 85 | −0.08 | ±0.22 | 섞임 |
| 좌석·계약 정액 구독 소프트웨어 | 192 | −0.11 | ±0.14 | 섞임 |
| 산업 설비·자동화·수송 | 67 | −0.16 | ±0.24 | 섞임 |
| 분산형 에너지·전동화 완제품 | 60 | **−0.26** | ±0.26 | **물량형** (겨우) |
| 광고 플랫폼 | 12 | −0.83 | ±0.58 | 판단 불가 |

## ⑥ 읽어야 할 것 셋

**㉠ 지표가 작동하는 곳이 있다 — 이름과 맞는다.**
`현물가 연동 메모리·저장장치`(MU·SNDK·STX·WDC)가 **판가형**으로 나왔다.
묶음 이름 자체가 "현물가 연동"이고, 주인이 처음 지목한 것도 MU·SNDK 였다.
71차의 종목 단위에서는 잡음에 묻혀 안 보이던 것이 묶으니 드러났다.
광통신도 판가형인데, 광부품은 실제로 단가 협상이 실적을 좌우한다.

**㉡ 그러나 13묶음 중 3개만 유형이 붙는다.** 10개가 섞임/판단 불가다.

**㉢ 가장 아픈 것 — 현재 주도 묶음의 유형을 모른다.**
`기기 OEM 반도체`는 표본 206에 띠가 ±0.14 로 아주 좁은데 점수가
**정확히 +0.00** 이다. 이것은 "표본이 부족해 모른다"가 아니라
**"확실히 한 유형이 아니다"** 는 뜻이다. 20종목을 합치니 서로 다른 성질이
상쇄된 것으로 보인다 — 72차 ④에 미리 적어 둔 위험 그대로다.

> **전략의 목적(분기점을 유형별로 보기)이 정작 지금 주도 묶음에서는
> 이룰 수 없다.** CRDO·MXL 도 이 묶음이라, 주인이 물량형으로 지목한
> 종목들이 섞임 안에 묻혀 있다.

## ⑦ 그래서 H26 도 지금은 **채택 불가**

문턱을 정하지 않는다. 유형이 붙은 묶음이 3개뿐이고, 그중 정작 주도인
묶음이 없다. 표적(전환 후 60거래일)을 재려 해도 **유형별로 나눌 전환
사건이 거의 없다.**

**다만 지표를 버리지는 않는다.** ㉠이 실재한다 — 메모리·광통신에서는
값이 이름과 맞고 띠 밖이다. 관찰 목록에 두고 로봇이 표본을 쌓는 대로
자동 재판정한다(원칙 3).

## ⑧ 다음에 시도할 것 (아직 등록 아님)

- **묶음을 더 잘게** — `기기 OEM 반도체` 20종목이 섞이는 것이 문제라면
  45차의 `GROUPS_FINE`(19버킷)으로 나눠 보는 길이 있다. 다만 잘게 쪼개면
  표본이 줄어 잡음 띠가 다시 넓어진다 — **정면충돌하는 두 요구**다.
- **종목별 유형을 묶음에서 빌리지 않기** — 애초에 유형은 종목의 성질이지
  묶음의 성질이 아닐 수 있다. 그렇다면 종목당 표본을 늘리는 길(창 확대·
  이력 확대)뿐인데 5년치가 한계다.

## ⑨ 시험

코드는 안 고쳤다(등록 + 탐색). `python3 run_tests.py` 종료코드 0 ·
**317개 통과, 0개 실패**

---

# 73차 — 재료 감사: **오염이 오염을 가려 주고 있었다** (2026-08-16)

주인 지시 "모델링보다는 **수집한 데이터부터 정확성을 완벽**하게".
그래서 72차의 모델링(묶음 쪼개기)을 미루고 재료로 돌아왔다.

## ① 문턱 없이 세기 — 63차의 잘못을 고쳤다

63차는 `|EPS| > 40` 이라는 **임의의 문턱**으로 7칸을 찾았다. 근거가 없는
숫자였고 실제로 GS 30.06·22.87(연간값)을 놓쳤다. 이번엔 문턱을 정하지
않고 두 가지를 먼저 재 봤다.

**시도 ㉠ — 종목 자신의 중앙값 대비 배수. 실패했다.**
4,584칸을 재니 상위가 이렇게 나왔다.

| 배수 | 종목 | 값 | 중앙값 |
|---|---|---|---|
| 23.2배 | CRDO | 1.16 | 0.05 |
| 15.8배 | GOOGL | 26.29 | 1.66 |
| 16.0배 | AMZN | 12.37 | 0.78 |

**전부 진짜 성장이다.** CRDO 는 조정 EPS 가 0.05 에서 1.16 까지 실제로
자랐다. 종목 적중 비율 상위도 SMCI 31% · CRDO 29% · PLTR 17% 로 전부
급성장주였다 — 66차 함정을 그대로 다시 밟은 것이다.

**시도 ㉡ — 바로 옆 분기와만 비교. 이게 갈랐다.**
성장은 이웃과 가깝고, 오염은 **앞·뒤 이웃 둘 다에서 멀다.** 이웃 대비
3배 이상 튄 칸은 4,037칸 중 **95칸(2.35%)** 이었고, CRDO·GOOGL·AMZN 은
한 칸도 걸리지 않았다.

## ② 진짜 결함: 가드가 자기가 못 잡은 값에 막혀 있었다

원칙 6(신호보다 장치를 먼저 의심)대로 장치부터 봤다. 52차에 넣은
누적값 가드는 "이 값이 **직전 4분기 합**과 거의 같은가"를 본다. 그런데
**직전 4분기 안에 이미 오염된 값이 끼어 있으면 합이 부풀어** 다음 연간값이
통과해 버린다. **오염이 오염을 가려 주고 있었다.**

실물 VZ(조정 EPS) — 1월 발표 행마다 연간값이 들어와 있다:

| 발표일 | 값 | 직전 4분기 합 | 판정 |
|---|---|---|---|
| 2023-01-24 | 5.18 | (이력 부족) | 통과 |
| 2024-01-23 | 4.71 | **8.81**(5.18 포함) | 통과 ← 가려짐 |
| 2025-01-24 | 4.59 | **8.20**(4.71 포함) | 통과 ← 가려짐 |
| 2026-01-30 | 4.71 | **8.41**(4.59 포함) | 통과 ← 가려짐 |

**고친 것**: 한 칸 지울 때마다 처음부터 다시 재기를, 더 지울 것이 없을
때까지 반복한다. **문턱은 하나도 바꾸지 않았다** — 같은 자를 끝까지 대
볼 뿐이다.

**실측(실행 출력에서 복사)**: 한 번만 재면 **38칸**, 반복하면 **57칸**.
10종목 19칸이 새로 잡혔다.

| 종목 | 칸 | 값 | 직전 4분기 합 |
|---|---|---|---|
| GS | GAAP EPS | 22.87 · 40.54 · 51.32 | 25.59 · 34.07 · 45.68 |
| DELL | GAAP EPS | 7.99 · 7.98 · 8.38 | 8.43 |
| NRG | 조정 EBITDA | 37.25억 (3분기 연속) | 36.01억 |
| ETN | 조정 EPS | 13.05 · 13.40 | 11.83 |
| DDOG·QCOM·ACLS·NTAP·GE·DIS | | 각 1~3칸 | |

근거는 **산술 하나뿐**이다 — 값이 직전 4분기 합과 20% 안으로 같다는 것.
"이것이 회사가 발표한 연간 EPS 와 같은가"는 이 환경에서 원문을 받을 수
없어 **확인 못 함**으로 둔다. 다만 GS 는 다섯 해 연속 1월 발표 행에서만
이 모양이 나와, 우연으로 보기 어렵다.

**주도 판정에 미친 영향**: 현재 주도 묶음은 `기기 OEM 반도체`로 **바뀌지
않았고**, 교체 횟수도 16회로 같다. 다만 **210주 중 7주**의 주도 묶음이
달라졌다. 작지만 실재하는 변화다.

## ③ 자동 삭제를 **포기했다** — 왜인지 적어 둔다

VZ 의 첫 칸(5.18)은 앞에 비교할 분기가 없어 반복으로도 못 잡는다.
그래서 "해마다 같은 자리에서 평소의 3~4배면 연간값"이라는 주기 규칙을
만들어 봤다. **그리고 버렸다.** 이 규칙에 걸린 목록이 이렇다.

| 종목 | 자리 | 값 | 우리 판단 |
|---|---|---|---|
| FSLR | 12-31 | 7.74 · 12.02 · 14.21 | 연간값 의심 |
| HPE | 10-31 | 1.54 | 연간값 의심 (결산월 10월) |
| NTAP | 04-28 | 5.79 | 연간값 의심 (결산월 4월) |
| VZ | 12-31 | 5.18 · 4.71 · 4.59 · 4.71 | 연간값 의심 |
| **ABNB** | **09-30** | **20억 달러 (2회)** | **결산월이 아니다 — 3분기다** |
| MKSI·PFE | | 2.7 안팎 | 근거 약함 |

(결산월은 우리 자료의 `filing_date` 가 해마다 되풀이되는 달로 읽은 것이다.
ABNB 만 12월 결산인데 튀는 자리가 9월이라, "연간값"으로 설명되지 않는다.)

**ABNB 와 FSLR 이 같은 모양이다.** 둘 다 "해마다 같은 자리에서 평소의
3~4배"다. 어떤 배수를 골라도 갈라지지 않는다. 66차에서 회계연도가
앞서가는 회사 307칸을 지울 뻔한 것과 **똑같은 함정**이다.

> 그래서 65차 §④ 에 등록한 방침 **"표시부터, 지우지 말고"** 를 그대로
> 따른다. 지우지 않으니 진짜 실적을 잃지 않고, 세어서 보여 주니 재료가
> 얼마나 더러운지도 감추지 않는다.

## ④ 만든 것 — `audit_data.py` (지우지 않는 상설 조사)

세 가지를 세고 **아무것도 지우지 않는다.** 계기판 맨 위 "재료 상태"
접기 칸에 그대로 나온다.

| 검사 | 실측 |
|---|---|
| 이웃보다 3배 튄 칸 | **94칸 / 4,024칸 = 2.3%** |
| 같은 값 되풀이 (사다리) | **90칸** (28묶음) |
| 기간 라벨 겹침 | **170행 / 39종목** |

라벨 겹침이 39종목이나 되는 것은 새로 안 사실이다(VZ 는 `23 Q1` 이 4행,
`25 Q1` 이 4행). 측정은 발표일로 하므로 계산이 틀리지는 않지만,
**파서가 분기를 못 읽었다는 표시**이므로 함께 센다.

여기 쓴 3배는 **삭제 기준이 아니라 표시 기준**이다. 지우지 않으므로
틀려도 잃는 것이 없다.

## ⑤ 남은 것 (감추지 않는다)

- VZ 첫 칸 5.18, GS 59.45, IPGP 99.10, SCHW 95.30, XOM 89.45,
  UCTT −45.30/−45.10/−44.60 은 **여전히 그대로 있다.**
- 큰 음수 급락(INTC −3.88 · BMY −5.89 · ETSY −7.62)은 **상각·일회성
  비용으로 진짜인 경우가 많다.** 그래서 손대지 않는다.
- 근본 수리는 **수집기 쪽**이다 — 결산 보도자료에서 '분기' 열 대신
  '연간' 열을 무는 것을 파서가 막아야 한다. 개발 환경에서는 원문을
  받을 수 없어 확인 못 함.

## ⑥ 시험 (실행 출력에서 복사)

`python3 run_tests.py` 종료코드 **0** · **336개 통과, 0개 실패**
(317 → 336: 반복 가드 1개 + 오염 조사 15개 + 원문 부탁 목록 3개)

**일부러 깨뜨려 빨간 불을 본 것** (규칙대로):

| 돌연변이 | 빨간 불 |
|---|---|
| 누적 가드를 한 번만 돌게 되돌림 | `test_yearly_values_hidden_behind_each_other_are_all_dropped` — 뒤의 4.80 두 칸이 살아남음 |
| 조사를 이웃 대신 전체 중앙값 비교로 바꿈 | `test_growth_series_is_not_counted_as_contamination` |
| 조사가 값을 지우게 만듦 | `test_audit_never_changes_the_data` · `test_summary_counts_match_the_lists` |
| 부탁 목록을 늘 비어 있게 만듦 | `test_wanted_raw_list_is_read_from_the_repo` |
| 부탁 목록 상한을 없앰 | `test_wanted_list_is_capped` |

## ⑦ 막힌 곳을 뚫을 장치 — "원문 부탁 목록"

⑤에 적은 대로 남은 칸의 원인은 **원문을 봐야** 알 수 있다. 그런데 지금
보관되는 원문은 "잣대값을 **하나도 못 읽은**" 실패 공시뿐이다. VZ 5.18 ·
GS 59.45 처럼 **잘못 읽은** 공시는 값을 읽는 데 성공했으므로 한 건도
남지 않는다. 그래서 감사가 원천적으로 막혀 있었다.

**만든 것**: 조사가 고른 (종목, 발표일)을 `data/measure/wanted_raw.json`
에 적어 커밋하면, 다음 로봇 실행이 **그 공시만 골라 원문을 함께** 담아
온다. **값은 지우지 않는다.**

이번에 적은 목록: **60건 / 43종목** (한 종목 최대 3건 — 보관 자리
12칸에 여유가 있어 실패 원문을 밀어내지 않는다).

목록 파일이 없거나 깨져 있어도 수집은 그대로 돈다(시험으로 확인). 이
길은 SEC 접속이 필요하므로 **개발 환경에서는 실행 증명이 불가능**하다 —
로봇 실행 뒤 `data/measure/raw/` 에 `부탁_` 이름의 파일이 들어오는지로
확인한다.

---

# 74차 — 원문으로 확증한 파서 결함 (73차 ⑦이 실제로 작동했다) (2026-08-16)

## ① 부탁 목록이 작동했다 — 실행 증명

73차 ⑦에서 만든 `wanted_raw.json`(60건)을 커밋하고 로봇을 돌렸다.
`data/measure/raw/` 에 **`부탁_` 이름 파일 56건**이 들어왔다. 새 코드
경로가 실제로 돌았다는 증명이다(개발 환경에서는 SEC 차단으로 불가능).

못 들어온 4건의 내역도 적어 둔다: 3건은 **같은 달 파일 이름 충돌**
(BE·INTC·ABNB — 말머리까지 합쳐 앞 10글자만 잘라 쓰는 옛 규칙 탓),
1건은 XOM 2023-07 로 공시를 못 찾았다. 이름 규칙은 이번에 고쳤다.

## ② 처음으로 **원인**을 눈으로 봤다 — 네 가지, 서로 다른 결함

지금까지는 "이 값이 이상하다"까지만 알 수 있었다. 원문이 오니 **왜**가
보였다. 아래 인용은 전부 실제 보도자료 문장 그대로다.

**㉠ IPGP 조정 EPS 99.10 — 공시 번호를 물었다**
> "… excluded from the calculation of **adjusted EPS**, stock based
> compensation of $11.0 million …"  ⏎ 3 ⏎⏎⏎ "**Exhibit 99.1**"

각주에 있는 이름 뒤로 줄과 문단을 넘어 **300자 뒤의 공시 번호 99.1**을
집었다. 값이 아니라 서류 번호다.

**㉡ GS GAAP EPS 59.45 — 같은 문장 안의 연간값**
> "Diluted earnings per common share (EPS) was **$59.45 for the year
> ended December 31, 2021** compared with $24.74 for the year ended
> December 31, 2020, and was **$10.81 for the fourth quarter of 2021**"

원문이 "for the year ended" 라고 **직접 적어** 두었는데 그걸 안 보고
맨 앞 숫자를 물었다. 진짜 분기값은 같은 문장 뒤쪽의 10.81 이다.

**㉢ VZ 조정 EPS 5.18 — 줄 전체가 연간 줄**
> "•**Full-year** 2022 earnings per share (EPS) of $5.06 … adjusted
> EPS1, excluding special items, of **$5.18**"

**㉣ UCTT GAAP EPS −45.30 — 주식 수를 EPS 로 읽고 부호까지 뒤집었다**
> Net loss per share …: Basic **$(0.40)** / Diluted **$(0.40)**
> **Shares used in computing** net loss per share: Basic **45.3**

"Shares used in computing net loss per **share**" 도 이름에 걸린다.
45.3(백만 주)을 EPS 로 읽고, 이름에 loss 가 있다고 −45.30 으로 만들었다.
주식 수는 해마다 비슷하니 **3년 연속 −45.30 · −45.10 · −44.60** 이
나왔다 — 73차 조사가 "같은 값이 되풀이된다"고 센 그 칸들이다.

## ③ 고친 것 (문턱 넷, 전부 원문 근거)

| 무엇 | 규칙 | 근거 |
|---|---|---|
| 탐색 범위 | 이름 뒤 숫자는 **같은 문단** 안에서만 (빈 줄을 넘지 않음, 300자 상한) | IPGP |
| 값 뒤 연간 | 값 바로 뒤가 "for the year ended" · "for 2021" 이면 건너뜀 | GS |
| 줄머리 연간 | 줄이 "Full-year …" · "Fiscal year 2023 …" 로 **시작**하면 그 줄은 연간 | VZ |
| 주식 수 | 이름 앞에 "shares used in computing" 류가 있으면 EPS 가 아님 | UCTT |

같은 문장 안 재시도 한도도 4 → 10 으로 늘렸다. GS 문장에서 날짜 정수
(31 · 2021 · 31 · 2020)를 건너뛰다 보면 **진짜 분기값에 닿기 전에
포기**했기 때문이다. 탐색을 문단 안으로 못박았으므로 늘려도 멀리 못 간다.

## ④ 전수 비교가 **내 실수 둘**을 잡아냈다

고친 뒤 원문 590건을 수리 전/후로 전부 돌려, **고친 것뿐 아니라 잃은
것**도 셌다. 두 가지가 나왔다 — 둘 다 내가 규칙을 너무 넓게 잡은 탓이다.

**㉠ "fiscal year 2022" 를 연간 표시로 봤다.** 실제로는 **비교 대상**
으로 늘 나오는 말이다.
> "…of $1.13 compared to $1.14 in the fourth quarter of **fiscal year
> 2022**; non-GAAP net income per share of **$1.54**…"

이것까지 연간이라 보면 진짜 분기값 1.54 를 버리고, 같은 줄 끝의
"unfavorable impact of approximately **$0.08** from foreign currency" 를
문다. 실제로 NTAP 이 그렇게 됐다.
→ 되돌아보기는 좁은 규칙만(full-year · for the year · twelve months ·
fiscal year **ended**), 줄머리에서만 "Fiscal year 2023" 까지 본다.

**㉡ "같은 줄"로 못박은 것이 너무 좁았다.** 이름이 **제목 줄**이고 값이
다음 줄에 오는 형식이 실제로 있다.
> "Diluted net earnings per share (“EPS”): ⏎ ◦GAAP of **$0.31**…"

이 형식 2건(HPE)을 잃고 있었다.
→ **같은 문단**(빈 줄을 넘지 않음) + 글자 수 상한 300 으로 넓혔다.
덤으로 CIEN 도 바로잡혔다 — 기초(0.14)가 아니라 **희석 0.13** 이다.

> **이 비교가 수리 자체보다 중요했다.** 안 했으면 결함 둘을 새로 심은
> 채 로봇에 올렸을 것이다. 원문 전수 비교를 앞으로도 파서 수리의
> 마지막 관문으로 삼는다.

**고친 뒤 최종 전수 비교** (실행 출력에서 복사):

| 칸 | 읽은 건수 | 값 바뀜 | 없어짐 | 새로 |
|---|---|---|---|---|
| 조정 EPS | 60 → **59** | **4** | 1 | 0 |
| GAAP EPS | 202 → **201** | **11** | 1 | 0 |
| 매출 | 381 → 381 | 0 | 0 | 0 |
| 조정 EBITDA | 35 → 35 | 0 | 0 | 0 |

바뀐 15칸은 **전부 오염 → 정상**이다:

| 종목 | 전 | 후 |
|---|---|---|
| NTAP | 조정 5.59 · GAAP 5.79 (연간) | **1.54 · 1.13** |
| VZ ×3 | 조정 5.18 · 4.71 · 4.59 (연간) | **1.19 · 1.08 · 1.10** |
| VZ ×3 | GAAP 5.06 · 2.75 · 4.14 | **1.56 · −0.64 · 1.18** |
| GS | GAAP 59.45 (연간) | **10.81** |
| FSLR ×2 | GAAP 12.02 · 4.38 (연간) | **3.65 · 1.23** |
| UCTT ×4 | GAAP −45.3 · −44.6 · −45.1 · −45.3 (주식 수) | **−0.40 · −0.21 · −0.11 · −0.40** |

없어진 2칸은 **둘 다 값이 아닌 것**이었다 — IPGP 99.10(공시 번호),
SCHW 95.30. **멀쩡한 값을 잃은 것은 0건이다.**
매출·조정 EBITDA 는 한 칸도 건드리지 않았다(이번 수리는 EPS 경로만).

한계를 적어 둔다: 이 590건은 **치우친 표본**이다(잣대값을 못 읽은 실패
원문 + 조사가 이상하다고 집은 원문). 잘 읽히던 보도자료는 거의 없으므로
"멀쩡한 것을 잃었나"는 부분적으로만 보인다. 나머지는 로봇 재수집 뒤
스냅샷 전체로 다시 잰다.

## ⑤ 고치지 않고 남긴 것 (원인은 밝혀졌다)

**MXL 2022-05-05 GAAP EPS 93.54** — 이 공시는 실적이 아니라 **인수
발표문**이다. 보도자료 안의 지표 이름 나열
> "… to Operating Income, Operating Margin, **Earnings per Share**, and
> Cash Flow"

을 이름으로 잡고, 125자 뒤의
> "Silicon Motion shareholders to receive **$93.54 in cash** … per ADS"

를 값으로 물었다. 주당 **인수대금**이지 이익이 아니다.

고치려면 "이름 뒤에 값이 아니라 쉼표와 다른 지표 이름이 온다"를 가려야
하는데, 근거 있는 규칙을 아직 못 찾았다. **㉠·㉡ 에서 방금 겪은 대로,
근거 없이 규칙을 넓히면 진짜 값을 잃는다.** 다음 회차로 넘긴다.

## ⑥ 화면 (모바일 412px 실렌더 확인)

새 "재료 상태" 접기 칸을 412px 에서 실제로 렌더링해 확인했다.
`scrollWidth 412 = innerWidth 412` — **가로 스크롤 없음**. 5열 표
(종목·칸·값·앞→뒤·배수)가 잘림 없이 들어간다. 앞·뒤 이웃은 한 칸에
모아 적어 열 수를 줄였다.

확인 중에 56차 장치도 살아서 작동하는 것을 봤다 — 판정이 옛 코드
(0cef1fe)로 계산됐다는 배너가 지금 코드(59de6c2) 옆에 떴다.

## ⑦ 시험 (실행 출력에서 복사)

`python3 run_tests.py` 종료코드 **0** · **345개 통과, 0개 실패**
(336 → 345)

**일부러 깨뜨려 빨간 불을 본 것**:

| 돌연변이 | 빨간 불 |
|---|---|
| 문단 경계를 무시하게 함 | `test_eps_search_never_crosses_a_line` (99.1 부활) |
| 값 뒤 연간 가드 제거 | `test_annual_value_after_the_number_is_skipped` · `test_headline_for_year_is_skipped` |
| 줄머리 연간 가드 제거 | `test_full_year_bullet_line_is_skipped` |
| 주식 수 가드 제거 | `test_share_count_row_is_not_read_as_eps` |
| 되돌아보기를 넓은 규칙으로 | `test_comparison_to_a_prior_fiscal_year_is_not_annual_context` |
| 줄머리를 좁은 규칙으로 | 〃 |
| 원문 파일 이름을 옛 규칙으로 | `test_raw_file_names_keep_the_whole_date_and_say_why` |

⚠️ 정직하게 적어 둔다: 주식 수 가드는 **처음 쓴 시험이 가짜 초록불**
이었다. 문단 경계만으로도 실물 UCTT 가 막혀서, 가드를 지워도 빨간 불이
안 켜졌다. 표가 **한 줄로 눌린** 경우를 시험에 넣어야 비로소 그 가드를
검사하게 됐다. 그런 실물은 아직 못 봤으므로 이 가드는 **예방**이다.

## ⑧ 다음

- 로봇 재수집 뒤 스냅샷에서 조사 숫자(73차 ④)가 얼마나 줄었는지 재기
- 남은 오염(MXL 인수 발표문 · SCHW · XOM 등)에 대해 새 부탁 목록 만들기

---

# 75차 — 두 번째 자를 붙인다 (야후 분기표, **갈아타지 않고 대조만**) (2026-08-16)

## ① 주인 질문

> "데이터를 SEC 문서가 아니라 야후 같은 곳에서 받아오는 게 정확성이 더
> 높으려나? 실적 같은 걸 읽는 게 아니라 **가져오기만** 하면 되니까."

**반은 맞다.** 74차에 고친 결함 네 개가 전부 '읽기' 결함이었다 — 공시
번호(99.1)를 이익으로, 주식 수(45.3)를 EPS 로, 연간값을 분기 칸에.
받아오기만 하면 이런 사고는 **원천적으로 없다.**

## ② 그런데 정작 1번 잣대는 받아올 수 없다

우리 사다리 첫 칸은 **회사가 스스로 정의한 조정 EPS** 다. 데이터 회사가
주는 "Reported EPS" 는 **월가 기준으로 그 회사가 통일시킨 값**이라
회사 발표와 다를 수 있다. 헌법 1조(창작 금지)가 막는 것이 이것이고,
"한 열 안에 정의가 섞여 델타가 뒤집힌 사고"(주식보상비)를 이미 겪었다.

더 무서운 것은 **조용한 소급 수정**이다. 데이터 회사는 과거 분기를
말없이 고쳐 쓴다. 우리 모델의 핵심은 "**첫** 신기록 돌파"라서 과거
최고점이 나중에 바뀌면 **과거 판정이 통째로 달라진다.** SEC 공시는
한번 나오면 안 바뀐다.

회사 **가이던스**는 데이터 회사에 아예 없다 — 헌법이 컨센서스를 전망
입력으로 금지하므로 그건 계속 보도자료에서 읽는다.

## ③ 그래서 갈아타지 않는다 — 두 번째 자로 붙인다

어느 쪽이 정확한지는 **재면 된다.** 짐작으로 본선을 바꾸지 않는다.

- 로봇이 야후 분기표를 함께 받아 `data/measure/vendor.json` 에 **따로**
  저장한다. **snapshot.json 과 섞지 않는다.** 측정·판정은 그대로다.
- 받는 것: 매출 · 매출원가 · **매출총이익** · 영업이익 · 순이익 ·
  희석 EPS · 희석 주식수, 그리고 발표일 + 월가 기준 EPS.
- 월가 EPS 의 이름은 `street_eps` 로 **일부러 구분**했다. `adj_eps` 로
  두면 다음 세션이 헌법 1조를 어기고 섞어 쓴다 (시험으로 막았다).
- **소급 수정 감시**: 어제 받은 값과 달라진 칸 수를 매번 세어 로봇
  기록에 남긴다. 막지는 않는다 — 세어서 보여 준다(73차 방침 그대로).

## ④ 다음 세션이 잴 것 (아직 재지 않았다)

- 종목·분기별 **불일치율** — 우리 파서 값 vs 야후 값
- 73차 조사가 "튀었다"고 표시한 94칸에서 **야후는 정상값을 주는가**
- 야후가 **소급 수정**을 실제로 얼마나 하는가 (며칠 쌓아 봐야 안다)

이 셋을 보고 나서 본선을 바꿀지 정한다. 특히 **매출총이익률**은 지금
103/160종목뿐인데 야후는 매출·매출원가를 표로 주므로, 이 칸만은 야후가
나을 가능성이 높다(그래도 재고 나서 정한다).

## ⑤ 한계 (감추지 않는다)

- 개발 환경은 야후가 차단돼 **여기서는 무엇이 실제로 오는지 알 수 없다.**
  야후 표에 어떤 줄이 있는지는 **로봇 실행 기록으로만** 확인된다.
  없는 줄은 "없음"으로 남긴다(0으로 채우지 않는다 — 시험으로 막았다).
- 야후 접속은 비공식이라 언제든 끊길 수 있다. 이 축은 **관찰 전용**이라
  실패해도 그날의 데이터 커밋을 막지 않게 했다.

## ⑥ 시험

`python3 run_tests.py` 종료코드 **0** · **355개 통과, 0개 실패**
(345 → 355)

**일부러 깨뜨려 빨간 불을 본 것**:

| 돌연변이 | 빨간 불 |
|---|---|
| 빈 칸(NaN)을 그대로 흘려보냄 | `test_nan_becomes_none` |
| 매출이 없을 때 이익률을 0 으로 채움 | `test_missing_revenue_gives_no_margin` |
| 소급 변경을 세지 않음 | `test_retroactive_changes_are_counted` |
| 실패한 종목의 옛 기록을 버림 | `test_failed_ticker_keeps_its_old_record` |
| 월가 EPS 를 `adj_eps` 라고 부름 | `test_street_eps_is_named_apart_from_company_adjusted_eps` |

## ⑦ 대조 도구도 미리 지었다 (`vendor_compare.py`)

로봇이 야후 값을 받아 오는 즉시 잴 수 있도록 대조 도구를 먼저 지었다.
**아무것도 고치지 않고 재기만** 한다.

**맞대는 칸 셋** (뜻이 같은 것만): GAAP EPS · 매출 · 매출총이익률.
**조정 EPS 는 일부러 뺐다** — 우리 것은 회사 정의, 야후 `street_eps` 는
월가 기준값이라 **뜻이 다르다.** 뜻이 다른 둘을 "불일치"라 세면 숫자가
거짓말을 한다(시험으로 막았다).

**짝짓기**: 우리 분기 종료일과 야후 분기 종료일이 **±7일** 안이면 같은
분기. 벗어나면 억지로 붙이지 않고 "짝 없음"으로 센다. 야후 분기 하나가
우리 두 행에 겹쳐 붙지 않게 한 번 쓰면 뺀다(같은 발표일 두 행 문제, 67차).

**핵심 질문을 따로 센다**: 조사(73차)가 "튀었다"고 표시한 칸에서
**야후는 뭐라 하는가** — `야후가_다름` / `야후도_같음` / `야후에_없음`.
야후가 다른(정상) 값을 준다면 우리 쪽 결함일 가능성이 크고, 야후도 같은
값을 준다면 그 값이 진짜이거나 둘 다 같은 원본을 잘못 읽은 것이다.

반올림 차이(EPS 1센트·비율 2%)는 같은 것으로 본다 — 그걸 불일치라
부풀리면 숫자가 쓸모없어진다.

시험 `python3 run_tests.py` 종료코드 **0** · **367개 통과, 0개 실패**
(355 → 367). 돌연변이 5건 추가 확인:

| 돌연변이 | 빨간 불 |
|---|---|
| 조정 EPS 도 맞대게 함 | `test_adjusted_eps_is_never_compared` |
| 짝 거리 제한을 없앰 | `test_far_apart_quarters_are_not_forced_together` |
| 야후 분기를 여러 번 재사용 | `test_one_vendor_quarter_is_used_only_once` |
| 반올림 차이를 불일치로 셈 | `test_rounding_difference_is_not_a_mismatch` |
| 한쪽만 있는 값을 '다름'으로 셈 | `test_missing_on_one_side_is_counted_apart` |

---

# 77차 — 두 자를 맞대 보니: **우리가 못 보던 오염이 드러났다** (2026-08-17)

로봇이 야후 분기표를 처음 받아 왔다(160종목 성공 · 0종목 실패 · 분기
959개 · 발표기록 639개). 75차에 지어 둔 대조 도구로 바로 쟀다.

## ① 실측 (실행 출력에서 복사)

```
두 자 대조 — 종목 160개 · 짝지은 분기 767개 (짝 못 찾음 2301개)
  GAAP EPS   둘 다 있음 564 · 같음 429(76.1%) · 다름 135 · 우리만 116 · 야후만  76
  매출        둘 다 있음 570 · 같음 451(79.1%) · 다름 119 · 우리만 104 · 야후만  77
  매출총이익률  둘 다 있음 453 · 같음 255(56.3%) · 다름 198 · 우리만  80 · 야후만 189
```

**짝을 2301개나 못 찾은 이유**: 야후 분기표는 종목당 **6분기 정도**만
준다. 우리는 5년치를 갖고 있다. 야후는 **최근만** 보여 주는 자다 —
이것부터가 갈아탈 수 없는 이유다(과거 5년이 없으면 측정 자체가 불가).

## ② 먼저 내 첫인상을 바로잡는다

불일치 목록을 배수 순으로 정렬해 보고 "우리 값이 죄다 야후의 4배"라고
읽었다. **틀렸다.** 정렬이 큰 것부터라 그렇게 보였을 뿐이다. 전체를 세면:

| 우리 ÷ 야후 | 칸 수 |
|---|---|
| 비슷한 크기 (0.67~1.5배) | **299** |
| 우리가 더 작음 | 104 |
| 1.5배 이상 (그 밖) | 30 |
| **3.2~4.8배 (연간÷분기 모양)** | **18** |

연간값이 분기 칸에 들어온 모양은 **18칸뿐**이다. 다만 그 18칸은 확실하다 —
원자료로 확인했다: ADBE 25 Q4 매출 **259억**(연간) vs 야후 **62억**(분기),
FDX GAAP EPS **14.20 · 14.80 · 14.33**(연속 3분기 — 이웃과 다 같아
73차 조사가 못 잡는다) vs 야후 **3.46 · 4.04 · 4.41**.

> **이것이 두 번째 자의 값어치다.** 우리 조사(73차)는 "이웃보다 튄 칸"을
> 찾는다. FDX 처럼 **연속으로 다 오염되면 튀지 않아 안 걸린다.**
> 다른 자를 대야만 보인다.

## ③ 더 큰 문제 — **매출총이익률이 44% 어긋난다**

가장 많이 어긋난 칸이 매출총이익률이다(198/453 = **43.7%**). 실물을 보니
우리 값이 틀렸다:

| 종목 | 우리 | 야후 | 실제 성질 |
|---|---|---|---|
| AMBA | 63.3 · 63.0 · **2.0** · **2.0** · 61.2 · 61.1 · **1.4** · **1.5** | 58~60% | AMBA 매출총이익률은 60% 안팎 |
| WMT | 43 · 21 · 53 · 12 · **4** · **2** · 13 | 24~25% | 월마트가 53%일 수 없다 |
| DELL | 21.2 · **3** · **5** · **1** · **1** · **4** · 18 · **57** | 18~21% | 한 회사 값이 1%~57% 를 오갈 수 없다 |

**파서가 "매출총이익률이 2% 올랐다"의 2% 를 이익률 자체로 읽고 있다.**
한 종목 안에서 값이 60% ↔ 2% 를 오가는데, 그동안 아무도 못 잡았다 —
범위 검사(-100~100%)를 통과하기 때문이다.

> ⚠️ **72차 H26 탐색을 이 값으로 했다.** 묶음별 유형점수(판가형/물량형)는
> `corr(Δ매출, Δ매출총이익률)` 인데, 그 재료가 이렇게 더럽다면 72차의
> 탐색 결과(광통신 +0.41 · 메모리 +0.38 · 기기 OEM +0.00)는 **믿을 수
> 없다.** 채택하지 않았으니 판단에 들어간 것은 없지만, 기록을 정정한다.

## ④ 그 다음 — GAAP EPS 가 체계적으로 **우리 쪽이 크다**

"비슷한 크기" 불일치 중 GAAP EPS 68칸을 보면 방향이 한쪽이다:

| 종목 | 우리 | 야후 | 차 |
|---|---|---|---|
| TER 25 Q3 | 1.12 | 0.75 | +0.37 |
| DELL 26 Q3 | 3.05 | 2.28 | +0.77 |
| TGT 26 Q1 | 2.27 | 1.71 | +0.56 |
| STRL 25 Q4 | 3.64 | 2.81 | +0.83 |

차이 중앙값 0.135. 우리가 늘 크다 — **논갭(조정) 값이 GAAP 칸에 섞여
들어온 모양**이다. 조정 EPS 는 정의상 GAAP 보다 크다.
(아직 원문으로 확증하지 않았다 — 다음 회차의 부탁 목록 대상이다.)

## ⑤ 핵심 질문은 **아직 판정 불가**

73차 조사가 표시한 칸에서 야후가 뭐라 하는지 세었다:

```
야후가 다름 1 · 야후도 같음 5 · 야후에 없음 50
```

**50/56 이 "야후에 없음"** 이다 — 야후 이력이 6분기뿐이라 우리가 표시한
옛 칸을 덮지 못한다. 이 질문은 야후 자료가 몇 달 쌓여야 답할 수 있다.
지금 답을 낸 척하지 않는다.

## ⑥ 그래서 주인 질문("야후가 더 정확한가")에 대한 지금의 답

**칸마다 다르다. 갈아타지 않고, 칸별로 고른다.**

| 칸 | 지금 판단 | 근거 |
|---|---|---|
| 매출총이익률 | **야후가 낫다** | 우리 44% 어긋남 + 실물 확인(AMBA·WMT·DELL) |
| 매출 | 아직 우리 | 79% 일치, 어긋난 것은 연간 혼입(고칠 수 있음) |
| GAAP EPS | 아직 우리 | 76% 일치, 다만 논갭 혼입 의심 — 원문 확인 필요 |
| 조정 EPS | **야후 불가** | 회사 정의 ≠ 월가 기준 (헌법 1조) |
| 5년 이력 | **야후 불가** | 6분기만 준다 |
| 가이던스 | **야후 불가** | 아예 없다 |

**소급 수정은 이번엔 0칸**이다(첫 수집이라 비교 대상이 없었다 —
며칠 쌓여야 진짜 값이 나온다).

## ⑦ 시험

코드는 안 고쳤다(대조·기록만). `python3 run_tests.py` 종료코드 **0** ·
**371개 통과, 0개 실패**

---

# 78차 — 매출총이익률 파서 수리 (77차가 찾은 44% 불일치의 원인) (2026-08-17)

## ① 원인 셋이 겹쳐 있었다 (실물 AMBA 원문으로 확증)

**㉠ 어순** — 회사는 이렇게 적는다:
> "Gross margin **on a non-GAAP basis** for the fourth quarter of fiscal
> 2026 ⏎ was **59.8%**, compared with 62.0% …"

우리 이름들은 전부 `non-GAAP` 이 **앞에** 오는 형태만 잡아서 이 문장을
통째로 놓쳤다.

**㉡ 그래서 훨씬 뒤쪽 각주를 물었다**:
> "The **difference between** GAAP and non-GAAP gross margin was **1.4%**
> and 2.0%, or $1.4 million and $1.7 million"

두 이익률의 **차이**다. 차이는 1~3% 라 범위 검사(-100~100%)를 그대로
통과해 아무도 못 잡았다. AMBA 가 63.3 · 63.0 · **2.0** · **2.0** · 61.2 ·
61.1 · **1.4** 로 오간 것이 이 때문이다.

**㉢ 어순을 고치자 이번엔 전망값을 물었다**:
> "Gross margin on a non-GAAP basis **is expected to be** between
> **59.0%** and 60.5%"

전망값 59.0 이 실제값 59.8 보다 라벨에 가까이 붙어 있다.
⚠️ 기존 전망 규칙은 `expects/expecting` 만 잡고 **`expected`** 를 못 잡는다.
그 규칙은 EPS 경로도 함께 쓰므로 건드리지 않고 **이익률 전용 규칙**을
따로 뒀다 (고치는 범위를 좁게).

## ② 실물 확인 (AMBA 원문 6건)

| 공시 | 전 | 후 | 원문 |
|---|---|---|---|
| 2026-02-26 | 1.4 | **59.8** | "was 59.8%" |
| 2026-05-28 | 1.5 | **59.9** | |
| 2024-08-27 | 63.3 | 63.3 (그대로) | "was 63.3%" |
| 2023-10-18 | 62.0 | **없음** | 경영진 교체 발표문의 **가이던스 재확인** 값이었다 |

## ③ 내가 한 번 거꾸로 갔다 — 기록해 둔다

남은 종목(WMT·DELL 등)의 원문을 받으려고 "종목 중앙값에서 멀면 이상치"로
재려 했다. **거꾸로 나왔다** — AMBA 는 틀린 값(2% 대)이 더 많아 중앙값이
**3.9%** 가 되고, 그러면 **진짜 값(63%)이 이상치로 찍힌다.**

> 오염이 다수면 중앙값도 오염된다. **어느 쪽이 옳은지 우리는 모른다.**

그래서 어느 쪽이 틀렸는지 정하지 않고, "이 종목은 폭이 너무 넓다"는
사실만 말하고 **양 끝 원문을 둘 다** 부탁하기로 했다.

실측: 폭 20%p 이상인 종목 **28개**. 가장 넓은 것들 —
PEP 88.0%p(11.0 ↔ 99.0) · MU 77.0%p(6.0 ↔ 83.0) · SNDK 74.9%p ·
LLY 73.0%p · AMBA 63.0%p.

## ④ 부탁 목록을 두 이유가 나눠 쓴다

목록 60건을 **튐 30 · 매출총이익률 폭 30** 으로 나눠 담는다. 한쪽이 다
차지하면 다른 쪽 원인을 영영 못 본다.

## ⑤ 시험

`python3 run_tests.py` 종료코드 **0** · **378개 통과, 0개 실패**
(371 → 378)

**일부러 깨뜨려 빨간 불을 본 것**:

| 돌연변이 | 빨간 불 |
|---|---|
| 차이 가드 제거 | `test_difference_between_margins_is_not_a_margin` |
| 전망 가드 제거 | `test_forecast_margin_is_not_an_actual` |
| 뒤집힌 어순 이름 제거 | `test_reversed_word_order_margin_is_read` |
| 전망 규칙에서 `expected` 빼기 | `test_forecast_margin_is_not_an_actual` |
| 양 끝 대신 중앙값 이상치로 되돌림 | `test_wide_gross_margin_swing_is_flagged_from_both_ends` |
| 매출총이익률 몫을 목록에서 뺌 | `test_wanted_list_mixes_both_reasons` |
| 폭 검사 없앰 | `test_steady_gross_margin_is_not_flagged` |

## ⑥ 남은 것

- WMT·DELL 등 27종목은 **원문이 없어 원인을 확인 못 했다.** 이번 부탁
  목록에 담았으니 다음 로봇 실행 뒤에 본다.
- 76차 파서 수리(구역 제목·배당금)의 **전수 비교를 아직 못 돌렸다** —
  개발 환경이 두 번 초기화되면서 중간에 끊겼다. 다음 세션에서 돌린다.

---

# 79·80차 — 매출총이익률 수리를 이어가다 **내 수리가 낸 사고**를 잡았다 (2026-08-17)

## ① 79차 — "from A% to B%" 는 A 가 지난 기간이다

78차 전수 비교가 BMY 74.0 → 77.3 을 보여 줬는데, **새 값도 틀렸다.**
원문은 "gross margin decreased **from 77.3% to 76.1%**" 이고 이번 분기는
**76.1** 이다. (옛 74.0 은 "expected to be approximately 74%" — 전망값.)

이름과 숫자 사이 20자 안에 `from` 이 있으면 건너뛴다. 원문 652건에
이 형태가 **12건**.

> ⚠️ 처음엔 "숫자 뒤에 `to 숫자` 가 오는가"까지 확인했는데, 원문 652건에
> **그 확인이 필요한 사례가 0건**이었다. 근거 없는 검사는 빼기로 했다.

## ② 80차 — PG 가 100% 로 나왔다 (내가 78차에 낸 사고)

| | 전 | 78차 후 |
|---|---|---|
| PG 2026-01-22 | 51.2 | **30.0** |
| PG 2026-04-24 | 49.5 | **100.0** |

이익률이 100% 일 수 없다. 원문을 보니 PG 보도자료는 **슬라이드가 눌린
것**이었다:

> `<img …/>` • Core gross margin • Core operating margin • Total
> productivity savings **5% 2% 1% 6% 3% 0% 3%**

막대그래프 숫자 나열이다. 옛 51.2·49.5 도 같은 나열에서 우연히 그럴듯하게
나온 값이었다. PG 실제는 50% 안팎 — **없음이 정답**이다.

**고친 것 둘**: ㉠ 슬라이드 규칙(13차 EPS 규칙을 이익률에도) ㉡ 퍼센트
나열 규칙(사이에 낱말 없이 이어지는 퍼센트 3개 이상 = 그래프).

> ⚠️ ㉠에서 **또 넘어졌다.** "줄 안에 `<img` 가 있으면"으로 했더니 눌린
> 문서는 한 줄이 수천 자라 **멀쩡한 문장까지 잃었다**(MDB "representing
> a **74%** non-GAAP gross margin"). 실측 거리(PG 슬라이드 84~93자 ·
> MDB 정상 문장 447자)로 라벨 앞 **150자**로 좁혔다.

## ③ `<img` 규칙이 정말 필요한가 — 실물 652건으로 쟀다

79차에 "to 숫자" 검사를 0건이라 뺐으므로, 같은 기준을 이 규칙에도 댔다.

```
<img 가드가 실제로 바꾸는 파일: 2건
   KLAC_2022-06-16.txt   가드있음 35.0 ↔ 가드없음 61.0
   QRVO_2024-06-11.txt   가드있음 None ↔ 가드없음 50.0
```

**둘 다 투자자 설명회 슬라이드다** (실적 발표가 아니다):
- QRVO: "`<img …>` **50%+ Gross Margin** Optimization Path back to 50%"
  → 장기 **목표**. 가드가 없애는 것이 맞다. **가드를 지킨다.**
- KLAC: 가드 있으면 35.0, 없으면 61.0 — **둘 다 분기 실적이 아니다**
  (61.5~63.5% 는 모델 가정, 35 는 KPI 표의 숫자). 가드가 이 파일을
  고치지는 못한다.

## ④ 전수 비교 (원문 652건, 78·79·80차 합쳐)

| 칸 | 읽은 건수 | 값 바뀜 | 없어짐 |
|---|---|---|---|
| 조정 EPS | 100 → 100 | 0 | 0 |
| GAAP EPS | 250 → 250 | 0 | 0 |
| 매출 | 438 → 438 | 0 | 0 |
| 조정 EBITDA | 42 → 42 | 0 | 0 |
| **매출총이익률** | 168 → 157 | **13** | **11** |

없어진 것 중 확인한 것은 전부 지워야 할 값이었다 — ADI 73.0(투자자
설명회 **장기 목표**) · CIEN 44.0("is expected to be" 전망) ·
AMBA 62.0(경영진 교체 발표문의 **가이던스 재확인**).

⚠️ **한계**: 이 비교의 기준판(dc9147c)에는 76차 수리가 이미 들어 있다.
따라서 위 "EPS 변화 0건"은 **78·79·80차의 무해함**을 말할 뿐,
**76차(구역 제목·배당금)의 부작용은 아직 확인하지 못했다.**

## ⑤ 남은 결함 (감추지 않는다)

- **KLAC 2022-06-16 은 투자자 설명회 자료인데 실적 발표로 통과한다**
  (`_looks_like_earnings` 가 True). 근본 수리는 그 문지기 쪽이고,
  근거를 더 모아 따로 해야 한다. 규칙을 더 얹지 않는다 —
  PG 사고가 보여 준 대로, 급히 얹은 규칙이 멀쩡한 값을 지운다.
- WMT·DELL 등 27종목은 여전히 원문이 없다(부탁 목록에 담겨 있다).

## ⑥ 시험

`python3 run_tests.py` 종료코드 **0** · **383개 통과, 0개 실패**
(378 → 383). 돌연변이로 빨간 불을 본 것: from 검사 제거 · from 창을
60자로 넓힘 · 퍼센트 나열 가드 제거 · `<img` 창을 줄 전체로 넓힘.

---

# 81차 — **76차 검증을 마쳤다**: 배당금·비율 계열이 통째로 걷혔다 (2026-08-17)

80차 ④에 "76차의 부작용은 아직 확인 못 했다"고 적어 뒀다. 기준판을
75차 파서(`da610ca`)로 되돌려 76차 파서(`93da20d`)와만 맞대어, **76차의
효과만 따로** 쟀다. 원문 694건 전수.

## ① 결과 — 다른 칸은 손도 안 댔고, GAAP EPS 만 정리됐다

| 칸 | 읽은 건수 | 없어짐 | 바뀜 | 새로 |
|---|---|---|---|---|
| 조정 EPS | 115 → 115 | 0 | 0 | 0 |
| **GAAP EPS** | **301 → 275** | **27** | **2** | **1** |
| 매출 | 468 → 468 | 0 | 0 | 0 |
| 조정 EBITDA | 44 → 44 | 0 | 0 | 0 |
| 매출총이익률 | 191 → 191 | 0 | 0 | 0 |

## ② 없어진 27건 — 원문으로 하나하나 확인했다. 전부 EPS 가 아니었다

**배당금 (19건)** — 67차에 JPM 에서 찾아낸 그 사고가 실은 훨씬 넓었다:

| 종목 | 값 | 원문 |
|---|---|---|
| JPM ×4 | 1.25 · 1.40 · 1.50 · 1.50 | 분기 배당금 인상 일정 그대로 |
| **MPWR ×11** | 0.75 · 1.00 · 1.25 · 1.56 · 2.00 | 배당금 인상 일정 (공시 11건) |
| JNJ ×2 | 1.19 | "quarterly **dividend** of $1.19 per share" |
| NRG | 1.63 | "quarterly dividend … $1.63 per share **on an annualized basis**" |
| HPQ | 1.00 | "Raises Annual **Dividend** … to $1 per Share" |
| OXY | 0.22 | "quarterly **dividend** increase of 22% to $0.22 per share" |

**비율 계열 (6건)** — BAC 12.76 · 13.02 · 13.97 · 13.61:
> "Return on average tangible common shareholders' equity (1)
>  14.27 13.02 15.43 13.40 **12.76**"

**자기자본이익률 표**를 EPS 로 읽고 있었다. BAC 진짜 분기 EPS 는 **$1.06**
("Diluted earnings per share of $1.06 compared to $0.81").

**투자자 설명회 (1건)** — KLAC 16.5 (연간값).

> 이로써 67차·68차부터 "표시부터, 지우지 말고"로 남겨 두었던 **금융권
> 오염이 뿌리에서 해결**됐다. 73차 조사가 "같은 값이 되풀이된다"고 센
> 칸들의 정체가 배당금 인상 일정이었다.

## ③ 바뀐 2건 · 새로 생긴 1건

- HPE 1.54 → **0.49** (연간 → 진짜 4분기) — 76차가 노린 그 수리
- **PG 1.83 새로 생김** — "Diluted Net EPS **$1.83**". 76차에 넣은
  `net EPS` 이름이 **그동안 못 읽던 값을 찾아냈다.** 없어지기만 한 것이
  아니다.

## ④ bps 규칙(81차)의 실물 영향 — 7건, EL 은 확실히 고쳐졌다

| 종목 | 규칙 없을 때 | 규칙 있을 때 | 판단 |
|---|---|---|---|
| **EL ×3** | 40.0 · 60.0 | **76.5 · 73.3** | ✅ 원문 "expanded 40 basis points, **to 76.5%** from 76.1%" |
| WMT ×4 | 50.0 · 53.0 · 2.0 | 6.7 · 8.3 · 0.2 | ⚠️ 둘 다 틀림 |

옛 40.0 은 "expanded **40** basis points" 의 40 이었다. 규칙이 그걸
걸러내니 진짜 값 76.5 가 잡혔다.

**WMT 는 여전히 틀린다** — 그 보도자료에는 이익률 수준이 아예 없어
정답이 "없음"인데 파서가 계속 다른 숫자를 찾는다. 여기서 규칙을 더
얹지 않는다 (80차 PG 사고).

## ⑤ 그래서 매출총이익률은 **야후로 간다** (77차 결론이 계속 확인됐다)

- WMT 처럼 **회사가 수준을 아예 안 적는** 경우는 파서로 풀 수 없다.
- 야후는 매출·매출원가를 표로 주므로 나눗셈이면 된다.
- 77차 실측: 우리 값이 야후와 **44% 어긋났다**.

다만 **아직 바꾸지 않았다.** 야후 소급 수정 감시가 며칠 쌓여야 하고
(지금까지 0칸 — 첫 수집이라 비교 대상이 없었다), 본선 교체는 숫자를
더 본 뒤에 한다.

## ⑥ 남은 결함

- **WMT·DELL 매출총이익률** — DELL 은 표에 "Gross margin" 행이 둘이고
  하나는 **금액**(4,937), 하나는 **비율**(21.1%) 인데 금액 행을 먼저 문다.
- **BAC 는 이제 "없음"** 이다. 진짜 값 1.06 이 원문에 있는데 못 읽는다 —
  틀림보다는 낫지만 아직 못 읽는 것은 사실이다.
- **KLAC 2022-06-16 이 실적 발표로 통과한다** (`_looks_like_earnings`).

## ⑦ 시험

`python3 run_tests.py` 종료코드 **0** · **384개 통과, 0개 실패**

---

# 82차 — 금액 행의 끝 퍼센트는 **증감률 열**이다 (실물 DELL) (2026-08-17)

81차 ⑥에 남겨 둔 결함을 하나 정리했다.

## ① 원인

DELL 보도자료의 표는 이렇게 생겼다:

```
Non-GAAP gross margin      $5,057        $4,992        1%     ← 증감률 열
Non-GAAP gross margin        20.7 %        21.6 %             ← 진짜 비율
```

파서가 **금액 행 끝의 증감률 1%** 를 매출총이익률로 저장하고 있었다.
DELL 실제 매출총이익률은 18~21% 다.

**고친 것**: 이름과 숫자 사이에 **금액 표기($5,057)** 가 끼어 있으면 그
줄은 금액 행이므로 건너뛴다. 비율만 적힌 줄에는 금액이 없어 안 걸린다.

**실물 확인**: DELL 2025-05-29 **1.0 → 21.1**, 2026-05-28 **57.0 → 17.8**.
다른 종목은 그대로 (MDB 74.0 · AMBA 59.8 · EL 76.5 · CGNX 71.0 ·
BMY 76.1 · WDC 54.4 · STX 52.7 · MCHP 42.0).

## ② 정직하게 적어 두는 한계

이 규칙이 **배선까지 제대로 걸렸는지를 가짜 예제로 재현하지 못했다.**
두 줄을 자간까지 그대로 옮겨도, 규칙을 빼면 가짜 예제는 그냥 통과한다 —
실물의 다른 줄들이 만드는 "이름↔숫자 거리" 관계를 흉내 내지 못하기
때문이다.

> 그래서 시험은 **규칙 자체**(금액 행을 알아보는가)만 검사하고,
> 배선의 근거는 **실물 DELL 파일**로 남긴다. 이 회차의 시험은 다른
> 회차보다 약하다 — 감추지 않는다.

## ③ 시험

`python3 run_tests.py` 종료코드 **0** · **386개 통과, 0개 실패**
(384 → 386)

## ④ 남은 결함 (81차 ⑥에서 이월)

- **WMT 매출총이익률** — 보도자료에 수준이 아예 없다(변화만 적는다).
  파서로 풀 수 없다. → 야후로 가는 근거.
- **BAC 는 "없음"** — 진짜 값 1.06 이 원문에 있는데 못 읽는다.
- **KLAC 2022-06-16 이 실적 발표로 통과한다** (`_looks_like_earnings`).

---

# 83·84차 — BAC 를 되살리고, 그 과정에서 **내가 낸 회귀를 잡았다** (2026-08-17)

## ① 83차 — 슬라이드 규칙이 멀쩡한 문장까지 버리고 있었다

81차 ⑥에 "BAC 는 이제 없음, 진짜 값 1.06 이 원문에 있는데 못 읽는다"고
적어 둔 것을 파고들었다. 원인은 내 수리가 아니라 **13차 슬라이드 규칙**
이었다 — "줄 안에 `<img` 가 **있기만 하면** 건너뛰기".

눌린 문서는 한 줄이 수천 자라, 이미지 뒤에 이어지는 멀쩡한 문장까지
통째로 버렸다:
> "Diluted earnings per share of **$1.06** compared to $0.81, up 31%"

실측 거리: BAC 진짜 문장 **1,094~4,751자** · PG 슬라이드 잡음 **104자**.
80차에 매출총이익률 쪽에서 이미 내린 판단(150자)을 EPS 쪽에도 댔다.

덤: **"Core EPS" 를 논갭으로** 인식하게 했다. P&G 는 자기네 논갭 지표를
"Core" 라고 부른다("Diluted EPS $1.63 … **Core EPS $1.59**").

## ② 전수 비교 (694건) — **내 주장 하나를 정정한다**

커밋 메시지에 "이 변경은 **구조상 덧셈만** 한다"고 적었다. **틀렸다.**
건너뛰기를 좁히면 전에는 무시되던 라벨이 새로 후보가 되므로, **기존 값이
바뀔 수도** 있다. 실제로 4건이 바뀌었다.

| 칸 | 읽은 건수 | 없어짐 | 바뀜 | 새로 |
|---|---|---|---|---|
| 조정 EPS | 115 → 136 | 0 | 0 | **21** |
| GAAP EPS | 275 → 311 | 0 | **4** | **36** |
| 매출·EBITDA·매출총이익률 | 그대로 | 0 | 0 | 0 |

새로 생긴 값 중 확인한 것: BAC ×5(1.06·0.98·1.11·1.21) · JPM ×4
(4.37·5.07·5.07·5.94) · PYPL ×8(0.44~1.20) · ABNB ×3 — 전부 그 회사의
실제 분기 EPS 크기와 맞는다.

바뀐 4건 중 **둘은 좋아졌고 하나는 나빠졌다**:

| 종목 | 전 | 후 | 판단 |
|---|---|---|---|
| MDB | 0.02 | **−0.02** | ✅ "Net loss … or $0.02 per share" — 부호가 맞아졌다 |
| STRL | 13.02 | **2.33** | ✅ 표의 진짜 분기값 |
| STRL | 6.78 | 5.60 | 크기는 맞는 쪽 |
| **UNH** | **6.04** | **17.10** | ❌ **내가 낸 회귀** |

## ③ 84차 — UNH 회귀를 고쳤다

UNH 문서에는 실적과 **전망표**가 함께 있다:

```
UnitedHealth Group Reports Second Quarter 2026 Results
• Earnings of $6.04 Per Share …                    ← 진짜 실적
Diluted Net Earnings per Share …   > $17.10        ← 전망(이상)
Earnings per Share            $18.45 - $18.95      ← 전망(범위)
```

슬라이드 규칙을 좁히자 이 전망표가 새로 읽히면서 6.04 → 17.10 이 됐다.

**고친 것 둘** (실적에는 붙지 않는 표기다):
- 숫자 앞의 `>` · `≥` · `~` → 목표·전망
- 범위 표기 `$18.45 - $18.95` 의 **양끝**
  (적자 표기 `-$0.40` 은 대시 앞에 숫자가 없어 안 걸린다)

**결과**: UNH 2026-07-16 GAAP **6.04** 회복(조정 6.38), 2026-04-21 6.90.
BAC·JPM·GS·HPE·UCTT·VZ 는 그대로.

## ④ 아직 확인 못 한 값

**MKSI 5.59** — "Non-GAAP net earnings per diluted share $5.59" 인데 같은
표의 "Non-GAAP net earnings **$377**" 이 MKSI 분기 규모가 아니라 연간
규모로 보인다. 문서에 기간 표기가 없어 **분기인지 연간인지 확정 못 했다.**
다음 회차의 부탁 목록 대상이다.

## ⑤ 시험

`python3 run_tests.py` 종료코드 **0** · **390개 통과, 0개 실패**
(388 → 390). 돌연변이 3건 빨간 불 확인:
이상 표기 가드 제거 · 범위 가드 제거 · 범위 뒤끝의 "앞에 숫자" 조건 제거
(적자 표기까지 걸림).

---

# 85차 — **투자자 설명회 자료**를 실적발표에서 걷어낸다 (2026-08-17)

## ① 세 번째 사례가 나와 근거가 찼다

80차 ⑤와 84차 ④에 "설명회 자료가 실적발표로 통과한다"를 남겨 두었다.
그때는 KLAC 한 건이라 미뤘는데, QRVO·MKSI 가 더 나왔다:

| 종목 | 잘못 읽은 값 | 정체 |
|---|---|---|
| KLAC | 조정 EPS 4.93 · 이익률 35% | 모델 가정 · KPI 표 숫자 |
| QRVO | 이익률 50% | "50%+ Gross Margin Optimization Path" 장기 목표 |
| MKSI | 조정 EPS 5.59 | 분기인지 연간인지도 **확정 못 함** |
| PG | GAAP EPS 6.51 (두 분기에 같은 값) · 이익률 49~51% | 막대그래프 숫자 |
| DELL | 조정 EPS 9.55 | 분기(2~3) 아님 |

## ② 갈라주는 표시 — **이미지 파일 이름**

설명회 자료는 장마다 그림이라 `src="…slide7.jpg"` · `…p14g1.jpg` ·
`…ex99_1p81g1.jpg` 처럼 **쪽 번호가 박힌 이름**을 쓴다. 실적발표문에도
그림(로고·차트)은 있지만 그런 이름은 아니다.

**저장소 원문 694건 실측 — 경계가 아주 깨끗하다:**

| 쪽 그림 수 | 파일 |
|---|---|
| 0개 | **672건** (BAC 19장·JPM 14장·UNH 17장인데 전부 0) |
| 1~9개 | 2건 (COF) |
| **10개 이상** | **20건** (KLAC 142 · GS 118 · QRVO 95 · MKSI 51 · PG 35) |

2와 10 사이가 비어 있어 문턱을 **10**으로 둔다.

## ③ 어디에 두었나 — 첨부(EX-99) 길을 막아야 한다

문지기(`_looks_like_earnings`)는 **첨부를 확보하면 건너뛰도록** 돼 있다
(표지에 숫자가 없어 진짜 실적발표가 탈락하던 문제 때문). 설명회 자료는
대개 EX-99 첨부라 **그 길로 그대로 들어왔다.** 그래서 설명회 판별을
표지 판별보다 **먼저** 두었다.

## ④ 잃는 것도 적어 둔다

이 20건이 지금 내놓는 값 11개 중 **8개가 틀린 값**이고, **3개(ABNB
0.87·2.17·1.38)는 맞아 보인다.** ABNB 는 주주서한 형식이라 설명회
자료 안에 진짜 분기 실적이 들어 있다.

다만 그 3개는 **83차 전에는 어차피 안 읽히던 값**이다(83차 전수 비교에서
"새로" 생긴 것들). 즉 이 규칙은 83차가 열어 준 설명회 문을 닫는 것이고,
83차의 진짜 성과(BAC·JPM·PYPL·UNH — 전부 설명회가 아님)는 그대로 둔다.

## ⑤ 로봇 실행 실패 — 내 탓이다

수리를 반영하려고 로봇을 돌렸는데 **SEC 호출 한도(HTTP 429)** 로 실패했다.
로그: "SEC Rate Limit Exceeded … Estimated Wait: ~10 minutes",
160종목 중 대부분 실패 → **로봇이 스스로 멈췄다**:
> ⛔ 절반 이상 실패 — 오늘 데이터로 덮어쓰지 않습니다 (종료코드 1)

**안전장치가 제대로 작동했다** — 반쯤 깨진 수집으로 좋은 데이터를 덮지
않았다. 원인은 오늘 하루에 로봇을 **여러 번 몰아서 돌린 것**이다.
교훈: 수리를 모아서 한 번에 돌린다.

## ⑥ 시험

`python3 run_tests.py` 종료코드 **0** · **391개 통과, 0개 실패**
(390 → 391). 돌연변이 2건 빨간 불 확인 — 설명회 판별 제거 ·
문턱을 1로 낮추고 그림 이름 규칙을 넓힘(정상 보도자료까지 걸림).

---

# 86차 — 수리 묶음이 **정말 나아졌는지** 바깥 자로 재고, 무너진 칸을 찾는다 (2026-08-17)

## ① 무엇을 쟀나 — 이건 85차 하나가 아니라 **여섯 수리의 합계**다

로봇 실행 `32024489102` 성공(14:06:57Z, head_sha `f8dcf69` = 85차).
반영 전후 스냅샷을 전수 대조했다.

- **앞** `1bb1322` (8/17 05:38) — **80차 전** 코드가 만든 것
- **뒤** `5432313` (8/17 14:06) — 85차까지 반영

두 스냅샷 사이에 코드 커밋이 여섯 개(80·81·82·83·84·85차) 있다.
**그러니 아래 숫자는 85차의 성적이 아니라 여섯 수리의 합계다.**
처음에 8/16 스냅샷을 앞으로 잡았다가 조상 관계를 확인하고 고쳤다.

바뀐 칸 **624개** (생김 162 · 사라짐 138 · 바뀜 324).

## ② 바깥 자(야후)로 잰 결과 — 세 칸 모두 나아졌다

같은 `vendor.json` 하나를 자로 삼아 앞뒤를 각각 쟀다.

| 칸 | 앞 | 뒤 | 변화 |
|---|---|---|---|
| GAAP EPS | 76.4% (430/563) | **76.9%** (440/572) | +0.5p |
| 매출 | 79.1% (451/570) | **79.8%** (455/570) | +0.7p |
| 매출총이익률 | 56.5% (256/453) | **59.1%** (266/450) | +2.6p |

바뀐 칸을 하나씩 야후에게 물었다 (뜻이 같은 칸만):

> **새 값이 맞음 22 · 옛 값이 맞았음 1 · 둘 다 틀림 17 · 야후에 없음 97**

되살아난 것: DELL 이익률 1.0→21.1 · PG 이익률 100→49.5 · EL 40→76.5 ·
GNRC 2.0→38.7 · ENTG GAAP EPS 4분기 · PG GAAP EPS 3분기 · MKSI 매출
464M→1,033M — **전부 야후 값과 소수점까지 일치**.

잃은 것은 하나뿐이다: **TGT 이익률 29.0 → 없음** (야후 29.01).
값이 틀린 게 아니라 **없어졌다** — 헌법 1조에서 "없음은 안전, 틀림은 위험"
이라 했으니 감수할 수 있는 쪽이다. 그래도 잃은 것으로 적어 둔다.

## ③ 조정 EPS 는 야후 자로 못 잰다 — 참고 측정에서 **회귀 9건**

조정 EPS 는 회사가 정의한 값이고 야후 `street_eps` 는 월가 기준값이라
맞다/틀리다를 물을 수 없다. 그래서 **어느 쪽이 더 가까운가**만 봤다
(참고 측정이지 판정이 아니다).

> 새 값이 가까움 11 · **옛 값이 가까움 9** · 기준 없음 80 · 한쪽 없음 7

**새 값이 맞은 쪽** — ENTG 4분기(0.72·0.70·0.86·0.93) · HUBS 3분기
(2.66·2.72·3.26) · MKSI 2.47 · UNH 7.23·6.38 — 월가 값과 **정확히 일치**.

**옛 값이 맞았던 쪽 (회귀)**:

| 종목 | 옛 → 새 | 월가 |
|---|---|---|
| **CRM** | 2.84 → **0.00** | 2.91 |
| **CRM** | 3.02 → **0.22** | 3.25 |
| **CRM** | 3.11 → **0.67** | 3.81 |
| CSCO | 1.01 → 0.69 | 1.00 |
| BILL | 0.49 → 0.60 | 0.53 |
| BILL | 0.54 → 0.69 | 0.61 |
| BILL | 0.69 → 0.78 | 0.68 |
| HUBS | 2.46 → 1.95 | 3.09 |
| NRG | 7.9 → 8.9 | 1.04 (둘 다 이상) |

## ④ 가장 심한 것은 CRM — 조정·GAAP 이 **같은 값으로 나란히** 무너졌다

| 분기끝 | 우리 조정 | 우리 GAAP | 야후 GAAP |
|---|---|---|---|
| 2025-04-30 | -0.05 | -0.05 | 1.59 |
| 2025-07-31 | 0.00 | 0.00 | 1.96 |
| 2025-10-31 | 0.22 | 0.21 | 2.19 |
| 2026-01-31 | 0.67 | 0.66 | 2.07 |

두 칸이 **똑같은 값**이 된 것은 파서가 숫자 하나를 집어 양쪽에 넣었다는
뜻이다. 어느 규칙이 그랬는지는 **보도자료 원문을 봐야 안다.**

**추측으로 고치지 않는다.** 78차와 83차에 바로 그렇게 해서 회귀를 냈다
(PG 이익률 100% · UNH 6.04→17.10). 개발 환경은 SEC 가 막혀 있으니
원문을 부탁해 다음 실행에서 받는다 (아래 ⑤).

## ⑤ 조사(audit_data)는 이 결함을 **구조적으로 못 본다**

73차 조사는 **이웃 분기와 비교**해 튄 칸을 찾는다. CRM 은 네 분기가
**나란히** 무너져 이웃과 견줘도 튀지 않는다 — 그래서 안 걸린다.
77차 FDX(14.20·14.80·14.33 연속)와 같은 종류의 사각지대다.

바깥 자는 이걸 본다. 그래서 **야후와 어긋난 칸을 원문 부탁 목록에 연결**
했다 (`vendor_compare.wanted_from_mismatch` → `audit_data.merge_wanted`).
바깥 증거를 앞자리에 둔다 — 우리 착각이 섞일 여지가 적다.

**맞대는 칸은 GAAP EPS·매출뿐이다.** 매출총이익률은 뺐다: 거기서
갈리는 것은 결함이 아니라 **정의 차이**다 (CSCO 우리 67.5=회사 논갭 ↔
야후 63.2=갭 계산 · BILL 84.9 ↔ 81.2). 이걸 "다름"이라 세고 부탁하면
멀쩡한 공시가 목록을 다 먹는다.

## ⑥ 목록을 짜면서 **두 번 틀렸다** — 둘 다 실물로 드러났다

1. **줄 세우는 법이 틀렸다.** `compare()` 의 불일치는 배수(우리÷야후)
   큰 순인데, 그대로 쓰니 우리가 **작게** 읽은 칸이 맨 뒤로 밀렸다.
   CRM 은 배수가 0 이라 30건 밖이었다 — **정작 가장 급한 칸**이.
   → 어느 쪽으로 벌어졌든 **벌어진 폭**으로 다시 세운다.
2. **한 종류가 자리를 다 먹었다.** 고치고 보니 30칸 중 24칸이 단위 차이
   한 종류(AMD·HD·TGT·NFLX·TER)였다. 같은 회사는 형식이 같아 다섯 건을
   봐도 배우는 게 없다. → **종목당 2건**으로 막았다 (4분기 발표문은
   연간 표가 붙어 형식이 달라 1건으로는 모자람).

결과: 부탁 목록 60건이 **25종목에 고루** 퍼졌고 CRM 두 건이 앞자리에 왔다.

## ⑦ 새로 드러난 결함 무리 — **단위가 섞여 있다**

| 종목 | 우리 | 야후 | 우리 단위 |
|---|---|---|---|
| AMD·HD·TGT·HPE | 10,270 | 10,270,000,000 | 백만 |
| NFLX·TER | 11,079,166 | 11,079,166,000 | 천 |
| SMCI·SO·EL | 774,846,000 | 12,682,491,000 | 뒤섞임 |

값 자체는 맞는데 **단위만 다르다.** 그런데 이건 우리 쪽 결함이다 —
같은 칸에 단위가 섞이면 TTM 합이 무너진다. 매출 불일치 20% 의 상당수가
이것이다. **다음 차수 과제로 적어 둔다** (원문 부탁 목록에 들어 있다).

## ⑧ 연간값이 분기 칸에 — 여전히 남아 있다

UNH 14.65↔3.74 · FDX 14.20↔3.46 · CAT 18.81↔5.12 · PG 6.51↔1.48 ·
STRL 7.87↔2.31 · BA 매출 89.5B↔23.9B · ADBE 25.9B↔6.19B ·
TMUS 77B↔24.3B — 대체로 **4배 안팎**, 연간값의 특징이다.

## ⑨ 조사 수치는 오히려 늘었다 — 정직하게 적는다

| | 앞 | 뒤 |
|---|---|---|
| 튄 칸 | 73 (1.8%) | 77 (1.9%) |
| 되풀이 칸 | 102 | 128 |
| 라벨 겹침 | 159행(38종목) | 164행(40종목) |

**늘었다.** 다만 값이 채워진 칸도 함께 늘어서(빈칸 → 값) 셈의 분모가
달라졌다. **어느 쪽 때문인지는 아직 못 갈랐다** — 갈랐다고 말하지 않는다.

## ⑩ 야후 소급 변경 23칸 — 75차 관찰 항목의 **첫 증거**

로봇이 처음으로 0 이 아닌 값을 보고했다(23칸). 무엇이 바뀌었는지 두
보관본을 직접 비교했다 (`672b36d` 02:27 ↔ `5432313` 14:06, **같은 날**).

> **23칸 중 22칸이 "값이 있다가 없어짐"이고, 전부 최신 분기(2026-06-30)다.**
> (CAT·ENTG·ETN·FORM·GS — 매출·매출총이익·순이익이 통째로 사라짐)
> **진짜 소급 수정은 1건뿐이다: BAC 2025-06-30 GAAP EPS 0.90 → 0.89.**

즉 야후가 과거를 말없이 고치는 것보다, **최신 분기가 하루 안에도
들락날락하는 쪽**이 문제다. 75차에 등록한 질문 — "매출총이익률 칸을
야후로 갈아탈까" — 에 대한 첫 실측 답:
**갈아타더라도 최신 분기는 믿을 수 없다.** 결정은 계속 미룬다.

## ⑪ 시험

`python3 run_tests.py` 종료코드 **0** · **401개 통과, 0개 실패**
(391 → 401).

돌연변이 **8건** 빨간 불 확인:
매출총이익률까지 부탁 · 발표일 대신 분기끝을 적음 · 발표일 없는 행을
안 거름 · 겹치는 공시를 안 거름 · limit 무시 · 배수 순으로 되돌림 ·
종목당 상한 제거 · **부르는 쪽에서 인자 순서 뒤집기**.

마지막 것이 중요하다. 처음엔 `merge_wanted` 의 **규칙만** 시험해서,
`write_wanted` 가 인자를 거꾸로 넘겨도 초록이었다 — 82차에 겪은
"규칙은 맞는데 배선이 틀린" 가짜 초록불이다. 그래서 규칙이 아니라
**적어 낸 파일 내용**을 보는 시험을 따로 넣었다.

## ⑫ 덧붙임 — ⑦의 단위 결함을 다시 재니 **내가 가볍게 적었다**

⑦에 "종목마다 단위가 다르다"고 적었는데, 그건 절반만 맞다. 다시 재 보니
**한 종목 안에서도 분기마다 단위가 뒤집힌다.**

이웃 분기 사이에 100배 이상 튀는 곳 — **44곳 / 20종목**:

| 종목 | 이어지는 분기의 매출 |
|---|---|
| AMZN | 110.8B → 147.8M → 15,227 → 89B → 20,576 |
| TGT | 25,270 → 30.45B → 25,443 |
| NFLX | 8.54B → 8,832,825 → 9.37B |
| GNRC·TER·U·VST·SBUX·NRG·UPS·XOM·META·GE·COF·C·OXY·ZETA·BMY·AMD·HPE | 같은 모양 |

값 자체는 대개 맞다 (TGT 25,270 = 백만 단위 · NFLX 8,832,825 = 천 단위).
**단위 표기를 못 읽고 숫자만 가져온 것**이다. 다만 AMZN 15,227 처럼
어느 단위로도 매출이 아닌 값(순이익으로 보임)도 섞여 있다.

**얼마나 위험한가 — 번지는 범위는 좁다:**

- `measure_engine.py`·`judge.py` 에 `revenue` 는 **한 번도 나오지 않는다.**
  잣대 사다리는 조정 EPS·EBITDA·GAAP EPS 뿐이라 **판정에는 닿지 않는다.**
- 다만 매출은 **다른 가드의 잣대**다 (`dataset.py` 68차 주석: 형제 행
  잔해 판정 100배 비율 · 영업이익 마진 검사 · EBITDA 단위 검사).
  그래서 오작동했는지 직접 셌다:

  > 형제 행 잔해로 버린 행 **7건** 중, 배율이 1,000배·100만배 언저리라
  > "잔해가 아니라 단위 차이"로 의심되는 것은 **1건**뿐이다
  > (JPM 23 Q3 — 17,653 ↔ 19.3, 915배. 19.3 은 단위 차이라기엔 작아
  > 진짜 잔해일 가능성이 크다).

  즉 **가드가 단위 뒤섞임에 속아 진짜 실적을 버린 사례는 사실상 없다.**

결론: 매출 칸 자체는 지금 신뢰할 수 없지만, **판정과 잣대는 오염되지
않았다.** 87차에서 원문으로 단위 표기(`(in thousands)` 류)를 읽는 쪽을
고친다 — 부탁 목록에 AMD·HD·TGT·NFLX·TER 원문이 들어 있다.

---

# 87차 — CRM 회귀의 원인 **두 가지**를 실물에서 찾아 고친다 (2026-08-17)

86차가 만든 "원문 부탁" 길이 처음으로 제 몫을 했다. 로봇이 CRM 두 건을
담아 왔고, 그 실물로 원인을 짚었다. **추측은 하나도 쓰지 않았다.**

## ① 원인 하나 — 영향을 말하는 문장을 실적으로 읽었다

CRM 보도자료에 이런 문장이 있다:

> "gains (losses) on strategic investments **impacted** GAAP diluted net
> income per share **by $0.00** and $(0.03) … and non-GAAP diluted net
> income per share **by $0.00** and $(0.03)"

이건 전략적 투자 손익이 주당순이익에 **미친 영향**을 말하는 문장이다.
이름이 조정·GAAP 양쪽에 다 걸려서 **두 잣대가 나란히 0.00** 이 됐다.
진짜 값은 같은 문서의 표에 2.91·1.96 으로 멀쩡히 있었다.

> **이름 하나에 두 잣대가 같은 값으로 무너지면 이 결함을 의심한다.**
> 86차에 "두 칸이 똑같아진 것은 숫자 하나를 집어 양쪽에 넣었다는 뜻"
> 이라고 적어 두었는데, 그 짐작이 실물에서 맞았다.

**저장소 원문 732건 실측 — 반례 0건.** 이 꼴로 나온 20곳이 전부
영향·변화 문장이었다:

| 종목 | 문장 |
|---|---|
| CRM | "impacted … per share **by** $0.00" |
| GOOGL | "increased … diluted net income per share **by** $2.35" |
| IPGP | "decreased … earnings per share **by** $0.03" |
| EL | "impact … per share growth **by** 6%" |
| GS | "Book value per common share increased **by** 20.4%" |

진짜 실적 표는 `Diluted net income per share (3)   $1.96` 처럼 이름과
값 사이에 "by" 가 없다. 그 숫자 하나만 건너뛰지 않고 **이름 자리를
통째로 포기**한다 — "by $0.00 **and $(0.03)**" 처럼 숫자가 이어지면
뒤엣것도 영향값이라 건너뛰기만으로는 또 문다.

## ② 원인 둘 — 찾는 창이 숫자 **한가운데**를 잘랐다

이쪽이 더 근본적이고, 파서 전체에 걸린다.

표에서 이름과 값 사이가 공백 40칸쯤 벌어지면 160자 창의 끝이 값 위에
떨어진다. 실측 — 창 끝 **197**, 값 `$1.59` 는 **194~198**:

```
"GAAP diluted net income per share        $1.5|9"
                                    창 끝 ────┘
```

그래서 1.59 가 아니라 **1** 로 읽혔고, 남은 ".59"(→59.0)와 옆 칸의
**전년 값 1.56** 까지 후보로 올라와 결국 전년 값이 채택됐다.

고치는 방향을 적어 둔다: **창을 넓히는 것은 답이 아니다** — 넓힌
자리에서 같은 사고가 다시 난다. 창은 "숫자를 **어디까지 찾아 나설지**"
를 정하는 것이지 "어디서 자를지"가 아니므로, 찾는 범위는 그대로 두고
(시작 위치가 창 안이어야 함) 숫자가 창 밖으로 이어지면 **끝까지 읽게**
했다. 여유는 40자.

**여기서 한 번 헛디뎠다.** 가드를 `match.start()` 로 썼더니 아무 일도
일어나지 않았다. 숫자 패턴이 **앞의 공백·괄호·$ 까지 포함해서** 시작
하므로 `match.start()` 는 이름 바로 뒤(공백의 시작)를 가리킨다.
`match.start("num")` — 숫자 자체의 시작 — 으로 고쳤다.

## ③ 고쳐진 값

| 공시 | 칸 | 전 | 후 | 야후 |
|---|---|---|---|---|
| CRM 2025-09-03 | 조정 EPS | 0.00 | **2.91** | 월가 2.91 |
| CRM 2025-09-03 | GAAP EPS | 0.00 | **1.96** | 1.96 |
| CRM 2025-05-28 | GAAP EPS | -0.05 | **1.59** | 1.59 |

## ④ 부수 피해 — **원문 732건 전수 대조에서 0건**

86차 파서와 87차 파서로 저장소 원문을 전부 읽어 맞댔다.

> **달라진 칸 4개** (조정 EPS 2 · GAAP EPS 2) — **전부 위 CRM 두 건**.
> 매출·매출총이익률·영업이익·EBITDA 는 **한 칸도 안 바뀌었다.**

`_parse_number_at` 은 파서 전체가 쓰는 함수라 넓게 번질까 걱정했는데,
실물에서는 이 두 건 말고 건드린 곳이 없다.

## ⑤ 아직 못 고친 것 — 세 번째 원인은 **미룬다**

CRM 2025-05-28 의 **조정 EPS 는 1.59 로 여전히 틀렸다** (참값 2.58).
원인은 따로 있다: 논갭 **제목 줄**("Non-GAAP diluted net income per
share" 뒤에 값이 없음)에 걸린 뒤, 문단 탐색이 다음 줄로 넘어가
**GAAP 행의 값**을 물어 온다.

고칠 후보 규칙 — "값이 나오기 전에 **다른 주당 이름**이 끼어들면
멈춘다" — 을 원문 732건에 재 보니 **88곳**이 걸리는데, 같은 이름이
제 짝을 무는 경우(BE·BMY·AMGN)가 섞여 있어 **근거가 깨끗하지 않다.**
①의 20곳/반례 0건 같은 선명함이 없다.

문단 탐색을 좁히는 쪽도 안 된다 — 74차에 "이름이 제목 줄이고 값이 다음
줄"인 형식(HPE) 2건을 잃는 것을 보고 일부러 넓힌 자리다.

**그래서 넣지 않는다.** 78차·83차가 근거 없이 고쳐 회귀를 낸 자리다.
다음 차수로 넘기고, 여기 이름을 붙여 둔다: **제목 줄 결함**.

## ⑥ 시험

`python3 run_tests.py` 종료코드 **0** · **405개 통과, 0개 실패**
(401 → 405).

돌연변이 3건 빨간 불 확인 — 영향 문장 가드 제거 · 창 여유 제거(숫자
잘림 부활) · 창 밖 시작 숫자 허용.

**셋째 것은 처음에 초록이었다.** 시험이 숫자를 창 밖 300자에 두어
가드를 **지나가지도 않는** 자리였기 때문이다(어차피 창에 안 들어와
None). 숫자를 여유 구간 한가운데로 옮겨 빨간 불을 본 뒤에 믿었다.
"새 시험은 일부러 깨뜨려 빨간 불을 본 뒤에만 믿는다"가 또 한 번
값을 했다.

## ⑦ 수리가 스냅샷에 닿았는지 — 로봇 재실행으로 검증 (18:09)

수리한 코드로 로봇을 다시 돌려(`7957ccc`) 실데이터에 반영했다.

**CRM 이 통째로 되살아났다** (야후 GAAP · 월가 조정과 나란히):

| 발표일 | 조정 EPS | GAAP EPS | 야후 GAAP |
|---|---|---|---|
| 2025-02-26 | 2.78 (전 0.08) | **1.75** (전 0.07) | — |
| 2025-05-28 | 1.59 ⚠️ | **1.59** (전 -0.05) | 1.59 |
| 2025-09-03 | **2.91** (전 0.00) | **1.96** (전 0.00) | 1.96 |
| 2025-12-03 | **3.25** (전 0.22) | **2.19** (전 0.21) | 2.19 |
| 2026-02-25 | **3.81** (전 0.67) | **2.07** (전 0.66) | 2.07 |
| 2026-05-27 | 3.88 | 2.42 | 2.42 |

⚠️ 표시한 2025-05-28 의 조정 EPS 만 여전히 틀렸다(참값 2.58) — ⑤의
**제목 줄 결함**이다. 나머지는 전부 맞다.

**바깥 자로 잰 판정 — 회귀 0건:**

> 새 값이 맞음 **6** · 옛 값이 맞았음 **0** · 둘 다 틀림 **0**

CRM 네 분기 + VZ 두 분기(0.55·0.92 — 없던 값이 생겼고 야후와 일치).

| 칸 | 전 | 후 |
|---|---|---|
| GAAP EPS | 76.9% | **77.4%** (+0.5p) · 다름 132→128 |
| 매출 | 79.8% | 79.6% (-0.2p) · 다름 **115→115** |
| 매출총이익률 | 59.1% | 58.9% (-0.2p) · 다름 **184→184** |

## ⑧ 매출·이익률이 0.2p 내려간 것은 **내 수리 탓이 아니다** — 확인했다

숫자만 보면 두 칸이 조금 내려갔다. 그냥 넘기지 않고 원인을 짚었다.

**"다름"은 하나도 안 늘었다** (115→115 · 184→184). 내려간 것은 분모다 —
맞대 볼 수 있는 칸이 매출 570→565, 이익률 450→448 로 줄었다.

줄어든 자리를 따라가니 **MA(마스터카드) 한 종목**이었다. 로봇 기록:

> MA `[get_facts] SSLVerificationError … https://data.sec.gov/api/xbrl/
> companyfacts/CIK0001141391.json … SSL: UNEXPECTED_EOF_WHILE_READ`

XBRL 뼈대를 못 받아 그 종목 행의 출처가 `근사치` → `역산` 으로 바뀌었다.
**SEC 접속이 그날 실패한 것**이지 파서가 값을 잃은 것이 아니다.
같은 행에서 값만 사라진 칸은 전체에서 **1칸**뿐이다(MCHP 조정 0.02).

근거 하나 더: 87차 파서를 원문 732건에 돌린 전수 대조에서 바뀐 칸은
**4개(전부 CRM)** 였고 MA 는 한 칸도 없었다.

교훈으로 적어 둔다: **스냅샷은 실행마다 흔들린다.** SEC 가 그날
막히면 종목 하나가 통째로 다른 경로로 채워진다. 그래서 앞뒤 비교는
비율만 보지 말고 **"다름"의 절대 수와 분모를 함께** 봐야 한다.

---

# 88차 — 매출 단위를 **이름에 붙여** 적는 형식을 읽는다 (2026-08-17)

86차 ⑫에 "87차에서 단위 표기를 읽는 쪽을 고친다"고 적어 두었다.
87차는 CRM 회귀에 다 썼으므로 여기서 갚는다. 로봇이 부탁 목록대로
AMD 원문을 담아 와서 실물로 짚을 수 있었다.

## ① 원인 — 단위가 **표 제목이 아니라 이름에** 있었다

AMD 보도자료에는 `(in millions)` 가 **문서 어디에도 없다.** 대신:

```
                        Q4 2025    Q4 2024      Y/Y
Revenue ($M)            $10,270     $7,658    Up 34%
```
```
CONDENSED CONSOLIDATED STATEMENTS OF OPERATIONS
(Millions except per share amounts and percentages) (Unaudited)
Net revenue                            $10,270
```

기존 단위 규칙 넷은 **전부 "in" 을 요구**했다
(`(in millions)` · `($ in millions)` · `dollars in millions` …).
그래서 10,270 이 그대로 들어갔다 (참값 102.7억). 86차 ⑫에 적은
"한 종목 안에서도 단위가 뒤집힌다"(44곳/20종목)의 원인이 이것이다.

한 종목 안에서 뒤집히는 까닭도 이제 설명된다 — 같은 문서 안에 단위가
붙은 표와 안 붙은 표가 **둘 다** 있고, 어느 쪽이 잡히느냐가 분기마다
달라진다.

## ② 무엇을 넣고 무엇을 뺐나 — 원문 732건 실측

| 꼴 | 곳 / 종목 | 결정 |
|---|---|---|
| `($M)` `($B)` `($K)` (달러기호 있음) | 300곳 / 19종목 | **넣는다** |
| `(Millions…` (in 없음) | 335곳 / 25종목 | **넣는다** |
| `(M)` (달러기호 **없음**) | 180곳 / 11종목 | **뺀다** |

마지막을 뺀 이유가 중요하다. 실물을 열어 보니 **전부 `Shares (M)`** —
**주식 수**의 단위지 금액의 단위가 아니다. 이걸 매출에 곱하면 100만 배
틀린다. 각주 기호 `(A) (B) (C)` 와도 구별이 안 된다.
**달러 기호가 붙은 것만** 인정한다.

`(Millions…` 에 닫는 괄호를 요구하지 않은 것도 실물 때문이다 —
AMD 손익계산서 머리글이 `(Millions except per share amounts and
percentages)` 라 닫는 괄호를 요구하면 못 잡는다. 괄호가 **단위 낱말로
시작하면** 단위 선언이다.

## ③ 부수 피해 — 원문 732건 전수 대조

> **달라진 칸 36개** · 매출 32 · EBITDA 3 · 영업이익 1 ·
> **조정 EPS 0 · GAAP EPS 0 · 매출총이익률 0**

| 무엇이 | 몇 칸 |
|---|---|
| 단위 배수 그대로(×1,000 · ×100만 · ×10억) 커진 칸 | 30 |
| 없던 값이 생김 (ABNB 매출 · AMD EBITDA ×3 · WDC 매출) | 5 |
| 값이 없어짐 (SEDG 매출 202 → 없음) | 1 |

되살아난 값들이 전부 제 크기를 찾았다 — TGT 23,846 → 238.5억 ·
JPM 20,272 → 202.7억 · MS 20,580 → 205.8억 · VST · SNOW · WDC · NOW ·
ISRG · AMD. 사라진 SEDG 202 는 원래 틀린 값이었다(참값 7.25억)
— **틀림에서 없음으로** 간 것이라 안전한 방향이다.

**조정 EPS·GAAP EPS·매출총이익률이 한 칸도 안 바뀐 것**이 가장 중요한
신호다. 이 저장소에서 제일 무서운 사고가 "주당 금액에 표 단위를 곱하는
것"($1.00 → $100만)인데, 단위 규칙을 넓히고도 그쪽은 건드리지 않았다.

## ④ 정직하게 — 아직 틀린 채로 남은 칸 둘

단위만 맞췄을 뿐 **값 자체가 틀린 칸이 둘** 있다:

| 종목 | 전 | 후 | 참값 |
|---|---|---|---|
| AXP | 4.53 | 45.3억 | 분기 매출은 약 180억 (읽은 숫자가 애초에 매출이 아님) |
| LITE | 1 | 100만 | 분기 매출은 약 4억 |

전에는 `4.53` · `1` 이라 **한눈에 이상해 보였는데**, 이제는 그럴듯한
크기가 되어 **더 위험해졌을 수 있다.** 단위 규칙이 낸 문제는 아니지만
(둘 다 전에도 틀렸다), 이 차수가 만든 상태이므로 적어 둔다.
다음 부탁 목록에 올린다.

## ⑤ 시험

`python3 run_tests.py` 종료코드 **0** · **408개 통과, 0개 실패**
(405 → 408).

돌연변이 **4건** 빨간 불 확인 — `($M)` 규칙 제거 · `(Millions…` 규칙
제거 · 달러기호 없는 `(M)` 도 허용(Shares (M) 사고 재현) ·
**EPS 경로에도 표 단위를 곱하도록 주입**.

마지막 것은 "구조상 안 곱한다"는 말로 넘기지 않고, 실제로 EPS 반환값에
`_detect_table_unit` 을 곱해 넣어 빨간 불을 봤다 (EPS 0.92 → 920,000).
말로 하는 안전 주장은 믿지 않는다.

---

# 89차 — 매출의 **조각**을 전체로 읽던 것을 고친다 (2026-08-17)

88차가 낸 회귀를 바깥 자(야후)가 잡아냈다. **88차 ④에 "그럴듯해져서
더 위험하다"고 적어 둔 일이 바로 다음 차수에 실제로 일어났다.**

## ① 88차가 낸 회귀 — SNOW 네 분기

| 분기끝 | 88차 전 | 88차 후 | 야후 |
|---|---|---|---|
| 2025-04-30 | 1,042,074,000 ✅ | 996,800,000 ❌ | 1,042,074,000 |
| 2025-07-31 | 1,144,969,000 ✅ | 1,090,500,000 ❌ | 1,144,969,000 |
| 2025-10-31 | 1,212,909,000 ✅ | 1,158,400,000 ❌ | 1,212,909,000 |
| 2026-04-30 | 1,390,951,000 ✅ | 1,334,300,000 ❌ | 1,390,951,000 |

원인: SNOW 보도자료의 머리기사가 **"Product revenue of $1.16 billion"**
이고 전체 매출(Total revenue)은 뒤쪽 재무제표에만 있다. 이름과 값의
거리로 고르는 규칙이라 **가까운 조각이 이겼다.**

88차 전에는 그 조각 값이 단위 없이 `1,090.5` 로 들어와 뒷단 검사에 걸려
XBRL 의 올바른 값이 살아남았다. **88차가 단위를 맞춰 주자 그럴듯한
크기가 되면서 올바른 값을 밀어냈다.**

## ② 고친 규칙 — 조각 이름

`revenue` 앞에 부문 이름이 붙으면 전체 매출이 아니다:
product · service(s) · subscription · deferred · organic · other ·
interest · trading · advertising · license · maintenance · hardware ·
software.

실측(원문 앞 낱말 세기): deferred 298 · service(s) 290 · product 264 ·
organic 151 · other 143 · interest 104 · trading 103 · advertising 94 ·
subscription 85. **deferred revenue 는 아예 부채 항목**이다.

## ③ 처음 규칙은 **너무 셌다** — 전수 대조가 잡아냈다

무조건 조각을 버렸더니 **VRTX 가 맞는 값(26.9억)을 잃었다.**
VRTX 문서에는 `Total/Net revenue` 가 **한 번도 안 나오고**
`Product revenue of $2.69 billion` 뿐인데, 그게 곧 전체 매출이다.

→ **"전체 매출 이름이 그 문서에 있을 때만 조각을 버린다"** 로 고쳤다.
전체가 있으면 조각을 버리고, 조각뿐이면 조각을 쓴다.

내 시험 하나가 처음의 센 규칙("조각만 있으면 없음")을 못박고 있어서
**시험도 함께 고쳤다.** 실물이 규칙을 이긴다.

## ④ 전수 대조 — 원문 732건

> **달라진 칸 31개** · 매출 29 · 영업이익 2 ·
> **조정 EPS 0 · GAAP EPS 0 · 매출총이익률 0**

되살아난 값: SNOW 4분기 · SMCI 매출 3.7억→57.6억·7.7억→126.8억 ·
GNRC 2.1억→11.1억 · U 2.2억→5.3억 · VRT · LOW · ZETA · FSLR.
잃은 값은 없다(VRTX 는 ③에서 되살렸다).

## ⑤ 넣었다가 **도로 뺀 규칙** — 전망 가드

FSLR 을 보다가 "전망(가이던스)을 실적으로 읽는" 결함을 발견했다.
조각 가드가 `deferred revenue`(부채!)를 걷어내자, 그다음으로 가까운
이름이 `2026 **net sales guidance** of $4.9 billion` 이어서 전망치가
분기 매출 자리에 들어왔다(참값 약 17억).

EPS 경로에는 이미 전망 가드(`_FORECAST_NEAR_RE`)가 있는데 **금액
경로에는 없어서**, 같은 규칙을 금액에도 달았다. 그리고 전수로 쟀다:

> 달라진 칸이 31개에서 **86개로 늘었고, 늘어난 55개가 대부분 나빠졌다.**
> ACLS 2.45억 → 202 · CRM 600억 → 2 · PFE → 202 · STX 21억 → 18 ·
> AMD 영업이익 18.5억 → **0** · HPE 매출 10,678 → 없음

보도자료에는 "outlook · expects" 가 본문 곳곳에 흩어져 있어, 이름 앞뒤
80자 창이 **멀쩡한 실적 줄까지** 걸러 냈다. EPS 는 값이 이름에 바싹
붙어 있어 괜찮았지만 금액 표는 다르다.

**그래서 도로 뺐다.** 재 보고 나빠지면 넣지 않는다 — 좋아 보이는
규칙이라도. 뺀 뒤 전수 대조는 다시 31개로 돌아왔다.

FSLR 의 전망 표(`net sales  $4.9B  to  $5.2B`)는 "guidance" 라는 낱말이
그 줄에 없어 낱말 가드로는 못 잡는다. **범위 표기(`A to B`)로 잡는
규칙**이 필요한데, 그건 EPS 경로의 84차 범위 가드와 같은 종류다.
근거를 따로 모아 다음 차수에 다룬다 — 이름을 붙여 둔다: **전망 표 결함**.

## ⑥ 시험

`python3 run_tests.py` 종료코드 **0** · **412개 통과, 0개 실패**
(408 → 412).

돌연변이 3건 빨간 불 — 조각 가드 제거 · `total`·`net` 까지 조각 취급 ·
**"전체가 있을 때만" 조건 제거**(VRTX 손실 재현).

## ⑦ 로봇 재실행으로 검증 (23:0x) — 회귀 0건

수리한 코드로 로봇을 다시 돌려 실데이터에 반영했다.

| 칸 | 89차 전 | 89차 후 |
|---|---|---|
| GAAP EPS | 77.7% | 77.7% (그대로) |
| **매출** | 79.9% | **81.7%** (+1.8p) · 다름 **120 → 110** |
| 매출총이익률 | 59.1% | 59.1% (그대로) |

> 새 값이 맞음 **13** · 옛 값이 맞았음 **0** · 둘 다 틀림 10

**88차가 낸 SNOW 회귀가 지워졌고, 새로 생긴 회귀는 없다.**
조각 가드는 매출만 건드리므로 EPS·이익률이 그대로인 것이 정상이다.

87차 이후 매출 일치율의 걸음: 79.6% → 79.9%(88차) → **81.7%**(89차).

---

# 90차 — 파서와 XBRL 을 **맞붙여 보기** (재고 뒤집기 1단계, 2026-08-18)

## ① 오류의 정체를 처음으로 분류했다

"데이터를 잘못 읽는 일이 왜 이렇게 잦은가"를 규칙 하나하나가 아니라
**종류별로** 재 봤다. 야후와 어긋난 칸을 배율(우리÷야후)로 나눈 결과:

**GAAP EPS — 어긋난 칸 128개 (맞대 본 574개 중 22.3%)**

| 비중 | 종류 | 실물 |
|---|---|---|
| 25.8% | 0.7~0.8배 — **전년 열** | GOOGL 2.15 ↔ 2.82 |
| 24.2% | 거의 같음 (반올림·정의 차이) | FORM 0.14 ↔ 0.12 |
| 18.8% | 약 3배 — **9개월 누적** | MRVL 0.62 ↔ 0.20 |
| 9.4% | 약 4배 — **연간값** | FDX 14.20 ↔ 3.46 |
| 7.8% | 5배 이상 | VRTX 85.00 ↔ 4.31 |
| 7.0% | **부호가 반대** | MDB −0.34 ↔ 0.18 |
| 6.2% | 3배 이상 작음 | AMZN 1.59 ↔ 5.75 |

**매출 — 어긋난 칸 110개 (600개 중 18.3%)**. 22.7%가 "3배 이상 작음"인데
실물을 보니 **사업부 하나를 회사 전체로** 읽은 것이었다 (VZ 82억 ↔ 364억).

> **약 4분의 3이 "숫자는 맞는데 기간·열·부문이 틀린 것"이고,
> 나머지 4분의 1은 진짜 오류가 아니다(반올림·정의 차이).**

## ② 그래서 규칙을 더 넣는 방식은 한계다 — 이번 세션이 증거다

80~89차에 규칙 10개를 더했는데:

- **2건이 회귀를 냈다** (83차 → UNH 6.04를 17.10으로 · 88차 → SNOW 4분기)
- **1건은 재 보고 도로 뺐다** (89차 전망 가드 — 31칸 → 86칸으로 나빠짐)
- 한 종류를 고치면 **가려져 있던 다른 종류가 드러난다** (89차 FSLR)

이유는 단순하다. **보도자료는 표를 글자로 납작하게 편 것**이라 숫자에
"이건 3개월치다 / 올해 열이다 / 전체가 아니라 사업부다"라는 표시가
남아 있지 않다. 파서는 거리로 짐작할 뿐이고, 규칙을 아무리 더해도
**없는 정보를 만들 수는 없다.**

## ③ 결정적인 사실 — 정답지를 이미 받아 놓고 덮어쓰고 있었다

SEC XBRL 은 값마다 **기간(시작~끝)·단위·부문**이 태그로 붙어 있다.
①의 오류 네 종류가 **구조적으로 불가능**하다. 그리고 이 저장소는
이미 그것을 받고 있었다:

```
_XBRL_CONCEPTS = { "gaap_eps": ["EarningsPerShareDiluted"],
                   "revenue":  [… "Revenues"],
                   "gross_profit": ["GrossProfit"] }
_quarterly_series(…)  →  months=3 으로 못박아 조회
```

그런데 `_apply_press_to_row` 에서:

```python
if press.get("gaap_eps") is not None:
    row["gaap_eps"] = press["gaap_eps"]      # ← 짐작이 태그를 덮어쓴다
```

**방향이 거꾸로였다.**

## ④ 그래도 바로 뒤집지 않았다 — 먼저 잰다

바로 뒤집는 것이 근본 해법으로 보이지만, **근거 없이 바꿨다가 되돌린
것이 바로 어제 89차**다(전망 가드). 같은 실수를 되풀이하지 않는다.

이번 차수에 한 일은 **덮어쓰기 전의 XBRL 값을 옆에 남기는 것뿐**이다.

- `gaap_eps_xbrl` · `revenue_xbrl` · `gross_margin_pct_xbrl` 을 추가
- **본선 값은 한 칸도 안 바뀐다** — 지금도 보도자료가 이긴다
- 조정 EPS·조정 EBITDA 는 **짝 칸을 만들지 않았다** — XBRL 에 없는
  값이라, 있는 척하면 다음 세션이 잘못 읽는다 (창작 금지)

그리고 심판을 세웠다 — `vendor_compare.duel()`:

> 같은 칸을 두 경로가 읽었을 때 야후가 누구 편인지 센다.
> 둘 다 맞음 · **보도자료만 맞음** · **XBRL만 맞음** · 둘 다 틀림.
> 야후가 그 칸을 모르면 **아예 안 센다** — "둘 다 없음"을 무승부라고
> 부르면 숫자가 거짓말을 한다.

다음 로봇 실행이 두 값을 담아 오면, 그 승률을 보고 **뒤집을지 말지를
숫자로** 정한다.

## ⑤ 시험

`python3 run_tests.py` 종료코드 **0** · **420개 통과, 0개 실패**
(412 → 420).

돌연변이 **5건** 빨간 불 확인 — 심판이 모르는 칸도 승부로 세기 ·
한쪽만 있는 칸을 승부로 세기 · XBRL 승리를 보도자료 승리로 세기 ·
XBRL 값을 안 남기기 · **덮어쓴 뒤에 남기기**(두 값이 같아져 승부가
영영 안 갈리는 조용한 고장).

마지막 것이 이 차수에서 가장 위험한 실수였을 것이다. 값은 다 있고
시험도 초록인데 **승부만 영원히 무승부**가 되어, 다음 세션이 "두 경로가
똑같네"라고 잘못 결론 냈을 것이다.

## ⑥ 덧붙임 — 남은 단위 결함 3종목은 **정규식으로 못 고친다** (실물 확인)

88차가 AMD·TGT·VST 의 단위를 고쳤지만 NFLX·TER·HD 는 그대로였다.
부탁 목록으로 받아 온 원문으로 원인을 짚었다.

**셋 다 단위 표기가 문서에 있다.** 그것도 파서가 이미 아는 표준형
(`(in millions)` · `(in thousands)`)이다. 문제는 표기가 아니라
**그 표기가 숫자에서 얼마나 · 어느 쪽에 떨어져 있는가**였다:

| 종목 | 숫자 자리 | 가장 가까운 단위 표기 | 왜 못 잡나 |
|---|---|---|---|
| NFLX | — | **17,843자 앞** | 찾는 창(3,000자) 밖 |
| HD | 9,926 | **3,214자 뒤** | 방향이 반대 (뒤는 안 봄) |
| TER | 9,487 | **9,746자 뒤** | 방향이 반대 + 멀다 |

실물 HD:

```
                              May 4,        April 28,     % Change
                               2025           2024
Net sales                    $39,856        $36,418
```

이 표에는 단위가 **한 글자도 없다.** `(in millions)` 는 3,214자 **뒤**
재무제표에 가서야 나온다. TER 도 같은 모양이고, 그 표기(`(In thousands)`)는
9,746자 뒤에 있다.

**즉 셋 다 보도자료 **맨 앞 요약표**의 숫자이고, 그 표는 단위를 자기
안에 적지 않는다.** 회사는 사람이 뒤 재무제표를 보고 알 것이라 여긴다.

### 그래서 규칙을 하나 더 넣지 않았다

고치려면 "앞에 없으면 뒤도 본다" 같은 규칙이 필요한데, 그건 창을 양쪽
2만 자로 넓히는 것과 같아서 **엉뚱한 표의 단위를 끌어오는 문**을 연다.
80~89차에 그렇게 문을 열었다가 회귀를 두 번 냈고 한 번은 도로 뺐다.

**이건 90차 방향의 세 번째 증거다.** 단위·기간·부문은 보도자료 글자에
**애초에 안 적혀 있고**, XBRL 에는 값마다 태그로 붙어 있다. 정규식을
더 정교하게 만드는 문제가 아니라 **어느 자료를 보느냐**의 문제다.

로봇이 두 값을 담아 오면 승부가 갈릴 것이다. XBRL 이 매출에서 이기면
이 세 종목은 **규칙을 하나도 안 더하고** 함께 고쳐진다.

---

# 91차 — 승부 결과: **매출은 XBRL 98:0**, GAAP EPS 는 잴 수도 없었다 (2026-08-18)

90차가 깔아 둔 두 값을 로봇이 담아 왔다(3,066행 전부). 야후를 심판으로
칸별 승률을 셌다.

## ① 매출 — **98 : 0**

| | 수 |
|---|---|
| 둘 다 읽음 | 589 |
| 둘 다 맞음 | 483 |
| **XBRL만 맞음** | **98** |
| **보도자료만 맞음** | **0** |
| 둘 다 틀림 | 8 |
| 한쪽만 있음 | XBRL 45 · 보도자료 11 |

**갈린 98칸에서 보도자료가 한 번도 이기지 못했다.**

이긴 자리의 종류를 나눠 보니 89차까지 이름 붙여 둔 결함들과 정확히
겹친다:

| 수 | 종류 |
|---|---|
| 38 | 보도자료가 조금 작음 |
| **27** | 보도자료가 **절반 이하** — 부문 매출 (89차 "조각 매출") |
| 18 | 비슷 (반올림) |
| **10** | 보도자료가 **1.5배 이상** — 연간값·전망표 (89차 ⑤) |
| 5 | 조금 큼 |

실물: ABBV 117.6억(부문) ↔ XBRL 154.2억 · ADBE 259억(연간) ↔ 61.9억.
ABBV 분기 매출은 실제로 154억대이고 ADBE 분기는 62억대다 — **심판을
빼고 봐도 XBRL 이 맞다.**

## ② 매출총이익률 — 169 : 0 이지만 **승부로 읽으면 안 된다**

숫자는 더 일방적이지만, 이 칸은 **정의가 다르다.**
실물 ADI: 보도자료 69.4%(회사 발표 **논갭**) ↔ XBRL 61.0%(**갭**).
야후도 갭 기준이라 XBRL 이 자동으로 이긴다.

86차에 "매출총이익률 불일치는 결함이 아니라 정의 차이"라고 이미 적어
두었다. **169:0 은 XBRL 이 더 정확하다는 뜻이 아니라 야후와 기준이
같다는 뜻이다.** 이 칸의 방향 전환은 이 숫자로 정하지 않는다.

## ③ GAAP EPS — **한 건도 안 들어와 있었다**

승부를 재려 했더니 "맞대 볼 칸이 없습니다"가 나왔다. 세어 보니:

> `gaap_eps_xbrl` 이 **3,066행 전부 없음(None)**.
> 반면 `revenue_xbrl` 2,383행 · `gross_margin_pct_xbrl` 1,528행은 정상.

그런데 **로봇 기록에 오류가 하나도 없다.** 조회는 돌았고 조용히 0건을
돌려준 것이다. 코드에는 `_XBRL_CONCEPTS["gaap_eps"] =
["EarningsPerShareDiluted"]` 가 있고 단위도 `USD/SHARES` 로 지정돼 있다.

**"XBRL GAAP EPS 수집 추가(대책 4)"는 완료로 표시돼 있었지만, 실제
데이터에는 한 건도 도착한 적이 없다.** 이 저장소가 여러 번 겪은
가짜 초록불이 또 하나 있었던 셈이다.

**추측으로 고치지 않았다.** 후보는 여럿이다 — 단위 글자 표기 · 개념
이름 · 모호 판정(EPS 는 값이 작아 두 후보의 비가 2배를 쉽게 넘고,
적자 분기는 `low > 0` 조건에서 통째로 탈락한다). 어느 것인지 모른 채
고치면 89차 전망 가드를 되풀이한다.

대신 **계기를 달았다**: 개념별로 `받음 · 버림 · 모호 · 남음` 을 세어
로봇 기록(`xbrl_calls`)에 남긴다. 다음 실행이 숫자로 말해 주면 그때
고친다. 조용한 0 을 없애는 것이 먼저다.

## ④ 그래서 무엇을 바꿨나 — **아직 아무 값도 안 바꿨다**

매출에서 98:0 이 나왔으니 뒤집을 근거는 섰다. 다만 이번 차수에서는
계기만 달고 값은 그대로 둔다. 이유:

1. GAAP EPS 는 아직 **승부 자체가 성립하지 않았다.** 세 칸 중 하나만
   보고 병합 규칙을 손대면 다음 실행에서 또 반쪽 결론이 난다.
2. 매출을 XBRL 우선으로 돌리면 `XBRL만 있음 45` 는 얻지만
   `보도자료만 있음 11` 은 잃는다. 잃는 쪽이 어떤 종목인지 먼저 봐야
   한다.

92차에서 ③의 계기 결과를 받아 세 칸을 **한꺼번에** 정한다.

## ⑤ 시험

`python3 run_tests.py` 종료코드 **0** · **420개 통과, 0개 실패**
(계기만 달았으므로 개수는 그대로).

## ⑥ 덧붙임 — 뒤집으면 **무엇을 잃는가** (92차 결정용 사전 조사)

91차 ④에 "매출을 뒤집으면 보도자료만 있는 11칸을 잃는데 그게 어느
종목인지 먼저 봐야 한다"고 적어 두었다. 재 보았다.

**잃는 11칸 중 7칸이 맞던 값이다:**

| | 종목 | 보도자료 ↔ 야후 |
|---|---|---|
| ✅ | ABNB ×4 | 30.96억 ↔ 30.96억 (일치) |
| ✅ | NTAP ×2 | 17.32억 ↔ 17.32억 (일치) |
| ✅ | APP ×1 | 16.58억 ↔ 16.58억 (일치) |
| ❌ | JPM ×2 | 938억·1,359억 ↔ 449억·529억 (연간값) |
| ❌ | BA ×1 | 655억 ↔ 233억 (연간값) |
| ❌ | BAC ×1 | 105,856 ↔ 312억 (단위 깨짐) |

얻는 45칸은 MCHP·MA·MCD·UNP 각 5 · FSLR·AMZN·HPE·V 각 4 · CRWD·SO 3 ·
S 2 · TTMI 1.

### 그래서 설계가 정해졌다 — "XBRL 우선, **없으면** 보도자료"

"XBRL 이 언제나 이긴다"로 단순하게 뒤집으면 **맞던 값 7칸을 잃는다.**
틀림을 없음으로 바꾸는 것은 안전하지만, **맞음을 없음으로 바꾸는 것은
손해**다 (헌법 1조는 "없음이 틀림보다 안전"이지 "없음이 맞음보다
안전"이 아니다).

대신 이렇게 하면 잃는 것이 **0** 이다:

| 상황 | 쓸 값 |
|---|---|
| 둘 다 있음 | **XBRL** (98칸 교정) |
| XBRL 만 있음 | XBRL (45칸 새로 얻음) |
| 보도자료만 있음 | 보도자료 (11칸 유지) |

남는 흠도 정직하게 적는다: 유지되는 11칸 중 **4칸(JPM·BA·BAC)은 지금도
틀린 값**이다. 다만 그건 오늘도 틀려 있으므로 **이 설계가 만드는 손해는
아니다.** 그 넷은 89차의 "전망 표 결함"·단위 결함으로 따로 남긴다.

92차에서 GAAP EPS 계기 결과가 오면 이 설계로 세 칸을 함께 손댄다.

---

# 92차 — 매출을 XBRL 로 뒤집고, GAAP EPS 를 버리던 규칙을 고친다 (2026-08-18)

91차 승부 결과가 나왔으니 **기다리지 않고** 확정된 것부터 손댔다.

## ① GAAP EPS 가 한 건도 안 들어오던 이유 — **규칙을 직접 돌려 확인했다**

91차에 "후보가 여럿이다(단위 글자·개념 이름·모호 판정)"라고만 적고
미뤘는데, 셋 중 하나는 **SEC 접속 없이 재현할 수 있었다.** 모호 판정
규칙을 EPS 모양의 값으로 그냥 돌려 봤다:

| 경우 | 후보 | 옛 규칙 |
|---|---|---|
| 적자 분기 | −0.31, −0.30 | **제외** ← 사실상 같은 값인데 |
| 0 근처 | 0.00, 0.02 | **제외** |
| 기본/희석 | 0.10, 0.30 | **제외** |
| 매출 (참고) | 154억, 154억 | 통과 |

옛 규칙은 `low > 0 and high / low <= 2.0` 하나뿐이었다. 원인이 둘이다:

1. **`low > 0` 이라 적자면 무조건 탈락한다.**
2. **비율 판정은 값이 작을수록 가혹하다** — 0.10 과 0.30 은 3배지만
   실제 차이는 20센트뿐이다.

즉 **주당 금액에는 맞지 않는 자를 쓰고 있었다.** 그래서 주당 항목만
**절대차(1센트)** 로 바꿨다. 금액은 자릿수가 커서 비율이 맞으므로
그대로 뒀다 — 거기까지 손대면 검증 범위가 넓어져 89차 전망 가드의
실수를 되풀이한다.

곁들여 **단위 글자 비교**도 낱말 비교로 바꿨다. 제공처마다
`USD/shares` · `USD-per-shares` · `usd/share` 로 적어 한 글자만 어긋나도
전부 버려진다. 주당 단위에는 반드시 "share" 가 들어가고 금액 단위에는
안 들어가므로 그것으로 가른다.

## ② 매출 — XBRL 우선, **없으면** 보도자료

91차 ①(98:0)과 덧붙임 ⑥(잃는 것 조사)을 그대로 반영했다.

| 상황 | 쓸 값 | 효과 |
|---|---|---|
| 둘 다 있음 | **XBRL** | 98칸 교정 |
| XBRL 만 있음 | XBRL | 45칸 새로 얻음 |
| 보도자료만 있음 | 보도자료 | 11칸 유지 (**잃는 것 0**) |

"XBRL 이 언제나 이긴다"로 두지 않은 이유는 그러면 **맞던 값 7칸**
(ABNB 4 · NTAP 2 · APP 1)을 잃기 때문이다. 헌법 1조는 "없음이 틀림보다
안전"이지 "없음이 **맞음**보다 안전"이 아니다.

## ③ 매출총이익률은 **뒤집지 않았다**

숫자만 보면 169:0 으로 더 일방적이지만 이 칸은 뜻이 다르다 —
보도자료는 회사 발표 **논갭**(ADI 69.4%), XBRL·야후는 **갭**(61.0%).
심판이 갭 기준이라 XBRL 이 자동으로 이길 뿐 더 정확해서가 아니다.
86차에 적어 둔 정의 차이가 그대로 나타난 것이므로 **그대로 둔다.**

## ④ 내 시험 하나가 옛 동작을 못박고 있었다

90차에 넣은 `test_xbrl_value_is_kept_beside_the_press_value` 가
"본선은 셋 다 보도자료가 이긴다"를 못박고 있어, 매출을 뒤집자 빨간 불이
났다. **시험이 맞고 코드가 틀린 것이 아니라, 91차 실측(98:0)이 나온
뒤라 시험 쪽이 낡은 것**이다. 함께 고쳤다 — 실물이 규칙을 이긴다.

## ⑤ 시험

`python3 run_tests.py` 종료코드 **0** · **425개 통과, 0개 실패**
(420 → 425).

돌연변이 **4건** 빨간 불 확인 — 주당도 옛 비율 규칙으로 되돌림 ·
단위를 글자 그대로 비교 · 매출을 보도자료 우선으로 되돌림 ·
이익률까지 뒤집음(정의 차이 무시).

## ⑥ 아직 확인 못 한 것 — 정직하게

- **GAAP EPS 가 이걸로 실제로 들어오는지는 아직 모른다.** ①은 규칙을
  돌려 증명했지만, 100% 결측의 원인이 이것뿐인지는 로봇이 돌아야 안다.
  91차에 단 계기(`xbrl_calls`)가 개념별 "받음·버림·모호·남음"을 적어
  오므로, 다음 실행에서 숫자로 확인한다.
- 매출 뒤집기의 실데이터 효과(일치율 81.7% → ?)도 다음 실행에서 잰다.

## ⑦ 덧붙임 — 92차 뒤집기가 **밀린 결함 셋을 함께 고친다** (미리 확인)

로봇을 기다리는 동안, 92차 규칙을 지금 스냅샷에 적용해 보았다
(값은 안 바꾸고 계산만). 그동안 이름 붙여 미뤄 둔 결함들이 **규칙을
하나도 더하지 않고** 함께 고쳐진다:

| 밀린 결함 | 어디에 적었나 | 결과 |
|---|---|---|
| 단위 결함 NFLX·TER·HD | 90차 덧붙임 ("정규식으로 못 고친다") | **13칸 전부 고쳐짐** |
| 전망 표 결함 FSLR | 89차 ⑤ | **4칸** 없음 → 정답 |
| 조각 매출 VZ·ABBV | 86차 · 91차 | **10칸** |
| 연간값 BA·VST·ADBE | 89차 · 91차 | **4칸** |

실물 몇 개:

```
HD    39,856          → 39,856,000,000   (야후와 일치)
NFLX  11,079,166      → 11,079,166,000   (야후와 일치)
VZ     6,255,000,000  → 34,504,000,000   (부문 → 전체)
BA    89,463,000,000  → 23,948,000,000   (연간 → 분기)
FSLR  없음            →  1,682,782,000   (전망표를 물던 자리)
```

관심 종목 14개에서만 **맞던 칸 4 → 35.**

90차 덧붙임에 "XBRL 이 매출에서 이기면 이 세 종목은 규칙을 하나도 안
더하고 함께 고쳐진다"고 적어 두었다. **그대로 됐다.**

남은 밀린 결함은 이제 **제목 줄 결함(87차 ⑤, CRM 조정 EPS 1.59 ↔ 참값
2.58)** 하나다. 조정 EPS 는 XBRL 에 없으므로 이건 파서로 고쳐야 한다.

---

# 93차 — 계기가 내 92차 수정이 **원인이 아니었다**고 말했다 (2026-08-18)

## ① 92차 수정은 옳았지만 이 문제의 원인은 아니었다

91차에 단 계기를 92차 코드로 돌린 결과가 왔다. 158개 종목 전부 같은
모양이다:

```
EarningsPerShareDiluted(3개월): 받음 32 · 버림 32 · 모호 0 · 남음 0
EarningsPerShareDiluted(3개월): 받음 192 · 버림 192 · 모호 0 · 남음 0
```

**받은 줄을 100% 버리고 있고, `모호 0` 이다.**

모호가 0 이라는 것은 **모호 판정 규칙에 닿지도 못했다**는 뜻이다.
92차 ①에서 고친 것이 바로 그 규칙이었다. 즉:

> **92차의 "주당은 절대차로 판정" 수정은 옳은 수정이지만,
> GAAP EPS 가 한 건도 안 들어오던 원인은 아니었다.**

92차 ⑥에 "100% 결측의 원인이 이것뿐인지는 로봇이 돌아야 안다"고 적어
두었는데, 돌려 보니 **이것이 아니었다.** 그대로 적는다.

같은 실행에 단위 비교도 낱말 비교로 바뀌어 있었으므로, 남은 범인은
**① 개념 이름 완전일치** 아니면 **② 단위**인데 계기가 둘을 합쳐 세고
있어 가릴 수가 없었다.

## ② 그래서 계기를 한 단계 더 쪼갰다

`받음 · 버림` 을 `받음 · 이름버림 · 단위버림 · 모호 · 남음` 으로 나누고,
**실제로 본 개념 이름과 단위 글자의 예시**를 함께 남기게 했다:

```
EarningsPerShareDiluted(3개월): 받음 192 · 이름버림 ? · 단위버림 ? ·
  모호 0 · 남음 0 · 본이름 [...] · 본단위 [...]
```

다음 실행이면 **짐작 없이 확정**된다. 이 저장소가 여러 번 확인한 규칙
— 규칙보다 장치를 먼저 의심하고, 의심은 숫자로 끝낸다.

## ③ 값은 이번에도 안 바꿨다

계기만 세분화했다. `python3 run_tests.py` 종료코드 **0** ·
**425개 통과** (개수 그대로).

## ④ 정정 — 내가 "92차 후"라고 부른 것은 **92차 전**이었다

92차 뒤집기의 효과를 재려고 앞뒤를 맞대었더니 **세 칸 모두 변화 0** 이
나왔다. "고쳤는데 배관에 안 닿았나" 싶어 실행 이력을 확인했다:

| 실행 | 시작 | 쓴 코드 |
|---|---|---|
| 32091162194 | 02:15 | **80cc705 = 91차** ← 내가 "92차 후"라고 부른 스냅샷 |
| 32094869465 | 03:16 | **a363e6b = 92차** ← 아직 돌고 있음 |

**92차 코드는 아직 한 번도 안 돌았다.** 내가 잰 "전 vs 후"는 사실
**91차 대 91차**였으므로 변화가 0 인 것이 당연하고, 그 비교에는 아무
뜻이 없다.

원인은 내가 커밋 순서(`git merge-base`)만 보고 "반영된 실행"이라고
단정한 것이다. 커밋이 먼저 있었다는 것과 **워크플로가 그 SHA 를
체크아웃했다는 것은 다른 말**이다. 실행이 어느 SHA 를 썼는지는
`head_sha` 로만 알 수 있다.

**교훈으로 남긴다: 실행 결과를 해석하기 전에 그 실행의 `head_sha` 를
먼저 확인한다.** 이 저장소가 여러 번 겪은 "고친 코드가 안 돌았다"의
새로운 얼굴이다.

⚠️ 다만 ①의 계기 결과(받음=버림, 모호 0)는 그대로 유효하다 — 그 계기는
91차에 달았고 91차 코드로 돌았으므로 91차 상태를 정확히 보고한 것이다.
따라서 "92차의 모호 판정 수정이 원인이 아니었다"는 결론도 그대로다
(모호 0 은 91차 코드에서 이미 그 규칙에 닿지 못했다는 뜻이므로).

---

# 94차 — 표본을 늘린다 (수집 5년 → 10년), 잘려도 스스로 채우게

**날짜**: 2026-08-18 · **커밋**: (이 커밋)

주인 지시: **"표본을늘려"**. 로봇 결과를 기다리지 않고 바로 손댈 수 있는
일이라 먼저 했다.

## ① 왜 기간인가 (종목 넓히기가 아니라)

지금 막힌 곳은 파서가 아니라 **표본 수**다. 사전 등록한 채택 기준이

> 신호 그룹 윌슨 95% 구간 **하한** > 기준선 **상한**, n≥10

인데, 판정 대기 가설 다수가 `n<10` 에 걸려 **영영 판정이 안 나는**
상태다. 표본을 늘리는 길은 둘뿐이다.

| 길 | 표본 | 장세 편향 |
|---|---|---|
| 종목을 넓힌다 | 늘어남 | **그대로** (같은 기간을 더 많이 볼 뿐) |
| 기간을 늘린다 | 늘어남 | **줄어듦** (다른 국면이 들어옴) |

헌법 4조가 "강세장 구간만의 우위는 믿지 않는다"이므로 기간 쪽이 먼저다.
5년(2021-09~)은 2022 약세장 하나를 겨우 담았다. 10년(2016-09~)이면
**2016~2018 금리 인상기**와 **2020 코로나 폭락·회복**이 들어온다.

예상 표본: 실적 행 3,066 → 약 6,000 · 일봉 20만 → 약 40만.
snapshot.json 5.7MB → 약 10MB (깃허브 한도 50MB 경고선 아래).

## ② 바꾼 값 넷

| 자리 | 전 | 후 | 왜 |
|---|---|---|---|
| `config.HISTORY_START_DATE` | 2021-09-15 | **2016-09-15** | 10년 |
| `config.EARLY_STOP_PARSED` | 24 | **44** | 10년×4분기+여유 4. 실측 중앙값이 20, 최대가 24였으므로 **24는 실제로 물려 있던 마개**다 |
| `config.MAX_8K_SCAN` | 100 | **200** | 8-K 는 실적 외 사유로도 올라와 훑을 건수가 분기 수보다 훨씬 많다 |
| `market_data.fetch_daily_data(period=)` | "5y" | **"10y"** | 야후의 5년 제한은 **분·시간봉** 이야기고 **일봉에는 안 걸린다**. 5y 는 야후의 한계가 아니라 우리가 적어 둔 기본값이었다 |

## ③ 진짜 위험은 "180분 강제 종료"였다 — 시간 예산을 걸었다

실측 런 소요를 먼저 봤다:

| 실행 | 소요 |
|---|---|
| 32091162194 | 69.3분 |
| 32083406730 | 67.0분 |
| 32074131552 | 67.2분 |
| 32060906908 | 60.3분 |
| 32047887380 | 74.5분 |
| 32039813410 | 91.8분 |
| 32024489102 | **166.2분** |

워크플로 한도가 **180분**이다. 이미 한 번 166분까지 갔다. 훑을 공시를
두 배로 늘리면 **거의 확실히 넘긴다.** 넘기면 어떻게 되는가:

- 그날 수집물을 **하나도** 커밋하지 못한다
- 애써 내려받은 공시 원문 캐시(`data/raw8k`)도 저장되지 않는다
  (캐시 저장은 작업이 끝난 뒤 단계라, 강제 종료되면 안 돈다)

즉 **매 런이 맨땅에서 시작해 매번 다시 시간 초과** — 10년치를 영영 못
받는다. 한도만 350분으로 늘리는 것은 답이 아니다. 6시간 상한에 다시
같은 덫이 있고, 그 사이 다른 일을 못 한다.

그래서 **시간 예산**을 걸었다 (`config.COLLECT_BUDGET_MINUTES = 130`).

- 130분을 넘기면 8-K 훑기를 **지금까지 받은 만큼으로 멈춘다**
- 8-K 는 **최신 것부터** 훑으므로, 잘리는 쪽은 **항상 가장 오래된 분기**다.
  최근 분기는 언제나 온전하다 — 화면과 판정이 망가지지 않는다
- 받은 원문은 캐시에 남아 **다음 런이 공짜로 이어받는다**
- 130 + (주가·측정·기록 30분) = 160 < 180 이라 여유가 있다

결과적으로 **하루 한 번 도는 로봇이 며칠에 걸쳐 스스로 10년을 채운다.**
사람이 지켜볼 필요가 없다.

## ④ 잘린 것을 숨기지 않는다

정직화 원칙대로, 잘린 종목은 세 곳에 남긴다.

- `report["시간초과"] = True` · `report["note"]` 에 사유
- 액션 로그에 `⏱ 시간 예산에 걸려 옛 분기를 못 받은 종목 N개: …`
- `robot_log.json` 의 종목별 칸에 `시간초과` 와 `note`

**"10년치를 받았다"고 짐작하지 말고 이 기록을 볼 것.** 며칠 뒤
`시간초과` 가 0 이 되면 그때 다 받은 것이다.

## ⑤ 시험 — 조용히 고장 나는 두 방향을 다 못박았다

이 장치는 고장 나도 아무 소리가 안 난다. 방향이 둘이다.

| 고장 | 결과 |
|---|---|
| 마감이 **안 걸림** | 180분 강제 종료 → 그날 수집물·캐시 통째 소멸 |
| 마감이 **항상 걸림** | 매 런이 즉시 멈춰 데이터가 안 늘어남 |

둘 다 시험으로 못박고, **일부러 깨뜨려 빨간 불을 봤다** (헌법 검증 규칙):

| 돌연변이 | 빨간 불 |
|---|---|
| `return clock() >= deadline` → `return False` (마감이 안 걸림) | ❌ `test_시간예산_넘기면_멈춘다` |
| 마감 없을 때 `return False` → `return True` (항상 걸림) | ❌ `test_시간예산_없으면_멈추지_않는다` · ❌ `test_시간예산_0이하는_무제한` |

시계는 가짜를 끼워 실제로 기다리지 않는다.

### 그런데 이 셋으로는 부족했다 — 장치가 **그 자리에 있는지**를 안 봤다

위 세 시험은 `_budget_over()` **자체**가 옳게 답하는지만 본다. 그것이
전부 참이어도 **훑기 반복문이 그 함수를 부르지 않으면** 아무 일도 안
일어난다. 이 저장소는 이미 "고친 코드가 실행 불가능한 자리에 있어
루멘텀 전망이 무너진" 사고를 겪었다 (헌법: *새 코드 경로는 실제로
실행됨을 증명한다*).

그래서 네 번째 시험을 더했다 — 가짜 공시 100건을 물려
`fetch_earnings_8k` 를 **직접 돌리고** 몇 건에서 멈추는지 센다.

| | 기대 |
|---|---|
| 마감 없음 | 100건을 끝까지 훑음 · `시간초과` False |
| 5건쯤에서 마감 | 100건 미만에서 멈춤 · `시간초과` True · `note` 에 사유 |

돌연변이로 확인: 반복문의 `if _budget_over():` 를 `if False:` 로 바꾸자
❌ `test_시간예산이_8K_훑기를_실제로_멈춘다 — 마감을 넘겼는데 100건을
다 훑았다`. 앞의 세 시험은 **전부 초록 불이었다.** 즉 이 네 번째가
없었으면 "장치가 없는 상태"를 통과시켰을 것이다.

시험: `python3 run_tests.py` 종료코드 **0** · **430개 통과** (425 → 430).

## ⑥ 아직 모르는 것

- 10년치가 **실제로 몇 년까지 들어오는가** — 로봇이 며칠 돌아야 안다.
  옛 보도자료는 형식이 달라 파싱률이 낮을 수 있다. 낮으면 "없음"으로
  남으므로 안전하지만, XBRL 쪽(기간·단위 태그가 붙은)은 옛것도 잘 들어올
  것으로 본다 — 92차에 매출을 XBRL 우선으로 뒤집어 둔 것이 여기서 값을 한다.
- 표본이 늘어난 뒤 **판정이 실제로 뒤집히는가**. 늘어난 표본은 장세가
  다른 구간이므로, 지금 유망해 보이는 신호가 **약해질 수도** 있다.
  그것이 이 확장의 목적이다 (헌법 4조).

## ⑦ 내 예산 설계에 구멍이 있었다 — 바닥을 깔았다

돌리기 **전에** 다시 따져 보니 위 설계에 구멍이 있었다.

예산은 **전체 시계**로 잰다. 그런데 종목은 일꾼 3개가 목록 순서대로
처리한다(160종목 ÷ 3 ≈ 53바퀴). 그러면 **예산을 넘긴 뒤에 차례가 온
뒷쪽 종목**은 8-K 를 한 건도 못 훑는다. 잃는 것이 옛 분기가 아니라
**최신 분기까지 통째로**다.

> **표본을 늘리려다 있던 표본을 깎는다** — 최악이다.

그래서 예산이 **절대 깎지 못하는 바닥**을 깔았다
(`config.COLLECT_BUDGET_FLOOR = 24`, 확장 전 값 그대로).

- 종목마다 최소 24건(≈5~6년치)은 **예산과 무관하게** 받는다
- 예산은 그 바닥을 채운 **뒤에만** 작동한다
- 즉 최악의 경우에도 **오늘과 똑같은 데이터**이고, 시간이 남는 만큼만
  10년 쪽으로 더 판다. **절대 오늘보다 나빠지지 않는다.**

돌연변이 확인: 바닥 조건을 빼자 ❌ `test_시간예산은_바닥을_못_깎는다 —
바닥 3건을 채우기 전에 멈췄다 (0건)`. 그리고 앞서 넣은 통합 시험이
**빨간 불로 이 변경을 알려 왔다** — 그 시험의 가짜가 `parsed_ok` 를
안 올려 바닥에 영영 못 닿았기 때문이다. 시험이 규칙 변경을 잡아낸 것이라
시험 쪽을 고쳤다.

시험: `python3 run_tests.py` 종료코드 **0** · **431개 통과** (425 → 431).

---

# 95차 — 92차의 실측 결과: 매출 75.7% → **97.5%**

**날짜**: 2026-08-18 · **실행**: 32094869465 · **`head_sha` a363e6b = 92차** ✅

93차에서 남긴 교훈대로 **`head_sha` 를 먼저 확인**했다. 이번에는 진짜
92차 코드가 돈 결과다 (`ran_at 04:11:26`, 런 종료 04:11:36 일치).

## ① 야후 심판 — 세 칸

| 칸 | 전 (91차) | 후 (92차) | 변화 |
|---|---|---|---|
| **매출** | 490/647 = **75.7%** | 631/647 = **97.5%** | **+21.8%p** (틀림 157 → **16**) |
| GAAP EPS | 449/583 = 77.0% | 487/622 = **78.3%** | +1.3%p (야후만 58 → **19**) |
| 매출총이익률 | 266/450 = 59.1% | 266/450 = 59.1% | 0 (뒤집지 않았으므로 당연) |

매출은 **맞음 490 → 631 (+141), 틀림 157 → 16, 잃은 칸 0**.
설계대로 "둘 다 있으면 XBRL, XBRL 이 없으면 보도자료"가 한 칸도 안 잃고
141칸을 고쳤다.

## ② 예고한 7종목이 전부 고쳐졌다 (92차 덧붙임의 사전 예측)

값을 보기 **전에** 적어 둔 예측이 그대로 맞았다.

| 종목 | 전 | 후 | 무슨 오류였나 |
|---|---|---|---|
| TER | 950,501 | 950,501,000 | 단위(천) |
| HD | 36,820 | 36,820,000,000 | 단위(백만) |
| NFLX | 6,436M | 7,483M | **전년동기 열** |
| VZ | 6,575M | 34,241M | **부문 매출** |
| BA | 47,493M | 15,278M | **누적/연간값** |
| FSLR | 2,875M | 584M | 누적 |
| ABBV | 11,677M | 14,886M | 전년 열 |

87~89차에 실측한 오류 네 종류(전년 열·누적·연간·부문)가 **한 수로 다
없어졌다.** 정규식을 더 붙이는 길로는 못 갔을 것이다 — 그 정보가 보도자료
평문에 아예 없기 때문이다 (NFLX 표식 17,843자 앞, HD 3,214자 **뒤**).

## ③ GAAP EPS — 원인이 확정됐다: **단위 글자 비교**였다

91차 계기: 158종목 전부 `받음 N · 버림 N · 모호 0 · **남음 0**`.
92차 계기: **남음 합계 7,306** · 남음 0인 종목 **0/158**.

| | 91차 | 92차 |
|---|---|---|
| `gaap_eps_xbrl` 채워진 행 | **0** / 3,066 | **1,766** / 3,066 |
| `gaap_eps` 채워진 행 | 2,629 | 2,891 |

92차는 두 곳을 고쳤다 — ㉠ 모호 판정(주당은 절대차 1센트) ㉡ 단위 글자
비교(낱말 비교). 91차 계기가 `모호 0` 이었으므로 ㉠ 규칙에는 **애초에
닿지도 못했다.** 따라서 3,066행을 전부 비우던 범인은 **㉡ 단위 글자
비교**로 확정된다. `USD/shares` · `USD-per-shares` · `usd/share` 처럼
표기가 갈리는데 한 글자만 어긋나도 전부 버리고 있었다.

⚠️ 93차에 단 세분화 계기(`이름버림`/`단위버림`)는 이제 **원인 규명용으로는
필요 없다**. 다음에 같은 일이 또 생겼을 때를 위한 계기로 남긴다.

## ④ 남은 매출 오차 16칸 — 두 무리다

| 무리 | 예 | 성격 |
|---|---|---|
| **은행 (7칸)** | JPM 135.9B vs 52.9B · BAC 105,856 · C 28 | 은행은 "매출"의 정의가 갈린다(총수익 vs 순수익). BAC·C 의 105,856·28·8 은 보도자료 파싱 쓰레기 — XBRL 이 없어 떨어진 칸 |
| **유틸리티·기타 (9칸)** | SO 6,795 vs 6,981 (0.97) · NEE 6,400 vs 6,700 (0.96) | 3~9% 차이. 야후와 회사의 매출 정의 차이로 보인다 |

**다음 표적으로 삼되 급하지 않다.** 647칸 중 16칸(2.5%)이고, 은행·유틸리티는
이 모델의 표적(이익 신기록 종목)이 아니다.

## ⑤ 아직 모르는 것

- GAAP EPS 78.3% 는 여전히 낮다. 남은 틀림 135칸 중 UNH 1.78 vs 0.01(178배),
  VRTX 85.0 vs 4.31 처럼 **우리가 명백히 틀린 것**과, NFLX 5.87 vs 0.587,
  FSLR 3.92 vs 0.392 처럼 **정확히 10배**인 것이 섞여 있다.
  10배가 반복되는 것은 **심판(야후) 쪽 결함**일 수 있다 — 헌법 6조대로
  신호보다 장치를 먼저 의심해야 한다. 아직 안 팠다.

---

# 96차 — 10년 표본의 첫 판정: **채택돼 있던 가설 2개가 무너졌다**

**날짜**: 2026-08-18 · **실행**: 32098707258 · **`head_sha` 36c39d1 = 94차** ✅
· 소요 **133분** (한도 180분 안) · **시간초과 종목 0개**

`head_sha` 를 먼저 확인했다 (93차 교훈). 94차 코드가 실제로 돌았다.

## ① 표본이 실제로 두 배가 됐다

| | 전 (5년) | 후 (10년) |
|---|---|---|
| 실적 행 | 3,066 | **5,851** |
| 종목당 분기 (중앙값/최대) | 20 / 24 | **39 / 44** |
| 발표일 범위 | 2021-09-23 ~ 2026-08-13 | **2016-10-19** ~ 2026-08-13 |
| 일봉 행 | 200,678 | **383,857** |
| 가장 이른 주가 | 2021-08-18 | **2016-08-18** |

시간 예산은 **한 번도 안 걸렸다**(시간초과 0). 133분이라 바닥 장치도
작동할 일이 없었다. 안전장치는 그대로 둔다 — 종목이 늘거나 캐시가
비는 날을 위한 것이다.

## ② 판정이 여섯 개 바뀌었다 — 그중 둘은 **채택 → 미채택**

| 가설 | 전 (5년) | 후 (10년) |
|---|---|---|
| **H5b 실적폭 중앙값** | **채택** | **미채택** |
| **H10 논갭영업이익 저평가 첫신기록** | **채택** | **미채택** |
| H7 EBITDA 첫돌파 | 판정 불가 (n=4) | 미채택 (n=13) |
| H11b 섹터정배열폭 80 | 판정 불가 | 미채택 |
| H21 주도섹터 분기점 | 판정 불가 (n=5) | 미채택 (n=12) |
| H20 주도섹터 전환 | 미채택 (n=13) | 미채택 (**n=32**) |

**채택 상태로 남은 가설이 하나도 없다.**

### H5b — 신호가 기준선보다 **못했다**

| | 신호 n | 신호 폭등률 | 기준선 n | 기준선 폭등률 |
|---|---|---|---|---|
| 5년 | 210 | **22.9%** [17.7, 29.0] | 807 | 15.0% [12.7, 17.6] |
| 10년 | 323 | **7.4%** [5.0, 10.8] | 2,146 | 13.2% [11.8, 14.7] |

신호가 기준선의 **절반**이다. 우위가 사라진 정도가 아니라 **뒤집혔다.**

### H10 — 우위가 그냥 없어졌다

| | 신호 n | 신호 폭등률 | 기준선 n | 기준선 폭등률 |
|---|---|---|---|---|
| 5년 | 50 | **28.0%** [17.5, 41.7] | 1,107 | 12.8% [11.0, 14.9] |
| 10년 | 54 | **11.1%** [5.2, 22.2] | 2,481 | 11.2% [10.0, 12.5] |

신호 11.1% 대 기준선 11.2%. **똑같다.**

## ③ 장치를 먼저 의심했다 (헌법 6조) — 셋 다 깨끗했다

결과가 이렇게 크게 뒤집히면 신호가 아니라 장치를 먼저 봐야 한다.
세 가지를 확인했다.

**(가) H5b 의 `신규_뒤시기 n=0` 이 수상했다.** 앞시기 323, 뒤시기 0.
확인해 보니 버그가 아니었다 — 시간 분할 경계는 **2022-10-28**(판정 표본
2,146개의 개수 중앙값)이고, H5b 신호가 실제로 뜬 해는
**2018년 16 · 2020년 4 · 2021년 226 · 2022년 77** 로 전부 경계 앞이다.
**2022년 10월 이후로는 한 번도 안 떴다.**

**(나) "신규" 가 등록일 기준인 줄 알았는데 아니었다.** 코드를 보니
`신규` = **발견 29종목을 뺀 것**이지 등록일 뒤가 아니다. 그래서 옛
데이터를 넣으면 신규 표본이 늘어나는 게 맞다 (내 첫 해석이 틀렸다).

**(다) 중앙값 문턱이 미래를 훔쳐보는지 확인했다.** `measure_engine.py:281`
이 `median(history)` 로, `history` 는 **그 시점 직전까지**다. 확장
중앙값이라 실시간 계산이 가능하고 미래를 안 본다. **깨끗하다.**

## ④ 이것이 바로 헌법 4조가 경고한 것이다

> **장세 전제.** 장세가 종목 선택을 지배합니다. 어떤 측정이든 앞/뒤
> **시간 분할**을 함께 보고하고, **강세장 구간만의 우위는 믿지 않습니다.**

5년 표본(2021-09~)은 2022 약세장 하나를 겨우 담았다. 그 위에서
"채택"이 두 개 나왔다. 2016~2018 금리 인상기와 2020 코로나를 넣자
**둘 다 사라졌다.**

이 확장의 목적이 바로 이것이었고 (94차 ⑥에 "약해질 수도 있다. 그것이
이 확장의 목적"이라고 미리 적어 두었다), **예상대로 됐다.**

**손해가 아니라 이득이다.** 없는 우위를 믿고 돈을 걸기 전에 알았다.

## ⑤ 아직 모르는 것 (짐작으로 메우지 않는다)

- **H5b 신호가 왜 2022-10 이후 한 번도 안 뜨는가.** 확장 중앙값이라
  이력이 길어질수록 문턱이 높아지는데, 2021년의 매우 넓은 실적 신기록
  폭이 문턱을 끌어올려 이후가 못 넘는 것으로 **보인다**. 아직 확인 안 했다.
  확인하면 "H5b 는 국면 초기에만 뜨는 신호"라는 해석이 가능하다.
- **5년일 때 22.9% 였던 신호와 10년일 때 7.4% 인 신호는 같은 사건 집합이
  아니다** (n 210 vs 323, 해가 다름). 문턱이 이력에 따라 움직이므로 당연하다.
  어느 사건이 들고 났는지는 아직 안 셌다.
- 10년 데이터에서 **옛 보도자료의 파싱 품질**은 아직 안 쟀다. 야후 심판
  대조를 다시 돌려야 한다 (심판 자료도 10년치가 있는지부터 확인 필요).

---

# 97차 — 심판이 옛 데이터의 84%를 못 본다 (그리고 그래도 96차는 선다)

**날짜**: 2026-08-18 · 96차 자기 점검 ①의 이행

## ① 발견 — 심판의 시야가 우리 데이터의 6분의 1이다

96차에서 "옛 보도자료의 파싱 품질을 아직 안 쟀다"고 적어 뒀다. 재려고
심판(야후) 자료부터 열었더니:

| | 범위 | 행 |
|---|---|---|
| **우리 데이터** | 2016-10-19 ~ 2026-08-13 | **5,851** |
| **심판 자료** | **2024-11-30** ~ 2026-06-30 | **962** |

심판은 **최근 1.7년치**밖에 없다. 10년 확장으로 새로 들어온
**2016~2023년은 심판이 한 칸도 안 본다.**

이것이 왜 심각한가: 96차의 결론("채택 가설 2개가 무너졌다")은
**84%가 검증 안 된 데이터 위에서 나온 판정**이다. 그리고 그럴듯한
**대안 설명**이 하나 있다 —

> "우위가 강세장 전용이었다"가 아니라 **"옛 데이터가 더 더러워서
> 신호가 엉뚱한 사건에 떴다"** 일 수도 있다.

옛 데이터에 오류가 많으면 "첫 신고점" 판별이 흔들려 신호가 가짜 사건에
뜨고, 그러면 적중률이 떨어진다 — **관측된 것과 똑같은 모양**이다.
헌법 6조(신호보다 장치를 먼저 의심)에 따라 이걸 먼저 갈라야 한다.

## ② 심판이 없으면 **서로 다른 두 눈**으로 본다

심판을 못 쓰니 내부에서 독립된 두 경로를 맞대었다.

- `gaap_eps` = **보도자료** 평문을 정규식으로 읽은 값
- `gaap_eps_xbrl` = **SEC XBRL** 구조화 태그에서 읽은 값

읽는 방법도, 원본 형식도, 코드 경로도 전혀 다르다. 둘이 얼마나 자주
일치하는지를 **심판이 보는 구간과 안 보는 구간으로 갈라** 셌다
(허용 오차 1.5센트).

| 구간 | 둘 다 있음 | 일치 | 일치율 |
|---|---|---|---|
| **심판 없는 옛 구간** (~2024-11) | 2,602 | 2,158 | **82.9%** |
| **심판 있는 최근** (2024-11~) | 639 | 530 | **82.9%** |

**소수점 첫째 자리까지 같다.** 옛 구간이 최근 구간보다 더 더럽다는
증거가 없다.

## ③ 커버리지도 10년 내내 평평하다

"읽어낸 비율"이 옛날에 떨어지면 그것도 오염 신호다. 연도별로 셌다.

| 연도 | 행 | 조정 EPS | GAAP EPS | 매출 |
|---|---|---|---|---|
| 2016 | 34 | 55.9% | 97.1% | 97.1% |
| 2017 | 221 | 50.7% | 97.3% | 99.5% |
| 2018 | 382 | 61.3% | 97.4% | 99.2% |
| 2019 | 485 | 62.5% | 97.9% | 99.2% |
| 2020 | 562 | 61.4% | 97.3% | 99.3% |
| 2021 | 596 | 60.7% | 95.5% | 99.2% |
| 2022 | 604 | 61.4% | 96.0% | 99.2% |
| 2023 | 626 | 62.6% | 95.7% | 98.4% |
| 2024 | 641 | 62.7% | 93.4% | 97.7% |
| 2025 | 634 | 66.9% | 95.4% | 97.9% |
| 2026 | 438 | 68.5% | 95.9% | 97.7% |

GAAP EPS 93~98% · 매출 97.7~99.5% — **옛날이 오히려 조금 높다**
(최근 종목이 늘며 어려운 회사가 섞인 탓으로 보인다).

## ④ 그래서 96차는 선다 — 다만 조건을 붙여 둔다

두 독립 경로의 일치율이 같고, 커버리지도 평평하다. **옛 데이터가
체계적으로 더 더럽다는 증거는 못 찾았다.** 따라서 96차의 결론
(강세장 전용 우위였다)이 더 그럴듯하다.

**⚠️ 하지만 이것은 증명이 아니다.** 두 눈이 같이 틀릴 수 있고, 심판은
여전히 84%를 못 본다. 96차를 인용할 때는 **"심판이 검증한 것은 최근
16%뿐"** 을 반드시 함께 적을 것.

## ⑤ 하나 진짜로 기운 것 — 조정 EPS 커버리지

| | 조정 EPS 채움 |
|---|---|
| 2016~2017 | 50.7~55.9% |
| 2025~2026 | 66.9~68.5% |

옛날이 **15%p 낮다.** 조정 EPS 는 모델의 **1순위 잣대**이므로, 옛
구간에서는 사다리가 더 자주 아래 칸(EBITDA·GAAP EPS)으로 내려간다는
뜻이다. 잣대가 구간마다 다르면 비교가 흔들린다.

원인 후보: 2016~2017 은 논갭 공시 관행이 지금만큼 표준화되지 않았다
(회사가 아예 안 준 것). 이건 파서 결함이 아니라 **원본에 없는 것**일
수 있고, 그렇다면 "없음"이 정답이다. **아직 안 갈랐다** — 옛 원문을
실제로 열어 봐야 한다.

## ⑥ 다음

가장 값어치 있는 일은 **두 번째 심판**을 구하는 것이다. 지금 심판은
① 하나뿐이고 ② 최근 1.7년만 보고 ③ 그 자신이 틀린 정황(NFLX·FSLR
정확히 10배)이 있다. 심판이 하나면 우리가 틀린 건지 심판이 틀린 건지
영영 못 가른다.

---

# 98차 — 97차 ⑤의 원인을 캐다 **내 진단 방법이 틀린 것**을 찾았다

**날짜**: 2026-08-18 · 주인 지시: "도움이 될만하다 판단되면 승인없이 진행해"

97차 ⑤에서 남긴 숙제 — 옛 구간의 조정 EPS 커버리지가 15%p 낮은데,
**파서 결함이냐 원본에 없는 것이냐**. 이걸 가르려다 정작 내 자를
못 믿게 됐다.

## ① 내가 쓴 자가 틀렸다 (공개 정정)

`source == '직접공시'` 를 "보도자료가 붙은 행"으로 읽고 연도별 표를
만들었다. **그 해석이 틀렸다.**

`source` 는 **논갭 영업이익(op_income)의 출처**만 적는 칸이다
(`config.py:879`). 보도자료가 붙었어도 그 안에 논갭 영업이익이 없으면
XBRL 값인 `'근사치'` 로 남는다. 실물로 확인했다:

| 종목 | 조정 EPS 읽은 건수 | 전 행의 source |
|---|---|---|
| FN | **39** | 전부 `근사치` |
| QCOM | **35** | 전부 `근사치` |
| TER | **33** | 전부 `근사치` |

FN 의 최근 행은 `adj_eps 3.72` · `gaap_eps 3.45` 다. 회사가 발표한
논갭 숫자를 **보도자료에서 읽어 온 것**인데 `근사치` 로 적혀 있다.
코드에서도 확인했다 (`_apply_press_to_row`) — 조정 EPS 는
*"op_income 성공 여부와 무관하게 따로 옮겨 둡니다"* 라고 주석까지
붙어 있다.

**따라서 97차에서 제가 만든 "보도자료 붙음 32.4% → 48.4%" 표는
보도자료 부착률이 아니라 "논갭 영업이익 직접 추출률"이다.**
그 표로 내린 진단은 무효다.

⚠️ 다만 97차 ⑤의 **발견 자체**(조정 EPS 커버리지 2016~17 50.7~55.9%
vs 2025~26 66.9~68.5%)는 `adj_eps` 를 직접 센 것이라 **그대로 유효하다.**
무효가 된 것은 그 원인에 대한 내 진단뿐이다.

## ② 곁가지로 하나 확인된 것

무효인 표에서도 한 줄은 살아 있다 — 논갭 영업이익을 읽어 낸 분기
**안에서** 조정 EPS 를 읽은 비율:

| 연도 | 2016 | 2017 | 2018 | 2020 | 2023 | 2025 | 2026 |
|---|---|---|---|---|---|---|---|
| 그중 조정 EPS 읽음 | 100% | 84.7% | 87.8% | 86.7% | 85.2% | 84.6% | 86.8% |

**10년 내내 평평하다.** 보도자료를 손에 넣기만 하면 옛것이든 새것이든
같은 비율로 읽는다. **파서가 옛 문서에 약하다는 증거는 없다.**

## ③ 진짜 문제 — 그걸 잴 **표시가 아예 없었다**

"조정 EPS 가 비었다"의 원인은 둘인데 대응이 정반대다.

| 원인 | 대응 |
|---|---|
| ㉠ 보도자료를 못 붙였다 | **수집 문제 — 고칠 수 있다** |
| ㉡ 붙었는데 값이 없었다 | **회사가 안 준 것 — "없음"이 정답** |

그런데 이 둘을 가르는 표시가 데이터에 **한 칸도 없다.** `source` 는
위에서 봤듯 다른 것을 적고, 나머지 칸은 값만 있다. 그래서 지금까지
**짐작밖에 할 수 없었다** — 내가 틀린 진단을 낸 것도 이 때문이다.

그래서 계기를 달았다: **`press_matched`**

- XBRL 뼈대 행을 만들 때 `False`
- 보도자료가 붙는 순간 `_apply_press_to_row` 가 `True`
- `measure_store.EPS_FIELDS` 에 넣어 **스냅샷까지** 간다

"없음"이 아니라 **False** 로 두는 이유: 칸 자체가 없으면 옛 데이터와
섞여 조용히 뭉개진다.

## ④ 시험 — **내가 저지른 실수를 그대로 돌연변이로 넣었다**

| 돌연변이 | 빨간 불 |
|---|---|
| 표시를 아예 안 남긴다 | 💥 `KeyError: 'press_matched'` |
| **97차의 내 실수 그대로 — `source` 로 대신한다** | ❌ `보도자료가 붙었는데 표시가 안 남았다` |
| 스냅샷에 안 담는다 (69차 사고 재현) | ❌ `보도자료_표시가_스냅샷까지_간다` |

두 번째가 핵심이다. 시험이 **논갭 영업이익이 없는 보도자료**를 일부러
물리므로, 누가 다시 `source` 로 이걸 재려 하면 그 자리에서 걸린다.
시험 안에 "이 경우가 바로 내가 틀렸던 자리"라고 적어 두었다.

시험: `python3 run_tests.py` 종료코드 **0** · **433개 통과** (431 → 433).

## ⑤ 다음 실행이면 짐작 없이 확정된다

`press_matched` 가 담긴 스냅샷이 오면 연도별로 두 줄만 세면 된다.

- 보도자료 붙은 비율이 옛날에 낮다 → **수집 문제** (8-K 훑기를 더 파야 함)
- 붙었는데 조정 EPS 만 없다 → **회사가 안 준 것** ("없음"이 정답, 고칠 것 없음)

②의 평평한 86% 로 미루어 **후자 쪽으로 기울지만, 짐작으로 적지 않는다.**

---

# 99차 — GAAP EPS 도 뒤집는다 (77.7% → 91.1%). 그리고 로봇이 429 로 멈춘 건

**날짜**: 2026-08-18

## ① 새 심판을 구하기 전에, **이미 가진 두 값으로 승부가 났다**

97차에서 "두 번째 심판이 제일 값어치 있다"고 적었다. 그런데 심판을
찾으러 가기 전에 할 수 있는 일이 남아 있었다 — 92차에 매출로 한 승부를
**GAAP EPS 로 그대로 한 번 더** 돌리는 것이다. 두 값(`gaap_eps` 보도자료 ·
`gaap_eps_xbrl` XBRL)이 90차부터 나란히 쌓여 있었다.

**갈린 칸에서: XBRL 61 : 보도자료 2** (둘 다 맞음 0 · 둘 다 틀림 2)

매출 때의 98:0 과 같은 방향이다. 전체 정확도로도 확인했다.

| 설계 | 심판 대비 | 커버 |
|---|---|---|
| 지금 (보도자료 우선) | 341/439 = **77.7%** | 439 |
| **XBRL 우선, 없으면 보도자료** | 400/439 = **91.1%** | 439 |
| XBRL 만 (참고) | 334/346 = 96.5% | **346** (93칸 잃음) |

**+13.4%p.** "XBRL 만"이 더 정확해 보이지만 93칸을 통째로 잃는다.
5,851행 중 **XBRL 만 있고 보도자료가 없는 칸은 0개**라, 채택한 설계는
커버리지를 **한 칸도 안 줄인다.**

## ② 잃는 것을 적어 둔다 — 2칸

| 종목 | 보도자료 | XBRL | 심판 |
|---|---|---|---|
| ETSY 2026-06-30 | **−0.49** ✅ | −0.36 | −0.49 |
| BE 2025-10-28 | **−0.10** ✅ | **−100.0** | −0.10 |

BE 의 −100.0 은 **우리 XBRL 읽기의 명백한 쓰레기값**이다. 1000배다.

92차 단위 규칙 완화의 부작용인지 의심해 전수로 셌다 —
**|EPS| ≥ 50 인 행은 3,241행 중 이 1건뿐이다.** 계통적 결함이 아니다.

그래서 **n=1 로 새 문턱 규칙을 만들지 않는다.** 근거 없는 가드를 넣었다가
되돌린 89차 전망 가드의 실수를 되풀이하지 않는다. **남은 결함으로
기록만 하고 넘어간다.**

## ③ 시험

`test_xbrl_value_is_kept_beside_the_press_value` 가 *"GAAP EPS 는 아직
승부가 안 났습니다"* 를 못박고 있어 빨간 불이 났다. 실측이 나온 뒤라
시험 쪽이 낡은 것이므로 고쳤다 (**실물이 규칙을 이긴다** — 92차와 같은 처리).
매출총이익률은 **여전히 보도자료**임을 같은 시험에 함께 못박았다
(승부가 아니라 정의 차이라 안 뒤집는다).

돌연변이: 보도자료 우선으로 되돌리자 ❌ `GAAP EPS 도 XBRL 이 이겨야
합니다 (99차)`.

시험: 종료코드 **0** · **433개 통과**.

## ④ 로봇이 실패했다 — 코드가 아니라 **내 운용 실수**다

내가 띄운 수집 런(32122463006, `head_sha d8b12e1`)이 종료코드 1 로 끝났다.
로그를 읽었다.

```
[159/160] OXY: ⚠️ [get_facts] TooManyRequestsError:
SEC Rate Limit Exceeded (HTTP 429)
Estimated Wait: ~10 minutes
⛔ 절반 이상 실패 — 오늘 데이터로 덮어쓰지 않습니다 (종료코드 1)
```

**결함이 아니다. 안전장치가 설계대로 작동한 것이다.** 반쯤 깨진 수집으로
좋은 데이터를 덮어쓰지 않고 멈췄다.

원인은 내 쪽이다. 앞 런이 06:32 에 끝났는데 09:20 에 또 띄웠고, 매일
09:30 예약 런과도 붙었다. `concurrency` 가 줄을 세우므로 충돌은 안 나지만
**SEC 입장에서는 무거운 요청이 계속 이어진 것**이다.

**교훈: 수집 런을 연달아 띄우지 않는다.** 특히 09:30 예약 런 앞뒤로는
띄우지 않는다. 데이터는 하루 한 번이면 충분하고, 급하다고 두 번 띄우면
**둘 다 잃는다.**

⚠️ 그래서 98차의 계기(`press_matched`)와 99차의 뒤집기는 **아직 실물로
안 돌았다.** 다음 예약 런(내일 09:30)이나 충분히 시간을 둔 수동 런에서
확인해야 한다. 짐작으로 "됐다"고 적지 않는다.

---

# 100차 — 96차의 결론이 **깨끗한 증거가 아니다** (게이지 앞머리 허상)

**날짜**: 2026-08-18 · 96차 자기 점검 ②의 이행

## ① 질문: H5b 신호가 왜 2022-10 이후 한 번도 안 뜨나

확장 중앙값 문턱을 연도별로 실제로 세었다.

| 연도 | 게이지 중앙값 | 문턱(확장 중앙값) | 넘긴 주 |
|---|---|---|---|
| 2018 | **85.7%** | 85.7% | 1/11 |
| 2019 | 53.9% | 81.5% | 0/52 |
| 2020 | 49.1% | 61.1% | 5/53 |
| 2021 | 63.0% | 58.8% | **41/52** |
| 2022 | 55.3% | 60.3% | 15/52 |
| 2023 | 38.5% | 57.8% | **0/52** |
| 2024 | 35.4% | 54.4% | **0/52** |
| 2025 | 36.6% | 49.8% | **0/52** |
| 2026 | 40.0% | 48.3% | 1/33 |

게이지 자체가 **85.7% → 35~40%** 로 구조적으로 내려앉았고, 문턱은
앞머리의 85.7% 에 끌려 48~58% 에 머문다. 그래서 못 넘는다.

## ② 그 85.7% 는 시장이 아니었다

분모를 셌다.

| 연도 | 게이지를 계산한 **종목 수** 평균 | 종목당 누적 분기 |
|---|---|---|
| 2017 | **2.4** | **5.0** |
| 2018 | **21.0** | 6.6 |
| 2019 | 43.8 | 8.0 |
| 2026 | 88.1 | 27.0 |

**2017년의 "시장 전체 실적 신기록 폭 85.7%" 는 평균 2.4개 종목으로 잰
값이다.** 시장의 폭이 아니라 두세 종목의 형편이다.

## ③ 더 깊은 원인 — **이력이 짧으면 신기록이 쉽다**

`decidable` 은 **직전 정점 하나만** 있으면 참이다
(`current is not None and had_prior`). 즉 5분기짜리 종목도 "신고점"을 낸다.
그 편향을 전수로 쟀다 — 그 종목의 **몇 번째** 판단 가능한 발표인가별
신고점 비율:

| 몇 번째 발표 | 1 | 3 | 5 | 8 | 12 | 20 | 25 |
|---|---|---|---|---|---|---|---|
| 신고점 비율 | **63.8%** | 57.1% | 53.9% | 51.5% | 42.2% | 42.9% | 36.1% |

| 묶어서 | 건수 | 신고점 |
|---|---|---|
| 1~5번째 | 548 | **56.8%** |
| 12번째 이후 | 1,072 | **40.4%** |

**단조 감소한다.** 이력이 짧으면 직전 최고점을 넘기가 쉽다 — 시장 사실이
아니라 **수학적 허상**이고, 어떤 데이터 창을 잡든 그 앞머리에 반드시 생긴다.

## ④ 그래서 96차의 결론을 **격하한다**

96차는 이렇게 적었다 — *"채택돼 있던 가설 2개가 무너졌다. 강세장 전용
우위였다."* 지금은 그렇게 단정할 수 없다.

H5b 의 문턱은 **표본 앞머리의 허상에 오염돼 있다.** 5년 표본과 10년
표본은 앞머리 위치가 달라 문턱 자체가 다른 자다. 따라서

> **H5b 가 무너진 것이 "우위가 강세장 전용이어서"인지 "자가 바뀌어서"인지
> 지금 데이터로는 못 가른다.**

⚠️ H10 은 게이지를 안 쓰므로(논갭 영업이익 + 52주선) **이 오염과 무관하다.**
H10 이 무너진 것은 그대로 유효하다.

## ⑤ 고쳤지만 — **거의 아무것도 안 바뀌었다** (정직하게 적는다)

분모 최소치를 걸었다 (`GAUGE_MIN_TICKERS = 10`). 새 숫자를 지어내지 않고
**이미 사전 등록된 최소 표본 n≥10** 을 그대로 썼다 (결과를 보고 고른
문턱이 아니다 — 그건 헌법 2조가 막는 사후 맞추기다).

효과를 쟀다.

| | 전 | 후 |
|---|---|---|
| H5b 신호 n | 323 (7.4%) | 310 (**7.1%**) |
| H5b 기준선 | 2,146 (13.2%) | 2,114 (13.2%) |
| 판정 | 미채택 | 미채택 |

**판정은 하나도 안 바뀌었다.** 2018년은 분모가 21개라 최소치를 통과하는데,
그해 게이지가 이미 85.7% 이기 때문이다. 즉 **내가 건 가드는 옳지만 이
문제의 급소가 아니었다.** 급소는 ③의 이력 깊이다.

가드는 그대로 둔다 — 분모 2.4개로 비율을 내는 것은 그 자체로 틀렸다.
다만 **"이걸로 고쳐졌다"고 적지 않는다.**

섹터 게이지는 최소치에서 뺐다 (`min_tickers=1`). 섹터는 원래 1~3종목이고
화면에 종목수를 함께 보여 주며, 판정에 안 쓰이는 관찰값이다. 최소치를
걸면 관찰 자체가 사라진다. 돌연변이 2건으로 양쪽을 못박았다.

## ⑥ 급소는 **일부러 안 고쳤다**

③의 이력 깊이 편향을 없애려면 `decidable` 을 바꿔야 하는데, 그러면

- H2·H2b·H6·H8·H9 등 **모든 가설의 사건 목록이 통째로 바뀐다**
- 그리고 **깊이 문턱을 결과를 보고 고르게 된다** — 사후 맞추기다

그래서 지금 손대지 않는다. **새 H번호로 사전 등록할 항목**으로 남긴다:

> *(등록 예정) 게이지의 분자·분모를 "판단 가능한 발표"가 아니라
> "TTM 정점 이력이 N개 이상인 발표"로 제한하면 앞머리 허상이 사라지는가.
> N 은 데이터를 보기 전에 정한다 — 후보는 이미 등록된 `LADDER_MIN_QUARTERS = 8`.*

탐색 규율대로 **이 표본으로는 판정하지 않고 새 데이터로만** 잰다.

시험: `python3 run_tests.py` 종료코드 **0** · **435개 통과** (433 → 435).

---

# 101차 — 계기판이 **틀린 숫자를 사실처럼 말하고 있었다** (412px 실물 확인)

**날짜**: 2026-08-18 · 96차 자기 점검 ⑤의 이행

판정이 전부 미채택으로 바뀌었으니 화면이 그 상태를 정직하게 말하는지
**실제로 412px 에서 띄워 봤다** (Chromium · 폭 412 · 배율 2).

## ① 좋았던 것

- **가로 스크롤 없음** (`scrollWidth 412 = innerWidth 412`) — 글자 잘림·열 가림 없음
- **"채택된 신호 없음 — 어떤 상태도 매수 판단의 근거가 아닙니다"** 가 제대로 뜬다.
  채택 종목 목록은 `채택` 분기 안에만 있어 지금은 아예 안 그려진다 ✅
- **"이 판정은 옛 코드로 계산됐습니다 (판정 36c39d1 · 지금 코드 ba08f51)"**
  경고가 정확히 작동 — 52차에 넣은 장치가 제 일을 하고 있다 ✅

## ② 나빴던 것 — **박힌 숫자가 거짓말이 돼 있었다**

10년 확장으로 데이터가 바뀌었는데 화면 문구는 그대로였다.

| 자리 | 화면이 하던 말 | 실제(10년) |
|---|---|---|
| 머리말 | 2016-08-18 ~ 2026-08-14 · **약 5년** | 약 **10년** |
| 정배열 폭 판정줄 | H11 실측 **9.4%** vs 기준선 **26.8%** — *"정배열이 다 찬 뒤 사는 것은 **오히려 불리**했습니다"* | **31.6% vs 31.6%** (n=187) — **불리하지 않다. 똑같다** |
| 가설 설명문 | *"**오히려 나빴습니다**(9.4% vs 기준선 26.8%)"* | 같은 이유로 거짓 |
| 폭 지도 기준선 | 기준선 **26.8%** (34차, n=1,872) | **31.6%** (n=5,004) |

날짜는 2016년부터라고 적어 놓고 바로 옆에 "약 5년"이라고 쓰여 있었다.
**한 줄 안에서 자기모순**이었는데 아무 경고도 없었다 — 글자로 박혀 있었기
때문이다.

## ③ 고친 방식 — 숫자를 **판정 파일에서 그때그때 읽는다**

박힌 글자는 데이터가 바뀌어도 아무도 모르게 남는다. 그래서 전부 읽어
오게 바꿨다.

- `breadth_verdict_lines(verdict)` — H11·H11b 판정줄을 판정 파일에서 만든다
- `breadth_baseline(verdict)` — 기준선을 판정 파일에서 읽고, **없을 때만**
  문서화된 예비값(26.8, 34차)을 쓴다. 지어내지 않고 출처를 남긴다
- 머리말의 연수는 **날짜에서 계산**한다 (`(끝−시작)/365.25`)
- 가설 설명문에서는 **숫자를 아예 뺐다.** 설명은 "무엇을 묻는가"만 적고,
  실측값은 바로 옆 판정줄이 보여 준다

## ④ 시험 — 돌연변이 3건 전부 빨간 불

| 돌연변이 | 빨간 불 |
|---|---|
| 판정줄 숫자를 다시 글자로 박는다 | ❌ `정배열폭_판정줄은_판정파일을_읽는다` |
| 기준선을 다시 상수로 되돌린다 | ❌ `기준선은_판정파일을_읽고_없으면_예비값을_쓴다` |
| 설명문에 옛 숫자를 다시 박는다 | ❌ `가설_설명문에는_숫자를_박지_않는다` (9.4% 검출) |

여기에 96차 상태를 지키는 시험을 하나 더 뒀다 —
`채택이_없으면_매수근거가_아니라고_말한다`. 채택이 다시 생기면 화면
문구를 반드시 다시 보게 만드는 장치다.

시험: `python3 run_tests.py` 종료코드 **0** · **439개 통과** (435 → 439).

실물 재확인 (412px, 고친 뒤):

```
가로 스크롤: 없음 ✅
'약 10년': 있음   ·  '약 5년': 없음
'채택된 신호 없음': 있음
'31.6%': 있음  ·  '9.4%': 없음  ·  '26.8%': 없음
```

## ⑤ 새로 생긴 문제 — **화면이 느려졌다 (57초)**

10년 확장의 대가다. 마지막 구역까지 그려지는 데 **57~59초** 걸린다
(412px, 두 번 재서 둘 다). 데이터가 두 배가 됐으니 이상한 일은 아니지만,
**휴대폰으로만 쓰는 주인에게 1분은 너무 길다.**

아직 안 고쳤고 원인도 정확히 안 짚었다 — 어느 구역이 오래 거는지부터
재야 한다 (화면은 위에서부터 그려지므로 멈추는 자리로 찾을 수 있다).
**짐작으로 캐시를 붙이지 않는다.** 다음 항목으로 남긴다.

---

# 102차 — 화면 57초 → **3초** (짐작하지 않고 하나씩 재서 찾았다)

**날짜**: 2026-08-18 · 101차 ⑤에서 남긴 항목

101차에 *"짐작으로 캐시를 붙이지 않는다. 어느 구역이 오래 거는지부터
재야 한다"* 고 적었다. 그대로 했다.

## ① 하나씩 재 봤다

| 계산 | 걸린 시간 |
|---|---|
| **`sm.confirmation_rows`** | **22.7초** ← 1위 (전체의 40%) |
| `sm.current_breadth` | 5.1초 |
| `sm.cycle_series` ×2 | 5.6초 |
| `dataset.build` | 1.4초 |
| `snapshot.json` 읽기 | 0.4초 |
| 시장 게이지 · 섹터 게이지 12회 | 0.2초 |
| **합계** | **약 35초** + 스트림릿 그리기 |

⚠️ **가장 무거운 것은 범인이 아니었다.** `leadership.stability_report` 가
**80초**로 제일 무거웠지만 **화면은 그걸 안 부른다** — 로봇이 미리 계산해
`verdict.json` 에 넣어 두고 화면은 읽기만 한다. 제일 큰 것을 보고 달려들었으면
엉뚱한 데를 고쳤을 것이다. 100차에서 이미 한 번 그런 실수를 했다
(분모 가드가 옳았지만 급소가 아니었다).

## ② 고친 방식 — 값은 안 건드리고, 다시 계산만 안 한다

이 값들은 **로봇이 새 데이터를 커밋할 때만** 바뀐다. 같은 스냅샷을 화면
새로 고칠 때마다 다시 계산할 이유가 없다.

- `cached_dataset` · `cached_confirmation_rows` · `cached_current_breadth`
  · `cached_cycle_series` 로 감쌌다
- 캐시 열쇠는 **스냅샷의 `saved_at`** (`snapshot_key`). 로봇이 새로
  커밋하면 저절로 갈린다. `saved_at` 이 없으면 종목 수라도 쓴다 — 지어내지 않는다
- 큰 표(`ds`)는 **밑줄로 시작하는 인자**로 넘겨 해시에서 뺐다. 통째로
  해시하면 그 자체가 몇 초씩 걸린다
- 시험은 스트림릿 없이 `app` 을 불러오므로, 스트림릿이 없으면 캐시를
  안 씌우고 그대로 도는 길을 뒀다

**측정 코드는 한 줄도 안 건드렸다.** 계산 결과를 다시 쓰기만 한다.

## ③ 실측 — 412px 에서 두 번 재기

```
101차 (캐시 없음)          57초 · 59초
102차 첫 접속 (캐시 빈 상태)  42초
102차 두 번째 접속          3초   ← 실제로 쓸 때의 시간
```

첫 접속이 42초인 것은 그때 계산해서 담아 두기 때문이다. 그 뒤로는
**3초**다. 로봇이 하루 한 번 커밋하므로, 주인이 하루 중 두 번째로
여는 순간부터 계속 3초다.

화면 내용도 함께 확인했다 (101차에서 고친 것들이 그대로 살아 있다):
`약 10년` 있음 · `채택된 신호 없음` 있음 · `31.6%` 있음 ·
`9.4%` 없음 · `26.8%` 없음 · 가로 스크롤 없음.

## ④ 시험 — 캐시가 값을 바꾸면 그건 결함이다

캐시는 **속도만** 바꿔야 한다. 그것을 못박았다.

| 시험 | 무엇을 지키나 |
|---|---|
| `캐시는_값을_바꾸지_않는다` | 캐시 결과 == 원본 함수 결과 (실데이터로) |
| `스냅샷_이름표는_새_수집이면_달라진다` | 열쇠가 안 바뀌면 **새 데이터가 와도 옛 화면**이 남는다 |

돌연변이: 캐시 열쇠를 `"고정"` 으로 바꾸자 ❌ `수집 시각이 다른데
이름표가 같습니다 — 새 데이터가 안 보입니다`.

시험: `python3 run_tests.py` 종료코드 **0** · **441개 통과** (439 → 441).

## ⑤ 남는 것

- **첫 접속 42초**는 그대로다. 줄이려면 `confirmation_rows` 자체를
  빠르게 하거나 로봇이 미리 계산해 두어야 한다 (`stability_report` 처럼).
  로봇이 미리 계산하는 쪽이 구조적으로 맞아 보이지만 **아직 안 했다.**
- `confirmation_rows` 가 왜 22.7초인지는 **안 팠다.** 캐시로 가려졌을 뿐
  느린 이유는 그대로 있다.

---

# 103차 — 마지막 밀린 결함(87차 ⑤)을 고쳤다. 그리고 하나 고치려다 셋을 잃을 뻔했다

**날짜**: 2026-08-18 · 99차 자기 점검 ④의 이행

99차로 GAAP EPS 가 XBRL 로 넘어가면서 **보도자료 파서가 남은 곳은
조정 EPS · EBITDA · 가이던스뿐**이 됐다. 그래서 87차 ⑤ 이후 계속 밀려
있던 조정 EPS 결함을 이제 손댔다.

## ① 결함 — 값 없는 제목 줄

실물 `CRM 2025-05-28` (조정 EPS 를 **1.59** 로 읽는데 참값은 **2.58**):

```
Non-GAAP diluted net income per share            ← 값이 없다 (제목 줄)
GAAP diluted net income per share      $1.59     ← 이걸 물었다
Plus: Amortization of purchased intangibles ...
Non-GAAP diluted net income per share  $2.58     ← 진짜 값 (같은 줄)
```

논갭 조정표는 맨 위에 결과 항목 **이름만** 적고, 조정 내역을 늘어놓은 뒤
마지막에 합계를 적는다. 그래서 첫 번째 "Non-GAAP … per share" 는 값이
없는 **제목**인데, 이름과 숫자의 **거리**로 고르는 규칙이 바로 아래
GAAP 줄의 숫자를 물어 버린다.

판별법은 분명하다 — **이름과 숫자 사이에 또 다른 주당 항목 이름이 끼어
있으면** 그 이름은 자기 값을 가진 것이 아니다. 값은 언제나 자기 이름 바로
뒤에 온다 (파서의 대전제).

## ② 처음 만든 가드는 **하나 고치고 셋을 잃었다**

모든 주당 이름에 걸었다. 원문 **982건 전수 대조** 결과:

| 원문 | 조정 EPS | GAAP EPS |
|---|---|---|
| CRM 2025-05-28 | 1.59 → **2.58** ✅ | 1.59 → 1.59 |
| **MDB 2025-12-02** | 1.44 → 1.44 | **−0.02 → 1.44** ❌ |
| **SNDK 2026-08-05** | 39.25 → 39.25 | **43.97 → 39.25** ❌ |
| **SNDK 2026-08-05(부탁)** | 39.25 → 39.25 | **43.97 → 39.25** ❌ |

셋 다 **GAAP 이 조정값으로 무너졌다** — 파서 주석이 이미 *"이름 하나에
두 잣대가 같은 값으로 무너지는 것이 이 결함의 표시"* 라고 적어 둔 바로
그 꼴이다.

원인을 팠다. MDB 조정표 머리글이 이렇다:

```
Reconciliation of GAAP net loss per share, diluted, to
non-GAAP net income per share, fully diluted:
Net loss per share, diluted, on a GAAP basis  $ (0.02)
```

머리글 **안의** "GAAP net loss per share" 가 바로 뒤의 올바른 값
$(0.02) 과 가장 가까워서 **우연히 정답을 내고 있었다.** 내 가드가 그
머리글을 막자, 더 나쁜 후보(논갭 값)가 이겼다.

## ③ 실측된 결함에만 걸리게 좁혔다

87차 ⑤ 로 등록된 결함은 **조정 EPS** 제목 줄이다. 그래서 가드를
**논갭 이름일 때만** 작동하게 했다 (`_NONGAAP_NEAR_RE` 재사용).

다시 전수 대조:

> **원문 982건 중 바뀐 것 1건** — CRM 조정 1.59 → **2.58**. 부수 피해 **0**.

## ④ 내가 쓴 시험이 헛돌았다 (그 자리에서 고침)

경계를 지키라고 쓴 `test_제목줄_가드는_GAAP_이름을_건드리지_않는다` 를
손으로 지어낸 짧은 예문으로 썼더니, **가드를 넓히는 돌연변이에 빨간 불이
안 켜졌다.** 회귀를 못 잡는 시험이었다.

돌연변이를 잡은 것은 **기존 시험** `test_real_sndk_paren_value_before_label`
하나뿐이었다. 내 시험을 **실물 원문(MDB 2025-12-02)** 을 읽도록 다시
썼더니 그제야 빨간 불이 켜졌다. **실물이 규칙을 이긴다.**

| 돌연변이 | 빨간 불 |
|---|---|
| 가드를 없앤다 (103차 전) | ❌ `값_없는_제목줄은_건너뛴다` |
| 가드를 모든 주당 이름으로 넓힌다 | ❌ `제목줄_가드는_GAAP_이름을_건드리지_않는다` · ❌ `real_sndk_paren_value_before_label` |

## ⑤ 곁가지 — 내가 102차에 넣은 시험이 전체를 죽이고 있었다

전체 시험을 돌렸더니 **종료코드 124(시간 초과)** 가 나왔다. 102차에 넣은
`test_캐시는_값을_바꾸지_않는다` 가 실데이터로 `confirmation_rows`(22.7초)
를 원본·캐시 두 번 돌리고 있었다.

**시험이 느려서 못 돌면 안 돌린 것과 같다.** 가짜 함수를 끼워 "캐시가
원본을 그대로 불러 돌려주는가"만 보게 바꿨다 — 값이 같은지는 이쪽이 더
확실하게 못박는다 (원본이 무엇을 돌려주든 그대로 나와야 하므로).
돌연변이로 캐시가 딴 값을 내게 하자 빨간 불이 켜졌다.

전체 시험: **46초** · 종료코드 **0** · **443개 통과** (441 → 443).

## ⑥ 아직 실물로 안 돌았다

98차(`press_matched`) · 99차(GAAP EPS 뒤집기) · 103차(제목 줄)는 전부
**이 브랜치에만** 있다. 매일 도는 예약 런은 `main` 에서 돌므로 이 코드를
쓰지 않는다 (10:55 런의 `head_sha` 가 `18b45c9` = main 이었다).
**머지되기 전에는 실물 확인이 불가능하다.**

---

# 104차 — 102차에서 "안 팠다"고 적어 둔 22.7초의 원인 (같은 계산을 4번 하고 있었다)

**날짜**: 2026-08-18 · 102차 ⑤의 이행

102차에 캐시로 화면을 3초로 만들었지만 이렇게 적어 뒀다 —
*"`confirmation_rows` 가 왜 22.7초인지는 **안 팠다.** 캐시로 가려졌을 뿐
느린 이유는 그대로 있다."* 이제 팠다.

## ① 세어 보니 정확히 4배였다

`aligned_flags`(이동평균)와 `_delta_series`(분기 델타) 호출 수를 셌다.

| | 실측 |
|---|---|
| 종목 수 | **160** |
| `aligned_flags` 호출 | **640회** |
| `_delta_series` 호출 | **640회** |
| `confirmation_rows` 전체 | **27.9초** |

**정확히 4배다.** 이유는 구조를 보면 바로 보인다 —
`confirmation_rows` 는 묶음마다 `_breadths_at` 을 **지금·직전** 두 번
부르고, 묶음은 **섹터와 테마 두 벌**이다. 한 종목은 섹터 하나·테마 하나에
속하므로 2 × 2 = **4번** 계산된다.

같은 종목의 같은 계산은 결과가 같다. 한 번만 하면 된다.

## ② 고친 방식 — 종목별 중간 계산을 그릇에 담아 나눠 쓴다

`_breadths_at` 에 `memo` 그릇을 받는 자리를 만들고, `confirmation_rows` 가
그릇 하나를 만들어 네 번의 호출에 함께 넘긴다. 다른 호출부는 없다
(이 함수는 `confirmation_rows` 만 부른다).

| | 전 | 후 |
|---|---|---|
| `confirmation_rows` | 27.9초 | **7.0초** |
| 화면 **첫 접속** (412px) | 42초 | **30초** |
| 화면 두 번째 접속 | 3초 | 3초 (그대로) |

## ③ 값이 그대로임을 실데이터로 확인했다

담아쓰기는 **속도만** 바꿔야 한다. 고치기 전 결과를 파일로 남겨 두고
고친 뒤와 통째로 비교했다.

> 행 28 → 28 · **값이 완전히 같은가: True**

## ④ 시험 — 조용히 틀리는 방향을 못박았다

이 장치가 고장 나는 방향은 **"그릇이 잘못 채워져 다른 종목의 값을 쓰는
것"** 이고, 그러면 값이 그럴듯하게 틀린다 (아무 오류도 안 난다).

돌연변이: 그릇 열쇠를 종목이 아니라 `"고정"` 으로 바꾸자
❌ `전력에너지: 묶음이 섞였습니다`.

시험은 ㉠ 그릇을 준 결과 == 안 준 결과 ㉡ 같은 그릇을 다른 묶음에 다시
써도 그 묶음의 값이 나오는가 — 둘 다 봅니다.

시험: `python3 run_tests.py` 종료코드 **0** · **444개 통과** (443 → 444).

## ⑤ 남는 것

첫 접속 30초 중 남은 큰 덩어리는 `current_breadth`(5.1초) ·
`cycle_series`(5.6초) · `dataset.build`(1.4초) 와 스트림릿 그리기다.
`current_breadth` 도 같은 종류의 중복이 있을 수 있으나 **아직 안 쟀다.**
30초면 두 번째부터 3초이므로 급하지 않다.

## ⑥ 덧붙임 — `current_breadth` 는 재 보니 **고칠 게 없었다**

⑤에 "`current_breadth` 도 같은 종류의 중복이 있을 수 있으나 아직 안 쟀다"
고 적어 뒀다. 쟀다.

| | 실측 |
|---|---|
| `current_breadth` | **6.5초** · 12행 |
| `aligned_flags` 호출 | **160회** (종목 **160개**) |
| `_delta_series` 호출 | 0회 |

**정확히 1배다 — 중복이 없다.** 6.5초는 10년치 일봉으로 이동평균을
160종목만큼 계산하는 값 그대로의 비용이고, 줄이려면 계산 자체를 바꿔야
한다 (그건 측정 코드를 건드리는 일이다).

`confirmation_rows` 와 `current_breadth` 가 각각 160회씩 부르므로 둘
사이에 그릇을 공유하면 160회는 아낄 수 있다(≈5초). 하지만 첫 접속 30초가
25초가 될 뿐이고 **두 번째부터는 어차피 3초**다. 공유 그릇은 두 함수의
경계를 흐리므로 **이득에 비해 위험이 크다고 보고 하지 않는다.**

> 재 보고 **안 고치기로 한 것**이지, 안 재고 넘긴 것이 아니다.

---

# 105차 — 심판의 시야를 **1.7년 → 최대 25년**으로 (창구가 둘이었다)

**날짜**: 2026-08-18 · 97차 ⑥에서 "제일 값어치 있다"고 적은 것의 이행

97차에 이렇게 적었다 — *"야후 심판은 2024-11 ~ 2026-06, 962행뿐이다.
우리 데이터 5,851행의 **16%만** 본다."* 그리고 *"가장 값어치 있는 일은
두 번째 심판을 구하는 것"* 이라고 결론지었다.

새 업체를 찾기 전에, **지금 심판의 한계가 진짜 한계인지** 먼저 봤다.
주가 `"5y"` 가 야후의 한계가 아니라 우리가 적어 둔 기본값이었던 일
(94차)이 있었기 때문이다.

## ① 창구가 둘인데 깊이가 전혀 다르다

| 창구 | 깊이 | 근거 |
|---|---|---|
| `quarterly_income_stmt` (지금 쓰던 것) | **5분기** | yfinance 소스 주석: *"Yahoo returns maximum 4 years or 5 quarters, **regardless of start_dt**"* — **야후의 하드 한계** |
| `get_earnings_dates(limit=…)` | **최대 100분기 ≈ 25년** | 서명이 `(limit=12, offset=0)` 이고 소스에 `if limit > 100: raise ValueError("Yahoo caps limit at 100")` |

**손익계산서 쪽은 못 늘린다.** 이건 우리 잘못이 아니라 야후의 한계고,
94차의 주가 건과 달리 **진짜 벽**이다. 그 사실을 확인한 것만도 소득이다 —
아니었으면 새 업체를 찾아 헤맸을 것이다.

그런데 **발표 기록 쪽은 다르다.** 우리가 쓰던 `earnings_history` 는
실측 **4건**뿐이었는데, `get_earnings_dates` 는 같은 야후에서 100분기를
준다. **창구만 바꾸면 된다.**

## ② 바꾼 것

- `config.VENDOR_EARNINGS_LIMIT = 100` 을 두고 `vendor_feed.fetch` 가
  `get_earnings_dates(limit=…)` 를 쓰게 했다
- 그 창구가 없거나 실패하면 **예전 창구로 돌아간다** (없는 것보다 낫다)
- 열 이름이 달라 **둘 다** 읽게 했다
  (`epsActual`/`epsEstimate` ↔ `Reported EPS`/`EPS Estimate`)
- 깊은 창구는 **앞으로 있을 발표**도 함께 주는데, 값이 둘 다 없는 줄은
  심판으로 쓸 수 없어 담지 않는다 — **값을 지어내는 것이 아니라 빈 줄을
  안 싣는 것**이다. 추정만 있는 줄은 남긴다 (그것도 사실이므로)

## ③ 이 심판이 볼 수 있는 것과 못 보는 것 (섞지 않는다)

이 표가 주는 것은 **월가 기준 EPS**(`street_eps`)다. 회사가 발표한 조정
EPS 와 **다르다.** 75차에 정한 두 번째 자 원칙 그대로 — 이름을 달리해
`vendor.json` 에 **따로** 담고 본선 값과 절대 섞지 않는다.

| 칸 | 심판이 보나 |
|---|---|
| 매출 · GAAP EPS · 이익률 | 여전히 **최근 5분기까지만** (야후 하드 한계) |
| **조정 EPS 계열** | **최대 25년** ← 새로 생긴 것 |

공교롭게도 **조정 EPS 는 XBRL 이 못 채워 주는 유일한 칸**이고
(채움 55.8%), 지금까지 **심판이 아예 없던 칸**이다. 거기에 심판이 생긴다.

## ④ 시험 — 돌연변이 2건 빨간 불

| 돌연변이 | 빨간 불 |
|---|---|
| 깊은 창구 열 이름(`Reported EPS`)을 못 읽게 | ❌ `깊은_창구의_열이름도_읽는다` |
| 값 없는 앞으로의 발표도 담게 | ❌ `아직_안_나온_발표는_담지_않는다` |

시험: `python3 run_tests.py` 종료코드 **0** · **446개 통과** (444 → 446).

## ⑤ 아직 모르는 것

- **실제로 몇 분기가 오는지는 로봇이 돌아야 안다.** 100을 달라고 했다고
  100이 오지는 않는다 (상장 기간·야후 보관 범위). 개발 환경은 야후가
  막혀 있어 여기서는 확인할 수 없다.
- 월가 EPS 와 회사 조정 EPS 가 **얼마나 갈리는지** 아직 안 쟀다. 갈리는
  폭을 먼저 재야 이 심판을 어디까지 믿을지 정할 수 있다.
- 이것도 다른 개선들과 마찬가지로 **머지 전에는 실물 확인이 불가능하다.**

## ⑥ 덧붙임 — 재 보려다 **심판이 애초에 못 쓰는 상태였음**을 찾았다

⑤에 "월가 EPS 와 회사 조정 EPS 가 얼마나 갈리는지 아직 안 쟀다"고
적어 뒀다. 재려고 두 값을 발표일로 맞대었더니 —

> **맞대어 볼 수 있는 칸: 0개**

전 종목에서 한 칸도 안 맞았다. 실물 NVDA 로 확인했다.

| | 날짜 |
|---|---|
| 우리 발표일 | 2025-05-28 · 08-27 · 11-19 · 2026-02-25 |
| 야후 쪽 | 2025-07-31 · 10-31 · 2026-01-31 · 04-30 |

야후 쪽은 **분기 종료일**이다 (NVDA 는 1월 결산이라 분기가 4·7·10·1월에
끝난다). 발표일이 아니다.

**원인: 우리가 이름을 잘못 붙였다.** `earnings_history` 의 줄 이름은
분기 종료일인데 코드가 그걸 `announced_date` 라고 적고 있었다.

**이게 왜 여태 안 드러났나** — 이 칸을 **아무도 안 썼기 때문**이다
(`announcements` 를 읽는 코드가 저장소에 하나도 없다). 심판이 있는
줄 알았는데 실은 못 쓰는 값이었고, 조용했다.

### 고친 방식 — 부르는 쪽이 날짜의 뜻을 알려 준다

창구마다 줄 이름의 뜻이 다르다.

| 창구 | 줄 이름의 뜻 | 담기는 이름 |
|---|---|---|
| `earnings_history` (옛) | **분기 종료일** | `period_end` |
| `get_earnings_dates` (깊은, 105차) | **실제 발표일시** | `announced_date` |

뜻을 모르면 지어내지 않고 `"분기끝"` 으로 둔다. 각 줄에 `날짜뜻` 도
함께 적어, 다음에 읽는 사람이 짐작하지 않게 했다.

⚠️ 공교롭게도 **105차에서 창구를 바꾼 것이 이 결함도 함께 고친다** —
깊은 창구는 진짜 발표일로 색인되기 때문이다. 하지만 그건 **덤**이고,
이름을 사실대로 붙이는 것이 본론이다.

돌연변이: 분기 종료일을 다시 `announced_date` 로 이름 붙이자
💥 `street_eps_is_named_apart…` · ❌ `깊은_창구의_열이름도_읽는다`.

시험: 종료코드 **0** · **446개 통과** (개수 그대로 — 시험을 더한 게
아니라 낡은 전제를 고쳤다).

### 그래서 ⑤의 질문은 아직 못 쟀다

월가 EPS 와 회사 조정 EPS 의 차이는 **로봇이 깊은 창구로 한 번 받아온
뒤에야** 잴 수 있다. 짐작으로 적지 않는다.

---

# 106차 — 은행 16칸을 보러 갔다가 **매출 182칸이 쓰레기**임을 찾았다

**날짜**: 2026-08-18 · 95차 ④에서 "급하지 않다"고 미뤄 둔 것

95차에 남은 매출 오차 16칸을 "은행·유틸리티의 정의 차이라 급하지 않다"고
적었다. 그런데 그건 **심판이 보는 16%** 안에서 센 것이었다. 전수로 보니
훨씬 컸다.

## ① XBRL 매출이 **통째로 비는 종목이 22개** 있다

| 종목 | `revenue_xbrl` 빈 비율 |
|---|---|
| JPM · BAC · C · GS · MS · WFC · SCHW · AXP · COF (금융 9) | **100%** |
| LLY · JNJ · MRK · PFE (제약 4) | **100%** |
| KLAC · ETN 등 | **100%** |

은행만의 문제가 아니다. 22종목 × 약 44분기 ≈ **900행**이다.

## ② 개념 이름 문제가 **아니었다** — 93차 계기가 답했다

짐작으로 개념 이름을 늘리려다, 93차에 달아 둔 계기를 먼저 읽었다.

```
BAC   Revenues(3개월): 받음 104 · 이름버림 0 · 단위버림 0 · 남음 55
LLY   Revenues(3개월): 받음 170 · 이름버림 0 · 단위버림 0 · 남음 63
KLAC  RevenueFrom…(3개월): 받음 72 · 이름버림 0 · 단위버림 0 · 남음 31
```

**사실은 멀쩡히 찾아진다.** BAC 만 해도 55건이 검사를 통과했다.
그런데 그 값이 **분기 행에 안 붙는다.** 문제는 개념 이름이 아니라
**찾은 사실을 분기에 붙이는 자리**다.

> 계기를 안 봤으면 개념 이름을 늘리는 헛수고를 했을 것이다.
> (91차·98차에 이어 세 번째로 계기가 헛발질을 막았다.)

## ③ 그래서 **쓰레기 보도자료 값이 대신 들어갔다**

92차에 매출을 "XBRL 우선, **없으면 보도자료**"로 바꿨다. 이 22종목은
XBRL 이 안 붙으니 **언제나 보도자료 쪽**으로 떨어진다. 그 값이 이렇다.

| 종목 | 발표일 | 담긴 매출 | 그 종목 중앙값 |
|---|---|---|---|
| BAC | 2026-07-24 | **8달러** | 약 220억 달러 |
| C | 2023-01-13 | **−500만** | 185억 달러 |
| COF | 2021-02-16 | **−500만** | 41억 달러 |

자기 이력 중앙값에서 **1000배 이상** 벗어난 칸을 전수로 셌다.

> **182칸 / 5,851 = 3.1%**
> JPM 37 · BAC 18 · COF 18 · LLY 18 · MS 13 · BMY 12 · C 8 · AXP 8 · XOM 8

95차에 "16칸(2.5%)"이라고 적은 것은 **심판이 보는 최근 16% 안에서만**
센 값이었다. 전수로는 **182칸**이다. 95차의 그 문장에 이 사실을 함께
읽어야 한다.

## ④ 안 고쳤다 — 짐작으로 가드를 넣지 않는다

"자기 이력 중앙값에서 1000배 벗어나면 버린다" 같은 규칙이 바로 떠오르고
실제로 위 표를 만들 때 그 자로 셌다. 하지만 **그 1000배는 내가 방금
고른 숫자**다. 근거 없는 가드를 넣었다가 되돌린 89차(전망 가드)의 실수를
되풀이하지 않는다.

무엇보다 **진짜 원인은 따로 있다** — 찾은 XBRL 사실이 분기 행에 안 붙는
것. 그것을 고치면 이 22종목은 XBRL 값을 쓰게 되어 쓰레기 값 자체가
사라진다. 증상에 반창고를 붙일 자리가 아니다.

⚠️ 원인을 짚으려면 SEC 에 붙어 봐야 하는데 개발 환경은 차단돼 있다.
**다음 로봇 실행에서 계기를 하나 더 달아 확인할 일**로 남긴다 —
"개념 검사는 통과했는데 분기에 안 붙은 사실이 몇 건인가".

## ⑤ 이 결함이 무엇을 망치나 (범위를 정직하게)

- **매출은 사다리의 잣대가 아니다** (사다리는 조정 EPS → EBITDA →
  GAAP EPS). 그래서 **H2·H2b·H5b·H10 등 등록된 판정에는 안 쓰인다.**
- 쓰이는 곳은 화면 표시와 아직 등록 전인 탐색(매출↔마진 재료)이다.
- 즉 **판정 결과를 흔들지는 않지만, 화면이 8달러를 매출이라고 말한다.**
  정직화 원칙에 어긋나므로 고쳐야 한다 — 다만 **원인을 짚은 뒤에.**

## ⑥ 덧붙임 — 원인을 짚었다. **분기 목록을 영업이익에서만 만든다**

④에 "다음 로봇 실행에서 계기를 달 일"이라고 적었는데, 코드를 읽으니
원인 후보가 바로 나왔다 (SEC 접속 없이 확인 가능한 부분이었다).

```python
period_ends = sorted(d for d in series.get("op_income", {}) if d >= start_date)
...
revenue = series["revenue"].get(period_end)      # ← 같은 열쇠로만 찾는다
```

**분기 목록이 논갭 영업이익에서만 만들어진다.** 매출·이익률·GAAP EPS 는
그 날짜를 열쇠로 찾으므로, **영업이익이 없는 분기의 값은 찾아 놓고도
버려진다.**

이것이 ②의 수수께끼를 정확히 설명한다 — BAC 는 `Revenues` 가 55건
살아남는데 붙을 자리가 없다. **은행·보험은 `OperatingIncomeLoss` 를
신고하지 않는다** (이자수익·대손충당금 구조라 "영업이익" 줄이 없다).
제약사도 분기에 따라 빠진다. 22종목의 정체가 이것으로 보인다.

⚠️ "**보인다**" 이지 확정이 아니다. 각 종목의 영업이익 분기 목록이 실제로
비어 있는지는 SEC 에 붙어 봐야 한다.

### 그래서 세는 장치만 달았다 (`orphan_counts`)

종목마다 이렇게 남긴다.

```
xbrl_orphan: {분기목록(영업이익 기준): N, revenue: M, gaap_eps: …, gross_profit: …}
```

**아직 안 고친다.** 분기 목록의 뼈대를 바꾸면 **모든 종목의 행이 달라져
판정까지 흔들린다.** 먼저 얼마나 버려지는지 세고, 그 숫자를 보고 정한다.
짐작으로 뼈대를 건드리지 않는다.

### 내가 쓴 첫 시험이 또 가짜 초록불이었다

세는 규칙을 **시험 안에 베껴 써서** 만들었다. 그러면 본 코드가 망가져도
초록 불이 켜진다 — 오늘 103차에서 잡아낸 것과 **같은 꼴**이다. 알아채고
규칙을 `sf.orphan_counts` 라는 진짜 함수로 빼서, 시험이 그것을 부르게
고쳤다.

| 돌연변이 | 빨간 불 |
|---|---|
| 짝 없는 것을 0으로 센다 | ❌ `짝없는_XBRL_사실을_센다` |
| 수집 시작일을 안 본다 | ❌ 같은 시험 |

시험: 종료코드 **0** · **447개 통과** (446 → 447).

### ⑥-2 확인 — 진단이 맞았다 (다만 두 종류가 섞여 있다)

⑥에 "보인다"고만 적은 것을 스냅샷으로 확인했다. `filing_date` 와
`source` 를 함께 보면 그 행이 **XBRL 뼈대**에서 나왔는지 **보도자료**에서
나왔는지 갈린다 (뼈대 행의 `filing_date` 는 분기 종료일, 8-K 행은 발표일).

| 종목 | XBRL 영업이익 | source | filing_date 앞 3개 |
|---|---|---|---|
| BAC | **0건** | 역산 44 | 2019-04-01 · 04-16 · 06-27 |
| JPM | **0건** | 역산 44 | 2022-04-13 · 04-13 · 06-27 |
| LLY | **0건** | 역산 44 | 2018-01-31 · 04-24 · 07-24 |
| KLAC | 40건 | 직접공시 40 · 역산 4 | 2016-10-20 · 2017-01-26 · 04-27 |

**BAC·JPM·LLY 는 XBRL 영업이익이 한 건도 없다.** 날짜도 분기 종료일이
아니라 제각각인 8-K 발표일이다. 즉 **XBRL 뼈대 행이 아예 안 만들어졌고,
44행 전부 보도자료에서 나왔다.** ⑥의 진단이 그대로 맞았다 —
분기 목록이 영업이익에서만 만들어지므로, 영업이익이 없으면 XBRL 매출도
GAAP EPS 도 **붙을 자리가 없다.**

⚠️ **KLAC 은 다른 경우다.** XBRL 영업이익이 40건 있어 뼈대는 있는데
`revenue_xbrl` 은 여전히 0 이다. 같은 증상에 **원인이 둘**이라는 뜻이고,
KLAC 쪽은 아직 못 짚었다. 하나로 뭉뚱그리지 않는다.

이로써 ⑤에 적은 범위 판단이 더 분명해진다 — 이 종목들은 **잣대(조정 EPS)
까지 보도자료에서 온다.** 매출만의 문제가 아니라, 이 22종목은 XBRL 의
보호를 **한 칸도** 못 받고 있다. 다만 조정 EPS 는 애초에 XBRL 에 없는
값이라(사다리 1칸) 다른 종목과 조건이 같다.

**여전히 안 고친다.** 뼈대를 바꾸는 일이고, `orphan_counts` 가 다음 실행에
숫자를 가져온다. 그 숫자를 보고 정한다.

---

# 107차 — 머지 후 첫 실물 검증: **다섯 가지가 전부 확인됐다**

**날짜**: 2026-08-19 · **실행** 2026-08-19T00:49 · **`head_sha` = 머지 커밋 9020b9b**
· 시간초과 **0종목**

92~106차 개선이 **처음으로 실물로 돈** 런이다. `head_sha` 를 먼저 확인했다
(93차 교훈).

## ① GAAP EPS — 77.7% → **92.1%** (99차 뒤집기)

| 칸 | 전 | 후 |
|---|---|---|
| **GAAP EPS** | 77.7% | **92.1%** (574/623) |
| 매출 | 97.5% | 97.5% (631/647) — 유지 |
| 매출총이익률 | 59.1% | 59.1% — 안 뒤집었으므로 당연 |

99차에 예측한 91.1% 보다 **1%p 더 나왔다** (예측은 옛 스냅샷으로 계산한
값이라 표본이 조금 다르다).

## ② 조정 EPS 옛 구간 15%p 차이 — **"회사가 안 준 것"으로 확정**

97차 ⑤부터 98차·105차까지 세 번 미뤄 온 질문이다. `press_matched` 계기가
답했다.

| 연도 | 행 | **보도자료 붙음** | 그중 조정 EPS 있음 |
|---|---|---|---|
| 2016 | 34 | **85.3%** | 58.6% |
| 2017 | 221 | **81.4%** | 57.8% |
| 2018 | 382 | **83.5%** | 65.2% |
| 2021 | 596 | **82.4%** | 65.2% |
| 2023 | 626 | **82.9%** | 66.9% |
| 2025 | 634 | **82.8%** | 72.4% |
| 2026 | 440 | **79.1%** | 73.6% |

**보도자료가 붙는 비율은 10년 내내 79~85% 로 평평하다** (옛날이 오히려
약간 높다). 수집이 옛 공시를 놓치는 것이 **아니다.**

차이는 오른쪽 칸에만 있다 — 2016~17 **58%** → 2025~26 **73%**.
같은 보도자료를 같은 비율로 손에 넣는데 **옛 보도자료 안에 조정 EPS 가
덜 들어 있다.**

> **결론: 파서 결함이 아니라 회사가 안 준 것이다. "없음"이 정답이므로
> 고칠 것이 없다.** 2016~17 은 논갭 공시 관행이 지금만큼 표준화되기
> 전이라는 짐작이 실측으로 맞았다.

⚠️ 98차 ②에 적은 "84.6~87.8% 로 평평" 과 여기 "58~73%" 는 **분모가
다르다.** 98차는 논갭 영업이익을 읽어 낸 분기 **안에서** 셌고(부분집합),
여기는 보도자료가 붙은 분기 전체에서 셌다. 이쪽이 옳은 분모다.

## ③ 매출 쓰레기의 원인 — **22종목의 분기목록이 정확히 0**

`xbrl_orphan` 이 160종목 전부에 기록됐다.

> **분기목록(영업이익 기준)이 0 인 종목: 22개**
> KLAC · ETN · JPM · BAC · WFC · C · GS · MS · SCHW · AXP · COF ·
> LLY · JNJ · MRK · PFE · BMY · NKE · GE · XOM · CVX · COP · OXY

106차에 스냅샷으로 짚은 22종목과 **정확히 같다.** 금융 9 · 제약 5 ·
에너지 4 · 그 외 4.

버려지는 사실의 양:

| 항목 | 짝이 없어 버려진 건수 |
|---|---|
| **매출** | **851** |
| **GAAP EPS** | **760** |
| 매출총이익 | 221 |

**구조화 데이터 1,832건을 찾아 놓고 버리고 있다.**

## ④ 심판 깊이 — 4분기 → **99분기** (105차)

| | 전 | 후 |
|---|---|---|
| 발표 기록 분기 수 (중앙값) | **4** | **99** |
| 최대 | 4 | **100** (야후 상한) |
| 날짜 키 | `period_end`(분기 종료일) | **`announced_date`** 160종목 전부 |

깊은 창구가 **전 종목에서 성공**했고, 105차 ⑥에 고친 날짜 이름도 제대로
`announced_date` 로 담겼다. 약 **25년치** 심판이 생겼다.

## ⑤ 제목 줄 결함 — 고쳐졌다 (103차)

```
CRM 2025-05-28  조정 EPS 2.58 (참값 2.58) ✅  ·  GAAP 1.59 (참값 1.59) ✅
```

87차 ⑤ 이후 계속 밀려 있던 마지막 파서 결함이 실물에서 확인됐다.

## ⑥ 정정 — 106차 ⑥-2 의 KLAC 해석이 틀렸다

106차 ⑥-2 에 이렇게 적었다 — *"KLAC 은 다른 경우다. XBRL 영업이익이
40건 있어 뼈대는 있는데 `revenue_xbrl` 은 0 이다. 같은 증상에 원인이 둘."*

**틀렸다.** `xbrl_orphan` 이 KLAC 의 분기목록을 **0** 으로 적어 왔다.
KLAC 의 op_income 40건은 **보도자료에서 온 것**이고(`source` 가
`직접공시`), XBRL 영업이익이 아니다. 내가 `op_income` 칸만 보고 그것이
XBRL 값이라고 단정했다.

**원인은 하나다** — 22종목 전부 "XBRL 영업이익이 없어 분기목록이 안
만들어진다". 원인을 둘로 갈랐던 것은 내 오독이다.

## ⑦ 이제 뼈대를 고칠 근거가 생겼다 (아직 안 고친다)

106차에 *"뼈대를 바꾸면 모든 종목의 행이 달라져 판정까지 흔들리니
숫자를 먼저 보고 정한다"* 고 적었다. 숫자가 나왔다 — **1,832건**.

**다음 차수의 일**로 남긴다. 고친다면 방향은 하나다:

> 분기목록을 **영업이익에서만** 만들지 말고, **잣대 사다리처럼**
> 여러 항목의 합집합에서 만든다.

⚠️ 하지만 이것은 **측정 장치의 뼈대**를 바꾸는 일이라, 사전 등록 없이
손대면 안 된다. 바꾸면 22종목이 새로 측정에 들어와 **모든 등록 가설의
표본이 달라진다.** 96차에서 표본이 늘자 채택 두 개가 무너진 것을 이미
봤다. **다음 차수에서 사전 등록 절차를 밟는다.**

---

# 108차 — 데이터 불량 방어체계 (주인 지시)

**날짜**: 2026-08-19 · 주인 지시: *"수집누락 등 데이터쪽의 불량을 방지하기
위한 방어체계를 설계해"*

## ① 먼저 — 내가 "누락"이라고 한 것은 **누락이 아니었다** (정정)

직전 보고에서 컨센서스 원장의 날짜별 종목 수가 160 → 55 → 2 → 38 → 1 로
줄어드는 것을 보고 **"수집이 새고 있다, 급하다"** 고 적었다. 틀렸다.

`append_snapshot` 의 주석이 답이었다 —

> *"마지막 항목과 값이 완전히 같음 → 붙이지 않는다 (변화 없는 날은 원장을
> 불리지 않습니다 — '발표일 이전 마지막 스냅샷' 조회 결과는 달라지지 않습니다)"*

로봇 기록도 `컨센서스: 확보 **160종목** · 원장 추가 1건 · 실패 **0종목**`
이다. **설계대로 작동하고 있었고, "1종목"은 그날 값이 바뀐 종목 수다.**

⚠️ 방어체계를 설계하랬다고 **없는 병에 약을 지을 뻔했다.** 헌법 6조
(신호보다 장치를 먼저 의심)가 이번엔 내 진단 쪽으로 되돌아왔다.

## ② 진짜 구멍 — 기존 그물이 무엇을 못 보나

`data_quality.py` 가 이미 있다. 하지만 그것은 **한 분기 안에서 앞뒤가
맞나**를 본다(마진이 100%를 넘나, 매출이 영업이익보다 작나…).

2026-08-18 하루에 찾아낸 사고들은 **전부 그 그물을 빠져나갔다.**

| 사고 | 왜 안 걸렸나 |
|---|---|
| `gaap_eps_xbrl` 3,066행 **전부** None | 어느 한 행도 이상하지 않다. **칸이 통째로** 비었을 뿐 |
| 매출 182칸 쓰레기 (BAC **8달러**) | 8달러는 그 자체로는 "말이 되는" 숫자다 |
| XBRL 사실 **1,832건** 버려짐 | 행이 만들어지기 **전에** 사라진다 |
| 심판 날짜가 분기끝인데 "발표일"로 이름 붙음 | 값은 멀쩡하다. **뜻**이 틀렸다 |
| 화면이 "약 5년" · "9.4% vs 26.8%" 라고 말함 | 데이터가 아니라 **화면**의 문제 |

**공통점: 전부 조용했다.** 예외도 안 나고 실패 카운터도 안 올라갔다.

그리고 **결국 잡아낸 방법도 공통점이 있다 — 전부 "세어 본 것"이다.**

## ③ 설계 — 5층, 각 층이 실제 사고 하나씩을 막는다

| 층 | 무엇을 보나 | 막는 사고 | 상태 |
|---|---|---|---|
| **1. 값 검사** | 한 분기 안의 앞뒤 | 마진 82,804,463% | 이미 있음 (`data_quality`) |
| **2. 채움률** | 칸이 얼마나 차 있나 | 91차 "통째로 빈 칸" | **108차 신설** |
| **3. 어제 대비** | 무엇이 달라졌나 | 87·89·103차 "고침의 부수 피해" | **108차 신설** |
| **4. 자기 이력 이상값** | 종목 안에서 튀는 값 | 106차 "BAC 8달러" | **108차 신설** |
| **5. 두 눈 대조** | 독립된 두 경로 | 92·99차 "어느 쪽이 맞나" | 이미 있음 (`vendor_compare`) |

2·3·4 를 `data_health.py` 하나로 묶어 **로봇이 매일 자동으로** 돌린다.
세션마다 사람이 손으로 세던 일을 장치로 옮긴 것이다.

## ④ 지킨 원칙 둘 — 이게 설계의 핵심이다

### **막지 않고 센다**

이 파일은 **값을 하나도 바꾸지 않는다.** 이상값을 찾아도 버리지 않고
개수와 예시만 남긴다.

왜냐면 89차에 근거 없는 가드를 넣었다가 되돌린 적이 있고, 106차에도
*"1000배 벗어나면 버린다는 그 1000배는 내가 방금 고른 숫자"* 라고 적어
두고 안 고쳤기 때문이다. **감시는 관찰이지 개입이 아니다.**

시험으로 못박았다 — `건강검진은_값을_바꾸지_않는다`.

### **경보 문턱은 아직 안 정한다**

"어제보다 몇 칸 넘게 바뀌면 경보" 같은 규칙이 바로 떠오르지만, 그 숫자를
지금 고르면 **사후 맞추기**다(헌법 2조). 며칠치를 쌓아 **정상 범위를
실측한 뒤** 사전 등록한다. 지금은 숫자만 남긴다.

## ⑤ 첫 실측 (오늘 수집물, 5,853행)

| 칸 | 채움률 |
|---|---|
| revenue | 97.0% |
| gaap_eps | 94.3% |
| op_income | 85.4% |
| **press_matched** | **84.1%** |
| revenue_xbrl | 72.1% |
| gross_margin_pct | 59.7% |
| **adj_eps** | **55.6%** ← 사다리 1칸인데 가장 낮다 |
| gaap_eps_xbrl | 55.4% |
| adjusted_ebitda | 10.7% |

이상값(매출) **183칸**. 예시로 `WDC 2016-12-22 매출 −1.0` 이 새로
드러났다 — 106차의 182칸에 없던 것이다 (그때는 손으로 셌고 음수 처리가
달랐다). **장치가 사람보다 더 봤다.**

## ⑥ 시험 — 조용한 고장 세 방향을 못박았다

이 장치가 고장 나면 **아무 소리 없이 "이상 없음"이라고 말한다.**

| 돌연변이 | 빨간 불 |
|---|---|
| 이상값을 안 센다 | ❌ `그럴듯한_쓰레기를_자기_이력으로_잡는다` |
| 어제 대비 변화를 안 센다 | ❌ `어제_대비_바뀐_칸을_센다` |
| 검진이 값을 건드린다 | ❌ `건강검진은_값을_바꾸지_않는다` |

⚠️ 세 번째는 **처음에 안 잡혔다.** 시험 자료를 전부 같은 값(8.0)으로
썼더니 중앙값도 8.0 이라 이상값이 하나도 안 걸렸고, **값을 건드리는 줄이
아예 실행되지 않아** 돌연변이가 초록 불로 통과했다. **오늘 네 번째
가짜 초록불**이다(103·106차와 같은 꼴). 자료를 고쳐 다시 확인했다.

시험: `python3 run_tests.py` 종료코드 **0** · **453개 통과** (447 → 453).

## ⑦ 아직 안 한 것

- **경보 문턱** — 며칠 쌓은 뒤 사전 등록 (위 ④)
- **화면 표시** — 지금은 `robot_log.json` 에만 남는다. 계기판의
  "재료 상태" 칸에 붙이는 일은 다음 차수
- **XBRL 뼈대 결함** (107차 ⑦, 1,832건) — 이건 방어가 아니라 수리이고,
  측정 장치의 뼈대라 사전 등록이 필요하다

---

# 109차 — 탐색: **신고점은 "넘었나"가 아니라 "얼마나 넘었나"** (+ H22 사전 등록)

**날짜**: 2026-08-19 · 주인 지시: *"모델링을 활용할 수 있는 수준까지 …
사이사이 좋은 전략이 있으면 제안"*

## ① 먼저 — 병목이 표본이 아니었다

| | |
|---|---|
| 잣대가 있는 종목 | **160 / 160** (adj_eps 116 · gaap_eps 35 · EBITDA 9) |
| 측정 가능한 사건 | **4,163건** (수익까지 잴 수 있는 것 3,100건) |
| 잣대 없어 제외 | **0종목** |

표본은 충분하다. 그런데 등록된 가설이 전부 미채택이다. **재료가 아니라
잣대의 문제**라는 뜻이다.

## ② 의심 — 지금 잣대는 **이진값**이다

`new_high` 는 참/거짓이다. 직전 정점을 **1% 넘은 것과 50% 넘은 것이
똑같이 "신고점"** 이다. 추세를 따라가겠다면서 추세의 **크기**를 안 보고
있었다.

그래서 신고점의 폭을 재 봤다 (탐색, 3,100건).

## ③ 결과 — **단조로 듣는다**

| 신고점의 폭 | n | 폭등률 |
|---|---|---|
| **기준선 (모든 발표)** | 3,100 | **13.0%** |
| 신고점 전체 (이진) | 1,304 | 12.6% ← 우위 없음 |
| 폭 0~1% | 67 | **9.0%** |
| 폭 1~3% | 201 | **7.5%** |
| 폭 3~5% | 218 | **7.8%** |
| 폭 5~10% | 334 | 12.6% |
| 폭 10~20% | 230 | **16.1%** |
| **폭 20%↑** | 254 | **18.5%** |

**"신고점"이 우위가 없던 이유가 설명된다** — 작게 넘은 것(7.5~9.0%,
기준선보다 **나쁨**)과 크게 넘은 것(18.5%, 기준선보다 **좋음**)이 한
바구니에 섞여 상쇄되고 있었다.

작게 넘은 것이 기준선보다 **나쁘다**는 것도 뜻이 있다. 간신히 넘긴
신고점은 신호가 아니라 **잡음**에 가깝다.

## ④ 안 듣는 것도 그대로 적는다 (헌법 5조)

| 후보 | n | 폭등률 | 판단 |
|---|---|---|---|
| **가속** (증가율이 직전보다 ↑) | 1,278 | **11.9%** | 기준선(13.0%)보다 **나쁨** |
| 감속 | 1,674 | 13.7% | — |
| 신고점 ∧ 가속 | 578 | 13.5% | 폭 단독보다 못함 |
| 폭 10%↑ ∧ 가속 | 275 | 17.8% | **폭 20%↑ 단독(18.5%)보다 못함** |

**가속은 안 듣는다.** 결합해도 폭 단독보다 나빠진다. 재 놓고 버린다.

## ⑤ 지금 채택할 수 있나 — **아니다** (아슬아슬하게)

| | 폭등률 | 윌슨 95% |
|---|---|---|
| 기준선 | 13.0% | [11.9, **14.2**] |
| 폭 20%↑ | 18.5% | [**14.2**, 23.7] |

**하한 14.2 = 상한 14.2.** 완전 분리에 딱 닿았지 넘지는 못했다.
그리고 **이건 탐색 표본이라 애초에 채택 근거가 아니다** (헌법 5조).

## ⑥ 사전 등록 — H22 · H22b (데이터를 더 보기 전에 못박는다)

> **H22 — TTM 잣대 신고점을 직전 정점 대비 5% 이상 넘긴 발표**
>
> **H22b — 같은 것, 20% 이상**
>
> · 창 60거래일 · 폭등 = SPY 대비 +20%p · 기준선 = 같은 표본의 모든 발표
> · 채택 = 신호 윌슨 95% 하한 > 기준선 상한, n≥10
> · **판정 표본은 등록일(2026-08-19) 뒤의 새 발표만.** 위 탐색에 쓴
>   3,100건은 판정에 재사용하지 않는다 (탐색 표본 재사용 금지)
> · 문턱 5%·20% 는 위 표를 보고 골랐다. **그래서 탐색 표본으로는 절대
>   판정하지 않는다** — 그것이 사후 맞추기를 막는 유일한 방법이다

⚠️ 왜 두 개인가: 5% 는 "잡음과 신호를 가르는 선"으로 보이고, 20% 는
"가장 강한 구간"이다. 어느 쪽이 실전에서 남는지는 새 데이터가 정한다.
H11/H11b 를 두 문턱으로 등록한 것과 같은 방식이다.

## ⑦ 이 발견이 왜 중요한가 (주인께 드리는 제안)

지금까지 모델은 **"신기록을 깼는가"** 를 물었다. 이 탐색은 그 질문이
반쯤 틀렸다고 말한다 — 물어야 할 것은 **"얼마나 크게 깼는가"** 다.

- 간신히 깬 것(폭 5% 미만, 486건)은 기준선보다 **나쁘다**. 지금 모델은
  이것들을 신호로 세고 있었다
- 크게 깬 것(폭 20%↑, 254건)만 **기준선을 뚜렷이 웃돈다**

이건 재료를 더 모으거나 파서를 더 고쳐서 나오는 개선이 아니다. **같은
데이터를 다르게 보는 것**이고, 등록만 하면 로봇이 자동으로 판정한다.

## ⑧ 아직 안 한 것

- 폭을 **측정 장치에 넣는 일** (`measure_engine` 이 사건마다 폭을 담게).
  등록했으니 다음 차수에서 구현하고, 로봇이 매일 판정하게 한다
- 폭과 **장세(H5b)** 의 결합 — 위층×아래층 두 층 구조가 헌법 0장인데
  이번 탐색은 아래층만 봤다

## ⑨ 구현 — 로봇이 매일 H22 를 판정한다 (같은 차수에서 바로)

등록만 하고 두면 판정이 안 되므로 곧바로 구현했다.

- `measure_engine.earnings_states` 가 사건마다 **`신고점폭`** 을 담는다
  (직전 정점이 **양수일 때만**. 적자에서 적자로 옮겨간 것을 "몇 % 성장"
  이라 부를 수 없으므로 없음으로 둔다 — 창작 금지)
- `judge.judge_newhigh_margin` 이 두 문턱(5%·20%)을 판정하고,
  `collect_job` 이 매일 부른다
- **판정 표본은 `announced > 2026-08-19` 뿐.** 탐색에 쓴 것은
  `탐색표본(참고)` 로 따로 적어 버리지 않되 판정에서 뺀다

첫 실행 결과 — 설계대로다.

```
H22_신고점폭_5   판정 불가 · 신규(판정) n=0 · 탐색표본(참고) n=572 16.4% [13.6, 19.7]
H22b_신고점폭_20 판정 불가 · 신규(판정) n=0 · 탐색표본(참고) n=149 19.5% [13.9, 26.6]
                                          (기준선 n=947 13.2% [11.2, 15.5])
```

**n=0 이 정답이다.** 등록일 뒤의 발표가 아직 없으니 판정할 것이 없다.
160종목이 분기마다 발표하므로 **약 3개월 뒤 첫 판정**이 나온다.

### 시험 — 돌연변이 4건

| 돌연변이 | 빨간 불 |
|---|---|
| 등록일 가드를 없앤다 (탐색 표본이 판정에 샘) | ❌ `H22_신고점폭은_등록일_뒤만_판정한다` |
| 폭 없는 발표를 0으로 채운다 | ❌ `H22_폭을_못_재는_발표는_표본에서_뺀다` |
| 측정 장치가 폭을 안 담는다 | ❌ `신고점의_폭을_함께_담는다` |
| 음수 정점도 폭을 잰다 | ❌ `정점이_음수면_폭을_안_잰다` |

⚠️ 마지막 둘은 **처음에 안 잡혔다.** 셋째는 측정 장치 쪽 시험이 아예
없었고, 넷째는 시험의 조건절(`ttm > 0`)이 이 자료에서 **한 번도 참이
안 되어** 아무것도 안 지켰다. **오늘 다섯 번째 가짜 초록불**이다
(103·106·108차와 같은 꼴). 둘 다 고쳐 다시 확인했다.

시험: `python3 run_tests.py` 종료코드 **0** · **457개 통과** (453 → 457).

---

# 110차 — 세션 장치 점검 (주인 질문: "스킬 훅이 제대로 작성되어 있나?")

**날짜**: 2026-08-19

## ① 점검 결과 — 훅은 **하나도 없었다**

| 장치 | 상태 |
|---|---|
| 권한 설정 (`.claude/settings.json`) | ✅ 저장소에 커밋됨 — 초기화에도 살아남음 (60개 정확한 이름) |
| 매시 자동 재개 트리거 (:12) | ⚠️ 작동은 하나 **복구 방법·안전 규칙이 안 실려 있었음** |
| 훅 (SessionStart 등) | ❌ **없음** |
| 프로젝트 스킬 | ❌ 없음 (기본 동기화 스킬뿐) |
| `~/.claude/settings.json` | 컨테이너 초기화로 소실 — 프로젝트 것이 커밋돼 있어 무해 |

가장 아픈 구멍: **컨테이너 초기화 때마다 손으로 복구**하고 있었다.
2026-08-18~19 사이에만 **여섯 번**, 매번 같은 fetch·checkout 을 반복했다.

## ② 세션 시작 훅을 만들었다 (`.claude/hooks/session_start.sh`)

세션이 시작될 때마다:

1. **상태 보고** — HEAD·origin/main·뒤처짐·미커밋·로봇 마지막 수집 시각
2. **무손실일 때만 자동 복구** — "미커밋 0 AND 로컬에만 있는 커밋 0"
   이 증명된 경우에만 origin/main 으로 맞춘다. 하나라도 있으면
   **절대 건드리지 않고** 보고만 한다
3. **운영 규칙 4개 주입** — head_sha 먼저(93차) · 수집 런 6시간 간격(99차)
   · 계기부터(91·98·106차) · 시험은 깨뜨려 본 뒤에만(가짜 초록불 5건)

실행 증명 (가짜 초기화 상황을 만들어 실측):

```
뒤처짐 3 · 미커밋 0 · 로컬 커밋 0  → "자동 복구했습니다" · HEAD == origin/main ✅
뒤처짐 3 · 로컬 커밋 1            → "건드리지 않았습니다" · HEAD 불변 ✅
저장소가 아예 없음                → 종료코드 0 (세션을 막지 않음) ✅
```

⚠️ 곁가지 — 두 번째 확인에서 내 검사 스크립트의 셸 변수명을 한글로
써서(`전=`) 판정이 엉터리로 나왔다. 훅은 옳았고 검사가 틀렸다.
ASCII 변수로 고쳐 재확인했다.

## ③ 시험 4개 + 돌연변이 2건 (`tests/test_repo_config.py`)

이 장치가 고장 나는 두 방향 중 **과잉 복구가 훨씬 위험하다** — 로컬
작업이 소리 없이 사라진다. 그래서 그쪽을 먼저 못박았다.

| 돌연변이 | 빨간 불 |
|---|---|
| 안전 조건을 없애 무조건 복구 | ❌ `로컬_작업이_있으면_절대_건드리지_않는다 — 로컬 커밋이 날아갔습니다` |
| 복구를 아예 안 함 | ❌ `초기화_상태면_무손실_복구한다` |

설정 파일 자체도 시험한다 — JSON 한 글자만 깨져도 권한·훅이 **아무 오류
없이 통째로 꺼지기** 때문이다.

## ④ 매시 트리거 문구 현행화

옛 문구에는 복구 방법이 없고 기준 브랜치 언급도 없었다. 새 문구에
main 기준 · 훅 실패 시 수동 복구 절차 · 운영 규칙 3개를 실었다.
트리거 문구는 문맥 압축에도 살아남는 **가장 질긴 기억**이므로 여기에
싣는 것이 가장 안전하다.

CLAUDE.md 는 **일부러 안 건드렸다** — 저장소 주인이 정한 규칙 파일이고,
같은 내용이 훅 출력과 트리거 문구로 매 세션 주입되므로 중복이다.

시험: `python3 run_tests.py` 종료코드 **0** · **461개 통과** (457 → 461).

---

# 111차 — 월가 EPS vs 회사 조정 EPS 첫 측정 (+ 새 데이터 412px 확인)

**날짜**: 2026-08-19 · 105차 ⑤에서 "짐작으로 적지 않는다"고 미룬 숙제

## ① 심판이 깊어져 처음으로 잴 수 있게 됐다

105차 ⑥ 수정 전에는 날짜 뜻이 틀려 **짝지어지는 칸이 0개**였다. 지금은:

| | |
|---|---|
| 발표일로 짝지은 칸 | **3,132개** |
| 1.5센트 안 일치 | **2,268개 (72.4%)** |
| 차이 중앙값 | **0.000달러** |

**중앙값이 정확히 0** — 절반 이상은 완전히 같다. 월가 "발표 EPS"는
대부분 회사 조정 EPS 를 그대로 쓴다는 뜻이고, 이 심판을 조정 EPS 감시에
쓸 수 있다는 근거다.

연도별 일치율은 2016년 47.4% → 2026년 78.7% 로 옛날일수록 낮다
(다만 2016 은 n=19 라 숫자가 흔들린다).

## ② 크게 갈린 꼬리는 **우리 쪽 연간값 오염** — 산술로 확인

가장 크게 갈린 6곳을 "그 발표일 직전 4개 분기 월가값의 합"과 맞대었다
(분기 합 = 연간 — 이 저장소의 유일하게 믿을 만한 검증법).

| 종목 | 우리 값 | 직전 4분기 합 | 판정 |
|---|---|---|---|
| **FDX** 2024-06-25 | 17.80 | **17.81** | **연간값 확정** (0.1% 차) |
| **AXP** 2026-01-30 | 15.38 | **15.39** | **연간값 확정** |
| UNH 2026-01-27 | 17.75 | 16.31 | 연간 모양 (8.8% 차 — 다음 해 전망일 수도) |
| SMCI 2024-08-06 | 22.09 | 2.20 | **2.20 × 10.04** — 액면분할(10:1) 미반영 × 연간으로 보임 |
| LLY 2025-02-06 | 22.50 | 13.00 | 정체 미확정 (전망값 모양이나 산술로 확정 못함) |
| UNH 2025-07-29 | 16.00 | 25.24 | 정체 미확정 |

**꼬리의 정체는 87차에 이름 붙인 그 결함(연간값 오염)이다.** 조정 EPS 는
XBRL 이 못 지키는 유일한 칸인데, 이제 깊은 심판이 그 칸의 의심 목록을
**종목·발표일 단위로** 만들어 줄 수 있다. 다음 차수 후보: 갈린 칸을
`부탁_` 원문 요청으로 잇는 배관 (73차에 GAAP 용으로 만든 것과 같은 틀).

⚠️ SMCI 는 새 종류다 — 야후는 액면분할을 소급 반영하고 우리는 발표
당시 그대로 적는다. **정의 차이**지만, 주가는 수정주가를 쓰므로 TTM
추세 계산에서 분할 전후가 이어지면 가짜 급락이 생길 수 있다.
**아직 실측 안 했다** — 분할 종목이 몇 개인지부터 세야 한다.

## ③ 새 데이터 412px 확인 (107차 이후 숙제)

```
첫 접속 28초 · 두 번째 3초 ✅ · 가로 스크롤 없음 ✅
'채택된 신호 없음' 있음 ✅ · '약 10년' 있음 · 옛 숫자(9.4%·26.8%·약 5년) 없음 ✅
'H22' 없음 — 정상. 판정 파일은 로봇이 쓰는데 109차 코드는 다음 런부터 돈다
```

## ④ 곁가지 — `pkill` 이 또 셸을 죽였다 (101차와 같은 덫, 두 번째)

이 기록을 처음 쓸 때 `pkill -f streamlit` 을 같은 명령 줄에 붙였다가
셸이 통째로 죽어 기록·커밋이 날아갔다. 101차에서 같은 일을 겪고도
반복했다. **규칙으로 남긴다: `pkill` 은 반드시 단독 명령으로, 뒤에
아무것도 잇지 않는다.**

---

# 112차 — 액면분할: 세어 보고(강한 의심 14종목), 단위 환산을 만들었다

**날짜**: 2026-08-19 · 111차 ②에서 "분할 종목 수부터 세야 한다"로 남긴 숙제 (주인 지시)

## ① 세었다 — 심판 비율의 산술 검출

우리 주당 값 ÷ 야후 소급 값이 **정수 배수**에 붙으면 분할 흔적이다.

| 검출 강도 | 종목 수 | 기준 |
|---|---|---|
| 원시 의심 | 35 | 배수 근접 짝 3개 이상 |
| **강한 의심** | **14** | 걸린 분기가 경계 앞에 몰리고, **경계 뒤는 1배로 복귀** |

강한 의심 14: ANET·AVGO·CAT·GE(역)·GILD·GOOGL·LRCX·NFLX·NOW·NVDA·
POWI·SCHW·TTD·WMT — 이 중 NVDA(10:1)·AVGO(10:1)·GOOGL(20:1)·
LRCX(10:1)·NFLX(10:1)·WMT(3:1)·GE(1:8 역) 등은 널리 알려진 실제 분할과
서명이 일치한다.

⚠️ **이 숫자는 의심 목록이지 확정이 아니다.** CAT·GILD 같은 것은 연간값
오염(111차)이 4배로 잡힌 노이즈일 수 있고, KLAC 처럼 다른 결함이 섞인
것도 있다. 손으로 센 것으로 배수를 확정하지 않는다 — **역산 금지.**

## ② 고쳤다 — 공식 기록 수집 + 데이터 계층의 단위 환산

**손 배수는 한 번도 쓰지 않는다.** 대신:

1. **`vendor_feed.splits_from_series`** — 로봇이 야후의 **공식 분할
   기록**(날짜·비율)을 그대로 옮겨 적어 `vendor.json` 에 담는다
2. **`dataset.load_splits` + `_apply_splits`** — 발표일이 분할일보다
   앞인 행의 **주당 칸만** 그 뒤 모든 분할 비율의 곱으로 나눈다.
   정분할(비율>1)은 작아지고 역분할(비율<1)은 커진다 — 나누기 하나로
   양쪽이 다 맞는다. 매출·영업이익은 주식 수와 무관하므로 안 건드린다
3. 기록이 없으면 **아무 일도 안 일어난다** (예전과 완전히 같음)

### 이것이 창작 금지 위반이 아닌 이유 (경계 문서에 명시)

분할 전후의 주당 값은 **단위가 다른 숫자**다 (주식 1개의 크기가 다르다).
환산은 값을 지어내는 것이 아니라 **이미 쓰고 있는 수정주가와 같은 단위
맞추기**다. 주가는 소급 조정하면서 EPS 는 발표 당시 그대로 이으면, 같은
회사의 TTM 이 분할일에 10분의 1로 꺼지는 **가짜 급락**이 생긴다 — 그쪽이
거짓이다. dataset.py 머리말의 "값을 고치지 않는다" 원칙에 이 단 하나의
예외를 사유와 함께 적어 두었다.

## ③ 시험 — 돌연변이 3건 전부 빨간 불

| 돌연변이 | 빨간 불 |
|---|---|
| 분할 **후** 행까지 환산 (경계 무시) | ❌ `분할 후 값을 건드렸습니다` |
| 환산을 아예 안 함 | ❌ `분할 전 조정 EPS 가 ÷10 되지 않았습니다` |
| **매출까지** 환산 | ❌ `매출은 주식 수와 무관 — 건드리면 안 됩니다` |

역분할(×8)·겹분할(비율 곱) · 기록 없음(무변화) · 불량 기록 거르기도
각각 시험으로 못박았다. 전체 **466개 통과** (461 → 466).

## ④ 발효 시점 (정직하게)

- 오늘 vendor.json 에는 splits 가 **아직 없다** — 로봇이 다음 런(09:30)에
  처음 수집한다. 그때까지 환산은 일어나지 않는다
- 다음다음 런부터는 그날 수집물 + 전날 분할 기록으로 환산이 돈다
  (판정 경로는 디스크의 vendor.json 을 읽으므로 하루 지연 — 분할은
  드물어 감수)
- 발효되면 **모든 등록 가설의 사건이 다시 계산**된다 — 분할 종목의
  가짜 신고점/가짜 급락이 사라지는 방향이다. 판정 변화는 발효 후
  실측으로 보고한다 (짐작 금지)

---

# 113차 — 뼈대 수리: 버려지던 XBRL 사실 1,832건이 붙을 자리를 만들었다

**날짜**: 2026-08-19 · 106차 ④에서 "숫자를 보고 정한다"고 미룬 수리.
107차에 숫자가 나왔다(1,832건) — 약속대로 고친다.

## ① 무엇을 고쳤나 — 분기 목록을 세 항목의 합집합으로

```python
# 전 (106차까지):
period_ends = sorted(d for d in series["op_income"] if d >= start_date)
# 후 (113차):
period_ends = sorted({d for key in ("op_income","revenue","gaap_eps")
                        for d in series.get(key, {}) if d >= start_date})
```

은행·보험은 `OperatingIncomeLoss` 를 신고하지 않아 분기 목록이 **아예 안
만들어졌고**, 찾아 둔 매출 851 · GAAP EPS 760 · 매출총이익 221 =
**1,832건이 붙을 자리가 없어 버려졌다** (22종목). 그 빈자리에 보도자료
쓰레기(BAC 매출 8달러)가 들어갔다.

합집합이면 영업이익 없는 분기도 행이 생기고, 기존 8-K 짝짓기 장치가
발표일 도장을 찍는다 — **짝짓기·도장·XBRL 우선 규칙은 하나도 안 고쳤다.**
전부 이미 범용으로 만들어져 있었고, 목록 하나가 병목이었다.

같이 손본 것: 영업이익 없는 행의 논갭 근사는 **지어내지 않고 없음**
(0으로 채우는 돌연변이가 빨간 불로 확인됨) · derivation 문구를 사실대로
· 셋 다 없는 분기는 행을 안 만듦.

시험을 위해 행 조립부를 순수 함수(`_quarters_from_series`)로 분리했다 —
개발 환경은 SEC 차단이라 가짜 시계열로만 시험할 수 있다.

## ② 사망 확인서 — BAC 시나리오 전체 시험

106차 실물을 그대로 재현하는 시험을 못박았다:

> 뼈대 행 생성(매출 2.0e10) → 8-K 짝지어짐(발표일 도장) → 92차 규칙
> "매출은 XBRL 우선"에 따라 **쓰레기 8.0 이 밀려나고** XBRL 값이 남는다.
> 조정 EPS 는 보도자료에서 온다 (XBRL 에 없으므로).

분기 목록을 옛 방식으로 되돌리는 돌연변이에서 이 시험이
`쓰레기 매출이 XBRL 을 밀어냈습니다: 8.0` 으로 빨간 불 — **182칸
쓰레기의 사망 경로가 코드로 증명**됐다.

## ③ 돌연변이 — 하나가 또 헛돌았다 (여섯 번째 가짜 초록불)

| 돌연변이 | 결과 |
|---|---|
| 분기 목록을 영업이익만으로 되돌림 | ❌ 2건 (뼈대 소멸 + BAC 쓰레기 부활) |
| 빈 분기도 행 생성 | **처음엔 초록** → 시험·가드 고친 뒤 ❌ |
| 영업이익 없으면 0으로 지어냄 | ❌ `없는 논갭 근사를 지어내면 안 됩니다` |

둘째가 헛돈 이유: 내 시험이 주식보상비에만 날짜가 있는 자료를 썼는데,
주식보상비는 **합집합에 애초에 안 들어간다** — 시험이 아무것도 안
지켰다. 파다 보니 **가드 위치도 틀려 있었다** — 교차검증이 매출을 비워
셋 다 없어진 행을 못 걸렀다. 가드를 검증 뒤로 옮기고, 가드에 실제로
닿는 두 경우(상한 초과 EPS · 매출≤0)로 시험을 다시 썼다.

시험: `python3 run_tests.py` 종료코드 **0** · **469개 통과** (466 → 469).

## ④ 발효와 예상 (짐작은 짐작이라고 적는다)

- **다음 로봇 런부터.** 22종목에 뼈대가 생기고, 8-K 가 거기 짝지어진다
- 예상: `xbrl_orphan` 합계 1,832 → 0 근처 · 매출 쓰레기 182칸 소멸 ·
  22종목이 측정에 정식 편입. **전 가설의 표본이 늘어난다** — 96차처럼
  판정이 흔들릴 수 있고, 그것은 장치가 정직해지는 방향이다
- 실측은 발효 후 계기로 확인한다 (건강검진의 "어제 대비"가 이 수리의
  파급 크기를 자동으로 세어 온다 — 108차 장치의 첫 실전)

# 114차 — 훅의 한계가 실물로 드러났다 (컨테이너 초기화 실측)

**날짜**: 2026-08-19

## ① 무슨 일이 있었나

03:12 UTC 자동 재개 점검에서 컨테이너가 또 초기화된 것을 발견했다.
그런데 이번에는 110차에 만들어 둔 세션 시작 훅이 **발화하지 않았다.**

원인을 확인해 보니 훅의 잘못이 아니라 **구조적 한계**였다:

> 새 컨테이너는 저장소를 **옛 커밋(ccbe870)의 스냅샷**으로 받아 온다.
> 훅 파일(`.claude/hooks/session_start.sh`)은 그보다 **뒤에** 커밋됐으므로,
> 세션이 시작되는 시점에 **훅 파일 자체가 디스크에 없다.**
> 파일이 없으니 발화할 수 없다.

즉 훅은 "컨테이너가 살아 있는 동안의 세션 재시작"에서만 유효하고,
**컨테이너 초기화 직후의 첫 세션은 지키지 못한다** — 하필 복구가 가장
필요한 바로 그 순간이다.

## ② 무엇이 실제로 지켜 줬나

**매시 트리거의 문구였다.** 110차에서 "트리거 문구는 문맥 압축에도
살아남는 가장 질긴 기억"이라며 복구 절차를 실어 둔 것이 이번에 실전으로
증명됐다. 트리거 문구의 수동 절차 그대로 복구했다:

```
git checkout -q -B main origin/main   → 688075a (113차)
bash .claude/hooks/session_start.sh   → 뒤처짐 0 · 앞섬 0 · 미커밋 0
```

복구 **뒤**에는 훅 파일이 생기므로, 같은 컨테이너 안의 다음 세션부터는
훅이 정상 작동한다 (복구 직후 실행으로 확인했다).

## ③ 남기는 규칙

- 자동 복구의 층은 이제 이렇게 이해한다:
  **1층 트리거 문구**(컨테이너 초기화 직후 — 유일하게 항상 살아남음) →
  **2층 훅**(같은 컨테이너의 이후 세션들) → **3층 커밋된 설정**(권한).
- 클론 스냅샷이 어느 커밋인지는 우리가 고칠 수 없다(플랫폼 소관).
  그러므로 **복구 절차는 반드시 트리거 문구에 유지**한다 — 훅이 있다고
  트리거 문구를 줄이면 안 된다.

코드 변경 없음 · 시험 469개 그대로.

---

# 115차 — 조정 EPS 의심 칸을 `부탁_` 원문 요청으로 이었다 (111차의 배관)

**날짜**: 2026-08-19 · 111차 ②에서 "다음 차수 후보"로 예고한 그 배관

## ① 무엇을 만들었나

조정 EPS 는 XBRL 이 못 지키는 유일한 잣대 칸이다(XBRL 에 그 값이 없음).
92·113차의 XBRL 방어망이 매출·GAAP EPS 를 지키는 동안, 조정 EPS 의
연간값 오염(87차에 이름 붙인 결함)은 잡을 자가 없었다 — 111차에서
월가 발표 EPS 라는 심판이 생겼고, 이번에 그 심판을 배관에 이었다.

`vendor_compare.py` 에 두 함수를 추가했다 (73차 GAAP 용과 같은 틀):

- **street_mismatch** — 발표일(±3일)로 짝지어 회사 조정 EPS 와 월가
  발표 EPS 가 1.5센트 넘게 갈린 칸을 센다. 105차 ⑥의 함정을 기억해
  날짜뜻 "발표일" 줄만 쓴다("분기끝" 줄은 발표일이 아니므로 제외).
  **연간값 모양** 판정: 우리 값이 그 발표까지의 월가 4분기 합과 2% 안.
- **wanted_from_street** — 갈린 칸을 `부탁_` 목록으로. 차례는
  ① 연간값 모양 먼저(우리 결함일 확률이 가장 높음 — 정의 차이는 이
  모양이 안 나온다) ② 벌어진 폭 순. 종목당 2건 제한(86차 이유 그대로).

`audit_data.py` 실행부가 이 목록을 야후 GAAP/매출 어긋남 뒤에 잇는다.
값은 지우지 않는다 — **원문을 달라고 적을 뿐이다.**

## ② 실물에 돌린 결과

| | |
|---|---|
| 발표일로 짝지어 1.5센트 넘게 갈린 칸 | 852건 |
| 그중 **연간값 모양** | **23건** (17종목) |
| 부탁 목록에 실린 조정 EPS 칸 | 20건 (전부 연간값 모양) |

연간값 모양 23건은 전부 "우리 값 ≈ 월가 4분기 합"이 소수점 둘째
자리까지 맞는다 (AXP 15.38≈15.39 · EL 3.46≈3.45 · LOW 11.80≈11.82 ·
V 3.48≈3.48 …). 87차 결함의 의심 목록이 처음으로 **종목·발표일
단위로** 만들어졌고, 다음 로봇 런이 이 20건의 보도자료 원문을 담아
오면 파서의 어느 구멍으로 연간값이 들어왔는지 실물로 확인한다.

곁가지 확인: 111차에 산술로 확정했던 FDX 2024-06-25(17.80)는 지금
데이터셋에서 이미 **없음**이다 — 값 검사가 먼저 버렸다("없음"이 안전).
심판 없이도 죽은 칸이 있고, 심판은 살아남은 칸을 잡는다.

## ③ 시험 — 일부러 깨뜨려 본 뒤에만 믿는다 (돌연변이 6종)

| 돌연변이 | 빨간 불 |
|---|---|
| 날짜뜻 필터 제거("분기끝"도 짝짓기) | ❌ 분기끝 시험 |
| 연간모양 판정 끔 | ❌ FDX 재현 시험 |
| 차례에서 연간모양 우선 제거 | ❌ FDX 재현 시험 |
| 종목당 상한 제거 | ❌ 상한 시험 |
| 짝짓기 허용 오차 3→60일 | ❌ 먼 날짜 시험 |
| 1.5센트 관용 제거 | ❌ 일치 시험 |

⚠️ 곁가지 사고: 돌연변이 검사 스크립트가 되돌리기에 `git checkout` 을
써서 **커밋 전이던 새 코드를 지워 버렸다** (돌연변이 2~5가 전부
"함수가 없다"로 죽어서 발각 — 죽은 이유를 안 읽었으면 초록으로 착각할
뻔했다). 다시 만들고 백업 사본으로 되돌리는 방식으로 재검사했다.
규칙으로 남긴다: **커밋 전 코드가 있는 동안 `git checkout <파일>` 금지.**

시험: `python3 run_tests.py` 종료코드 **0** · **474개 통과** (469 → 474).

## ④ 발효

- `wanted_raw.json` 을 다시 적어 커밋했다 (60건 = 야후 어긋남 30 +
  조정 EPS 20 + 이웃 튐 10). 다음 로봇 런부터 이 공시들의 원문이
  `부탁_` 으로 담겨 온다.
- 다음 세션 할 일: 담겨 온 원문으로 연간값이 들어온 파서 구멍을 찾아
  수리 (실물 없이 짐작으로 고치지 않는다).

---

# 116차 — H23 사전 등록: 깊은 게이지 (100차 ⑥에서 예고한 등록)

**날짜**: 2026-08-19

## ① 등록문 (데이터를 보기 전에 적는다)

100차 ③에서 실측한 편향: `decidable` 은 직전 정점 하나만 있으면 참이라
**이력이 짧은 종목은 신고점이 수학적으로 쉽다** (1~5번째 발표 56.8% vs
12번째 이후 40.4%, 단조 감소). 그래서 게이지의 앞머리(표본 초기)가
시장 사실이 아니라 허상으로 부풀고, 그 게이지의 중앙값을 문턱으로 쓰는
H5b 의 자가 오염된다.

> **H23_실적폭_중앙값_깊은게이지** — 게이지의 분자·분모를
> "그 종목의 **8번째 이상** 판단 가능한 발표"로 제한해 계산한 게이지가
> 직전 이력 중앙값을 넘는 상태(H5b 와 같은 규칙)에서 발표된 사건은
> 폭등(SPY 대비 +20%p/60거래일) 비율이 기준선을 웃도는가.

고정하는 것 (전부 이미 등록된 값 — 결과를 보고 고르지 않았다):

| 항목 | 값 | 출처 |
|---|---|---|
| 이력 문턱 N | **8** | 12차 등록 `LADDER_MIN_QUARTERS` (100차 ⑥에서 후보로 지정) |
| 게이지 규칙 | H5b 그대로 (중앙값 초과, 워밍업 52주, 신선 140일, 분모≥10) | 11차·100차 |
| 창·표적·채택 기준 | 60거래일 · +20%p · 윌슨 하한>상한 ∧ n≥10 | 측정 기본형(고정) |
| 등록일 | **2026-08-19** — 판정은 이 날 **뒤** 발표만 | 탐색 규율(헌법 5조) |

탐색 표본(등록일 이전)은 **참고로만** 기록한다 — 100차의 깊이 표를 보고
만든 가설이므로 그 표본으로 판정하면 사후 맞추기다 (H22 와 같은 규율).

## ② 장치 감사(판정 아님) — 깊은 게이지는 앞머리에서 무엇이 달라지나

등록과 별도로, 깊이 제한이 **장치로서** 무엇을 바꾸는지는 지금 데이터로
잰다(원칙 6 — 장치 감사는 언제나 허용, 신호 채택만 새 데이터). 결과는
아래 ④에 적는다.

## ③ 구현과 시험

- `earnings_states` 가 발표마다 **판단횟수**(몇 번째 판단 가능한 발표인가)를
  붙인다. 판단 불가 발표는 세지 않고 값도 없음.
- `gauge_series(min_history=8)` — 깊이 미달 발표를 분자·분모 **양쪽**에서
  뺀다 (분자만 빼면 비율이 거짓말을 한다).
- 사건마다 `h5b_깊은`(깊은 게이지의 H5b 규칙 판정)이 붙고, `judge.py` 의
  `judge_deep_gauge` 가 H22 와 같은 틀(신규만 판정·탐색은 참고)로 기록한다.
  **기존 h5·h5b 는 한 글자도 안 바뀌었다** (등록된 가설의 표본 보존).

돌연변이 4종 전부 빨간 불: 깊이 필터 제거 ❌ · 판단 불가도 깊이에 셈 ❌ ·
등록일 가드 제거 ❌ · h5b_깊은 부착 제거 ❌.

시험: `python3 run_tests.py` 종료코드 **0** · **478개 통과** (474 → 478).

## ④ 장치 감사 결과 — 앞머리 허상이 "없음"으로 바뀐다

실물 10년 데이터로 연도별 게이지 중앙값을 맞대었다 (판정 아님 — 장치 감사):

| 연도 | 기존 게이지 | 깊은 게이지 |
|---|---|---|
| 2017 | **85.7** | **없음** (분모<10) |
| 2018 | **88.6** | **없음** |
| 2019 | 52.8 | **없음** |
| 2020 | 49.2 | 55.6 (연중부터 시작) |
| 2021 | 63.7 | 57.9 |
| 2022 | 56.8 | 53.1 |
| 2023 | 40.0 | 39.0 |
| 2024 | 36.2 | 33.8 |
| 2025 | 35.5 | 36.1 |
| 2026 | 40.0 | 41.5 |

**깊은 게이지는 앞머리 허상을 값으로 고치지 않고 "없음"으로 만든다** —
2017~2019 는 깊이 8 이상 종목이 10개가 안 돼 분모 최소치에 걸린다.
그것이 정직한 답이다: 그 시절 "시장의 폭"은 잴 수 없었다.
2021년 이후 두 게이지는 1~3%p 안에서 나란히 간다 — 깊이 제한이
최근 구간을 왜곡하지 않는다는 뜻이다.

H5b 의 병(96차: 확장 중앙값이 앞머리 85.7%에 영구 오염되어 2022-10 이후
신호 0건)이 깊은 게이지에서는 구조적으로 생길 수 없다 — 오염원이
표본에 아예 없기 때문이다. 사건 수준: h5b 판단 가능 2,695 → h5b_깊은
2,032 (워밍업·분모 조건이 2020년 이후로 밀리며 줄어든 것).

**판정은 하지 않는다.** H23 의 판정 표본은 등록일(2026-08-19) 뒤의 새
발표뿐이고, 첫 의미 있는 표본은 다음 실적 시즌(11월경 n≥10 예상)이다.
verdict.json 에는 다음 로봇 런부터 "판정 불가"로 실리기 시작한다.

---

# 117차 — 건강검진 결과를 계기판에 실었다 (108차의 화면판)

**날짜**: 2026-08-19

108차 건강검진(채움률·이상값·어제 대비)은 로봇 기록(robot_log.json)에만
남고 화면에는 안 보였다. "재료 상태" 펼침 안에 **수집물 건강검진** 절을
추가했다:

- 채움률(행 수·매출·조정EPS·GAAP EPS·매출XBRL) · 자기 이력 1,000배 밖
  이상값(매출·영업이익) · 어제 대비(맞대본/바뀐/새/사라진 분기)를
  로봇 기록의 숫자 그대로 옮겨 적는다.
- 검진이 아직 없으면(옛 코드로 돈 수집) **"아직 없음"** 이라고 말한다 —
  없는 것을 지어내지 않는다. 어제 수집물이 없으면 "어제 대비: 없음".
- 문장 조립을 순수 함수 `health_lines` 로 빼서 화면 없이 시험한다
  (실행 불가능한 위치의 코드 사건 이후 규칙: 새 경로는 실행을 증명한다).

시험 2개 + 돌연변이 2건(없음을 지어냄 ❌ · 어제 대비를 0으로 지어냄 ❌).
412px 실물 렌더링: 제목·"아직 없음" 문구 표시 확인 · **가로 스크롤 없음**.
(현재 로봇 기록에는 검진이 없어 "아직 없음" 경로가 실물이고, 숫자 경로는
시험이 증명한다. 오늘 09:30 런부터 실물 숫자가 실린다.)

시험: `python3 run_tests.py` 종료코드 **0** · **480개 통과** (478 → 480).

---

# 118차 — 대수리 첫 실전 런 검증 (12:30 커밋, head e7a6161)

**날짜**: 2026-08-19 · 예약 런(10:55 시작)이 108·109·112·113·115·116차를
전부 처음 실었다. code_rev **e7a6161** — 오늘 아침까지의 코드 전부다.
아래 숫자는 모두 실행 출력에서 복사했다.

## ① 액면분할 첫 수집 — 검출기 성적표 ✅

공식 기록 **99종목 452건**이 vendor.json 에 담겼다. 112차의 강한 의심
14종목 전부 공식 기록과 일치:

- NVDA 10:1(2024-06) · AVGO 10:1 · GOOGL 20:1 · LRCX 10:1 · WMT 3:1 ·
  TTD 10:1 · POWI 2:1 · ANET 4:1×2 · AMZN 20:1 · TSLA 5:1·3:1 · ISRG 3:1
- **SMCI 10:1(2024-10-01)** — 111차에서 산술로 "×10.04"라고 추정한 것이
  공식 기록으로 확정됐다 (검출기가 옳았다)
- NFLX 10:1(2025-11), NOW 5:1(2025-12) — **최근 분할**이라 환산이 급하다
- GE 0.125:1 — 역분할도 담겼다 (환산 코드는 비율 그대로 곱하므로 안전)

환산 발효는 설계대로 **다음 런부터** (오늘 런은 어제의 vendor.json 을
읽고 돌았다).

## ② H22·H23 첫 기록 ✅ — 그리고 정직하게 적어야 할 숫자 하나

셋 다 verdict.json 에 처음 실렸고 **판정 불가(신규 n=0)** — 등록일 당일
이니 정상. 탐색표본(참고):

| | 신호 | 기준선 |
|---|---|---|
| H22(폭5%+) | 607건 16.0% | 988건 13.1% |
| H22b(폭20%+) | 157건 19.1% | 988건 13.1% |
| **H23(깊은 게이지)** | **435건 6.0%** | **2,106건 14.0%** |

**H23 의 탐색 구간 성적은 기준선보다 나쁘다.** 깊은 게이지로 재도
"장세가 평소보다 좋음" 신호는 옛 표본에서 여전히 밑돈다(H5b 의 7.1% 와
같은 방향). 깊이 제한은 앞머리 허상을 없앴지만(116차 ④) 그것만으로
가설이 살아나는 것은 아니다 — 판정은 규율대로 새 데이터로만 하되,
**참고 숫자가 이 방향인 것을 숨기지 않는다.**

## ③ 건강검진 첫 실전 ✅ — 113차 수리의 파급을 자동으로 세어 왔다

행 5,851 → **5,925** · 채움률: 매출 97.4% · GAAP EPS **95.8%**(92.1%→,
뼈대 효과) · 조정 EPS 55.8% · XBRL GAAP 65.2%.

**어제 대비(첫 계산): 바뀐 칸 2,471 · 새 분기 288 · 사라진 분기 185.**
많이 바뀐 종목 상위가 OXY·JNJ·KLAC·CVX·MRK·XOM·SCHW·C — 정확히
영업이익 미신고로 뼈대가 없던 무리다. 108차 장치가 설계대로 113차
수리의 폭발 반경을 세어 온 첫 실전이다.

이상값: 매출 17건(WDC −1.0 · GS 3.0 · XOM 198.0 — 다음 감사 후보) ·
영업이익 712건(NET·OKTA·ZS 등 0.0 — 0 이 중앙값의 1/1000 미만으로
걸리는 계산 특성, 문턱 등록 때 0 처리 규칙을 정해야 한다).

## ④ 고아 소멸 ✅ — 113차 발효 확정

xbrl_orphan: 매출 **851 → 0** · GAAP EPS **760 → 0** · 매출총이익
**221 → 15**. BAC 매출 40행 전부 XBRL 값(293억~316억 달러),
**8달러 쓰레기 0행**. 은행 무리가 정식 편입됐다. 시간초과 0종목.

## ⑤ 부탁_ 원문 ✅ — 조정 EPS 감사 재료 도착

이번 커밋에 부탁_ 원문 46건. 115차의 조정 EPS 20건 중 **17건 도착**
(AXP·ADBE·AMD·LOW·V·QCOM·EL 2024…). 못 온 3건: WMT 2025-11 은 이미
저장돼 있었고(중복 안 담는 설계), DIOD 2023·EL 2023 은 "44건 확보 후
조기 종료"에 걸린 옛 공시다 — 다음 런의 몫으로 남는다.

**다음 세션 할 일: 이 17건의 원문으로 연간값이 들어온 파서 구멍 수리**
(87차 결함의 첫 실물 감사).

## ⑥ 다음 런(내일 10:55 UTC) 검증 예약

내일이 첫 발효인 것: **분할 환산**(오늘 수집된 452건이 처음 적용 —
NFLX·NOW 최근 분할 종목의 TTM 이 이어지는지) · 부탁_ 잔여 3건.
검증 예약 14:00 UTC.

---

# 119차 — 연간값 오염의 파서 구멍 수리 (부탁_ 원문 17건 첫 실물 감사)

**날짜**: 2026-08-19 · 87차에 이름 붙이고 111차에 산술로 확정하고 115차에
배관을 놓은 그 결함의 **첫 실물 수리**. 118차 ⑤로 도착한 원문 17건이 재료다.

## ① 구멍은 하나가 아니라 여섯이었다

17건 전부 현재 파서로 오염이 재현됐고(17/17), 원인 문맥이 갈라졌다:

| 구멍 | 실물 | 수리 |
|---|---|---|
| 연간 낱말 목록에 **annual** 없음 | ADBE "reported **annual** … non-GAAP diluted EPS of $4.31" | 낱말 추가 (+FY 20xx) |
| 이름과 값 **사이**의 연간 표시 | SWKS "per share **for fiscal year 2018** was $7.22" | 사이 ≤60자 가드 |
| **전망 범위 "X to Y"** 미인지 | LOW "approximately **$11.80 to $11.90**" (연간 전망) | 84차 "X - Y" 가드의 to 판 (양끝) |
| 줄머리 **"Fiscal 20xx"**(year 생략) | SYNA "• **Fiscal 2018** revenue … $4.05" | 줄머리 가드 확장 (quarter 예외) |
| **겹친 머리글** (연간 표시가 윗줄에만) | QCOM "Fiscal 2017 Revenues ⏎ Non-GAAP EPS $4.28" | 윗줄 짧은 제목 검사 |
| **"from $X to $Y" 변화 문구** | AMGN "from $5.31 to $5.29" — 5.31 이 전년, **5.29 가 진짜** | from 짝 예외 + 전년값 가드 |

## ② 전수 채점 — 짐작이 아니라 월가 심판으로

원문 **1,029건 × 2칸(조정·GAAP)** 을 옛/새 파서로 전부 다시 읽어 바뀐
37곳을 월가 발표 EPS(±1.5센트)로 채점했다:

| | 건수 |
|---|---|
| ✅ 틀림 → **월가와 일치** | **8** (ADBE·AAOI·AMGN×2·LOW·SWKS·SYNA·WMT) |
| ⬜ 틀림 → 없음 | 14 ("없음"이 "틀림"보다 안전) |
| 🔶 틀림 → 다른 값 | 14 (FDX 6.88·QCOM GAAP −2.89 등 개선으로 보이는 것 포함) |
| ❌ 잃음 | **1** (SMCI — 옛 값이 **전망 문장**에서 우연히 맞았던 가짜 정답) |

## ③ 채점이 잡아 준 내 실수 두 개 (실물이 규칙을 이긴다)

- **DIS 회귀**: 처음 만든 범위 뒤끝 가드(`숫자 to $`)가 성장 문구
  "increased 16% for **Q3 to $**1.61" 까지 물어 진짜 값을 버리고 전년값
  1.39 를 집었다 → `$소수 to $` 로 좁혀 해소.
- **AMGN 재발견**: "from $5.31 to $5.29" — 옛 파서의 5.31 은 애초에
  **전년값**이었고 진짜는 5.29(월가와 정확히 일치). 범위 가드를 넓게
  걸면 이 5.29 까지 잃는다 → from 짝 예외.

곁가지 정정: 103차 시험이 MDB 조정 EPS 정답을 1.44 로 박아 두었는데,
실물을 다시 보니 1.44 는 **다음 분기 전망표**("$1.44 to $1.48")의
앞끝이었다. 진짜 분기값은 대조표의 1.41 — 시험을 실물 근거와 함께
고쳤다. **당시 동작을 정답으로 박은 시험은 결함도 함께 박는다.**

## ④ 못 고친 것 (숨기지 않는다)

AMD(연간 문단이 앞서는 형식) · AXP·HPQ(연간 전용 표의 열 구조) ·
EL(연간 발표문 헤드라인) · V(문장에 연간 표시가 아예 없음) ·
STRL(CEO 인용문) · QCOM(실적-대-전망 비교표 위험으로 더 못 좁힘) —
전부 **월가 심판 감시망(115차)에 계속 걸려** 있으므로 놓치지는 않는다.
없음으로 바뀐 14건도 다음 수집에서 XBRL/다른 경로로 채워질 수 있다.

시험: 새 가드 6개 각각 돌연변이로 빨간 불 확인 ·
`python3 run_tests.py` 종료코드 **0** · **489개 통과** (480 → 489).

발효: 다음 로봇 런부터. 오염 17건 중 9건이 즉시 정상값/없음으로
바뀌고, 건강검진 "어제 대비"가 그 파급을 세어 온다.

---

# 120차 — 분할 환산 첫 발효 런 검증 + 모델링 중간 평가 (08-20 런, 커밋 a986bab)

검증을 14:00 예약보다 앞당겨 처리했다(주인의 "기다리지 말고 빠르게").
head: 런의 코드는 aeb79b6(119차) — 커밋 계보(aeb79b6→a986bab)와 아래
파서 수리 파급이 실물로 확인해 준다.

## ① 분할 환산 첫 발효 ✅

dataset 이 어제 커밋된 분할 기록(99종목 452건)을 처음 실제로 적용했다.

- **NFLX** (10:1, 2025-11-17): 분할 전 GAAP EPS 5.28→**0.528**,
  5.87→**0.587** 로 환산되고 분할 후 발표(1.23·0.80)는 그대로 —
  시계열이 한 척도로 이어진다. 10배 가짜 급락 소멸.
- **NOW** (5:1, 2025-12-18): 조정 EPS 0.818~0.98 연속, 5배 단차 없음.

## ② 119차 파서 수리 실물 반영 ✅

건강검진 "어제 대비" 바뀐 칸 **2,471 → 212**. 많이 바뀐 종목
(SMCI 23·AMGN 21·MDB 12·FDX 9·NEE 8)이 119차 수리 대상과 일치 —
수리가 이 런에서 실제로 돌았다는 실행 증명. CSCO·AAOI·OKTA 는
새 발표/야후 소급 갱신 쪽. 정상 범위 실측 2일째(2,471→212), 며칠 더
쌓아 경보 문턱을 등록한다.

## ③ 가설 판정 현황 (19개 등록 기준)

- **채택 0 · 미채택 13 · 판정 불가 6.** 표본 신규 3,520 / 전체 4,200.
  (집계는 verdict.json 을 코드로 세어 검산 — 처음 눈으로 센 11·8 이
  틀렸고 검산이 잡았다.)
- H2b(첫돌파) 13.0% vs 기준선 12.8% — 차이 없음.
- **H5b 는 역방향 완전 분리**: 신호 6.9% [4.8, 9.9] vs 기준선 13.3%
  [11.9, 14.8] — 신호 상한 < 기준선 하한. "실적폭이 중앙값을 넘으면
  폭등 확률이 오히려 유의하게 낮다"가 사전 등록 측정에서 나온 사실.
- **H11b(정배열폭 80%)가 유일한 방향성 후보**: 40.0% [29.3, 51.7] vs
  31.6% [30.3, 32.9] — 아직 겹침. 같은 적중률이 유지된다면 분리에
  신호 n≈180 필요(현재 70).
- H22/H22b/H23/H18 신규 표본 0 — 등록 다음날이니 정상. 신규 사건은
  발표 후 60거래일 성숙이 필요해 첫 표본은 이르면 11월.

## ④ 나머지 계기

- 부탁_ 원문: 이번 런 60건/46종목 재편(감시망이 AMD·AXP·HPQ·EL·QCOM·
  NEE·STRL 등 미수리 오염을 계속 요청). DIOD 2023-02-06 · EL
  2023-08-18 은 이번에도 미도착(원문 사이트의 조기 종료 — 목록에 남아
  재시도).
- 이상값: 매출 17건 그대로(WDC −1.0 · GS 3.0 · XOM 198 · MRK 228/310
  — 단위 오파싱 의심, 다음 감사 후보). 영업이익 0값 713건(계산 특성).
- 야후 소급 변경 23칸 · 컨센서스 원장 추가 30건 · 실패 0종목.

## ⑤ 모델링 중간 평가 (주인 요청)

장치는 신뢰 수준에 도달(오염 소멸·분할 환산·고아 0·건강검진·감시망).
반면 **등록 가설 중 채택은 아직 0** — 무조건부 종목 신호만으로는
우위가 없다는 것이 현재까지의 정직한 결론. 헌법 0장의 질문은 원래
**장세 조건부**인데 조건부 결합 가설은 아직 등록된 적이 없다.
제안 5건(유니버스 확대·조건부 가설 등록·회피 신호·단위 감사·EBITDA
채움)을 주인에게 보고했다 — 채택되면 각각 사전 등록 절차를 밟는다.

# 121차 — 장세 조건부 첫돌파 탐색 + H24 사전 등록

주인에게 보고한 제안 ②의 실행. 헌법 0장의 질문("**좋은 장세라는 조건
아래에서만** 첫 돌파가 통하는가")은 조건부인데, 지금까지 등록된 종목
가설은 전부 무조건부였다. 조건부 결합을 처음 실측했다.

## ① 탐색 측정 (조정 EPS 신규 사건 2,326건 — 채택 근거 아님, 전부 기록)

장세 게이지 = **시장 전체 정배열 폭**(33차 aligned_flags 정의를 기준지수
뺀 전 종목에 적용, 새 정의 없음). 발표 사건에 발표일 직전 주의 폭을 붙여
구간별 60거래일 SPY+20%p 적중률을 쟀다:

| 장세폭 | 기준선(전부) | 첫돌파(streak=1) | 신고점폭>5% 첫돌파 |
|---|---|---|---|
| 0~20% | n=1098 · 12.0% | n=85 · **8.2%** | n=44 · 6.8% |
| 20~40% | n=1004 · 14.8% | n=83 · 15.7% | n=47 · 19.1% |
| 40~60% | n=220 · 8.2% | n=24 · **20.8%** | n=16 · 12.5% |
| 60%+ | n=4 | n=1 | n=0 |

- 방향은 헌법 구조와 일치: **죽은 장세(0~20%)에서 첫돌파는 기준선보다
  나쁘고**(8.2% vs 12.0%), 장세가 살아 있을 때만 웃돈다. 무조건부
  H2b(13.0%≈기준선)는 이 둘이 섞여 상쇄된 것이었다.
- H5b(서프라이즈>중앙값)의 나쁨도 죽은 장세에 집중: 0~20%에서 3.7%.
- **그러나 채택 수준 근거는 없다.** 유망 칸의 윌슨 구간이 전부 기준선과
  겹친다: 첫돌파&폭20~60 = 16.8% [10.9, 25.0] vs 기준선 13.6%
  [11.8, 15.7]. 앞/뒤 분할은 13.2%/20.4%로 앞시기가 약하고, 40~60% 칸
  (n=24)은 2017·2020 강세장에 17/24가 몰려 있다(헌법 4조 — 믿지 않는다).
- 시장폭이 60%를 넘는 주는 사실상 없다(사건 4건). 33차 정의가 엄격해
  시장 전체로는 22%가 중앙값, 상위 10%가 39% — H11b의 80% 문턱은 시장
  전체 게이지로는 옮겨 쓸 수 없다.

## ② H24 등록 (띠 20~60%, 등록일 2026-08-20, 신규만 판정)

- 이름: `H24_장세조건부_첫돌파` · 신호 = 첫돌파(streak=1) ∧ 시장폭
  [20%, 60%) · 표본 = 조정 EPS 사건 중 장세폭 판단 가능 · 채택 기준은
  11차 그대로(윌슨 하한>상한, n≥10).
- **띠는 위 표를 보고 골랐으므로 탐색 표본으로는 판정하지 않는다** —
  등록일 뒤의 새 발표만 센다(H22·H23과 같은 틀). 탐색 수치는
  '탐색표본(참고)'로 verdict 에 함께 실린다.
- 현재 시장폭은 12.5~18%(띠 밖)라 당분간 신호 표본은 거의 안 쌓인다 —
  그것 자체가 정보다("지금은 첫돌파를 믿을 장세가 아니다").

## ③ 구현·검증

- `sector_model.market_breadth_series / attach_market_breadth`(장치),
  `judge.judge_regime_breakout`(판정 적용), `collect_job` 배선.
- 새 시험 3개(등록일 가드·띠 경계 [20,60)·직전 주 부착과 None 정직)를
  돌연변이 3종으로 **먼저 빨간 불 확인** 후 통과.
- `python3 run_tests.py` 종료코드 **0** · **492개 통과** (489 → 492).
- 발효: 다음 로봇 런부터 verdict 에 H24 가 실린다.
- 정직 메모: 위 탐색 표는 발견 29종목을 뺀 2,326건이고, verdict 의
  '탐색표본(참고)'는 H22·H23 관례대로 발견 종목을 포함해 신호 n=136 ·
  17.6% [12.2, 24.9]로 찍힌다(둘 다 참고일 뿐 판정과 무관). 판정 표본은
  등록일 뒤의 새 발표라 발견 여부와 무관하게 사후 맞추기가 성립하지
  않는다 — H22·H23과 같은 근거.

## ④ 계기판에 장세 게이지 (121차 이어서)

"② 정배열 폭 지도" 맨 위에 **시장 전체 정배열 폭**을 올렸다. 종목 신호를
읽기 전에 조건부터 보라는 헌법 4조의 화면판이다. 정직화 규칙대로 지금
구간의 **과거 실측 적중률·기준선·H24 판정 상태**를 함께 적는다.

- `app.market_regime_lines(폭, 직전폭, verdict)` — 순수 함수(시험 가능).
  폭이 없으면 "판단 불가", H24 가 verdict 에 없으면 "아직 없음".
- 새 시험 3개를 돌연변이 2종으로 빨간 불 확인 후 통과.
- **412px 실렌더 확인**(헤드리스 크로미움): 실제 표시 =
  "시장 전체 정배열 폭: 12% · 지난주 대비 -6%p / 🟥 약한 장세(20% 미만)
  … 종목 신호를 믿을 근거가 없던 구간입니다 / H24 판정: 아직 없음".
  글자 잘림 없음 · 가로 넘침 없음(scrollWidth ≤ clientWidth).
- `python3 run_tests.py` 종료코드 **0** · **495개 통과** (492 → 495).

---

# 122차 — 유니버스 4차 확장 배치1 +45종목 (2026-08-20, 데이터 도착 **전에** 명단 박제)

주인 지시 "확대진행". 160 → **205종목**. 19차와 같은 절차: **선정 규칙과
명단을 수집 전에 커밋**한다 — 데이터를 보고 명단을 손대면 사전 등록이
무너진다.

## ① 왜 넓히는가 (실측 근거)

121차까지 등록 가설 19개 중 채택 0. 유일한 방향성 후보 H11b는
40.0% [29.3, 51.7] vs 기준선 31.6% — 지금 적중률이 유지된다 해도 완전
분리에 신호 n≈180이 필요한데 현재 70이다. **표본이 병목**이고, 새 가설은
등록일 뒤 발표 + 60거래일 성숙이라 기다림 비용이 가장 크다. 유니버스를
넓히면 과거 표본이 즉시 늘고 축적 속도도 함께 빨라진다.

## ② 선정 규칙 (데이터 보기 전 고정)

1. 미국 상장 · **2016-08 이전 상장**(10년 이력) — 사다리 8분기·창
   60거래일을 채운다.
2. 지금 유니버스에 없는 종목.
3. **업종 편중 시정 우선** — 현재 160종목에 아예 없는 칸(부동산·소재
   화학·보험·유틸리티)과 얇은 칸(필수소비재·운송·리테일)을 채운다.
4. **성과 편향 방지** — 업종마다 부진·급락 이력 회사를 섞는다(원래 29
   종목에 부진주를 넣은 것과 같은 규율). 배치1의 부진·급락·순환 종목:
   BXP·ALB·NUE·FCX·AIG·PCG·KHC·TSN·AAL·DG·DLTR·SPG (12/45 = 27%).
5. **회사 실체가 바뀐 종목은 제외** — COMM 을 뺀 것과 같은 기준.
   실제로 이 규칙에 걸려 뺀 후보: DOW·DD(2019 분할 신설), LIN(2018
   합병 신설), CB(2016 합병), EXC(2022 분할) → 각각 PPG·LYB·ECL·AFL·
   SRE 로 대체했다.
6. **조정지표 발표 여부는 거르는 조건이 아니다** — 잣대 사다리
   (조정 EPS → 조정 EBITDA → GAAP EPS)가 처리하고, 없으면 없음으로
   남긴다. 웹조사로 미리 거르지 않는다(검색 요약은 숫자를 틀린다).

## ③ 명단 (45종목, 박제)

- **부동산 리츠 8**: AMT PLD EQIX SPG O PSA DLR BXP
- **소재·화학 8**: SHW APD PPG LYB ECL NUE FCX ALB
- **보험·자본시장 8**: PGR ALL TRV MET AIG AFL ICE SPGI
- **유틸리티 6**: DUK AEP XEL ED SRE PCG
- **필수소비재 7**: CL KMB MDLZ GIS KHC HSY TSN
- **운송 4**: CSX NSC DAL AAL
- **리테일 4**: TJX DG DLTR ORLY

## ④ 한계 (지우지 않는다)

- **생존 편향**: 이 명단은 지금 존재하는 회사 중에서 골랐다. 상장 폐지·
  피인수로 사라진 회사가 빠져 있어 **절대 적중률은 부풀 수 있다.**
  다만 신호와 기준선이 같은 표본을 공유하므로 **둘의 비교**는 덜
  왜곡된다 — 채택 기준이 비교식인 것이 여기서 값을 한다.
- **새 업종 문법**: 리츠(FFO/AFFO), 보험(운영 EPS·combined ratio),
  유틸리티(조정 EPS·rate base)는 파서가 처음 만나는 형식이다. 19차
  때처럼 **첫 수집 후 오염 감사를 반드시 재실행**한다(정수·자릿수·
  연간값·단위). 특히 리츠의 FFO 는 EPS 가 아니므로 조정 EPS 칸에
  섞이면 안 된다 — 감사 1순위.
- **수집 비용**: 지금 런은 232 CPU분/일꾼 3 = 벽시계 약 77분(예산
  130분·한도 180분, 시간 초과 0건). 새 종목은 캐시가 없어 첫 수집이
  느리다. 그래서 **한 번에 45종목씩** 넣고 다음 런의 시간·실패를 본 뒤
  배치2(+45)를 결정한다.

## ⑤ 구현·검증 (같은 122차, 데이터 도착 전)

- `config.TICKERS` 160 → **205**. `SECTORS`(화면용)에 부동산·소재화학·
  보험·유틸리티 4칸 신설, `GROUPS`(45차 사이클 분류)에 "금리 민감
  임대·인프라 자산"·"규제 요금 전력·가스"·"소재·화학 원자재"·"보험
  인수" 4묶음 신설(전부 [실물·전통] 층). **기존 종목의 분류는 한 칸도
  건드리지 않았다** — 33차·45차 등록이 그 표를 쓰고 있어 사후에 바꾸면
  사전 등록이 무너진다. `CONFIG_VERSION` 27 → 28.
- 새 시험 2개(중복·미분류·층 없음 잡기 / 발견 29종목 보존)를 돌연변이
  3종으로 **먼저 빨간 불 확인**: 분류 한 칸 삭제 → 잡음, 중복 추가 →
  잡음, 발견 종목 삭제 → 잡음.
- 수집 한도: 워크플로 `timeout-minutes` 180 → **300**. 근거는 실측 —
  08-20 런 232 CPU분/일꾼 3 = 벽시계 약 77분, 캐시 적중 15,978 vs 새
  내려받기 17건. 새 45종목은 캐시가 없어 첫 수집이 훨씬 무겁고, 한도를
  넘기면 **그날 수집이 통째로 버려진다**. 일꾼 수는 3 그대로 — 늘리면
  SEC 429(99차 사고).
- `python3 run_tests.py` 종료코드 **0** · **497개 통과** (495 → 497).

## ⑥ 다음 런에서 확인할 것 (배치2 결정 근거)

① 실패 종목 수(티커가 SEC 에서 안 잡히는 경우 — edgartools 해석 실패는
로봇 기록에 남는다) ② 벽시계 시간과 시간 초과 종목 수 ③ 45종목의 잣대
확보 현황(리츠는 FFO 라 조정 EPS 가 없을 수 있다 — 없으면 없음이 정답)
④ **오염 감사**: 리츠 FFO 가 조정 EPS 칸에 섞였는가 · 보험 운영 EPS ·
유틸리티 조정 EPS 의 연간값 오염 ⑤ 건강검진 "어제 대비"가 새 종목
때문에 크게 뛰는 것은 정상(새 분기 수천 개).

# 123차 — 유니버스 4차 확장 배치2 +45종목 (2026-08-20, 데이터 도착 **전에** 명단 박제)

주인 재지시 "확대진행". 205 → **250종목**(제안했던 250~300 목표 도달).
선정 규칙은 122차 ②를 그대로 쓴다 — 규칙을 바꾸지 않는 것이 사전 등록의
뜻이다.

## ① 명단 (45종목, 박제)

- **헬스케어 11** (12 → 23): CVS CI ELV HCA MCK BSX MDT SYK ZBH BIIB ILMN
  — 지금까지 대형 제약·바이오뿐이었고 **보험사·병원·유통·기기**가 통째로
  빠져 있었다. 부진: CVS·ZBH·BIIB·ILMN.
- **금융 8**: USB PNC KEY ZION FITB CME MCO TROW — **예금 기반 지역은행**
  이 0종목이었다. 2023 지역은행 스트레스는 귀한 부진 표본(KEY·ZION).
- **산업재 8**: MMM EMR ITW PH CMI PCAR LMT WM — 부진: MMM.
- **석유가스 6**: PSX VLO MPC HAL EOG KMI — 정유·파이프라인·유전서비스로
  넓힘. 부진: HAL.
- **여행·레저 6** (신설): MAR HLT RCL CCL LVS MGM — 2020 봉쇄로 **거의
  전부 붕괴했다가 회복한** 업종. 지금 유니버스에 없는 국면을 준다.
- **리테일 6**: LULU BBY KSS W ROST AZO — 부진·급락: BBY·KSS·W.

부진·급락·순환 종목 15/45 = 33% (규칙 4 충족).

## ② 규칙 5(실체 변경 제외)에 걸려 뺀 후보 — 대체 내역

- **TFC**(2019 BB&T+SunTrust 합병 신설) → FITB
- **RTX**(2020 Raytheon+UTC 합병 신설) → CMI
- **WBD**(2022 합병 신설)·**PARA**(2019 합병) → 제외, 대체 없음
- **CARR·OTIS**(2020 분할 신설)·**MRNA**(2018 상장)·**CHWY**(2019 상장)
  → 상장·설립 시점 규칙에 걸려 제외
- ⚠️ **HLT** 는 2017 년에 Park·HGV 를 분할해 회사가 작아졌다. 실체가
  바뀐 것은 아니라고 판단해 넣되, **이 판단을 여기 적어 둔다** — 나중에
  이상하면 이 줄을 근거로 다시 본다.
- **EA·TTWO** 는 넣으려다 뺐다. 게임은 기존 어느 묶음에도 안 맞아 새
  묶음이 필요한데 2종목뿐이라 정배열 폭의 최소 인원(3)에 미달한다 —
  "판단 불가"만 만들어 낼 자리다. 대신 리테일 ROST·AZO 로 채웠다.

## ③ 한계 (122차 ④에 더해)

- **여행·레저는 2020 년에 EPS 가 큰 폭 적자**였다. 잣대 사다리는 직전
  정점이 양수일 때만 신고점 폭을 재므로(H22 표본 정의) 이 구간은
  **자동으로 표본에서 빠진다** — 없는 것을 만들지 않는다. 대신 회복
  국면의 첫 흑자 전환이 새 표본으로 들어온다.
- **은행·보험·병원의 보도자료 문법**(운영 EPS·조정 순이자마진·동일점포)
  은 파서가 처음 만나는 형식이다. 122차 ⑥의 오염 감사 목록에 더한다.

## ④ 구현·검증 (같은 123차, 데이터 도착 전)

- `config.TICKERS` 205 → **250**. `SECTORS` 에 "여행레저" 1칸 신설,
  나머지는 기존 칸을 채움. `GROUPS` 에 "의료 보험·유통·병원"(5) ·
  "의료기기"(4) · "예금 기반 은행"(5) · "여행·레저 수요"(6) 4묶음 신설
  — **전부 인원 3 이상**으로 맞췄다(정배열 폭의 최소 인원). 기존 종목의
  분류는 한 칸도 건드리지 않았다. `CONFIG_VERSION` 28 → 29.
- 122차에 만든 유니버스 정합성 시험이 그대로 지킨다(중복·미분류·층 없음·
  발견 29종목 보존). 검산: 총 250 · 중복 0 · 섹터 없음 0 · 묶음 없음 0 ·
  층 없음 0 · **인원 3 미만 묶음 0**.
- `python3 run_tests.py` 종료코드 **0** · **497개 통과**.

## ⑤ 사고 기록 — 컨테이너 초기화가 작업 중간에 끼어들었다

이 회차 작업 도중 컨테이너가 초기화되어 HEAD 가 옛 커밋(ccbe870)으로
돌아갔고, 그 사실을 모른 채 커밋한 123차 사전 등록 글이 **옛 측정결과.md
위에 얹혔다**(972fc73). 푸시 전에 발견해 ① 그 커밋의 diff 에서 새 글만
파일로 뽑아 두고 ② `git checkout -B main origin/main` 으로 복구한 뒤
③ 제자리에 다시 붙였다 — 손실 0.

교훈: **매시 초기화가 실제로 작업 도중에도 일어난다.** 커밋 전에
`git log --oneline -1` 로 base 를 확인하는 습관이 이번에 값을 했다.
(115차의 "커밋 전 코드가 있는 동안 git checkout <파일> 금지"와 짝이 되는
규칙: 되돌리기 전에 **먼저 내용을 파일로 뽑아 둔다**.)

## ⑥ 이번 확장의 총계 (122·123차 합)

160 → **250종목** (+90). 새 업종 칸 5개(부동산·소재화학·보험·유틸리티·
여행레저), 새 사이클 묶음 8개. 다음 런이 첫 수집이며, 122차 ⑥의 확인
목록에 **은행 운영 EPS·병원 동일점포·여행레저 2020 적자 구간**을 더한다.

---

# 124차 — 실적 서프라이즈 해부 + H25 사전 등록 (주인 질문의 실측 답)

주인 질문: "실적 서프라이즈의 기준이 뭐지? 어떤 기준으로 서프라이즈인데도
하락이고 그 반대가 있는지 명확히 분석할 것 — ① 주도섹터였는가 ② 정배열
상태였는가 ③ 과거 실적이 이미 델타 상승을 해오던 것 아닌가."

## ① 이 저장소에서 "서프라이즈"라 부르는 것 (정의 정리)

- **종목 서프라이즈** = (실제 발표 EPS − 월가 추정) / |추정|.
  재료는 vendor.json 발표기록(날짜뜻 "발표일", 추정+실제 12,148건)이고
  ±3일 짝짓기·|추정|<0.10 제외(몇 센트가 수백 %로 부풀므로).
- H5b 는 종목 서프라이즈가 **아니라** 시장 전체 신기록 폭 게이지다.
  120차 보고에서 이를 "종목 서프라이즈"처럼 쓴 표현은 부정확했다 — 정정.

## ② 탐색: 서프라이즈 크기만으로는 아무것도 못 가른다 (2,187건)

| 서프율 | n | 폭등(60일 SPY+20%p) | 하락(초과<0) | 초과 중앙값 |
|---|---|---|---|---|
| 큰 미스(−10%↓) | 90 | 15.6% | 54.4% | −1.2%p |
| 미스(−10~0) | 139 | 10.8% | 57.6% | −2.6%p |
| 소폭 상회(0~5) | 672 | 7.9% | 47.9% | +0.6%p |
| 상회(5~15) | 721 | 11.4% | 49.7% | +0.1%p |
| 큰 상회(15~40) | 401 | 14.0% | 53.4% | −1.3%p |
| 초대형(40%↑) | 164 | 12.8% | 51.2% | −0.3%p |
| **기준선** | **2187** | **11.0%** | **50.6%** | **−0.2%p** |

**어느 칸이든 절반은 하락한다.** "서프라이즈인데 하락"은 예외가 아니라
기본값이다. 크기의 단조 관계도 없다(큰 미스가 폭등률 최고라는 역설 포함).

## ③ 주인 가설 3개의 실측 판정 (큰 상회 15%↑, n=565 를 조건으로 가름)

1. **주도섹터였는가** → **못 가른다.** 주도 O 11.1%(n=45) vs X 13.6%.
   단 주도 판정 장치 자체가 표본 4국면짜리라 이 판정도 약하다.
2. **정배열이었는가** → **방향만 맞다.** 정배열 O 14.6%·중앙값 +1.4%p
   vs X 13.1%·−2.2%p. 구간이 겹쳐 증거는 약하다.
3. **이미 델타 상승 중이었는가(그래서 반영돼 하락?)** → **방향이 반대다.**
   - 사전 60거래일 초과수익(런업)별: <0 → 14.0% · 0~20 → 8.9% ·
     **20%p 이상 → 24.0%**. 미리 달려온 종목의 큰 서프라이즈가 제일 좋았다.
   - 연속 신기록(streak≥2) 13.6% vs **첫 신기록 5.6%**(최악) vs 신기록
     아님 14.9%. "이미 상승해 온 실적"은 벌점이 아니었고, 오히려
     갑작스러운 첫 신기록의 큰 서프가 나빴다.
   즉 "이미 반영됨 → 하락"이 아니라 **추세 지속(모멘텀)** 쪽이 데이터와
   맞는다. 미스 쪽도 같은 결(런업<0 미스 12.3% vs 런업 0+ 미스 14.1% —
   기대가 낮아도 우위는 안 나온다).

## ④ 처음으로 완전 분리된 탐색 칸 (그래서 H25 등록)

기준선 11.0% [9.8, 12.4] 대비:

- **큰 상회(15%↑) ∧ 런업 +20%p↑**: **24.0% [16.8, 33.1]** (n=104) —
  하한 16.8 > 기준선 상한 12.4. 앞/뒤 시기 23.1%/25.0%로 안정,
  연도 전 구간 분포(2020 쏠림 26%), 적중 25건이 21개 종목에 분산.
- **런업 +20%p↑ 단독**: 20.3% [15.7, 25.9] (n=236) — 역시 분리되나
  앞시기 14.4%로 약함.

⚠️ 문턱(15%·20%p)은 표를 보고 골랐으므로 **이 표본으로는 채택하지
않는다**. H25(런업×큰상회)·H25b(런업 단독)로 등록하고 등록일(2026-08-21)
뒤의 새 발표만 판정한다. 한 가지 정직한 주의: 폭등률은 높아도 이 칸의
**하락 비율도 50%다** — "반은 여전히 진다"를 화면에 함께 적어야 한다.

## ⑤ 구현·검증

- `measure_engine.backward_excess / attach_runup`(발표 전 60거래일 초과
  수익 — 결과 창과 겹치지 않음, 이력 부족은 None),
  `vendor_compare.attach_street_surprise`(발표일 기록만 ±3일,
  SURPRISE_MIN_EST=0.10), `judge.judge_momentum_beat`(H25·H25b),
  collect_job 배선(vendor 는 디스크의 어제 파일 — 분할과 같은 하루 늦음).
- 새 시험 4개를 돌연변이 3종(등록일 가드 제거 · 분기끝 필터 제거 ·
  이력 부족 가드 제거)으로 **먼저 빨간 불 확인** 후 통과.
- `python3 run_tests.py` 종료코드 **0** · **501개 통과** (497 → 501).
- 발효: 다음 로봇 런부터 verdict 에 H25·H25b 가 실린다.

---

# 125차 탐색 — 주인 제안 결합(주도 × 완성 직후 × 델타 상승)의 실측

주인 제안: "주도섹터를 가리고 예상하며, 주봉 정배열 완성 직후 종목만
추리고, 예상치 하회라도 실적 델타가 상승 중이었다는 조건을 AND 로."
완성 사건 전수(1,979건, 60거래일 SPY+20%p, 기준선 13.1% [11.7, 14.7])로
쟀다. **좋든 나쁘든 전부 기록한다** (헌법 5조).

## ① 제안 조건들 — 전부 기준선과 겹침

| 조건 | n | 적중 | 구간 |
|---|---|---|---|
| 완성 × 델타 상승 | 860 | 14.3% | [12.1, 16.8] |
| 완성 × 델타 하락 | 561 | 11.6% | [9.2, 14.5] |
| 델타 × 주도섹터(당시) | 87 | 14.9% | [8.9, 23.9] |
| 델타 × 무리(91일 내 같은 묶음 완성 2+) | 729 | 14.4% | [12.0, 17.1] |
| 델타 × 무리 × 주도 | 81 | 11.1% | [6.0, 19.8] |
| 델타 × 직전 발표 미스 | 50 | 14.0% | [7.0, 26.2] |
| 델타 × 직전 발표 상회 | 785 | 13.5% | [11.3, 16.1] |

- 델타 상승은 방향만 맞고(14.3 vs 11.6) 갈라지지 않는다.
- 주도섹터·무리 형성·직전 서프라이즈 부호는 아무것도 더하지 않는다.
- AND 를 겹칠수록 표본만 줄고 적중률은 그대로다.

## ② 완성 사건에서 실제로 가르는 것은 이미 등록돼 있다 (H18)

| 조건 | n | 적중 | 구간 |
|---|---|---|---|
| **이격도 30%+ (H18 신호)** | **407** | **27.8%** | **[23.6, 32.3]** |
| 이격도30 × 델타 | 206 | 27.2% | [21.6, 33.6] |
| 이격도30 × 무리2+ | 333 | 27.9% | [23.4, 33.0] |
| 이격도30 × 델타 × 무리 | 167 | 27.5% | [21.3, 34.8] |
| 이격도30 × 주도 | 48 | 27.1% | [16.6, 41.0] |

이격도(완성 주에 주가가 52주선 위로 30% 이상 — 완성까지 얼마나 힘있게
왔는가) 단독이 27.8%로 기준선과 완전 분리(앞/뒤 27.1/28.4 안정).
**델타·무리·주도를 얹어도 1%p 도 안 움직인다** — 이격도를 알고 나면
나머지 조건은 정보가 없다. 124차(런업이 가르고 서프라이즈는 못 가름)와
같은 결: **가르는 것은 가격 모멘텀이고, 실적 쪽 조건은 그 그림자다.**

## ③ 결론 (등록 없음)

- 새 등록은 하지 않는다 — 최선의 칸(이격도 30%+)은 **H18 로 이미
  등록**돼 있고(2026-08-15, 등록 뒤 새 완성만 판정), 제안된 AND 는
  H18 의 표본만 깎고 적중률을 더하지 못한다.
- 주도섹터 층: 종목 수준에서는 판별력이 실측되지 않았다. 주도 "예상"은
  묶음 수준 장치(H19b 완성 후 확인형)가 이미 새 데이터를 쌓는 중이며,
  그 판정이 서기 전에는 종목 선별에 쓰지 않는다(검증 실패 시 미반영).
- 관찰 노트: 델타×주도 칸은 적중률은 평범해도 초과수익 **중앙값**이
  +2.5%p 로 유일하게 뚜렷이 양수였다 — "크게 터질 확률"이 아니라
  "평균적으로 덜 밀리는" 성질일 수 있다. 표본이 쌓이면 다시 본다.

---

# 126차 — 긴 창 백테스트 + H18b(완성 이격도, 1년 표적) 등록

주인 지적: "60거래일을 왜 계속 쓰나 — 저 조건이 완성되면 최소 1년,
최대 2년 이상 지속 상승한다. 이 조건으로 백테스트를 다시 하라."

## ① 왜 지금까지 60거래일이었나 (변명이 아니라 기록)

11차 사전 등록의 측정 기본형이 60거래일이었고, 긴 창은 **우측 검열**
(창이 안 끝난 최근 사건을 못 셈)이 커서 기본형으로 쓰지 않았다.
이번 실측: 완성 2,026건 중 창 미완료가 60일 47건 · 250일 241건 ·
500일 456건 — 긴 창은 최근 1~2년을 잃는 대가가 실제로 있다. 그래도
완성·모멘텀류 신호의 주장 자체가 "1~2년 지속"이므로 표적을 주장에
맞춰 다시 쟀다.

## ② 백테스트 결과 (SPY 대비, 창 완료분만)

**1년(250거래일), +20%p 문턱(33차 H11 그대로):**

| 조건 | n | 적중 | 구간 | +50%p | 중앙값 |
|---|---|---|---|---|---|
| 모든 완성(기준선) | 1785 | 27.5% | [25.4, 29.6] | 12.0% | −3.0%p |
| **이격도 30%+** | **337** | **46.6%** | **[41.3, 51.9]** | **30.0%** | **+12.5%p** |
| 델타 상승 | 745 | 29.7% | [26.5, 33.0] | 13.3% | −2.9%p |
| 델타 × 주도 | 79 | 22.8% | [14.9, 33.2] | 15.2% | −8.4%p |
| 이격도30 × 델타 | 163 | 46.0% | [38.5, 53.7] | 27.6% | +9.0%p |

**2년(500거래일):** 기준선 33.1% · 이격도30+ **47.1%** (+50%p 34.9%,
중앙값 +9.6%p) — 분리가 2년까지 유지된다.

- **주인 주장이 이격도 30%+ 완성에서 실측으로 확인됐다**: 1년 적중
  46.6%, 앞/뒤 시기 50.0%/43.2%, 전 연도 분포(2017~2025), 2022
  약세장에도 7/18(39%), 적중이 101종목 중 69종목에 분산.
- 정직한 한계: 이 최고의 칸에서도 1년 뒤 SPY 를 이긴 비율은 59.1% —
  "지속 상승 보장"이 아니라 10건 중 4건은 시장에 진다. 그리고 델타×
  주도는 1년 창에서도 기준선 이하(22.8%, 중앙값 −8.4%p)로, 125차의
  결론(주도·델타는 이격도에 정보를 더하지 못함)이 긴 창에서도 성립.

## ③ H18b 등록 (등록일 2026-08-21, 신규 완성만)

- 이름 `H18b_완성이격도_1년` — 신호는 H18 그대로(완성 ∧ 이격도 30%+),
  **표적만 초과250 ≥ +20%p**(33차 1년 문턱 재사용, 새 숫자 없음).
- 위 백테스트는 탐색이므로 판정은 등록일 뒤의 새 완성만 센다. 1년 창
  성숙이 필요해 **첫 판정 표본은 빨라야 2027년 하반기** — 그때까지
  탐색표본(참고)가 verdict 에 함께 실린다.
- 구현: `judge.judge_completion_gap_1y` · `sector_model.H18B_START_DAY`
  · collect_job 배선. 새 시험 1개 돌연변이(등록일 가드 제거)로 빨간 불
  확인 후 통과. `python3 run_tests.py` 종료코드 **0** · **502개 통과**.

---

# 127차 — 주도 후보 관찰판 (판정 전에도 포착은 계속된다)

주인 질문: "26년 말부터 포착할 수 있는 방법이 있나?" — 답: **포착(관찰)과
정식 판정을 분리**한다. 판정은 등록 절차대로 새 데이터를 기다리되,
계기판이 오늘부터 매일 후보를 띄운다 (헌법 3조 — 사실 표시는 허용,
점수·추천은 금지).

## ① 만든 것

계기판 ② 장세 게이지 아래에 **주도 후보 관찰판** 신설:
- **묶음판**: 최근 91일(125차 무리 창 재사용) 정배열 완성을 묶음별로
  집계 — 완성 수·이격도30%+ 수·델타상승 수·마지막 완성일. 신호 수 →
  완성 수 순으로 나열만 한다(125차에서 무리가 적중률을 못 올렸음을
  알므로 점수화하지 않는다).
- **종목판**: 이격도 30%+ 완성 종목(H18·H18b 신호) 나열 + 정직화 캡션
  (1년 실측 46.6%/기준선 27.5%/이긴 비율 59%/채택 전 참고/4할은 진다).
- `recent_completion_rows / leader_watch_rows` 순수 함수, 이격도 없음은
  신호 불인정, 91일 밖 제외.

## ② 검증

- 새 시험 1개를 돌연변이 2종(최근 창 컷 제거 · 이격도 None 을 신호로)
  으로 **먼저 빨간 불 확인** 후 통과. `run_tests.py` 종료코드 **0** ·
  **503개 통과** (502 → 503).
- **412px 실렌더 확인**: 실데이터 표시 — 묶음판 1위 "좌석·계약 정액
  구독 소프트웨어" 완성 7(신호 5·델타 4), 2위 "기기 OEM 반도체" 완성
  7(신호 3·델타 5); 종목판 EXTR 64.7 · CRDO 64.3 · OKTA 56.7 ·
  QCOM 56.3 · DDOG 55.9 · NTAP 50.8 · ZETA 50.7 · LITE 48.7.
  글자 잘림·가로 넘침 없음.

## ③ 시점 정리 (주인 질문의 답 — 기록용)

- 관찰판: **오늘부터** 매일 갱신.
- H18(60일 표적) 첫 정식 판정: 신규 완성 n≥10 차는 대로, 이르면 11월.
- H25(런업×큰상회): 8/21 이후 발표가 60거래일 성숙하는 **11월 중순부터**
  표본이 쌓이기 시작.
- H18b(1년 표적): 2027 하반기. H19b(주도섹터 정식): 국면 단위라 더 뒤.
- 즉 "26년 말" 시점에는 관찰판 + H18 첫 판정 + H25 초기 표본이 있고,
  그 전 구간은 탐색 실측치(46.6% 등)를 참고 딱지로 붙여 쓴다.

---

# 128차 — 모델 전체 점검 스킬(model-verify) 등록 + "델타→이격도" 가설 실측

## ① 점검 스킬 (주인 요청: "누락 대비 안전망")

- `model_verify.py`: 등록 가설 **전부**(judge 상수에서 자동 추출 — 지금
  23개)가 판정 파일에 실려 있는지, 옛 코드 판정인지, 포착 창구(시장 폭·
  관찰판 신호 수)가 살아 있는지 한 번에 검사. 종료코드 0/1.
- `.claude/skills/model-verify/SKILL.md`: 세션이 절차대로 돌리고 해석하는
  스킬로 등록. 새 가설은 judge 상수에 추가되는 순간 자동으로 점검망에
  들어간다 — 등록 시 judge 상수 필수라는 규칙을 스킬에 명시.
- **첫 실행이 바로 값을 했다**: 어제 판정 파일에 H18b·H24·H25·H25b 가
  "빠짐"으로 잡혔다(오늘 등록됐고 아직 런이 안 돎 — 오늘 밤 런에서
  실려야 정상. 안 실리면 배선 누락으로 수리 대상). 옛 코드 경고
  (aeb79b6 → 6ddfd19)도 정확히 떴다.
- 시험 3개, 돌연변이(누락 검사 무력화)로 빨간 불 확인.
  `run_tests.py` 종료코드 **0** · **506개 통과** (503 → 506).
- 세션의 자동 점검(매시 재개·매일 14:00 런 검증)은 그대로 계속한다 —
  이 스킬은 그 위의 이중 안전망이다.

## ② "실적 델타가 나오니까 정배열·이격도가 커지는 것 아닌가?" (주인 가설)

완성 사건 전수로 실측:

- **동반율**: 이격도 30%+ 완성의 68.2%(214/314)가 완성 시점에 이미 델타
  상승 동반. 이격도 <30 완성은 58.4% — **10%p 높다. 방향은 맞다.**
- **뒤따름**: 완성 때 델타가 하락이던 이격도30+ 100건 중 판단 가능한
  93건에서 **94.6%가 6개월 안에 델타 상승으로 전환**했다. 다만 이격도
  <30 쪽도 91.0%라 이 지표의 변별력은 낮다(분기 델타는 원래 자주
  뒤집혀 기저율이 높음).
- 해석(기록): 큰 이격도의 상당 부분은 실제 실적 개선을 미리 반영한
  것이 맞다 — 그러나 **완성 시점에는 그 정보가 이미 주가(이격도)에
  들어 있어** 델타를 조건으로 더 걸어도 예측이 안 좋아진다(126차
  46.6%→46.0%). 시장이 실적을 선행하고, 실적은 뒤따라 확인된다
  (H19b 의 광통신 관찰 — 완성 먼저, 델타 5~61일 뒤 — 와 일치).

---

# 129차 — 계기판 재구성 (주인 지적: 번잡함·중복 카드·구성 종목 안 보임)

주인 지적 4건과 조치:

1. **"화면이 번잡하다 — 필요 없는 로우데이터는 안쪽으로"** →
   메인에 남긴 것: 오늘 요약(신설 4줄) · ① 주도 후보(확인 카드+관찰판
   상위 3+신호 종목 5) · ② 장세(게이지+막대 차트) · ③ 채택된 신호.
   접기로 넣은 것: 한 조건만 채운 묶음 · H14 정직화 경고 · 관찰판
   나머지 묶음·종목 · 섹터별 자세히(12+ 나열) · **사이클 추적 전체** ·
   **섹터 실적 한눈에 전체** (기존 접기: 미채택 상세·용어·한계·재료).
2. **"헬스케어가 (섹터)·(테마) 두 장 — 차이가 뭐냐"** → 두 분류표
   (업종 SECTORS · 사이클 테마)가 같은 구성원이면 수치도 같아 중복이었다.
   `dedupe_confirmations` 로 **같은 수치면 한 장**으로 합침(수치가 다르면
   정보가 다른 것이므로 둘 다 유지).
3. **"헬스케어 어떤 종목인지 어떻게 보나"** → 확인 카드·관찰판 묶음마다
   **"구성 종목 보기" 접기** 신설 — 종목별 최근 완성일·이격도·신호·델타
   태그 표시 (`group_member_rows`, 두 분류표 합집합).
4. **"주도후보판정은 뭐야"** → 이름을 "관찰판"으로 줄이고 ① 머리에
   두 장치의 관계를 한 줄로 설명("확인 신호 = 늦지만 강함 · 관찰판 =
   이르지만 약함").

검증: 새 순수 함수 3개(중복 합치기·구성 종목·오늘 요약) 시험 3개를
돌연변이 2종으로 빨간 불 확인 후 통과. `run_tests.py` 종료코드 **0** ·
**509개 통과** (506 → 509). **412px 실렌더**: 오늘 요약 4줄 →
헬스케어 카드 1장(중복 소멸) → 접기들 → 관찰판 상위 3 → 신호 종목 5,
구성 종목 펼침 실작동(헬스케어: ABBV 07-02 20.9% · AMGN 08-07 23.5%
델타상승 · LLY 29.6% 등), 가로 넘침 없음.

---

# 130차 — 계기판 현대화 + LITE 숨김 사고 수리 + 정배열 유지 목록

주인 지적 4건과 조치:

1. **"증권앱처럼 보기 좋게"** → MODERN_CSS 주입(카드 그리드·둥근 모서리·
   초록 포인트 헤더·배지·크롬 숨김) + 오늘 요약을 **2×2 카드**로
   (`summary_cards_html` 순수 함수): 장세(약하면 빨강) · 확인 신호 ·
   이번 주 새 완성 · 채택/몰리는 묶음.
2. **"LITE 가 맨 위 신호에 안 켜진 이유"** → 켜져 있었으나 **이격도순
   정렬에 밀려 8위 → '나머지 보기' 접기 속에 숨음**. 최신 완성 순
   정렬로 바꾸고 7일 안 완성에 NEW 배지. 수리 후 실렌더: LITE(8/19 ·
   48.7% · 델타상승 · NEW)가 1위. **이번 주에만 이격도30%+ 완성 5건
   (LITE·ONTO·CRDO·TER·S) — 몰림 자체가 관찰거리.**
3. **"CRDO 와 분류가 다른 이유"** → 사이클 묶음은 사업 형태(광부품 vs
   반도체 칩)로 나뉘고, **테마(AI-광통신네트워크)로는 한 식구** —
   신호 줄에 테마를 병기해 화면에서 보이게 함.
4. **"MU·SNDK·AMD 누락"** → 관찰판은 "최근 91일 **새** 완성"만 본다.
   MU(4/24)·AMD(5/1)는 창 밖이고 **셋 다 지금 주봉 정배열이 깨져 있음**
   (MU 이격 89.8%·SNDK 99.7%·AMD 53.4%인데 배열 붕괴 = 조정 중) —
   정직한 부재. 빈틈은 새 목록 **"지금 주봉 정배열 유지 중인 종목
   43개(이격도순)"** 접기로 메움 (`aligned_now_rows`).

검증: today_summary_lines 는 카드로 대체하며 **삭제**(죽은 코드 금지).
새 시험 2개(카드 정직·최신순+NEW)를 돌연변이 2종으로 빨간 불 확인.
`run_tests.py` 종료코드 **0** · **510개 통과** (509 → 510).
412px 실렌더: 카드 4장 → 헬스케어 카드 → 접기들 → NEW 신호 5건 →
유지 43개 접기, 가로 넘침 없음.

---

# 131차 — 계기판 로빈후드풍 개편 (탭 · 히어로 · 종목 행)

주인 요청: "로빈후드와 비슷하게 화면과 기능을 개선."

## 바꾼 것 (전부 표시 계층 — 측정·판정 불변)

1. **탭 내비게이션**: 🏠 주도 · 📈 장세 · 📊 실적 · 🧾 검증 4탭.
   홈 = 주도 후보(카드·관찰판·신호 행·유지 목록), 장세 = 게이지+폭 지도,
   실적 = 사이클 추적+섹터 실적 차트, 검증 = 채택 신호+미채택 상세+
   용어+한계. 같은 내용을 자리만 옮겼다 — 지우지 않았다.
2. **히어로**: 홈 맨 위에 시장 정배열 폭을 큰 숫자(추세색: 하락 빨강·
   상승 초록) + 최근 52주 스파크라인으로 — 로빈후드의 계좌 차트 자리에
   "모든 신호를 읽는 배경"을 놓았다.
3. **종목 행**: 이격도 30%+ 완성 종목을 좌(종목+새완성 초록점+묶음·테마·
   완성일·델타) / 우(초록 알약 +이격도%) 행으로 (`signal_row_html`
   순수 함수). 포인트 색은 로빈후드 초록(#00C805)으로 통일.

## 검증

- 새 시험 1개(알약·점·없음 정직) 돌연변이로 빨간 불 확인.
  `run_tests.py` 종료코드 **0** · **511개 통과** (510 → 511).
- 412px 실렌더: 카드 4장 → 탭 → 히어로 12.5%(빨강)+스파크라인 →
  확인 카드 → 신호 행 5건(LITE +48.7% 알약·초록점 등), 가로 넘침 없음.

---

# 132차 — 별도 웹앱 (GitHub Pages, 무료 정적 호스팅)

주인 승인: "웹앱진행해". 스트림릿 계기판의 한계(느린 첫 로딩·구식 UI·
종목 상세 없음)를 벗어나되 **비용 0원**으로 갑니다 — 서버 없이 로봇이
만든 JSON 만 읽는 정적 페이지입니다.

## ① 구조 (설계도 3장의 경계를 그대로 지킴)

- `web_build.py` — 로봇이 매일 수집·판정을 끝낸 **뒤** 실행. 계기판
  (app.py)이 쓰는 **똑같은 함수**를 불러 `docs/data/` 로 씁니다.
  두 화면이 다른 숫자를 말할 수 없습니다. 여기서 새로 계산하거나
  판정하지 않습니다 — 없으면 null 로 두고 화면이 "없음"이라 적습니다.
  · `app.json` **42KB** (첫 화면 전부) · `t/<종목>.json` 종목당 ~7KB
    (주봉 3년·52주선·완성 이력·실적 이력) — 합계 1.3MB, 눌러 담습니다.
- `docs/index.html · style.css · app.js` — 라이브러리·CDN 없이 순수
  자바스크립트. 차트도 SVG 를 직접 그립니다(바깥 주소를 부르지 않으므로
  차단 환경에서도 뜹니다 — 시험이 못박음).
- 화면: **아래 고정 탭 4개**(주도·장세·종목·검증) + 히어로 큰 숫자 +
  종목 행(초록 알약) + **종목 상세 페이지**(주봉 차트 + 52주선 점선,
  완성 이력 표에 60일·1년 성적, 실적 이력 표).

## ② 로봇 배선

`collect.yml` 에 "웹앱 데이터 만들기" 단계 추가(`continue-on-error` —
웹앱 빌드가 실패해도 **그날 수집 커밋은 막지 않습니다**), 커밋 대상에
`docs/data` 추가, **충돌 재커밋 경로에서도 웹앱 데이터를 백업·복원**
(2026-08-14 수집 유실 사고의 교훈을 그대로 적용).

## ③ 검증

- 새 시험 8개(빌더 6 + 배선 지킴이 2)를 **돌연변이 6종**으로 먼저 빨간
  불 확인: null→0 치환 · 이동평균 앞자리 채움 · 판정 대기 순서 뒤로 ·
  가설 하나 누락 · 워크플로 빌드 단계 제거 · 웹앱이 바깥 주소 호출.
  `run_tests.py` 종료코드 **0** · **519개 통과** (511 → 519).
- **412px 실렌더 4화면 확인**(헤드리스 크로미움, 자바스크립트 오류 0·
  실패 요청 0·가로 넘침 0): 주도(히어로 12.5% 빨강 + 스파크라인 +
  헬스케어 확인 카드 + 관찰판) · 장세(섹터 막대) · 종목(검색 + 정배열
  유지 43종 이격도순) · **LITE 상세**(이격도 48.7% · 주봉/52주선 차트 ·
  완성 이력 11건: 2025-07 +712%p, 2024-08 +135%p 이지만 2020~2022
  완성 5건은 −11~−43%p — **좋은 것만 보여 주지 않습니다**).

## ③-2 곁가지 수리

계기판 설명문의 `**굵게**` 표시가 웹앱에서 별표 그대로 보였다. esc()로
막은 **뒤에** 굵게만 되살리는 `bold()` 를 넣어 해소(남의 글자를 코드로
실행시키지 않는 순서를 지킴). 재렌더로 별표 0건·오류 0건 확인.

## ④ 남은 한 가지 (사람이 눌러야 함)

GitHub Pages 는 저장소 **Settings → Pages → Source: main / docs** 를
주인이 한 번 켜야 켜집니다(권한 문제로 로봇이 못 켭니다). 켜면 주소는
`https://j2h5516-star.github.io/Model/` 입니다. 그 전까지는 로컬에서
`cd docs && python3 -m http.server` 로 볼 수 있습니다.

---

# 133차 — 250종목 첫 수집 런 검증 (08-21, 커밋 bfae7b8 "충돌 재커밋")

head_sha **a2819e0**(131차) — 웹앱(132차)보다 먼저 시작한 런이라 웹앱
빌드 단계가 없다(정상, 첫 웹앱 데이터는 내일 런부터). 10:59:38Z 시작,
14:27:43Z 종료 — **벽시계 약 208분**(122차에 한도를 300분으로 올려 둔
것이 정확히 값을 했다. 옛 한도 180분이었으면 통째로 잃었다).

## ① 확장 첫 수집 결과

- 250종목 전부 수집. **실패 1종목**(HSY) · 시간 초과 72종목(새 종목이
  캐시 없이 받느라 예산에 걸림 — 옛 분기만 못 받고 최신은 확보, 다음
  런이 캐시로 이어받음) · 종목당 중앙값 66.5초 · 합계 547 CPU분.
- 행 5,927 → **9,466** (+60%). 새 분기 3,539개. 판정 표본 신규
  3,520 → **5,289** (+50%).
- **새 90종목 잣대 확보**: 조정 EPS 44 · GAAP EPS 42 · EBITDA 4 —
  전부 사다리에 걸렸다(잣대 없음 0). 리츠·보험·유틸리티도 GAAP 로
  측정 가능.
- 자칫 놀랄 지표 하나를 실측으로 눌렀다: 로봇 보고의 "실적 0건" 59
  종목(AMZN·JPM·TXN 등)은 **데이터 손실이 아니다** — 그 종목들은 XBRL
  병합 경로로 채워지며, 데이터셋에서 확인하니 **비어 있는 종목 0개**.
- 건강검진 어제 대비: 맞대본 5,926 중 **바뀐 칸 7개**(AMAT 4·BILL 3) —
  3일치 실측이 2,471 → 212 → **7** 로 내려왔다. 정상 범위가 잡혔다.
  이상값 매출 17 → 25건(새 종목 유입분, 다음 감사 대상).

## ② 확장 표본에서 다시 잰 유망 신호 (전부 탐색 참고)

종목이 160 → 250 으로 늘며 **탐색 표본이 새 데이터로 반쯤 갈렸는데도
분리가 유지**됐다 (신호 vs 기준선, 윌슨 95%):

| 가설 | 신호 | 기준선 | 판정 |
|---|---|---|---|
| H18b 완성×이격도30 (1년) | **44.0%** [39.3, 48.9] n=402 | 22.2% [20.8, 23.8] | 완전 분리 |
| H18 완성×이격도30 (60일) | **25.9%** [22.2, 30.0] n=486 | 9.4% [8.5, 10.5] | 완전 분리 |
| H25b 런업20+ 단독 | 20.9% [17.5, 24.8] n=469 | 12.5% [11.4, 13.5] | 완전 분리 |
| H25 런업×큰상회 | 23.1% [16.8, 31.0] n=134 | 12.2% [11.0, 13.5] | 완전 분리 |
| H24 장세조건부 첫돌파 | 13.9% [9.5, 20.0] n=165 | 12.4% [11.4, 13.5] | 겹침 |
| H11b 섹터폭 80% | 27.0% [19.3, 36.4] n=100 | 23.9% [22.9, 24.9] | 겹침 |

- 126차 백테스트(160종목, 1년 46.6%)와 250종목 재측정(44.0%)이 2.6%p
  차이로 붙었다 — 새 90종목이 들어와도 무너지지 않았다는 뜻이다.
- **그래도 채택이 아니다.** 이 표는 전부 '탐색표본(참고)'이며 정식
  판정은 등록일 뒤 새 발표로만 한다(H24·H25·H25b·H18b 신규 n=0 —
  등록 이틀째라 정상). 헌법 5조를 여기서 굽히지 않는다.
- H24 는 확장 표본에서도 겹침 — 121차의 "탐색 구간이 겹쳤다"가 재확인.

## ③ 안전장치가 실제로 작동한 기록

로봇이 커밋하려 할 때 내가 132차(웹앱)를 먼저 푸시해 **push 가 거부**
됐고, 워크플로의 재커밋 로직이 최신 main 위에 수집분을 통째로 다시
얹었다("충돌 재커밋"). 결과: 수집물 261파일 갱신 + **웹앱 파일 5개·
docs/data 160종목 전부 보존**. 2026-08-14 에 1시간 48분치 수집을 잃고
만든 장치가 오늘 값을 했다.

---

# 134차 — 새 업종 오염 감사 + 부탁 배관의 구멍 수리

133차에서 미룬 "새 90종목 오염 감사"를 실행했다. 월가 발표 EPS 심판
(vendor_compare.street_mismatch)으로 250종목 전수: 갈린 칸 810건 중
**새 90종목이 199건**.

## ① 업종별 실측 — 걱정한 것과 실제가 달랐다

| 업종 | 갈린 칸 | 연간값 모양 | 우리값이 월가의 1.5배↑ |
|---|---|---|---|
| 리츠 8종목 | **0건** | 0 | 0 |
| 보험 6종목 | 7 | 0 | 2 (MET) |
| 은행 5종목 | 4 | 1 | 2 (KEY) |
| 유틸리티 6종목 | 17 | 0 | 4 (ED 등) |

- **리츠 FFO 오염은 0건이었다** — 122차에 "감사 1순위"로 걱정했으나
  파서가 FFO 를 EPS 칸으로 가져오지 않았다. 걱정이 빗나간 것을 그대로
  적는다(원칙: 잰 것은 좋든 나쁘든 전부 기록).
- **MET 은 우리 잘못이 아닐 가능성이 크다**: 2026-05·08 은 우리 조정
  EPS 와 월가가 정확히 일치(2.42·2.43)하는데 2025-08·11 만 월가가
  **우리 GAAP 값**(1.03·1.22)과 같다 — 야후 쪽 기준이 분기마다 흔들린
  것으로 보인다. 우리 값을 고치지 않고 감시망에 둔다.

## ② 진짜 결함 — **반기·3분기 누적(YTD)** 이 분기 칸에 들어왔다

실물 증거(같은 행의 GAAP 는 월가와 일치 → 조정 EPS 만 오염):

- **ED**(콘에디슨) 2025-08-07: 우리 2.93 · 월가 0.67 · 우리 GAAP 0.68.
  **2.26(Q1) + 0.67(Q2) = 2.93** — 반기 누적.
  2025-11-06: 우리 4.84 = 2.93 + 1.90(Q3) — 9개월 누적.
- **KEY**(키코프) 2025-10-16: 우리 1.09 = 0.33+0.35+0.41 — 9개월 누적.

**기존 누적 차단 장치(73차)가 왜 못 잡았나** — 실측으로 확인:
그 장치는 "값 ≈ **직전 4분기 합**"만 봅니다. ED 의 2.93 은 직전 4분기
합 5.43 과 46% 차이라 통과했고, 지난 중앙값(1.68)도 **이미 오염된 값들**
이라 배수가 1.74 로 문턱 2.5 에 못 미쳤다. 즉 **YTD 사슬은 스스로를
가려 준다** — 앞 칸의 오염이 뒤 칸의 잣대를 부풀린다.

이 결함은 이번 회차에서 **고치지 않는다.** 새 모양의 검사를 눈대중으로
만들면 진짜 분기값까지 지울 위험이 크고(66·73차의 교훈), 지금은
**원문 없이 설계하는 것**이기 때문이다. 대신 아래 ③으로 원문을 확보해
다음 회차에 실물 위에서 만든다. 그때까지 ED·KEY 등은 감시망에 남는다.

## ③ 배관 구멍 수리 — 부탁 목록이 사람 손에만 매여 있었다

**133차 감사에서 발견**: 부탁 목록(wanted_raw.json)에 새 90종목이
**한 건도 없었다.** 원인은 115차 배관이 `audit_data.py` 의 `__main__`
에만 있어 **사람이 직접 돌릴 때만** 갱신됐기 때문이다. 로봇은 매일
목록을 **읽기만** 했으므로, 4차 확장 뒤로 새 오염이 목록에 들어갈 길이
아예 없었다.

- `audit_data.refresh_wanted(quarters, vendor, ...)` 로 같은 로직을
  함수로 빼고(규칙은 한 줄도 안 바꿈), `collect_job` 이 판정 뒤에
  매일 부르게 배선. 실패해도 수집은 계속(감싸고 기록).
- **실행 증명**: 지금 돌려 보니 목록 60건 중 **새 90종목이 32건**
  (직전 0건) — APD·DUK·FITB·CL·AMT·CSX·TSN 등. 다음 런이 이 원문을
  담아 오면 ②의 YTD 수리를 실물 위에서 설계할 수 있다.
- 새 시험 2개(재료 실패해도 목록은 적힘·로봇 배선 존재)를 돌연변이
  2종으로 빨간 불 확인. `run_tests.py` 종료코드 **0** · **521개 통과**
  (519 → 521).

---

# 135차 (2026-08-21) — 부탁 목록의 **네 번째 그물**: 값이 아예 없는 칸

## 왜 시작했나 — 주인의 질문 "제타는 정배열 상태 아니야? 왜 빈칸이지?"

웹앱 종목 상세에서 ZETA 의 실적 델타가 "—" 로 비어 있었다. 화면 결함을
의심했으나 **장치를 먼저 의심하라**(헌법 6조)는 규칙대로 재료부터 봤다.

실물: ZETA 의 잣대(조정 EBITDA)가 **2021-11 다음이 2024-09** — 그 사이
12개 분기가 통째로 비어 있다. 화면은 정직했다. 없는 것을 없다고 적었을
뿐이다. 결함은 **파서가 그 12개 공시에서 아무 값도 못 읽은 것**이다.

## ① 기존 그물 셋이 전부 "틀린 값"만 찾고 있었다

- 야후와 어긋난 칸(vendor_compare) — 값이 있어야 대조가 된다
- 월가와 갈린 조정 EPS(115차) — 값이 있어야 갈린다
- 이웃 분기와 튄 칸(audit_data) — 값이 있어야 튄다

**값이 아예 없는 칸은 어느 그물에도 안 걸린다.** 전 종목 실측 —
잣대가 있는 구간 **안쪽**의 빈 칸 **408개**
(UCTT 23 · AMBA 23 · ROKU 21 · EL 19 · BA 18 · ZETA 12 …).

앞뒤 **바깥**의 빈 칸(상장 전 · 아직 발표 전)은 정상이므로 세지 않는다.
값이 있는 첫 분기와 마지막 분기 **사이**만 본다.

## ② 만든 것 — `wanted_from_holes` + 따로 떼어 둔 자리

- `audit_data.wanted_from_holes(quarters)` — 구간 안쪽 빈 칸을 종목당
  최대 2건씩 모은다. **구멍이 많은 종목부터** 담는다.
- `refresh_wanted` 가 이 목록을 `tail` 로 넘겨 부탁 목록 **맨 뒤**에 붙인다
  (헌법 1조 — "없음은 안전하고 틀림은 위험", 틀린 값이 먼저).

## ③ 그냥 맨 뒤에 붙였더니 **한 건도 못 들어갔다** — 실측이 잡았다

처음 구현은 60칸 목록의 맨 뒤에 붙이는 것이었다. 실데이터로 재 보니:

    부탁 목록에 들어갈 구멍 후보: 60건 / 합친 뒤 60칸 중 구멍이 차지한 자리: 0

틀린 값 후보만 110건(바깥 자 50 + 이웃 튐 60)이라 60칸을 먼저 다 채운다.
**이 배관은 영원히 안 도는 코드였다** — 134차에 고친 것과 똑같은 병이다.

그래서 구멍에게 **따로 자리 15칸**(`HOLE_QUOTA`)을 떼어 준다. 앞 60칸은
그대로 틀린 값 몫이라 우선순위는 지켜지고, 배관은 실제로 돈다. 실행 출력:

    최종 목록: 75칸 / 그중 구멍 15건 → AMBA·BA·EL·GNRC·ROKU·UCTT·VRT·ZETA

주인이 물은 **ZETA 가 실제로 목록에 들어갔다.**

## ④ 순서 편향도 실측으로 잡았다

목록은 날마다 **똑같이 다시** 만들어진다. 알파벳 순으로 자르면 앞 글자
종목만 계속 서비스받고 UCTT·ZETA 는 영원히 차례가 오지 않는다. 그래서
**구멍이 많은 종목부터** 담도록 바꿨다(가장 많이 망가진 델타부터 수리).

## ⑤ 검증

새 시험 5개 — 구간 안쪽만 셈 · 종목당 상한 2 · 목록 맨 뒤 · 틀린 값이
자리를 다 먹어도 들어감 · 많이 빈 종목부터. **돌연변이 5종**(구간 제한
제거 · 상한 제거 · 구멍을 앞으로 · 떼어 둔 자리 제거 · 알파벳 정렬)으로
전부 빨간 불 확인.

첫 시험 작성 때 내가 기대값을 `2020-10·2021-01` 로 잘못 적었는데
**시험이 실제 값(`2020-07·2020-10`)을 들이대 잡아냈다.** 기대값 쪽을
고쳤다. 두 번째 시험은 가상 종목의 값이 8분기에 못 미쳐 잣대가 안
정해지는 바람에 **시험이 헛돌고 있었다**(돌연변이에도 초록). 20분기로
늘려 실제로 돌게 고친 뒤 빨간 불을 확인했다.

`python3 run_tests.py` 종료코드 **0** · **526개 통과** (521 → 526).

## ⑥ 남은 것

- ②의 YTD 누적 오염(ED·KEY)은 여전히 **원문 대기**. 이번 부탁 목록으로
  새 종목 원문이 들어오면 실물 위에서 설계한다.
- 잣대 구멍 408건 중 이번에 부탁하는 것은 15건. 틀린 값이 줄면 자동으로
  구멍 차례가 더 온다 — 사람이 순서를 다시 정할 필요는 없다.

---

# 136차 (2026-08-21) — 건강검진 계기의 **부호 결함**, 그리고 드러난 단위 혼선

## ① 장치를 먼저 의심했다 (헌법 6조)

로봇의 매일 건강검진이 "영업이익 이상값 **1,112칸**"을 몇 회차째 적어
왔는데, 아무도 그 안을 들여다보지 않았다. 이번에 열어 봤다.

**계기가 고장 나 있었다.** 검사는 `값 ÷ 그 종목 중앙값` 을 보는데,
음수 ÷ 양수 = 음수라 **하한(1/1000)보다 언제나 작다**. 즉 **모든 적자
분기가 자동으로 쓰레기로 세어지고 있었다.**

실측: 1,112칸 중 **524칸이 그냥 적자**였다 — 진짜 값이다.

`abs(값) ÷ abs(중앙값)` 으로 고쳤다. 적자냐 흑자냐는 자릿수 문제가
아니다. 실행 출력: **1,112 → 666칸**(오경보 446칸 소멸). 매출 쪽은
25칸 그대로 — 거기 걸린 음수 3칸(WDC −1달러 · FITB −17달러)은 부호가
아니라 **크기**가 무너진 값이라 그대로 걸린다(그래서 시험도 두 방향을
모두 못박았다).

**왜 중요한가**: 오경보가 절반이면 계기를 아무도 안 믿게 되고, 그러면
진짜 결함이 소음에 묻힌다. 실제로 아래 ②가 6회차 넘게 묻혀 있었다.

## ② 쪼개 보니 정체가 드러났다 — **단위 혼선**

총 건수만 적던 것을 **모양별로 쪼개** 적게 했다:

    영업이익 666칸 · 93종목
      이력보다 1000배 이상 작음  499칸
      이력보다 1000배 이상 큼    163칸
      정확히 0                    4칸

표본을 보니 쓰레기가 아니었다. **한 종목 안에서 단위가 섞여 있다.**

- MU 영업이익: 694 ↔ 17.4억 (백만 단위 분기가 섞임)
- TTMI: 68,209 ↔ 54.8억 (천 단위 분기가 섞임)
- **HD 매출 2026-08-18: 47,861 ↔ 4.1조** — **가장 최근 분기**가 백만 단위
- GS 매출은 **29칸 전부** 백만 단위(은행), JPM 은 **십억 단위**

즉 파서가 보도자료 표의 단위 머리글(in millions / in thousands)을
읽지 않고 숫자만 가져온다. 손질(dataset)이 상당수를 지우지만
(마진이 상식 밖이면 없음 처리 · 매출 1만 달러 미만이면 없음 처리),
**통과해 남는 칸이 있고 그 칸은 화면에 틀린 숫자로 뜬다.**

## ③ 다섯 번째 그물 — 자릿수 어긋난 칸의 원문 부탁

135차의 네 그물(야후 어긋남 · 월가 갈림 · 이웃 튐 · 잣대 구멍)에
`audit_data.wanted_from_scale` 을 더했다. **손질을 통과했는데도**
자기 이력과 자릿수가 다른 칸의 원문을 부탁한다. 실행 출력:

    자릿수 어긋남 후보: 7건 · 4종목
      MS  2018-07-18 매출     1,000,000   12,986배
      BA  2020-01-29 매출        76,559  260,975배
      HD  2026-08-18 매출        47,861  736,560배
      HD  2026-08-18 영업이익      7,017  636,027배
      LOW 2018-02-28 매출        15,494 1,345,714배
    원문 부탁 목록 80건 (바깥 자 50 · 자릿수 7 · 잣대 구멍 15)

**값은 고치지 않는다.** 환산 규칙을 원문 없이 지어내면 66·73차의
실수(그럴듯한 규칙이 진짜 실적 307칸을 지울 뻔함)를 되풀이한다.
단위 머리글을 실물로 본 뒤에 파서를 고친다.

재료마다 **제 몫을 먼저 떼어** 넘기게 바꿨다(자릿수 10 · 구멍 15).
135차에 실측한 함정 — 앞 재료가 자리를 다 먹으면 뒤 배관은 영원히
안 돈다 — 이 재료도 같은 함정에 빠질 자리였다.

## ④ 검증

새 시험 6개. **돌연변이 6종**으로 전부 빨간 불 확인:
부호 결함 되살리기 · 모양 이름 뒤바꾸기 · 자릿수 검사에서 부호 무시
빼기 · 자릿수 재료 배선 빼기 · 어긋남 조건 없애기 · 화면 모양별 줄 빼기.

**시험이 헛돌던 것을 또 잡았다**: `자릿수_재료도_제_몫을_받는다` 가
처음엔 손으로 만든 입력만 확인해, 배선을 빼도 초록이었다(돌연변이 4가
안 죽었다). 로봇이 부르는 자리(`refresh_wanted`)의 **출력 파일**을
먼저 보도록 고친 뒤에야 빨간 불이 켜졌다.

`python3 run_tests.py` 종료코드 **0** · **533개 통과** (526 → 533).

## ⑤ 남은 것 (원문 대기)

- YTD 누적 오염(ED·KEY) — 135차부터 대기
- 단위 혼선 93종목 — 이번 부탁으로 HD·BA·LOW·MS 원문 확보 예정
- GS·JPM 은 **종목 전체가** 다른 단위라 자기 이력과 어긋나지 않아 이
  그물에 안 걸린다. 대신 손질의 절대 문턱(매출 1만 달러 미만은 없음)이
  GS 29칸 중 23칸을 지운다. 이것도 원문을 보고 함께 고친다.

---

# 137차 (2026-08-21) — **값은 있는데 날짜가 없어** 빠지던 분기 776개 되찾기

## ① 조용한 손실 — 전체 행의 24.7%에 발표일이 없었다

136차에서 계기를 고치다가 옆의 칸을 봤다. 실측:

    전체 9,466행 중 발표일이 없는 행 2,336행 (24.7%, 211종목)
    그중 **잣대 값은 멀쩡히 있는 행 785개** (66종목)
      FITB 24 · AZO 24 · CMI 22 · TRV 21 · ZBH 21 · ZION 21 · ROST 21 …

측정은 발표일로 창을 잡는다. 그러니 이 785분기는 **실적이 있는데도 한
번도 세어진 적이 없다.** 예외도, 실패 카운터도 없었다 — 108차에 적은
"전부 조용했다"의 또 한 사례다.

## ② 함정을 먼저 확인했다 — 분기끝을 발표일로 쓰면 안 된다

행에는 `filing_date` 라는 날짜가 **2,336행 전부** 있다. 이름만 보면
접수일 같아서 그대로 쓰고 싶어진다. 재 봤다 — 둘 다 있는 7,130행에서
`발표일 − filing_date`:

    중앙 30일 · 90% 39일 · 95% 45일 · 99% 58일 · 최대 117일 · 음수 0건

`filing_date` 는 접수일이 아니라 **분기 종료일**이다(vendor_compare
머리말도 같은 말을 한다). 이것을 발표일로 쓰면 **뉴스가 나오기 한 달
전에 사는 셈**이 되어 모든 가설이 부풀려진다. 108차의 "값은 멀쩡한데
**뜻**이 틀렸다"가 그대로 재현될 자리였다. 쓰지 않는다.

## ③ 바깥 자(야후)의 실제 발표일로 채운다 — 근거는 잰 것이다

야후 기록에는 `날짜뜻: "발표일"` 이 붙은 날짜가 있다. 믿을 만한가?
우리 발표일이 **있는** 7,130행과 맞대 봤다:

    ±3일 안 일치 6,874 · 어긋남 256 → **일치율 96.4%**

시간축으로 쓸 만하다. 규칙은 좁게 잡았다:

- 분기끝 뒤 **0~75일** 안에 야후 발표일이 **정확히 하나**일 때만 채운다
- 우리 발표일이 있으면 **절대 덮어쓰지 않는다**(어긋난 256행은 대조용)
- 채운 행에 `_발표일출처="야후"` 를 남긴다 — 숨기지 않는다

창 75일은 위 분포에서 골랐다. 창별 실행 출력:

    0~60일 → 딱 1개 2,226 · 애매 0        0~90일  → 2,229 · 애매 1
    0~75일 → 딱 1개 2,229 · 애매 0        0~120일 → 1,008 · 애매 1,222

90일을 넘기면 **다음 분기 발표**가 창에 들어와 애매해진다. 75일은 실제
지연의 99.5%를 덮으면서 애매한 건이 0인 자리다.

## ④ 겹침 가드 — 실측이 또 잡았다

처음 판을 돌리니 한 종목 안에서 **같은 발표일이 두 행에 붙은 것이 16건**
생겼다(되찾기 전 1건 → 후 17건). 회사는 한 날에 두 분기를 발표하지
않으므로 둘 중 하나는 틀린 것이다. 어느 쪽인지 모르므로 **이미 쓰인
날짜는 다시 쓰지 않는다.** 가드를 넣으니 겹침이 원래의 1건으로 돌아왔다
(그 1건은 우리 파서가 만든 것으로, 되찾기와 무관하다).

## ⑤ 성적 — 측정 표본이 13% 늘었다

    되찾은 행                     2,213행
    잣대까지 있는 측정 가능 분기   6,175 → 6,951 (+776, +12.6%)
    측정 사건                     5,969 → 6,743 (+774, +13.0%)
    신고점 첫 돌파 사건             473 →   533 (+60, +12.7%)

**판정은 하나도 바뀌지 않았다** (8개 가설 전부 미채택 유지). 표본만
늘었다 — H8 신호 146 → 205, H9 126 → 138. 나머지는 사전 등록일 뒤
데이터만 쓰므로 아직 그대로다. 표본이 느는 것은 채택 기준(윌슨 하한 >
기준선 상한)을 **더 빨리** 넘을 수 있다는 뜻이지, 기준을 무르게 한
것이 아니다. 문턱은 한 줄도 건드리지 않았다.

## ⑥ 부르는 곳이 9군데라 build 안에 넣었다

`dataset.build` 가 인자 없이도 스스로 vendor.json 을 읽게 했다. 인자를
넘겨야만 돌게 하면 화면과 로봇이 **서로 다른 표본**을 보게 된다 — 이
저장소가 실제로 겪은 종류의 사고다. 시험에서 끄려면 `announcements={}`.
로봇은 디스크의 어제치 vendor.json 을 읽으므로 **가장 최근 분기의
발표일은 하루 늦게 붙는다**(액면분할과 같은 조건, 이미 감수한 것).

## ⑦ 검증

새 시험 6개. **돌연변이 5종**으로 빨간 불 확인: 우리 발표일 덮어쓰기 ·
후보 여럿일 때 첫 번째 고르기 · 겹침 가드 제거 · 창 200일로 넓히기 ·
날짜뜻 확인 없이 다 읽기.

**시험이 헛돌던 것을 또 잡았다**(3회차 연속): 겹침 가드 시험이 처음엔
가드를 빼도 초록이었다. 보니 내가 고른 겹침 날짜가 애초에 창 밖이라
창 검사에서 이미 걸렀고, 가드는 한 번도 실행되지 않았다. 날짜를 창
안으로 옮긴 뒤에야 빨간 불이 켜졌다.

`python3 run_tests.py` 종료코드 **0** · **539개 통과** (533 → 539).

## ⑧ 남은 것

- 되찾지 못한 106행 — 야후에도 그 분기 발표 기록이 없다. 비운 채로 둔다.
- 136차의 단위 혼선·135차의 잣대 구멍·YTD 누적(ED·KEY)은 원문 대기.

---

# 138차 (2026-08-21) — **틀린 날에서 창을 시작하던** 사건 90개 바로잡기

## ① 137차가 만든 자를 137차 자신에게 대 봤다

137차에서 "야후 발표일이 우리 것과 96.4% 일치한다"고 적었다. 뒤집어
보면 **3.6%는 어긋난다**. 어긋난 236행은 누가 틀린 것인가?

판별법은 **분기끝 → 발표일 지연**이다. 발표는 결산 뒤에 나오므로
지연이 상식 범위를 벗어나면 그 날짜는 실적 발표일이 아니다.
어긋난 236행에서 양쪽을 견줬다 (실행 출력):

    우리 날짜: 중앙 17일 · 상식 범위(10~70일) 안 146/236
    야후 날짜: 중앙 31일 · 상식 범위 안         216/236

**어긋날 때는 우리 쪽이 틀린 경우가 훨씬 많다.** 실적과 무관한 8-K
(투자설명회·채권발행 같은)를 집은 것으로 보인다. 실물:

    STRL 24 Q2  분기끝 2024-06-30 · 우리 2024-09-18(80일) · 야후 2024-08-05(36일)
    STRL 23 Q1  분기끝 2023-03-31 · 우리 2023-05-31(61일) · 야후 2023-05-01(31일)
    DLR         분기끝 2021-12-31 · 우리 2022-01-03(3일!) · 야후 2022-02-17

**왜 중요한가**: 창을 틀린 날에서 시작하면 그 사건의 수익률은 **다른
기간을 잰 숫자**다. 236행이면 잰 사건의 3.3%가 잡음이었다.

## ② 문턱은 바깥 자가 확인해 준 행에서만 뽑았다

의심스러운 행을 섞어 문턱을 정하면 자가 오염된다. 그래서 **야후와
±3일 안에서 일치한 6,874행**만으로 분포를 냈다:

    0.5% = 10일 · 2.5% = 14일 · 50% = 30일 · 97.5% = 50일
    99.5% = 58일 · 99.9% = 62일 · 최대 117일

범위를 **10~70일**로 잡았다(아래 = 0.5 백분위, 위 = 99.9 백분위에 여유
8일). 문턱 후보별 성적도 재고 골랐다:

    5~90일  → 어긋난 행 28개만 잡음   (확인된 행 20개도 걸림)
    7~80일  → 어긋난 행 52개          (확인된 행 20개)
    10~70일 → 어긋난 행 90개          (확인된 행 31개)

## ③ 버리기만 한다 — 되채우는 것은 137차 배관이 한다

여기서는 **날짜를 지우기만** 한다(창작 금지). 지운 자리는 137차의
발표일 되찾기가 바깥 자 기록으로 채우되, 후보가 정확히 하나일 때만
채운다. A/B 실행 출력:

    가드 끔: 날짜 없는 행 123 · 측정 사건 6,743 · 첫 돌파 533
    가드 켬: 날짜 없는 행 129 · 측정 사건 6,741 · 첫 돌파 533

즉 **128행을 지워 122행이 되채워지고 6행만 날짜 없이 남는다.**
그 대가로 **바깥 자와 어긋나던 행이 236 → 146으로 줄었다**(90개 수리).
잃은 측정 사건은 2개다.

**판정은 하나도 바뀌지 않았다**(8개 가설 전부 미채택 유지, 폭등률도
소수점 첫째 자리까지 그대로). 잡음 90건을 걷어내도 결론이 그대로라는
것은, 지금의 미채택이 **날짜 잡음 탓이 아니라는 뜻**이다.

## ④ 시험 2개가 순서 결함을 잡아냈다

처음엔 이 검사를 행 하나씩 도는 자리에 넣었다. 그러자 **기존 시험 2개가
빨간 불**을 켰다(`test_parse_debris_row_is_dropped`,
`test_same_day_drop_is_recorded_not_silent`).

원인: 잔해 판정과 같은 날 발표 판정은 **발표일로 행을 묶어** 본다.
날짜를 먼저 지우면 그 그물들이 **눈이 멀어** JPM 배당금 잔해 같은
오염이 살아남는다. 검사를 형제 행 규칙들 **뒤로** 옮겨 해결했다.

이번엔 내가 만든 시험이 아니라 **옛 시험이 새 코드의 결함을 잡았다.**
돌연변이 3(검사를 앞으로 도로 옮기기)으로 그 옛 시험이 지금도 이
순서를 지키고 있음을 확인했다.

## ⑤ 검증

새 시험 3개(너무 가깝거나 먼 날짜는 버림 · 상식 범위 안은 안 건드림 ·
버린 자리는 되채워짐). **돌연변이 3종**으로 빨간 불 확인: 배선 제거 ·
범위 0~999일로 무효화 · 형제 규칙보다 앞으로 옮기기.

`python3 run_tests.py` 종료코드 **0** · **542개 통과** (539 → 542).

## ⑥ 남은 것

- 아직 어긋나는 146행 — 지연은 상식 범위 안인데 야후와 다르다. 어느
  쪽이 맞는지 **원문 없이는 모른다.** 건드리지 않고 남긴다.
- 136차 단위 혼선 · 135차 잣대 구멍 · YTD 누적(ED·KEY)은 원문 대기.

---

# 139차 (2026-08-21) — 가격 감사(결함 0건) + **채택까지 얼마나 남았나**

## ① 가격 계층 감사 — 처음으로 재 봤고, 깨끗했다

지금까지 감사는 전부 실적(펀더멘털) 쪽이었다. 그런데 수익률을 실제로
만드는 것은 **주가**다. 한 번도 안 재 봤으므로 쟀다 (실행 출력):

    하루 ±30% 넘는 움직임: 132건 · 57종목
      그중 분할 기록과 같은 날: **0건** → 분할은 제대로 반영돼 있다
    SPY 마지막 날(2026-08-21)보다 5일 넘게 뒤처진 종목: **0개**
    SPY 달력에 있는데 빠진 거래일이 있는 종목: **0개** (총 0일)
    가격이 아예 없는 종목: **0개**

큰 움직임 상위는 전부 진짜 사건이었다 — MXL +76%(2026-04-24) ·
PCG −52%(2019 파산) · AAOI · SNAP · OXY(2020 유가 급락).

**결함 0건.** 좋은 결과도 기록한다(헌법 5조 — 잰 것은 좋든 나쁘든
전부 기록). 이제 "주가 쪽은 안 봤다"는 말은 못 한다.

## ② 주인의 질문에 산수로 답한다 — "언제부터 포착 가능한가"

채택 기준은 **신호 윌슨 하한 > 기준선 상한**이다. 하한은 표본이 늘면
올라가므로, **지금 폭등률이 유지된다면** 몇 개를 더 모아야 넘는지
그냥 계산된다. 새 기준이 아니라 **있는 기준의 산수**다.

실행 출력 (2026-08-21 판정 파일, 표본이 쌓인 11개):

    ┌ 이미 기준을 넘음                          0개
    ├ 표본만 더 모으면 넘을 수 있음              1개
    └ 지금 폭등률로는 표본을 더 모아도 못 넘음   **10개**

    유일한 후보 — H11b(섹터 정배열폭 80% 첫 돌파)
      폭등률 27.0% vs 기준선 상한 24.9% · 지금 n=100
      → **n≈1,684 필요** (지금의 16.8배)

## ③ 이 결과의 뜻 — 기다린다고 되는 것이 아니다

10개 가설은 **실측 폭등률 자체가 기준선 상한보다 낮다.** 윌슨 하한은
아무리 표본이 늘어도 폭등률 위로 못 올라가므로, 표본을 10배 모아도
채택되지 않는다. 예:

    H2_신고점        폭등률 11.6% · 기준선 상한 12.3%  (n=1,177)
    H5_실적폭_고정20  폭등률 11.2% · 기준선 상한 12.4%  (n=3,102)
    H8_GAAPEPS_첫돌파 폭등률  4.1% · 기준선 상한  7.1%  (n=147)

즉 **"신고점을 깬 종목이 평균보다 더 오른다"는 것이 지금 표본에서는
사실이 아니다.** 오히려 살짝 낮다. 137·138차에서 표본을 13% 늘리고
날짜 잡음 90건을 걷어냈는데도 결론이 그대로였다는 점이 이를 뒷받침한다.

⚠️ '못 넘음'은 **영원히**가 아니라 **지금 실측된 폭등률이 그대로라면**
이라는 뜻이다. 표본이 쌓이면 폭등률 자체가 움직일 수 있다. 없는 것을
단정하지 않기 위해 화면 문구도 이렇게 적었다.

## ④ 화면에 올렸다

`judge.needed_sample` · `judge.adoption_distance` · `app.adoption_distance_lines`
를 만들어 점검 탭에 띄운다. 좋은 소식(1개)만 적고 막힌 10개를 빼면
정직화 위반이므로, **막힌 개수를 먼저 적고** 넘을 수 있는 것의 이름과
필요 표본을 그 아래 적는다. 시험이 이 구성을 못박는다.

## ⑤ 검증

새 시험 5개(폭등률이 상한 아래면 None · 필요 표본이 진짜 최소값인지
윌슨으로 되짚기 · 표본 없는 가설은 지어내지 않기 · 이미 넘은 가설 ·
화면이 막힌 가설을 숨기지 않기). **돌연변이 3종**으로 빨간 불 확인:
상한 검사 제거 · 필요표본을 시작 n 그대로 반환 · 화면에서 '못 넘음'
줄 빼기.

두 번째 시험은 계산한 n에서 하한이 **실제로** 상한을 넘는지, 그리고
한 단계 작은 n에서는 **안 넘는지**를 둘 다 본다 — 최소값이 아니면
"16.8배 필요"라는 말이 거짓이 되기 때문이다.

**단위 착오를 스스로 잡았다**: 처음 계산에서 `wilson_interval` 이 이미
%로 돌려주는데 100을 또 곱해 "n≈100이면 충분"이라는 틀린 답이 나왔다.
판정 파일의 하한(19.3%)과 맞대 보니 어긋나 발견했다. 고친 뒤 1,684.

`python3 run_tests.py` 종료코드 **0** · **547개 통과** (542 → 547).

---

# 140차 (2026-08-21) — 표본이 **0인** 가설 10개는 언제부터 생기나

## ① 139차가 못 한 말

139차는 "표본이 쌓인 가설"에 대해 채택까지 남은 표본을 계산했다.
그런데 등록된 23개 중 **12개는 아직 표본이 0**이다. 그 가설들에
대해서는 139차의 계산이 **아무 말도 못 한다** — 화면은 그저 계속
"표본 없음"이라고만 적는다. 주인은 왜 그런지 알 길이 없다.

## ② 첫 표본의 가장 이른 날은 산수로 정해져 있다

사전 등록 규율(헌법 2조)상 **등록일 뒤의 새 신호만** 판정 표본이 되고,
표적을 재려면 그 신호의 **창이 끝나야** 한다. 그러니

    첫 표본 가능일(하한) = 등록일 + 창 길이(거래일)

이보다 이르게는 **어떤 일이 있어도** 표본이 생기지 않는다. 예측이
아니라 규칙에서 바로 나오는 하한선이다. 실행 출력:

    H18_완성시_52주선이격도      2026-08-15 등록 · 60거래일 → 빨라야 2026-11-07
    H19b_주도섹터_완성후확인      2026-08-15 등록 · 60거래일 → 빨라야 2026-11-07
    H20_주도섹터_전환           2026-08-16 등록 · 60거래일 → 빨라야 2026-11-08
    H22_신고점폭_5             2026-08-19 등록 · 60거래일 → 빨라야 2026-11-11
    H22b_신고점폭_20           2026-08-19 등록 · 60거래일 → 빨라야 2026-11-11
    H23_실적폭_중앙값_깊은게이지    2026-08-19 등록 · 60거래일 → 빨라야 2026-11-11
    H24_장세조건부_첫돌파         2026-08-20 등록 · 60거래일 → 빨라야 2026-11-12
    H25_런업_큰상회 / H25b      2026-08-21 등록 · 60거래일 → 빨라야 2026-11-13
    **H18b_완성이격도_1년        2026-08-21 등록 · 252거래일 → 빨라야 2027-08-09**

즉 **올해 11월 중순부터 9개 가설의 표본이 처음 생기기 시작하고,
1년 창인 H18b 는 2027년 8월 전에는 첫 표본조차 없다.**

## ③ 등록일을 모르는 둘은 말하지 않는다

H19(주도섹터 판정)·H21(주도섹터 분기점)은 등록일 상수가 코드에 없다
(판정 함수의 인자로만 들어온다). **날짜를 지어내지 않고 목록에서 뺐다.**
시험이 이 침묵을 못박는다 — 돌연변이로 H19 에 날짜를 넣어 보니 빨간 불.

등록일은 **각 모듈의 상수를 그대로 읽는다.** 여기에 날짜를 다시 적어
두면 한쪽만 고쳐져 화면이 거짓말을 하게 된다(101차에 화면에 박힌 옛
숫자가 거짓이 된 사고와 같은 병).

## ④ 새 시험이 진짜 결함을 잡았다

화면 함수는 "표본이 쌓인 가설이 하나도 없으면" 그 줄만 적고 **바로
돌아가고** 있었다. 그러면 **모든 가설의 표본이 0인 때** — 이 안내가
가장 필요한 때 — 새로 만든 "언제부터 생기나"가 통째로 사라진다.
시험을 먼저 쓰고 돌렸더니 빨간 불이 켜져 발견했다. 구조를 고쳤다.

## ⑤ 검증

새 시험 4개(1년 창 가설의 하한 · 이미 표본 있는 가설은 제외 · 등록일
모르면 침묵 · 화면에 하한선과 그 뜻이 적히는가). **돌연변이 4종**으로
빨간 불 확인: H18b 창을 60일로 · 표본 있어도 목록에 넣기 · H19 등록일
지어내기 · 화면에서 목록 빼기.

`python3 run_tests.py` 종료코드 **0** · **551개 통과** (547 → 551).

## ⑥ 139·140차를 합치면 — 모델의 현재 상태 한 장

    표본이 쌓인 11개 → 10개는 지금 폭등률로는 표본을 더 모아도 못 넘음
                     → 1개(H11b)만 가능하나 표본 16.8배 필요
    표본이 0인 12개 → 10개는 2026-11 중순부터 표본이 생기기 시작
                     → H18b 는 2027-08 부터
                     → H19·H21 은 등록일 상수가 없어 말하지 않음

**지금 화면의 어떤 신호도 매수 근거가 아니다**(전부 미채택). 이것은
숨길 사실이 아니라 화면 맨 앞에 있어야 할 사실이고, 139·140차로
그 근거까지 화면에 올렸다.

---

# 141차 (2026-08-21) — 탐색: **창 길이를 바꿔 가며** 첫 돌파를 다시 재다

⚠️ **이 회차는 전부 탐색입니다.** 탐색은 채택 근거가 아닙니다(헌법 5조).
여기 숫자로 무엇을 판정하거나 화면에 신호를 켜지 않습니다. 잰 것은
좋든 나쁘든 전부 기록한다는 원칙에 따라 남깁니다.

## ① 왜 쟀나

139·140차가 드러낸 것: 표본이 쌓인 11개 가설 중 10개는 **지금 폭등률이
기준선 상한보다 낮아** 표본을 아무리 모아도 채택될 수 없다. 그렇다면
문제는 표본이 아니라 **재는 방식**일 수 있다.

주인이 이미 같은 의심을 말했다(126차): "완성이 되면 최소 1년은 지속
상승하는데 왜 60거래일인가?" 그때는 완성 이격도(H18b) 하나만 1년 창으로
다시 쟀다. 이번에는 **창 길이 자체를 훑었다.**

## ② 실행 출력 — 창 길이별 (조정 EPS 신고점 **첫 돌파**, 폭등 = SPY +20%p)

     창    신호 n    신호%    하한  |  기준%    상한   분리
     60일    304    13.2     9.8  |  10.0    10.7
    100일    295    20.7    16.4  |  15.5    16.4
    125일    295    25.8    21.1  |  17.4    18.4   ✅ **완전 분리**
    150일    285    26.0    21.2  |  20.5    21.5
    250일    269    31.6    26.3  |  25.8    26.9

**125거래일(약 6개월)에서만 채택 기준(하한 > 상한)을 넘는다.**
60일에서는 안 넘고, 250일에서도 안 넘는다. 길수록 좋은 것이 아니라
**창마다 기준선도 같이 올라가기 때문**이다(60일 10.0% → 250일 25.8%).

## ③ 시간 분할 — 강세장 구간만의 우위가 아니다 (헌법 4조)

    중간 2022-07-27 기준
    앞시기 125일  신호 160건 26.2% (하한 20.0) vs 기준 18.4% (상한 19.7)  ✅
    뒤시기 125일  신호 135건 25.2% (하한 18.6) vs 기준 16.3% (상한 17.7)  ✅
    앞시기 250일  33.8% vs 28.1%  ✗      뒤시기 250일  28.4% vs 22.9%  ✗

**양쪽 시기 모두 125일에서 분리된다.** 이것이 이 탐색을 버리지 않고
기록하는 이유다 — 한쪽 시기만의 우위였다면 여기서 끝냈을 것이다.

## ④ 다른 신호는 125일에서도 안 된다 (전 구간)

    H2 신고점(첫 돌파 아님)  125일  19.8% (하한 17.8) vs 17.4% (상한 18.4)  ✗
    H8 GAAP EPS 첫 돌파      125일   8.3% (하한  5.3) vs 17.4%             ✗
    H9 저평가 첫 신기록        125일  17.2% (하한 12.0) vs 17.4%             ✗

즉 **"조정 EPS 잣대의 첫 돌파"에만** 나타난다. 신고점 연속(H2)에는 없고,
GAAP 잣대에는 오히려 반대로 나온다.

## ⑤ 믿지 말아야 할 이유들 (숨기지 않고 적는다)

1. **여러 번 들여다본 표본이다.** 창 5개 × 신호 4개를 훑어 그중 하나가
   분리됐다. 20번 보면 하나쯤은 우연히 분리된다 — 이것만으로는 근거가
   못 된다. 이것이 헌법 5조가 "탐색은 채택 근거가 아니다"라고 못박은
   바로 그 상황이다.
2. **폭등 문턱 20%p 는 60일 창을 위해 정한 값**이다. 다른 창에 그대로
   쓰는 것은 측정 기본형에서 벗어난 것이며, 이 표는 그 사실 위에 있다.
3. 250일 창은 우측 검열로 최근 1년 사건이 빠진다(6,741 → 6,074).

## ⑥ 그래서 무엇을 하나

이 결과로는 **아무것도 판정하지 않는다.** 새 H번호로 사전 등록한 뒤
**등록일 뒤의 새 사건으로만** 판정해야 한다(헌법 5조). 등록·판정 방법은
주인에게 보고하고 정한다 — 특히 "새 데이터"를 시간으로 볼 것인지
(등록일 뒤 발표, 첫 판정 2027년 상반기) 유니버스로 볼 것인지
(탐색에 안 쓰인 새 종목의 이력, 몇 주 안에 판정 가능)는 방법론 선택이라
혼자 정하지 않는다.

---

# 142차 (2026-08-21) — 로봇 일꾼 3 → 6, 그리고 **빨라졌는지 재는 계기**

주인 지시: "유니버스로 검증해. 로봇 일꾼부터 늘려." 순서대로 일꾼부터.

## ① 짐작이 아니라 계기부터 봤다 (91·98·106차 규칙)

일꾼을 올리기 전에 로봇 기록에서 **실제 요청 속도**를 계산했다:

    내려받기 6,890건 ÷ 벽시계 182분 = **초당 0.63 요청**
    SEC 허용 초당 10건의 **6%**

즉 망 쪽은 여유가 아주 크다. 일꾼을 두 배로 올려도 초당 1.3건, 허용치의
13% 다. v1 사고(무절제 병렬 → SEC 403)의 원인은 **속도 무제한**이었지,
일꾼 수 자체가 아니었다.

## ② 그런데 병목이 망만은 아니다 — 이것도 실측했다

느린 종목을 뜯어 보니 두 종류가 섞여 있었다:

    PLD   1,273초 · 내려받기 166건 · 캐시적중   0  → 내려받기당 7.7초 (망 대기)
    V       905초 · 내려받기   0건 · 캐시적중 141  → **내려받기가 없는데 15분**

**비자(V)는 한 건도 안 받아 왔는데 15분이 걸렸다.** 캐시에서 읽어 오는데
느리다는 뜻이고, 곧 **파싱이 계산을 잡아먹는다**는 뜻이다. 전 종목으로
세어 보니 **내려받기가 0인 종목이 158개, 시간 합 242분**(전체 547분의
44%)이었다.

파이썬 스레드는 계산 구간에서 서로 밀어낸다(GIL). 그러니 일꾼을 늘려도
**망 대기 부분만 빨라지고 파싱 부분은 그대로**일 것이다. 얼마나 먹힐지는
지금 알 수 없다 — 그래서 **재는 장치를 같이 넣었다.**

## ③ 넣은 것

- `config.COLLECT_WORKERS` **3 → 6**. 상한을 두는 이유(GIL 경합·v1 사고)를
  주석에 적었고, 다음에 또 올릴 때는 **로봇 기록의 계기를 먼저 보라**고
  못박았다.
- `collect_job` 이 수집 구간의 **벽시계**를 재어 로봇 기록에 남긴다:

      일꾼 · 코어 · 벽시계_분 · 일감합_분 · 겹침배수 · 내려받기 · 초당요청

  **겹침배수 = 일감합 ÷ 벽시계**가 핵심이다. 일꾼 수에 가까우면 병렬이
  먹힌 것이고, 1에 가까우면 서로 밀어낸 것이다. 종목별 seconds 의 합은
  **일꾼이 나눠 쓰기 전의 일감**이라 벽시계가 아니다 — 둘을 함께 남겨야
  판단할 수 있다.
- 러너 코어 수도 함께 기록한다(`os.cpu_count()`). 지금까지는 아무도
  몰랐고, 몰랐으므로 일꾼 수를 정할 근거가 없었다.

## ④ 검증

새 시험 3개 — 일꾼 수가 SEC 허용치를 위협하지 않는가(난간) · 계기가
**로봇 기록에 실리는가**(화면에만 찍고 버리면 다음 세션이 못 본다) ·
겹침배수 계산이 맞는가. **돌연변이 2종**으로 빨간 불 확인(일꾼 40으로
올리기 · 계기를 기록에서 빼기).

`python3 run_tests.py` 종료코드 **0** · **554개 통과** (551 → 554).

## ⑤ 다음 (이 회차에서 하지 않은 것)

- **내일 런에서 계기를 읽고** 일꾼 6이 실제로 먹혔는지 판단한다. 겹침배수가
  3 근처에 머물면 파싱이 병목이라는 뜻이므로, 그때는 일꾼을 더 올릴 것이
  아니라 **파싱 결과 캐시**를 만들어야 한다(158종목 242분이 걸려 있다).
- 유니버스 확대(5차)와 H26 사전 등록은 **일꾼 효과를 확인한 뒤** 한다.
  확대 전에 **먼저 사전 등록을 커밋**해야 한다 — 새 종목 데이터를 보기
  전에 판정 규칙이 못박혀 있어야 표본 외 검증이 성립한다(헌법 2조).

---

# 143차 (2026-08-22) — **H26 사전 등록**: 창 125일 첫 돌파를 새 종목으로 검증

주인 지시(2026-08-22): "유니버스로 검증해."

⚠️ **이 글은 새 종목의 데이터를 한 줄도 보기 전에 적고 커밋합니다.**
보고 나서 정하면 그것은 검증이 아니라 탐색입니다 (헌법 2조).

## ① 무엇을 등록하나

    이름     H26_첫돌파_125일_새종목
    잣대     조정 EPS (사다리가 조정 EPS 로 정해진 종목만)
    신호     TTM 신고점 **첫 돌파** (연속 돌파는 신호가 아님)
    표적     발표 다음 거래일 진입 · **125거래일** 보유 · SPY 대비 **+20%p**
    기준선   **같은 새 종목들의 모든 발표** (같은 잣대·같은 창)
    채택     신호 윌슨 하한 > 기준선 상한, n ≥ 10 (등록된 기준 그대로)
    표본     **탐색에 쓰지 않은 새 종목만** (config.UNIVERSE_V5_NEW)

## ② 왜 125일인가 — 141차 탐색이 근거이되, 근거로 쓰지 않는다

141차에 창 길이를 훑었더니 조정 EPS 첫 돌파가 **125일에서만** 채택
기준을 넘었다(25.8% 하한 21.1 vs 기준 17.4% 상한 18.4). 60일도 250일도
안 넘는다 — 창이 길어지면 기준선도 같이 올라가기 때문이다.

**그런데 그것은 탐색이다.** 창 5개 × 신호 4개를 훑어 하나가 걸린 것이라
스무 번 보면 우연히 하나쯤 나온다. 그래서 그 숫자로는 아무것도 판정하지
않고, **표본을 갈아 끼워 다시 잰다.**

## ③ 왜 "시간"이 아니라 "종목"으로 가르나

시간으로 가르면(등록일 뒤 발표만) 125일 창이 성숙해야 하므로 첫 판정이
**2027년 상반기**다. 종목으로 가르면 새 종목의 **과거 이력**을 쓸 수 있어
**몇 주 안에** 판정된다. 통계적으로도 정당한 표본 외 검증(교차 홀드아웃)이다.

**한계 — 감추지 않는다.** 새 종목의 이력도 탐색과 **같은 달력 구간**
(2016~2026)이다. 종목은 달라도 **장세는 같다.** 그러므로 이 판정이
통과해도 "다른 시대에도 통한다"는 뜻은 아니다. 시간 분할(앞/뒤 시기)을
반드시 함께 보고한다(헌법 4조).

## ④ 골라 담을 수 없는 이유 — 구조적 보장

새 종목 목록은 그 종목들의 실적·수익률을 **보기 전에** 커밋한다. 다짐이
아니라 환경이 강제한다:

- 개발 환경은 SEC·주가가 전부 차단되어 있어 **애초에 볼 수가 없다.**
- 저장소 수집물(snapshot.json)에도 **현 250종목 것만** 있다.

즉 종목을 먼저 정해 커밋하고, 로봇이 받아 와야 비로소 숫자를 본다.
그리고 **한 번 넣은 종목은 빼지 않는다** — 성적이 나쁘다고 빼면 그것은
검증이 아니라 골라 담기다. 이 규칙을 config 주석과 시험에 못박았다.

## ⑤ 만든 것

- `measure_engine.H26_WINDOW_DAYS = 125` · `window_return`/`excess_return`
  에 `days` 인자 추가. **기본값은 60거래일 그대로** — 기존 가설의 표본을
  한 칸도 바꾸지 않는다(실행으로 확인: CSX 첫 사건 초과수익 13.8055 불변).
- 사건마다 `초과125` 를 붙인다. 실행 출력: **사건 6,741개 중 125일 창이
  끝난 것 6,493개** (나머지는 아직 안 끝남 → None, 지어내지 않음).
- `judge.judge_newhigh_125(events, new_tickers)` — 목록이 비면 판정하지
  않고 그 사실을 적는다.
- `config.UNIVERSE_V5_NEW = ()` — 5차 확장 때 채울 자리.
- 로봇(`collect_job`)과 점검기(`model_verify`)에 배선. 기대 가설 **24개**.

## ⑥ 검증

새 시험 7개 — 목록이 비면 판정 안 함 · 탐색 종목은 한 건도 안 셈 ·
창이 안 끝난 사건을 0으로 세지 않음 · 채택 기준을 그대로 씀(채택/미채택/
판정 불가 세 갈래) · 시간 분할 보고 · 새 목록이 현 유니버스와 안 겹침 ·
125일 창이 실제로 125일을 잼.

**돌연변이 3종**으로 빨간 불 확인: 새 종목 걸러내기 제거(시험 4개가
죽음) · 창 안 끝난 것을 0으로 세기 · 창 길이를 60으로 바꾸기.

세 번째 돌연변이는 **처음에 안 죽었다** — 창 길이를 못박는 시험이
없었다. 시험을 추가하고서야 빨간 불이 켜졌다. 그 시험도 처음엔 가상
주가를 잘못 만들어(60일 지점이 이미 갈라져 있었다) 스스로 빨간 불을
냈고, 고친 뒤 통과했다.

`python3 run_tests.py` 종료코드 **0** · **561개 통과** (554 → 561).

## ⑦ 다음

1. 내일 런의 수집 계기(겹침배수)를 보고 일꾼 6의 효과를 판단한다.
2. 그 뒤 유니버스 5차 확장 — 새 종목을 `UNIVERSE_V5_NEW` 에 적고 커밋.
   **목록을 커밋한 뒤에** 로봇이 받아 온다.
3. 받아 온 다음 회차에 H26 첫 판정을 읽는다.

---

# 144차 (2026-08-22) — H26 을 **결론까지 끌고 가려면 몇 종목이 필요한가**

## ① 확장을 하기 전에 계산했다

H26 은 새 종목으로만 판정한다. 그런데 새 종목이 적으면 표본이 모자라
영영 "판정 불가"에 머문다 — **확장을 해 놓고도 답을 못 얻는다.**
그래서 필요한 종목 수를 먼저 쟀다.

지금 재료의 밀도 (실행 출력):

    조정 EPS 잣대이면서 125일 창이 끝난 사건 3,667개 · 158종목
      그중 첫 돌파(신호) 295개 → 잣대종목당 1.87개
      기준선(전체)     3,667개 → 잣대종목당 23.2개
    전체 250종목 중 조정 EPS 잣대는 **158종목(63%)**

## ② 결과 — **새 종목 130개가 최소, 150개면 여유**

141차 탐색 비율(신호 25.8% · 기준 17.4%)이 새 종목에서도 나온다는
가정 아래 (실행 출력):

    새 종목 100개 → 신호 n≈118 · 하한 18.4% vs 상한 19.4%  ✗ 못 넘음
    새 종목 130개 → 신호 n≈153 · 하한 19.2% vs 상한 19.2%  ✅ 겨우 분리
    새 종목 150개 → 신호 n≈177 · 하한 20.1% vs 상한 19.1%  ✅ 분리
    새 종목 200개 → 신호 n≈236 · 하한 20.7% vs 상한 18.8%  ✅ 분리

⚠️ 이 계산은 **탐색 참고**다(헌법 5조). "141차 비율이 새 종목에서도
그대로 나온다"는 가정에 기댄다. 그 가정이 틀리면(= 탐색이 우연이었다면)
어떤 표본 크기로도 분리되지 않는다 — **그것이 바로 이 검증이 답하려는
질문이다.** 그러므로 계획용일 뿐, 판정에는 한 칸도 쓰지 않는다.

## ③ 그래서 일꾼 효과가 **전제조건**이 됐다

새 종목 150개면 유니버스가 **400종목**이 된다. 수집 시간 추정
(지금 250종목 182분 = 종목당 0.73분 기준):

    총 400종목 · 일꾼 3 → **291분** (강제 종료선 300분에 닿음)
    총 400종목 · 일꾼 6이 완전히 먹히면 → **146분** (안전)

즉 **일꾼 6이 실제로 먹히지 않으면 H26 검증 자체가 불가능하다.**
142차에 넣은 겹침배수 계기가 이 판단의 근거가 된다:

    겹침배수 5~6 → 병렬이 먹힘 → 400종목까지 확장 가능
    겹침배수 3 근처 → 파싱이 병목 → **먼저 파싱 캐시를 만들어야 한다**
                     (내려받기 0인 158종목이 242분을 쓰고 있다)

## ④ 내가 손으로 계산하다 틀린 것 — 시험으로 못박았다

처음 계산에서 **잣대 비율 63%를 빠뜨려** "새 종목 80개면 된다"고 적었다.
새 종목 100개를 더해도 그중 63개만 조정 EPS 잣대라 H26 표본이 되는데,
그걸 100개로 세면 **필요한 종목 수를 절반 가까이 과소평가**한다.
다시 계산해 130~150개로 정정했다.

같은 실수가 다시 나지 않게 `judge.sample_plan` 으로 코드화하고, 잣대가
조정 EPS 가 **아닌** 종목을 섞은 시험으로 못박았다. 이 시험은 처음엔
잣대 비율을 무시해도 초록이었다(가상 종목이 전부 조정 EPS였다) —
비율이 0.5가 되도록 고친 뒤에야 돌연변이가 빨간 불을 냈다.

## ⑤ 검증

새 시험 2개(종목이 늘수록 분리에 가까워지고 잣대 비율을 반영하는가 ·
재료가 없으면 지어내지 않는가). 돌연변이 1종(잣대 비율 무시)으로 빨간
불 확인. `python3 run_tests.py` 종료코드 **0** · **563개 통과** (561 → 563).

## ⑥ 다음 회차가 할 일 (순서 고정)

1. 내일 런의 `robot_log["수집계기"]["겹침배수"]` 를 읽는다.
2. 5~6이면 → 새 종목 **150개**를 골라 `UNIVERSE_V5_NEW` 에 적고 커밋.
   3 근처면 → 확장을 멈추고 **파싱 캐시**부터 만든다.
3. 목록을 커밋한 **뒤에** 로봇이 받아 온다. 그다음 회차에 H26 첫 판정.

---

# 145차 (2026-08-22) — **보이지 않던 SEC 접속**을 드러내고 줄이다

## ① 142차에 내가 한 말이 틀렸다 — 실측이 뒤집었다

142차에 "비자(V)는 내려받기 0건인데 905초 → **파싱이 계산을 잡아먹는다**"
고 적었다. 짐작이었다. 이번에 실제로 쟀다:

    저장소의 실물 보도자료 120건으로 parse_press_release 측정
      → 건당 **53ms** · 문지기 검사 1ms
      하루 캐시 적중 15,995건 전부라 해도 **파싱은 14분**

**파싱은 병목이 아니다.** 142차의 진단은 틀렸고, 그 위에 세운 "파싱 캐시를
만들어야 한다"는 계획도 근거를 잃었다.

## ② 진짜 원인 — 성공한 접속만 세고 있었다

같은 조건인데 시간이 100배 차이 난다:

    CRDO  캐시적중  36 · 내려받기 0 · XBRL 13회 →    7.8초
    V     캐시적중 141 · 내려받기 0 · XBRL 13회 →  905.4초

코드를 보니 원인이 나왔다. `_earnings_text_cached` 는 **텍스트를 얻었을
때만** `cache_downloads` 를 올린다:

    text, source, had_exhibit = _earnings_text(filing, report)   ← 접속 발생
    if report is not None and text:
        report["cache_downloads"] += 1                            ← 성공만 셈

8-K 는 실적 말고도 인사·배당·계약 등으로 자주 올라온다. 그런 공시는
받아 봐야 실적 텍스트가 없어 **빈 결과**가 되고, 그러면 **한 건도 안
세어진다.** 비자처럼 8-K 가 잦은 대형사는 이 보이지 않는 접속이 대부분을
차지한다. 그것이 905초의 정체다.

**이 결함은 판단을 오염시켰다.** 142차에 "초당 0.63요청 = SEC 허용의 6%"
라고 적고 그 근거로 일꾼을 3 → 6 으로 올렸는데, **분모가 실제보다 작았다.**
진짜 요청 속도는 그보다 높다(얼마나 높은지는 다음 런이 알려 준다).

## ③ 고친 것

**⑴ 접속 **시도**를 전부 센다** — `fetch_attempts`. 접속 **전에** 올리므로
빈 결과도 빠지지 않는다. 로봇 계기의 `초당요청` 을 이 값으로 재도록
바꿨다(옛 방식은 `초당요청_내려받기만` 으로 함께 남겨 비교 가능).

**⑵ 음성 캐시** — "받아 봤는데 실적 문서가 아님"을 기억해 다음 런에서
다시 받지 않는다. 비자 같은 종목의 반복 접속이 사라진다.

⚠️ 원래 빈 결과를 캐시하지 않은 이유("일시적 망 실패가 '텍스트 없음'으로
**영구 박제**되는 것을 막는다")는 지금도 옳다. 그래서 **예외가 한 건이라도
났으면 기억하지 않는다.** 예외 없이 비어 있던 공시만 기억하고, 판단이
바뀌면 `RAW8K_CACHE_VERSION` 을 올려 통째로 무효화한다.

**⑶ 효과를 재는 칸** — `negative_cached`(새로 기억한 수)·`negative_hits`
(그 덕에 안 받은 수)를 로봇 기록에 남긴다. 줄었는지 봐야 안다.

## ④ 검증

기존 시험 하나가 **빨간 불로 규칙 변경을 알려 줬다** —
`test_raw8k_cache_serves_second_read` 는 "빈 결과는 매번 다시 시도"를
못박고 있었다. 그것을 약화시키지 않고 **두 갈래로 쪼갰다**:

    ⓐ 예외가 났고 비었다  → 매번 다시 시도 (원래 걱정 그대로)
    ⓑ 예외 없이 비었다    → 한 번만 받고 기억 (새 동작)

새 시험 3개(접속 시도를 접속 **전에** 셈 · 예외 시 기억 안 함 · 적중을
따로 셈). **돌연변이 2종**으로 빨간 불 확인: 예외가 나도 기억하기 ·
접속 시도를 안 세기.

`python3 run_tests.py` 종료코드 **0** · **566개 통과** (563 → 566).

## ⑤ 다음 런에서 읽을 것 (판단 순서)

    ① 초당요청(시도 기준) — SEC 허용 10건에 얼마나 가까운가.
       가까우면 일꾼 6이 위험하므로 **되돌린다.**
    ② 음성적중 — 보이지 않던 접속이 실제로 얼마나 줄었나.
    ③ 겹침배수 — 병렬이 먹혔나(400종목 확장 가능 여부).

②가 크면 400종목 확장이 훨씬 가벼워진다. ①이 위험하면 일꾼부터 되돌린다.

---

# 146차 (2026-08-22) — 로봇 판정 경로 **통합 확인** (코드 변경 없음)

137·138·143·145차에 데이터 계층·측정 장치·판정·수집을 모두 손댔다.
각 회차의 시험은 통과했지만, **로봇이 실제로 도는 순서 그대로** 이어
붙였을 때도 도는지는 확인하지 않았다. 헌법 검증 규칙("새 코드 경로는
실제로 실행됨을 증명한다")에 따라 다음 런 전에 확인했다.

## 방법 — 손으로 옮겨 적지 않았다

처음엔 판정 순서를 손으로 재현했는데, `judge.run` 이 전체 판정 묶음을
돌려주는 것을 잘못 알고 `verdict["가설"].update(judge.run(...))` 로 써서
**"가설 17개가 빠졌다"는 가짜 경보**를 냈다. 로봇은 멀쩡했고 내 재현이
틀렸다.

그래서 **collect_job.py 의 판정 블록을 코드에서 그대로 뽑아** 실행했다
(수집 부분만 저장소의 snapshot 으로 대체). 손으로 옮겨 적으면 로봇과
다른 것을 검사하게 되어 초록불이 아무 뜻도 없어진다.

## 결과 (실행 출력)

    판정된 가설: 24 · 기대: 24
    빠진 것: 없음
    판정 집계: 미채택 13 · 판정 불가 11
    H26: 판정 불가 — "새 종목 목록이 비어 있습니다"
    로봇이 쓸 파일: data/measure/verdict.json

- **24개 전부** 판정 경로를 탄다 — H26 배선(143차)이 실제로 돈다.
- H26 은 등록만 되고 표본이 없어 "판정 불가"라고 **이유까지** 말한다.
  확장 전이므로 이것이 정확한 상태다.
- 판정 불가가 10 → 11 로 는 것은 H26 이 늘어서다. 미채택 13 은 그대로 —
  137·138차의 표본 수리가 판정을 바꾸지 않았다는 앞선 확인과 일치한다.

## 남은 위험 하나 (다음 런에서 확인)

이 확인은 **판정 경로**만 돌렸다. 145차에 고친 **수집 경로**(접속 시도
계수·음성 캐시)는 SEC 접속이 필요해 개발 환경에서 돌릴 수 없다. 시험과
돌연변이로 규칙은 못박았지만, 실물 동작은 다음 런의 계기로 확인한다:

    negative_cached 가 0 이면 → 음성 캐시가 한 번도 안 걸린 것 (배선 의심)
    negative_hits 는 첫 런에서 0 이 정상 (기억을 이제 막 만들었으므로),
      **그다음 런부터** 커져야 한다

---

# 147차 (2026-08-22) — 웹앱 데이터가 **어제 것**이었다 (주인 질문: "제타는 정배열인데 왜 화면에 없지?")

## ① 먼저 ZETA 를 실측했다 — 정배열 맞고, 화면에도 나온다

    주봉 정배열 유지 중 (2026-08-14 완성, 2주째)
    52주선 이격도 **50.7%**
    홈 화면 "신호 종목" 목록 **5위** — 펼쳐지는 5개 안에 아슬아슬하게 포함

    1. LITE 08-21 59.8 · 2. ONTO 08-21 37.1 · 3. CRDO 08-21 36.0
    4. TER 08-21 35.8 · **5. ZETA 08-14 50.7**

⚠️ **5위는 위태롭다.** 오늘 완성 종목이 하나만 더 나오면 ZETA 는 6위로
밀려 "나머지 N개 보기"라는 **접힌 칸 안으로 사라진다.** 130차에 LITE 가
정확히 이 자리에서 안 보였다. 정렬이 "최신 완성순"이라 이격도가 더 높아도
완성일이 하루 늦은 종목에 밀린다. (표시 개수 조정은 주인 확인 대기)

## ② 진짜 결함 — 웹앱이 **한 번도 갱신된 적이 없었다**

`docs/data/app.json` 을 열어 보니:

    수집 시각 **2026-08-20** · 종목수 **160개**   (지금은 250종목)

원인: 웹앱 빌더는 **132차에 만들었는데, 그 커밋이 로봇의 마지막 런보다
뒤에 들어갔다.** 그래서 빌더는 아직 한 번도 실행된 적이 없다.

**더 위험한 것**: 워크플로에서 그 단계는 `continue-on-error: true` 다.
빌더가 깨져 있었다면 **아무 소리 없이 영원히 옛 화면**이었을 것이다.
108차에 적은 "전부 조용했다"의 또 한 사례가 될 자리였다.

## ③ 그래서 직접 돌려 실행을 증명했다 (헌법 검증 규칙)

    python3 web_build.py → 종료코드 0
    "웹앱 데이터: app.json 60,116바이트 · 종목 250개 (합계 1.8MB)" · 약 10분

정상 작동한다. 새로 만든 데이터에 ZETA 는 신호종목 5위 · 정배열유지
11위 · 완성전체 10위로 들어 있다. 그 결과를 커밋해 화면을 오늘 것
(수집 2026-08-21 · 250종목)으로 맞췄다.

## ④ 곁가지로 확인한 것 — 완성전체의 "중복"은 결함이 아니다

ZETA 가 완성전체에 두 번(10위·60위) 나온다. 결함이 아니라 사실이다:

    2026-06-26 완성 · 이격도  3.2  → 그 뒤 정배열이 무너짐
    2026-08-14 완성 · 이격도 50.7  → 지금 유지 중

이 목록은 **종목이 아니라 완성 사건**의 나열이다. 같은 종목이 여러 번
완성할 수 있다(KMI 3회 · CRDO·FCX·DE·O·ZM·ETN 각 2회). 열 개 종목이
해당된다.

## ⑤ 남은 위험

빌더가 10분 걸린다. 로봇 예산(130분)·강제 종료(300분) 안이지만,
5차 확장(400종목)을 하면 이 단계도 함께 늘어난다. 다음 런에서
`web_build` 단계의 실제 소요를 확인해야 한다.

`python3 run_tests.py` 종료코드 **0** · **566개 통과** (변동 없음 — 코드
변경 없이 데이터만 갱신).

---

# 148차 (2026-08-22) — 일꾼 6 **성공**(23% 단축), 그러나 시간의 정체는 또 딴 데 있었다

런 id 32566553778 · head_sha **db200b1**(142~147차 전부 반영된 첫 런) ·
수집 2026-08-22T12:50 · 250종목.

## ① 일꾼 6은 확실히 먹혔다 — 계기로 증명

    일꾼 6 · 코어 4
    벽시계 140.8분   (지난 런 182분 → **23% 단축**)
    일감합 835.1분
    **겹침배수 5.93**  ← 일꾼 6 중 5.93명분이 실제로 겹쳐 돌았다

코어가 4인데 겹침이 5.93이라는 것은, 시간의 대부분이 **계산이 아니라
기다림**이라는 뜻이다(기다리는 동안에는 스레드가 서로 안 밀어낸다).
142차에 "파싱(계산)이 병목"이라 짐작했던 것과 정반대다.

## ② 요청 속도 — 안전하다 (다만 아래 ③ 단서)

    8-K 문서 접속 2,325 + XBRL 조회 3,848 = **6,173회**
    → 초당 **0.73요청** (SEC 허용 10건의 **7%**)

142차에 적었던 0.63은 8-K 문서만 센 값이었다. XBRL 조회까지 더해도
7% 수준이라 일꾼 6은 안전하다.

## ③ 그런데 145차의 진단도 틀렸다 — 세 번째 후보가 나왔다

145차에 "보이지 않는 빈 결과 접속이 V의 905초의 정체"라고 적었다.
이번 계기가 그것도 **아니라고** 말한다:

    GS   1,331초 · 캐시적중 200 · **접속 시도 0** · 내려받기 0
    PLD  1,325초 · 캐시적중 166 · 접속 시도 0
    V      901초 · 캐시적중 141 · 접속 시도 0

**접속을 한 번도 안 했는데 22분이 걸렸다.** 음성 캐시도 0건 기억했다
(빈 결과가 한 건도 없었다는 뜻이니 145차의 배선은 정상이고, 그 가설이
틀린 것이다).

그러면 시간은 어디로 갔나? 남은 후보는 **8-K 목록을 얻는 접속**이다
(`get_filings`). GS 는 `8-K 200건까지만 확인했습니다`로 상한에 걸렸는데,
그 200건의 **목록을 얻는 것** 자체가 SEC 접속이고 — 내 계기 어디에도
안 세어진다.

**이번엔 원인을 단정하지 않는다.** 두 번 연속 짐작이 틀렸다. 다음
회차에 목록 조회를 세는 계기를 붙인 뒤에 말한다.

## ④ 웹앱이 로봇 손으로 처음 갱신됐다

    생성 2026-08-22 · 수집 2026-08-22T12:50 · 종목수 **250**

147차에 손으로 채워 넣은 것이 아니라, **로봇이 자기 손으로** 만들어
커밋했다(커밋 673d0c1에 docs/data/app.json 포함). 132차에 만든 빌더가
드디어 자동으로 돈다.

## ⑤ 판정 — 변화 없음

    미채택 13 · 판정 불가 11 (가설 24개, H26 포함)

137·138차의 표본 수리와 142~145차의 수집 변경이 판정을 바꾸지 않았다.
139차의 결론(10개는 표본으로 못 넘음)이 그대로다.

## ⑥ 5차 확장 가능 여부 — **가능하다**

    현재 250종목 · 벽시계 140.8분
    350종목 예상 197분 · **400종목 예상 225분** (강제 종료 300분)

400종목이어도 종료선까지 75분 여유가 있다. 여기에 웹앱 빌더 약 10분과
나머지 단계가 더해지지만 아직 안전하다.

⚠️ 함께 볼 것: **시간 예산(130분)에 걸린 종목이 31개**다. 확장하면 이
수가 늘어 옛 분기를 더 잃는다. 확장과 함께 예산 조정을 검토해야 한다.

---

# 149차 (2026-08-22) — 유니버스 5차 확장: 250 → **401종목** (H26 표본 확정)

주인 지시(2026-08-22): "유니버스로 검증해. 로봇 일꾼부터 늘려."
일꾼은 142차에 올려 148차에 효과를 확인했다. 이제 확장이다.

⚠️ **이 목록은 그 종목들의 실적·수익률을 한 줄도 보기 전에 커밋한다.**

## ① 규모 — 151개 (144차 계산대로)

144차에 **확장 전에** 필요한 규모를 계산해 뒀다:

    새 종목 100개 → 신호 n≈118 · 하한 18.4% vs 상한 19.4%  ✗
    새 종목 130개 → 신호 n≈153 · 하한 19.2% vs 상한 19.2%  ✅ 겨우
    새 종목 150개 → 신호 n≈177 · 하한 20.1% vs 상한 19.1%  ✅

여유 쪽인 **151개**를 넣었다. 130개 미만이면 확장을 해 놓고도 H26 이
영영 "판정 불가"에 머문다 — 그것을 막는 난간을 시험으로도 걸었다.

## ② 고르는 규칙 (결과를 보고 고를 여지가 없음)

- 미국 상장 · SEC 에 실적 8-K 를 내는 회사
- 현 250종목에 **없던** 종목만 (겹침 0건, 시험이 확인)
- 섹터를 폈다: 헬스케어 27 · 산업재 24 · 소프트웨어 19 · 금융 18 ·
  소재화학 17 · 인터넷 12 · 보험 9 · 석유가스 9 · 반도체 8 ·
  반도체장비 6 · 소비재 2
- **새 묶음을 만들지 않고 기존 25개 묶음에만 배정**했다. 묶음을 새로
  만들면 그 묶음의 과거 통계가 없어 비교가 안 된다.

**구조적 보장**: 개발 환경은 SEC·주가가 전부 차단되어 이 종목들의
숫자를 볼 수 없고, 저장소 수집물에도 현 250종목 것만 있다. 목록을 먼저
커밋해야 로봇이 받아 온다. 그리고 **한 번 넣은 종목은 성적이 나빠도
빼지 않는다** — 빼면 그것은 검증이 아니라 골라 담기다.

## ③ 시간 예산 130 → 190분

예산은 **전체 시계**로 재는데 종목이 1.6배가 됐다. 그대로 두면 뒤쪽
종목이 8-K 를 훑기도 전에 잘려 최신 분기까지 잃는다(94차에 적은
"표본을 늘리려다 있던 표본을 깎는 최악").

    실측(148차): 250종목 벽시계 140.8분 · 시간 예산에 걸린 종목 **31개**
    예상(401종목): 벽시계 **226분** · 강제 종료 300분

190분은 8-K 훑기 구간을 넉넉히 덮으면서 웹앱 빌더(약 10분)·주가 수집·
나머지 단계에 여유를 남기는 자리다. 다음 런의 시간초과 종목 수와
벽시계를 보고 다시 조정한다 — 짐작하지 말고 계기를 볼 것.

## ④ 동어반복 시험을 하나 잡았다

143차에 쓴 `새_종목_목록은_현_유니버스와_겹치지_않는다` 는 **무슨 값을
넣어도 통과**했다. 전체 목록에서 새 목록을 빼고 그것과 비교했으니
당연했다. 지금은 **구조**를 쓴다 — config 가 새 종목을 목록 **맨 뒤에**
붙이므로 앞쪽 조각이 확장 전 유니버스이고, 거기 새 종목이 하나라도
있으면 빨간 불이다. 돌연변이(새 목록에 NVDA 넣기)로 확인했다.

## ⑤ 검증

    종목 401 · 섹터 401 · 묶음 401 · 중복 0 · 층 빠짐 0
    CONFIG_VERSION 30 · UNIVERSE_V5_NEW 151 · 예산 190분

`python3 run_tests.py` 종료코드 **0** · **567개 통과** (566 → 567).

## ⑥ 다음 런에서 볼 것

1. **벽시계** — 226분 예상. 300분에 닿으면 일꾼을 8로 올리거나 예산을 줄인다.
2. **시간초과 종목 수** — 31개보다 크게 늘면 예산이 부족한 것.
3. **새 151종목의 수집 성적** — 실패가 많으면 그만큼 H26 표본이 준다.
4. 그다음 회차에 **H26 첫 판정**을 읽는다.

---

# 150차 (2026-08-22) — 사라진 589분기는 **손실이 아니라 개선**, 새 업종 오염 감사, 원문 자리 재분배

08-22 런 검증의 남은 숙제(④⑥⑦)를 끝냈다. 그러면서 부탁 배관에서
결함 하나를 실측으로 찾아 고쳤다.

## ① 사라진 589분기 — 짐작하지 않고 어제 파일과 직접 맞댔다

건강검진이 "사라진 분기 589 / 새 분기 589"를 처음으로 냈다(그동안 0).
총 행 수는 9,466으로 어제와 같았다. 짐작하지 말고 어제 수집물을 git 에서
꺼내 대조했다(`git show 673d0c1^:data/measure/snapshot.json`).

    어제 9465 · 오늘 9465 · 사라짐 589 · 새것 589
    사라진 분기가 많은 종목: BIIB 20 · KMB 19 · AEP 19 · CL 18 · AFL 18 ·
                          NSC 18 · TRV 17 · SYK 17
    [KMB] 어제 40행 · 오늘 40행
      어제: ('16/12', 2016-12-31, 발표일 없음, 조정EPS 없음)
      오늘: ('16 Q4', 2016-12-31, 2017-01-24, 1.45)

**KMB 가 답이다.** 어제 것은 XBRL 만으로 만든 빈 껍데기 행이었고,
오늘 것은 기간 라벨·**실제 발표일**·**실적 값**이 다 들어찬 행이다.
건강검진의 짝짓기 열쇠가 `(종목, 발표일 or 분기끝)`이라, 발표일이
"없음 → 실제 날짜"로 바뀌면 **사라짐 + 새로 생김**으로 세어진다.

즉 589건은 **잃은 것이 아니라 채워진 것**이다. 134~136차의 원문 부탁
배관과 시간 예산 여유 덕에 로봇이 그 분기들의 보도자료를 처음 읽어냈다
(파싱 실패 원문도 914 → 1,013건으로 늘었다 = 더 많이 시도했다는 뜻).

> **남길 것**: 건강검진의 "사라진 분기"는 손실 지표가 아니다. 발표일이
> 채워질 때도 같은 값이 오른다. 다음에 이 숫자가 크게 뜨면 **먼저
> 어제 파일과 맞대 볼 것** — 놀라기 전에 대조.

## ② 24개 가설 전부 실렸다 (⑥)

    python3 model_verify.py  종료코드 0
    판정 집계 — 채택 0 · 미채택 13 · 판정 불가 11
    H26_첫돌파_125일_새종목: 판정 불가 (신규 신호 n=—)
    ⚠️ 판정이 옛 코드(db200b1)로 계산됨 — 다음 로봇 런에서 갱신

H26 이 판정 파일 배선까지 닿아 있음을 확인했다. 값은 다음 런부터.

## ③ 새 업종 오염 감사 (⑦) — 걱정한 곳은 깨끗했고, 딴 곳이 나왔다

리츠의 FFO 가 조정 EPS 칸에 섞였는지가 걱정이었다. 실측:

    [리츠] AMT·PLD·SPG·O·EQIX·PSA·DLR — 조정 EPS 칸 **0건** (오염 0)
           잣대: AMT·O·EQIX = adjusted_ebitda / PLD·SPG·PSA·DLR = gaap_eps
    [보험] PGR·ALL·TRV 조정 0건 · MET 34건(조정/GAAP 1.62) ·
           AFL 34건(0.91) — 월가와 갈린 칸 7건, 연간값 모양 0건
    [은행] USB·SCHW 0건 · PNC 3 · FITB 1 · KEY 2 · ZION 7 —
           월가와 갈린 칸 8건, 연간값 모양 **1건**(KEY 2026-01-20)

- **FFO 오염은 없다.** 리츠는 조정 EPS 칸이 아예 비어 잣대 사다리가
  아래로 내려갔다. 창작 금지 원칙이 제대로 작동한 것이다.
- 대신 **새 한계**: 리츠 4종목은 `gaap_eps` 로 판정된다. 리츠의 GAAP
  EPS 는 감가상각 때문에 실체를 크게 낮춰 잡는다 — 이 4종목의 신호는
  다른 종목과 같은 뜻이 아니다. 지금은 표시만 하고 고치지 않는다.
- 보험 MET 1.62·AFL 0.91 은 회사가 실제로 발표하는 조정이익이라 정상.
- 은행 KEY 2026-01-20 은 이미 알려진 **YTD 누적 오염**이고, 부탁 목록에
  올라 있음을 확인했다.
- **새 151종목은 아직 감사할 수 없다** — 수집물이 250종목뿐이다.
  다음 런 뒤에 같은 감사를 다시 돌린다.

## ④ 찾은 결함 — 우리 결함 확률 1순위 칸이 자리 부족으로 밀려나고 있었다

감사를 돌리다 재료 배분을 실측했다:

    갈린 칸 1024 · 그중 **연간값 모양 39건**(18종목)
    그런데 월가 재료가 내놓는 건 20건 → **19건이 목록에 닿지도 못함**
    그 20건이 앞자리 60칸을 먹어 → 이웃 튐 재료 60건 중 **11건만** 실림

"연간값 모양"은 우리 값이 월가 4분기 합과 2% 안에 드는 칸 = **연간값을
분기 칸에 넣은 것**이다. 산술로 확정되는 우리 결함이라 원문 부탁 1순위인데,
그게 잘려 나가고 있었다. 135차에 잣대 구멍에게 배운 것과 **같은 병**이라
같은 약을 썼다 — 재료마다 제 몫을 먼저 뗀다.

    MISMATCH_QUOTA = 30  (바깥 자)
    STREET_QUOTA   = 40  (월가 갈림 — 신설)
    NEIGHBOR_QUOTA = 25  (이웃 튐 — 신설, 상한이 없었다)
    SCALE_QUOTA    = 10  ·  HOLE_QUOTA = 15
    WANTED_LIMIT   60 → 95

**비용은 0에 가깝다**: 원문 부탁은 로봇이 이미 내려받은 글을 저장만
하는 것이다(`sec_fundamentals._is_wanted_raw`). 접속이 늘지 않고 저장
용량만 는다(건당 약 34KB · 지금 1,443건 49MB).

실측 결과:

    전(20칸): 월가 재료 20 · 연간모양 20 · **연간모양 종목 15개**
    후(40칸): 월가 재료 40 · 연간모양 24 · **연간모양 종목 18개**
    새로 원문이 닿는 종목: FSLR · STRL · TJX

    부탁 목록 전체 81 → **112건**
      바깥자 30 · 월가 39 · 이웃튐 23 · 자릿수 6 · 구멍 14

## ⑤ 헛도는 시험을 또 하나 잡았다 (네 번째)

`test_이웃튐_재료는_제_몫만_먹는다` 를 처음 쓸 때 튀는 칸을 행의
**맨 끝**에 뒀다. `spike_cells` 는 앞뒤 이웃이 둘 다 있는 칸만 보므로
재료가 **0건**이 나왔고, 시험은 "0 ≤ 25"로 통과했다 — 돌연변이(몫
자르기 제거)를 넣어도 초록이었다.

가운데로 옮기고, **재료가 몫보다 실제로 많은지**를 먼저 확인하는 줄을
넣었다. 다시 돌연변이:

    돌연변이 ① STREET_QUOTA 40 → 20  → ❌ "25건 중 20건만 들어왔습니다"
    돌연변이 ② 이웃튐 몫 자르기 제거   → ❌ "30건 실렸습니다 (몫 25)"

두 시험 모두 빨간 불을 본 뒤에 믿는다.

## ⑥ 검증

`python3 run_tests.py` 종료코드 **0** · **569개 통과** (567 → 569).

## ⑦ 다음 런에서 볼 것

1. 149차 ⑥의 항목들(벽시계 226분 예상 · 시간초과 31개 대비 · 새 151종목 성적)
2. **원문 112건이 실제로 담겨 오는지** — `부탁_` 파일 개수
3. 새 151종목에 대해 ③의 업종 오염 감사를 다시 돌린다
4. 그다음 회차에 **H26 첫 판정**

---

# 150차-B (2026-08-22) — "제타는 정배열인데 왜 화면에 없지?" — 원인은 **둘**이었다

주인이 물은 것을 짐작으로 답하지 않고 실데이터로 재현했다. 원인이
하나가 아니라 둘이었고, 둘째는 **화면이 거짓을 말하고 있던 결함**이다.

## ① 첫째 원인 — 딱 한 칸 차이로 접혀 있었다

    이격도30%+ 신호 20개 중 제타는 **6번째** · 화면은 앞 5개만 펼침
    제타 이격도 **50.7%** (20개 중 4위) · 완성일 2026-08-14 (8일 전)

정렬이 **완성일순**이라 이격도가 높아도 며칠 지나면 뒤로 밀렸다.
이 정렬은 129차에 "LITE 가 이격도순에 밀려 접기 속에 숨었다"는 지적을
고치며 넣은 것인데, 이번엔 **반대 방향으로 같은 사고**가 났다.

**수리**: 차례를 ① 새 완성(7일 안) 먼저 ② 그 안에서 이격도순 으로.
두 사고를 한꺼번에 막는다. 펼침 개수도 5 → **8**.

## ② 둘째 원인 — 웹앱이 제타를 "신호 아님"이라고 말하고 있었다

    웹앱 ZETA.json  →  지금정배열 true · 이격도 49.4 · **신호 false**

정배열이고 이격도 49.4%인데 신호가 꺼져 있었다. 원인:

```python
최근 = {r["종목"]: r for r in app.recent_completion_rows(...)}
```

이 한 줄이 **한 종목당 한 줄**을 가정했는데 실제로는 여러 줄이 왔다.
제타는 완성이 둘(06-26 이격도 3.2% · 08-14 이격도 50.7%)이었고,
정렬이 완성일 내림차순이라 **뒤에 온 낡은 06-26 이 최신 08-14 를
덮어썼다**. 화면이 낡은 값으로 "신호 아님"이라고 말한 것이다.

## ③ 같은 뿌리에서 셋째 결함 — 관찰판이 종목 수를 부풀렸다

정배열이 깨졌다 다시 붙기를 반복하면 한 종목이 여러 번 완성한다.
관찰판은 "한 묶음에서 여러 **종목**이 몰렸는가"를 보는 판인데 그것을
그대로 세고 있었다. 실측:

    최근 완성 89행 · 서로 다른 종목 **78개** · 두 번 이상 나온 종목 10개
      CRDO·ZETA·FCX·DE·O·ZM·ETN·EOG·CL 각 2회 · KMI **3회**

    부풀려진 묶음 (표시 → 실제 종목수):
      에너지 원자재·유전 서비스  5 → **2**
      산업 설비·자동화·수송     15 → 13
      광고 플랫폼               2 → **1**   ← 제타 한 종목뿐인데 "완성 2개"

**'광고 플랫폼'은 제타 하나뿐인데 완성 2개로 관찰판에 올라 있었다.**

**수리**: 종목당 **가장 최근 완성 하나만** 남긴다. 세 결함이 한 뿌리라
한 번에 고쳐진다.

## ④ 고친 뒤 (실행 출력)

    최근 완성 78종목 (89행 → 78종목) · 이격도30%+ **18개** (전 20 = 중복 2)
    홈 화면 8칸:
      1. 🆕 LITE 55.6%  2. 🆕 **ZETA 50.7%**  3. 🆕 TER 37.8%
      4. 🆕 ONTO 37.7%  5. 🆕 CRDO 37.1%      6. 🆕 S 30.7%
      7.    EXTR 64.7%  8.    OKTA 56.7%
    웹앱 ZETA.json → **신호 true**
    광고 플랫폼 완성 1개 (관찰판 몰림 조건에서 빠짐 — 옳다)

## ⑤ 화면 글자도 고쳤다

웹앱 제목이 "이격도 30%+ 완성 종목 **최신순**"으로 남아 있었다.
차례가 바뀌었는데 라벨이 그대로면 그 자체가 거짓말이라
"**새 완성·이격도순**"으로 바꿨다.

## ⑥ 모바일 412px 실제 렌더링 확인

    가로 {'sw': 412, 'cw': 412}   — 넘침 없음
    본문에 ZETA 가 2번째로 표시됨 · "이번 주 새 완성 LITE · ZETA · TER"

## ⑦ 돌연변이 검증

    ① 중복 제거 무력화        → ❌ "같은 종목이 두 번 나옵니다: [ZETA, KMI, KMI, KMI, ZETA]"
    ② 옛 정렬(완성일순) 복원  → ❌ 제타가 목록에서 사라짐
    ③ 150차 수리 전으로 복원  → ❌ "낡은 완성이 최신 완성을 덮어 신호가 꺼졌습니다"

129차의 난간(`최신완성이_먼저오고_새완성표시가_붙는다`)은 새 정렬에서도
초록이다 — 두 사고를 동시에 막고 있음을 확인했다.

`python3 run_tests.py` 종료코드 **0** · **573개 통과** (569 → 573).

---

# 150차-C (2026-08-22) — 정직화 정보가 **주인이 보는 화면**에 안 닿고 있었다

150차-B 에서 고친 "한 종목당 한 줄" 가정이 다른 곳에도 있는지 훑고,
잣대 사다리를 감사한 뒤, 웹앱의 빠진 정보를 채웠다.

## ① 같은 결함이 하나 더 있었고, 이미 함께 고쳐져 있었다

    app.py:936  group_member_rows()
      최근 = {r["종목"]: r for r in recent_completion_rows(...)}

150차-B 에서 고친 web_build 의 그 줄과 **똑같은 줄**이다. 즉 "헬스케어
구성 종목 보기" 같은 화면도 낡은 완성을 보여 주고 있었다. 뿌리(중복
제거)를 고쳤으므로 두 곳이 한 번에 고쳐졌다. 확인:

    광고 플랫폼 구성 → ZETA 완성 2026-08-14 · 이격도 50.7 · **신호 True**

전체 코드에서 같은 모양은 이 둘뿐이었다(다른 곳 없음).

## ② 잣대 사다리 감사 — 튀는 모양을 봤지만 **원인을 단정하지 않는다**

    잣대 분포: 조정 EPS 162 · GAAP EPS 74 · 조정 EBITDA 14
    조정EPS 칸 수 분포에서 문턱(8) 바로 아래 **7개에 10종목**이 몰림
      CLS·STRL·FSLR·AXP·DIS·CSX·AAL·ZION·LULU·KSS

처음엔 장치 결함을 의심했다. 확인한 것:

- `yardstick_of` 는 단순히 `>= 8` 을 센다 — 세는 방식에 결함 없음
- 그 7분기가 **최근 7분기가 아니라 여러 해에 흩어져** 있다
  (FSLR 2016~2020 · ZION 2019~2023) — "최근 N개만 읽는다" 모양이 아님
- 검사에서 버려진 값 때문도 아니다 (10종목 중 STRL 1건뿐)
- **내 계기부터 의심했다**: 처음엔 문턱 아래만 세어 분포가 7에서
  잘렸다. 전체 분포로 다시 보니 이웃 칸이 6개→3 · 7개→10 · 8개→6 ·
  9개→2 · 10개→4 로, 10은 높지만 **잡음 범위를 벗어났다고 말할 수 없다**.

> **결론: 관찰됐으나 원인 미규명.** 142차·145차에 두 번 원인을 단정했다
> 반증당했다. 세 번째는 하지 않는다. 다음 런에서 같은 모양이 유지되는지
> 본다.

## ③ 세운 가설이 틀렸다 — 배관 구멍은 없었다

"문턱 바로 아래 종목은 구멍 그물이 못 본다(잣대가 GAAP 이므로 GAAP
구멍만 찾는다)"고 짐작했다. 재 보니 **틀렸다**:

    문턱 아래 10종목 중 부탁 목록에 이미 올라 있는 것 **8개**
    구멍 그물 후보 200건 중 표적 종목 **8개**

부탁 배관은 이들을 이미 보고 있다. 고칠 것이 없다.

## ④ 진짜 결함 — 채택 거리가 웹앱에 없었다

139·140차에 만든 **"채택까지 얼마나 남았나"**가 계기판(app.py)에만
있고 웹앱에는 한 글자도 없었다. `grep adoption_distance web_build.py
docs/app.js` → **0건**.

주인은 **휴대폰만 쓴다**. 계기판은 개발 환경에서 도는 것이고, 주인이
실제로 보는 것은 GitHub Pages 웹앱이다. 즉 이 정직화 정보는 만들어만
놓고 **정작 보는 화면에 안 닿고 있었다**. 헌법 4장("한계는 코드 주석·
문서·화면 세 곳 모두에 적는다")이 화면에서 깨져 있었던 것이다.

**수리**: `web_build.hypothesis_rows` 가 `judge.adoption_distance` 를
그대로 옮겨 적고(스스로 계산하지 않는다 — 계산하면 두 화면이 갈린다),
`docs/app.js` 검증 탭이 그린다.

모바일 412px 실제 렌더링:

    가로 {'sw': 412, 'cw': 412}   — 넘침 없음
    채택까지: 표본 1,684개 필요 (지금의 16.8배)
    채택까지: 지금 폭등률로는 표본을 더 모아도 못 넘음
    채택까지: 표본 없음

139차에 단위 착오로 "n≈100"이라 적었다 정정한 그 1,684 가 이제
주인 화면에 그대로 뜬다.

## ⑤ 돌연변이 검증

    ① web_build 채택거리 배선 끊기  → ❌ 값이 None
    ② docs/app.js 에서 안 그리게    → ❌ "웹앱 화면이 채택 거리를 그리지 않습니다"

시험은 **값이 담기는 것**과 **화면이 그리는 것**을 둘 다 본다 — 값만
담고 안 그리면 주인은 여전히 못 본다.

`python3 run_tests.py` 종료코드 **0** · **574개 통과** (573 → 574).

---

# 150차-D (2026-08-22) — 같은 앱이 "정배열"을 두 뜻으로 쓰면서 그 말을 안 하고 있었다

모바일 412px 에서 네 탭을 전부 띄워 보다가 **두 탭이 서로 모순되는
말**을 하는 것을 봤다.

## ① 눈에 띈 모순

    장세 탭:  반도체장비 정배열 폭 **0%**
    홈  탭:  TER·ONTO(반도체장비)가 **2026-08-21 정배열 완성**
    종목 탭:  "지금 주봉 정배열 유지 중" 75개에 TER·ONTO **포함**

비개발자가 보면 앱이 고장 났거나 거짓말을 하는 것으로 읽힌다.

## ② 계산은 둘 다 맞았다 — 일부러 다른 잣대였다

`sector_model.aligned_flags_chart` 의 주석에 39차부터 적혀 있었다:

    · aligned_flags       (33차 등록, H11·H12·H14 정배열 **폭**)
        이평선 배열 **+ 주봉 종가 > 4주선**
    · aligned_flags_chart (39차 등록, H15·H16·H18 정배열 **완성**)
        이평선 **배열만** (종가 조건 없음)

39차에 종가 조건을 넣으면 주가가 잠깐 눌릴 때마다 "완성"이 수십 번
반복되는 것을 실측했다(COHR 8번 vs 1번). 그래서 완성 쪽만 종가 조건을
뺐고, **한쪽에 맞추면 사전 등록이 무너진다**고 주석에 못박아 두었다.

실측으로 확인:

    TER  주봉 종가 375.74 · 4주선 385.38 → 종가 조건 **False**
         이평선 385.38 > 380.98 > 357.49 > 272.72 → 배열 **True**
         ⇒ 폭 잣대 False · 완성 잣대 True
    ONTO 종가 293.33 · 4주선 297.98 → 같은 모양

## ③ 그러면 무엇이 결함인가 — **화면**이다

계산은 옳고 주석에도 적혀 있는데 **화면에는 한 글자도 없었다.**
헌법 4장은 한계를 "코드 주석·문서·화면 **세 곳 모두**"에 적으라고 한다.
세 곳 중 화면 하나가 비어 있었고, 하필 그 하나가 주인이 보는 곳이다.

규모도 작지 않다:

    두 잣대가 지금 갈리는 종목 **41개** (완성 True · 폭 False)
    AFL·ALL·AMZN·ANET·AVGO·BA·BAC·CL·CRDO·CRWD·DAL·DDOG·DELL·DLR·
    ETN·ETSY·FITB·GE·ITW·JPM·MET·MS·NET·NUE·NVDA·O·OKTA·ONTO·PCAR·
    PH·PNC·PSA·QRVO·ROST·SPG·TER·TROW·TRV·TWLO·USB·ZION

"정배열 유지" 75종목 중 41개가 폭에는 안 세어진다. 설명 없이 보면
두 숫자가 계속 어긋나 보인다.

## ④ 고친 방식 — 계산은 건드리지 않는다

계산을 맞추는 것은 **사전 등록을 무너뜨리는 짓**이므로 하지 않았다.
대신 `app.gauge_gap_rows(ds)` 를 만들어 지금 갈리는 종목을 세고, 장세
화면이 그 사실을 적게 했다.

    이 폭과 홈 화면의 완성은 서로 다른 잣대입니다 — 일부러 그렇게
    두었습니다(각각 33차·39차에 따로 등록한 정의라 한쪽에 맞추면
    사전 등록이 무너집니다). 폭은 주봉 종가가 4주선 위일 것까지
    요구하고, 완성은 이동평균선의 배열만 봅니다. … 어느 한쪽이
    틀린 것이 아닙니다.
    지금 그런 종목 41개: AFL · ALL · AMZN · ANET · AVGO · BA 외 35개

> **한 번 잘못 쓸 뻔했다**: 처음엔 "TER 종가 375.74 < 4주선 385.38"
> 이라는 실제 숫자를 화면에 **박아 넣었다**. 다음 주면 그 숫자가
> 거짓이 된다. 화면에 박는 숫자는 반드시 **그때 데이터에서 계산**해야
> 한다 — 지금은 종목 목록을 매 빌드마다 다시 센다.

## ⑤ 네 탭 모바일 412px 전수 확인

    주도(home)   가로 412=412 · 넘치는 요소 없음
    장세(market) 가로 412=412 · 넘치는 요소 없음
    종목(stocks) 가로 412=412 · 넘치는 요소 없음
    검증(check)  가로 412=412 · 넘치는 요소 없음

(요소 하나하나의 좌우 끝을 재서 412px 밖으로 나가는 것이 있는지 봤다 —
가로 스크롤만 보면 `overflow:hidden` 에 가려 놓칠 수 있다.)

## ⑥ 돌연변이 검증

    ① gauge_gap_rows 조건 뒤집기      → ❌ 종목수 0
    ② 화면 설명에서 "서로 다른 잣대" 지우기 → ❌ "화면 설명에 … 가 없습니다"

시험은 **전제부터 확인한다** — 가짜 종목이 정말 "완성 True · 폭 False"
인지 먼저 단언하고 나서 함수를 부른다. 전제가 깨지면 시험이 헛돌기
때문이다(150차에 그 사고를 이미 한 번 냈다).

`python3 run_tests.py` 종료코드 **0** · **576개 통과** (574 → 576).

---

# 150차-E (2026-08-22) — 401종목 첫 런 **예행 검사**, 그리고 낡은 주석 청소

내일 10:55 UTC 가 유니버스 401종목의 첫 런이다. 새 종목 151개는 그때
처음 수집되는데, 그중에 **분기가 몇 개 없거나 주가가 짧은** 종목이
반드시 섞인다. 거기서 화면 만들기가 죽으면 하루를 통째로 잃는다.
기다렸다 겪지 말고 **미리 겪어 보기로** 했다.

## ① 예행 검사 — 마른 종목 6가지를 섞어 로봇 경로를 그대로 돌렸다

손으로 재현하지 않고 `collect_job.py` 의 판정 블록을 **코드에서 그대로
뽑아** 돌렸다(146차 교훈 — 손으로 재현했다 "17개 빠짐" 가짜 경보를 낸
적이 있다).

    빈종목(0분기·주가없음) · 한분기 · 주가없음(20분기) ·
    주가1일 · 주가짧음(5일) · 정상새것

    ① dataset.build            OK · 종목 256
    ② judge.run                OK · 가설 9
    ③ 사건·판정 블록            OK · 가설 18
    ④ 부탁 목록 갱신            OK
    ⑤ 완성·주도                OK · 완성사건 3,341 · 가설 20
    ⑥ web_build.build_payload  OK · 신호종목 18 · 잣대차이 41
    ⑦ 건강검진                 OK · 행 9,547 · 아주 빈 칸 없음

**전부 통과.** 마른 종목이 파이프라인을 깨지 않는다.

> 정직하게: 이 예행은 가설 20개까지만 태웠다(주도섹터 H19·H20·H21·H19b
> 판정 호출을 뺐다). 실제 런은 24개다. 뺀 4개는 `leadership` 쪽이라
> 새 종목의 마른 데이터와 직접 닿지 않지만, **전부를 태운 것은 아니다.**

> 또 정직하게: ⑦에서 한 번 넘어졌는데 **내 잘못이었다.** `data_health.
> report(snap)` 이라고 짐작해 불렀는데 로봇은 `snap["eps"]` 를 넘긴다
> (`collect_job.py:203`). 146차에 배운 것을 또 어겼다 — 부르는 방법은
> 짐작하지 말고 호출부를 복사할 것.

## ② 예행을 **상시 시험으로** 남겼다

한 번 통과하고 끝날 일이 아니다. 확장할 때마다 걸려야 한다.
`test_데이터가_마른_새_종목이_화면을_깨뜨리지_않는다` 는 죽지 않는 것만
보지 않는다 — **없는 값이 0 으로 둔갑하지 않는지**까지 본다:

    빈종목·한분기  → 잣대 None · 실적 []       (지어내지 않음)
    주가없음       → 이격도 None · 주봉 []
    주가없음·1일·짧음 → "정배열 유지"에 **안 들어감** (판단 불가)

돌연변이:

    ① build_ticker 의 주가·잣대 방어 제거 → 💥 KeyError: '빈종목'
    ② 짧은 주가도 정배열로 세게          → ❌ "주가1일 은 판단할 수 없는데 정배열로 셌습니다"

## ③ 낡은 주석 세 곳

    tests/test_app.py  "유니버스 **79종목** 전부에 섹터가" → 지금 401
    web_build.py       "매일 **250종목**치를 커밋하므로"   → 지금 401
    judge.py           "수집물에도 현 250종목 것만 있습니다" → 내일이면 거짓

앞 둘은 **지금 사실이 아니고**, 셋째는 143차에는 맞았지만 내일부터
틀린다. 79 라고 적힌 것이 401 이 되도록 아무도 못 고쳤다는 것이
문제다 — 그래서 시험 설명에서는 **숫자를 아예 빼고** "전 종목"이라
쓰고, judge.py 쪽은 "143차 시점의 사실 기록"이라고 시점을 못박았다.
(다른 곳의 "250종목"은 그때의 실측 기록이므로 그대로 둔다.)

`python3 run_tests.py` 종료코드 **0** · **577개 통과** (576 → 577).

---

# 150차-F (2026-08-22) — 내 고침이 남긴 어긋남을 내가 찾았다

150차-B 의 수리가 다른 도구까지 닿았는지 확인하다가, **닿지 않은 곳**을
하나 찾았다.

## ① 같은 저장소의 두 도구가 다른 수를 댔다

    python3 model_verify.py
      관찰판: 최근 91일 완성 **89건** · 이격도30%+ **20건**

    웹앱·계기판
      최근 완성 **78종목** · 이격도30%+ **18종목**

150차-B 에서 "한 종목 한 줄"로 고친 것이 앱에는 반영됐는데 점검기는
옛 숫자를 그대로 말하고 있었다. 원인은 단순하다 — **점검기가 세는
코드를 따로 적어** 두었다:

    최근 = [e for e in 완성 if e["day"] >= cut]          # 사건을 셈
    신호 = [e for e in 최근 if e["이격도"] >= H18_GAP_MIN]

앱은 종목을 세고 점검기는 사건을 센다. 중복 완성(KMI 3회 등) 때문에
차이가 났다.

## ② 이 저장소가 이미 아는 병이다

`web_build.build_payload` 의 주석에 적혀 있다:

> 계기판과 **같은 함수**로 만들기 때문에 두 화면이 다른 숫자를 말할 수
> 없습니다.

웹앱에는 그 규율을 적용했으면서 점검기에는 빠뜨렸다. 고친 방식도 같다 —
`app.recent_completion_rows` 를 부른다.

    관찰판: 최근 91일 완성 78종목 · 이격도30%+ 18종목  ← 이제 일치

세는 단위가 바뀌었으므로 화면 글자도 "건" → "**종목**"으로 고쳤다.
89 와 78 은 둘 다 맞는 수인데 **세는 것이 달랐다** — 단위를 안 적으면
그 자체가 오해를 부른다.

## ③ 시험은 숫자를 박지 않는다

다음 수집이면 78 도 18 도 바뀐다. 그래서 **같은 함수를 부르는가**를
본다. 옛 셈법이 되살아나는 것까지 막는다:

    assert "app.recent_completion_rows(" in text
    assert 'e["이격도"] >= sm.H18_GAP_MIN' not in text

돌연변이(옛 셈법 되살리기) → ❌ "점검기가 관찰판을 계기판과 다른
방법으로 셉니다 — 두 도구가 갈립니다"

## ④ 남길 교훈

> **고친 뒤에는 그 계산을 쓰는 곳을 전부 훑는다.** 150차-B 에서
> `recent_completion_rows` 를 고쳤을 때 `web_build`·`group_member_rows`
> 는 같은 함수를 불러 자동으로 고쳐졌지만, **함수를 안 부르고 스스로
> 세던 곳**은 조용히 옛 숫자에 남았다. 자동으로 고쳐지는 곳과 안 되는
> 곳을 가르는 것은 "같은 함수를 부르는가" 하나다.

`python3 run_tests.py` 종료코드 **0** · **578개 통과** (577 → 578).

---

# 150차-G (2026-08-22) — 워크플로 주석이 **코드와 반대로** 말하고 있었다

내일 첫 런의 설정 자체를 확인하다가 두 가지를 찾았다. 설정이 틀리면
코드가 아무리 멀쩡해도 그날을 통째로 잃는다.

## ① 실행 시각을 내가 잘못 알고 있었다

여러 회차에 걸쳐 "10:55 UTC 런"이라고 적어 왔는데, 워크플로의 예약은
`cron: "30 9 * * *"` = **09:30 UTC** 다. 계기로 확인:

    robot_log.json  ran_at 12:50 UTC · 수집계기 벽시계_분 140.8
    → 시작 = 12:50 − 2:21 = **10:29 UTC**

즉 예약은 09:30 이고 깃허브가 **약 1시간 늦게** 띄운다. 관측된
10:29 를 "예약 시각"으로 착각해 옮겨 적고 있었다.

이번 런은 예상 226분이라 끝나는 시각이 **13:30~15:30 UTC** 다.
검증 점검을 15:30 → **16:30 UTC** 로 옮겼다 — 15:30 에 보면 최악의
경우 아직 돌고 있는 것을 보고 "절반"으로 판단하게 된다.

## ② 주석이 코드와 반대로 말하고 있었다

워크플로에 이렇게 적혀 있었다:

    일꾼 수는 3 그대로 둡니다 — 늘리면 SEC 429(99차 사고)를 부릅니다.

그런데 `config.COLLECT_WORKERS` 는 **이미 6** 이다(142차에 올리고
148차에 실측으로 안전을 확인). 08-22 계기: 일꾼 6 · 초당 **0.28요청**
= SEC 허용 10의 **3%**. 429 와 거리가 멀다.

**주석이 코드와 반대면 다음 사람이 그 주석을 믿고 되돌린다.** 이건
낡은 문구가 아니라 잘못된 지시다.

## ③ 시험 둘 — 하나는 문서, 하나는 진짜 사고 방지

    test_시간예산이_워크플로_한도보다_넉넉히_작다
    test_워크플로_주석이_일꾼수를_거꾸로_말하지_않는다

첫째가 더 중요하다. `COLLECT_BUDGET_MINUTES` 를 올리면서 워크플로
`timeout-minutes` 를 안 올리면 런이 **일하는 중에 잘린다** — 94차에
적은 "표본을 늘리려다 있던 표본을 깎는 최악"이 그대로 재현된다.
예산은 8-K 훑기만 재고 뒤에 주가·판정·웹앱 빌더가 더 붙으므로
여유 60분을 요구한다(08-22 실측 뒤 단계 약 20분).

돌연변이:

    ① 예산 190 → 320 (한도 300 위)  → ❌ "런이 일하는 중에 잘립니다"
    ② 예산 190 → 260 (여유 40분)    → ❌ "여유가 40분뿐입니다"

> **시험이 내 주석까지 잡았다.** 처음엔 틀린 문장을 설명하려고 그대로
> 인용했는데, 시험이 그 인용을 잡아냈다. 옳다 — 훑어보는 사람에게는
> 인용이든 지시든 똑같이 "3 으로 두라"로 읽힌다. 인용을 빼고 풀어 썼다.

## ④ 컨테이너가 턴 중간에 초기화됐다

시험을 돌렸더니 **580개가 아니라 191개**가 나왔다. 종료코드는 0 이고
"모두 통과"인데 수가 줄었다 — 파일이 통째로 건너뛰어진 것이다.
확인하니 저장소가 `ccbe870`(옛 커밋)으로 되돌아가 있었고, 시험 파일이
21개 → 12개였다.

> **남길 것**: 시험 수가 줄었는데 초록이면 **파일이 안 돌았다는 뜻**이다.
> "모두 통과"만 보지 말고 **돈 파일 수와 총 개수**를 함께 볼 것.
> 이번엔 `grep -c "^▶"` 로 20개 파일이 돈 것을 확인하고 나서 믿었다.

푸시된 것은 안전했으므로(578개는 bc10c72 에) 복구 후 재적용했다.

`python3 run_tests.py` 종료코드 **0** · 파일 **20개** · **580개 통과**
(578 → 580).

---

# 150차-H (2026-08-22) — 새 세션이 **가장 먼저 읽는 문서**가 끝난 일을 가리키고 있었다

오늘 얻은 교훈 여덟 개가 11,900줄짜리 `측정결과.md` 에 묻혀 있었다.
새 세션은 그것을 읽지 않는다 — `인수인계.md` 를 읽는다. 그런데 그
문서를 열어 보니 더 큰 문제가 있었다.

## ① 5장이 **이미 끝난 재건축**을 가리키고 있었다

    | 단계 | 할 일 | 상태 |
    | 0 | 봉인·청소·인수인계 | ✅ 완료 |
    | 1 | 전략.md v3 — 헌법 | ▶ 다음      ← 여기서 멈춰 있었다
    ...
    **1단계에서 가장 중요한 것**: … 후보 질문을 여러 개 만들어
    사용자에게 고르게 하세요.

재건축 0~6단계는 **2026-08-13 에 전부 끝났다**(CLAUDE.md 머리말에 그렇게
적혀 있다). 그런데 새 세션이 가장 먼저 읽는 문서는 "1단계가 다음"이라며
**이미 정해진 헌법을 다시 만들라고** 지시하고 있었다.

4장 16번("주석이 코드와 반대면 낡은 문구가 아니라 **잘못된 지시**다")이
문서에도 그대로 적용된다.

**수리**: 5장을 지금 상태로 바꿨다 — 운영 단계 · 유니버스 401 · 가설 24 ·
채택 0 · 시험 580 · 기다리는 것(H26 첫 판정, 원문 112건) · **미규명으로
남긴 것**(수집 시간, 사다리 문턱 아래 몰림) · 주인이 할 일(Pages 켜기).

## ② 2장 "**지금** 데이터에 무엇이 들어 있나"가 2026-08-12 표였다

    적혀 있던 것        실제 (2026-08-22, 실행 출력)
    종목 79             **250** (유니버스 401)
    발표일 있는 분기 1,359   **9,337**
    조정 EPS 1,160      **4,405**
    파싱 실패 원문 137건   원문 보관 **1,443건**(부탁_ 56건)
    "조정 EPS 를 한 건도 발표하지 않는 종목 6개: ZETA·APP·TXN…"
      → 지금은 잣대 사다리가 있어 **잣대를 못 정한 종목 0개**

열 배 넘게 틀린 표가 "지금"이라는 제목을 달고 있었다. 갱신하면서
**다음에 또 썩지 않도록** 머리말을 달았다:

> 이 표는 회차마다 바뀌므로, 숫자가 궁금하면 이 문서를 믿지 말고 직접
> 재세요: `python3 model_verify.py` · `data_health.report(...)`.

리츠가 GAAP EPS 로 판정되는 한계(150차 ③)도 여기 적었다 — 헌법 4장이
요구하는 "세 곳"(코드 주석·문서·화면) 중 문서 자리다.

## ③ 장 제목이 지금과 과거를 안 갈랐다

    1. **지금** 저장소에 무엇이 있나  →  저장소에 무엇을 **남겼나
                                       (2026-08-13 봉인 당시의 기록)**
    3. v1·v2가 무엇에 실패했나       →  … **(지난 일의 기록)**

머리말에 "1·3장은 지난 일, 2·5장이 지금, 4장이 가장 값지다"를 적었다.
읽는 사람이 **어느 문장이 지금 사실인지** 알 수 있어야 한다.

## ④ 오늘의 교훈 여덟 개를 4장에 옮겼다 (11~18번)

11. 부르는 방법을 짐작하지 말고 **호출부를 복사**하라 (146차·150차-E 재범)
12. 원인을 **세 번째로 단정하지 마라** — 내 계기부터 의심하라
13. 고친 뒤에는 그 계산을 **쓰는 곳을 전부 훑어라** ("같은 함수를 부르는가")
14. **시험 수가 줄었는데 초록이면 파일이 안 돈 것**이다
15. 화면에 박는 숫자는 **그때 데이터에서 계산**하라
16. 주석이 코드와 반대면 낡은 문구가 아니라 **잘못된 지시**다
17. 한계는 세 곳에 — **화면을 빼먹기 쉽다**
18. 건강검진의 "**사라진 분기**"는 손실 지표가 아니다

## ⑤ 살아 있는 숫자 셋도 고쳤다

    인수인계 `data/` (5.6MB) 79종목    → 지금 70MB · 수집물 250 · 유니버스 401
    인수인계 "지금 79종목은 섞여 있습니다" → 지금 유니버스(401종목)
    전략.md  "(지금 79종목은 섞여 있다)"  → (지금 유니버스는 섞여 있다)

**"79종목으로 재니 문턱이 항상 ON"** 같은 줄은 **그때의 실측 기록**이므로
그대로 뒀다. 지금을 말하는 문장과 그때를 말하는 문장을 갈라야 한다 —
전부 바꾸면 측정 기록이 거짓이 된다.

인수인계 4장 9번에는 아예 못박았다:
**"종목 수를 이 문서에 박지 마세요 — 79 라고 적힌 것이 401 이 되도록
아무도 못 고쳤습니다."**

`python3 run_tests.py` 종료코드 **0** · 파일 **20개** · **580개 통과**
(코드 변경 없음 — 문서만).

---

# 150차-I (2026-08-23) — 설계도가 시스템의 **절반만** 그리고 있었다

150차-H 에서 인수인계 문서를 고쳤으니, 새 세션이 읽는 나머지 문서도
같은 병이 있는지 기계로 확인했다. 눈으로 훑지 않고 **파일 목록과
맞대 봤다.**

## ① 설계도에 모듈 14개가 빠져 있었다

    설계도.md 가 말하는 .py  vs  실제 파일

    빠진 것 14개:
      audit_data · config · consensus_feed · data_health · exp1 · exp2 ·
      lab · leadership · model_verify · run_tests · sector_model ·
      vendor_compare · vendor_feed · web_build

`설계도.md` 는 새 세션이 **모듈 경계를 배우는** 문서다. 그런데 흐름도에
①dataset ②measure_engine ③judge ④app 네 개만 있고, 실제로 판정에
참여하는 `sector_model`(정배열 폭·완성) · `leadership`(주도섹터) ·
`vendor_compare`(두 자 대조)도, 화면을 만드는 `web_build` 도 없었다.

새 세션은 **이 부품들이 없는 줄 알게 된다.** 그러면 이미 있는 것을 다시
짓거나, 고쳐야 할 곳을 못 찾는다.

**수리**: 흐름도를 실제 구조로 다시 그렸다 —

- 수집 쪽에 `vendor_feed`(두 번째 자) · `consensus_feed` 추가
- `dataset` 아래에 `audit_data`(오염 조사) · `data_health`(건강검진) ·
  `vendor_compare`(두 자 대조)
- `measure_engine` 아래에 `sector_model` · `leadership`
- `judge` 뒤를 **두 갈래**로: ④`app.py`(계기판) 와
  ⑤`web_build.py` → `docs/`(휴대폰 화면). **web_build 는 app 의 순수
  함수를 재사용한다**고 그림에 못박았다 — 150차-F 에서 점검기가 따로
  세다 어긋난 사고의 예방이다.
- **판정 경로가 아닌 것**을 따로 묶었다: `model_verify`(점검기) ·
  `lab`·`exp1`·`exp2`(주도섹터 실험용 복제본) · `run_tests` · `config`.
  판정에 쓰이는 것과 안 쓰이는 것을 갈라야 헷갈리지 않는다.

## ② 문서는 아무도 안 보면 반드시 썩는다 — 시험이 대신 본다

150차-E·H·I 에서 세 번 연달아 낡은 문서를 고쳤다. 사람이 기억으로
지키는 것은 안 된다는 뜻이다.

    test_설계도가_실제_모듈을_전부_적고_있다

파일 목록과 문서를 **양방향으로** 맞댄다:

    돌연변이 ① 설계도에서 sector_model 지움
      → ❌ "설계도.md 에 안 적힌 모듈: ['sector_model.py']"
    돌연변이 ② 없는 파일 ghost_module 을 설계도에 적음
      → ❌ "설계도.md 가 없는 파일을 말합니다: ['ghost_module.py']"

한 방향만 보면 **지워진 파일이 문서에 유령으로 남는다.** 둘 다 본다.

## ③ 어제 놓친 모순 하나

인수인계 1장에 이렇게 남아 있었다:

    `collect_job.py` 안의 자동 판정 호출도 함께 뺐습니다 —
    **지금 로봇은 수집만 합니다.**

5장은 "로봇이 매일 수집·자동 판정한다"고 말하는데 1장은 "수집만
한다"고 말했다. 1장을 "지난 일의 기록"으로 표시했어도 **굵게 강조된
'지금'** 은 눈에 먼저 들어온다. 시점을 명시하고 지금 사실을 덧붙였다.

## ④ 검증

`python3 run_tests.py` 종료코드 **0** · 파일 **20개** · **581개 통과**
(580 → 581).

---

# 150차-J (2026-08-23) — "표본 없음" 열한 번은 아무것도 알려 주지 않는다

주인이 웹앱 검증 탭을 열면 가설 24개 중 **11개가 전부 "표본 없음"**
이라고만 말하고 있었다. 이 화면만 보면 시스템이 멈춘 줄 안다.

## ① 이유가 서로 다른데 화면은 같은 말만 했다

    H18_완성시_52주선이격도    등록 2026-08-15 · 창  60일
    H18b_완성이격도_1년        등록 2026-08-21 · 창 252일
    H24_장세조건부_첫돌파       등록 2026-08-20 · 창  60일
    …                       (전부 "표본 없음")

실제 이유는 **구조적**이다. 표적 창이 60거래일이므로 08-15 에 등록한
가설은 **11-07 이전에 판정이 나올 수가 없다.** 이건 고장이 아니라
사전 등록 규율이 제대로 도는 모습이다. 그런데 화면이 그 말을 안 하니
"멈춤"으로 읽힌다.

140차에 `judge.first_verdict_floor` 로 이미 계산해 두었는데, **계기판
(app.py:723)에만 있고 웹앱에는 한 글자도 없었다** — 150차-C 에서
고친 것과 **똑같은 병**이다(만들어 놓고 주인이 보는 화면에 안 닿음).

## ② 고친 뒤 (실제 렌더링, 412px)

    가로 {'sw': 412, 'cw': 412} · 넘치는 요소 없음

    채택까지: 표본 없음 — 표적 창(60거래일) 때문에
              **2026-11-07** 이전에는 나올 수 없습니다
    채택까지: 표본 없음 — 표적 창(252거래일) 때문에
              **2027-08-09** 이전에는 나올 수 없습니다

10개 가설에 날짜가 붙었다. 등록일 상수를 못 찾는 가설(H19 등)은
**날짜 없이 "표본 없음"만** 그대로 둔다 — 지어내지 않는다.

## ③ 헛도는 시험을 또 잡았다 (다섯 번째)

처음 쓴 시험은 화면 파일에 `"가장이른날"` **글자가 있는지**만 봤다.
돌연변이로 `h.가장이른날 ?` 를 `false ?` 로 바꿔 **안 그려지게** 만들어도
초록이었다 — 문자열이 `esc(...)` 쪽에 남아 있었기 때문이다.

**값을 실제로 조건으로 쓰는가**를 보게 고쳤다:

    assert "h.가장이른날 ?" in js
    assert "이전에는 나올 수 없습니다" in js

돌연변이 셋:

    ① 배선 끊기(가장이른날 = None) → ❌ "등록일이 있는 가설인데 날짜가 없습니다"
    ② 모르는 가설에 날짜 지어내기   → ❌ "모르는 가설의 날짜를 지어냈습니다"
    ③ 화면에서 안 그리게           → ❌ "조건으로 쓰지 않습니다 — 안 그려집니다"

> **남길 것**: 화면 시험에서 **글자가 있는지**만 보면 헛돈다. 그 값이
> **조건·출력에 실제로 쓰이는지**를 봐야 한다.

## ④ 값을 지어낼 뻔한 것 하나

처음엔 JS 에 `${h.창_거래일 || 60}` 이라 썼다. 창 길이를 모르면 60이라고
**지어내는** 것이다(H18b 는 252다). 실제 값을 payload 로 넘기고, 없으면
창 길이를 아예 안 적게 고쳤다. 시험도 `"|| 60" not in js` 로 막는다.

`python3 run_tests.py` 종료코드 **0** · 파일 **20개** · **582개 통과**
(581 → 582).

---

# 150차-K (2026-08-23) — 주인 질문에서 결함 둘: 꼬리 구멍 197건이 **그물에 안 보였다**

주인이 화면을 보고 물었다 — "정배열에서 60일과 1년 차이가 뭐지? 실적이
없는 건 뭐지?" 두 번째 질문을 데이터로 따라가니 결함이 나왔다.

## ① 답 자체는 정상이었다 (검산으로 확인)

TTM 은 **네 분기의 합**이라 하나라도 없으면 못 만든다. 화면 종목은
ZETA(조정 EBITDA)와 LITE(조정 EPS)였고, 못 읽은 분기는:

    ZETA  24Q4(2025-02-25) 없음 · 25Q1(2025-05-01) 없음
    LITE  25Q4(2025-08-12) 없음 · 26Q4(2026-08-11) 없음

값이 있는 칸은 산술로 검산했다:

    화면 2026-04-30  298,092,000
    계산 58,769,000 + 78,057,000 + 95,126,000 + 66,140,000 = 298,092,000 ✅
    화면 2026-08-04  331,020,000
    계산 78,057,000 + 95,126,000 + 66,140,000 + 91,697,000 = 331,020,000 ✅

지어낸 숫자가 아니다. 손질 기록에도 이 분기들을 **버렸다는 말이 없다** —
검사에서 탈락한 게 아니라 **애초에 못 읽은 것**이다.

## ② 그런데 그 네 칸이 부탁 목록에 **한 건도 없었다**

    LITE 2025-08-12  없음     ZETA 2025-02-25  없음
    LITE 2026-08-11  없음     ZETA 2025-05-01  없음

주인이 화면 맨 위에서 보는 신호 **1·2위 종목**의 최신 실적이 비어
있는데, 로봇이 그 원문을 안 가져오고 있었다.

## ③ 파고드니 결함이 둘이었다

### 결함 1 — 그물이 **꼬리 구멍을 아예 못 봤다** (197건 · 29종목)

`wanted_from_holes` 는 `첫 < 날 < 끝` 만 셌다. **값이 있는 마지막 분기
뒤**의 빈칸을 "아직 발표 전"으로 보고 뺀 것이다. 그런데 **발표일이 있는
행은 이미 발표된 것**이다 — 못 읽었을 뿐이다.

    가려진 꼬리 구멍 **197건 · 29종목**
    최신 15건이 전부 **2026년 7~8월** — 즉 가장 최근 분기
      CSCO 08-12 · LITE 08-11 · SMCI 08-11 · ABNB 08-06 · SEDG 08-05 …

**가장 최근 분기가 비면 지금의 TTM·신고점·첫돌파가 통째로 막힌다.**
주인이 화면에서 보는 바로 그 칸이다.

수리: 조건을 `첫 < 날` 로 바꿔 꼬리를 열었다. **앞쪽은 그대로 닫아
둔다** — 상장 전이거나 그 지표를 아직 안 내던 때라 빈 것이 정상이다.

### 결함 2 — "구멍 많은 종목부터"가 **최신 구멍을 영원히 밀어냈다**

실측: 15칸에 실리는 것이 **전부 2017~2021년 것**이고 최근 1년 구멍은
**0건**이었다.

    구멍 후보 248건의 연도 분포
      2017:26 2018:34 2019:26 2020:27 2021:33 2022:40 2023:27
      2024:10 2025:17 2026:8
    그런데 15칸: AMBA 2019 · UCTT 2018 · ROKU 2018 · TJX 2017 · EL 2018 …

옛날 구멍은 **이미 끝난 측정**에만 걸리고, 최신 구멍은 **지금 판정**을
막는다. 급한 쪽이 반대였다.

수리: `FRESH_HOLE_QUOTA = 12` — 최신 구멍에게 몫을 따로 뗀다
(135·150차에 다른 재료에게 쓴 것과 같은 약).

## ④ 고친 뒤

    부탁 목록 112 → **124건**
      바깥자 30 · 월가 39 · 이웃튐 23 · 자릿수 6 · 구멍 **26**(전 14)
    **LITE 2026-08-11 · 2025-08-12 들어옴** ← 주인이 본 바로 그 칸

> **ZETA 는 아직 못 들어갔다.** 구멍이 2025-02-25·2025-05-01 로 최신 12칸
> 에도, "구멍 많은 종목" 15칸에도 못 들었다. 앞의 것들이 채워지면 다음
> 회차에 올라온다 — **되는 것처럼 말하지 않는다.**

## ⑤ 고치지 **않은** 것 — 잣대가 3~5년째 죽어 있는 종목 12개

꼬리 구멍을 세면서 두 무리가 갈렸다:

    꼬리 1~2개 (17종목) — 파서가 최신 분기를 못 읽음 (LITE·CSCO·ABNB…)
    꼬리 6~19개 (12종목) — **그 잣대의 마지막 값이 3~5년 전**
      DG   꼬리 19개 · 마지막 adj_eps **2021-03-18**
      ISRG 꼬리 18개 · 마지막 adj_eps **2022-01-20**
      MCD  꼬리 18개 · 마지막 adj_eps **2022-01-27**
      DAL·BSX 17개 · EQIX·ZBH 15개 · VRTX 14개 …

이 12종목은 **잣대가 살아 있지 않은 채로 유니버스에 들어 있다.** 새
신호를 못 내므로 틀린 값을 주지는 않지만, 사실상 빈자리다.

원인이 "회사가 그 지표를 그만 냈다"인지 "2022년에 보도자료 서식이
바뀌어 파서가 못 읽는다"인지는 **원문을 봐야 안다.** MCD 는 지금도
조정 EPS 를 낸다고 알려져 있으니 후자일 가능성이 높지만, **단정하지
않는다**(142·145차에 두 번 단정했다 반증당했다).

**잣대 사다리 규칙은 건드리지 않았다.** "≥8분기면 그 잣대" 는 사전
등록된 규칙이고, 지금 본 것을 보고 "최신성도 보자"고 고치는 것은
헌법 8조(결과를 보고 규칙을 고치지 마라) 위반이다. 이 12종목의 원문이
들어와 원인이 밝혀지면, 그때 **새 번호로 사전 등록**한다.

## ⑥ 돌연변이 검증 (넷)

    ① 꼬리 열기 되돌림(첫<날<끝) → ❌ "마지막 값 뒤의 구멍을 못 셉니다"
    ② 앞쪽까지 열기              → ❌ "첫 값 앞의 빈칸까지 셉니다"
    ③ 최신 구멍 몫 배선 끊기      → ❌ "최신 구멍이 옛날 구멍에 밀렸습니다"
    ④ newest_first 를 옛 정렬로   → ❌ 같은 실패

`python3 run_tests.py` 종료코드 **0** · 파일 **20개** · **585개 통과**
(582 → 585).

---

# 150차-L (2026-08-23) — 델타 흐름 그래프 (주인 요청 1번)

주인이 CRDO 화면을 보고 물었다 — "델타는 계속 상승인데 아래 실적은 왜
빈칸이지?" 그리고 "델타 그래프도 추가하자. 상승/하락의 흐름을 보게."

## ① 왜 둘이 다른 말을 하는가 (답)

    CRDO 조정 EPS  0.07 → 0.07 → 0.25 → 0.52 → 0.67 → 1.07 → 1.16
                   (하나도 안 비었고 전부 상승)

    그런데 분기 두 개가 **통째로 빠졌다**:
      2024-05-29 → 2024-12-02  187일   (정상 91일)
      2025-03-04 → 2025-09-03  183일

- **델타** = 분기 대 분기 → 값이 다 있으니 계속 "상승"
- **TTM** = **네 분기의 합** → 빠진 분기가 들어가는 창 5개가 전부 "—"

검산: `2026-06-01 TTM = 0.52 + 0.67 + 1.07 + 1.16 = 3.42` ✅

**고장이 아니라 둘이 다른 것을 재는 것**이다. 그런데 화면이 그 말을
안 해서 모순처럼 보였다. 그래프 아래에 그 설명을 적었다.

## ② 그래프

`web_build` 가 `sm._delta_series` 를 그대로 옮겨 싣고(스스로 세면 두
화면이 갈린다), `docs/app.js` 의 `델타막대()` 가 그린다.

    막대 하나 = 분기 하나 · 높이 = 그 분기의 잣대 값(절댓값)
    초록 = 직전 분기보다 올랐음 · 빨강 = 내렸음
    "최근 11분기 중 9분기 상승" 처럼 세어서 같이 적는다

**값을 못 잰 분기는 그리지 않는다** — 0 으로 그리면 "이익이 0"으로
읽혀 거짓이 된다(헌법 1조).

모바일 412px 렌더링: 가로 412=412 · 넘치는 요소 **없음**.

## ③ 같은 실수를 두 번 했다 — 헛도는 검사 (여섯 번째)

150차-J 에서 "글자만 찾으면 헛돈다"를 적어 놓고 **여기서 또 했다**:

    assert "델타막대(" in js        → 함수 **정의**에도 있어서
                                     호출을 지워도 통과
    assert "x.값 !== null" in js    → **다른 줄**에도 있어서
                                     0 채움 돌연변이가 통과

돌연변이가 실제로 적용됐는지부터 확인해서 잡았다(안 바뀌었으면 시험이
아니라 돌연변이가 헛돈 것이므로 먼저 확인해야 한다).

고친 검사 — **호출 지점**과 **함수 몸통**을 각각 콕 집는다:

    assert "html += 델타막대(델타)" in js          # 호출
    몸통 = js.split("function 델타막대(")[1]...    # 정의 몸통만 잘라
    assert "filter" in 몸통 and "!== null" in 몸통

    돌연변이 ① 델타흐름 배선 끊기 → ❌
    돌연변이 ② 호출 지우기        → ❌
    돌연변이 ③ 0 채움             → ❌

## ④ 시험 데이터에서 배운 것

가짜 분기를 만들 때 `filing_date` 를 발표일과 같게 뒀더니 **발표일이
통째로 지워져** 델타가 0건이었다. 이 저장소에서 **분기끝 노릇을 하는
칸은 `filing_date`** 이고, 138차 상식 검사가 "발표일 − filing_date 가
10~70일"을 요구한다. 시험 주석에 적어 두었다.

`python3 run_tests.py` 종료코드 **0** · 파일 **20개** · **586개 통과**
(585 → 586).

---

# 150차-M (2026-08-23) — 델타에 **성장 속도**를 넣었다 (주인 지적), 다중 에이전트 금지 해제 정리

## ① 주인 지적이 정확했다

> "델타의 상승상태를 추가해야 할 것 같은데. 예를 들어 1-10-20 이면
> 올라갔다 내려가야지. 10배에서 2배가 된 거니까."

150차-L 의 그래프는 **값의 방향**만 봤다. 1 → 10 → 20 은 셋 다 "상승"
이지만 성장은 **+900% → +100%** 로 반토막이다. 실물로 확인:

    CRDO 조정 EPS  값은 계속 오르는데
    성장률  75% → 257% → 168% → 60% → **8.4%**   ← 뚜렷이 둔화 중

값만 보면 "9분기 중 9분기 상승"이라 좋아 보이는데, **성장은 급격히
꺾이고 있다.** 화면이 그 말을 안 하고 있었다.

## ② 고친 것 — 색을 셋으로

    진초록  값이 올랐고 **성장이 빨라짐**(가속)
    연초록  값은 올랐지만 **성장이 느려짐**(둔화)
    빨강    값이 내려감

그리고 캡션에 성장률 흐름을 그대로 적는다:

    전분기 대비 성장률 흐름: 75% → 257% → 168% → 60% → 8% — **둔화 중**

값이 0 이하를 지나면 비율의 뜻이 뒤집히므로 **성장률을 없음으로 둔다**
(적자에서 적자로 옮겨간 것을 "몇 % 성장"이라 부를 수 없다). 그 칸은
가속·둔화를 **판단하지 않고** 중간색으로 그린다.

> ⚠️ `상승`(방향)은 **사전 등록된 델타 정의**라 건드리지 않았다
> (H19·H21 등이 쓴다). 성장률·가속은 **화면 표시**일 뿐이다.

## ③ 실수 하나 — 앞값을 엉뚱한 데서 찾았다

처음엔 앞값을 **델타흐름 안에서** 찾았다. 그런데 `_delta_series` 는
쌍을 만들므로 **둘째 분기부터** 시작한다 — 첫 항목의 성장률이 통째로
없어졌다. 시험이 잡았다(`성장률: None`). 앞값을 **분기 목록**에서
찾도록 고쳤다.

## ④ 돌연변이 — 두 번 다시 만들었다

처음 만든 돌연변이 셋 중 둘이 **무효**였다:

    ① x["가속"] = True if False else (원래식)   → 아무것도 안 바뀜
    ③ c = "#00c805"; if (false) c = (...)       → 문자열이 남아 통과

**돌연변이가 실제로 적용됐는지부터 확인**하고 제대로 만들어 다시 돌렸다:

    ① 가속을 항상 True 로        → ❌ "둔화로 표시되지 않았습니다"
    ② 부등호 뒤집기(성장 < 앞성장) → ❌ 같은 실패
    ③ 그래프에서 색 구분 제거     → ❌ "색으로 나누지 않습니다"

## ⑤ 다중 에이전트 — 금지 문구를 지웠다 (주인 지시)

> "다중에이전트를 활용하는 게 정확성 및 다른 이점이 확실하다면 언제나
> 사용하고. 사용금지 같은 내용이 어딘가 들어있다면 그 내용은 삭제해."

`CLAUDE.md` 5장은 이미 주인 결정(2026-08-13)으로 해제돼 있었다.
남아 있던 두 곳을 고쳤다:

    인수인계.md 4장 10번  "쓰지 마라"        → "이득이 분명할 때 쓴다"
    전략.md 사고 백서 13번 "쓰지 않는다"      → "전면 금지는 아니다"

**실패 기록 자체는 사실이므로 지우지 않았다** (13개 중 11개 실패,
58만 토큰, 결과 0건, 한 세션 전수 처리가 더 정확했음). 남길 것은
"쓰지 마라"가 아니라 **"어디에 쓰느냐"** 다 — 결정적 산수·파싱·측정은
한 세션 전수 처리가 기본(검증 가능성 우선), 병렬이 이득인 일에는 쓰되
출력을 산술 검산으로 교차 확인한다.

`python3 run_tests.py` 종료코드 **0** · 파일 **20개** · **587개 통과**
(586 → 587).

---

# 150차-N (2026-08-23) — 화면↔데이터 정합성 점검기 + 스킬 (주인 요청 3번)

주인 지시: "스킬 또는 클로드.md에 위와 같은 내용을 검증할 수 있도록
내용을 작성해. 화면과 데이터의 정합성을 계속 검증할 수 있도록."

## ① 왜 필요한가 — 사고 여섯 건이 전부 같은 모양이었다

시험은 **코드**를 지킨다. 그런데 150차의 사고는 전부 **"코드는 멀쩡한데
화면이 다른 말을 하거나 아무 말도 안 한 것"** 이었다:

    150차-B  웹앱이 제타를 "신호 아님"으로 표시 (낡은 완성이 덮어씀)
    150차-C  "채택까지"가 계기판에만 있고 웹앱엔 없음
    150차-D  같은 앱이 "정배열"을 두 뜻으로 쓰면서 말 안 함
    150차-F  점검기와 앱이 관찰판을 다른 수로 셈 (89 vs 78)
    150차-J  가설 11개가 전부 "표본 없음"이라고만 말함
    150차-K  주인이 물어 꼬리 구멍 197건이 드러남

**여섯 건 중 다섯 건을 주인이 화면을 보다 발견했다.** 기계가 대신
봐야 한다.

## ② `screen_verify.py` — 11가지를 맞댄다

웹앱 파일에 **실린 값** vs 지금 수집물로 **다시 잰 값**:

    기준일 · 종목 수 · 신호 종목 · 한 종목 한 줄 · 목록 vs 종목상세 ·
    가설 수 · 채택 거리(정직화 칸) · 두 정배열 잣대 차이 ·
    빠진 분기 · 끊김 너머 신고점폭 · 판정 코드 버전

실행 결과(지금):

    기준일        웹앱 2026-08-21 · 데이터 2026-08-21
    신호 종목      웹앱 18개 · 지금 18개
    한 종목 한 줄   완성 목록 78줄 · 서로 다른 종목 78개
    가설 수       웹앱 24개 · 등록 24개
    채택 거리      없는 가설 0개
    ⚠️ 빠진 분기    170건 · 68종목 · 추정 182개 (원인 미규명)
    ⚠️ 끊김 너머 폭  2124칸 중 89칸 (최대 3,700% — CRDO 2026-06-01)
    → 이상 없음 (살펴볼 것 2건)

`⛔` 는 즉시 수리, `⚠️` 는 알려진 미해결(어제보다 늘면 파고든다).
**읽기 전용**이다 — 어떤 값도 고치지 않는다.

## ③ 여섯 가지로 일부러 깨뜨려 확인했다

장식이 아니라 **정말 잡는지**를 봐야 한다. `docs/data/app.json` 을
손으로 망가뜨려:

    신호 빼기      → ⛔ "웹앱에 빠진 것 ['ETSY','NET','ROST']"
    중복 넣기      → ⛔ "완성 목록 80줄 · 서로 다른 종목 78개"
    기준일 바꾸기   → ⛔ "웹앱 2026-08-14 · 데이터 2026-08-21"
    가설 빼기      → ⛔ "웹앱 20개 · 등록 24개 · 빠짐 [...]"
    채택거리 지우기  → ⛔ "채택 거리가 없는 가설 3개"
    잣대차이 바꾸기  → ⛔ "웹앱 7개 · 지금 41개"

여섯 가지 전부 잡혔고, 시험이 그 여섯을 각각 다시 확인한다.
점검기가 **파일을 안 고치는지**도 바이트 단위로 확인한다.

## ④ 스킬 `screen-verify` 등록

`.claude/skills/screen-verify/SKILL.md`. 절차에 **모바일 412px 렌더링**
확인을 넣었다 — 주인은 휴대폰만 쓰므로, 데이터가 맞아도 화면이 깨지면
소용없다. 요소마다 `getBoundingClientRect()` 로 412px 밖을 넘는지 본다
(가로 스크롤만 보면 `overflow:hidden` 에 가려 놓친다).

스킬 문서와 실제 파일이 짝이 맞는지도 시험이 본다 — 스킬이 없는
스크립트를 부르면 주인이 부를 때 죽는다.

## ⑤ 150차-I 의 시험이 **내 새 모듈을 잡았다**

`screen_verify.py` 를 만들고 시험을 돌렸더니:

    ❌ 설계도.md 에 안 적힌 모듈: ['screen_verify.py']

150차-I 에 "문서는 아무도 안 보면 반드시 썩는다"며 건 그물이 **하루 만에
제 손을 잡았다.** 설계도에 적고 `run_tests.py` 목록에도 넣었다.

`python3 run_tests.py` 종료코드 **0** · 파일 **21개** · **596개 통과**
(587 → 596).

---

# 150차-O (2026-08-23) — 궁금한 종목을 **몇 분 안에** 보기 (주인 요청)

주인 지시: "앱 화면에서 종목을 서치하면 현재 화면 상태의 데이터를 그대로
볼 수 있게. 우리가 데이터를 가지고 있지 않은 종목을 치면 새롭게 데이터를
다 긁어와서 그 자리에서 판단하는" → 이어서 "로봇이 긁어오는 걸 기다리지
말고 바로 긁어오게 할 순 있어?"

## ① 할 수 있는 것과 없는 것 (정직하게)

**웹앱 버튼으로 자동 실행은 안 됩니다.** 깃허브에 "지금 수집해"라고
시키려면 인증 토큰이 필요한데, 웹앱은 **공개 페이지**라 토큰을 넣으면
누구나 저장소 열쇠를 갖게 됩니다. 브라우저가 SEC 에 직접 접속하는 것도
서버가 없어 불가능합니다.

**대신 한 종목만 도는 워크플로**를 만들었습니다. 정기 수집은 401종목을
훑어 약 226분이지만, 한 종목이면 실측 10~25초(캐시 있을 때)이고 캐시
없는 새 종목이라도 몇 분입니다. 주인이 휴대폰에서 몇 번 누르면 됩니다.

## ② 가장 중요한 것 — **판정 표본을 건드리지 않는다**

궁금해서 부른 종목을 가설 판정 표본에 넣으면 **"결과를 보고 종목을
고르는 것"** 이 되어 사전 등록이 무너집니다(헌법 2조 · v1이 그렇게
무너졌습니다).

    collect_one.py    → data/lookup/ 과 docs/data/t/ 에만 쓴다
    collect_one.yml   → `git add data/lookup docs/data/t`
                        **data/measure 는 일부러 뺐다**
    화면              → "요청 수집분 — 가설 판정에는 쓰이지 않습니다"

시험이 세 곳 모두를 지킵니다. 돌연변이(`git add` 에 `data/measure` 추가)
→ ❌ "워크플로가 판정 표본을 커밋합니다".

## ③ 검색이 **250종목 전부**를 훑는다

전에는 "정배열 유지 + 최근 완성" 종목만 목록에 있어서, 조정 중인 종목은
**검색해도 안 나왔습니다** — 주인이 보기엔 "자료가 없다"입니다.

    목록 줄 수: 250   (전 75 + 78)
    'MU'   → 보이는 줄 2 · 못찾음판 안 뜸
    'NVDA' → 보이는 줄 1 · 못찾음판 안 뜸
    'TSM'  → 보이는 줄 0 · 못찾음판 **뜸**

## ④ 없는 종목을 치면 — 없다고 말하고 **방법을 준다**

    TSM — 자료가 없습니다
    아직 이 종목을 수집한 적이 없습니다. 지금 수집하면 몇 분 뒤
    이 화면에서 볼 수 있습니다.
      ▶ 지금 수집하기   (GitHub 실행 화면으로 가는 링크)
      GitHub → Run workflow → 종목 코드에 TSM 입력 → Run
    왜 화면에서 바로 안 되나: … 서버 없는 정적 페이지 …
    미리 알아 둘 것: 이렇게 받아 온 종목은 가설 판정 표본에 쓰이지 않습니다

모바일 412px 렌더링 확인 — 가로 412=412, 넘치는 요소 없음.

## ⑤ "살지 말지"는 넣지 않았습니다

주인이 "결과까지 주는 거지 살지 말지"라고 하셨지만 **채택된 신호가
0개**입니다. 이 상태에서 매수 판단을 내놓으면 근거 없는 점수를 먼저
만든 v1의 길입니다. 대신 **사실은 전부** 보여 줍니다 — 정배열 여부 ·
이격도 · 델타 흐름과 성장 둔화 · 실적 이력 · 첫 돌파 · 그 신호의 과거
실측 적중률.

## ⑥ 기존 시험이 걸렸다 — 약화가 아니라 **정밀화**

`test_웹앱_화면_파일이_그대로_있다` 가 "app.js 에 http 글자가 있으면
실패"였는데, 사람이 눌러야 열리는 링크를 넣자 걸렸습니다.

규칙을 느슨하게 하는 대신 **뜻을 정확히 나눴습니다** — 금지해야 할 것은
"화면이 **스스로** 바깥에서 받아오는 것"(`fetch(`·`<script src`·
`@import` 등)이고, 사람이 누르는 `<a href>` 는 아무것도 안 받아옵니다.
바깥 주소는 **이 저장소의 깃허브만** 허용합니다.

    돌연변이 ① fetch("https://evil.example.com/x") 넣기
             → ❌ "웹앱이 바깥에서 받아옵니다(fetch()"
    돌연변이 ② 링크를 남의 도메인으로
             → ❌ "허용되지 않은 바깥 주소: ['https://random-site.example/…']"

## ⑦ 주인이 직접 해야 하는 것

1. **GitHub Pages 켜기** — Settings → Pages → main / `/docs`
   (아직 안 켜져 있으면 웹앱을 휴대폰에서 못 봅니다)
2. **`collect-one` 워크플로를 한 번 눌러 보기** — 제가 개발 환경에서
   실행할 수 없습니다(SEC 차단). 실제로 도는지는 주인이 한 번 눌러야
   확인됩니다. 실패하면 로그를 보고 제가 고칩니다.

`python3 run_tests.py` 종료코드 **0** · 파일 **22개** · **601개 통과**
(596 → 601).

---

# 150차-P (2026-08-23) — "폭 3700%"가 화면에서 **거짓말을 멈췄다**

주인이 CRDO 화면에서 본 것을 화면이 스스로 설명하게 만들었다.

## ① 무엇이 문제였나

    2026-06-01  TTM 3.42  첫 돌파  **폭 3700%**
    2026-03-02  —  ·  2025-12-01  —  ·  2025-09-03  —
    2025-03-04  —  ·  2024-12-02  —
    2024-05-29  TTM 0.09  첫 돌파  폭 50%

직전 정점 0.09(2024-05-29)와 지금 3.42(2026-06-01) 사이 **2년의 TTM 이
전부 없다.** 그래서 여러 분기에 걸쳐 오른 것이 **한 번에 뛴 것처럼**
계산됐다. 산수는 맞지만 그대로 읽으면 거짓이다 — 인수인계 4장 7번
"끊긴 이력을 이어붙이지 마라"가 금지한 바로 그 모양.

전 유니버스 실측: 폭이 계산된 **2,124칸 중 89칸**이 이 모양.

## ② 계산은 **고치지 않았다**

`신고점폭` 은 H22·H22b 의 **사전 등록된 신호값**이다. 지금 본 것을 보고
계산을 바꾸는 것은 헌법 8조(결과를 보고 규칙을 고치지 마라) 위반이다.
**표시만** 달았다.

## ③ 화면이 이제 먼저 말한다

    발표일        TTM    신기록   폭
    2026-06-01   3.42   첫 돌파  3700% **⚠**
    …
    ⚠ 가 붙은 폭은 **중간 분기가 빠진 구간을 건너뛰어** 잰 값입니다.
      직전 정점과 지금 사이의 TTM 을 못 만들어서, 여러 분기에 걸쳐 오른
      것이 한 번에 뛴 것처럼 보입니다. 산수는 맞지만 그대로 읽으면
      안 됩니다.

    **수집에서 빠진 분기가 있습니다** — 2024-05-29 → 2024-12-02
      (187일, 약 1분기) · 2025-03-04 → 2025-09-03 (183일, 약 1분기).
      정상 간격은 약 91일입니다. TTM 은 연속 네 분기의 합이라, 빠진
      분기가 들어가는 창은 전부 "—"가 됩니다. **왜 빠졌는지는 아직
      밝히지 못했습니다** — 원문을 받아 확인 중입니다.

빠진 분기는 그동안 화면에 **그냥 없어서 안 보였다.** 없는 것을 없다고
말해야 주인이 "왜 빈칸이지?"를 다시 겪지 않는다.

원인을 **모른다고 화면에 적었다** — 세 번 짚어 세 번 반증당했으므로
(150차-K) 아는 척하지 않는다.

## ④ 시험 데이터에서 두 번 걸렸다 (남길 것)

가짜 종목을 만들 때 **YTD 오염 검사**가 두 번 값을 지웠다:

    1차: 앞 0.1 → 뒤 5.0, 6.0 …
         "B1: adj_eps=6.0 는 직전 4분기 합 5.30 과 거의 같고…" → 지움
    2차: 앞 0.1 → 뒤 2.0, 2.2 …
         끊김 뒤 "직전 4분기"가 A 셋 + B0 이라 합이 2.30 → 또 지움

**앞 구간 값이 너무 작으면, 끊김 뒤 '직전 4분기 합'이 새 값과 비슷해져
또 걸린다.** 앞 1.0 · 뒤 2.0~2.8 로 두니 통과했다. 한 칸이 지워지면
구간이 또 쪼개져 TTM 이 아예 안 생기므로 시험이 통째로 헛돈다.

## ⑤ 돌연변이 셋

    ① 끊김너머를 항상 False 로 → ❌ "표시가 안 붙었습니다"
    ② 빠진구간 안 세게         → ❌ "빠진 분기 구간을 못 찾았습니다"
    ③ 표에서 표시 안 달게      → ❌ "표가 끊김 표시를 조건으로 안 씁니다"

③은 처음에 `대상 없음`으로 **돌연변이가 적용조차 안 됐다.** 그대로
"통과"로 읽었으면 헛도는 시험을 믿을 뻔했다 — **적용됐는지부터 확인**
(150차-M 에 배운 것).

`python3 run_tests.py` 종료코드 **0** · 파일 **22개** · **602개 통과**
(601 → 602).

---

# 150차-Q (2026-08-23) — 빈칸의 원인을 **실행으로 확정**하고, 더 나쁜 것을 찾았다

주인 지시: "값이 없는 이유와 채워 넣을 방법을 강구해."

세 번 짚어 세 번 반증당했으므로(150차-K) 이번엔 **짐작 대신 코드를
돌려** 확정했다.

## ① 원인 — 뼈대 가운데 구멍은 **메울 길이 없었다**

`merge_quarters` 에서 보도자료가 할 수 있는 일은 둘뿐이었다:
  ① 기존 XBRL 행을 **덮어쓰기** ② **마지막 분기 뒤로** 승격

그래서 XBRL 뼈대 **가운데**가 비면 그 분기의 8-K 는 덮어쓸 행도 없고
승격 대상도 아니라 **통째로 버려졌다.** 실행으로 재현:

    XBRL  04-30 · (07-31 없음) · 10-31 · 01-31
    보도  09-03 발표, 조정EPS 0.5
    → 합친 결과 3행. 조정EPS 0.5 **사라짐**

실데이터 규모 — 발표일이 130일 넘게 벌어진 **170건**의 내역:

    한 분기 빠짐(150~215일)   **156건 · 61종목**   ← 복구 후보
    두 분기 빠짐(216~305일)      2건
    그 밖(305일 초과 등)        12건

## ② 파다가 **더 나쁜 것**을 찾았다 — 앞 분기를 덮어쓴다

값이 사라지기만 하는 게 아니었다. "가장 가까운 분기가 임자" 규칙은
**임자가 될 행이 있을 때만** 듣는다. 구멍이면 임자가 없어서 **앞 분기가
그 8-K 를 가져가 자기 숫자를 덮어썼다.**

    앞끝 04-30 · 구멍 07-31 · 뒤 10-31
    발표 08-14(앞끝+106일) → **앞 분기(04-30)를 덮어씀** ⛔
    발표 08-20(앞끝+112일) → **앞 분기를 덮어씀** ⛔
    발표 08-28(앞끝+120일) → **앞 분기를 덮어씀** ⛔
    발표 09-03(앞끝+126일) → 짝짓기 창(120일) 밖이라 버려짐

**값이 없는 것보다 나쁘다** — 앞 분기 숫자가 통째로 틀려진다.

## ③ 고친 것 둘

**(가) 가로채기 막기**: 분기 발표는 보통 분기끝 뒤 10~70일이다(138차
등록). **100일이 넘는** 8-K 가 이 행을 차지하려 하면 **다음 분기 자리가
비어 있는지** 본다. 비어 있으면 그건 그 빈 분기의 발표이므로 가져가지
않는다.

**(나) 구멍 메우기**: 짝짓기도 승격도 못 한 보도자료를 새 행으로 끼운다.
**분기끝을 지어내지 않는다** — 앞뒤 XBRL 행이 **정확히 두 분기**
(150~215일) 떨어져 있을 때만 그 중간을 분기끝으로 본다. 분기 달력이
정하는 자리이지 추정이 아니다. 간격이 애매하면 **아무것도 안 한다**.

    고친 뒤 — 앞끝+106 · +112 · +120 · +126 · +143일 **전부 구멍 메움 ✅**
    정상 발표(앞끝+34일)는 **그대로 앞 분기에 붙음 ✅**

안전 난간 넷: 이익 숫자 있는 발표만 · 분기끝 뒤 10~70일 · 이미 끼운
것과 60일 이상 · 기존 행과 45일 이상.

## ④ 돌연변이 검증 — 두 번 만들었다

처음 넷 중 **둘이 통과**했다. 확인해 보니 **다른 방어막이 대신 막고
있어서** 각 조건을 따로 검사하지 못한 것이었다(간격 조건을 꺼도 지연
조건이 막음). 시험을 조건별로 갈라 다시 만들었다:

    가로채기 방어 끄기      → ❌ "앞 분기(2024-04-30)를 덮어썼습니다"
    문턱을 0 으로(과잉 차단) → ❌ 3건 (기존 시험 2개 포함 — 멀쩡한
                              짝짓기까지 막으면 더 나쁘다)
    구멍 메움 끄기          → ❌ "구멍을 못 메웠습니다"
    이익 없는 공지도 끼우기  → ❌ "이익 숫자가 없는 공지를 분기로 만들었습니다"

> **남길 것**: 돌연변이가 통과하면 시험이 헛돈 것일 수도 있지만,
> **다른 방어막이 대신 막은 것일 수도 있다.** 어느 쪽인지 확인해야
> 각 조건이 정말 일하는지 안다.

## ⑤ 언제 채워지나

이 수리는 **보도자료를 다시 읽어야** 효과가 납니다 — 다음 정기 런
(내일 09:30 UTC 예약)부터입니다. 복구 후보는 156건·61종목이지만,
**실제로 몇 건이 채워질지는 그때 세어 봐야 압니다** — XBRL 에 정말
그 분기가 없는지, 8-K 가 있는지는 개발 환경에서 확인할 수 없습니다.

`python3 run_tests.py` 종료코드 **0** · 파일 **22개** · **609개 통과**
(602 → 609).

---

# 150차-R (2026-08-23) — 내가 **여러 회차에 걸쳐 틀린 안내**를 했다 (주인 지적)

주인이 GitHub Settings → Pages 화면을 찍어 보내며 물었다: **"잘되어
있는데?"**

맞다. **이미 켜져 있었다.**

    Your site is live at https://j2h5516-star.github.io/Model/

## ① 무엇을 틀렸나

인수인계.md "사람이 해야 할 것" 칸에 **"GitHub Pages 활성화 — Settings
→ Pages → main / /docs"** 가 적혀 있었고, 여러 세션이 그것을 그대로
읽어 주인에게 되풀이해 말했다. 주인은 이미 켜 둔 상태였다.

## ② 왜 틀렸나 — 못 본 것을 "없다"로 넘겨짚었다

세션에서 사이트를 직접 열어 확인했다:

    curl -sS https://j2h5516-star.github.io/Model/
    → curl: (56) CONNECT tunnel failed, response 403

**`github.io` 도 조직 정책 차단이다.** 사이트가 살아 있어도 세션은
못 연다. 앞선 세션들은 "확인이 안 된다"를 **"안 켜져 있다"** 로 바꿔
읽었다. 헌법 1조(창작 금지)가 금지하는 바로 그 짓 — **없는 값을
"없음"이 아니라 "틀림"으로 채운 것**이다.

> 이번 것은 숫자가 아니라 **상태**였지만 구조가 같다. 못 본 것은
> "못 봤다"이지 "없다"가 아니다.

## ③ 실제로 확인한 것 (돌려서 본 것만)

배포 경로가 `/Model/` 하위여도 되는지는 실행으로 확인할 수 있었다 —
파일을 읽으면 되니까:

    docs/app.js       DATA = "data/app.json"          → 상대경로 ✅
    docs/app.js       TICK = (t) => `data/t/${t}.json` → 상대경로 ✅
    docs/manifest.json "start_url": "./index.html#/home" → 상대경로 ✅
    docs/index.html   <script src="app.js">           → 상대경로 ✅

절대경로(`/data/...`)가 하나라도 있으면 `/Model/` 하위에서 404 가
난다. **없다.** 앞으로도 넣지 않는다.

## ④ 고친 것

- `인수인계.md` 5장: "Pages 활성화" 항목을 지우고 **웹앱 주소 기록 +
  경고문**으로 교체. 남은 "사람이 할 일"은 `collect-one` 워크플로를
  한 번 눌러 보는 것 하나뿐이다(세션은 SEC 차단으로 실행 불가).
- `CLAUDE.md` 6장 차단 목록에 **`github.io`** 추가 — 다음 세션이 같은
  넘겨짚기를 반복하지 않도록.

## ⑤ 남길 교훈

> **"내가 못 여는 것"과 "그것이 없는 것"은 다르다.**
> 확인 수단이 막혀 있으면 **모른다고 말하고 주인에게 묻는다.**
> 특히 **주인에게 시키는 일("~를 켜세요")** 은 근거를 실행으로
> 확인한 뒤에만 적는다 — 안 그러면 주인이 이미 한 일을 계속
> 시키게 된다.

---

# 150차-S (2026-08-23) — **직접 눌러 보니 한 번도 안 되던 기능이었다**

150차-R 에서 "주인이 `collect-one` 을 한 번 눌러 봐야 한다"고 적었다.
그런데 **내가 누를 수 있었다** — 깃허브 액션은 API 로 띄울 수 있고,
SEC 차단은 *개발 환경*의 제약이지 깃허브 서버의 제약이 아니다.
눌렀다. **실패했다.**

## ① 실행 로그 (그대로 옮김)

    === NVDA 지금 수집 ===
    [1/1] NVDA: 직접공시 39건 · 조정EPS 39건 · 캐시 100/신규 0 · 5.8초
    NVDA: 분기 0개 · 주가 있음 · 6.9초
    ⛔ NVDA: 실적을 못 읽었습니다
    ##[error]Process completed with exit code 1
    The following paths are ignored by one of your .gitignore files:
    data/lookup
    변경 없음 — 커밋 생략

수집은 **성공**했다(39건). 그런데 **분기 0개**. 그리고 파일을 만들고도
깃이 **버렸다**. 결함 셋이 한 줄씩 서 있었다.

## ② 결함 1 — 분기 자료를 버리는 함수에서 분기를 꺼내려 했다

`collect_job.collect_fundamentals()` 를 불러 `["quarters"]` 를 꺼냈다.
그 함수는 이렇게 생겼다:

    _quarters, report = sf.get_fundamentals(ticker, use_cache=False)
    ...
    reports[index] = report        # ← 분기는 `_quarters` 에 버려진다

**진단 보고만 돌려준다.** `"quarters"` 라는 칸은 아예 없다. 그래서
`.get("quarters") or []` 는 **언제나 빈 목록**이었다.

매일 도는 로봇은 다른 길로 간다 — `get_fundamentals()` 가 디스크에
저장해 둔 것을 `measure_store.load_cache → eps_rows` 로 읽는다.
**같은 길을 쓰게 고쳤다**: `sf.get_fundamentals()` → `ms.eps_rows()`.

## ③ 결함 2 — 주가를 판정 표본과 **다른 모양**으로 넘겼다

받아 온 일봉 표(DataFrame)를 그대로 `snap["prices"]` 에 넣었다.
판정 표본은 `{"dates": [...], "close": [...]}` 모양이다.
`dataset.build` 가 못 읽는다. 결함 1 때문에 여기까지 못 가서
**드러나지도 않았다.** `ms.price_rows()` 로 옮겨 적게 고쳤다.
기준지수(SPY)가 없으면 견줄 수 없으므로 그것도 성공 조건에 넣었다.

## ④ 결함 3 — 만든 파일을 깃이 버렸다

`.gitignore` 에 `data/*` 가 있고 `!data/measure/` 만 예외였다.
`data/lookup` 은 무시 대상이라 `git add` 가 거절했고 워크플로는
**"변경 없음 — 커밋 생략"** 으로 끝냈다. 성공했더라도 주인 휴대폰에는
아무것도 안 떴다. `!data/lookup/` 을 넣었다.

## ⑤ **왜 시험 609개가 이걸 못 잡았나** — 가장 중요한 대목

`tests/test_collect_one.py` 의 시험 5개는 이런 것들이었다:

    · 종목 코드 아닌 입력을 거절하나        (입력 검사)
    · 코드에 "snapshot.json" 글자가 없나     (글자 찾기)
    · 워크플로에 "python collect_one.py" 가 있나 (글자 찾기)

**수집 함수를 한 번도 부르지 않았다.** 초록불 5개가 전부
"안 되는 기능"을 지키고 있었다. 인수인계 4장 2번("새 코드 경로는
실제로 실행됨을 증명한다")을 적어 놓고 **내가 어겼다.**

새로 넣은 시험 둘은 실제로 부른다(SEC 는 안 두드리고 가짜로 갈아끼움):

    test_수집이_실제로_분기를_돌려준다
      분기 12개를 넣고 → 12개가 나오는지 · 칸 이름이 판정 표본과 같은지 ·
      주가가 {"dates","close"} 모양인지 · 화면 자료가 실제로 만들어지는지
    test_요청수집_파일이_깃에_올라간다
      `git check-ignore` 로 **깃에게 직접 물어본다**

돌연변이 셋으로 빨간 불을 봤다:

    옛 경로로 되돌리기       → ❌ "분기를 하나도 못 받았습니다"
    주가를 표 그대로 넘기기   → ❌ "주가 모양이 판정 표본과 다릅니다: DataFrame"
    data/lookup 다시 무시하기 → ❌ ".gitignore 에 걸려 커밋되지 않습니다"

## ⑥ 곁다리로 넣은 그물 — 웹앱 경로

150차-R 에서 손으로 확인한 "전부 상대경로"를 시험으로 바꿨다
(`test_웹앱_경로는_전부_상대경로다`). 주소가 `github.io/**Model/**`
하위라 절대경로가 하나만 들어가도 화면이 통째로 빈다. 개발 환경은
`github.io` 가 막혀 있어 **사람이 못 보는 자리**다 — 기계가 막는다.
`index.html`·`app.js` 를 각각 절대경로로 망가뜨려 빨간 불을 봤다.

## ⑦ 남길 교훈

> **"눌러 보라"고 남기기 전에 내가 눌러 볼 수 있는지부터 본다.**
> 개발 환경이 막혀 있어도 깃허브 서버는 안 막혀 있다. 남에게 시킬 일과
> 내가 못 하는 일은 다르다.

> **입력 검사와 글자 찾기만 하는 시험은 초록불을 켜 줄 뿐이다.**
> 그 기능의 **한가운데 함수**를 실제로 부르지 않으면, 시험 개수는
> 늘고 기능은 안 된다.

---

# 150차-T (2026-08-23) — 분기 **이름**이 652칸에서 거짓말을 하고 있었다

150차-S 에서 고친 요청 수집기를 실제로 돌려 GS 를 받아 왔다. 목적은
150차-Q 의 "구멍 메우기"가 실데이터에서 몇 건을 채우는지 보는 것이었다.

## ① 구멍 메우기 — GS 에서 **0건** 채웠다 (실측)

    옛 판정표본: 분기 30개 · 130일 초과 구멍 9개
    새 요청수집: 분기 30개 · 130일 초과 구멍 9개   ← 그대로

    2017-10-17 → 2018-04-17 (182일)
    2018-10-16 → 2019-04-15 (181일)
    ... 아홉 해 내리 같은 자리

구멍이 **아홉 해 내리 같은 자리**다. 분기 이름을 보면 이유가 보인다:

    GS: 22Q2 22Q3 23Q1 23Q2 23Q3 24Q1 24Q2 24Q3 25Q1 …

**Q4 가 하나도 없다.** 1월에 내는 연간(Q4) 실적이 통째로 안 잡힌다.
150차-Q 의 메우기는 "앞뒤 XBRL 행이 정확히 두 분기 떨어져 있을 때"만
끼우는데, 이 자리는 **8-K 보도자료 자체가 안 잡히거나** 뼈대에 걸릴
자리가 없어 조건에 안 닿는 것으로 보인다. **원인은 아직 미규명이다 —
단정하지 않는다**(142·145차에 두 번 반증당했다).

전체로 세면 빠진 분기의 **61.7%(92건)가 Q4** 다. 나머지는 Q1 32건 ·
Q2 20건 · Q3 5건. 한쪽으로 크게 쏠려 있다.

## ② 파다가 **더 큰 것**을 찾았다 — 이름이 겹친다

GS 를 보다 JPM 의 분기 이름이 이상해서 전수로 셌다.

    SLB   '20 Q4' 라는 이름이 붙은 행 **4개**
          기간끝 2021-03-31 · 2021-06-30 · 2021-09-30 · 2021-12-31
    INTC  '18 Q2' 라는 이름이 붙은 행 **3개**
          기간끝 2018-03-31 · 2018-06-30 · 2018-12-29

이름은 보도자료 본문에서 글자로 뽑는다(`extract_period_label`). 본문이
직전 분기나 작년 같은 분기를 함께 말하면 엉뚱한 것을 집는다. 그리고
합칠 때 이 이름이 XBRL 이 만든 **정확한** 이름을 덮어쓴다
(`sec_fundamentals.py:3049`).

### 얼마나 되나 — 두 번 재서 같은 값이 나왔다

먼저 "이름의 분기번호 ≠ 기간끝의 달력분기"로 세니 2,358칸(31.9%)이
나왔다. **이 숫자는 틀렸다.** CRDO 처럼 4월 결산인 회사는 회계 Q3 이
1월에 끝나 늘 2칸 밀린다 — 어긋난 게 아니라 **일정하게 밀린 것**이다.

제대로 재는 법: 종목마다 "이름의 분기번호 − 기간끝의 달력분기"를 모아
**가장 많은 값이 그 종목의 규칙**이라고 보고, 거기서 벗어난 칸만 센다.

    검사한 칸 7,399개 중 **어긋난 이름 652칸(8.8%) · 122종목**
    NUE 18/39 · INTC 17/31 · SLB 15/33 · WDC 15/32 · TTMI 15/32 …

## ③ 이 이름이 **어디에 쓰이나** — 계산에도 쓰이고 있었다

`data_quality.fiscal_quarter_of()` 가 **이름을 먼저** 믿었다. 이 함수는
두 곳이 쓴다: 계절성 판정 · 이상값의 "같은 분기 평소". 둘 다 **분기
번호로 묶기만** 한다. 이름이 겹치면 **서로 다른 네 분기가 한 묶음**이
된다.

고쳤다: **기간종료일을 먼저 본다.** 기간끝은 XBRL 이 준 구조값이라
글자로 뽑은 것이 아니다. 회계연도가 달력과 어긋나도 **어긋난 폭이
일정**하므로 묶음은 그대로 맞는다. 이름은 화면 표시용으로 남기되
**계산에는 안 쓴다**(헌법 1조: 탈락한 값은 고치지 말고 버린다).
기간끝이 아예 없을 때만 이름으로 돌아간다.

## ④ 고친 효과 — **오늘 바뀌는 숫자는 0개다** (실측)

정직하게 적는다. 옛 함수와 새 함수로 각각 전체를 돌려 맞댔다:

    계절 장사 판정  옛 0종목 → 새 0종목 · 뒤집힌 종목 **0개**
    분기 자료       250종목 중 달라진 종목 **0개**
    데이터 메모     2,196개 → 2,196개

이유: `detect_seasonality` 가 지금 표본에서 **한 종목도** 계절 장사로
판정하지 않는다(가장 높은 TJX 가 비 2.7). 두 소비처 모두 그 깃발 뒤에
있어 **지금은 아무 데도 안 쓰인다.**

> 그래도 고쳐 둔다. 지금 안 쓰인다는 것이 "틀려도 된다"는 뜻은 아니다.
> 종목이 늘거나 표본이 쌓여 어느 종목이 계절 장사로 잡히는 순간,
> 옛 코드는 **조용히** 네 분기를 한 묶음으로 셌을 것이다.

곁다리 관찰: 250종목 중 계절 장사가 0개라는 것 자체가 이상하다
(HD·TGT·TJX·COST 가 전부 False). 문턱을 **지금 만지지 않는다** —
데이터를 보고 문턱을 고치는 것은 헌법 2조가 금지한다. 관찰로만 남긴다.

## ⑤ 그물 둘

- `test_분기번호는_이름이_아니라_기간끝으로_정한다` — SLB 네 행이 서로
  다른 번호를 받는지. 돌연변이(이름 먼저로 되돌리기) → ❌ 빨간 불.
- `screen_verify` 12번째 검사 **"분기 이름 어긋남"** — 매일 세어 보고한다.
  회계연도가 달력과 다른 회사를 오탐하지 않는지(4월 결산 CRDO 형)까지
  시험한다. 돌연변이 셋(주류 규칙 없애기 · 안 세기 · 목록에서 빼기)
  전부 ❌ 빨간 불.

지금 값: **이름이 있는 7,399칸 중 어긋난 것 652칸 · 122종목.**

---

# 150차-V (2026-08-23) — 빈칸의 **가장 큰 원인**을 고쳤다 (주인 질문)

주인 질문: "값이 비는 게 많아 크리도도 그렇고 방법을 찾아야 해.
왜 값이 비는지 어떻게 찾아서 가져올 수 있는지 등"

## ① 원인 셋을 세었다 (전수)

    ① 단위 표기를 놓쳐 1,000배 작게 들어옴 → 검사에 걸려 버림
       영업이익 1,543칸 + 조정EBITDA 257칸 = **1,800칸 · 138종목**
    ② 누적(YTD)값이 분기 칸에 들어옴 → 버림          212칸
    ③ 분기 자체가 안 잡힘                            170건

크리도는 영업이익 19칸 중 **11칸**이 ①로 지워져 있었다.

## ② 짐작하지 않고 **원문을 봤다**

150차-S 에서 깃허브 액션을 직접 띄울 수 있음을 알았으므로, 계기를 달아
CRDO 를 다시 받았다. `collect_one` 이 원문 캐시를 읽어 보고한다.

**1차 결과 — 내 짐작이 틀렸다.** "3,000자 창이 좁아서 못 찾은 것"이라고
생각했는데, 원문 15건 전부 **단위 표기가 문서 어디에도 없었다.**

계기를 보강해 다시 받았다. 그러자 답이 나왔다:

    단위 낱말은 글에 있음 15건   ← "in thousands" 라는 말은 있다
    XBRL 맞댐: 같은 크기 8건 · **약 1,000배 11건**

    분기끝 2022-10-29  합쳐진값     3,399 · XBRL   3,152,000 → 927배
    분기끝 2023-01-28  합쳐진값     6,694 · XBRL   2,283,000 → 341배
    분기끝 2025-08-02  합쳐진값 96,197,000 · XBRL  88,000,000 → 1.0배

원문의 라벨 둘레를 보면 이유가 보인다 — 파서가 **표가 아니라 서술문**의
라벨을 물었다:

    "...non-GAAP gross margin, non-GAAP operating expenses,
     **non-GAAP operating income (loss)**, non-GAAP operating income
     (loss) margin, non-GAAP net income (loss)..."

이 문단 앞에는 표 머리가 없으니 단위도 없다.

## ③ 결정적 계기 — 출처로 갈라 보니 한쪽으로 몰렸다

    마진이 0.1% 미만(단위 의심)인 행의 출처
        직접공시(보도자료)  **1,467**  ·  근사치(XBRL) 4  ·  역산 12
    정상 마진 행의 출처
        근사치(XBRL) 4,166  ·  역산 518  ·  직접공시 1,434

**매출이 늘 맞았던 이유**도 여기 있다 — 매출은 **언제나 XBRL** 에서 온다
(실측: 매출 ÷ 매출_XBRL 이 전 종목 1.000). 보도자료가 덮어쓰는 것은
영업이익뿐이라, 매출만 달러이고 영업이익만 천 단위가 된다.

## ④ 고친 방법 — **XBRL 을 자(尺)로 삼는다**

XBRL 영업이익은 규정상 **절대 달러**다. 같은 분기의 GAAP 과 논갭이
1,000배 차이 날 수는 없다. 그래서 XBRL 이 **독립된 자**가 된다.

    배수 후보 1 · 1,000 · 100만 · 10억 중에서
    (보도값 × 배수) ÷ XBRL값 이 띠(0.05~20배) 안에 들어오는 것을 고른다
    → **딱 하나로 정해질 때만** 고친다. 아니면 아무것도 안 한다.

> **왜 이것이 창작이 아닌가**(헌법 1조): 숫자를 만드는 것이 아니라
> **같은 자릿수의 같은 숫자**를 어느 단위로 읽을지 셋 중에서 고르는
> 것이고, 그 선택을 **다른 자**가 정한다. 자가 없거나 하나로 안
> 정해지면 지금처럼 버린다 — "없음"은 안전하고 "틀림"은 위험하다.

같은 문서에서 온 조정 EBITDA 에도 **같은 배수**를 적용한다 — 한 표의
단위는 하나이기 때문이다.

고친 자취(`op_income_xbrl` · `unit_scale_fixed`)를 판정 표본에 남긴다.
자취를 안 남기면 나중에 감사할 수가 없다.

## ⑤ 돌연변이 — 하나가 헛돌았고, 그것도 적는다

    단위 고치기 아예 끄기        → ❌ 2건 빨간 불
    자(XBRL)를 안 남기기         → ❌ 빨간 불
    띠를 1,000배 넘게 넓히기      → ❌ 빨간 불
    "하나로 정해질 때만" 규칙 빼기 → **✅ 통과 (헛돌았다)**

마지막 것은 시험이 나쁜 게 아니라 **띠 폭 검사가 대신 막고 있어서**다.
배수 후보가 1,000배씩 떨어져 있고 띠가 1,000배 미만이므로 둘이 동시에
맞는 일이 구조적으로 없다. 그 **성질 자체**를 시험으로 바꿨다
(`test_어떤_띠에서도_배수는_둘_이상_맞을_수_없다`) — 후보에 100배를
끼워 넣는 돌연변이로 빨간 불을 봤다. 코드 주석에도 "이 줄은 지금 한 번도
안 걸린다"고 적어 두었다.

## ⑥ 곁다리로 **또** 잠든 시험을 찾았다 — 이번엔 8개

새 시험을 `test_parsing.py` 끝에 붙였는데 개수가 안 늘었다. 실행 스위치
(`if __name__ == "__main__":`)가 **파일 중간**에 있었고, 그 뒤에 적힌
시험은 그 시점에 아직 만들어지지도 않은 데다 블록이 `sys.exit` 로
끝나 **영원히 안 돈다.** 초록불은 그대로 뜬다.

    갇혀 있던 8개 — 그중 2개는 내가 만든 것도 아닌,
    예전부터 잠들어 있던 것이었다:
        test_xbrl_addbacks_are_used_only_when_every_quarter_has_them
        test_all_exceptions_are_recorded_not_just_the_first

**같은 사고가 두 번째다**(1차: 12개, 인수인계 4장). 사람이 조심해서 될
일이 아니므로 기계가 막는다 — `test_실행스위치_뒤에_갇힌_시험이_없다`
가 모든 시험 파일을 훑는다. 돌연변이(시험 하나를 스위치 뒤로 옮기기)로
빨간 불을 봤다. 러너를 맨 끝으로 옮기니 81 → 90개가 됐다.

## ⑦ 효과는 **다음 정기 런에서** 센다

이 고침은 보도자료를 다시 읽어야 효과가 난다. 오늘 09:30 UTC 예약 런
부터다. 복구 후보는 1,800칸이지만 **실제로 몇 칸이 살아나는지는 그때
세어 봐야 안다** — XBRL 영업이익이 그 분기에 정말 있는지는 받아 봐야
알기 때문이다. 미리 숫자를 말하지 않는다.

## ⑧ 아직 안 고친 것 — 누적(YTD) 오탐

②의 212칸을 갈라 보니, **79칸은 누적이 아니라 급성장으로 보인다.**
가르는 자로 "주당이익 ÷ 매출"이 이웃보다 튀는지를 썼다:

    누적으로 보임(제대로 지움)  126칸   예: LITE 20 Q4 이웃의 4.4배
    **급성장으로 보임(오탐 의심) 79칸**  예: CRDO 26 Q2 이웃의 0.9배

크리도 26 Q2 의 gaap 0.44 는 0.34 → 0.44 → 0.82 → 0.88 로 매끄럽다.
지금 검사는 "직전 4분기 합과 비슷하면 누적"으로 보는데, **1년에 2.5배씩
크는 회사는 한 분기가 지난 네 분기 합과 비슷해진다.** 게다가 ③(빠진
분기) 때문에 직전 4분기 합이 작게 잡혀 오탐이 더 늘어난다.

**문턱을 지금 만지지 않는다** — 데이터를 보고 문턱을 고치는 것은 헌법
2조가 금지한다. 갈라 세어 기록만 남기고, 고칠 규칙은 **사전 등록한 뒤**
새 데이터로 판정한다. 이 자가 갖는 맹점도 적어 둔다: 매출까지 함께
누적값이면 비가 안 튀어 못 가른다(MU 26 Q2 가 그 후보다).

---

# 150차-W (2026-08-23) — 고침을 실행으로 확인하고, 남은 한 칸을 위한 **두 번째 자**

## ① 150차-V 가 실제로 먹혔다 — 크리도를 다시 받아 확인

    기간끝         매출          영업이익       마진%     XBRL(자)     고친배수
    2025-11-01   268,027,000  124,120,000   46.31%  124,120,000   ×1000
    2026-01-31   407,012,000  201,782,000   49.58%  201,782,000   ×1000
    2026-05-02   437,003,000  216,722,000   49.59%  205,544,000   ×1000

마진 **0.05% → 49.6%**. 영업이익 **11칸**이 되살아났다.

덤: **빠져 있던 두 분기(2024-07-30 · 2025-05-03)도 돌아왔다** —
150차-Q 의 구멍 메우기가 크리도에서는 실제로 먹혔다(GS 에서는 0건).
19분기 → **21분기**.

## ② 남은 한 칸 — 자가 없는 행

    CRDO 2025-05-03  영업이익 62,523 ↔ 매출 170,025,000 = 마진 0.04%
                     XBRL 영업이익 **없음**

**구멍을 메워 새로 만든 행**이라 짝지을 XBRL 행이 없다. 자가 없으면
150차-V 는 아무것도 안 한다.

## ③ 처음 만든 규칙은 **위험했다** — 스스로 물렀다

먼저 "이웃 분기 마진 띠 안에 들어오는 배수로 고친다"를 만들었다.
오늘 표본에서 **1,048칸**이 살아났다. 숫자만 보면 성공이다.

그런데 이 규칙은 **진짜로 손익분기인 분기**(매출 10억, 영업이익 50만 =
마진 0.05%)를 1,000배 부풀린다. 지금은 그런 분기를 **버리는데**, 새
규칙은 **틀린 값을 만든다.** 헌법 1조가 금지하는 쪽이다.

> 1,048칸이 아깝다고 규칙을 남기지 않는다. **"없음"은 안전하고 "틀림"은
> 위험하다.**

## ④ 다시 만든 규칙 — 근거를 '마진'이 아니라 '증명된 배수'로

150차-V 의 XBRL 자가 **같은 종목의 다른 분기에서** 배수를 이미
정해 뒀다면, 그것은 "이 회사의 보도자료 표가 천 단위로 찍힌다"는
**독립된 증명**이다. 표가 천 단위면 그 표에서 나온 **모든 값**이
천 단위다 — 진짜로 작은 값이든 큰 값이든 똑같이 ×1,000 이 맞다.

조건 넷을 **모두** 넘어야 고친다:

    ① 같은 종목에서 XBRL 자가 정한 배수가 **하나뿐**이고 **2분기 이상**
    ② 이 행에는 XBRL 자가 없다 (있으면 150차-V 가 이미 봤다)
    ③ 지금 값이 매출의 0.1% 미만이다 (= 어차피 버려질 값)
    ④ 그 배수를 곱하면 이 종목의 성한 분기 마진 띠 안에 들어온다

**오늘 표본에서는 0칸이 고쳐진다** — `op_income_xbrl` 자취가 아직
없기 때문이다. 09:30 UTC 런 뒤부터 일한다. 미리 숫자를 말하지 않는다.

## ⑤ 돌연변이 여섯 — 둘이 헛돌아 시험을 갈랐다

    증명 요구 없애기          → ❌ 2건 빨간 불
    한 분기만 증명돼도 통과     → ❌ 빨간 불
    띠 검사 끄기              → ❌ 빨간 불
    배수 엇갈려도 첫 것 쓰기    → **✅ 통과 (헛돌았다)**
    XBRL 자가 본 행도 덮어쓰기  → **✅ 통과 (헛돌았다)**

갈라서 확인했다:

- **엇갈린 배수**: 엉뚱한 배수를 골라도 **띠 검사에 걸려** 아무것도
  안 한다. 즉 이 조건이 지키는 것은 '틀린 값'이 아니라 **'근거 없이
  고치지 않음'** 이다. 그 성질 자체를 시험으로 만들었다
  (`test_엉뚱한_배수는_어차피_띠에_걸린다`) — 후보에 100배를 끼워
  넣는 돌연변이로 빨간 불을 봤다.
- **XBRL 자가 본 행**: 성한 값인 행은 조건 ③ 이 이미 거르고 있었다.
  조건 ② 만 따로 재려고 **자가 있으면서 값도 수상한** 행을 만들어
  시험을 다시 썼다 → 돌연변이가 빨간 불을 봤다(8,256 → 8,256,000).

> 두 번째로 같은 모양이다(150차-Q·V). **돌연변이가 통과하면 시험이
> 헛돈 것일 수도, 다른 방어막이 대신 막은 것일 수도 있다.** 어느
> 쪽인지 갈라야 각 조건이 정말 일하는지 안다.

## ⑥ 사고 하나 — `git checkout` 으로 새 시험을 날렸다

돌연변이를 되돌리려고 `git checkout tests/test_dataset.py` 를 썼는데,
그 파일에는 **아직 커밋 안 한 새 시험 6개**가 들어 있었다. 통째로
사라졌다(바로 다시 넣었다).

> **남길 것**: 돌연변이를 되돌릴 때는 `git checkout` 이 아니라
> **미리 뜬 사본**(`cp 파일 /tmp/…` → `cp /tmp/… 파일`)으로 되돌린다.
> 커밋 안 한 작업이 있는 파일에 `git checkout` 을 쓰지 않는다.

---

# 150차-X·Y (2026-08-23) — 분기가 왜 통째로 없나, 그러다 **매출이 틀린 것**을 찾았다

## ① GS 의 Q4 가 왜 없나 — 원자료에서 봤다

계기(`빠진분기_진단`)를 달아 깃허브에서 GS 를 받았다.

    뼈대 분기 30개 · **끝나는 달 분포 {09: 10, 03: 10, 06: 10}**
    → 12월로 끝나는 분기가 **하나도 없다**

    [revenue]   3개월 0개 · 연간 0개
    [op_income] 3개월 0개 · 연간 0개      ← 은행이라 XBRL 에 없음
    [gaap_eps]  3개월 56개 · 연간 0개

**답**: GS 의 뼈대는 **GAAP EPS 하나로만** 만들어진다. 그런데 EPS 는
`연간 − (1+2+3분기)` 로 채우지 않는다 — 주식 수가 분기마다 달라 뺄셈이
성립하지 않기 때문이다(옳은 결정, 창작 금지). GS 는 4분기 EPS 를
3개월 단위로 신고하지 않는다. 그래서 **12월 분기가 영원히 없다.**

돈 항목(매출·영업이익)은 4분기를 채울 수 있지만 **GS 는 둘 다 XBRL 에
없다.** 채울 재료가 없다.

> 남은 길은 **1월 8-K 보도자료**뿐이다. 150차-Q 의 구멍 메우기는 앞뒤
> XBRL 행이 두 분기(150~215일) 떨어져 있을 때 끼우는데, GS 는
> 9월말 → 3월말이 182일이라 조건에 맞는다. 그런데도 안 끼워졌다.
> **왜인지는 아직 모른다 — 다음 회차 몫으로 남긴다.** 단정하지 않는다.

## ② 파다가 **더 나쁜 것**을 찾았다 — 매출이 틀렸다

GS 화면 자료를 보다가:

    GS 2026-06-30 분기 매출 = **15,520 달러**

실제는 155억 2천만이다. 전수로 세니 **26행 · 15종목**:

    XOM 198 · MRK 228 · TXN 3,414 · MS 10,857 · LOW 15,494
    HD 2026-08-18 **47,861**   ← 최근 행도 있다

**왜 안 걸렸나** — 세 겹이다:

1. 매출은 보통 XBRL(절대 달러)에서 온다. 이 26행은 **전부
   `revenue_xbrl` 가 없다** — XBRL 에 그 분기 매출이 없어 보도자료 값이
   단위 없이 그대로 들어왔다.
2. 이미 있는 매출 단위 고침은 **영업이익이 있어야** 돌아간다. 은행
   (GS·MS·JPM)은 XBRL 에 영업이익이 아예 없어 그 길로도 안 간다.
3. 자릿수 하한이 **1만 달러**라 15,520 은 그물을 빠져나간다.

**빈칸보다 나쁘다 — 틀린 값이 조용히 통과한다.**

## ③ 고친 방법 — 이 저장소가 이미 쓰는 잣대 그대로

매출 단위 고침은 예전부터 **이웃 분기와 크기가 맞을 때만** 고친다
(`_neighbor_revenue`). 같은 잣대를 쓰되, 전체 중앙값이 아니라
**가까운 분기 4개**로 잰다 — 빠르게 크는 회사를 오해하지 않기 위해서다.

    ① 이 행의 매출이 가까운 분기 중앙값의 **1/100 미만**인가
    ② ×1,000 / ×100만 / ×10억 중 **딱 하나만** 중앙값의 1/3~3배에
       들어오면 → 그 배수로 고친다
    ③ 하나로 안 정해지면 → **버린다**(없음)

실측 결과:

    고친 행 **15개** · 버린 행 **11개** · 고친 뒤 남은 이상 매출 **0행**

    고침: TXN 16 Q4 3,414 → ×100만 · MS 19 Q4 10,857 → ×100만
          HD 26 Q2 47,861 → ×100만 · LOW 16 Q4 15,494 → ×100만
    버림: MS 18 Q2~Q4 1,000,000(자리표시자로 보임) · MRK 16 Q3 228

## ④ 못 고친 것 — GS (정직하게 적는다)

GS 는 **모든 행**이 백만 단위로 들어와 있어 **견줄 성한 행이 없다.**
그래서 이 규칙은 GS 에 대해 아무것도 하지 않는다. 지어내지 않는
쪽이지만, 화면에는 여전히 "매출 15,520" 이 뜬다. **미해결이다.**

다행히 GS 의 잣대는 GAAP EPS 이고 그 값들은 멀쩡하다(8.40·14.12·
17.55·20.98). 매출은 화면 표시와 마진 검사에만 쓰인다.

## ⑤ 돌연변이 여섯 중 둘이 헛돌아 갈랐다

    고치기·버리기 끄기          → ❌ 2건
    못 정해도 안 버리기          → ❌
    성한 행이 모자라도 판단하기   → 💥 4건 (빈 목록 중앙값)
    배수 여럿이어도 첫 것 쓰기    → ✅ 통과 (띠가 대신 막음)
    전체 중앙값으로 재기         → ✅ 통과 (시험 지문이 못 갈랐음)

갈라서 다시 만들었다:

- `test_빠르게_크는_회사를_전체_중앙값으로_오해하지_않는다` — 5M→1,000M
  으로 200배 크는 회사. 전체 중앙값으로 재면 초창기 두 분기가
  **버려진다**(돌연변이가 ❌ "버림 2").
- `test_매출_배수도_둘_이상_맞을_수_없다` — 띠(9배)가 배수 간격(1,000배)
  보다 좁다는 **성질 자체**를 잰다. 띠를 100배로 넓히는 돌연변이가
  ❌ 빨간 불.

## ⑥ 진단기가 딴 길로 가면 거짓말을 한다 (150차-X)

`빠진분기_진단` 이 `_fill_missing_q4` 를 **직접** 부르고 있었다. 그러면
실제 수집 경로가 채우기를 그만둬도 진단은 "채워졌다"고 말한다.
돌연변이로 알았고, 실제 경로 함수(`_series_for_key`)를 그대로 부르게
고쳤다. **150차-S 와 같은 부류다** — 계기는 재는 대상과 같은 길을
써야 한다.

---

# 150차-AA·AB (2026-08-23) — **골드만삭스의 4분기가 아홉 해 내리 사라진 이유**

빠진 분기의 61.7%가 Q4 였다. GS 는 **Q4 가 하나도 없다.** 왜인지를
짐작하지 않고 계기를 달아 깃허브에서 세 번 받아 봤다.

## ① 1차 계기 — 뼈대는 3·6·9월뿐

    뼈대 분기 30개 · 끝나는 달 {09: 10, 03: 10, 06: 10}
    [revenue]   3개월 0개 · 연간 0개
    [op_income] 3개월 0개 · 연간 0개      ← 은행이라 XBRL 에 없음
    [gaap_eps]  3개월 56개 · 연간 0개

GS 의 뼈대는 **GAAP EPS 하나로만** 만들어진다. EPS 는 `연간 −
(1+2+3분기)` 로 채우지 않는다(주식 수가 분기마다 달라 뺄셈이 성립하지
않음 — 옳은 결정). 그래서 12월 분기가 뼈대에 없다.

## ② 2차 계기 — 1월 8-K 는 **있다**

    8-K 200건(상한에 걸림) · 실적으로 인정 73건 · 숫자 뽑음 41건
    논갭 영업이익 0건 · 조정 EPS 0건 · **GAAP EPS 39건**
    짝 못 찾음 12건 · **구멍 메움 0건**

1월 발표를 못 읽는 게 아니었다. 읽었는데 **행이 안 됐다.**

## ③ 3차 계기 — 짝 못 찾은 8-K 의 **날짜**를 남겼더니

개수만으로는 "1월 것이 남은 것"인지 "다른 분기가 가져간 것"인지 알 수
없어 날짜를 남기게 했다(150차-AA). 그러자:

    2018-01-17 · 2019-01-16 · 2020-01-15 · 2021-01-19 · 2022-01-18
    2023-01-17 · 2024-01-16 · 2025-01-15 · 2026-01-15
    (그 밖 2017-09-12 · 2022-02-17 · 2022-06-27)

    월별: **1월 9건** · 9월 1 · 2월 1 · 6월 1

**빠진 아홉 개의 Q4 와 정확히 같은 자리다.** 구멍 메우기는 기회를
얻고도 **0건**이었다.

## ④ 원인 — 문지기가 GAAP EPS 를 이익으로 안 봤다

150차-Q 의 구멍 메우기에는 문지기가 있다:

    if (조정EPS 없음 and 논갭 영업이익 없음 and 조정EBITDA 없음):
        건너뛴다        ← "실적 발표가 아닐 수 있다"

**은행은 그 셋을 아예 발표하지 않는다.** GAAP EPS 만 낸다. 그래서
GS 의 1월 발표가 전부 "실적 발표가 아님"으로 걸러졌다.

이 문지기의 목적은 **예비 매출 공지**가 분기를 차지해 진짜 발표를
밀어내는 것을 막는 것이다(9차 감사 CRDO 26Q3). GAAP EPS 가 실린
문서는 예비 매출 공지가 아니다. 게다가 **잣대 사다리는 gaap_eps 를
셋째 칸으로 이미 인정한다** — 여기서만 빼 두면 앞뒤가 안 맞는다.

고쳤다: 이익 숫자 목록에 `gaap_eps` 를 넣었다.

## ⑤ 돌연변이 둘 — 방향이 서로 반대여야 한다

    GAAP EPS 를 다시 빼기(옛 상태)  → ❌ "은행 발표가 구멍을 못 메웠습니다"
    문지기를 통째로 없애기            → ❌ 2건 "예비 공지가 분기를 차지했습니다"

한쪽만 봐서는 **너무 넓힌 것**을 못 잡는다. 넓히는 고침에는 반드시
"너무 넓히면 깨지는" 시험을 함께 둔다.

## ⑥ 효과는 다음 정기 런에서 센다

보도자료를 다시 읽어야 효과가 난다. 복구 후보는 GS 9건을 포함해
빠진 Q4 92건 가운데 **은행형(GAAP EPS 만 발표)** 인 것들이다.
**몇 건이 실제로 살아나는지는 그때 세어 봐야 안다** — 다른 종목은
원인이 다를 수 있다. 미리 숫자를 말하지 않는다.

## ⑦ 남은 것

- GS 는 **8-K 200건 상한**(`MAX_8K_SCAN`)에 딱 걸렸다. 은행처럼 공시가
  잦은 회사는 옛 분기가 잘린다. 상한을 올리면 수집 시간이 늘어난다 —
  **정기 런의 시간 예산을 보고 따로 판단할 일**로 남긴다.
- GS 매출은 여전히 백만 단위로 들어와 있다(150차-Y 에서 못 고침).

---

# 150차-AC (2026-08-23) — GS 매출의 출처를 원문에서 확인하고, **못 고치는 것을 말하게** 했다

150차-Y 는 "그 종목의 성한 분기"와 견주어 고친다. 그래서 **모든 분기가
다 어긋난 종목**은 견줄 데가 없어 손을 못 댄다. GS 가 그렇다.

## ① 계기를 넓혀 매출을 따로 봤다

예전 단위진단은 **논갭 영업이익 라벨이 없는 문서를 통째로 건너뛰었다.**
그래서 은행처럼 논갭을 발표하지 않는 회사는 한 건도 안 보였다 — GS 는
원문 200건 전부가 그랬다. 매출만 따로 세게 고치니:

    매출 라벨 있음 83건 · **매출 단위 못 찾음 82건** · ×100만 찾음 1건

## ② 원문을 보니 GS 는 **산문으로** 쓴다

    Net Revenues   1Q26   **$17.23 billion**
    Net Earnings   1Q26   $5.63 billion
    EPS1           1Q26   $17…

    Net Revenues   2025   $58.28 billion   4Q25   **$13.45 billion**

그런데 우리가 저장한 값은 **12,738**(2026-03-31 = 1Q26)이다.
**17.23 billion 과 아무 관계가 없다.** 산문 값을 집은 것이 아니라
표 어딘가의 다른 숫자(백만 단위)를 집었다.

> 즉 GS 매출은 "단위만 틀린" 것이 아니라 **다른 항목일 수 있다.**
> 배수를 곱해 고치면 안 된다 — 그건 틀린 값을 그럴듯하게 만드는 것이다.

## ③ 지금 하지 않은 것 — 파서 고치기

고칠 방향은 보인다: **자기 단위를 스스로 말하는 값**("$17.23 billion")을
**단위를 못 찾은 표 숫자**보다 앞세우는 것. 하지만 이 함수는 모든 항목이
쓰는 핵심이고, **정기 수집 40분 전**이다. 여기서 건드리면 오늘 런 전체가
위태롭다. **다음 회차로 미룬다.**

## ④ 대신 한 것 — 화면이 **말하게** 했다

고칠 수 없어도 **조용히 두면 주인은 그 숫자를 믿는다.**
`screen_verify` 14번째 검사를 넣었다:

    ⚠️ 종목 전체 매출  분기 최대 매출이 500만 달러 미만인 종목 1개
                      ['GS'] — 고칠 자가 없어 미해결(150차-AC)

돌연변이 셋으로 확인했다: 세기 끄기 → ❌ · 문턱을 크게 올려 멀쩡한
종목까지 잡기 → ❌ · 점검 항목 자체를 빼기 → ❌.

세 번째 돌연변이는 처음에 **통과했다.** 시험이 "코드에 글자가 있나"만
봤기 때문이다(`깨진매출['통째수상']` 이 다른 줄에도 있었다). 검사
이름(`적기("종목 전체 매출"`)을 보게 고치니 빨간 불이 떴다.
**글자 찾기 시험은 늘 이렇게 헛돈다**(150차-S 의 교훈 재확인).

---

# 150차-AD (2026-08-23) — 주 잣대에 **연간값이 남아 있다**. 고치려다 **더 나빠져 물렀다**

정기 수집이 10:02 UTC 에 시작(head_sha 350ea4a — 오늘 고침 전부 포함)해
코드를 만져도 그 런에 영향이 없는 창이 열렸다. 그 사이 정제된 데이터를
전수로 훑어 "검사를 통과했지만 말이 안 되는 값"을 찾았다.

## ① 훑은 결과 — 걸린 것은 한 종류뿐

    검사한 종목 250개 · 분기 9,466개
    같은 분기끝이 두 줄 · 발표일이 분기끝보다 앞 · 발표 200일 초과 ·
    매출총이익률 범위 밖 · 영업이익 > 매출 · EPS 절대값 100 초과
        → **전부 0건**
    조정EPS 가 GAAP 의 10배 초과 → 40건

40건을 하나씩 보니 **31건은 진짜**였다. 주식보상비·상각으로 GAAP 이
0.06~0.30 으로 눌린 회사들(NOW·CRM·QCOM·ABBV…)이다. 세어서 판단할 일이지
"이상해 보인다"로 버릴 일이 아니다.

## ② 그러다 **진짜 결함**을 찾았다 — 셔윈윌리엄스

    24 Q1 2.17 · 24 Q2 3.70 · 24 Q3 3.37 · **24 Q4 11.33**
    25 Q1 2.25 · 25 Q2 3.38 · 25 Q3 3.59 · **25 Q4 11.43**

2.17+3.70+3.37 = 9.24. **4분기 자리에 연간값이 들어와 있다.** 그것도
네 해 내리. 그런데 이것은 **조정 EPS** — 이 모델의 **주 잣대**다.
Q4 마다 가짜 신고점과 가짜 가속이 생긴다. 모델이 찾는 바로 그 모양이다.

GAAP EPS 쪽은 검사가 잡았다(`SHW 24 Q4 gaap_eps=10.55 … 없음 처리`).
**조정 EPS 만 빠져나갔다.**

## ③ 왜 못 잡았나 — 실행으로 확정

검사는 "누적값은 **직전 4분기의 합**과 거의 같다"를 본다. 돌려 보니:

    자리  분기끝        값    지난중앙  직전4합   배수  합대비차
    6   2019-12-31  21.12    6.15    31.80   3.4    33.6%  못 잡음
    22  2023-12-31  10.35    3.84    17.26   2.7    40.0%  못 잡음
    26  2024-12-31  11.33    3.65    19.59   3.1    42.2%  못 잡음
    30  2025-12-31  11.43    3.59    20.55   3.2    44.4%  못 잡음

**직전 4분기 합이 이미 오염돼 있다** — 그 안에 작년 연간값이 들어 있다.
한 번 놓치면 다음부터 영원히 못 잡는다. 첫 것(2019)부터 놓쳤으므로
반복 통과(`_drop_cumulative_values`)로도 풀리지 않는다.

## ④ 고치려 한 방법과 **왜 물렀나** (측정 결과 그대로)

기대 연간값을 "직전 4분기 합" 대신 **"4 × 최근 8칸의 중앙값"**(오염에
강한 중앙값)으로 바꿔 보았다.

    옛 방식 232칸 지움 · **새 방식 298칸 지움**
    둘 다 108 · 새 방식만 187 · 옛 방식만 123

숫자만 보면 더 많이 잡는다. 그런데 **새로 지운 것을 보니**:

    AMD 21 Q1 gaap_eps 0.45 · AMD 25 Q2 0.54 · AMD 26 Q1 0.84
        ← AMD 의 **평범한 분기 EPS** 다. 지우면 안 된다.
    RCL 1칸 → 3칸 · MPC 3칸 → 5칸 · CCL 1칸 → 2칸
        ← 크루즈·정유의 **진짜 계절 성수기**를 죽인다.

빠르게 크는 회사는 "최근 중앙값 × 4" 가 지금 분기와 가까워진다 —
150차-V 에서 본 급성장 오탐과 **같은 함정**이다.

> **채택하지 않는다.** 더 많이 잡는 것이 더 좋은 것이 아니다.
> 잃는 쪽이 평범한 분기와 진짜 계절성이면 그건 개선이 아니다.

## ⑤ 그럼 무엇이 옳은가 — 아직 모른다 (단정하지 않음)

생각해 본 다른 자: "그 회계연도의 앞 세 분기 합을 뺀 나머지가 그럴듯한
분기값인가". SHW 는 11.33 − 9.24 = 2.09 로 맞고, AMD 1분기는 앞 분기가
없어 **구조적으로 안 걸린다.** 그런데 RCL 의 3분기 성수기도 우연히
조건을 만족한다(3.86 − 3.4 = 0.46). **아직 가르지 못한다.**

## ⑥ 지금 아는 것 — "한 분기만 늘 3배"인 곳 25건

    ENTG·CIEN adjusted_ebitda  355~574배  ← 단위 오류(150차-V 부류)
    BXP gaap_eps Q4 97배 · SYNA Q2 12배   ← 수상
    SHW adj_eps Q4 3.1배 · MDLZ adj_eps Q4 3.5배 · XOM/AEP/CI/NRG Q4 ~3~4배
    RCL Q3 2.6배 · MPC Q2 2.6배 · ALB Q2 2.6배  ← **진짜 계절성**

칸별 gaap_eps 14 · adjusted_ebitda 6 · adj_eps 5 · 분기별 Q4 14 · Q2 6.

**이 목록에는 오염과 진짜 계절성이 섞여 있다.** 25건을 한 번에 처리하는
규칙은 아직 없다. 화면 경보로 띄우지 않는다 — 0 이 될 수 없는 경보는
사람이 곧 무시하게 된다.

## ⑦ 남길 것

> **더 많이 잡는 규칙이 더 좋은 규칙이 아니다.** 새 규칙을 넣기 전에
> **무엇을 잃는지** 반드시 함께 세라. 이번엔 AMD 의 평범한 분기와
> 크루즈의 성수기였다.

> **오염은 스스로를 지킨다.** "직전 4분기 합"처럼 **과거 값으로 만든
> 기준**은, 그 과거가 이미 오염되면 검사 자체를 무력화한다. 누적 검사를
> 다시 설계할 때는 **오염에 안 닿는 기준**(구조·달력)을 써야 한다.

---

# 150차-AE (2026-08-23) — 정기 런이 **300분에 잘렸다**. 스스로 못 빠져나오는 덫이었다

오늘 09:30 예약 런(실제 10:02:40 시작 · head_sha 350ea4a)이 **취소**됐다.
커밋 단계는 건너뛰어졌고 **오늘 수집은 통째로 없다.**

## ① 시간이 어디로 갔나 (로그에서 복사)

    8-K 수집     10:03:23 → 14:28:13  = **265분** (400/401 종목 완료)
    주가·컨센서스 14:28:57 → 15:02:56  = 34분 (진행 중 취소)
    ─────────────────────────────────────────────────────────
    필요했던 시간 약 **305분** · 한도 **300분** → **5분이 모자랐다**

원인은 5차 확장 **151종목의 첫 수집**이다. 로그가 그대로 말한다:

    [94/401]  NRG  … 캐시 106/신규 0   · 53.5초
    [250/401] AMKR … **캐시 0/신규 92** · 309.7초
    [311/401] SYF  … **캐시 0/신규 200** · 735.0초

250종목까지는 캐시가 받쳐 주다가, 그 뒤 새 종목은 **전부 캐시 0**이라
종목당 100~900초가 걸렸다. 어제(250종목·캐시 적중)는 140.8분이었다.

## ② **진짜 결함은 시간이 아니라 캐시였다**

워크플로는 `actions/cache@v4` 하나로 복원과 저장을 함께 했다.
그 액션은 **작업이 성공했을 때만 저장한다.** 취소되자 저장 단계가
통째로 건너뛰어졌다(로그: `Post 공시 원문 캐시 복원 … skipped`).

그래서 **4시간 26분 동안 받은 새 151종목 공시 원문이 전부 버려졌다.**

    내일도 캐시가 비어 있다 → 똑같이 4시간 반이 걸린다 → 또 취소된다
    → 또 캐시가 안 남는다 → …

**스스로 못 빠져나오는 덫**이다. 시간 한도만 올렸다면 이 고리는 그대로
남았을 것이다.

## ③ 고친 것 둘

**(가) 캐시 복원과 저장을 나눴다.** `actions/cache/restore` 로 받아 쓰고,
수집 실행 **바로 뒤**에서 `actions/cache/save` 를 `if: always()` 로
저장한다. 시간 초과로 취소돼도 받은 원문은 남는다.

**(나) 한도 300 → 350분.** 실측 필요 시간이 305분이었다. 깃허브 한도가
360분이므로 350은 안전하다. 캐시가 차면 어제처럼 140분대로 내려간다.

> 8-K 훑기 예산(190분)은 **손대지 않았다.** 예산이 못 자른 이유는
> 바닥(`COLLECT_BUDGET_FLOOR = 24`) 때문인데, 그 바닥은 "뒷쪽 종목이
> 최신 분기까지 잃는 것"을 막으려고 94차에 일부러 넣은 것이다.
> 캐시가 차면 저절로 풀리는 문제라 지금 건드리지 않는다.

## ④ 돌연변이 셋 — 전부 빨간 불

    캐시 저장 단계 지우기          → ❌ "저장 단계가 없습니다"
    if always() 떼기               → ❌ "always 조건이 없습니다"
    통짜 actions/cache 로 되돌리기 → ❌ "복원 단계가 없습니다"

## ⑤ **내가 만든 그물에도 결함이 있었다**

새 시험을 넣자 150차-V 의 "실행 스위치 뒤에 갇힌 시험" 검사가
**가짜 경보**를 냈다. 그 검사는 `글.find('if __name__ ==')` 로 스위치를
찾는데, **그 글자가 그 검사 자신의 설명글 안에도 있었다.** 설명글을
스위치로 착각해, 그 뒤의 시험을 전부 "갇혔다"고 말했다.

줄 **맨 앞**에서 시작하는 것만 스위치로 보게 고쳤다(설명글 안의 인용은
들여쓰기가 있어 안 걸린다). 돌연변이(옛 방식으로 되돌리기) → ❌ 빨간 불.

> **남길 것**: 글자 찾기로 코드 구조를 판단하면, **그 글자를 설명하는
> 글에도 걸린다.** 구조를 보려면 줄 맨 앞·들여쓰기 같은 **구조 표시**를
> 함께 봐야 한다.

## ⑥ 오늘 데이터는 어떻게 되나

**어제(08-22) 수집물이 그대로 남아 있다.** 잃은 것은 "오늘 하루치를 못
채운 것"이고, 있던 데이터가 깨진 것은 아니다. 화면도 어제 것 그대로다.

고침을 넣었으니 **지금 수동으로 다시 돌린다** — 마지막 성공(08-22 12:50)
뒤 6시간을 훌쩍 넘겨 99차의 SEC 429 규칙에 걸리지 않고, 실측 초당요청도
0.28건(허용 10건의 3%)이라 여유가 있다.

---

# 150차-AF — 값이 비는 진짜 이유를 원문에서 찾았다 (2026-08-23)

주인이 **"값이 비는 게 많다. 크리도도 그렇고. 왜 비는지, 어떻게 찾아
가져올 수 있는지 찾아라"** 고 했다. 계산 뒤가 아니라 **읽는 자리**를
팠다. 세 가지 후보를 전부 원문 1,443건 전수로 재고, **둘만 채택했다.**

## ① 왜 비는가 — 먼저 센 것

`data/measure/snapshot.json` 의 9,466개 분기 행에서 빈칸 비율:

| 항목 | 빈칸 | 비율 |
|---|---:|---:|
| 매출 | 255 | 2.7% |
| 영업이익 | 1,655 | 17.5% |
| 조정 EPS | 4,987 | 52.7% |
| GAAP EPS | 447 | 4.7% |
| 조정 EBITDA | 8,540 | 90.2% |
| 매출총이익률 | 4,574 | 48.3% |

그런데 **"비어 있는 것"보다 나쁜 것이 있었다 — 채워져 있는데 틀린 것.**

    GS   매출 10,115 · 12,738 · 15,520      ← 달러 단위. 참값은 101억·127억·155억
    HD   최신 분기 47,861, 직전 분기 382억  ← 한 종목 안에서 단위가 뒤섞임
    AMZN 728 · 1,611 · 20,576              ← 참값 1,556억·1,878억·1,677억

## ② (가) 괄호 없는 단위 선언 — **채택**

지금까지 알아보던 단위 표기는 **전부 괄호를 요구**했다. 그런데 GS 는:

    Segment Net Revenues (unaudited)
    $ in millions                     ← 괄호가 없다
    Net revenues            12,738

원문 1,443건 실측:

    "$ in millions" 꼴 (괄호 없음)   194곳 / 21종목  (GS·FITB·DIS·DE·KEY…)
    줄머리 맨 "In millions" 꼴       181곳 / 14종목  (HPE·EOG·LOW·HD·CVS…)

둘째 것을 **줄머리로 한정**한 근거도 실측이다 — 걸린 서로 다른 줄
**51가지가 전부 표 머리**였고 서술문은 한 건도 없었다.

효과: 해당 24종목 214건 중 **33건 / 11종목**의 매출이 제자리로.

## ③ (나) 부문 줄이 합계 줄을 이김 — **채택**

은행·증권사는 부문 표를 먼저 싣고 합계를 뒤에 놓는다. 이름과 값 사이가
전부 공백뿐이라 **거리가 똑같이 0** 이 되고, 그러면 먼저 나온 것이 이긴다.

    Net revenues        12,738   ← Global Banking & Markets 부문 (이게 뽑혔다)
    Net revenues         4,078
    Net revenues           411
    Total net revenues  $17,227  ← 진짜 전체 매출

종전 맨 앞 이름 `total revenues` 는 사이에 `net` 이 끼어 있어 안 걸렸다.
그 **한 낱말** 때문에 놓쳤다.

    "Total net revenues"  215곳 / 17종목  (SCHW·GS·EXTR·ROKU·PYPL·JPM…)
    "Total net sales"      53곳 / 11종목  (GNRC·HD·PG·CL·AMZN…)

전수 재측정 — 바뀐 것 **23건 / 8종목**:

    AMZN 728 → 1,556억 · 20,576 → 1,677억 · 1,611 → 1,878억
    CL   203억(연간!) → 52.3억(분기)
    SBUX 70.7억 → 94.0억 · JPM 202억 → 573억 · GNRC 1.88억 → 11.7억
    GS   2.0 → 134억

## ④ (다) 거리를 숫자 시작 자리에서 재기 — **버렸다**

거리가 표 줄에서 전부 0 으로 묶이는 것이 문제의 뿌리이니, 재는 자리를
공백 뒤(숫자가 실제로 시작하는 곳)로 옮기면 풀릴 것 같았다.

전수로 재니 **442건 / 141종목**이 바뀌는데, 바뀐 것을 보니:

    AAL  9,420 → 201        ABNB 37억 → 4.59억
    ALL  132억 → -100만     AMBA 정상값 → 12

**서술문이 표를 이겨 버린다.** 채택하지 않는다.
(150차-AD 와 같은 결론 — 더 많이 잡는 규칙이 더 좋은 규칙이 아니다.)

## ⑤ 창(3,000자) 문제 — 조건 둘을 얹어 **채택**

(나)를 넣자 새 문제가 드러났다. 합계 줄은 부문 표들 **뒤**에 있어
단위 선언에서 멀다. GS 는 3,225자 — 창(3,000자) 밖이다. 그래서
숫자는 맞는데(17,227) 단위를 잃었다. 이런 것이 6건.

창을 문서 전체로 넓혀 재니 **47건이 바뀌는데 27건이 잘못됐다**:

    Citi 10.35조 · JPM 100조 · TROW 5.6조   ← 멀리 있는 "billion" 선언
    ENPH·HON·PEP·TSN 202 → 2.02억            ← 서술문·슬라이드 숫자

88차에 적어 둔 **"그럴듯해져서 더 위험하다"** 가 그대로 재현됐다.
그래서 두 조건을 함께 요구했다:

  **(가) 멀리 있는 10억 선언은 안 쓴다.** 재무제표 표는 천·백만으로
  적지 10억으로 적지 않는다. 실측 — 먼 곳의 10억 선언 **6건이 6건 모두
  틀렸다.**

  **(나) 숫자가 표의 칸에 있을 때만 쓴다.** `(in millions)` 는 그 **표**의
  단위 선언이지 문장의 단위가 아니다. 이름과 값 사이가 공백 두 칸 이상
  벌어진 것을 표의 칸으로 본다(서술문은 한 칸으로 붙는다).

전수 재측정 — 바뀐 것 **29건 중 23건이 제자리로**:

    GS 5 · MCK 3 · LLY 3 · MET 2 · MCD 2 · CMCSA 2 · NFLX 2 · AFL · BAC · CL · TTMI
    GS 2026 1분기 17,227 → 172.27억 (드디어 맞다)

남은 6건(CGNX 3 · SO 2 · JPM 1)은 **고치기 전에도 틀린 값**이고,
`dataset.py` 의 자릿수 안전망이 이웃 분기와 견주어 잡는다.

## ⑥ 돌연변이 — 여덟 전부 빨간 불

    새 단위 패턴 둘 삭제 · 줄머리 패턴만 삭제 · 줄머리 닻(^) 제거
    새 이름 둘 삭제 · 새 이름을 맨 뒤로 옮기기
    먼 선언 쓰기 통째로 없앰 · 표의 칸 조건 제거 · 10억 거르기 제거

## ⑦ 내 계기가 두 번 틀렸다

- 단위 선언까지의 거리를 재려고 `_detect_table_unit` 을 가로챘는데,
  그 함수는 **후보마다** 불린다. 마지막 호출을 기록해 **이기지 않은
  후보**의 거리를 쟀다. 이긴 후보를 되짚게 고쳤다(교훈 25 재확인).
- 먼 선언 시험에 메꿈 글을 2,700자만 넣었다. **창(3,000자)보다 짧아**
  가까운 길로 통과했다 — 시험이 아무것도 재지 않을 뻔했다. 이제
  시험 안에서 `assert 글자수 > 3000` 으로 스스로 지킨다.

> **남길 것**: 창·문턱을 시험할 때는 **메꿈이 문턱을 넘는지 시험이
> 스스로 확인하게** 하라. 안 그러면 초록 불이 거짓말을 한다.

---

# 150차-AG — 고친 것이 영업이익에도 들었다. 그리고 후보 하나를 더 버렸다 (2026-08-23)

## ① 버려지던 값 1,800칸의 정체 — 이유를 전수로 셌다

`_clean_quarters` 가 남기는 쪽지 2,206건을 이유별로 묶으니 서로 다른
이유가 431가지인데, **압도적 다수가 한 종류**였다:

    "영업이익 21,779 은 매출 103,790,000 대비 마진 0.021% — 단위 착오 의심"

곧 **매출과 영업이익의 단위가 서로 다르게 읽힌 것**이다. ×1,000 하면
마진이 21% 로 멀쩡해진다. 실물 예:

    POWI 21,779 → 21%   NOW 52,341 → 12%   MCHP 390,511 → 39%
    ISRG 390.6  → 42%   NVDA 708    → 35%   AMGN 2,621  → 42%

## ② 150차-AF 고침이 영업이익·EBITDA 에도 그대로 듣는다

`_detect_table_unit` 은 모든 항목이 함께 쓰는 자리라, 매출만 고친 것이
아니었다. 원문 1,443건 전수 재측정:

| 항목 | 바뀐 파일 | 종목 |
|---|---:|---:|
| 매출 | 29 (앞서 셈) | 14 |
| **논갭 영업이익** | **78** | **30** |
| **조정 EBITDA** | **39** | **19** |

전부 옳은 방향이었다 — AMD 2,854 → 28.5억 · CRDO 216,722 → 2.167억 ·
DASH 430 → 4.3억 · CVS 16,620 → 166.2억 · APD 399.9 → 3.999억.

## ③ **버린 후보: "단위를 아는 값이 모르는 값을 이긴다"**

150차-AC 가 남긴 숙제("자기 단위를 말하는 값을 앞세우기")를 **좁게**
다시 잤다 — 거리 규칙은 그대로 두고, 끝내 단위를 못 찾은 값만 한 등급
뒤로 미루는 것. 전수 결과 **매출 97건 / 영업이익 19건**이 바뀌는데:

    얻는 것   CRWD 358 → 3.377억 · HPE 영업이익 -1 → 27억 · C 4 → 205억
    잃는 것   JPM 매출 19.8 → **17.79조** (자산 항목을 물었다)
              FITB -30 → 2,010억 · KLAC 6,000 → 9,000만 · C 10,353 → 0

**채택하지 않는다.** 얻는 만큼 심하게 잃고, 잃는 쪽이 조 단위다.
150차-AF ④(거리 재는 자리 옮기기)에 이어 이 방향의 **두 번째 반증**이다.

> **남길 것**: "단위를 모르는 값"을 뒤로 미루면 그 자리를 **다른 표의
> 엉뚱한 값**이 채운다. 단위를 모르는 것이 문제가 아니라, 그 문서에서
> **어느 줄이 맞는 줄인지**가 문제다. 줄을 고르는 일과 단위를 붙이는
> 일은 따로 풀어야 한다.

## ④ 채택: 단위 선언이 표 **아래 각주**로 붙는 경우 (실물 MCHP)

MCHP 는 이렇게 쓴다:

    Net sales                                    $1,484.7
    ...
    (1) In millions, except per share amounts and percentages of net sales.

**앞쪽만 보는 규칙**이라 이 문서는 단위를 영영 못 찾았고, MCHP 매출이
14.8억이 아니라 **1,484.7 달러**로 들어왔다. (각주 꼴 `(1) In millions`
는 종전 괄호 패턴에도 안 걸린다 — `\([^)]…` 가 닫는 괄호를 못 넘는다.)

여는 조건은 앞의 것과 같다 — **표의 칸**이어야 하고 **10억은 안 쓴다**.
여기에 하나를 더 좁혔다: 숫자 **바로 뒤 2,000자** 안의 각주만 본다.
더 멀리 보면 다음 표의 각주를 이 표의 것으로 오해한다.

전수 실측 — 바뀐 것 **7건 / 3종목**뿐이고 사고는 없었다:

    MCHP 4건  1,484.7 → 14.847억 · 1,186 → 11.86억 · 1,075.5 → 10.755억 · 1,649.8 → 16.498억
    BMY  2건  42,518 → 425.18억 (자릿수 맞음)
    EOG  1건  32 → 3,200만 (고치기 전에도 틀린 값)

돌연변이 셋 전부 빨간 불(각주 보기 통째로 없앰 · 10억 거르기 제거 ·
각주 창을 문서 전체로).

---

# 150차-AH — "값이 왜 비는가"를 끝까지 세었다 (2026-08-23)

주인의 물음("값이 비는 게 많다. 왜 비는지, 어떻게 가져올 수 있는지")에
대한 **숫자로 된 답**이다. 짐작 없이 계기만 읽었다.

## ① "빠진 분기"는 분기가 없는 것이 아니다 — **발표일이 없는 것**이다

150차-K 이후 "원인 미규명"으로 남아 있던 자리다. 실물 ABNB:

    filing_date  announced_date  revenue        press_matched
    2025-03-31   (없음)          2,272,000,000  False
    2025-06-30   (없음)          3,096,000,000  False
    2025-09-30   (없음)          4,095,000,000  False
    2025-12-31   (없음)          2,778,000,000  False

**분기도 있고 매출도 있다.** 없는 것은 발표일뿐이다. 그런데 측정은
발표일로만 전 종목을 줄 세우므로(전략.md 7장), 발표일이 없으면 그 분기는
측정에 못 들어간다. 화면의 "빠진 분기"는 발표일 간격이 130일을 넘는
자리를 센 것이라 이렇게 보인다.

전체 9,466행 중 **발표일 없음 1,754행 = 18.5%.**

## ② 구멍은 **오래된 분기**에 몰려 있다

| 연도 | 발표일 없음 / 전체 | 비율 |
|---|---:|---:|
| 2016 | 293 / 385 | **76.1%** |
| 2017 | 526 / 893 | **58.9%** |
| 2018 | 358 / 917 | 39.0% |
| 2019 | 256 / 947 | 27.0% |
| 2020 | 163 / 959 | 17.0% |
| 2021 | 75 / 956 | 7.8% |
| 2022 | 38 / 973 | 3.9% |
| 2023~2026 | 45 / 3,436 | 1.3% |

최근 자료는 사실상 다 차 있고, **금리 인상기(2016~2018)가 절반 넘게
비어 있다.** 헌법 4조가 요구하는 앞/뒤 시간 분할에서 '앞'이 얇다.

## ③ 상한(`MAX_8K_SCAN = 200`)은 주범이 **아니다**

단정하기 전에 갈라서 셌다(142·145차 반증 교훈):

    상한에 닿음(캐시 ≥195) 7종목  → 2016~2018 빈칸 평균 66.3%
    많음(100~194)        85종목  → 40.7%
    보통(40~99)         138종목  → 61.2%
    적음(<40)             5종목  → 88.0%

상한에 닿은 GS 는 옛 분기 빈칸이 **14%**인데, 공시를 33건만 받은 ZION 은
**100%** 다. 가른 것은 상한이 아니라 **받아 온 공시의 수**다. 시간 예산이
뒤쪽 종목의 훑기를 바닥(`COLLECT_BUDGET_FLOOR = 24`)까지 줄이면 그 종목은
최근 2~3년치 공시만 갖게 된다. 캐시가 차면 저절로 나아지는 종류다.

## ④ 보관된 '파싱 실패 원문' 1,155건을 갈라 보았다

| 갈래 | 건수 |
|---|---:|
| 애초에 **실적 발표가 아닌 공시** (배당·인사·합병 등) | **459** (39.7%) |
| 실적 발표로 보임 | 696 |
|   └ 그 분기는 **이미 다른 경로로 채워짐**(XBRL·다른 8-K) | 239 |
|   └ **지금 파서로 살릴 수 있음** | **60** (조정EPS 5 · 논갭이익/EBITDA 14 · GAAP EPS 41) |
|   └ 아직 못 읽음 | 397 |

> **남길 것**: "파싱 실패 원문 1,013건"이라는 계기 숫자는 **문제의 크기를
> 부풀려 말한다.** 그 안의 40%는 실적 발표가 아니고, 또 34%는 그 분기가
> 이미 채워져 있다. 계기를 읽을 때는 **분모가 무엇인지** 먼저 물어야 한다.

## ⑤ 그래서 다음 런에서 실제로 채워질 것은 **약 60분기**다

150차-AF·AG 의 단위 고침은 **값의 크기를 바로잡는 것**이지 없는 값을
만들어 내지 않는다. 빈칸을 실제로 채우는 것은 이 60건이고, 나머지 397건은
아직 읽는 방법을 모른다 — 다음 회차의 일감이다.

살릴 수 있는 종목 상위: JPM 5 · BAC 4 · ABNB 3 · DELL 3 · AXP 2 ·
COHR 2 · C 2 · GE 2 · GS 2 · MS 2 · NKE 2 · SCHW 2 — **은행이 많다**
(150차-AB 에서 확인한 대로 은행은 논갭을 안 내므로 사다리 맨 아래인
GAAP EPS 로 살아난다).

---

# 150차-AI — 08-23 재수집 런 검증: 캐시 고침이 들었고, 유니버스가 401종목이 됐다

run_id 32648596054 · head_sha **510bb05** · 15:27:16 → 20:30:03 UTC.
**⚠️ 이 런은 150차-AF·AG(단위 고침) 이전 코드로 돌았다.** 아래 좋아진
것은 어제까지의 고침(150차-V·W·Y·AB)과 늘어난 공시 훑기의 효과다.

## ① 150차-AE 고침이 실제로 일했다

| 단계 | 결과 | 벽시계 |
|---|---|---|
| 공시 원문 캐시 **복원** | 성공 | 9초 |
| 수집 실행 | 성공 | **297분** |
| 공시 원문 캐시 **저장** | **성공** ← 150차-AE 의 핵심 | 16초 |
| 웹앱 데이터 · 커밋 | 성공 | 4분 |

한도 350분에 실제 302분 — 여유 48분. 어제 취소된 런(300분 한도·필요
305분)과 달리 **캐시 저장 단계가 돌았으므로** 다음 런은 캐시로 빨라진다.
수집계기: 벽시계 260.3분 · 겹침배수 5.98 · 내려받기 10,717건 ·
초당요청 0.69(허용 10건의 7%).

## ② 유니버스가 250 → **401종목**이 됐다 (5차 확장 첫 수집 완료)

분기 9,466행 → **15,074행**. 신호 종목 18 → 26개, 완성 78 → 127줄.

## ③ 어제와 **같은 250종목**으로 견준 채움률 (사과 대 사과)

| 항목 | 어제 08-22 | 오늘 08-23 | 변화 |
|---|---:|---:|---:|
| 분기 행 | 9,466 | 9,592 | +126 |
| 매출 | 9,211 | 9,349 | **+138** |
| GAAP EPS | 9,019 | 9,196 | **+177** |
| 논갭 영업이익 | 7,811 | 7,867 | +56 |
| **조정 EPS**(주 잣대) | 4,479 | 4,740 | **+261** |
| 조정 EBITDA | 926 | 1,025 | +99 |
| 매출총이익률 | 4,892 | 4,978 | +86 |
| **발표일 없는 분기** | 1,754 | **1,375** | **−379** |

## ④ 버려지던 값이 크게 줄었다

같은 계기를 어제 자료에도 돌려 견주었다(계기는 같은 길을 써야 한다):

    어제 08-22 · 250종목   버려진 영업이익·EBITDA  1,795건 · 136종목
    오늘 08-23 · 401종목   버려진 영업이익·EBITDA    605건 · 138종목

**종목이 60% 늘었는데 버려지는 값은 1,190건 줄었다.**

## ⑤ 화면 점검 — ⛔ 0건

| 항목 | 어제(250종목) | 오늘(401종목) |
|---|---|---|
| 빠진 분기 | 170건·68종목 | **102건·57종목** |
| 어긋난 분기 이름 | 652칸 / 7,399 (8.8%) | 916칸 / 11,094 (8.3%) |
| 매출 자릿수 깨진 행 | 0행 | **0행** |
| 종목 전체 매출 수상 | ['GS'] 1개 | ['GS'] 1개 (150차-AF 가 고쳤으나 다음 런부터 반영) |

`python3 run_tests.py` 종료코드 0 · 파일 22개 · **653개 통과**.

---

# 150차-AJ — 주당 값이 아랫줄에 있는 표를 읽는다 (2026-08-24)

150차-AH 에서 "아직 못 읽는 실적 발표"로 남긴 것을 갈래로 묶어 파고들었다.

## ① 내 분류기가 또 부풀려 말했다

"아직 못 읽음 576건" 중 **가장 큰 갈래 360건이 실적 발표가 아니었다.**
원문을 보니 전부 **예비 매출 공지·가이던스 갱신**이었다:

    ACMR "Announces Preliminary Unaudited Revenue Range for Third Quarter 2023"
    AAOI "Updates Third Quarter 2018 Revenue Guidance"

내 분류기가 "third quarter 2023" 같은 말만 보고 실적 발표로 셌다.
**교훈 34(분모를 물어라)가 하루 만에 다시 재현됐다.**

## ② 진짜 결함: 주당 값이 **아랫줄**에 있는 표 (실물 ALL 올스테이트)

    Net income (loss) applicable to common shareholders   (1,042)   1,595
    per diluted common share (1)                           (3.81)    5.26
    Adjusted net income (loss)*                             (209)     316
    per diluted common share* (1)                          (0.76)    3.79

지금 파서는 **이름과 "per share" 가 같은 줄에 붙어 있다**고 보고 찾는다.
그래서 ALL 은 GAAP EPS 도 조정 EPS 도 전부 "없음"이었다.

**아무것도 못 찾았을 때만 도는 마지막 수단**으로 넣었다 — 이미 찾은 값은
절대 바꾸지 않으므로 없음을 값으로 바꿀 뿐이다.

가르는 법은 **윗줄의 이름**이다. adjusted·non-GAAP·core 면 조정 잣대,
net income/earnings/loss 면 GAAP 잣대, 둘 다 아니면 손대지 않는다.

## ③ 첫 판이 헛값 셋을 만들었다 — 문지기를 달았다

전수로 재니 14건이 바뀌는데 그중 셋이 틀렸다:

    PG   "per share from early debt retirement … Tax Rate range of 18% to 19%"
         → 세율 18 을 EPS 로 읽음 (참값 1.6대)
    CGNX "Per share impact of discrete tax adjustments identified above  0.0x"
         → 영향값을 EPS 로 읽음

둘 다 이름 뒤에 **설명 글**이 이어진다. 진짜 표의 줄은 이름이 끝나면
바로 숫자다. 그래서 **"per share" 뒤에는 각주 번호·별표·공백만** 오도록
조였다. 다시 재니 **8건 · 회귀 0건 · 헛값 0건** — 전부 ALL 이고 전부 맞다.

    ALL 22Q2 GAAP −3.81 · 조정 −0.76    ALL 22Q3 GAAP −2.58 · 조정 −1.56
    ALL 22Q4 GAAP −1.17 · 조정 −1.36    ALL 23Q2 GAAP −5.29 · 조정 −4.42

## ④ **내가 만든 그물이 나를 잡았다**

새 시험 5개를 파일 맨 끝에 붙였는데, 그 자리가 **실행 스위치 뒤**였다.
150차-V 가 막으려던 바로 그 사고다 — 시험이 한 번도 안 돌면서 초록 불만
났다. `test_실행스위치_뒤에_갇힌_시험이_없다` 가 이름까지 찍어 잡아 줬다.

    ❌ test_eps_parse.py: 스위치 뒤에 5개 — ['test_주당값이_아랫줄에_있는_표를_읽는다', …]

블록을 스위치 앞으로 옮겨 653 → **658개**가 됐다.

> **남길 것**: 그물은 남을 잡으려고 치는 것이 아니라 **나를 잡으려고**
> 치는 것이다. 오늘 그 값을 실제로 받았다.

돌연변이 넷 전부 빨간 불(규칙 통째로 없앰 · 윗줄 검사 없앰 · 설명글
문지기 제거 · 마지막 수단이 아니라 맨 먼저 돌게).
한 번은 통과했는데 갈라 보니 **같은 코드**였다 — `^` 를 떼도 `.match()` 가
이미 줄머리에 닻을 내리므로 진짜 돌연변이가 아니었다.

`python3 run_tests.py` 종료코드 0 · 파일 22개 · **658개 통과**.

---

# 150차-AK 사전 등록 — 회계 4분기 자리의 연간값을 회계 달력으로 잡는다

**⚠️ 이 글은 재기 전에 적었습니다.** 150차-AD 에서 "더 많이 잡는 규칙"을
먼저 재고 나중에 고르다가 물린 적이 있어, 이번에는 규칙과 문턱을 먼저
못박고 그 뒤에 잽니다.

## 왜 지금 검사가 못 잡나

지금 검사(`_drop_cumulative_values`)는 **"직전 4분기 합과 거의 같은가"**
를 봅니다. 그런데 그 합에 **이미 연간값이 끼어 있으면** 합이 부풀어
진짜 연간값과 안 맞게 됩니다. 실물 SHW 는 2019년 첫 것부터 합대비차
33.6% 로 놓쳤고, 그 뒤로는 40~44% 라 영영 못 잡습니다.

**오염이 스스로를 지키는 것**이라, 과거 값으로 만든 기준으로는 못 풉니다.

## 새 기준 — 오염에 안 닿는 것: 회계 달력

연간값은 **회계연도 4분기와 함께** 발표됩니다. 그리고 회계 항등식이
있습니다: `Q1 + Q2 + Q3 + Q4 = 연간`. 그러므로 4분기 자리에 연간값이
들어오면 그 값은 **앞 세 분기 합보다 정확히 한 분기만큼 큽니다.**

150차-AD 에서 이 방향을 시험했을 때 RCL 의 3분기 성수기가 우연히 만족해
물렸습니다. 이번에는 **회계 4분기 자리로 대상을 못박아** 그 반례를
구조적으로 비껴갑니다 — RCL 3분기는 4분기가 아닙니다.

## 규칙 (문턱 고정)

**대상**: 회계연도 **4분기 자리**의 값. 필드는 지금과 같이
`adj_eps` · `adjusted_ebitda` · `gaap_eps`.

**필요 조건**: 같은 회계연도의 Q1·Q2·Q3 값이 **모두 있고 모두 양수**일 것.
(적자가 섞이면 항등식이 부호로 흐트러져 판단할 수 없습니다.)

- **조건 ①** 4분기 값 > (Q1+Q2+Q3)
- **조건 ②** 차액 D = 4분기 값 − (Q1+Q2+Q3) 이 Q1~Q3 중앙값의 **0.3 ~ 3.0배**
  (그럴듯한 한 분기 값이어야 한다)
- **조건 ③** 4분기 값 자체가 Q1~Q3 중앙값의 **2.5배 초과**
  (한 분기라기엔 너무 크다 — 지금 검사와 같은 배수를 씁니다)

**처리**: **없음 처리(버림).** 차액 D 로 고쳐 넣지 않습니다 — 창작 금지
(헌법 1조). 고치는 안은 이번에 아예 재지 않습니다.

## 채택 기준 (먼저 못박음)

- SHW 의 네 해 4분기 연간값을 **잡아야** 한다.
- 150차-AD 에서 물렸던 반례를 **죽이지 않아야** 한다 —
  AMD 의 평범한 분기 EPS · RCL/MPC/CCL 의 진짜 계절 성수기.
- 새로 버리는 값 하나하나를 눈으로 보고, **진짜 분기값을 죽이면 버린다.**

## 150차-AK 결과 — **채택하지 않았다** (사전 등록한 채택 기준에 걸림)

규칙을 사전 등록한 그대로 넣고 **문턱을 하나도 바꾸지 않고** 쟀다.

### 잡은 것 — 186건 · 95종목

목표였던 SHW 를 정확히 잡았다. FY2019~2025 의 9칸(주 잣대 조정 EPS 포함):

    SHW FY2024 adj_eps  Q1~Q3 2.17/3.70/3.37 → Q4 11.33  (합 9.24 + 2.09)
    SHW FY2025 adj_eps  Q1~Q3 2.25/3.38/3.59 → Q4 11.43

그 밖에도 진짜 연간값을 많이 잡았다 — RCL FY2024 10.94 · AMD FY2018 0.46
· CAT FY2024 22.05 · AXP FY2024 14.01 · CI FY2020 22.96 · ELV FY2021 24.73.
파이프라인에 넣어 돌리니 SHW 의 4분기 자리가 실제로 `없음` 이 됐다.

### 죽인 것 — **진짜 4분기 값** (그래서 버린다)

    AMZN FY2017 gaap 3.75   연말 성수기 — 진짜 4분기값
    TSLA FY2023 gaap 2.27   일회성 이연법인세 환입으로 4분기만 크다
    SNAP FY2024 adj  0.16   광고업이라 4분기가 원래 가장 크다
    TTD  FY2020 EBITDA 1.529억  같은 이유

**이 규칙은 원리적으로 둘을 못 가른다.** 4분기 자리 값이 연간값이든
진짜 성수기 값이든, `Q4 − (Q1+Q2+Q3)` 은 **언제나** 정의되고 언제나
"그럴듯한 한 분기"로 보일 수 있다. 숫자만으로는 항등식이 항등식이라
아무것도 가려 주지 않는다. 150차-AD 에서 RCL 3분기로 물렸던 것이,
대상을 4분기로 좁히자 이번에는 **4분기가 원래 큰 회사들**로 돌아왔다.

사전 등록한 채택 기준은 "진짜 분기값을 죽이면 버린다"였다. 지킨다.
코드는 되돌렸다(사본은 남겼다).

### 그래도 얻은 것 — **옳은 길이 무엇인지 확정했다**

가르려면 **독립된 자**가 있어야 한다. 그리고 우리는 이미 갖고 있다 —
**XBRL 의 12개월(연간) 계열**이다(`_annual_series`, `_fill_missing_q4` 가
쓰는 그것). 회계연도 연간값 A 를 따로 알고 있으면 판정은 결정적이다:

    4분기 자리 값 ≈ A  →  연간값이 들어온 것 (버린다)
    4분기 자리 값 ≠ A  →  진짜 4분기값 (둔다)

150차-V 가 단위를 고칠 때 **XBRL 을 독립된 자로 삼았던 것과 같은 수**다.
숫자를 만드는 것이 아니라, 같은 자리에 대해 **다른 출처가 뭐라고 하는지**
묻는 것이다.

**막는 것**: 지금 `snapshot.json` 의 분기 행에는 그 회계연도의 XBRL 연간값이
실려 있지 않다. 수집 쪽에서 한 칸(`annual_xbrl` 같은 것)을 함께 실어야 한다.
그건 수집 코드를 고치는 일이라 **다음 회차로 넘긴다** — 정기 런이 8시간
뒤라 지금 손대면 오늘 수집이 위태롭다(150차-AC 에서 같은 판단을 했다).

> **남길 것**: 항등식은 아무것도 증명하지 않는다. `A = B + C` 가 성립하는
> 것은 A 가 오염됐을 때도, 멀쩡할 때도 똑같다. 가르려면 **식 밖에서 온
> 값**이 있어야 한다.

---

# 150차-AL — 오염을 가릴 **독립된 자**를 수집에 실었다 (2026-08-24)

150차-AK 에서 확정한 것: 항등식(Q1+Q2+Q3+Q4=연간)만으로는 4분기 자리의
연간값과 진짜 성수기 4분기를 **원리적으로 못 가른다.** 가르려면 **식 밖에서
온 값**이 있어야 한다.

## 그 값을 우리는 이미 받고 있었다

XBRL 의 **12개월(연간) GAAP EPS** 계열이다. 이미 `_annual_series` 로 뽑고
있었지만(달러 항목의 4분기 메우기에만 씀) EPS 쪽은 아예 안 뽑고 있었다.

그리고 이 계열에는 **덤**이 있다 — 12개월 값은 **결산일에만** 있으므로
그 열쇠가 곧 결산일이다. **회계 달력을 짐작할 필요가 없다.**
(150차-AK 는 이름 다수결로 결산달을 추측해야 했다.)

## 무엇을 붙였나 (칸 하나 · 수집 시간은 안 늘어난다)

    sec_fundamentals._연간_gaap_eps(facts)     이미 받은 자료에서 뽑기만 함
    행에 "gaap_eps_annual_xbrl" 한 칸          결산일 행에만 값이 붙음
    measure_store.EPS_FIELDS 에 그 칸 추가     스냅샷까지 나름

**XBRL 을 새로 받지 않는다** — 이미 손에 든 `facts` 에서 12개월짜리를
골라내는 것뿐이라 수집 벽시계가 늘지 않는다. 그래서 정기 런 7시간 전인
지금 올려도 안전하다고 판단했다(150차-AC 에서 미룬 것은 **핵심 파서**를
런 40분 전에 고치는 일이었다 — 종류가 다르다).

`_annual_series` 가 `unit` 인자를 받고도 **안 넘기던 것**도 함께 고쳤다.
달러 항목은 기본값이 USD 라 티가 안 났지만, 주당 단위를 쓰려면 넘겨야 한다.

## 아직 **버리는 규칙은 넣지 않았다**

이 칸은 다음 수집부터 생긴다. 실데이터 없이 버리는 규칙을 넣으면 그것은
재지 않고 넣는 것이다. 다음 회차에 **실측하고** 넣는다. 그때 볼 것:

    4분기 자리 값 ≈ 연간값(XBRL)  →  연간값이 들어온 것 (버린다)
    4분기 자리 값 ≠ 연간값(XBRL)  →  진짜 4분기값 (둔다)

150차-AK 에서 죽었던 넷이 이 자로는 살아야 한다 —
AMZN FY2017(연간 6.15 ≠ 4분기 3.75) · TSLA FY2023(4.30 ≠ 2.27) ·
SNAP FY2024 · TTD FY2020. 그리고 SHW 는 여전히 잡혀야 한다.

돌연변이 넷 전부 빨간 불(연간 계열 비우기 · 12개월 대신 3개월 뽑기 ·
행에 칸 안 붙이기 · 주당 단위 안 넘기기).

`python3 run_tests.py` 종료코드 0 · 파일 22개 · **661개 통과** (658 → 661).

---

# 150차-AM — 빈칸의 정체를 갈라 세고, 수집 규격을 문서로 못박았다 (2026-08-24)

주인 지시: *"크리도 등 아직도 많은 종목에 빈칸이 너무나 많아 데이터를
신뢰성·정확성 있게 가져올 방법을 찾아봐. 찾아오는 기준을 최대한 지정하고
정형화시켜야 서로서로 데이터매칭오류가 안 생길 것 같은데?"*

## ① 크리도 실물 — 빈칸 세 종류가 한 종목에 다 있다

    2021-01-31  발표일 없음 · 조정EPS 없음   ← 상장 전. 8-K 가 존재하지 않는다
    2021-07-31  "                          ← 같음
    2021-10-31  "                          ← 같음
    2022-04-30  야후는 2022-06-01 발표라는데 우리는 8-K 를 못 붙였다  ← **일감**
    2025-02-01  발표는 붙었는데 조정EBITDA 없음  ← 회사가 안 낸다

XBRL 에는 2021년 분기가 있습니다 — 상장 서류에 과거 재무가 실리기
때문입니다. 그래서 "행은 있는데 조정 EPS 만 빈" 모양이 됩니다.

## ② 전수로 갈라 세었다 (401종목 · 15,074행 · 조정 EPS)

| 갈래 | 칸 | 고칠 수 있나 |
|---|---:|---|
| 있음 | 6,606 (43.8%) | — |
| 회사가 조정 EPS 를 **안 냄** | 4,838 (32.1%) | ✗ 없는 값 |
| **발표는 있었는데 8-K 를 못 붙임 · 그 회사는 냄** | **2,308 (15.3%)** | ✓ **일감** |
| 못 붙였지만 그 회사는 애초에 안 냄 | 1,010 (6.7%) | 붙여도 값 없음 |
| 발표 기록 자체가 없음(상장 전) | 233 (1.5%) | ✗ |
| 구멍 메운 행 | 79 (0.5%) | 부분적 |

> ⚠️ **처음 센 숫자를 정정한다.** 원본 스냅샷만 보고 세었을 때는
> "상장 전 3,324 · 못 가져옴 225"였다. **틀렸다.** 정제 단계에서 바깥
> 자(야후)의 발표일이 채워지면(`_recover_announced`) "발표는 분명히
> 있었는데 우리가 못 붙인" 분기가 드러난다. 정제 **뒤**의 자료로 세어야
> 옳고 그 수는 2,308칸이다.
> **남길 것**: 계기는 재는 대상과 **같은 단계**를 봐야 한다. 앞 단계에서
> 재면 뒤 단계가 채운 것을 못 보고, 문제를 10분의 1로 줄여 말한다.

## ③ 일감 2,308칸의 원인 — 파서가 아니라 **훑은 깊이**

로봇 계기(`robot_log.json`)로 확정했다. 가장 많이 빈 종목이 전부
5차 확장으로 새로 들어온 종목의 **첫 수집**이고 캐시가 0 이었다:

    CE   캐시적중 0 · 새로받음 36     HBAN 0 · 60     ZTS  0 · 34
    BAH  0 · 33                    TDG  0 · 63     XYL  0 · 32
    DXCM 0 · 45                    WAT  0 · 46     LDOS 0 · 38

공시 30~60건은 최근 2~3년치다. 그 앞 분기는 8-K 자체가 안 받아졌다.
**캐시가 차면 저절로 깊어진다** — 며칠 지켜보고 안 줄면 시간 예산을
손본다(짐작으로 뼈대를 건드리지 않는다).

따로 남는 것은 **형식 때문에 못 읽는 종목**이다 — FITB 는 캐시가 83건
있는데 조정 EPS 를 1개밖에 못 읽었다(슬라이드형 문서).

## ④ 정형화 — `데이터규격.md` 를 세웠다

주인 지적이 정확했다. 항목별 수집 규칙이 코드 여기저기 흩어져 있어
같은 분기에 출처가 다른 값이 섞여 들어왔다(GS 12,738달러 · HD 47,861 ·
AMZN 728). 한 장짜리 계약서로 못박았다:

    1장 분기의 신원      — 기간종료일 하나. 이름은 표시용, 계산 금지
    2장 출처 우선순위    — 항목마다 1·2·3순위와 "없으면 없음"
    3장 단위 자격 네 가지 — 낱말 / 가까운 표머리 / 먼 표머리 / 표 아래 각주
    4장 이름 고르는 자격  — 좁은 이름 먼저, 같은 줄 먼저, 조각 금지
    5장 짝짓기 규칙      — 창·상식 범위·가장 가까운 분기가 임자·알맹이 우선
    6장 없음의 사유      — 다섯 갈래
    7장 규격을 지키는 시험 · 8장 고치는 법

**코드가 이 문서와 다르면 코드가 틀린 것**으로 정하고, 새 세션이 반드시
먼저 읽도록 `CLAUDE.md` 머리에 걸었다.

## ⑤ 화면이 사유를 말하게 했다

`screen_verify` 에 15번째 검사를 넣었다. 빈칸을 그냥 비워 두면 주인은
고칠 수 없는 것을 고치라 하거나 고칠 수 있는 것을 지나친다.

    주 잣대 빈칸 사유  조정 EPS 15074칸 — 있음 6606 · 회사 미발표 4838 ·
    발표 기록 없음 233 · 못 붙였고 그 회사는 안 냄 1010 ·
    **못 붙였고 그 회사는 냄 2308** ← 일감 · 구멍메움 79

돌연변이 셋 전부 빨간 불(회사가 내는지 안 가림 · 상장 전을 안 가림 ·
보고 줄을 없앰).

`python3 run_tests.py` 종료코드 0 · 파일 22개 · **664개 통과** (661 → 664).

---

# 150차-AN — 08-24 런 검증: 단위 고침이 실제로 들었다 (2026-08-24)

run #54 · 예약(09:30) 이 94분 늦은 **11:04:23** 에 떠서 **15:14:37 성공**.
head_sha **5fa102f** — 150차-AF·AG·AJ·AL·AM 이 전부 실린 첫 런이다.

> 곁가지: 10:13 에 확인했을 때 예약 런이 안 떠 있어 11:15 에 수동으로
> 하나 더 띄웠는데, 그 10분 사이에 예약 런이 떴다. 중복 런(#55)은 바로
> 취소했다(겹침 방지 `concurrency: collect` 덕에 줄만 서 있었다).
> **남길 것**: 수동 실행 직전에 목록을 **다시** 읽어라. 10분 전 관찰로
> 지금을 판단하면 안 된다.

## ① 단위 고침이 들었다 — 가장 직접적인 증거

`screen_verify` 의 **"종목 전체 매출"** 경보가 `['GS'] 1개` → **0개**.
살펴볼 것도 4건 → **3건**.

    GS   12,738 달러  →  172.27억 · 203.38억
    AMZN 728          →  1,815억 · 2,006억
    MCK               →  963억 · 1,054억
    MCHP 1,484.7      →  14.847억   (표 아래 각주 고침 — 150차-AG)
    SHW               →  56.7억 · 67.9억
    TSLA              →  224억 · 282억

## ② 채움률 (같은 401종목, 행 15,074 → 15,088)

| 항목 | 어제 08-23 | 오늘 08-24 | 변화 |
|---|---:|---:|---:|
| 매출 | 14,566 | 14,631 | +65 |
| GAAP EPS | 14,201 | 14,282 | +81 |
| 논갭 영업이익 | 12,026 | 12,048 | +22 |
| **조정 EPS**(주 잣대) | 6,715 | 6,948 | **+233** |
| **조정 EBITDA** | 1,595 | **2,287** | **+692** (10.6% → 15.2%) |
| 매출총이익률 | 7,734 | 7,744 | +10 |

버려진 영업이익·EBITDA 605건·138종목 → **589건·133종목**.
매출 자릿수 쪽지 136 → **72건**(고칠 것이 줄었다).

## ③ 빈칸 사유 (150차-AM 의 계기로 다시 셈)

| 갈래 | 어제 | 오늘 |
|---|---:|---:|
| 있음 | 6,606 | **6,831** |
| 회사 미발표 | 4,838 | 5,054 |
| **못 붙임 · 그 회사는 냄** ← 일감 | 2,308 | **2,103** |
| 못 붙임 · 그 회사도 안 냄 | 1,010 | 816 |
| 발표 기록 없음 | 233 | 188 |

일감이 **205칸 줄었다.** 캐시가 차면 저절로 깊어진다는 예상대로다.

## ④ 시간 예산 — 캐시가 일했다

    벽시계   260.3분 → 203.5분
    내려받기 10,717건 → **1,459건** (캐시 적중으로 8분의 1)

런 전체(단계 포함) 11:04 → 15:14 = 250분. 한도 350분에 100분 여유.
런이 **성공**으로 끝났으므로 `공시 원문 캐시 저장` 단계도 통과했다.

## ⑤ 새 자(150차-AL)가 실제로 생겼고, **정확히 작동한다**

`gaap_eps_annual_xbrl` 이 **3,193행 · 385종목**에 붙었다.
그 자로 4분기 자리를 가려 보니 150차-AK 의 채택 기준을 **그대로 통과**한다:

    SHW FY2022~2025  4분기 GAAP 7.72·9.25·10.55·10.26 = XBRL 연간과 **같음** ⛔
    CAT FY2021·23·24·25  11.83·20.12·22.05·18.81 = **같음** ⛔
    AMZN FY2017  4분기 3.75 ≠ 연간 6.15  → **살아남음** ✓
    TSLA FY2023  4분기 2.27 ≠ 연간 4.30  → **살아남음** ✓
    SNAP 전 회계연도 전부 정상 ✓

150차-AK 가 원리적으로 못 가르던 것을 **식 밖에서 온 값**이 갈랐다.

`python3 run_tests.py` 종료코드 0 · 파일 22개 · **664개 통과** (새 자료에서도).

---

# 150차-AO — 4분기 자리의 연간값을 **독립된 자**로 버린다 (2026-08-24)

150차-AL 에서 실은 자(XBRL 12개월 GAAP EPS)가 오늘 수집으로 실제 자료에
들어왔다(3,193행·385종목). 그 자로 판정하는 규칙을 넣었다 —
150차-AK 에서 원리적으로 못 하던 일이다.

## 규칙

    4분기 자리 GAAP EPS ≈ 그 행의 XBRL 연간값  →  연간값이 들어온 것 (버린다)
    다르면                                     →  진짜 4분기값 (둔다)

"같다"는 **2센트 또는 1%** 안. 같은 행의 **조정 EPS·조정 EBITDA 도 함께**
버린다 — GAAP 이 연간값으로 확증되면 그 행의 다른 잣대도 **같은 표에서
온** 연간값이다(실물 SHW 24Q4: GAAP 10.55 = 연간, 조정 11.33 도 연간).

고치지 않고 **버린다.** 차액으로 채우면 창작이고, EPS 는 비율이라
연간에서 분기 합을 빼도 4분기 EPS 가 아니다(주식 수가 분기마다 다르다).

## 순서를 고쳐야 했다 — 150차-AD 수수께끼의 정체

처음 넣었을 때 SHW 의 GAAP 은 지워지는데 **조정 EPS 가 살아남았다**
(11.33 그대로). 원인은 순서였다. 앞선 `_drop_cumulative_values` 가
GAAP EPS 를 먼저 잡아 없음으로 만들어, 내 규칙이 **자로 쓸 값이 사라진
뒤에** 도착했다.

**150차-AD 가 "GAAP 은 검사가 잡는데 조정 EPS 만 빠져나간다"고 적은 그
현상의 정체가 이것이었다.** 연간값 검사를 누적값 검사보다 **앞으로**
옮기니 둘 다 지워졌다.

> **남길 것**: 검사끼리도 순서가 있다. **다른 검사가 자를 지워 버리면**
> 뒤에 오는 검사는 눈이 먼다. 증거로 쓰는 값을 지우는 검사가 앞에 있으면
> 안 된다.

## 실측 (401종목 · 15,088행)

버린 행 **151행 · 75종목**. 순서를 고치기 전에는 84행·49종목이었다.

    AEP 6 · PCTY 6 · OTIS 5 · EL 4 · CAT 4 · XOM 4 · EQIX 4 · SHW 4 · XEL 4 · PCG 4

**채택 기준(150차-AK 에서 사전 등록한 것)을 그대로 통과했다:**

    SHW  FY2022~2025  GAAP·조정 **둘 다** 없음 처리  ⛔ ← 목표였던 그것
    CAT  FY2023~2025  없음 처리                      ⛔
    AMZN FY2022~2025  0.31 · 0.28 · 0.94 · 1.43 **그대로**  ✓
    TSLA FY2023~2025  2.27/0.71 · 0.66/0.73 · 0.39/0.40 **그대로**  ✓
    SNAP FY2022~2025  **그대로**  ✓

## 돌연변이 넷 전부 빨간 불

    규칙 통째로 없앰 · 같은 행의 조정 EPS 는 안 버림 ·
    "같다"는 띠를 크게(0.02 → 3.0) · 자가 없어도 버림

`python3 run_tests.py` 종료코드 0 · 파일 22개 · **668개 통과** (664 → 668).

## 매일 화면 점검 (같은 날)

⛔ 0건 · 살펴볼 것 4건 → **3건**(GS 매출 경보 해소).
모바일 412px 네 탭 전부 **넘침 0 · 콘솔 오류 0**.

---

# 150차-AP — 원문 2,064건을 갈래로 나누고, 조정 EPS 를 여섯 가지 더 읽는다

## 왜 했나

인수인계 5장 "다"에 남긴 일감이다: **아직 읽는 법을 모르는 실적 발표
원문**. 거기에 경고도 함께 적혀 있었다 — "세기 전에 그것이 정말 실적
발표인지 먼저 가르세요(576건 중 360건이 예비 매출 공지였음)."

## 자(尺)를 네 번 고쳐야 했다

세는 자부터 틀렸다. 판마다 실물로 확인하고 고쳤다.

| 판 | 어떻게 갈랐나 | 무엇이 틀렸나 |
|---|---|---|
| 1 | 제목투 + 손익계산서 | "모름" 818건(40%) — 자가 눈이 멀었다 |
| 2 | 본문 증거(주당말+실적말) | 투자설명회 자료·사채 발행 공시까지 실적 발표로 셌다 |
| 3 | + "회사 미발표"를 갈라냄 | ACLS 는 조정 EPS 를 **아예 안 낸다** — 우리 결함이 아니다 |
| 4 | + 곁들이 자료 갈래 | `Forecast` 안의 `recast` 를 물었다(ABT) · "reconciliation of non-GAAP" 상투구로 멀쩡한 발표를 곁들이로 밀어냈다(ABNB) |
| 5 | **제목 줄**로 가름 | 지금 쓰는 것 |

> **남길 것**: 세는 자도 코드다. 실물을 몇 개 열어 보기 전에는 그 숫자를
> 보고하지 않는다. 낱말 하나를 경계 없이 넣으면(`recast`) 엉뚱한 것이
> 걸린다.

## 갈래별 (원문 2,064건, 5판 기준)

    실적 발표(손익계산서 실림)   590     실적 발표(추정)  347
    무관(사채·배당·인사·임상)     390     예비·가이던스     107
    곁들이 자료(IR·재작성표)       42     모름             588

**손익계산서가 실린 590건**이 확실한 표본이다. 그 안에서:

    다 읽음        295 (50%)
    GAAP 못 읽음   224     ← XBRL 이 대신 채우므로 실제 구멍은 아니다
    조정 EPS 일감  145     ← **이것이 진짜 일감**
    조정 EPS 회사 미발표 204  ← 정상 (사다리가 GAAP 으로 내려간다)
    매출 못 읽음    14

일감 145건은 종목이 뭉쳐 있었다 — UCTT 10 · CGNX 10 · SCHW 10 · BA 9 ·
AMBA 6 · EL 5 · STRL 5 · HPE 5. **한 꼴을 고치면 열 건이 풀린다.**

## 고친 여섯 가지 (전부 실물 근거)

1. **core** — 보잉은 자기 논갭 지표를 "core loss per share" 라고 부른다.
   `_NONGAAP_NEAR_RE` 는 core 를 이미 알아서 GAAP 칸 오염은 막고 있었는데,
   **조정 EPS 이름 목록에는 없어서** 값이 어디에도 안 들어가고 사라졌다.
2. **"…였고, 주당 얼마"(or 꼴)** — "net income was $14.5 million **or
   $0.31 per diluted share**". 값이 이름보다 **앞**에 온다. 이름을 "…or"
   까지로 끊고 뒤를 읽되, 뒤에 "per share" 가 실제로 오는지 미리보기로 확인.
3. **수식어 두 개 겹친 줄임말** — "non-GAAP **diluted net** EPS"(HPE),
   "Non-GAAP **net diluted** EPS"(SEDG).
4. **제외 문장** — "…EPS **excludes** … of approximately $1.28 per share".
   빼낸 항목의 크기이지 잣대가 아니다.
5. **비교값** — 값 바로 앞의 "compared to $"(PYPL), 값 바로 뒤의
   "during the same period"(PCG).
6. **연간 표시** — 멀리 있는 "for the fiscal year"(PG 발표자료) ·
   "Financial **Highlights**" 구역 제목(CSX) · "for 2019"(FSLR) ·
   전망의 "expect**ed**"(PYPL).

## 스스로 잡은 잘못 넷 (전수 실측이 아니었으면 다 나갔다)

| 넣었던 것 | 무엇이 깨졌나 | 어떻게 했나 |
|---|---|---|
| 맨 `core EPS` | PG·PEP 발표자료의 "Core EPS **Growth** +19%" 에서 숫자를 물어 **네 건 다 틀림**(0.40·5.35·0.10·8.16) | 뺐다. "core earnings per share were $1.83" 꼴만 남겼다 |
| `exclude/excluding` 전부 | VZ "adjusted EPS, **excluding** special items, of $1.19" 같은 **진짜 값 8건**이 죽었다 | `excludes?` 만. "제외한**다**"와 "제외**한**"은 다른 말이다 |
| 이름~값 사이 어디든 비교 문구면 포기 | GS 10.81 · NTAP 1.54 를 잃었다(**기존 시험 2개가 빨간 불**) | 값 **바로 앞 24자**로 좁혔다 |
| 거른 뒤 계속 앞으로 훑기 | DG 는 배당금 0.32 를, PYPL 은 러시아 영향 0.20 을 물었다 | 비교·각주로 거르면 **그 자리를 통째로 포기**한다 |

> **남길 것**: 하나를 고치면 셋을 잃을 수 있다. **전수로 재기 전에는
> 넣지 않는다.** 그리고 기존 시험은 새 규칙의 가장 좋은 감시자다.

## 실측 (저장원문 2,064건 전·후 비교)

    조정 EPS  없음 → 값     46건
      BA 9 · UCTT 11 · GNRC 7 · MCHP 5 · ISRG 4 · PG 3 · LITE 2 · VECO 2 ·
      CIEN 1 · CSCO 1 · PCG 1
    조정 EPS  값 → 다른 값   18건   (PYPL 8 · HD 3 · MRVL 2 · SEDG 2 · PFE · UNH · ECL)
    GAAP EPS  값 → 다른 값   15건   (PYPL 7 · DELL 2 · VECO 2 · EMR · FSLR · MDB · MS)
    틀린 값 → 없음            8건   (ABT 5.00 · CSX 0.42 · DG 6.45/6.55 ·
                                    JNJ 10.0512×2 · CVS 7.00 · PG 6.51)
    **멀쩡한 값이 죽은 것: 0건**

바뀐 값은 실물 원문으로 하나씩 확인했다. PYPL 은 여덟 분기가 통째로
**앞 분기 값**을 물고 있었다 (Q1'22 조정 1.15 → **0.88**, GAAP 1.00 →
**0.43**). UNH 는 연간 전망 17.75 → 4분기 **2.11**. FSLR 은 연간 −1.09
→ 4분기 **−0.56**.

남은 것 하나: **ECL** 1.27 → 0.82. 둘 다 맞지 않는다(분기 열을 잘못
고르는 다른 병). 고치기 전보다 나빠지지도 좋아지지도 않았다고 적어 둔다.

## 돌연변이 열둘 전부 빨간 불

새 시험 12개를 하나씩 꺼 보았다. 그중 **셋은 처음에 통과했다** — 시험이
헛돈 것이 아니라 **다른 문지기가 대신 막고 있었다**(연간·전망·전기 문지기).
시험글에서 그 낱말을 빼서 **재려는 것만 재게** 고친 뒤 셋 다 빨간 불이 됐다.

`python3 run_tests.py` 종료코드 **0** · **684개 통과** (668 → 684).

---

# 150차-AQ — 앱에서 종목을 치면 로봇이 자동으로 받아 온다

## 주인 요청

"앱에서 종목을 치면 자동으로 연계될 수 있게 구현해."

150차-O 때는 깃허브 실행 화면으로 가는 링크만 주었다. 주인은 거기서
종목 코드를 **직접 타이핑**해야 했다(휴대폰에서 특히 번거롭다).

## 열쇠 없이 잇는 길

웹앱은 서버 없는 정적 페이지라 브라우저가 SEC 에 직접 붙지 못하고,
깃허브에 실행을 시키려면 열쇠가 필요하다. **그 열쇠를 공개 페이지에
넣으면 누구나 저장소를 건드릴 수 있어 더 위험하다.**

그래서 열쇠가 필요 없는 길을 썼다:

    앱: 종목 코드가 미리 채워진 **요청장(이슈) 주소**를 연다 (주소만 만든다)
    주인: 이미 깃허브에 로그인돼 있으므로 **초록 단추 한 번**
    로봇: `collect_request.yml` 이 깨어나 그 종목을 수집하고, 끝나면
          요청장에 결과를 적고 닫는다
    앱: **20초마다 스스로 확인**하다가 자료가 오면 새로고침 없이 열린다

## 자물쇠 세 겹

    ① 저장소 **주인이 연 요청장만** 실행한다 (남이 열면 무시)
    ② 제목이 "수집: XXXX" 꼴이어야 한다
    ③ 종목 코드는 영문 대문자·점·붙임표 8자까지만 — 셸에 그대로 넣지
       않고 환경변수를 거친다 (명령 끼워넣기 차단)

collect_one 과 마찬가지로 **판정 표본(`data/measure`)은 건드리지 않는다.**
궁금해서 부른 종목을 표본에 넣으면 "결과를 보고 종목을 고르는 것"이 되어
사전 등록이 무너진다(헌법 2조).

시험 4개를 새로 넣고 넷 다 돌연변이로 빨간 불을 확인했다.
모바일 412px 실물 렌더링: **가로 넘침 0 · 콘솔 오류 0**.

## 곁가지로 고친 것 — 시험이 저장소 데이터를 망가뜨릴 수 있었다

`test_screen_verify` 의 깨뜨리기 도우미가 **진짜 `docs/data/app.json` 을
덮어쓰고** finally 에서 되돌리고 있었다. 시험이 도는 도중에 프로세스가
죽으면(시간 초과·컨테이너 재시작) 되돌리기가 실행되지 않아 **저장소의
화면 데이터가 망가진 채로 남는다.** 실제로 이번에 그 일이 나서, 다음
시험이 "완성 목록 135줄 · 서로 다른 종목 133개" 라는 거짓 경보를 냈다.

이제는 딴 곳에 복사해 망가뜨리고 점검기가 그 파일을 보게 한다 —
진짜 파일은 건드리지 않는다. 일부러 죽여서 확인했다(진짜 파일 그대로).

> **남길 것**: 시험이 **저장소의 진짜 파일을 고치면 안 된다.** finally 는
> 프로세스가 죽으면 실행되지 않는다.

---

# 150차-AR — 6차 확장을 **계산해 보고 미룬다** (사전 검토)

## 주인 질문

"가설판정에 그냥 다 추가하는 게 좋지 않아? 표본도 더 늘어날 거고."

맞는 방향이다. 다만 **요청 수집분을 그대로 넣는 것**은 표본을 늘리는
것이 아니라 **기울인다** — 우리가 앱에서 찾는 종목은 무작위가 아니라
**눈에 띄었기 때문에** 찾는 것이라(크게 올랐거나·뉴스가 났거나) 신호
그룹 성적이 저절로 좋아 보인다. 그래서 늘리더라도 **규칙으로** 늘려야
한다(5차까지 해 온 방식).

그런데 **몇 개를 늘려야 하는지 계산해 보니 결론이 뒤집혔다.**

## ① 표본을 늘려 살아날 수 있는 가설이 있나

지금 등록된 11개의 신규(판정) 표본 실측:

| 가설 | 신호 n | 신호율 | 하한 | 기준 상한 | 차 |
|---|---:|---:|---:|---:|---:|
| H5 실적폭 고정20 | 5,573 | 9.2 | 8.5 | 10.0 | −1.5 |
| **H9 저평가×첫신기록** | 229 | 10.0 | 6.8 | 8.6 | −1.8 |
| H2 신고점 | 2,217 | 9.0 | 7.9 | 9.9 | −2.0 |
| H2b 신고점 첫돌파 | 464 | 9.5 | 7.1 | 9.9 | −2.8 |
| **H7 EBITDA 첫돌파** | 48 | 16.7 | 8.7 | 14.3 | −5.6 |

윌슨 구간으로 **분리에 필요한 신호 건수**를 역산했다(지금 신호율이
참값이라고 가정):

    H9   229건 → 약 1,550건 필요  (6.8배)
    H7    48건 → 약   800건 필요 (16.7배)
    H2b  464건 → 6만 건까지 늘려도 분리 안 됨 (효과가 사실상 0)

## ② 표본을 늘리는 두 길의 값어치

    ① 종목 +100 (401→501)     표본 1.25배 · 첫날 벽시계 약 304분 (한계 350)
    ② 조정 EPS 빈칸 46%→80%    표본 1.74배 · 수집 시간 **0분 증가**
    ③ 둘 다                    표본 2.17배

시간 실측(08-24 런): 401종목 · 벽시계 **203.5분** · 일감합 1,220.5분 ·
겹침배수 6.0. 종목당 벽시계 0.507분. 새 종목은 캐시가 없어 첫날 약 2배.

    +50종목  → 안정기 229분 · 첫날 약 254분
    +100종목 → 안정기 254분 · 첫날 약 304분
    +150종목 → 안정기 280분 · 첫날 약 354분  ⛔ 강제 종료(350분)

## 결론 — 순서를 바꾼다

1. **빈칸이 종목보다 이득이 크다.** 1.74배 vs 1.25배인데 수집 시간은
   늘지 않는다. 주 잣대 조정 EPS 는 15,088칸 중 **6,948칸(46.0%)** 뿐이다
   (매출 97.0% · GAAP EPS 94.7% · 조정 EBITDA 15.2%).
2. **지금 종목을 늘리면 데이터가 나빠진다.** 시간 예산 190분이 이미 차
   있어(08-24 실측 203.5분), 종목이 늘면 **모든 종목이 얕게** 훑인다 —
   94차에 적은 "표본을 늘리려다 있던 표본을 깎는 최악"이다.
3. **정직하게: 둘을 다 해도(2.17배) 지금 가설은 채택되지 않는다.**
   가장 가까운 H9 조차 6.8배가 필요하다. 필요한 것은 표본이 아니라
   **더 나은 가설**이다 — 탐색에서 나온 후보를 새 H번호로 사전 등록하고
   새 데이터로만 판정하는 길(헌법 5조).

## 사전 등록 — 6차 확장은 이 조건을 채운 뒤에 한다

    조건: 조정 EPS 채움률이 **80%** 에 닿는다 (지금 46.0%)
    규모: 첫 걸음 **+50종목** (첫날 예상 254분, 한계 350분)
    규칙: 미국 상장 · SEC 에 실적 8-K 를 냄 · 지금 401종목에 없던 종목 ·
          업종 쏠림 없이 · **새 묶음을 만들지 않고 기존 25개 묶음에만** 배정
    한 번 넣은 종목은 성적이 나빠도 빼지 않는다.

> **남길 것**: "표본을 늘리자"는 말은 옳지만, **어디를 늘릴지는 계산이
> 정한다.** 종목 수는 눈에 보이는 숫자라 늘리고 싶어지는데, 실제로는
> 이미 가진 종목의 **빈칸**이 더 크고 더 싸다.

---

# 150차-AS — 이름이 두 줄로 쪼개진 표, 그리고 그 표를 읽다가 드러난 넷

## 왜 했나

150차-AR 에서 정한 순서 그대로다 — **빈칸부터**. 150차-AP 의 갈래 세기가
남긴 일감 145건에서 가장 큰 덩어리가 **UCTT 10 · CGNX 10 · SCHW 10** 였고,
CGNX·SCHW 는 같은 꼴이었다.

## 그 꼴

표의 항목 이름이 길어 줄바꿈이 되면서, **어느 잣대인지 말해 주는 꼬리표가
아랫줄로** 밀려난다. 값은 윗줄에 있다.

    CGNX
      Net income per diluted weighted-average common and common-equivalent   $0.23
      share (Non-GAAP)                        ← 여기서야 논갭이라고 말한다

    SCHW
      Adjusted net income available to common stockholders   $1,358   $.74
      (non-GAAP), Adjusted diluted EPS (non-GAAP)   ← 둘째 칸이 EPS 라고 말한다

기존 이름들은 전부 "non-GAAP … per share" 가 **붙어 있기**를 요구해 이
꼴을 하나도 못 읽었다. 값 고르기는 이미 쓰던 규칙 그대로 — **소수점이
있는 첫 숫자**. SCHW 의 "$1,358"(백만 달러)은 소수점이 없어 저절로 걸러진다.

## 읽다가 드러난 넷 (전부 실물 근거)

| 무엇 | 실물 | 무엇이 틀렸나 |
|---|---|---|
| **소수점으로 시작하는 표기** | SCHW `$.74` | 그 칸을 못 읽고 건너뛰어 **6개월 누적 칸($1.68)** 을 분기값으로 읽고 있었다 |
| **0.00 은 EPS 가 아니다** | BE `GAAP EPS, Diluted  $.00  $(0.10)` | 빈칸 대신 놓인 0. 진짜 값은 −0.10 |
| **`Approx.` 는 전망표의 말** | CCL `Approx. $0.00  Approx. $1.70` | 둘 다 전망(분기·연간). 참값 0.14 |
| **영향의 크기는 잣대가 아니다** | PYPL `GAAP EPS included a negative impact of approximately $0.11` · BMY | PYPL 0.11(참값 0.93) · BMY −0.01(참값 1.82·2.05) |

## 되돌아보기 창 — 글자 수가 아니라 **문장**이 답이었다

`exclude_nongaap`(GAAP 을 찾을 때 논갭 이름을 건너뛰는 규칙)의 창이 40자
였는데, 두 실물이 **서로 반대 방향**으로 이걸 깨뜨렸다.

    SCHW  "…costs, **adjusted** (1) net income and diluted common
           **earnings per share** … $.74"      51자 앞 → 넓혀야 잡는다
    CRDO  "…**non-GAAP** net income of $208.8 million. **GAAP diluted
           net income per share** of $0.82"    55자 앞 → 넓히면 잃는다

40자로 두면 SCHW 의 **조정값이 GAAP 칸에** 들어가고(GAAP·조정이 같은 값
0.74 로 무너진다 — 87차에 적어 둔 그 표시), 60자로 넓히면 CRDO 의 진짜
GAAP 값 0.82 를 잃는다.

차이는 글자 수가 아니라 **문장**이었다. CRDO 는 앞 문장의 이야기이고 SCHW
는 같은 문장 안의 이야기다. 창을 120자로 넓히되 **문장 끝이 있으면 거기
서부터만** 보게 했다.

> 첫 시도는 `". "` 로 잘랐는데 **기존 시험 5개가 빨간 불**이 됐다.
> 표 안의 소수점("1.00")과 약자를 문장 끝으로 오인했고, `rfind` 가 −1 을
> 돌려줄 때 +2 를 해 첫 글자를 잘라먹기까지 했다("Non-GAAP" → "on-GAAP").
> 마침표 **앞뒤를 함께**(소문자·숫자 뒤, 공백+대문자 앞) 보게 고쳐 해결했다.

## 넣었다가 되돌린 것 — 제목 줄 + Basic/Diluted 짝

SCHW 는 `Earnings Per Common Shares Outstanding (1):` 아래 Basic·Diluted
가 따로 오는데, 이름 뒤 600자 안의 `Diluted` 를 이름의 일부로 삼는 안을
넣어 봤다. SCHW 는 0.69(Basic) → **0.68(정답)** 이 됐지만, 전수로 재니
GAAP 65칸이 새로 생기면서 그 안에 **말이 안 되는 값**이 섞였다:

    LITE −84.65 · MPWR 29.88 · UCTT −3.58 · APPF 3.86(세 번) ·
    PYPL 0.93 → 0.11 (멀쩡하던 값이 깨짐)

600자 창이 상관없는 곳까지 건너뛰어 `Diluted` 를 찾은 것이다.
**되돌렸다.** SCHW 의 GAAP 칸이 Basic(0.69)으로 읽히는 것은 남은 흠으로
적어 둔다 — 조정값(0.74)으로 무너지던 것보다는 훨씬 가볍고, GAAP EPS 는
XBRL 이 94.7% 를 채운다.

> **남길 것**: 하나를 정확히 맞히려고 넓힌 창이 **예순다섯 곳을 헝클** 수
> 있다. 이득은 눈에 띄고 손해는 흩어져 있어, **전수로 세지 않으면 보이지
> 않는다.**

## 실측 (저장원문 2,064건 · 오늘 세션 전체 = AP + AS)

    조정 EPS  없음 → 값     60건    (원문에서 읽히는 칸 269 → 323, **+20%**)
    조정 EPS  값 → 다른 값   23건
    GAAP EPS  값 → 다른 값   26건
    GAAP EPS  없음 → 값      10건
    틀린 값 → 없음           15건

새로 읽힌 종목: BA 9 · UCTT 11 · SCHW 10 · GNRC 7 · MCHP 5 · ISRG 4 ·
PG 3 · KEY 2 · LITE 2 · VECO 2 · CGNX · CIEN · CSCO · PCG · AXP.

바뀐 값은 실물 원문으로 확인했다. 특히:

    PYPL  여덟 분기가 통째로 **앞 분기 값**을 물고 있던 것이 풀림
    BMY   조정 EPS −0.01 → **1.82 · 2.05** (영향값을 잣대로 읽고 있었음)
    QRVO  GAAP 1.88(논갭값) → **−0.18**
    FITB  GAAP −0.68 · −0.19 → **0.16 · 0.84**
    SCHW  조정 EPS 열 분기가 전부 새로 읽힘 (0.68~1.62)

## 돌연변이

새 시험 7개(AS)를 하나씩 껐다 — 전부 빨간 불. 그중 **"창을 40 으로 좁히기"**
는 SCHW 시험만, **"문장 창 끄기"** 는 CRDO 시험 두 개를 잡아, 두 방향의
반례가 서로를 지키고 있음을 확인했다.

`python3 run_tests.py` 종료코드 **0** · **691개 통과** (684 → 691).

---

# 150차-AT — 남은 일감 98건에서 뭉쳐 있던 네 꼴, 그리고 전망 창의 흠

## 어디까지 왔나

150차-AP 가 센 일감 145건이 AS 로 **98건**이 됐다. 그 98건도 종목이
뭉쳐 있었다 — **CGNX 9 · AMBA 6 · EL 5 · HPE 5 · STRL 5 · DIS 4**.
이번에는 CGNX·AMBA·HPE 세 꼴을 읽게 했다.

## 네 꼴

| 꼴 | 실물 | 왜 못 읽었나 |
|---|---|---|
| **이름 가운데 수식어가 김** | CGNX `Net income per **diluted weighted-average common and common-equivalent** share (Non-GAAP)` | 줄임 표기가 "per (diluted) share" 가 **붙어 있기**를 요구했다 |
| **적자 분기의 같은 문장** | AMBA `non-GAAP net **loss** of $5.9 million, or **loss** per diluted ordinary share of $0.13` | 앞 이름이 "net profit … or **earnings** per …" 만 받았다 |
| **제목 줄 아래 하위 항목** | HPE `•Diluted net earnings per share (“EPS”): ⏎◦GAAP of $0.44 ⏎◦Non-GAAP(1) of $0.79` | GAAP 쪽만 이름이 있고 **논갭 쪽 짝이 없었다**(76차에 반쪽만 넣었다) |
| **줄머리 Excluding** | KSS `• **Excluding** tax reform benefits, diluted earnings per share were $4.31` | 정의상 GAAP 이 아닌데 GAAP 칸에 들어갔다(게다가 연간값) |

## 전망 창이 값 **뒤**까지 읽고 있었다

HPE 를 읽게 만들었는데도 값이 안 나와 파고들었더니, 이름·값과 상관없는
곳에서 걸리고 있었다.

    "◦Non-GAAP(1) of **$0.79**, up $0.41 from the prior-year period and
     above our **outlook** range of $0.51 - $0.55"

전망 문지기의 뒤쪽 창은 주석에 "**이름과 값 사이**의 to approach 류"라고
적혀 있는데, 실제로는 **값을 지나 그 뒤 설명까지** 읽고 있었다. 값 뒤의
"outlook" 은 이번 분기 실적을 전망과 견주는 말인데, 그것 때문에 **진짜
실적을 통째로 버리고** 있었다.

두 가지를 고쳤다.

1. 뒤쪽 창을 **값 앞에서 멈추게** 했다.
2. 이름이 여러 줄에 걸치는 꼴(MPWR·HPE 형)에서는 **이름 몸통을 통째로
   보지 않는다** — 이름 안에 다른 항목의 설명이 통째로 들어오기 때문이다
   (HPE 의 이름 안에는 GAAP 줄의 outlook 이 들어 있다). 값에 닿아 있는
   세 곳만 본다: 이름 앞 · 이름의 **마지막 줄** · 값 앞.

곁가지 이득: MKSI 두 건이 0.98 → **1.32**(정답)로 고쳐졌다. 값 뒤의
"above the midpoint of **guidance**" 때문에 버려지던 것이다.

## 대신 넣은 문지기 둘 (넓힌 만큼 좁혔다)

    QRVO 가이던스 갱신 공시  "$2.14 **at** the midpoint of guidance"
      → 값이 곧 전망. MKSI 의 "**above** the midpoint" 와 낱말 하나가 다르다.
    KSS  "• **Excluding** tax reform benefits, …"
      → 줄머리에서만 본다. 문장 중간까지 막았더니 PYPL 의 진짜 GAAP 값
        세 분기가 죽었다("…; excluding eBay, revenue grew 15% … • GAAP EPS of $0.43").

> **남길 것**: 창을 넓히는 고침은 **반드시 같은 크기의 좁힘과 짝**이어야
> 한다. 넓히기만 하면 그날은 이득이 보이고 손해는 며칠 뒤 다른 종목에서
> 나온다.

## 실측 (저장원문 2,064건, AS 대비)

    조정 EPS  없음 → 값     18건  (AMBA 6 · CGNX 5 · HPE 3 · MKSI 2 · SYK 2 · VRT · VZ)
    조정 EPS  값 → 다른 값    2건  (MKSI 0.98 → 1.32, 둘 다 정답)
    GAAP EPS  값 → 다른 값    2건  (WMT 2.93 → 2.92, 정답)
    GAAP EPS  없음 → 값       1건  (VRT 0.46)
    **멀쩡한 값이 죽은 것: 0건**

## 돌연변이 여섯 전부 빨간 불

새 시험 8개(반례 시험 3개 포함)를 넣고, 문지기를 하나씩 껐다. 여섯 전부
해당 시험을 빨간 불로 만들었다. ② 를 껐을 때는 **기존 AMBA 시험까지**
빨간 불이 되어, 새 이름이 옛 이름을 삼키지 않았음도 확인됐다.

`python3 run_tests.py` 종료코드 **0** · **699개 통과** (691 → 699).

## 오늘 하루 누적 (150차-AP·AS·AT)

    조정 EPS 를 읽는 칸  269 → **341** (+27%, 저장원문 2,064건 기준)
    일감(손익계산서가 실렸는데 못 읽는 것)  145 → **80**

---

# 150차-AU — 조정 EBITDA 의 빈칸은 **고칠 수 있는 것이 아니다** (읽기만 한 조사)

## 왜 쟀나

150차-AR 에서 가장 가까운 가설 둘을 골랐는데, 그중 **H7(조정 EBITDA
첫 돌파)** 은 효과가 가장 크면서(16.7% vs 기준 11.5%) 표본이 가장 작다
(신호 n=48). 분리에 약 800건이 필요하니 **16.7배**가 있어야 한다.
조정 EBITDA 채움률이 15.2% 밖에 안 되니, 파서를 고치면 되지 않을까?

## 실측 (저장원문 중 손익계산서가 실린 622건)

    조정 EBITDA 를 읽음                158
    **회사가 아예 안 냄**              **460  (74%)**
    낱말만 있고 값 없음                  4
    **값이 있는데 우리가 못 읽음**       **0건**

**한 건도 없다.** 조정 EPS 는 같은 방식으로 재서 83건의 일감이 나왔는데
(150차-AP), EBITDA 는 0 이다. 채움률 15.2% 는 **우리 결함이 아니라
회사들이 그 지표를 안 내기 때문**이다. 조정 EBITDA 를 정기적으로 내는
곳은 미디어·통신·에너지·사모펀드 보유 기업처럼 자본집약 업종에 몰려 있고,
반도체·소프트웨어·헬스케어·은행은 거의 안 낸다.

## 그래서 결론

    · **EBITDA 파서에 시간을 쓰지 않는다.** 고칠 것이 없다.
    · **H7 은 표본이 구조적으로 부족한 가설**이다. 종목을 늘려도
      "EBITDA 를 내는 회사"의 비율이 그대로라 같은 벽에 부딪힌다.
      401종목에서 48건이니, 800건을 채우려면 **약 6,700종목**이 필요하다.
      미국 상장 전체를 넣어도 모자란다.
    · 관찰 목록에는 그대로 두되, **"표본이 구조적으로 모자라 판정 불가"**
      라고 적어 두어야 한다. 매일 "미채택"만 보고 있으면 언젠가 표본이
      쌓일 것처럼 오해하게 된다.

## 이 조사의 한계 (정직하게)

이 표본은 **파싱에 실패해서 저장된 원문**이다. 조정 EPS 는 잘 읽었지만
EBITDA 만 못 읽은 발표가 있다면 **그 파일은 저장되지 않아 여기 없다.**
그러므로 "일감 0건"은 *이 표본 안에서* 0건이라는 뜻이다.
다만 460 대 0 이라는 비율은, 낮은 채움률의 **주된 원인**이 미발표라는
쪽을 강하게 가리킨다.

> **남길 것**: 채움률이 낮다고 곧 파서 결함인 것은 아니다. **고칠 수
> 있는 빈칸과 고칠 수 없는 빈칸을 먼저 가르면**, 안 될 일에 시간을
> 쓰지 않는다. 조정 EPS 는 절반이 일감이었고(고칠 것이 있었고),
> EBITDA 는 0 이었다(고칠 것이 없었다).

---

# 150차-AV — 08-25 런은 SEC 429 로 4분 만에 죽었다 (우리 결함 아님)

## 무엇이 일어났나

    런 32840215901 · 11:02 UTC 시작 · **11:06 실패** · head_sha 448cca2
    401종목 **전부** `SEC Rate Limit Exceeded (HTTP 429)`
    "Estimated Wait: ~10 minutes"
    계기: 벽시계 2.9분 · **초당 0.0요청**(SEC 허용 10) · **접속 시도 0**

## 우리 결함이 아니라는 증거 셋

1. **받아오기 단계에서 멈췄다.** 종목마다 `직접공시 0건 · 조정EPS 0건 ·
   캐시 0/신규 0` 이다. 150차-AP·AS·AT 의 파서 변경은 **글을 다 받아온
   뒤** 읽는 부분이라 여기까지 오지도 못했다.
2. **우리 요청량이 아니다.** 초당 0.0요청이고 접속 시도가 0 인데 429 가
   났다. 공용 러너 IP 쪽 제한으로 보인다.
3. **네트워크는 멀쩡했다.** 같은 런에서 주가는 395/402종목을 받아 왔다.
   SEC 만 막혔다.

## 안전장치가 제 일을 했다

    ⛔ 절반 이상 실패 — 오늘 데이터로 덮어쓰지 않습니다 (종료코드 1)

**어제(08-24) 데이터가 그대로 살아 있다.** 화면도 정상이다. 잃은 것은
"오늘 새로 받았을 데이터"뿐이다. 94차에 적은 "표본을 늘리려다 있던 표본을
깎는 최악"을 막는 장치가 실제로 작동한 첫 기록이다.

## 어떻게 할 것인가

99차에 배운 것 — **429 뒤에 곧바로 다시 띄우면 둘 다 잃는다.**
약 한 시간 뒤 수동으로 한 번 띄운다. 그때도 429 면 오늘은 포기하고
내일 예약 런을 기다린다.

⚠️ 수동 실행 **직전에 런 목록을 다시 읽는다** — 08-24 에 10분 사이에
예약 런이 떠 있는 줄 모르고 수동 실행해 중복 런을 취소한 일이 있다.

> **남길 것**: 런이 실패했을 때 **먼저 물어야 할 것은 "내가 무엇을
> 바꿨나"가 아니라 "어느 단계에서 죽었나"** 다. 이번엔 파서를 세 번
> 고친 다음 날이라 내 탓으로 몰기 쉬웠는데, 로그의 "접속 시도 0" 한 줄이
> 그게 아님을 바로 알려 줬다.

---

# 150차-AW — 화면을 고쳐도 휴대폰에는 옛 화면이 남고 있었다

## 주인이 보여 준 것

08-25 아침, 주인이 앱에서 `BNMW` 를 검색한 화면을 보내 주었다. 거기에는
**어제 없앤 옛 안내**가 그대로 떠 있었다:

    "GitHub → Run workflow → 종목 코드에 BNMW 입력 → Run"

150차-AQ 에서 이미 "요청장 한 번 누르기"로 바꿔 커밋·푸시했는데도.

## 원인 — `<script src="app.js">`

`index.html` 이 화면 코드를 **주소만으로** 부르고 있었다. 그 주소는 늘
같으니 휴대폰이 **예전에 받아 둔 파일을 계속 썼다.** 저장소에는 새 코드가
있고, 깃허브 페이지도 새 코드를 내보내는데, **브라우저가 물어보지 않았다.**

이 고장의 고약한 점은 **아무 오류도 안 낸다**는 것이다. 화면은 멀쩡히
뜨고, 숫자도 맞고(app.json 은 따로 `cache: "no-cache"` 로 받고 있었다),
**틀만 옛것**이다. 주인이 알려 주지 않았으면 몇 달이라도 몰랐을 것이다.

## 고침

한 시간 단위로 주소를 바꿔 부른다 — 늦어도 한 시간이면 새 화면이 온다.
빌드 단계를 만들 필요가 없고, 파일이 작아 비용도 없다.

    s.src = "app.js?v=" + Math.floor(Date.now() / 3600000);

시험도 넣었다(`test_화면_코드가_캐시에_굳지_않게_불린다`). 돌연변이로
빨간 불을 확인했다.

> ⚠️ 시험을 처음 넣었을 때 **멀쩡한 코드에서도 빨간 불**이 났다.
> `index.html` 의 **한국어 주석 안에** 옛 코드 모양이 글자로 적혀 있어
> 시험이 그것을 집은 것이다. 주석 문구를 바꿔 해결했다.
> (교훈: 코드를 글자로 찾는 시험은 **주석도 코드처럼 읽는다**.)

## 곁들여 고친 것 — 단추가 눈에 안 띄었다

"가져오기"가 작은 글자 링크(`.back`)라 휴대폰에서 누를 자리로 보이지
않았다. 폭을 꽉 채운 초록 단추(`.go`)로 바꾸고, 문구도 **한 번 누르면
끝**이 먼저 보이게 다시 썼다. 없는 종목 코드를 넣었을 때를 대비해
"미국 상장 종목만 됩니다"도 적었다(주인이 넣은 BNMW 는 미국 상장이 아니다).

모바일 412px 실물 렌더링: **가로 넘침 0 · 콘솔 오류 0**.
`python3 run_tests.py` 종료코드 **0** · **700개 통과** (699 → 700).

> **남길 것**: 화면을 고친 뒤에는 **주인이 실제로 보는 화면**에서 바뀌었는지
> 확인해야 한다. 저장소에 올라간 것과 휴대폰에 보이는 것은 다를 수 있다.
> 132차에 "웹앱이 조용히 옛 데이터로 굳는" 고장을 막는 시험을 만들었는데,
> **데이터만 보고 화면 코드는 보지 않았다.** 같은 병의 다른 자리였다.

---

# 150차-AX — 08-25 런 검증: 조정 EPS +818칸, 그리고 계기 하나를 고쳤다

## 런 (운영 규칙 ① — head_sha 부터)

    런 32846730173 · head_sha **586b862** (오늘 고침 AP·AS·AT·AW 전부 포함)
    12:16 시작 · 16:24 **성공** · 수집 실행 242분 · 커밋 956f53a (516개 파일)

앞선 예약 런은 SEC 429 로 4분 만에 죽었고(150차-AV), 한 시간쯤 뒤 수동으로
다시 띄운 것이 이 런이다.

## 계기 (운영 규칙 ③ — 짐작 전에 계기부터)

    벽시계 **201.3분** (08-24 203.5분) · 일감합 1,207.2분 (1,220.5) ·
    겹침 6.0 · 내려받기 **730** (1,459)

> **걱정했던 것이 아니었다.** 150차-AT 에서 넣은 마지막 수단 파서가
> 실패한 문서마다 모든 줄을 훑기에 느려졌을까 의심했는데(검증메모에
> 적어 두었다), **벽시계도 일감합도 오히려 조금 줄었다.** 의심은
> 기록하되 계기로 갈랐다.

## 효과 — 주 잣대가 크게 늘었다

| 항목 | 08-25 | 08-24 | 변화 |
|---|---:|---:|---:|
| 매출 | 14,653 (97.0%) | 14,631 (97.0%) | +22 |
| GAAP EPS | 14,342 (94.9%) | 14,282 (94.7%) | +60 |
| **조정 EPS** | **7,766 (51.4%)** | 6,948 (46.0%) | **+818 (+5.4%p)** |
| 조정 EBITDA | 2,301 (15.2%) | 2,287 (15.2%) | +14 |

행 15,088 → 15,105.

**저장 원문 2,064건으로 잰 예측(+54칸)보다 훨씬 컸다.** 그 표본에는
"이미 잘 읽히던 보도자료"가 없기 때문이다(150차-AT 에 적어 둔 사각지대).
전체 유니버스에서 처음 재 보니 열다섯 배였다.

조정 EBITDA 가 +14 에 그친 것은 150차-AU 의 결론("고칠 것이 없다")과 맞는다.

## 안 깨졌는가 — 하나가 걸렸고, 계기 쪽이 틀렸다

    빠진 분기        91건·56종목 → **90건·54종목**  (줄었다)
    끊김 너머 폭      104칸 → **97칸**              (줄었다)
    분기 이름 어긋남   954/11,530칸 → 997/11,813칸  (비율 8.27% → 8.44%)
    ⛔ 매출 자릿수    0행 → **1행 ['ALNY']**        ← 새로 생김

### ALNY 를 파고든 결과 — **값이 아니라 자가 틀렸다**

경보의 이름표는 "**가까운 분기의** 1/100 미만"인데, 코드는 종목 **전체
중앙값**과 견주고 있었다. 말과 코드가 달랐다.

    ALNY 매출(백만 달러): 38 · 22 · 30 · **2.069** · 21 · 33 · 45 · 70 …
                          … 594 · 774 · 1,249 · 1,097 · 1,167 · 1,291

34배 자란 회사다. 전체 중앙값이 224.8M 이라 2018년 분기(2.069M)가 1/100
밑으로 떨어져 ⛔ 가 났다. 그런데 **앞뒤 이웃은 21~33M** 이니 자릿수가
어긋난 것이 아니라 그 시절이 작았던 것이다.

150차-Y 가 잡으려던 것(GS 15,520 = 실제 155억처럼 **한 행만** 단위가
어긋난 것)은 이웃과 견줘도 그대로 걸린다. 그래서 이름표대로 **앞뒤 4분기**
와 견주게 고쳤다.

### 시험이 헛돌 뻔했다

새 시험에 ALNY 수치를 **스무 개만 추려** 넣었더니, 중앙값이 178M 으로
낮아져 **옛 자로도 안 걸리는** 표본이 됐다. 돌연변이(창을 전체로 되돌리기)
를 돌려 보고서야 알았다 — 걸린 행이 0 이었다. 실물 35개를 그대로 넣으니
중앙값 259M 이 되어 옛 자로는 1행, 새 자로는 0행이 됐다.

> **남길 것**: 반례 시험은 **옛 자로는 반드시 걸리는 표본**이어야 한다.
> 추려서 만든 표본은 조용히 힘을 잃는다. 돌연변이 없이는 알 수 없다.

## 마무리

    screen_verify  ⛔ **0건** · 살펴볼 것 3건(전부 알려진 미해결) · 종료코드 0
    모바일 412px   네 탭 전부 가로 넘침 0 · 412px 밖 요소 0 · 콘솔 오류 0
    run_tests.py   종료코드 0 · **700개 통과**

---

# 150차-AY — ⚠️ 정정: 6차 확장 조건 "채움률 80%" 는 **도달할 수 없는 숫자**였다

## 무엇을 잘못 걸었나

150차-AR 에서 6차 확장을 사전 등록하며 조건을 이렇게 적었다:

    조건: 조정 EPS 채움률이 **80%** 에 닿는다 (지금 46.0%)

그때는 채움률이 낮은 이유를 갈라 보지 않고 "고치면 오르겠지"로 적었다.
오늘 런 뒤 사유별로 세어 보니 **그 숫자에는 닿을 수가 없다.**

## 실측 (조정 EPS 15,105칸, 08-25)

    있음                       7,549   (50.0%)
    **일감**(못 붙였고 그 회사는 냄)  2,134   ← 우리가 손댈 수 있는 것
    회사 미발표(글을 읽었는데 값이 없음) 4,617
    못 붙였고 그 회사는 안 냄        521
    발표 기록 없음(상장 전 등)        189
    구멍메움                      95

    일감을 **하나도 남김없이** 다 풀면      9,683칸 = **64.1%**
    회사 미발표 중에도 읽을 게 있다고 보면
      (저장원문 실측: 못 읽은 실적발표 중 값이 실제로 글에 있던 비율 29%)
                                    +1,335 → 11,018칸 = **72.9%**

    80% 는 12,084칸 — **낙관적 천장보다도 1,066칸 많다.**

## 왜 천장이 있나

조정 EPS 는 **논갭 지표**라 XBRL 태그가 없다. 회사가 보도자료에 안 쓰면
어디에도 없다. 은행·항공·유틸리티·일부 헬스케어는 아예 안 낸다.
150차-AU 에서 조정 EBITDA 로 같은 것을 확인했다 — 그쪽은 미발표가 74%
였고 우리 일감은 **0건**이었다.

## 조건을 바꾼다 (다시 사전 등록)

    (옛) 조정 EPS 채움률 80% 도달 후 +50종목        ← 도달 불가, 폐기
    (새) **일감이 1,000칸 아래로 내려오면** +50종목
         지금 2,134칸. 오늘 하루에 2,103 → 2,134 로 거의 그대로였지만
         "있음"이 +778 늘었다 — 고친 만큼 **다른 갈래에서 옮겨 왔다.**

왜 일감으로 재는가: 채움률은 **회사가 안 내는 몫**에 묶여 있어 우리가
아무리 잘해도 못 올린다. 일감은 **순전히 우리 몫**이라, 우리가 일을 했는지
안 했는지를 정직하게 잰다. 재는 대상이 우리 손 안에 있어야 조건이 뜻을 갖는다.

시간 예산 쪽 근거(벽시계·첫날 254분)는 150차-AR 그대로 유효하다.

> **남길 것**: 목표 숫자를 걸 때는 **그 숫자에 닿을 수 있는지 먼저 세어야**
> 한다. 닿을 수 없는 조건은 "영원히 안 하겠다"와 같은 말인데, 겉보기에는
> 신중해 보여서 아무도 의심하지 않는다. 150차-AU 에서 "고칠 수 있는 빈칸과
> 고칠 수 없는 빈칸을 먼저 가르라"고 적어 놓고, **내가 건 조건에는 그 자를
> 대지 않았다.**

---

# 150차-AZ — GAAP 쪽에는 있는데 조정 쪽에만 빠져 있던 세 곳

## 남은 일감을 새 원문(2,174건)으로 다시 셌다

08-25 런이 원문 68건을 더 담아 왔다. **150차-AT 에서 배운 자**(값이 글에
실제로 붙어 있는 것만 일감으로 센다)로 다시 세니:

    실적발표(손익계산서 실림)            667
      읽음                          307
      그 발표에 조정 잣대가 없음         306
      **일감(값이 글에 있는데 못 읽음)  54**
    실적발표 아님(손익계산서 없음)      1,506

## 54건을 다시 갈랐더니 — **또 절반이 헛것**이었다

    FFO(리츠 지표)          4   ← DLR "core FFO per share" 는 EPS 가 아니다
    전망 범위($X - $Y)      5   ← VRT "Adjusted diluted EPS $0.53 - $0.57"
    쉼표 낀 이름            4   ← UTHR "Non-GAAP earnings**,** per diluted share"
    그 밖                  41

"그 밖" 41건을 실물로 열어 보니 **연간값·재작성표·전망 문장**이 섞여 있었고,
진짜 고칠 수 있는 것은 세 가지 모양이었다.

## 고친 세 곳 — 전부 **GAAP 쪽 이름에는 이미 있던 것**

| 무엇 | 실물 | 왜 빠졌나 |
|---|---|---|
| **common share** | EL "Adjusted diluted net earnings **per common share** increased to $.32" · SMCI | GAAP 쪽 `_SHARE` 는 common\|ordinary 를 둘 다 받는데 조정 쪽은 **ordinary 만** 받고 있었다 |
| **operating** 수식어 | AMP "Adjusted **operating** earnings per diluted share increased 7 percent to $9.11" | 보험·자산운용사의 표준 표현인데 수식어 목록에 없었다 |
| **쉼표** | UTHR "Non-GAAP earnings**,** per diluted share(1) $3.34" | 이름 안의 쉼표를 허용하지 않았다 |

셋 다 **반쪽만 넣은 자리**였다. GAAP 쪽을 넓힐 때 조정 쪽도 같이 넓혔어야 했다.

가장 뚜렷한 증거는 SMCI 다 — GAAP 과 조정이 **같은 값 0.60 으로 무너져**
있었다(87차에 적어 둔 그 표시). 이제 GAAP 0.60 · 조정 0.69 로 갈린다.

## 실측 (저장원문 2,173건 전·후)

    조정 EPS  없음 → 값     16건  (SMCI 5 · EL 4 · UTHR 4 · AMP 2 · AVY · CLF · FANG · VLO)
    조정 EPS  값 → 다른 값    3건  — **셋 다 원문으로 확인, 셋 다 정답**
        AVY  9.15 → **1.65**  (9.15 는 FY2022 연간)
        CLF  −0.40 → **−0.05** (원문: "adjusted net loss of $25 million or $0.05 per diluted share")
        SMCI 0.60 → **0.69**  (0.60 은 GAAP 값이었다)
    **멀쩡한 값이 죽은 것: 0건** · 매출·GAAP·EBITDA 변화 0건

## 돌연변이 셋 전부 빨간 불

새 시험 3개를 넣고 넓힌 곳을 하나씩 되돌렸다 — 셋 다 해당 시험만 정확히
빨간 불이 됐다.

`python3 run_tests.py` 종료코드 **0** · **703개 통과** (700 → 703).

> **남길 것**: 이름을 넓힐 때는 **GAAP 쪽과 조정 쪽을 함께** 봐야 한다.
> 한쪽만 넓히면 그 차이가 곧바로 "두 잣대가 같은 값으로 무너지는" 결함이
> 된다. 오늘 찾은 셋은 전부 그 꼴이었다.

---

# 151차 — 새 가설 탐색 (주인 지시: "주가를 미리 포착하는 모델링") + H27·H28 사전 등록

## 왜 했나

주인 지시(2026-08-26): "새 가설 후보를 전반적으로 분석해서 확보해봐.
주가를 미리 포착하는 모델링으로." 150차-AR 의 결론("필요한 것은 표본이
아니라 더 나은 가설")의 다음 걸음이기도 하다.

## 탐색 규율 (헌법 5장 그대로)

    · 등록된 24개(H2~H26)와 확정 지식(전략.md 1장)을 먼저 읽어 중복 회피
    · 자는 등록된 측정 장치 그대로 — 창 60거래일 · 폭등 = SPY 대비 +20%p ·
      윌슨 95% · 발표 시점에 알 수 있는 것만
    · 앞/뒤 시간 분할 병기 · 좋든 나쁘든 전부 기록
    · **폭락률(SPY 대비 −20%p↓)과 초과수익 중앙값을 나란히** — 이번에
      새로 세운 대조인데, 이것이 가장 큰 발견을 만들었다 (아래 ①)

표본: 주잣대 사건 10,479건 (401종목 · 10년) · 매출 독립 사건 12,287건.

## 잰 것 전부 (16개 후보)

| 후보 | 신호 n | 폭등률 | 기준선 | 차(하한−상한) | 판독 |
|---|---:|---:|---:|---:|---|
| F1 매출 TTM 첫돌파 (독립) | 908 | 8.7 | 8.9 | −2.4 | 기각 |
| F1b 매출 신고점 전체 | 7,218 | 8.4 | 8.9 | −1.6 | 기각 |
| F2 쌍 신기록(이익 첫돌파∧매출 신고점) | 400 | 11.2 | 10.1 | −2.3 | 기각 |
| F2b 쌍 첫돌파 | 87 | 8.0 | 10.1 | −6.8 | 기각 |
| F3 논갭영업이익 첫돌파 | 595 | 9.7 | 9.4 | −2.5 | 기각 |
| F4 첫돌파∧마진 YoY 개선 | 180 | 14.4 | 13.2 | −4.1 | 기각 |
| F4b 마진 YoY +2%p↑ 단독 | 919 | 14.5 | 13.2 | −1.9 | 기각 |
| F5 발표 반응 +5%p↑ | 1,453 | 15.1 | 9.1 | +3.7 | **U자 — 변동성 착시** (아래 ①) |
| F5b 발표 반응 +10%p↑ | 655 | 18.2 | 9.1 | +5.7 | 〃 |
| **F6 가뭄 8발표↑ 끝 첫돌파** | **58** | **20.7** | **10.1** | **+1.5** | **→ H27 등록** |
| F7 매출폭 게이지>중앙값 (위층) | 1,926 | 8.9 | 9.5 | −2.4 | 기각 |
| F8 매출 YoY 가속 | 4,919 | 9.7 | 9.1 | −0.7 | 기각 (EPS 가속 기각과 같은 결) |
| **F9 첫돌파∧52주 고가 −15%↓** | **180** | **16.7** | **10.1** | **+1.1** | **→ H28 등록** |
| F9c 첫돌파∧고가 −5% 이내 | 196 | **4.6** | 10.1 | — | **이미 달린 첫돌파는 늦다** (②) |
| F10 동료 선행(같은 묶음 +10%p 반응) | 2,380 | 13.0 | — | — | U자 — 기각 (폭락 10.5%) |
| F11 게이지 8주 방향 | 256/634 | 12.5/13.7 | 8.8 | — | 방향 못 가름 — 기각 |

## 발견 ① — 폭등률 하나만 보는 자는 **변동성에 속는다** (장치 지식)

발표 반응 +5%p↑ 는 15.1%로 기준선(9.1%)을 크게 이기는 듯 보였다.
그런데 대조를 대니:

    반응 +10%p↑ : 폭등 18.2% · 폭락 14.7% · 초과수익 중앙 −0.1%p
    반응 −2~+2% : 폭등  6.0% · 폭락  4.2% · 중앙 −0.4%p
    반응 −10%p↓ : 폭등 20.7% · 폭락 11.6% · 중앙 −0.5%p   ← 반대쪽도 높다!

**U자다.** 발표에 크게 움직인 종목은 방향과 무관하게 이후에도 크게
움직인다 — 분포의 중심은 안 움직이고 꼬리만 넓어진다. 한쪽 꼬리(+20%p)만
세는 폭등률은 이것을 "우위"로 잘못 읽는다. 동료 선행(F10)·게이지
방향(F11)도 같은 병이었다.

> **남길 것**: 앞으로 어떤 후보든 **폭락률과 중앙값을 함께** 본 뒤에만
> 유망하다고 부른다. 진짜 신호는 비대칭(폭등↑ 폭락↓ 중앙+)이어야 한다.

## 발견 ② — 이미 달린 첫돌파는 늦다

첫돌파를 발표 전일 주가 위치로 갈랐다:

    첫돌파 ∧ 고가 −15%↓ 아래 : 폭등 16.7% · 폭락 9.4% · 중앙 +2.0%p
    첫돌파 ∧ 고가 −5%~−15%  : 10.5%
    첫돌파 ∧ 고가 −5% 이내   :  4.6%   ← 기준선(10.1%)의 절반도 안 됨

확정 지식 5("추세와 실적은 동행하나 확인하면 늦다")의 **종목 단위 재현**
이다. 첫돌파의 우위는 "주가가 아직 안 달린" 쪽에 몰려 있다.

## 발견 ③ — 가뭄 끝 첫돌파 (H27 후보)

직전 신고점 이후 **8발표 이상**(약 2년) 신기록이 없던 회사의 첫 돌파:

    폭등 20.7% [12.3, 32.8] vs 기준선 10.1 [9.4, 10.8] — 탐색치로 분리
    폭락 3.4% (기준선 7.4 의 절반) · 중앙 +4.4%p — **비대칭 (U자 아님)**
    앞/뒤 23.1%(13건)/20.0%(45건) · 57종목에 분산 (군집 없음)

정직하게 함께 적는 약점:
    · **가뭄 길이의 단조성이 없다** — 0~3발표 11.4% · 4~7발표 6.0% ·
      8+ 20.7%. 중간이 움푹하다. H1 을 기각할 때 단조성 부재를 근거로
      썼으므로, 이것이 우연한 승자일 위험은 실재한다.
    · 문턱 8은 표를 보고 골랐다.
    · 신호가 드물다 — 10년에 58건(연 약 6건). n≥10 까지 약 2년.

## 사전 등록 — H27 · H28 (등록일 2026-08-26, 판정은 새 데이터만)

    H27_가뭄끝_첫돌파:  조정 EPS 사건 전체가 표본(기준선), 신호 = 첫돌파
      ∧ 가뭄(직전 신고점 이후 발표 수) ≥ 8. 가뭄 None(이력상 최초)은
      신호 아님. 채택 기준은 11차 그대로(하한>상한, n≥10).
    H28_안달린_첫돌파:  고가대비(발표 전일 종가의 52주 고가 대비 %)를 잴
      수 있는 조정 EPS 사건이 표본, 신호 = 첫돌파 ∧ 고가대비 ≤ −15.
      대조 병기: 위치 단독(전체∧−15%↓)이 13.7%였다 — 우위의 상당 부분은
      위치 효과다.
    둘 다 첫 표본 하한 = 등록일 + 60거래일 ≈ **2026-11-18** (H18b 규칙).

구현: 측정 장치에 "가뭄"(earnings_states → 사건)과 "고가대비"
(attach_high52 — **발표 전일까지의 종가만**, 미래 금지 시험 포함)를 넣고,
판정기·수집 로봇·model_verify 등록부·가설 시계에 배선했다.

곁가지 수리: 화면 점검 ⑥("가설 수")이 "등록됐지만 로봇이 아직 안 돈"
상태를 배선 사고(⛔)로 오인해 **등록하는 날마다 거짓 경보**가 나게 되어
있었다(실제로 시험이 깨졌다). 마지막 수집 시각과 등록일을 견줘 "다음 런
대기"(확인 못함)와 진짜 빠짐(⛔)을 가르게 했다.

돌연변이 아홉(가뭄 리셋·None 지어내기·미래 누출·문턱·표본 오염·등록일
무시·배선 누락·화면 유예) 전부 빨간 불 확인.

## 151차 후속 — 08-26 로봇 런 검증 (H27·H28 첫 탑재 확인)

08-26 예약 런(11:04 UTC 시작, head_sha `5ad0514` — H27·H28 포함)이
14:44 UTC에 판정을 끝내고 `b5abc64`로 커밋됐다. 검증 결과 (숫자는
전부 실행 출력에서 복사):

- **H27·H28 이 verdict.json 에 처음 실렸다** (가설 26개). 둘 다
  신규(판정) n=0 · "판정 불가" — 등록일(08-26) 뒤 새 발표가 아직 없으니
  이것이 정답이다. 탐색표본(참고)은 로봇의 오늘 데이터로 재계산되어
  H27 신호 19.0%(n=63) · H28 신호 15.8%(n=190) — 등록 때 수치
  (20.7%/n=58 · 15.8%)와 데이터 하루 차이만큼 다르고, 참고란이므로
  판정에 안 쓰인다.
- **계기 (어제 → 오늘)**: 벽시계 201.3 → 192.9분 (가뭄·고가대비 추가로
  느려지지 않음) · 매출 14,653(97.0%) → 14,664(97.1%) · 조정 EPS
  **7,766(51.4%) → 8,210(54.3%)** — 150차-AP~AZ 파서 고침이 로봇에서
  처음 돈 효과(+444칸) · GAAP 14,342(94.9%) → 14,418(95.4%) ·
  EBITDA 2,301(15.2%) → 2,373(15.7%) · 일감(못 붙였고 그 회사는 냄)
  2,134 → **1,834칸**(−300).
- 어제 대비 바뀐 칸 344 (어제는 1,436) — 많이 바뀐 종목 YETI 100 ·
  AMP 33 · ALNY 25 · SMCI 25. 사라진 분기 401/새 분기 405는 어제
  (325/343)와 같은 일상적 흔들림 범위.
- screen_verify ⛔ 0 · 가설 수 26/26 (⑥ 유예 수리가 실전에서 통과) ·
  판정 code_rev 5ad0514. run_tests 712개 통과 · 종료코드 0.

다음 관찰점: H27·H28 첫 표본 하한 2026-11-18. 그때까지 두 가설의
장치·판정 코드는 건드리지 않는다.

# 152차 — 소프트웨어 섹터 집중 관찰 (주인 지시, 2026-08-27)

주인 관찰: "소프트웨어가 오늘도 폭등 중, 델타 상승 중에 정배열 대부분
진입 중으로 보임. AI 하드웨어→소프트웨어 주도 전환인지 판단 필요."
데이터는 08-26 종가까지 (읽기 전용 관찰 — 코드·문턱 변경 없음).

실측 (숫자는 전부 실행 출력에서 복사):

- 묶음 동일가중 SPY 대비 초과수익(%p): 소프트웨어 20일 +7.0 · 60일
  +8.3 · 125일 +32.8. 같은 기간 기기 OEM 반도체 +4.4 / **−19.0** /
  +25.5, 팹 장비 +7.1/−9.3/+5.2, 데이터센터 납품 +13.3/−13.7/+5.8,
  광통신 +21.7/−25.7/+15.3. → 최근 60거래일은 소프트웨어가 하드웨어를
  27%p 이상 앞섬. 125일로는 아직 하드웨어(교체주기 IT +95.6, 메모리
  +92.8)가 크게 앞 — 전환이라면 초기 국면.
- 정배열(주봉 엄격: 종가>4주>13주>26주>52주): 완성 0/39. 그러나
  **3/4 충족이 19/39** — ① 26주>52주만 남은 10종목(APPF BOX DBX GTLB
  MANH PATH PAYC PCTY TEAM VEEV, 간격 −0.7%~−20%): 깊은 하락 뒤 4~5주
  급반등(PAYC 8주 139→231, TEAM 89→168)이라 장기선이 아직 위. ② 종가>
  4주만 놓친 9종목(CRWD FTNT NET OKTA PANW S TENB TWLO ZM — 보안
  소프트웨어 위주): 이미 완성 뒤 잠깐 눌림. 참고용 일봉(20>60>120일)
  정배열은 16/39. → "정배열 진입 중"이라는 관찰과 일치.
- 주도섹터 장치(H19): 최근 13주 완성수 소프트웨어 9 = **전 묶음 최다**,
  델타폭 57~63%. 그러나 완성밀도 9/39=23% < 문턱 30% → 도전 조건
  미달, 주도점수 14. 현재 주도는 "규제 요금 전력·가스"(점수 75)인데
  최근 20일 상대 −7.2%p로 힘 빠짐 — 도전자 없어 유지되는 상태.
  소프트웨어가 마지막으로 조건충족이던 주는 2025-12-05.
- 과거: 5년 33개 주도 국면 중 소프트웨어 주도는 1번(2020-07-02~10-09,
  이후 60거래일 +6.9%p). 하드웨어 계열 주도는 데이터센터 납품
  (2025-09~2026-01), 기기 OEM 반도체(2026-05~06).

한계 (정직 고지): ① 08-27 장중 폭등은 데이터에 없음 — 내일 로봇 런에
실림. ② H20(주도 전환 시 새 주도 매수)은 5년 실측 성공 20%로 미채택 —
전환 확인 자체는 검증된 매수 신호가 아님. ③ 표본은 우리 401종목.

관찰 계획: 26주>52주 간격이 −7% 이내인 DBX(−0.7)·BOX(−4.5)·PAYC
(−6.8)·MANH(−7.7)가 몇 주 안에 완성되면 완성수가 밀도 문턱(12/39)에
접근 — 상태기계가 스스로 전환을 선언하는지 매일 런에서 지켜본다.

## 152차-B — 주인 제안 조합 백테스트와 H29 사전 등록

주인 제안(2026-08-27): "델타 상승 + 주봉 정배열 완성 + 이격선 상승으로
보면 어떤가? 지금 이게 최고잖아?" — 등록된 자(60거래일 · SPY+20%p)로
5년 전수 백테스트했다 (완성 4,971건, 창 완료분).

**종목 단위 (숫자는 실행 출력에서 복사):**

| 조건 | n | 폭등 | 폭락 | 중앙 | 앞/뒤 |
|---|---|---|---|---|---|
| 완성 전체(기준선) | 4,971 | 9.0% | 6.9% | −0.7p | 9.1/8.9 |
| +델타↑ | 2,154 | 10.4% | 6.6% | −0.5p | 10.8/10.1 |
| +델타↑∧이격상승 | 1,790 | 10.6% | 6.3% | −0.5p | 10.9/10.2 |
| **+이격도 30%+ 까지 셋** | **324** | **24.4%** | 12.0% | **+2.8p** | **26.2/22.5** |
| (비교) 이격 30%+ 단독 = H18 | 714 | 23.5% | 14.1% | +2.8p | 24.3/22.8 |

이격 수준별 단조: 0~10% 5.7 · 10~20% 5.6 · 20~30% 12.3 · 30~50% 20.9 ·
50%+ 33.0. 폭등이 폭락의 2배(비대칭)·중앙 양수·시간 분할 안정 —
151차 U자 검사 통과. 몸통은 H18이고 델타↑∧이격상승은 폭락률을
14.1→12.0으로 깎는 보조 필터다.

**섹터 단위 번역판 — 등록하지 않는다:** 묶음에서 조합 사건이 13주 안에
2건+ 몰린 첫 주(76건) → 묶음 동일가중 60거래일: 폭등 15.8% vs 기준선
7.9%로 전체는 좋아 보이나, **2023년 이후만 자르면 폭등 6%·중앙 −3p로
소멸**. 소프트웨어 묶음은 2018~2021년 4번 전부 +15~30p 성공 뒤
**2023-09부터 6연속 마이너스**(−8.0, −8.7…−17.4p). 시간 분할이
가르는 전형이라 기각하고 기록만 남긴다 (원칙 4·5).

**H29 등록 (등록일 2026-08-27, 판정은 등록일 뒤 새 완성만):**

    H29_완성_델타이격_결합: 표본 = 초과60·이격도·델타·이격도_4주전을
    모두 잴 수 있는 정배열 완성(39차 정의) 사건 전부. 신호 = 델타 상승
    ∧ 완성 주 이격도 ≥ 30% ∧ 이격도 > 4주 전 이격도.
    채택 기준은 11차 그대로. 첫 표본 하한 ≈ 2026-11-19.
    약점 명기: 문턱(30%·4주)은 탐색 표를 보고 골랐다 · 섹터 번역판은
    최근 3년 실패 · H18과 표본이 겹친다(H29는 H18의 강화판 — 판정이
    갈리면 그 자체가 정보다).

구현: completion_events 에 "이격도_4주전"(이력 4주 미만이면 없음),
judge_completion_combo, 수집 로봇·시계·model_verify 배선. 시험 5개
추가, 돌연변이 7종(이격상승 >→≥ · 델타 조건 제거 · 판정을 탐색 표본으로
· 없음 검사 제거 · 4주전 대신 이번주 · 이력부족 검사 제거 · 시계 누락)
전부 빨간 불 확인. 도중 사고 1건: 돌연변이 되돌리기(git checkout)가
커밋 전 새 코드까지 지워 가짜 빨간불(💥)이 났다 — 재작성 후 **커밋
먼저, 돌연변이는 그 다음** 순서로 다시 돌려 전부 확인했다.

현재 상황 대조(참고): 오늘 기준 조합 충족 소프트웨어 = OKTA·FTNT,
임박(이격30%+·델타↑) = TEAM·SNOW·DDOG. 조합 충족 수 상위는
의약·에너지·IT하드(각 3). H29가 내일 런부터 매일 자동 판정된다.

# 153차 — 채움률이 매일 출렁이는 원인: 시간 예산이 캐시 파싱까지 자름

08-27 런 검증에서 조정 EPS 채움이 8,210→7,994(−216칸)로 줄었다.
전날(+444)과 이날(−216)의 정체를 파니 **같은 원인**이었다:

- 8-K 훑기 시간 예산(94차)은 전역 벽시계라, 예산에 걸리는 종목이
  날마다 다르다 — 시간초과 종목 수: 08-25 **51개** → 08-26 **20개** →
  08-27 **44개**. 걸린 종목은 옛 분기 훑기를 통째로 멈춰(break) **캐시에
  멀쩡히 있는 원문의 파싱까지** 건너뛰었다.
- 실측: GWW 조정 EPS 10→27→10칸 (2016~2020 분기가 사라졌다 돌아왔다),
  오늘 잃은 216칸은 전부 시간초과 44종목(GWW·ROK·JCI·RTX·LDOS·URI…)의
  옛 분기. GWW 오늘 기록: 내려받기 0건·캐시 적중 45건·15초 — 네트워크를
  전혀 안 썼는데도 예산에 걸려 잘렸다.
- **정직 정정**: 151차 후속에 "조정 EPS +444칸은 파서 고침 효과"라고
  적었으나, 그 일부는 전날(51개)보다 시간초과가 적었던(20개) 운이다.
  파서 효과와 예산 운이 섞여 있었고 분리해 재지 않았다. 파서 효과의
  순수분은 예산 수리 뒤 채움률이 안정된 다음에야 제대로 보인다.

수리 (153차): 예산 초과 시 통째로 멈추지 않고, **캐시에 없는 공시만**
건너뛴다(내려받기만 다음 런으로). 캐시된 원문 파싱은 계속한다 —
네트워크가 아니라서 종목당 몇 초다. `_text_in_cache`(네트워크 없이
판 번호까지 확인) 신설, 예산 가지 break→continue.

시험: 캐시 판별 4갈래 + 예산 가지 모양(continue·캐시조건) 검사.
돌연변이 3종(판 번호 검사 제거 · continue→break · 캐시 조건 제거) 전부
빨간 불 — 단, 처음 두 번은 시험 창이 넓어 돌연변이를 놓쳤고(가짜
초록) 창을 좁혀 보강한 뒤에 잡혔다. 그리고 **커밋 전에 git checkout 으로
되돌리다 새 코드를 두 번 지워 먹었다** — 돌연변이 검증은 반드시
"커밋 먼저, 변이는 그 다음"으로 돈다(작업 규칙으로 남김).

실행 증명 계획: 내일(08-28) 런에서 ① 시간초과 종목의 조정 EPS 가
줄지 않고 ② GWW 27칸 회복 ③ 채움률이 8,200선 위로 안정 — 를 확인한다.
예산 수리로 벽시계가 크게 늘지 않는지(캐시 파싱 추가분 +몇 분 예상)도
함께 본다.

## 153차 후속 — 08-28 런 실행 증명 (수리 실효 + H29 첫 탑재)

08-28 런(head_sha `de334ec`, 15:16 UTC 완료, 수동 실행 — 예약 이틀 연속
미발화)의 검증. 숫자는 전부 실행 출력에서 복사:

- **예산 수리 실효**: 조정 EPS 잃은 종목 **0개** (어제는 25종목 −216칸),
  회복 +224칸 → 8,218칸(54.3%, 최고치). GWW 10→27칸 복귀.
  벽시계 194.9→198.6분(+3.7분) — 캐시 파싱 추가 비용이 예상(몇 분)대로.
  시간초과 표시는 20종목 남음 = 진짜 새 내려받기만 미룬 것(설계대로).
- **H29 첫 탑재**: 가설 27개. 신규(판정) n=0 "판정 불가" — 등록 이튿날이니
  정답. 탐색표본(참고) 신호 24.6%(n=325) vs 기준선 9.4% — 제 152차
  백테스트(24.4/9.0)와 독립 재계산이 일치(장치 교차 확인).
- screen_verify ⛔ 0 · 가설 수 27/27 · code_rev de334ec.
- 소프트웨어(주가 08-28 장중 포함): 조합 충족 5종목(CRWD FTNT NET OKTA
  TENB) 유지 · 임박 6종목(ESTC 추가) · 묶음 1위 유지. 델타폭 57→65.7%
  상승. 완성밀도 24.3%(도전 문턱 30% 미달) — 주도는 여전히 전력가스.

남은 관찰: 예약 런 미발화가 이틀째(28일도 09:30 예약이 안 뜸) — 계속되면
예약을 다시 등록하는 처방을 검토한다.

## 153차 후속-B — 08-29 새벽 런(종가 확정) 검증: 수리 이틀째 안정

예약 런이 12시간 늦게 스스로 떠서(08-28 21:48 UTC, schedule) 금요일
**종가 확정치**로 수집됐다 (head_sha 4e430f8 · 성공 · 벽시계 190.1분).

- 조정 EPS 잃은 종목 **0개**(이틀 연속), +101칸 → **8,319칸(55.0%)**
  또 최고치. 시간초과 44→7종목(캐시가 차며 줄어드는 중), GWW 27칸 유지
  — 153차 수리가 이틀째 안정.
- 소프트웨어(08-28 종가): 조합(정배열∧델타↑∧이격30%+) 충족
  **5→6종목**(S 신규 완성: CRWD FTNT NET OKTA S TENB) · 임박 5 ·
  묶음 1위(2위의 2배). 완성밀도 24.3%(문턱 30%), 주도는 여전히
  전력가스 — 전환 미선언.

# 154차 — 조합 신호판을 화면에 올리고, 비중 이동 질문에 백테스트로 답했다

주인 지시(2026-08-29): ① 조합 충족 종목을 앱에 표시 ② 종목 명단 ③
백테스트로 하드웨어→소프트웨어 비중 이동 판단.

**화면**: 홈 탭에 "조합 신호 (H29)" 칸 신설 — 지금 네 조건(정배열 유지
∧ 델타↑ ∧ 이격 30%+ ∧ 이격 상승)을 동시에 만족하는 종목을 이격도순으로
나열. 정직화: 152차 실측(폭등 24.4% vs 기준선 9.0% · 폭락 12.0%)과
"H29 판정 불가 — 표본 수집 중, 첫 표본 11-19"를 함께 적음. 신선도
(140일)·미래 금지 가드 포함. 오늘 명단 17종목(신선도 적용):
HPE OKTA MPC VLO CRWD LITE ZETA CRL TGT TENB STT NET MRK S TRGP ANET DXCM.

**비중 이동 백테스트 (전부 탐색 — 등록 아님, 문턱은 지금 값을 보고 골랐다)**:
- 지금 국면 재현(SW 60일 상대 ≥+5 ∧ HW ≤−10 첫 진입): 5년간 **2번뿐**
  (2019-07 SW −14.8p 패 · 2024-09 SW +17.1p 승) — 표본 부족, 판단 불가.
- 조합 사건의 묶음 몰림 효과(창 완료 331건): 몰림(13주 내 선행 2건+)
  폭등 23.4% · 외톨이 24.1% · 중간 26.5% — **몰림 보너스 없음.**
  우위는 종목 신호 자체에 있고 섹터 집중은 아무것도 더하지 않는다.
- 이미 잰 것: 섹터 통째 매수 번역판은 2023년 이후 우위 소멸(152차-B).

**판단**: "섹터 비중 이동"이라는 거시 베팅은 데이터가 지지하지 않는다
(재현 표본 2건, 몰림 보너스 0, 섹터 번역판 실패). 데이터가 지지하는
방식은 **조합 충족 종목을 담는 것**이고, 그 결과로 소프트웨어 비중이
자연히 커진다(17개 중 5개). 이 판단을 주인에게 그대로 보고한다.

**곁가지 결함 2건 수리**: ① 새 화면 점검(⑧b)이 액면분할 보정 없는
조립으로 재계산해 CRWD 가 거짓 ⛔ — 화면 빌더와 같은 조립(보정 포함)
으로 고침. ② 시험 2개가 실행부 뒤에 붙어 한 번도 안 돌았음(가짜
초록불) — 앞으로 옮겨 빨간 불 확인 후 통과. 돌연변이 5종(네 조건
각각 제거·화면 종목 빼기) 전부 빨간 불. 시험 723개·22파일·종료코드 0 ·
모바일 412px 네 탭 이탈 0·오류 0 (조합칸 렌더링 확인).

## 154차 후속 — 08-29 오후 런 검증 (조합 신호판 첫 자동 갱신)

08-29 예약 런(head_sha 2cdbaf5 · 13:41 UTC 완료 · 벽시계 198.7분):
- **조합 신호판이 로봇 손으로 처음 갱신됐다** — 22종목(DELL·FTNT·ILMN 등
  합류). 가격은 금요일 종가 그대로이고 늘어난 것은 오늘 수집된 실적
  갱신으로 델타가 바뀐 종목들이다. 가설 27개 · 시간초과 8종목(안정).
- 조정 EPS 8,319→8,311(−8): 전부 RH 한 종목. 사흘 추적 16→24→16 —
  어제 새벽 런의 +8 이 오히려 이상치였다(같은 '16 Q4' 이름에 다른 값
  0.68/1.27 이 중복 편입). 오늘 16으로 돌아온 것이 안정 상태로 보이나,
  RH(1월 결산)의 분기 짝짓기가 런마다 흔들리는 것 자체는 관찰 대상으로
  남긴다 — 이틀 더 지켜보고 흔들리면 회차를 연다.

# 155차 — RH 흔들림의 진범: XBRL 일시 오류가 규격 위반 행을 만들고 있었다

154차 관찰 조건(이틀 더 흔들리면 회차 개설)이 걸렸다 — RH 조정 EPS 가
나흘간 16→24→16→24칸으로 번갈아 뜀. 계기로 원인을 확정했다:

- 24칸 판이 나온 두 런 모두 RH 의 XBRL 조회가 **SSLVerificationError**
  (SEC 쪽 일시 오류)로 실패해 뼈대가 통째로 비었고, merge_quarters 의
  대체 경로가 보도자료 행을 발표일 그대로 분기 행으로 승격했다.
  그 행들은 분기 신원(기간종료일)이 틀렸다 — 실측: 한 날짜(2023-03-27,
  10-K)에 분기 4개(4.26/6.12/6.63/7.78), 이름 중복 5쌍. 규격 위반이며
  "없음"이 아니라 **"틀림"** 을 만들고 있었다(헌법 1조 위반).
- 조합 신호판이 17→22→17로 흔들린 것도 같은 병이다(승격 행이 델타
  계산을 바꿈). YETI 가 매일 "많이 바뀐 종목" 1위였던 것도 같은
  갈래로 의심된다(다음 런들에서 확인).

수리: ① XBRL 조회를 3회 재시도(간격 2·4초) — 일시 오류 흡수.
② 그래도 실패하면 xbrl_error 표시, merge_quarters 는 **오류로 빈
뼈대에는 승격하지 않고 빈 목록**을 돌려준다(없음이 안전). 원래 XBRL 이
없는 회사(오류 아님 — 41차 은행·제약 구멍)는 기존 승격 경로 유지,
회귀 시험으로 못박음.

시험 2개 추가(106개), 돌연변이 3종(재시도 제거·표시 안 찍음·표시 무시)
전부 빨간 불. 실행 증명: 다음 런부터 RH 16칸 고정 · 조합판 종목수
안정 · robot_log 에 xbrl_error 종목이 남으면 그 종목은 그날 뼈대
없음(다음 런 회복)으로 읽는다.

# 156차 — "빠진 분기 91건"의 정체를 갈랐다 (150차-K 이후 첫 진전)

150차-K 이후 "원인 미규명"으로 남아 있던 항목을 파고들었다. 갈래를 세니
성격이 완전히 다른 두 무리였다 (숫자는 실행 출력에서 복사):

- **행 자체가 뼈대에 없음 79건** ← 진짜 결함
- 행은 있는데 발표일만 없음 12건 (8-K 짝짓기 실패 — 이미 아는 갈래)

79건의 모양 (실측):
- 종목 편중: JPM 6 · MTB 6 · RF 5 · SYF 4 · WFC 3 · TFC 3 — **은행이 상위**
  (은행은 OperatingIncomeLoss 를 신고하지 않는다는 106차 지식과 이어진다)
- 시기 편중: 2016년 17 · 2017년 23 · 2018년 8 · 2019년 13 · 2020년 12 —
  **73%가 2020년 이전**. 2024년 이후는 1건뿐.
- 간격이 대부분 180~191일 = 정확히 **한 분기가 통째로 빠짐**
- 실물 확인: INTC 2016-10-01 → 2017-04-01 (2016-12-31 4분기 없음) ·
  ADI 2017-10-28 → 2018-05-05 (2018-02-03 없음). 두 종목 다 그 구간은
  매출이 비어 있고 GAAP EPS 만 있는 '근사치' 행이다.

**원인 후보 두 가지**로 좁혔고, **단정하지 않는다**:
  (가) 4분기가 3개월 계열에 없는데 `_fill_missing_q4` 도 실패 —
       앞선 세 분기가 모자라거나 연간값 자체가 없어서
  (나) 옛 회계기준 구간이라 매출 개념이 달라 계열이 통째로 비어,
       분기 목록(op_income·revenue·gaap_eps 합집합)이 안 만들어짐
확정하려면 XBRL 원자료가 필요한데 **개발 환경은 SEC 가 막혀 있다.**
그래서 짐작으로 뼈대를 고치지 않고(뼈대를 바꾸면 전 종목 행이 흔들려
판정까지 움직인다 — 106차 경고), **로봇이 답을 실어 오게 계기를 심었다.**

심은 계기 (`robot_log.per_ticker[].q4_채움`, 항목별):
  연간수 · 분기수 · 채움 · 이미있음 · 앞선세분기부족 · 연간없음 ·
  못채운예(날짜 5개까지)
이 숫자가 오면 (가)와 (나)가 갈린다 — 앞선세분기부족이 크면 (가),
연간없음·연간수 0 이 크면 원문에 값이 없는 것이라 **고칠 수 없는 갈래**다.

시험 3개 추가(109개), 돌연변이 4종 전부 빨간 불. 그중 하나는 내
시험의 구멍을 드러냈다 — collect_job 쪽 배선만 보는 시험은 "계기를
안 넘김" 돌연변이를 놓쳤다(가짜 수집기가 그 경로를 안 타서 빈 칸이
그대로 통과). 실행 경로를 실제로 태우는 시험으로 바꾼 뒤에 잡혔다.

# 157차 — 08-31 런 검증: 155차 수리 확인 + 156차 계기가 원인을 확정했다

08-31 예약 런(head_sha `a2aeb9b` · 13:45 UTC 완료 · 벽시계 201.1분).

**① 155차 수리 실효 확인**: RH 조정 EPS **16칸으로 안정**(토 16 → 일 24 →
월 16). 오늘 xbrl_error 종목 0 — SEC 가 멀쩡한 날은 정상 경로로 16칸이고,
오류가 났던 일요일에만 24칸(이름 중복 5쌍)이었다. 수리대로다.
⚠️ **내 오독 정정**: 처음 로그를 읽을 때 `adj_eps_ok`(로봇이 8-K 에서 읽은
건수, 늘 24)를 스냅샷 칸 수로 착각해 "수리가 안 먹었다"고 잠깐 판단했다.
두 숫자는 다른 것이다 — 계기 이름을 확인하고 읽어야 한다.

**② 156차 계기 첫 숫자 — 빠진 분기의 원인 확정**:

| 종목 | revenue 연간 | revenue 분기 | 채움 | 못채움 |
|---|---|---|---|---|
| SYF | **0** | **0** | – | – |
| TFC | **0** | **0** | – | – |
| RF | 6 | **0** | 0 | 6 |
| MTB | 15 | 11 | 2 | 13 |

**은행은 XBRL 매출 계열이 아예 없거나(SYF·TFC) 분기 계열만 통째로
비어 있다(RF).** 앞선 세 분기가 없으니 4분기 채움이 원리적으로 불가능하고,
분기 뼈대 자체가 안 만들어진다. 빠진 분기 상위가 전부 은행인 이유가
이것이다(JPM 6·MTB 6·RF 5·SYF 4·WFC 3·TFC 3). 106차의 "은행은
OperatingIncomeLoss 를 신고하지 않는다"와 같은 뿌리이고, 이번에
**매출도 마찬가지**임이 계기로 확정됐다.

전체 채움 성적(참고): revenue 채움 2,489 · 이미있음 1,786 · 못채움 768 ·
op_income 채움 2,972 · 못채움 675. 즉 채움 장치 자체는 잘 돈다.

**계기의 흠도 기록**: `_fill_missing_q4` 는 표본 시작(2016) **밖**의
연도까지 세므로 "못채움" 숫자가 부풀려져 있다(JPM 못채운예에 2007·2008
이 나온다). 갈래를 가르는 데는 지장이 없어 그대로 두고 적어 둔다.

**③ 157차 — 다음 답을 위한 계기**: 은행이 매출 자리에 쓰는 개념 5개
(RevenuesNetOfInterestExpense · InterestAndDividendIncomeOperating ·
InterestIncomeExpenseNet · InterestIncomeExpenseAfterProvisionForLoanLoss ·
NoninterestIncome)가 **실제로 있는지 세기만** 한다. 값은 쓰지 않고 뼈대도
그대로 둔다 — 있는 것이 확인되면 그때 넣는다(짐작으로 뼈대를 건드리지
않는다). 시험 3개 추가, 돌연변이 3종 빨간 불.

## 158차 — 매일 점검의 자를 저장소로 (매번 다시 만들던 것)

모바일 412px 점검 스크립트를 임시 폴더에 두고 써 왔는데, 세션 컨테이너가
자주 초기화돼 **하루에 세 번 다시 만들었다.** 절차의 일부이므로
`tools/mobile_check.py` 로 옮기고 스킬 문서가 그것을 가리키게 했다.
시험으로 못박음(파일 있음 · 412 · 요소별 넘침 검사 · 콘솔 오류 검사 ·
스킬이 그 경로를 가리킴). 돌연변이 3종 빨간 불.

곁가지로 내 실수 하나: 시험을 붙일 때 `if __name__ == "__main__":` 을
글자로 찾아 넣었더니 **다른 시험의 설명글 한가운데**에 들어가 파일이
깨졌다(150차-AE·AW 와 같은 병 — 글자로 코드를 찾으면 설명글에도 걸린다).
줄 맨 앞에서 시작하는 것만, 그것도 **마지막 것**을 찾아 다시 넣었다.

## 159차 — 한 번도 수집되지 않은 6종목 (조용한 생존편향)

**결론 한 줄**: 유니버스 401종목 중 **6개가 8일 내내 한 건도 수집되지
않았는데**, 매일 점검 어디에도 그 사실이 뜨지 않았다.

### 어떻게 찾았나

빠진 분기(107개)의 원인을 갈래로 나누다가, 실적이 **0칸**인 종목이
따로 있다는 것을 발견했다. 로봇 기록의 오류 칸을 세 보니:

| 갈래 | 종목 수 |
|---|---|
| `CompanyNotFoundError` (회사를 못 찾음) | **6** |
| `ZeroDivisionError` | 1 |
| 오류 없음 | 394 |

6종목은 ZI·CFLT·DFS·HOLX·X·HES. 로봇 기록 25일치를 되짚으니
**08-22 까지 0건 → 08-23 부터 매일 똑같이 6건**. 149차에 유니버스를
250 → 401 로 넓힌 그날부터다. 새로 들어온 151개 중 6개(4%)가 처음부터
빈 채로 있었다.

### 왜 못 찾나

SEC 가 주는 **티커 → 회사번호(CIK) 표에는 지금 상장된 티커만** 들어
있다. 그래서 ① 티커를 바꾼 회사와 ② 인수·합병으로 상장이 끝난 회사가
표에서 빠진다. 공시 자체는 **회사번호로는 그대로 남아 있다.**

개발 환경에 깔린 edgartools 꾸러미 안의 SEC 파생 자료 두 개를 뒤져
**교차 확인**했다(SEC 접속은 막혀 있으므로 이것이 유일하게 확인 가능한
자료다):

| 확인처 | 결과 |
|---|---|
| `company_tickers.parquet` (10,365행) | ZI 없음 · 이름으로 찾으니 **"ZoomInfo Technologies Inc." cik 0001794515, 지금 티커 GTM** |
| `popular_us_stocks.csv` | **Ticker ZI · Cik 1794515** |
| 두 자료 모두 | Hologic · Hess · United States Steel · Discover · Confluent **없음** |

즉 ZI 는 **티커가 바뀐 것**(ZoomInfo → GTM)이고, 번호는 두 자료가
일치했다. 나머지 5개는 두 자료 어디에도 없어 이 환경에서는 번호를
확인할 방법이 없다.

### 왜 이것이 측정의 문제인가

인수되어 사라진 회사를 조용히 빼고 재면 **살아남은 것만 재는 편향**
(생존편향)이 생긴다. 인수는 대개 웃돈을 얹고 이뤄지므로 결과가 한쪽으로
기운다. 무엇보다 **빠진 줄도 몰랐다**는 것이 가장 나쁘다.

### 고친 것

1. **ZI 는 번호로 연다** — `config.TICKER_CIK = {"ZI": 1794515}`.
   확인된 것만 적었고, 나머지 5개는 **빈칸으로 뒀다**(헌법 1조:
   짐작한 번호를 넣지 않는다).
2. **나머지 5개는 계기로** — `config.TICKER_NAME_HINT` 에 회사 이름을
   적고, 뼈대 조회가 끝내 실패하면 로봇이 그 이름으로 SEC 를 찾아
   **무엇이 나왔는지 기록만** 한다(값은 안 씀). 이름이 틀리면 아무것도
   안 찾아지고 그대로 "없음"으로 남는다 — 틀린 값이 들어갈 길이 없다.
   다음 런의 숫자를 보고 `TICKER_CIK` 에 넣는다(106·157차 규칙).
3. **매일 점검에 "빈 종목" 칸 추가** — 실적이 한 칸도 없는 종목을
   날마다 드러낸다. 세는 것과 **보고하는 것**은 다르다(150차-C).
4. 빠진 분기 칸의 설명을 "원인 미규명"에서 **157차가 확정한 원인**으로
   고쳤다.

### 곁가지로 잰 것 — 빠진 분기 107개의 갈래

| 갈래 | 분기 수 |
|---|---|
| 은행·보험 (XBRL 매출 계열 없음 — 157차 확정) | 38 |
| 그 밖 | 69 |

빠진 자리를 달력으로 세니 **1분기에 몰린다**(2017-Q1 17 · 2018-Q1 15 ·
2019-Q1 8 · 2020-Q1 8 · 2021-Q1 7 · 2022-Q1 6). 1분기 발표 = **결산
분기(4분기) 발표**이므로, 156차가 짚은 "4분기 채움 실패"와 같은 뿌리다.

**새 가설 하나 — 아직 채택하지 않음**: 매출 개념을 종목별로 세 보니
**51종목은 `RevenueFromContractWithCustomerExcludingAssessedTax` 하나만**
있다. 이 이름표는 2018년 새 회계기준(ASC 606)과 함께 생긴 것이라,
그 종목들은 **2018년 이전 XBRL 매출이 통째로 없다**(INTC 실측: 분기
26칸 = 2018년 이후만). 다만 이 갈래가 설명하는 빠진 분기는 **107개 중
6개뿐**이고(INTC·NXPI·DG·ROST·CHWY·LIN), 매출 값 자체의 채움률은
보도자료가 메워 2017년 92% · 2018년 이후 98~99% 다. **작다.**
그래서 뼈대는 건드리지 않고 적어만 둔다 — 짐작으로 뼈대를 고치지
않는다(106차).

### 빈 종목 7개의 갈래 (159차 실측)

| 종목 | 왜 비었나 | 고칠 수 있나 |
|---|---|---|
| ZI | 티커가 GTM 으로 바뀜 | **고쳤다** (번호 1794515) |
| CFLT·DFS·HOLX·X·HES | SEC 티커표에서 사라짐 | 다음 런의 계기를 보고 |
| NVMI | **12개월(연간) 자료만 있고 분기 자료가 0** | 아니오 |

NVMI 는 오류가 하나도 없는데 분기가 0이다. 로봇 기록을 보면 모든
개념이 `(12개월)` 만 있고 `(3개월)` 호출은 아예 없다 — 미국에 상장한
외국 회사라 분기보고서(10-Q)를 내지 않고 연 1회 보고서만 내기 때문이다.
**없는 것은 만들 수 없다.** 유니버스에 남겨 두되 빈 종목으로 보고한다.

## 160차 — 09-01 런 검증: 은행에 대한 156차 결론이 뒤집혔다

09-01 예약 런(head_sha `387f22f` · 12:32 UTC 완료 · 커밋 `af2e404`).

### ① 159차 수리 실효 확인 — ZI 되살아남

| | 어제 | 오늘 |
|---|---|---|
| ZI 조정 EPS | **0칸** (회사 못 찾음) | **25칸** |
| 빈 종목 | 7개 | **6개** |

로봇 기록에 `회사번호로_열었음: 1794515` 가 찍혔고 오류는 사라졌다.
티커가 바뀐 회사를 번호로 여는 길이 실제로 작동한다.

⚠️ 다만 **주가는 여전히 없다**(로그: "주가 없음: ZI …"). 주가는 티커로
받는데 지금 티커가 GTM 이기 때문이다. 실적은 살았지만 측정에는 아직
못 들어간다 — 반쪽만 고친 것이므로 그렇게 적는다.

### ② 사라진 회사 5개 — 이 환경에서는 고칠 수 없는 갈래로 확정

159차 계기가 이름으로 SEC 를 뒤진 결과:

| 티커 | 찾은 이름 | 판단 |
|---|---|---|
| CFLT | (0건) | 못 찾음 |
| HOLX | (0건) | 못 찾음 |
| HES | IHI Corporation · Kose · Disco | **전혀 다른 회사** |
| X | UNITED STATES ANTIMONY CORP · Toyota Tsusho | **전혀 다른 회사** |
| DFS | CHOICEONE FINANCIAL SERVICES · Intercorp | **전혀 다른 회사** |

edgartools 의 회사 검색 색인도 **SEC 의 현재 티커표에서 만들어진다.**
그래서 티커표에 없는 회사는 이름으로도 안 나온다. 엉뚱한 회사가 나온
것이 그 증거다 — 그것을 값으로 삼지 않았으니 아무 일도 일어나지
않았다(설계대로). 짐작으로 번호를 넣지 않고 **"고칠 수 없음"으로
확정**하고, 매일 점검의 "빈 종목" 칸이 계속 드러내게 둔다.

### ③ 157차 은행 계기의 첫 숫자 — 156차 결론이 틀렸다

**156차 결론(틀림)**: "은행은 XBRL 매출 계열이 아예 없다."
**160차 실측(맞음)**: 있다. **이름이 다를** 뿐이다.

| 종목 | 156차가 본 `Revenues` 분기 | 오늘 센 은행 개념 분기(최대) |
|---|---|---|
| TFC | **0** | **55** |
| RF | **0** | **55** |
| SYF | **0** | **48** |
| GS | 0 | 44 |
| JPM | 20 | 55 |
| MTB | 11 | 55 |

401종목 전체에서 개념별로 분기 계열이 있는 종목 수:
`InterestIncomeExpenseNet` 107 · `InterestAndDividendIncomeOperating` 27 ·
`NoninterestIncome` 26 · `InterestIncomeExpenseAfterProvisionForLoanLoss` 17 ·
`RevenuesNetOfInterestExpense` 12.

**교훈**: "없다"가 아니라 **"내가 부른 이름으로는 없다"** 였다. 계기가
없었다면 156차의 틀린 결론이 그대로 굳었을 것이다. 157차에 값을 안 쓰고
세기만 한 것이 옳았다.

### ④ 그래서 왜 은행의 분기가 빠지나 (원인 정정)

뼈대는 `op_income ∪ revenue ∪ gaap_eps` 의 합집합이다(113차). 은행은
앞의 둘이 없고 `gaap_eps` 만 있는데, **EPS 는 4분기를 채우지 않는다**
(비율이라 연간에서 뺄 수 없다 — 창작 금지). 그래서 은행은 **연말 분기
행이 통째로 없고**, 빠진 자리가 1분기(결산 발표)에 몰린다.

은행 개념은 3개월 값이 55개, 연간이 19개 있으므로 `연간 − (1+2+3분기)`
로 4분기를 채울 수 있다. 즉 **되살릴 길이 있다.**

### ⑤ 그런데 아직 넣지 않았다 — 먼저 세는 계기(160차)

뼈대를 바꾸면 **모든 종목의 행이 달라져 판정까지 흔들린다**(106차).
그래서 "넣었다면 분기가 몇 개 늘었을까"를 개념별로 세는 계기를 넣었다
(4분기 채움까지 똑같이 흉내 내므로 실제 수와 같다). 값은 안 쓴다.

넣을 때의 어려움도 미리 적어 둔다 — **어느 개념을 매출로 볼 것인가**가
간단하지 않다. `RevenuesNetOfInterestExpense` 는 은행의 정식 총수익이지만
12종목뿐이고, 나머지는 "순이자수익 + 비이자수익"을 더해야 총수익이 된다.
한 열 안에 정의가 섞이면 분기마다 뜻이 달라진다(주식보상비에서 이미 겪은
병). 그래서 다음 회차의 선택지는 둘이다:

- **(가) 값도 넣는다** — 정의가 하나로 정해지는 종목만.
- **(나) 기간만 쓴다** — 행만 만들고 매출 칸은 비운다. 모델이 실제로
  쓰는 것은 TTM 조정 EPS 이므로 이쪽이 목적에 더 가깝고 안전하다.

지금 세는 숫자를 보고 정한다.

### ⑥ 매일 점검 (09-01)

이상 없음 — 경보 0건. 신호 종목 25 · 조합 신호 15 · 가설 27개 모두 탑재 ·
휴대폰 폭 네 화면 정상(가로 넘침 0 · 콘솔 오류 0).
조정 EPS 일감 1,837 → **1,703칸**. 빠진 분기 107 → **106개**.

### ⑦ v3 들어 **처음으로 가설이 채택됐다** — 그리고 그것이 위험하다

09-01 런에서 **H9_저평가_첫신기록**("첫 신기록 × 발표 전일 종가가 52주
고가 −15% 이하")이 채택 기준을 넘었다. v2 이후 채택 0개가 계속되던
상태가 처음 깨졌다.

| | 값 |
|---|---|
| 신호 | n=217 · 폭등률 **12.4%** · 95% 구간 [8.7, 17.5] |
| 기준선 | n=9,514 · **8.0%** · 95% 구간 [7.5, 8.6] |
| 채택 여부 | 하한 **8.7** > 상한 **8.6** → 완전 분리 (**여유 0.1%p**) |

**그런데 이것을 매수 근거로 읽으면 안 된다.** 세 가지가 그렇게 말한다.

**(가) 날마다 뒤집힌다.** 판정 파일 이력을 되짚었다 — 신호 구간 하한이
날마다 흔들리고 오늘 처음 0.1%p 넘었다:

| 날짜 | 판정 | 하한 vs 상한 |
|---|---|---|
| 08-26 | 미채택 | 8.2 vs 8.6 |
| 08-28 | 미채택 | 8.5 vs 8.6 |
| 08-29 | 미채택 | 8.0 vs 8.6 |
| 08-31 | 미채택 | 8.4 vs 8.6 |
| **09-01** | **채택** | **8.7 vs 8.6** |

하루치 표본 2건(215 → 217)이 만든 차이다. 내일 도로 미채택이 될 수 있다.

**(나) 앞시기에는 우위가 없다.** 시간 분할이 앞 **8.3%**(n=109,
구간 4.4~15.0 — 기준선과 겹침) · 뒤 **16.7%**(n=108). 헌법 4원칙이
정확히 이것을 경고한다: "강세장 구간만의 우위는 믿지 않는다."
2026년 기준선이 45.6%까지 올라간 장세를 생각하면 더욱 그렇다.

**(다) 전체 표본으로는 미채택이다.** 같은 자로 전체(참고) 표본을 재면
신호 [8.5, 17.0] · 기준선 [8.3, 9.4] 로 겹친다.

**판정은 건드리지 않았다.** 문턱은 데이터를 보기 전에 등록한 것이고,
결과를 보고 규칙을 고치면 사전 등록이 무의미해진다(헌법 8번). 대신
**화면이 함께 말하게** 했다.

### ⑧ 그 과정에서 찾은 결함 — 정직화 문구가 죽은 변수였다

계기판(`app.summary_cards_html`)이 `채택설명`("모든 표시는 관찰 —
매수 근거 아님")을 **만들어 놓고 그리지 않고** 있었다. 웹앱에는 있었고
계기판에는 없었다. 만들어 놓고 안 쓰는 칸은 없는 것과 같다(150차-C 와
같은 병). 채택이 처음 생긴 날 이것을 찾은 것이 다행이다.

### ⑨ 넣은 것 (160차)

- `app.adoption_caveats` — 채택된 가설마다 **여유(하한−상한) · 앞/뒤
  시간 분할 · 전체 표본 판정**을 옮겨 적는다. 값은 만들지 않는다.
  주의 문구는 **규칙으로만** 붙인다: 앞시기 구간이 기준선 구간과
  겹치면 "앞시기에는 우위 없음", 전체 표본 판정이 채택이 아니면
  "전체 표본으로는 ○○". 결과를 보고 정한 문턱은 없다.
- 계기판·웹앱 **양쪽**에 표시. 검증 탭에는 "채택은 확정이 아니다 —
  27개를 날마다 다시 재므로 아슬아슬한 것은 뒤집힌다"를 상설로 적었다.
- 매일 점검에 검사 ⑧c "채택 정직화" — 채택이 있는데 화면 자료에 주의가
  없으면 ⛔.
- 죽어 있던 `채택설명` 을 실제로 그리게 고쳤다.
- 시험을 데이터 독립으로 다시 썼다. 예전 시험은 **실제 판정 파일**을 읽어
  "채택이 0개여야 한다"고 못박고 있어서, 오늘 코드가 멀쩡한데도 빨간
  불이 났다. 데이터가 바뀐 것을 코드 결함처럼 알리는 시험이었다.

## H30 사전 등록 (2026-09-01) — 무너진 섹터를 팔아야 하는가

**주인 질문**: "AI 가 오늘도 하락 중인데 비중을 다른 섹터로 옮겨야 하나?"

### 탐색으로 잰 것 (채택 근거 아님 — 헌법 5원칙)

규칙을 값 보기 전에 고정하고 쟀다: 시점은 60거래일 간격 격자 · 가름은
그 시점의 과거 60거래일 초과수익 5분위 · 표적은 이후 60거래일 초과수익.

**섹터 단위** (섹터-시점 680건):

| 분위 | 다음 60일 중앙값 | 이긴 비율 |
|---|---|---|
| 1분위 (가장 약함) | **+2.4%p** | **62.5%** |
| 2~4분위 | −0.3 ~ 0.0 | 약 50% |
| 5분위 (가장 강함) | **−2.2%p** | 41.9% |

시간 분할: 앞시기는 평평(1분위 +1.7 · 5분위 +1.6), **뒤시기(2021-11~)가
훨씬 강하다** — 1분위 +1.8 · 이긴비율 60.3% vs 5분위 **−6.1%p ·
이긴비율 26.5%**.

**종목 단위** (15,130건)도 같은 방향: 1분위 +1.4%p · 5분위 −1.5%p.
다만 1·5분위 **양쪽 다** 폭등률이 높다(16.3% · 15.2%) — 151차의 U자
변동성 착시가 여기서도 보인다. 그래서 폭등률이 아니라 **중앙값·이긴
비율**로 읽어야 한다.

**지금의 AI 하드웨어와 같은 국면** (섹터 60일 상대 −15%p 이하, 32건):
다음 60일 중앙값 **+3.6%p** · 이긴비율 **68.8%**.
사례: 헬스케어 2026-06 −16→**+11** · 통신미디어 2026-06 −29→**+12** ·
소재화학 2024-12 −17→+3 · 소프트웨어 2026-03 −20→−3 · 인터넷 2026-03 −18→−8.

### 등록문 (값을 더 보기 전에 고정)

| 항목 | 내용 |
|---|---|
| 이름 | `H30_약한섹터_역전` |
| 사건 단위 | (섹터, 시점) — 시점은 표본 시작일에서 60거래일씩 **고정 격자** |
| 신호 | 그 시점의 과거 60거래일 섹터 중앙값 초과수익 **≤ −15%p** |
| 표적 | 이후 60거래일 섹터 중앙값 초과수익 **> 0** (시장을 이김) |
| 기준선 | 같은 표본의 **모든** (섹터, 시점) |
| 채택 | 윌슨 95% 하한 > 기준선 상한 · n≥10 · **서로 다른 시점 4개 이상** |
| 등록일 | 2026-09-01 — 이 날 **뒤** 시점만 판정. 위 탐색 표본은 참고로만 |

### 미리 적는 약점 (숨기지 않는다)

1. **문턱 −15%p 는 탐색 표를 보고 골랐다.** 그래서 새 데이터로만 판정한다.
2. **같은 시점의 섹터들은 독립이 아니다** — 같은 장세를 공유하므로 구간이
   실제보다 좁게 나온다. 그래서 "서로 다른 시점 4개 이상"을 함께 걸었다.
3. **표적이 측정 기본형(+20%p 폭등)이 아니다.** 섹터 중앙값은 종목보다
   흔들림이 작아 60일에 +20%p 가 거의 안 나온다. 기준선도 같은 자로
   재므로 비교 자체는 공정하지만, 다른 가설과 숫자를 나란히 놓으면 안 된다.
4. **생존편향** — 유니버스 401종목은 살아남은 회사 위주다.
5. **판정에 오래 걸린다** — 시점이 1년에 4개뿐이라 최소 1년(2027-09경).
6. 격자를 고정했어도 **표본 시작일이 바뀌면** 격자가 통째로 바뀐다.
   `config.HISTORY_START_DATE` 를 건드리면 이 가설의 표본이 무효가 된다.

### 그래서 지금 주인에게 한 답

**섹터를 통째로 옮기는 것은 우리 데이터가 지지하지 않는다.** 오히려
반대 방향이다. 다만 **이것은 탐색이므로 근거로 쓰지 않는다** — H30 이
판정될 때까지는 "옮길 근거도 없고 옮기지 말 근거도 검증되지 않았다"가
정확한 상태다. 154차의 결론(섹터 통째 이동 반대 · 종목 단위 조합으로)은
그대로 유지된다.

## 160차-B — 그물을 촘촘히 할수록 느려지던 구조를 고쳤다

**증상**: 화면 점검 시험 파일 하나가 **17분**이었고, 검사를 더할 때마다
몇 분씩 늘었다. 오늘 검사 두 개(빈 종목·채택 정직화)를 더하자 바로
느려졌다 — 그물을 촘촘히 할수록 느려지므로, 결국 검사를 안 늘리게 된다.

**짐작 전에 계기**(91·98·106차 규칙). 점검 한 번을 토막 내 쟀다:

| 토막 | 시간 |
|---|---|
| snapshot.json 읽기 | 0.4초 |
| dataset.build | 5.0초 |
| completion_events | **34.9초** |
| gauge_gap_rows | **35.2초** |
| 조합 신호(재조립 포함) | **22.7초** |
| recent_completion_rows | 0.0초 |
| **점검 한 번 전체** | **99.8초** (검사 18개) |

즉 100초 중 **93초가 세 계산**이고, 시험은 이 점검을 열여덟 번 부른다.
검사를 하나 더해도 그 세 계산은 그대로이므로, 늘어난 것은 **부르는 횟수**다.

**고침**: 무거운 재료(ds · 완성 사건 · 잣대차이 · 조합 신호)를 한
프로세스 안에서 한 번만 짓는다. 열쇠는 **(경로, 고친 시각, 크기)** —
로봇이 새 수집물을 쓰면 열쇠가 바뀌어 새로 짓는다. 시험이 망가뜨리는
것은 `app.json` 뿐이고 수집물은 그대로라, 캐시가 정확히 맞는 자리다.

| | 전 | 후 |
|---|---|---|
| 점검 첫 번째 | 97.5초 | 97.5초 |
| 점검 두 번째 | 97초 | **0.2초** |
| 화면 점검 시험 파일 | 17분 | **3분 27초** |
| 전체 검증 | 약 25분 | **7분 10초** |

세 번 되풀이해 **결과가 완전히 같음**을 확인했고(⛔ 0건), 시험으로도
못박았다.

**시험을 만들다 저지른 실수도 적어 둔다.** 처음에는 진짜 수집물(28MB)로
캐시를 시험했는데, 그 시험이 캐시를 비우는 바람에 **뒤 시험들이 100초짜리
조립을 세 번 더** 하게 됐다 — 빠르게 하려고 넣은 시험이 도리어 느리게
만들었다. 무거운 조립만 가짜로 바꾸고 **재료 함수 자체는 진짜를 태우는**
방식으로 고쳤다(그래야 돌연변이를 잡는다).

돌연변이 2종 빨간 불:
① 열쇠에서 고친 시각을 빼기 → "파일이 바뀌었는데 옛 재료를 그대로 썼습니다"
② 기억을 아예 안 쓰기 → "같은 파일인데 다시 지었습니다"

검증: 22개 파일 · **747개 통과** (745 → 747).

## 161차 — 빠르게 자라는 회사를 골라 죽이던 덫 (2026-09-01)

**계기**: 주인이 "크리도(CRDO)가 실적발표 후 폭락했어, 델타 하락이야?"라고
물었는데, 화면에서 CRDO 가 2024-12 이후 **줄곧 판단 불가**였다. 정작
가장 빨리 자란 회사가 모델에서 통째로 빠져 있었다.

**처음엔 오진했다.** "파서 결함"이라 보고했으나, 원본 스냅샷에 조정 EPS
1.16 이 그대로 있었고 파서도 제대로 읽었다. 지운 것은 **정제기**
(`dataset._drop_cumulative_values`)였다 — 헌법 6조(신호보다 장치를 먼저
의심)대로 장치를 따라가 원인을 정정한다.

### 왜 산수로는 못 가리나

정제기는 "이 분기 값 ≈ 직전 4분기 합"이면 연간 누적값이 분기 자리에
잘못 들어온 것으로 보고 버렸다(52차·73차 사고를 막던 장치). 그런데
이익이 분기마다 비율 r 로 자라면 직전 4분기 합 = v·(1/r+1/r²+1/r³+1/r⁴)
이고, **r ≈ 1.9~2.2 에서 이 값이 v 의 20% 안**에 들어온다. 즉 이 덫은
"분기마다 두 배씩 자라는 회사"만 골라 죽인다. CRDO 의 0.25·0.52·1.07·
1.16 이 정확히 그 자리였다.

### 고침 — 식 밖에서 온 자로 확인한다

산수만으로는 진짜 분기와 누적값을 가를 수 없으므로, **기간이 태그로
붙어 누적일 수 없는 값**(XBRL 3개월 매출 `revenue_xbrl`)과 견준다.
`_분기행임이_XBRL로_확인됨(row)`:

- 발표 매출이 XBRL 3개월 매출과 **1% 안**이면 그 행은 분기 행이다 → 면제
- 단, GAAP EPS 가 XBRL **연간** EPS 와 같으면 면제하지 않는다
  (실측: "매출은 분기인데 EPS 는 연간값"인 행이 **157개** — NXPI 23·24·25
  Q4 · SWKS 21·22 Q4 등)
- 견줄 XBRL 자가 없으면 **예전처럼 버린다** (헌법 1조 — "없음"이 "틀림"보다 안전)

### 전후 (실행 출력에서 복사)

| | 전 | 후 |
|---|---|---|
| 누적값으로 버린 칸 | 300 | **27** (잔여 27은 XBRL 자 전무 — 확인 불가로 남김) |
| CRDO 조정 EPS 칸 | 12 | **15** |
| TTM 잴 수 있는 발표 | 8,361 | **8,755** (+394, +4.7%) |
| 첫돌파 사건 | 882 | **890** |
| 잣대 칸 | 11,249 | **11,393** |
| 조합 신호(화면) | 15 | **16** (PSX 추가) |

판정 표본이 4.7% 바뀌는 변경이라 **전체 검증 통과 전에는 커밋하지 않았다.**
전체 검증 첫 회차에서 화면 점검 1건이 빨간 불 — 웹앱이 수리 **전** 값으로
지어져 있어 조합 신호 15 vs 16 이 어긋난 것. 재빌드 뒤 0건.

### CRDO 는 그래서 어땠나

마지막 발표 2026-06-01: TTM 3.42 로 **신고점**(델타 "상승")이다. 그러나
TTM 증가율 67.6 → 50.4 → 45.8 → **31.0%**, 분기 EPS 전분기비 59.7 →
**8.4%**, 매출 전분기비 51.9 → **7.4%**, 가이던스 4.70억(+7.6%). 즉
"신고점은 계속 깨지는데 **속도가 꺾인**" 모양이다. 이 모델의 자
(첫 돌파)는 이 모양을 구별하지 못한다 — 가속·감속 표를 화면에 상설로
싣기로 했다(다음 회차).

돌연변이 3종 빨간 불:
① 면제 함수가 항상 True → "확인할 자가 없는데도 전부 살렸습니다"
② 연간 EPS 조건 제거 → 연간행이 True 로 통과
③ `continue` 제거(면제 미적용) → "빠르게 자란 진짜 분기 값이 지워졌습니다"

검증: 22개 파일 · **750개 통과** (747 → 750). 화면 점검 이상 없음
(살펴볼 것 5건은 알려진 미해결). 모바일 412px 밖으로 나간 요소 0.

## 162차 — 성장 속도표를 화면에 상설로 (2026-09-01)

**계기**: 주인 지시 "저런 표를 앞으로의 기준으로 삼아" (CRDO 분석에서 쓴
TTM 증가율·직전 증가율·분기 QoQ·매출 QoQ 표). 161차에서 확인한 것 —
"TTM 신고점(델타 상승)"은 계속 깨지는데 **속도가 꺾인** 회사(CRDO:
TTM 증가 67.6 → 31.0%)를 이 모델의 자(첫 돌파)는 구별하지 못한다.

### 무엇을 넣었나 (판정이 아니다)

`app.py` 순수 함수 세 개 — 계기판·웹앱·점검기가 **같은 함수**를 쓴다.

- `growth_row(ds, ticker)` — 마지막 **연속** 구간(≥6분기)만 써서
  TTM · 신고점 · TTM증가 · 직전TTM증가 · 가속(이번 > 직전) · 분기QoQ ·
  직전분기QoQ · 매출QoQ · 직전매출QoQ · 상대60(발표일까지 60거래일
  SPY 대비). 잣대는 사다리 규칙 그대로(섞지 않음).
  직전 값이 0 이하면 비율은 None(없는 값을 만들지 않음).
- `growth_table_rows(ds)` — 마지막 발표가 기준일에서 **140일**
  (`sm.DELTA_FRESH_DAYS`, 게이지·조합과 같은 기준 — 새 문턱 아님) 안인
  종목만. 실측으로 이 필터가 없으면 UTHR 2022 · PFE 2023 · ZBH 2022 가
  "지금의 감속"으로 섞여 들어왔다(298 → 285종목). 차례: 가속 → 판단불가
  → 감속, 그 안에서 TTM증가 큰 순.
- `growth_sector_rows(ds)` — 묶음별 가속/감속/판단불가 수와 가속 비율.
  가릴 수 있는 종목이 **5개 미만이면 비율 None** (한 종목이 20%p 를 움직임).

웹앱: `app.json["성장속도"]` = {묶음별, 종목들, 기준, 정직화}. 장세 탭에
묶음별 가속 비율 막대 + 가속/감속/가릴 수 없는 종목 표(펼치기), 종목
상세에 그 종목의 성장 속도 한 줄. 정직화 문구: **"사실의 나열입니다 —
가속이 이후 수익을 예측하는지는 아직 재지 않았습니다. 판정도 점수도
아니며 채택 근거로 쓰지 않습니다."**

점검기(`screen_verify.py`) ⑧d "성장 속도표": 웹앱 줄 수와 **가속 종목
집합**을 지금 계산과 맞댄다(150차-C 규칙 — 화면에 칸을 더하면 점검도
더한다). 재료 캐시에 포함(화면 빌더와 같은 조립 — 액면분할 보정 포함).

### 이 표로 본 지금 (2026-09-01 · 09-01 수집분 · web_build 출력에서 복사)

표에 오른 종목 **285개** — 가속 156 · 감속 122 · 판단불가 7. 묶음별 가속
비율(가릴 수 있는 종목 5개 이상):

| 묶음 | 가속/감속 | 비율 |
|---|---|---|
| 반도체 팹 장비·소재 | 11/3 | 78.6% |
| 소비 최종재·유통 | 16/6 | 72.7% |
| 에너지 원자재·유전 서비스 | 10/5 | 66.7% |
| 기기 OEM 반도체 | 11/6 | 64.7% |
| 광고 플랫폼 · 광통신 · 교체주기 IT 하드웨어 · 금리 민감 임대 | 3/2 | 60.0% |
| 산업 설비·자동화·수송 | 19/14 | 57.6% |
| 의료기기 | 7/6 | 53.8% |
| 금융 중개·자본시장 | 8/7 | 53.3% |
| 소비 거래·결제 · 소재·화학 · 예금 은행 · 의약·의료 | — | 50.0% |
| 분산형 에너지·전동화 | 2/3 | 40.0% |
| 좌석·계약 정액 구독 소프트웨어 | 12/22 | **35.3%** |
| 보험 인수 | 4/8 | 33.3% |

작은 묶음(비율 안 적음): 데이터센터 납품 4/0 · 발전 3/0 · 규제 전력 2/0 ·
메모리 2/0(판단불가 1). 주인 질문 "델타가 늘어나는 섹터가 없다는 건가 —
헬스케어인가?"의 답: 헬스케어(의료기기 53.8 · 의약 50.0)는 **중간**이라
답이 아니다. **소프트웨어↓ / 반도체·장비↑ / 소비재·에너지↑.**

- PLTR: TTM 1.20 신고점 · TTM증가 26.7 → 26.3(정속, 감속으로 분류) ·
  매출 QoQ 16.0 → 18.6 가속 · 상대60 −9.6%p.
- **CRDO 는 표에 없다.** 2025-06-02 발표(FY25 Q4, 0.35)가 구멍메움 행이라
  XBRL 자가 없어 빈칸으로 남았고(161차 잔여 27건 부류), 그래서 마지막
  연속 구간이 4분기(0.52·0.67·1.07·1.16)뿐이다. 앞 회차 CRDO 분석표는
  그 빈칸을 손으로 채워 잰 것이었다. 없는 값을 만들지 않으므로 화면은
  CRDO 를 "가릴 수 없음"으로 둔다 — 0.35 를 자로 확인하는 것이 일감.
- **주인 질문 "크리도도 반도체인데 델타 감속이잖아?"** — 맞다. 빈칸을
  0.35 로 가정하고 같은 함수로 재면(화면에는 안 넣음, 가정 계산):
  TTM 증가율 연쇄 65.1 → 67.6 → 50.4 → 45.8 → **31.0** (세 분기 연속
  감속) · 매출 QoQ 51.9 → **7.4** · 분기 QoQ 59.7 → 8.4. 즉 06-01 발표
  시점에 이미 감속이 데이터에 있었다. 같은 묶음(기기 OEM 반도체 17줄)의
  가속 11개는 TXN·ADI·MPWR·AMD·LSCC·SMTC 같은 **낮은 기저에서 올라오는
  구경기 반도체**이고, 감속 6개에 NVDA(33.3→21.1)·MXL(90.0→57.9)·
  SLAB(48.0→31.9)가 든다 — CRDO 가 표에 있었다면 여기 7번째다. **묶음
  비율 64.7% 는 이 갈림을 가린다**: 이 묶음은 AI 데이터센터 실리콘과
  구경기 반도체가 한 통에 담겨 있다(150차 묶음 재편 때 이미 지적한
  "데이터센터 실리콘 5"를 따로 두지 않은 결과). 표를 볼 때 묶음 막대보다
  **종목 줄의 가속/감속**을 보라 — 막대는 분모가 무엇인지에 좌우된다.
- ZETA: 연속 5분기라 표에 없음(24Q4·25Q1 조정 EBITDA 미부착 — 파서 일감).
  BMNR 유니버스에 없음. LLY 가 가속 1위(TTM증가 152.9%, 직전 −38.8% —
  전년 일회성 비용의 기저효과, 표는 그것을 가리지 않는다).

검증(실행 출력에서 복사): 새 시험 8개(test_app 6 · test_web_build 1 ·
test_screen_verify 1) — 돌연변이 6종(신선도 필터 제거 · 가속 부등호
뒤집기 · 0 이하 분모 · 연속 구간 대신 전체 · app.js 성장판 호출 삭제 ·
점검기 검사 무력화) 전부 빨간 불 확인 뒤 원복. 전체 `run_tests.py`
22개 파일 758개 통과 · 0개 실패. `screen_verify.py`: 성장 속도표 웹앱
285줄(가속 156) · 지금 285줄(가속 156) 일치, 이상 없음(살펴볼 것 5건은
알려진 미해결). 412px 실렌더: 네 탭 문서폭 412 · 밖으로 나간 요소 0 ·
콘솔 오류 0; 성장판 details 전부 펼친 뒤에도 412; NVDA 상세에 성장 줄
표시 확인(TTM 증가 +21.1% · 직전 +33.3% → 감속). 기존 시험 하나
(`test_같은_파일이면_재료를_다시_짓지_않는다`)가 새 재료 "성장" 때문에
KeyError 로 깨져 가짜 조립에 성장표도 포함시켰다 — 검사를 하나 더하면
그 시험도 함께 고쳐야 한다는 것을 기억할 것.

⚠️ 이 표는 **탐색**이다. "가속 ∧ 첫돌파"가 기준선을 이기는지는 H31 로
사전 등록한 뒤 **새 데이터로만** 잰다(헌법 2·5조). 주인 응답 대기.

## 163차 — 주인 보유 종목을 재다가 나온 장치 결함 3건 (2026-09-02)

**계기**: 주인이 보유 종목 11개(STRL·CLS·CRDO·MU·SNDK·MXL·ZETA·ICHR·
하이닉스·AMD·LITE·TTMI)의 분석을 청했다. 헌법 6조(신호보다 장치를
먼저 의심)대로 종목 줄을 읽기 전에 그 줄을 만든 장치를 감사했고,
결함 3건이 나왔다. **분석 보고는 이 고침 뒤의 값으로만** 한다.

### 결함 ① STRL 조정 EPS 11.03 — 연간값이 4분기 자리에 (161차 면제의 부작용)

161차는 "발표 매출이 XBRL 3개월 매출과 1% 안이면 그 행은 분기 행"이라
**행 단위**로 누적 검사를 면제했다. 그런데 STRL 26Q1 자리의 행은 매출은
분기 값(8.257억)인데 조정 EPS 는 **연간값 11.03**(직전 3.08 의 3.58배)
이었다. 매출이 분기라는 것이 EPS 도 분기라는 뜻은 아니었다.

- 원인의 원인: 이 행은 **2026-04-08 8-K(지속가능성 보고서)** 와 짝지어져
  있었다. 분기끝(3/31) 8일 뒤 나온 그 8-K 본문에 "fourth quarter 2025"·
  연간 조정 EPS $11.03 이 적혀 있어 파서가 그것을 읽었고, 진짜 발표
  (5/4, 34일 뒤)는 짝이 이미 정해져 조용히 버려졌다.
  (`data/measure/raw/STRL_2026-04-08.txt`: "2026 Sustainability Report")
- 같은 부류 실측(옛 규칙이 살린 195칸): 연간값이 분기 자리에 든 칸이
  ETN 10.45 · AXP 11.85(9개월 누적) · LOW 12.25 · VZ 4.71 · TMO 21.86 ·
  GWW 39.48 · WM 3.97(직전의 **397배**) · NOC 22.25(55배) · APD 12.03 ·
  SHW 22.08 · CVS 5.95 · CI 15.73 · ELV 24.73 · WMT 5.74.

**고침 A — 칸 단위 면제** (`dataset._점프가_XBRL로_뒷받침됨`): 행이
분기 행이어도 **그 칸의 점프**가 식 밖에서 온 분기 자(XBRL 3개월 GAAP
EPS · XBRL 3개월 영업이익)로 뒷받침되어야 면제한다.
  ① GAAP 칸: XBRL 3개월 GAAP EPS 와 값이 같아야 면제
  ② 조정 EPS·EBITDA 칸: 이번/직전 비율이 자의 비율의 **1.5배 이내**
     (자가 둘이면 큰 쪽 — EOG 18Q1 처럼 GAAP 에만 일회성이 붙은 분기를
     살리기 위해)
  ③ 자가 없거나 직전 값 ≤ 0 이면 버린다(없음 > 틀림)

계수 실측: 1.25 면 VRT 24Q4(1.30 vs 1.23)가 죽고, 2.0 이면 CF 22Q4
3.27(3.34 vs 1.61)·NUE 26Q2 4.84 가 살아남는다 → **1.5**.
결과(195칸): 살림 94 · 버림 101. 살아남은 진짜: MU 26Q2 12.20·26Q3
25.11(비율 2.55·2.06 vs GAAP XBRL 2.62·2.04), CRDO 25Q3 0.25, VRT 24Q4
0.99, NVDA 24Q3 GAAP 3.71. 잃는 것(진짜인데 자가 없어 버림 — 기록만):
GE 23Q3 0.82 · MGM 22Q4 1.53 · NET 22Q3 0.06 · TTD 20Q4 3.05 · AMP
21Q3 8.65 · MTSI 22Q4 3.36 · CVNA 25Q1 1.61. 남는 구멍(알고 둠): NEE
18Q1 7.70 — 직전 칸도 같은 연간값이라 비율 1.0 으로 살아남는다.
연속 오염은 비율로 못 잡는다.

**고침 B — 수집 쪽 짝짓기 후순위** (`sec_fundamentals.merge_quarters`,
`config.EARNINGS_LAG_MIN_DAYS = 10`): 분기끝 뒤 10일 미만에 나온 8-K 는
그 분기의 실적 발표일 가능성이 낮으므로(138차 실측 0.5%) 짝짓기에서
**뒤로만** 보낸다 — 버리지 않는다. ORCL 은 분기끝 9일 뒤에 발표하는
회사이고 NVDA 18Q1 도 9일이었다(발표 지연 <10일 169행 검토: lag 0 은
승격 행이라 정상 · lag 1~9 중 진짜 발표 있음). 같은 값은 `dataset.
ANNOUNCE_LAG_MIN` 과 한 자리에서 가져온다. **개발 환경은 SEC 에 못
가므로 이 고침은 다음 로봇 수집 때 STRL 26Q1(5/4 발표, 조정 EPS 2.5
근처)로 확인한다.** 그때까지 STRL 성장 줄은 화면에 **없음**이다 —
누적 그물이 11.03 을 지운 뒤 26Q1 조정 EPS 가 비어 마지막 연속 구간이
1분기(GROWTH_MIN_QUARTERS 6 미달)라서다. 정직한 없음이다.

### 결함 ② TTMI 26Q2 쌍둥이 행

같은 분기(period_label 26 Q2 · 매출 1,004,054,000 · GAAP 0.77)가 두
행 — 뼈대 행(XBRL 자 있음, EBITDA 없음)과 승격 행(조정 EBITDA 1.668억
있음, XBRL 자 없음). 둘이 따로 있으면 TTM 이 한 분기를 두 번 센다.

**고침** (`dataset._merge_same_quarter_twins`): period_label ∧ 매출 ∧ GAAP
EPS 가 같은 두 행은 한 행으로 합친다 — `revenue_xbrl` 있는 쪽을 뼈대로,
빈 칸(`_TWIN_FILL_FIELDS`)만 다른 행에서 채운다. GAAP 이 다르면 쌍둥이가
아니다(다른 분기일 수 있다). 실측 실데이터에서 **1건**(TTMI 26 Q2)만
걸린다 — n=1 이라 규칙은 최소로(열쇠 3개 전부 일치)만 만들었다.

### 결함 ③ LITE GAAP EPS 84.65 — 보도자료에서 주당 금액이 아닌 수를 EPS 로

LITE 26Q4 GAAP 84.65(조정 3.23). 상한 100 아래라 `PER_SHARE_ABS_LIMIT`
을 통과했다. 같은 부류를 전수로 찾았다 — **보도자료에서만 온 GAAP**
(gaap_eps_xbrl 없음)이 XBRL **연간** GAAP 보다 훨씬 큰 칸 **20건**:
BXP 67~115(5건) · PODD 111~361 · RS 157~271 · WST 346 · ALGN 264 ·
RUN 157 · PFE 51 · XYL 53 · MTCH 37 · BALL 13. 상한 100 이 11건을 이미
거르고, 나머지 9건이 남아 있었다.

**고침** (`dataset._보도자료만_있는_GAAP이_연간보다_훨씬_크면_버린다`):
XBRL 3개월 GAAP 이 없고 ∧ |값| > **2.0×**|XBRL 연간 GAAP| ∧ |값| >
**15×**직전 8분기 |GAAP| 중앙값(이력 ≥ 4) → GAAP EPS 만 없음. 다른 칸은
둔다. 계수 실측: 연간×1.5·이력×15 → HAL 17Q4 −0.94 가 잡힘 ⛔ · 연간×2.0
·이력×10 → VECO 23Q4 2.58 이 잡힘 ⛔ · **연간×2.0·이력×15 → 20건 전부
쓰레기, 진짜 0건 ✅**. 살려 둔 진짜 큰 4분기: CIEN 17Q4 7.32 · NOW 19Q4
3.17 · WDAY 24Q4 4.52 · ESTC 26Q4 4.14 · SNDK 26Q4 43.97.
실데이터 노트 9건: PFE 22Q1 51.1 · BXP 21/23/24/25/26 Q1 · MTCH 24Q4 ·
XYL 23/01 · BALL 25Q4.

**남는 구멍(알고 둠)**: LITE 26Q4 84.65 자체는 **안 잡힌다** — XBRL 연간
GAAP 이 −92.96 으로 그것도 이상하다(연간 자가 오염되면 이 규칙은 눈을
잃는다). n=1 로 규칙을 더 만들지 않는다(89차 교훈). LITE 의 잣대는
조정 EPS(3.23)라 성장표·판정에는 영향이 없다. 다음 수집에서 XBRL 이
채워지면 ①의 GAAP 규칙이 값 자체를 견준다.

### 전후 (실행 출력에서 복사 — 같은 스냅샷 09-01 수집분, 같은 함수)

| | 전(HEAD 6dc166d) | 후(163차) |
|---|---|---|
| 누적값으로 버린 칸(노트) | 27 | **161** |
| 쌍둥이 합침 | — | 1 (TTMI 26Q2) |
| 보도자료 GAAP 이상값 없음 처리 | — | 9 |
| 잣대 칸 | 11,393 | **11,301** (−92) |
| 판정 사건 | 10,991 | **10,900** (−91) |
| 첫돌파 사건 | 830 | **816** (−14) |
| STRL 끝 4칸 조정 EPS | 3.48·3.08·**11.03**·5.8 | 3.48·3.08·**없음**·5.8 |

잃은 사건 91개는 대부분 연간값이 든 4분기 자리다 — 그 자리를 채운
TTM 은 어차피 틀린 TTM 이었다.

검증(실행 출력에서 복사): 새 시험 7개 — `test_dataset` 4(쌍둥이 합침 ·
GAAP 다르면 쌍둥이 아님 · 보도자료 GAAP 버림 · 진짜 큰 4분기는 둠) ·
`test_parsing` 1(너무 빠른 8-K 후순위 — STRL 형 · ORCL 형 단독 유지 ·
빠른 진짜 vs 늦은 예비) 외. 돌연변이 4종 빨간 불 확인 뒤 원복: 쌍둥이
합침 호출 제거 · 열쇠에서 GAAP 제외 · 보도자료 GAAP 계수 2.0 → 1000 ·
짝짓기 early 항 제거. 전체 `run_tests.py`: 22개 파일 767개 통과 · 0개
실패 · 종료코드 0 (웹앱 재빌드 뒤 실행; 재빌드 전에는 화면 정합성
점검기 1건이 "웹앱 285줄 · 지금 266줄" 로 빨간 불 — 옛 데이터로 지은
화면이라 재빌드로 해소).

### 보유 종목을 재다가 더 나온 것 — 매출 가이던스 파서 (일감, 안 고침)

ZETA 26Q2 행의 "다음 분기 매출 가이던스"가 4.43억 = **이번 분기 실제
매출**(4.4277억)이다. 원문: "Delivered revenue of $443 million for the
second quarter … exceeding midpoint of **guidance**" — 분기 낱말 + guidance
낱말이 한 문장에 있어 전망 문장으로 읽혔고, 진짜 가이던스("Third Quarter
2026" 제목 줄 **다음 줄**의 "$469 million to $472 million")는 제목과
값이 다른 줄이라 구획 닻(`_QUARTER_DECL_RE`, 같은 줄 안 90자)에 안
걸렸다. 전수: 매출 가이던스 2,267행 중 점(低=高) 515 · 이번 분기 실제
매출과 0.5% 안 295 · 그중 점 **187행**(CLS 25Q1~26Q2 5건 · ICHR 26Q2 ·
ZETA 26Q1·Q2 · COHR·APP·LSCC·ONTO·AVGO·MCHP·NXPI …). 단순 "실제와 같으면
버림" 규칙은 못 쓴다 — NVDA 18Q4 29.0억(실제 29.11억)·19Q4 22.0억(실제
22.05억)은 **진짜** 다음 분기 가이던스가 우연히 0.4% 안이다. 문장 경로에
"결과 문장(delivered·reported·was)" 배제 + 제목 줄 다음 줄로 구획 닻
확장이 필요하다. 가이던스는 채택 신호(H9)·판정·성장표 어디에도 안
들어가므로 판정 표본은 무관하다. **다음 회차 일감.** 이번 보유 종목
보고에서는 가이던스 칸을 쓰지 않았다(SNDK·LITE 처럼 범위로 온 것만 인용).

## 164차 — H31 "가속 ∧ 첫돌파" 사전 등록 (2026-09-02)

**등록 시점에 이 가설의 결과 숫자를 하나도 보지 않았다.** 162차 성장
속도표는 "사실의 나열"로만 화면에 올렸고, 가속이 이후 수익을 예측하는지
잰 적이 없다. 163차 보유 종목 보고에서 이 값을 판단 근거처럼 쓰고 싶은
유혹이 생겼으므로, 쓰기 전에 먼저 등록한다(헌법 2조).

**질문**: 이익 TTM 이 첫 신고점을 깼을 때, 그 증가 **속도가 빨라지는
중**(가속)이면 속도가 느려지는 첫돌파보다 60거래일 안에 SPY 를 20%p
이기는 비율이 높은가?

**정의(새 숫자 없음 — 전부 이미 있는 자)**:
- 표본: 조정 EPS 잣대 사건 전체(`_adj`, H2b 와 같은 표본).
- 가속 = `TTM증가 > 직전TTM증가`. TTM증가 = 이번 발표 TTM 이 한 발표 전
  TTM 보다 몇 % 늘었나, 직전TTM증가 = 그 한 발표 전에는 몇 % 늘었었나.
  **같은 연속 구간 안**의 TTM 세 개가 필요하다(끊긴 분기를 이어붙이면
  한 푼도 안 늘었는데 가속이 나옴 — 사고 7). 비교 대상 TTM 이 0 이하면
  비율의 뜻이 없어 **없음**. 둘 중 하나가 없으면 가속도 없음(판단 불가).
  162차 `app.growth_row` 와 같은 정의다.
- 신호 = 첫돌파(`newhigh_streak == 1`) ∧ 가속 == True.
- 표적·창·기준선·채택 기준: 측정 기본형 고정(60거래일 · SPY+20%p ·
  같은 표본의 모든 발표 · 윌슨 95% 신호 하한 > 기준선 상한 · n≥10).
- 등록일 2026-09-02. **판정은 등록일 뒤 새 발표만.** 등록일 이전
  표본은 "탐색표본(참고)"로 나란히 적되 채택 근거로 쓰지 않는다.
- 대조(참고로 함께 적음, 판정 아님): 첫돌파 ∧ 감속(가속 == False).
  가속이 듣는다면 이 군은 기준선 아래여야 한다.

**미리 적는 약점**: ① 첫돌파는 정의상 TTM 이 직전 정점을 넘은 순간이라
"가속"과 상관이 높을 수 있다 — 신호 n 이 첫돌파 전체에 가까우면 이
가설은 H2b 의 재탕이다. 신호 n / 첫돌파 n 비율을 함께 적는다. ② 가속은
분기 값의 잡음(일회성·계절)에 예민하다(config 1216행 계절 메모). ③
장세 전제(헌법 4조): 앞/뒤 시기 분할을 함께 보고하고, 강세장만의 우위는
믿지 않는다.

**구현 자리**: `measure_engine.earnings_states` 에 `ttm_growth`·
`accel` 칸 추가 → `collect_events` 가 사건에 `가속` 으로 붙임 →
`judge.judge_accel_breakout` → `collect_job` 이 `verdict.json` 에 기록 →
`hypothesis_clock` 에 등록일. 기존 사건 칸은 한 개도 안 바뀐다(표본 보존).

**함께 한 일 — 성장 속도표에 세분 묶음 병기 (162차 ③-a)**: `config.GROUPS`
는 H19~H21 의 등록 장치라 사후에 바꾸지 않는다. 대신 45차 ④ 감도 분석용
세분 분류(`_FINE_OVERRIDE`: 데이터센터 실리콘 = NVDA·AMD·AVGO·MRVL·CRDO ·
사용량 과금 소프트웨어 = SNOW·DDOG·MDB·NET·TWLO)를 성장 속도표 아래에
**확정 묶음과 숫자가 다른 줄만** 따로 싣는다(`web_build._세분만`, 화면은
"세분해 보면(감도 분석, 판정 아님)"). 재빌드 결과 세분 줄 4개:
기기 OEM 반도체 8/5/1 (61.5%) · 좌석·계약 정액 구독 소프트웨어 10/20/0
(33.3%) · 데이터센터 실리콘 3/1/0 (n<6 비율 없음) · 사용량 과금 소프트웨어
1/2/0 (비율 없음). 사실의 나열이며 판정·점수가 아니다.

**검증(실행 출력에서 복사)**:
- 돌연변이 5종 전부 빨간 불 확인 뒤 원복 — ① 가속 `>`→`>=`
  ② 사건의 `가속` 칸 제거 ③ 판정기 등록일 `>`→`>=` ④ 대조군 None
  ⑤ `_세분만` 필터 제거(`return 세분`) → `test_성장속도표는_세분_묶음을_
  다른_줄만_따로_싣는다` 실패 · "웹앱 빌더 검증: 16개 통과, 1개 실패".
- 새 시험 8개(측정 장치 4 · 자동 판정 3 · 웹앱 빌더 1)는 처음에
  `__main__` 스위치 **아래**에 붙어 한 번도 돌지 않았다(개수 32→32,
  51→51 로 발견 — CLAUDE.md 4장의 "12개 시험 사건"과 같은 병). 위로
  옮긴 뒤 36·54·17 확인.
- 전체 `run_tests.py`: 22개 파일 **775개 통과 · 0개 실패 · 종료코드 0**
  (163차 767 → +8).
- `web_build.py` 재빌드: app.json 201,087바이트 · 종목 401개 ·
  `screen_verify.py` "이상 없음 (살펴볼 것 5건 — 알려진 미해결)" ·
  `tools/mobile_check.py` 412px 네 화면 모두 밖으로 나간 요소 0 · 콘솔
  오류 0 · 종료코드 0.
- 로봇 `verdict.json` 에는 다음 수집(등록일 뒤 첫 런)까지 H31 이 없다.
  `screen_verify` 는 마지막 수집 시각 < 등록일이면 "다음 런 대기"로
  처리하므로 ⛔ 가 아니다. **등록 시점에 H31 결과 숫자는 하나도 보지
  않았다** — 탐색표본(참고) 숫자는 커밋 뒤 별도 문단에 적는다.

**탐색표본(참고) — 커밋 `1503b9a` 뒤 처음 봄 (채택 근거 아님, 실행 출력
복사)**: 조정 EPS 잣대 사건 7,736건(등록일 이전). 첫돌파 597건 중
가속 259 · 감속 52 · 판단 불가 286(TTM 세 개가 같은 구간에 없거나 비교
TTM ≤ 0).
- 신호(가속 첫돌파) n=259 · 폭등률 **9.3%** (윌슨 6.3~13.4)
- 기준선 n=7,736 · **9.8%** (9.2~10.5)
- 대조(감속 첫돌파) n=52 · 13.5% (6.7~25.3)
- 시기 분할(중앙 발표일 2022-06-02): 앞 — 신호 n=126 6.3% (3.3~12.0) ·
  대조 n=18 5.6% · 기준선 9.2% / 뒤 — 신호 n=133 12.0% (7.5~18.6) ·
  대조 n=34 17.6% (8.3~33.5) · 기준선 10.5%.

**읽기**: 탐색표본에서 가속 첫돌파는 기준선과 구별되지 않고(9.3 vs 9.8),
대조군인 감속 첫돌파가 오히려 높다(방향이 가설과 반대, 다만 n=52 라
구간이 넓다). 신호 n / 첫돌파 n = 259/597 = 43% 라 H2b 의 재탕은 아니다.
미리 적은 약점 ①은 해당 없음, ②(잡음)는 판단 불가 286건(48%)로 드러남 —
가속을 셀 수 없는 첫돌파가 절반이다. **판정은 여전히 등록일 뒤 새 발표로만
한다**(헌법 5조). 탐색이 이 방향이라 채택 가능성은 낮게 본다.

## 165차 — 매출·EBITDA 가이던스 파서가 지난 분기 실적을 전망으로 읽던 결함 (2026-09-02)

**발단**: 163차 보유 종목 보고에서 ZETA 2026-08-04 발표의 매출 전망이
443 로 붙어 있었다. 실물을 보니 443 은 **막 끝난 2분기 실적**("Delivered
revenue of $443 million for the second quarter … exceeding midpoint of
guidance")이고, 진짜 3분기 전망은 제목 줄 "Third Quarter 2026" 아래
불릿("Increasing revenue guidance to a range of $469 million to $472
million")에 있었다. 헌법 6조(신호보다 장치)대로 파서를 먼저 감사했다.

**원인 2가지**:
1. 분기 전망 구획의 닻(`_QUARTER_DECL_RE`)이 "guidance for the third
   quarter" 같은 **문장형 선언**만 알았다. ZETA 처럼 **제목 줄** 한 줄
   ("Third Quarter 2026")로 구획을 나누는 보도자료는 구획이 열리지 않아,
   그 대신 본문의 "revenue … guidance" 낱말이 든 실적 문장이 잡혔다.
2. 낱말 검사가 "guidance/outlook 이 들어 있으면 전망"으로 봤다. 그런데
   실적 문장이 **전망에 견주어** 자기 실적을 평가하는 일이 흔하다
   ("exceeded the midpoint of our guidance", "above the high end of the
   company's outlook range", "better-than-expected").

**고침(`forward_estimates.py`)**:
- 제목 줄 닻 `_QUARTER_TITLE_RE` 추가 — "Third Quarter 2026"·"Q3 FY2026"
  꼴의 **홑줄**만, 그리고 그 앞 240자 안에 guidance/outlook 이 있을 때만
  구획을 연다(실적 구획의 같은 꼴 제목이 새지 않게). "Full Year …" 제목은
  기존 연간 표지에서 잘린다.
- 과거 실적 문장 배제 `_is_past_result` — "revenue/net sales/adjusted
  EBITDA … was/were/grew/decreased/exceeded/beat/…" 또는 "delivered/
  reported/… revenue" 꼴이면 버린다. 단 **앞을 보는 강한 말**(expect·
  anticipat·forecast·project·"outlook for/is/of"·"guidance of/to/for/
  range"·"to be approximately/between")이 있으면 전망으로 남긴다.
  맨몸 outlook/guidance 는 강한 말에서 뺐고, "than expected"·
  "better-than-expected"·"than anticipated" 도 뺐다(ZG 실물 3건).
- 사이 60자에 소수점("$3.3 billion")을 허용 — 첫 수리판에서 ZG
  2022-02-10 "revenue of $3.3 billion for Q4 well exceeded" 가 소수점 탓에
  빠져나가 3.3B 를 전망으로 읊었다. 전수 비교에서 잡아 고쳤다.

**전수 비교(원문 2,287개, 옛 코드 vs 새 코드, 실행 출력 복사)** — 바뀐
값 28건, 전부 원문으로 확인:

| 종목·발표 | 항목 | 옛 → 새 | 원문 판정 |
|---|---|---|---|
| ZETA 2026-08-04 | 매출 | 443M → **470.5M** | ✓ 3분기 전망 469~472 |
| ZETA 2026-08-04 | EBITDA | 없음 → **115.5M** | ✓ 115.0~116.0 |
| HUBS 2025-05-08 (×2) | 매출 | 없음 → 739M | ✓ 새로 읽음 |
| MTCH 2025-02-04 | 매출 | 없음 → 825M | ✓ 새로 읽음 |
| NTAP 2023-05-31 (×2) | EPS | 없음 → 1.05 | ✓ non-GAAP $1.00~1.10 |
| ZG 2022-02-10 / 05-05 / 08-04 / 11-02 | 매출 | 483M·4.3B·1.0B·483M → 없음 | ✓ 전부 지난 분기 실적("above … outlook range") |
| ZG 2022-05-05 / 08-04 / 11-02 | EBITDA | 220M·164M·130M → 없음 | ✓ 지난 분기 실적 |
| YELP 2026-05-07 / 08-06 (×2씩) | 매출 | 361M·376M → 없음 | ✓ "exceeded the high end of our outlook" — 실적 |
| VTRS 2022-02-28 | 매출 | 4.33B → 없음 | ✓ "Q4 2021 net sales totaled $4.33 billion" |
| NOW 2019-10-23 | 매출 | 9M → 없음 | ✓ "exceeded the mid-point of guidance **by $9 million**" |
| RCL 2026-01-29 | 매출 | 17.9M → 없음 | ✓ 연간 과거값 |
| TMO 2020-07-06 | 매출 | 1.4B → 없음 | ✓ COVID 검사 매출 실적 |
| MCHP 2025-05-08 | 매출 | 970.5M → 없음 | ✓ 지난 분기 실적 (진짜 전망은 표) |
| S 2024-05-30 (×2) | 매출 | 186.4M → 없음 | ✓ 지난 분기 실적 (진짜 전망은 표) |
| CCL 2024-12-20 | EBITDA | 1.2B → 없음 | ✓ "Record Q4 adjusted EBITDA of $1.2 billion" |
| DKNG 2022-05-06 | EBITDA | 50M → 없음 | ✓ 오답 제거 |
| NRG 2024-09-25 | EBITDA | 3.6B → 없음 | ✓ 연간값 제거 |

새로 틀리게 된 건 0건. **틀림 → 없음 22건, 없음 → 맞음 6건, 틀림 → 맞음
2건.** "없음"은 안전하고 "틀림"은 위험하다(헌법 1조).

**남은 한계(기록만)**: 표 형식 전망 — S "Q2FY25 Guidance / Revenue $197
million" 열, MCHP "Net Sales $1.020 to $1.070 billion" 행 — 은 여전히 못
읽는다(없음으로 남음). 문장형 전망만 읽는 것이 이 파서의 경계다.

**검증**:
- `tests/test_parsing.py` 새 시험 4개(`__main__` 위): 제목 줄 닻 ·
  과거 실적 문장 배제 · guidance 문맥 없는 제목은 닫힘 · ZG 실물 4문장.
  117 → **121 통과**.
- 돌연변이 5종 각각 정확히 1개 빨간 불 뒤 원복: `_is_past_result`→False ·
  제목 닻의 guidance 조건 제거 · 제목 닻 제거 · 소수점 허용 제거(ZG 3.3B
  문장 빨간 불) · 하이픈 뒤보기 제거("better-than-expected" 빨간 불).
- 전체 `run_tests.py`: 22개 파일 **779개 통과 · 0개 실패 · 종료코드 0**
  (164차 775 → +4).
- 저장된 스냅샷의 `guid_rev` 값은 로봇이 다음 수집에서 원문을 다시 읽어야
  바뀐다. 다음 런 뒤 확인할 것: ZETA 2026-08-04 매출 전망 470.5M ·
  2026-04-30 의 396(과거값) 갱신 · ZG 2022 네 건 없음.

**같은 회차에 진단만 한 것 — CRDO 2025-06-02 구멍(수집 측, 여기서는 못
고침)**: 스냅샷에는 조정 EPS 0.35 가 있는데 `dataset.build` 의 누적값
오인 규칙이 버린다(직전 4분기 합 0.43 의 20% 안 · 지난 중앙값의 8.8배).
163차 면제는 XBRL 자(`revenue_xbrl`·`gaap_eps_xbrl`·`op_income_xbrl`)가
필요한데 이 행은 **보도자료 전용**(XBRL 칸 전부 없음, `press_matched`
없음). 25Q1 행(2024-07-30)도 같은 상태. 로봇 계기 q4_채움: 연간수 7 ·
분기수 15 · 채움 4 · 이미있음 1 · 앞선 세 분기 부족 2(2020·2021). 원인은
수집 단계에서 CRDO 의 XBRL 분기 행과 보도자료가 결합되지 않은 것이고,
개발 환경에서는 SEC 접속이 안 되어 재현·수리할 수 없다. 결과: CRDO 사건
목록에서 2025-06-02 가 빠지고 연속 구간이 끊겨 2025-09-03 부터 streak 0
으로 다시 시작(2026-06-01 이 첫돌파 1 로 잡힘). 없는 값을 지어 넣지 않고
로봇 측 결함 목록에 남긴다.

## 166차 — 유니버스 6차 확장: IREN·NBIS 사전 등록 (2026-09-02)

**주인 지시**: "네비우스와 아이렌을 추가검토해서 표본에 넣자." AI
데이터센터 **운영자**(GPU 클라우드) 2종목. 이 문단과 `config._V6_NEW` 는
두 회사의 실적·주가 숫자를 **하나도 보기 전에** 커밋한다(헌법 2조). 개발
환경은 SEC·주가가 차단되어 있어 구조적으로도 볼 수 없다. 한 번 넣은
종목은 성적이 나빠도 빼지 않는다(149차 규칙 그대로).

**등록 내용**:
- `TICKERS` 401 → **403**. 업종 표시 전력에너지 · 테마 AI-데이터센터전력 ·
  묶음 "데이터센터 시스템·전력설비 납품"(새 묶음을 만들지 않음 — 과거
  통계가 없는 묶음은 비교가 안 된다). 돈 버는 방식이 "데이터센터를 지어
  GPU 용량을 팔기"라 같은 건설 사이클에 두었다.
- H26 표본(`UNIVERSE_V5_NEW`)에는 넣지 않는다 — 그 목록은 143차에
  못박았다. 두 종목은 기준선·판정 표본에 **새 발표부터** 들어간다.
- `MEASURE_DISCOVERY_TICKERS`(판정 제외 29종목)에도 넣지 않는다 — 탐색에
  쓴 종목이 아니다.

**제출 서식이 달라 로봇을 한 곳 고쳤다** (`sec_fundamentals.fetch_earnings_8k`):
- IREN — 2025-07-01 부터 미국 국내 제출자(10-K·10-Q·8-K, US GAAP 전환).
  그 전은 20-F·6-K 시절이라 8-K 가 없다 → **2025년 8월 이후 분기만**
  모인다. 성장표는 연속 6분기가 필요하므로 2027년 전에는 못 잴 수 있다.
  기준선 사건은 60거래일 결과가 나온 발표부터 들어간다.
- NBIS — 네덜란드 회사, 20-F·**6-K** 만 낸다. 8-K 만 훑는 로봇은 한 건도
  못 찾아 159차 "0칸 종목" 사고와 같은 꼴이 된다. 그래서
  `config.FPI_6K_TICKERS = {"NBIS"}` 를 두고 이 종목만 `form=["8-K","6-K"]`
  로 훑는다. 실적 판별(설명회 자료·표지 검사)은 서식과 무관하게 같다.
  **한계(알고 둠)**: 분기 XBRL(10-Q)이 없어 모든 분기 행이 보도자료 전용이
  된다 → 누적값 면제(163차)는 XBRL 자가 필요하니 큰 점프 분기는 버려질 수
  있다(없음 > 틀림). 6-K 는 배당·계약 공시에도 쓰여 훑을 건수가 많다
  (`MAX_8K_SCAN` 200 안에서 처리). 개발 환경에서는 SEC 접속이 안 되어
  **실제 6-K 수집은 다음 로봇 런의 기록으로만 확인**할 수 있다 — 확인
  항목: robot_log `per_ticker.NBIS.filings_found > 0`, `text_ok`,
  `parsed_ok`, 그리고 IREN 의 8-K 건수(2025-08 이후 5건 안팎 기대).

**검증(실행 출력 복사)**:
- 새 시험 2개: `test_repo_config`(6차 종목이 유니버스·분류표·6-K 표에
  맞게 있는가) · `test_collect_job`(가짜 회사를 물려 NBIS 는
  `["8-K","6-K"]`, IREN 은 `"8-K"` 가 실제로 넘어가는지 — 코드 경로 실행
  증명). 돌연변이: 서식 선택을 `"8-K"` 고정으로 → NBIS 가 `'8-K'` 로
  빨간 불 확인 뒤 원복.
- `test_judge` 의 "새 종목은 맨 뒤" 시험이 6차 꼬리 때문에 빨간 불 →
  6차 꼬리를 먼저 떼고 5차를 검사하도록 고침. 돌연변이(6차를 앞에 끼움)
  로 빨간 불 확인 뒤 원복.
- 전체 `run_tests.py`: 22개 파일 **781개 통과 · 0개 실패 · 종료코드 0**
  (165차 779 → +2).
- `screen_verify.py`: 이상 없음(살펴볼 것 5건 — 알려진 미해결). 두 종목은
  아직 수집물이 없어 화면에 없다. 다음 런 뒤 "빈 종목" 항목에 IREN·NBIS
  가 뜨면 그것이 수집 실패 신호다.

**분석 보고는 이 커밋 뒤에** 웹 검색 요약(출처 2개 교차·산술 검산)으로
따로 한다 — 저장소에는 아직 두 회사의 숫자가 없다.

### 166차-B — 커밋 `1b1dc16` 뒤 본 두 회사의 숫자 (웹 검색 요약 · 산술 검산)

저장소에는 아직 두 회사의 수집물이 없다. 아래는 웹 검색 요약에서 모은
값이며, **네 분기 합 = 연간 발표치** 검산을 통과한 것만 "확인"으로 적는다.
주가는 요약이 말한 날짜의 종가를 그대로 옮겼다(개발 환경은 주가를 못 받음).

**IREN (회계연도 6월 말)** — 검산 ✅: 매출 240.3+184.7+144.8+137.2 =
**707.0**(FY26 발표치와 일치) · 조정 EBITDA 91.7+75.3+59.5+19.2 = **245.7**
(일치).

| 분기(끝) | 매출 | 조정 EBITDA | 순이익 | AI 클라우드 매출 |
|---|---|---|---|---|
| 25-09 | 240.3 | 91.7 | 384.6 | 7.3 |
| 25-12 | 184.7 | 75.3 | −155.4 | (Q2+Q3 합 51.0 — 도출) |
| 26-03 | 144.8 | 59.5 | (−247.8 도출) | |
| 26-06 | 137.2 | 19.2 | −684.0 (채굴기 손상 450.4 포함) | 70.5 (매출의 51%) |

읽기: 잣대는 조정 EBITDA 가 될 것이고(조정 EPS 미발표), TTM 245.7 은 FY25
269.7 보다 **낮다 → 신고점 아님**, 분기 EBITDA 는 4분기 연속 감소. 비트코인
채굴 → AI 클라우드 전환기라 매출·EBITDA 가 줄면서 AI 매출만 는다. 회사가
말한 것: 2026년 12월까지 계약 ARR 40억 달러(FY26 용량 매진), FY27 자본지출
250~300억 달러(한 요약은 420억까지라 함 — 불일치, 확인 못함), 조달 약 190억
(마이크로소프트 97억 달러 5년 계약 선급 19억·GPU 금융 36억 <6% 등). 주가:
8월 말 35.36(실적 뒤 −12.8%), 6월 고점 63.17 → 약 −44%.

**NBIS (12월 결산, US GAAP)** — 검산 ✅: Q2/Q1 = 582.3/399.0 = +46%(요약과
일치) · EBITDA 마진 236/582.3 = 40.5%(요약 41%) · ARR 3.0/1.9 = +58%(일치)
· 전년 Q2 매출 582.3/5.54 = 105(454% 성장과 일치).

| 분기 | 매출 | QoQ | 조정 EBITDA | ARR(분기 말) |
|---|---|---|---|---|
| 25-Q3 | 146 | | (확인 못함) | |
| 25-Q4 | 227.7 | +56% | 15 | 12.5억 |
| 26-Q1 | 399.0 | +75% | 129.5 | 19억 |
| 26-Q2 | 582.3 | +46% | 236 | 30억 |

읽기: 잣대는 조정 EBITDA(조정 순손실 −0.12/주, GAAP −0.68/주). TTM 조정
EBITDA 는 최근 4분기 모두 양수 전환 뒤 **첫 신고점 구간**에 해당(25-Q3 값을
못 확인해 수치는 적지 않음). 분기 QoQ 는 +56→+75→+46% 로 비율은 꺾였으나
증가 금액은 +82→+171→+183(백만) 으로 커지는 중. 2026 가이던스 매출
30~34억: 상반기 9.81억 → 하반기 20.2~24.2억, 분기 평균 10~12억이 필요
(Q2 5.82억에서 분기마다 약 +40~45%). 연말 ARR 70~90억 목표(6월 30억 대비
2.3~3배), 자본지출 200~250억. 주가: 8/26 종가 213.93, 사상 최고 종가
286.69(6/18) → −25%, 8/12 실적 뒤 하루 +34%.

**두 회사 공통 한계**: 이 모델의 자(TTM 첫 신고점·가속)는 연속 6분기가
필요하다. IREN 은 8-K 가 2025-08 부터라 2027년 전에 성장표에 못 오르고,
NBIS 는 6-K 수집이 실제로 되는지 다음 런 기록으로 확인해야 한다. 둘 다
"이익"이 아직 자본지출과 감가상각 앞에 있는 회사라 **조정 EBITDA 잣대의
첫 신고점은 신호가 아니라 사실**이며, 그 신호의 과거 적중률(H7 EBITDA
첫돌파: 판정 미채택)을 함께 봐야 한다.

## 167차 — 웹앱 "내 포트폴리오" 탭 + 일일 원장 (2026-09-02)

**주인 지시**: "웹앱에 내 포트폴리오 넣어서 데일리로 계속 비교해가면서
비중이동을 판단하는 로직을 모델링에 추가" → "진행하자".

**지은 것** (판정·점수·추천 아님 — 사실의 나열):
- `config.PORTFOLIO` — 보유 11종목(STRL·CLS·CRDO·MU·SNDK·MXL·ZETA·ICHR·
  AMD·LITE·TTMI). 사실이므로 사전 등록 대상이 아니다. 하이닉스는 SEC 자료가
  없어 못 넣는다.
- `app.ticker_fact_row` — 한 종목의 사실 한 줄. **전부 이미 있는 장치**에서
  옮긴다: 성장표(가속·TTM 증가·분기·매출 QoQ) · 신고점 연속(earnings_states)
  · 가격 위치(1년 최고 종가 대비 · 주봉 52주선 대비) · 마지막 발표일부터
  SPY 대비 초과수익 · 두 관찰판 등재(완성 30%+ / H29 조합) · 회사
  가이던스(스냅샷 칸) · 야후 컨센서스 · 다음 발표 짐작(작년 같은 분기
  발표일 + 364일, ±25일 안에 있을 때만).
- `app.portfolio_rows` / `app.swap_candidate_rows` — 보유 종목과 **교체
  후보**(두 관찰판에 동시에 오른 비보유 종목)가 **같은 열**을 갖는다.
  후보라는 말은 "두 판에 같이 있다"는 사실이고, 두 판의 가설(H18·H29)은
  등록 뒤 새 표본이 없어 판정 불가임을 화면이 함께 적는다.
- `web_build.append_portfolio_ledger` — `data/measure/portfolio_ledger.json`
  **추가 전용** 일일 원장. 열쇠는 기준일. 같은 날은 두 번 쓰지 않고 과거
  날은 고치지 않는다("그날 화면이 무엇을 말했나"를 소급 수정 없이 되짚기
  위해 — 컨센서스 원장과 같은 원칙). 로봇이 매일 `web_build.py` 를 돌리며
  `data/measure` 를 커밋하므로 원장은 자동으로 쌓인다.
- 웹앱 다섯째 탭 `#/port`(💼 내포트). 카드는 말줄임(…) 대신 줄바꿈 —
  첫 판은 `.row .meta` 의 nowrap 때문에 "고점 대비 −53% · 52주선 −4% ·
  발표 후 SPY 대…"로 잘렸다(412px 실제 렌더링에서 발견). `.row.port` 로
  줄바꿈하게 고친 뒤 재확인.

**두 가지 "없음 > 틀림" 규칙**:
1. 컨센서스 칸은 야후 칸의 `year_ago` 가 우리 표의 **4분기 전 조정 EPS**
   와 2% 안에서 같을 때만 다음 분기로 믿는다(165차 LITE 실물: 0q 가 이미
   발표된 분기를 가리켰다). 어느 칸도 안 맞으면 "분기 정렬 확인 불가".
   실측: LITE·ZETA 두 종목이 확인 불가로 빠짐.
2. 가이던스 매출이 이번 분기 실제 매출과 0.5% 안이면 없음 — 165차 수리 전
   파서가 남긴 "가이던스 = 실제 매출" 칸(CLS·ZETA·ICHR 실물)이 "+0%"로
   보이는 것을 막는다. 로봇이 원문을 다시 읽으면 칸 자체가 바로잡힌다.

**검증(실행 출력 복사)**:
- 새 시험 6개: `test_repo_config` 1(보유 목록 ⊆ 유니버스·중복 없음) ·
  `test_app` 4(보유·후보 같은 열, 후보는 교집합만 · 컨센 정렬 규칙 ·
  다음 발표 짐작 · 가이던스 되읊음 없음) · `test_web_build` 1(app.json 에
  실림, 자료 없으면 빈 줄·빠진종목 명시, 원장 추가 전용).
- 돌연변이 3종 각각 빨간 불 뒤 원복: 교집합→합집합 · 컨센 허용 2%→100% ·
  원장 "같은 날 안 고침" 제거("과거 날을 고쳤습니다").
- 전체 `run_tests.py`: 22개 파일 **787개 통과 · 0개 실패 · 종료코드 0**
  (166차 781 → +6).
- `web_build.py`: app.json 211,255바이트 · 원장 1일치(기준일 2026-08-31) ·
  `tools/mobile_check.py` 다섯 탭 모두 문서폭 412 · 밖으로 나간 요소 0 ·
  콘솔 오류 0 · `screen_verify.py` 이상 없음(살펴볼 것 5건 — 알려진 미해결).
- 첫 화면 실측(2026-08-31 기준): 보유 11종목 전부 실림 · 교체 후보 6
  (OKTA·CRWD·FTNT·ILMN·CRL·S — ZETA 는 보유라 제외). 보유 중 두 판 등재:
  ZETA(둘 다) · LITE·CRDO(완성 30%+ 만). CRDO 다음 발표 짐작 2026-09-02.

**아직 안 한 것 — 비중이동 "판단" 로직**: 판단은 헌법 2·3조대로 가설로
사전 등록해 새 표본으로 판정한 뒤에만 화면에 올린다. 제안 문안(H32,
등록 전 — 문턱을 주인과 정한 뒤 등록):
  신호 = 보유 종목이 (감속 ∧ 발표 후 60거래일 SPY 대비 ≤ −10%p) 이고
  같은 날 교체 후보(두 판 교집합)가 1개 이상 있을 때, "보유 → 후보(동일
  가중)" 교체. 표적 = 교체 뒤 60거래일 SPY 대비 초과수익. 기준선 =
  교체하지 않고 보유 유지. 채택 = 신호 윌슨 하한 > 기준선 상한, n≥10.
  약점 미리 적음: 후보 판 자체가 판정 불가(H18·H29) · 문턱(−10%p·60일)은
  아직 표를 안 봤지만 기존 장치의 수를 그대로 쓴 것 · 표본이 보유 11종목
  에서만 나와 매우 느리다(유니버스 전체로 일반화한 판을 따로 두어야 함).

## 168차 — H32·H32b 사전 등록: "달린 뒤 감속"은 줄여야 하는가 (2026-09-02)

**주인 지시**: "웹앱에 내 포트폴리오 넣어서 … 비중이동을 판단하는 로직을
모델링에 추가" → "전부 진행해". 167차에 포트폴리오 **사실표**를 지었고,
이번에 그 판단 부분을 헌법대로 **가설**로 등록한다. 이 문단과 `judge.py`
의 상수는 두 가설의 결과 숫자를 **보기 전에** 커밋한다(헌법 2조). 탐색표본
숫자는 커밋 뒤 별도 문단에 적는다(164차와 같은 순서).

**왜 유니버스 전체인가**: 보유 11종목만으로는 신호가 1년에 몇 건이라
n≥10 까지 여러 해가 걸린다. "달린 뒤 감속한 발표"는 어느 종목에서나
같은 뜻이므로 유니버스 전체의 조정 EPS 발표 사건으로 잰다. 포트폴리오
탭은 이 가설이 채택되면 그때 보유 종목에 그 사실을 표시한다.

**정의 (새 숫자 없음)**:
- 표본(기준선) = 조정 EPS 잣대 발표 사건 중 **런업**(발표 전 60거래일
  SPY 대비 초과수익, H25 장치)을 잴 수 있는 것 전부. 가속 판단이 안 되는
  사건(TTM 세 개가 같은 구간에 없음)은 신호가 못 되지만 기준선에는 남는다.
- **H32 회피 신호** = 감속(이번 TTM 증가율 ≤ 직전) ∧ 런업 ≥ **+20%p**
  (H25_RUNUP_MIN 그대로). 질문: 60거래일 뒤 SPY 를 20%p 이기는 비율이
  기준선보다 **낮은가**.
- **H32b 유지 신호** = 가속 ∧ 런업 ≥ +20%p. 질문: 기준선보다 높은가.
- 표적·창: 측정 기본형(60거래일 · SPY+20%p) 그대로.

**채택 기준**: H32 는 회피 가설이므로 거울 기준 — 신호 윌슨 95% 구간
**상한 < 기준선 하한**, n≥10 → "회피 채택". H32b 는 기존 기준(하한 >
상한). 폭락률(SPY 대비 −20%p 이하)도 함께 적되 판정에는 쓰지 않는다 —
회피 신호가 "안 오르는" 것인지 "무너지는" 것인지 읽기 위한 사실이다.

**채택되면 무엇을 하나**: 포트폴리오 탭에 보유 종목의 마지막 발표가 H32
신호였는지를 사실로 표시하고, 그 신호의 실측 폭등률·폭락률·기준선을 함께
적는다. 그래도 "팔라"는 말은 화면에 쓰지 않는다 — 비율의 차이를 보여 줄
뿐이다. 채택 전에는 어디에도 쓰지 않는다(헌법 3조).

**미리 적는 약점**: ① 런업이 큰 종목은 변동성이 커서 폭등과 폭락이 같이
높아지는 U자(151차 착시)가 나올 수 있다 — 폭락률을 같이 적는 이유.
② 가속/감속 판단 불가가 표본의 절반 가까이(164차 48%)라 신호가 느리게
쌓인다. ③ "감속"은 TTM 증가율의 부호가 아니라 크기의 비교라, 증가율이
+90%→+58%(MXL 형)도 감속이고 +5%→+3%도 감속이다 — 정도를 가르지 않는다.
④ 런업 문턱 20%p 는 H25 에서 가져온 것이지 이 질문에 맞춰 잰 것이 아니다.

**장치**: `judge.judge_decel_runup` · `collect_job.run` 배선 ·
`model_verify.expected_hypotheses` · 시계 `hypothesis_clock` · 점검기와
웹앱이 "회피 채택"을 알 수 없는 판정으로 세지 않게 고침.

### 168차-B — 커밋 `9e9491c` 뒤 처음 본 H32·H32b 탐색표본 (채택 근거 아님, 실행 출력 복사)

조정 EPS 사건 중 런업을 잴 수 있는 것 7,664건(등록일 이전). 런업 ≥ 20%p
813건 — 그중 감속 247 · 가속 250 · 판단 불가 316.

| | n | 폭등률 (윌슨 95%) | 폭락률 |
|---|---|---|---|
| 기준선(전체) | 7,664 | 9.8% (9.2~10.5) | 7.2% |
| **H32 감속 ∧ 런업** | 247 | **19.0%** (14.6~24.4) | **15.0%** |
| H32b 가속 ∧ 런업 | 250 | 16.0% (12.0~21.1) | 11.6% |
| 안 달린 감속 (런업 < 20) | 2,365 | 9.6% | — |
| 안 달린 가속 | 1,877 | 7.4% | — |
| 앞시기(중앙 2020-05-06) 감속런업 / 가속런업 | 103 / 112 | 13.6% / 13.4% | — |
| 뒤시기(중앙 2024-07-25) 감속런업 / 가속런업 | 144 / 138 | 22.9% / 18.1% | — |

**읽기 — 등록 전 짐작이 틀렸다.** "달린 뒤 감속한 발표는 줄여야 한다"는
회피 가설인데, 탐색표본에서는 그 발표의 폭등률이 기준선의 **2배**(19.0
vs 9.8)다. 회피 채택(신호 상한 < 기준선 하한)과는 정반대 방향이다.
동시에 폭락률도 2배(15.0 vs 7.2) — 등록문에 미리 적은 약점 ①(변동성
U자)이 그대로 나타났다. 가속/감속을 갈라도 차이가 없다(19.0 vs 16.0,
구간 겹침; 앞시기 13.6 vs 13.4). 즉 결과를 가르는 것은 **런업(이미 달렸는가)**
이고, 이익 속도의 방향은 60거래일 안에서는 구별력이 없다. 안 달린 쪽은
감속(9.6)이 가속(7.4)보다 오히려 높다.

**결론**: ① H32 는 등록대로 새 표본으로만 판정하지만, 탐색 방향으로는
회피 채택 가능성이 매우 낮다. ② 포트폴리오 탭의 "H32 회피신호" 표시는
그대로 두되, 화면 문구를 "판정 대기"로만 적고 줄이라는 뜻으로 읽히지
않게 한다(이미 그렇게 되어 있음). ③ "달린 종목은 오르든 무너지든 크게
움직인다"가 이 표의 단단한 사실이며, 비중 판단에 쓸 수 있는 것은 방향이
아니라 **크기(변동성)** 다 — 이것은 새 가설로 따로 등록해야 한다.
④ 이 결과는 163·165차 보고에서 "감속 → 축소 검토"로 적은 CRDO·MXL·ICHR
논리의 근거가 60거래일 폭등률로는 없다는 뜻이다. 그 보고의 논리는 실적
사실이었고, 그것이 수익을 예측한다는 증거는 지금도 없다. 그렇게 고쳐
읽어야 한다.

지금 H32 회피신호에 해당하는 보유 종목: **MXL**(런업 +73%p ∧ 감속).
H32b 유지신호: MU(런업 +178%p ∧ 가속) · AMD(+21%p ∧ 가속). 교체 후보 중
H32 해당: FTNT·ILMN·S.

### 168차-C — 주인 질문 "소프트웨어가 작년 AI처럼 폭발하는 것 아닌가" (사실만)

두 관찰판의 교집합 6개 중 4개(OKTA·CRWD·FTNT·S)가 소프트웨어다. 같은
자(가속비율 · TTM 증가 중앙값, 그 시점까지의 발표만)로 AI 테마 60종목과
구독 소프트웨어 묶음을 반년마다 되짚었다:

| 시점 | AI 60종목 가속비율 · TTM 중앙값 | 구독 SW 묶음 가속비율 · TTM 중앙값 |
|---|---|---|
| 2024-09 | 34% · +3.7% | 26% · +5.2% |
| 2025-03 | 54% · +6.8% | 42% · +5.1% |
| 2025-09 | 48% · +4.0% | 46% · +3.0% |
| 2026-03 | 53% · +7.0% | 52% · +5.6% |
| **2026-08-31** | **67% · +14.6%** | **33% · +5.6%** |

AI 는 가속비율 34 → 67%, 이익 증가 중앙값 3.7 → 14.6% 로 **이익이 실제로
가속**한 뒤·중에 주가가 달렸다. 소프트웨어는 3월 52% → 지금 33% 로
가속비율이 **떨어지는 중**이고 이익 증가는 한 자릿수(5.6%)다. 개별로도
OKTA(+4.0%)·DDOG(+8.6)·TENB(+9.9)·CRM(+21.6) 가속 vs CRWD·FTNT·PANW·S·
NET·ZS·NOW·ADBE 감속. 즉 지금 소프트웨어의 완성·이격 30%+ 는 **가격이
먼저 움직인 것**이고 이익 가속은 뒤따르지 않고 있다. 섹터 정배열폭도
소프트웨어 24%(1년 전 같은 시기 21~29%) 로 특별하지 않다. 152차-B: 소프트
웨어 묶음 몰림 신호는 2023-09 이후 6연속 마이너스.

단, 반대 증거도 적는다: 2025년 AI 도 주가(완성 무리, 하드웨어 정배열폭
21~42%)가 먼저 달리고 이익 가속(53 → 67%)은 2026년에 확인됐다. 가격이
이익을 앞서는 일은 있다. 그래서 "폭발하지 않는다"가 아니라 "이익 자료는
아직 그렇게 말하지 않는다"가 정확한 문장이다. 이 질문은 H30(약한 섹터
역전)·H11(섹터 정배열폭)의 판정이 답할 것이며, 둘 다 아직 미채택/판정
불가다.

### 168차-D — H33 "런업 변동 폭" 사전 등록 (2026-09-02, 주인 지시 "H33 문안도 지금 등록")

**출발점(168차-B 사실)**: 발표 전 60거래일 SPY 대비 +20%p 이상 달린 발표는
가속·감속을 가리지 않고 폭등률·폭락률이 둘 다 기준선의 2배였다(감속
19.0/15.0 · 가속 16.0/11.6 vs 9.8/7.2). 방향이 아니라 **폭**이 다르다.

**정의**:
- 표본(기준선) = 런업을 잴 수 있는 조정 EPS 발표 사건 전부.
- 신호 = 런업 ≥ **+20%p** (H25 문턱 그대로, 가속/감속 무관).
- 표적 = 60거래일 SPY 대비 초과수익의 **절대값 ≥ 20%p** = "큰 움직임"
  (폭등 ∪ 폭락). 폭등·폭락도 따로 적는다.
- 채택 = 신호 큰움직임 비율의 윌슨 95% 하한 > 기준선 상한, n≥10.
- 시간 분할(앞/뒤 시기)을 탐색표본에 함께 적는다 — 큰 움직임은 시장
  변동성이 큰 시기에 같이 늘어나므로.

**쓰임(채택되면)**: 방향이 아니라 **비중 크기**에 쓴다. 큰 움직임 확률이
기준선의 k배인 보유 종목은 같은 위험을 지려면 비중을 1/k 로 줄여야
한다는 사실을 포트폴리오 탭에 적는다. "팔라/사라"는 여전히 쓰지 않는다.

**정직하게 적는 것**: ① 이 가설은 168차-B 탐색 표를 **보고** 만들었다 →
탐색표본으로는 판정하지 않고 등록일 뒤 새 발표만 센다(헌법 5조). 탐색표본
의 큰움직임 비율은 커밋 뒤 별도 문단에 적는다. ② 문턱 20%p 둘은 기존
장치의 수를 그대로 쓴 것이지 이 질문에 맞춰 잰 것이 아니다. ③ 신호가
드물지 않다(탐색 813/7,664 ≈ 11%)라 n≥10 은 한두 분기면 찬다.

**장치**: `judge.judge_runup_swing`(`_swing_stats`·`_judge_swing`) ·
`collect_job.run` 배선 · `model_verify` 등록부 · `hypothesis_clock`.
시험 2개(폭등·폭락이 같이 세는지 · 배선), 돌연변이 1종(절대값 제거 →
폭락이 안 세어져 빨간 불) 확인 뒤 원복.
