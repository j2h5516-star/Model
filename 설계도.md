# 설계도.md — v3 뼈대 설계도 (재건축 3단계)

작성: 2026-08-13, v3 재건축 3단계.
이 문서는 **코드를 쓰기 전에** 그리는 도면입니다. 4~6단계에서 지을
모듈(부품)들의 경계 — 각자 무엇을 하고, 무엇을 절대 하면 안 되는지 —
를 여기서 확정합니다. 헌법(`전략.md`)이 "무엇을 왜"라면,
이 문서는 "어떤 부품으로, 어떤 순서로"입니다.

> 이 설계도와 헌법이 다르게 읽히면 **헌법(`전략.md`)이 우선**입니다.
> 경계를 바꾸고 싶으면 **코드보다 이 문서를 먼저** 고칩니다.

---

## 1. 전체 흐름 — 데이터가 지나가는 길

```
[깃허브 서버 (로봇)]                     [개발 환경 / 배포 환경]

수집 로봇 collect_job.py
  ├ sec_fundamentals.py  (8-K 파서)
  ├ forward_estimates.py (가이던스 파서)
  ├ market_data.py       (주가 수신)
  ├ vendor_feed.py       (두 번째 자 — 야후 분기표, 75차)
  ├ consensus_feed.py    (컨센서스 원장, 관찰용)
  └ measure_store.py     (파일로 꾸리기)
        │
        ▼  커밋
data/measure/snapshot.json  ←─── 국경선. 이 파일이 수집과 판단의 경계
   · vendor.json · consensus.json · robot_log.json 도 같이 커밋
        │
        ▼  읽기 전용
① dataset.py        데이터 계층 (4단계) — 재료 손질
        │              ├→ audit_data.py     재료 오염 조사 (73차)
        │              │   읽기만 한다 · 아무것도 지우지 않는다 ·
        │              │   `부탁_` 원문 목록을 적어 로봇에게 넘긴다
        │              ├→ data_health.py    건강검진 (108차) — 세기만 함
        │              └→ vendor_compare.py 두 자 대조 (75차)
        ▼
② measure_engine.py 측정 장치 (5단계) — 사건과 층별 상태 계산
        │              ├→ sector_model.py  섹터 정배열 폭·완성 (33·39차)
        │              └→ leadership.py    주도섹터 (44차)
        ▼
③ judge.py          자동 판정 (5단계) — 윌슨 구간·채택 판정
        │
        ▼  기록
data/measure/verdict.json
        │
        ├──────────────────┐
        ▼  읽기 전용        ▼  읽기 전용
④ app.py            ⑤ web_build.py → docs/data/*.json → docs/app.js
   계기판(streamlit)     웹앱 데이터 빌더 (132차)   휴대폰 화면
   개발 환경용           **app.py 의 순수 함수를 그대로 재사용**한다 —
                        그래야 두 화면이 다른 숫자를 말할 수 없다

[점검·실험 — 판정 경로가 아님]
  model_verify.py  모델 전체 점검기 (128차) — 등록 가설이 판정 파일에
                   전부 실렸는지, 옛 코드로 계산됐는지 확인
  lab.py           주도섹터 장치의 **매개변수판 복제본** — 기본값으로
                   돌리면 leadership.py 와 완전히 일치해야 한다
  exp1.py exp2.py  lab.py 로 창 길이·연속 확인을 바꿔 보는 실험 스크립트
  run_tests.py     시험 전체 실행 (지금 파일 20개 · 580개)
  config.py        종목 목록·수집 설정. 유니버스 401종목

```

> **⚠️ 이 그림은 150차-I 에 실제 파일과 맞췄습니다.** 그 전에는 14개
> 모듈이 빠져 있어, 새 세션이 시스템을 절반만 보게 돼 있었습니다.
> 파일을 새로 지으면 **여기에도 적으세요** — 안 적으면 다음 사람은
> 그 부품이 없는 줄 압니다.

핵심 원칙 (헌법 4장): **수집과 판단을 분리한다.**
모듈 사이의 통행증은 두 가지뿐입니다 — **파일**(snapshot.json,
verdict.json) 아니면 **정해진 함수 하나**. 옆 모듈의 내부에
손을 넣지 않습니다.

---

## 2. 부품별 정의

### 이미 있는 부품 (v2에서 살아남은 수집 계층 — 고치지 않음)

| 부품 | 하는 일 | 3단계 결정 |
|---|---|---|
| `collect_job.py` | 로봇 몸통. 수집만 한다 | 유지. 5단계에서 `judge.py` 호출 한 줄만 추가 예정 |
| `sec_fundamentals.py` | 8-K에서 조정 EPS·EBITDA 추출 | 유지 |
| `forward_estimates.py` | 회사 가이던스만 추출 | 유지 |
| `market_data.py` | 일봉 주가 수신 (로봇 환경에서만 동작) | 유지 |
| `data_quality.py` | 값 검사 — 이상한 값은 고치지 않고 없앤다 | 유지. 수집 계층(영업이익 중심)의 검사 — 아래 ① 참조 |
| `measure_store.py` | 수집 결과를 snapshot.json으로 꾸림 | **유지 확정** (4단계) — 아래 상자 참조 |

> **`measure_store.py`의 운명 (0단계에서 미뤄 둔 결정 — 4단계에서 확정)**
> 이 파일은 **로봇의 쓰기 파이프**로 유지합니다.
> 쓰기(로봇 쪽)와 읽기(측정 쪽)를 한 파일에 합치면 경계가 무너지기
> 때문에, 읽기는 `dataset.py`가 맡습니다. 즉:
> **snapshot.json에 쓰는 것은 measure_store, 읽는 것은 dataset** —
> 파일이 국경이고 두 부품은 서로를 모릅니다.
> (`dataset.py`를 실제로 지어 본 4단계에서 이대로 확정했습니다.)

### ① `dataset.py` — 데이터 계층 (4단계에서 지음 — ✅ 완료)

- **하는 일**: 재료 손질. `snapshot.json`을 읽어
  ⑴ 자체 값 검사를 통과한 값만 통행시키고 (숫자 여부 · 날짜 형식 ·
  주당 금액 절대 상한. `data_quality.py`의 분기 검사는 영업이익
  중심이라 — 영업이익 없는 분기를 통째로 버림 — 조정 EPS만 있는
  8-K 행까지 잘못 버리므로 여기서는 쓰지 않고, "고치지 않고
  버린다"는 원칙만 물려받습니다. 4단계에서 확정)
  ⑵ 시간순으로 정렬하고
  ⑶ 측정이 쓰기 좋은 형태의 분기 표·가격 표로 정돈합니다.
  버린 것은 전부 `notes`에 남깁니다 — 조용히 버리지 않습니다.
- **입력**: `data/measure/snapshot.json` — **읽기 전용**.
- **출력**: 파이썬 안에서 쓰는 정돈된 표. **파일로 저장하지 않습니다** —
  진실은 snapshot 하나여야 하고, 손질본을 파일로 남기면
  "어느 쪽이 진짜인가" 헷갈리는 사고의 씨앗이 됩니다.
- **절대 하면 안 되는 일**:
  - 값을 만들거나 고치기 (창작 금지 — 탈락한 값은 버리고 "없음"으로)
  - TTM·신고점 같은 **파생 계산** (그건 측정 장치의 일 — 계산 규칙이
    두 곳에 살면 사고 6·7이 재발합니다)
  - 인터넷 접속 (수집은 로봇만)

### ② `measure_engine.py` — 측정 장치 (5단계에서 지음 — ✅ 완료)

- **하는 일**: 헌법 0장의 질문을 층별로 잽니다.
  - TTM 계산 — **진짜 연속 4분기 합만** (사고 7: 가짜 TTM)
  - 신고점·**첫** 돌파 판정 — 이력 끊김 처리 포함 (사고 6: 신고점 망각)
  - 실적 폭 게이지 (위층) — 재보정 정의는 **사전 등록 후에만**
  - 사건 만들기 — 발표일 기준. 이익 숫자를 실은 발표가 항상 이김 (사고 8)
  - 수익률 창 — 발표 다음 거래일 종가부터 60거래일, SPY 대비 +20%p = 폭등
- **입력**: `dataset.py`의 정돈 표만. **출력**: 사건 목록 + 층별 상태.
- **절대 하면 안 되는 일**:
  - 채택/미채택 **판정** (그건 자동 판정의 일 — 재는 자와 판정하는
    심판을 분리해야 "결과 보고 규칙 고치기"가 물리적으로 어려워집니다)
  - `snapshot.json` 직접 읽기 (재료는 반드시 데이터 계층을 거쳐서)
  - 등록 안 된 문턱·창·표적 사용 (측정 기본형은 헌법 2장 고정값)
- **짓는 조건**: **사전 등록이 먼저.** `측정결과.md`에 등록을 적어
  머지하기 전에는 이 부품의 코드를 쓰지 않습니다.

### ③ `judge.py` — 자동 판정 (5단계에서 지음 — ✅ 완료)

- **하는 일**: 측정 장치의 결과에 등록된 채택 기준을 적용합니다.
  - 윌슨 95% 신뢰구간 계산
  - 채택 판정: 신호 하한 > 기준선 상한 (완전 분리), n≥10
  - 앞/뒤 **시간 분할을 항상 병기** (강세장 착시 방지 — 사고 11)
  - 결과를 `data/measure/verdict.json`에 기록
- **누가 부르나**: 로봇이 매일, **수집이 성공한 날만** 호출합니다.
  사람 손은 없습니다 (헌법 2장 제3조: 자동 축적·재판정).
- **절대 하면 안 되는 일**:
  - 측정 규칙 소유 (재는 법은 측정 장치가, 판정 기준은 등록 문서가 소유)
  - 기준 미달인데 "아깝다"고 반올림 (미채택은 미채택으로 기록)
- 지금 `data/measure/verdict.json`에 있는 것은 **v2 유물**입니다
  (2026-08-12 계산). 5단계에서 새 장치가 첫 실행 때 대체합니다.

### ④ `app.py` — 계기판 (6단계에서 지음 — ✅ 완료, streamlit)

- **하는 일**: `verdict.json`과 측정 결과를 **읽어서 그대로** 보여 줍니다.
  - 사실(델타·신고점·게이지 상태) + 그 상태의 **과거 실측 폭등률·
    기준선·판정 상태**를 항상 나란히 (정직화 — 헌법 2장 제3조)
  - "채택된 신호 없음"이면 그대로 그렇게 표시
- **절대 하면 안 되는 일**:
  - 자체 계산·점수·추천 (v1 붕괴의 원인 — 사고 1)
  - 미채택 신호를 판단처럼 보이게 표시 ("가속 = 매수"로 읽히는 요소 금지)
- **화면 검증**: 모바일 폭 412px에서 실제 렌더링으로 확인.
  글자 잘림·열 가림이 없어야 합니다.
- 화면 기술(어떤 도구로 그릴지)은 6단계에서 정합니다 —
  이 설계도는 경계만 확정합니다.

---

## 3. 경계 규칙 한눈표

| 부품 | 읽어도 되는 것 | 쓰는 것 | 대표 금지 |
|---|---|---|---|
| 로봇(수집 계층) | SEC·야후 (깃허브 서버에서만) | `data/measure/*` | 판단·계산 금지 |
| ① dataset | snapshot.json | (파일 없음 — 메모리만) | 창작·파생 계산·인터넷 금지 |
| ② measure_engine | ①의 출력만 | (파일 없음 — 메모리만) | 판정 금지, snapshot 직접 읽기 금지 |
| ③ judge | ②의 출력만 | verdict.json | 측정 규칙 소유 금지 |
| ④ app(계기판) | verdict.json, ①②의 결과 | (파일 없음) | 자체 계산·점수·추천 금지 |

세로 방향(위→아래)으로만 흐르고, 거꾸로 부르지 않습니다.
예: 측정 장치가 로봇을 부르거나, 계기판이 판정을 다시 계산하는 일은 없습니다.

---

## 4. 짓는 순서와 시험 전략

| 순서 | 일 | 단계 |
|---|---|---|
| 1 | `dataset.py` + 시험 — **한 번에 파일 하나** | 4 |
| 2 | `measure_store.py` 운명 최종 확정 (유지 가닥의 재검토) | 4 |
| 3 | `측정결과.md`에 10차 사전 등록 작성·머지 (위층 게이지 재보정 정의 포함) | 5 |
| 4 | `measure_engine.py` + 시험 | 5 |
| 5 | `judge.py` + 시험, 로봇에 호출 한 줄 연결 | 5 |
| 6 | `app.py` 계기판 | 6 |

모든 부품 공통의 시험 규칙 (사고 백서에서 나온 것):

- **새 시험은 일부러 깨뜨려 빨간 불을 본 뒤에만 믿는다** (사고 2)
- 시험 재료는 지어낸 예제가 아니라 **실물** — `data/measure/raw/`의
  파싱 실패 원문 137건과 실제 snapshot이 우선 재료다
- 신고점 망각·가짜 TTM·짝짓기(사고 6·7·8)는 **회귀 시험**으로 남긴다 —
  같은 사고가 새 몸에서 재발하는지 지을 때마다 검사
- 파서·계산의 출력이 **저장까지 도달하는지 배관 전체**를 시험한다 (사고 14)
- 실행은 `python3 run_tests.py; echo $?` — 종료코드 직접 확인,
  고친 직후엔 `__pycache__` 청소

---

## 5. 이 설계도를 고치는 법

- 부품의 경계(하는 일·금지)를 바꾸려면 **이 문서를 먼저** 고치고
  코드를 고칩니다. 코드가 문서와 다르면 고칠 쪽은 코드입니다.
- 부품 이름은 여기서 확정한 것을 씁니다 — 단계마다 이름을 다시
  정하지 않습니다: `dataset.py` · `measure_engine.py` · `judge.py` · `app.py`.
- 문턱·창·표적 같은 **측정 규칙의 값**은 이 문서가 아니라
  `측정결과.md`의 사전 등록이 소유합니다. 여기 적힌 것은 경계뿐입니다.
