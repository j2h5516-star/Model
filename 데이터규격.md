# 데이터규격.md — 값을 **어디서 어떤 자격으로** 가져오는가

주인 지시(2026-08-24): *"찾아오는 기준을 최대한 지정하고 정형화시켜야
서로서로 데이터매칭오류가 안 생길 것 같다."*

맞습니다. 지금까지 각 항목의 수집 규칙은 **코드 여기저기에 흩어져** 있었고,
그래서 같은 분기에 대해 출처가 다른 값이 섞여 들어오는 사고가 반복됐습니다
(GS 매출 12,738달러 · HD 47,861 · AMZN 728 …). 이 문서가 **한 장짜리
계약서**입니다. 코드가 이 문서와 다르면 **코드가 틀린 것**입니다.

> 헌법(`전략.md`)이 위이고, 이 문서는 그 아래 **수집·정제의 실무 규격**입니다.
> 충돌하면 헌법이 이깁니다.

---

## 0. 한 줄 요약

**분기의 신원은 기간종료일 하나로 정한다. 값마다 출처·단위근거·기간을
함께 지닌다. 없으면 "없음"으로 두되 왜 없는지 사유를 남긴다.**

---

## 1. 분기의 신원 — 기간종료일 하나

| 무엇 | 규칙 |
|---|---|
| 분기를 가리키는 열쇠 | **`filing_date` (= 기간종료일, XBRL 구조값)** |
| 분기 이름(`period_label`) | **화면 표시용. 계산에 쓰지 않는다.** |
| 회계분기 번호 | 그 종목의 **분기끝 날짜 순서**로 정한다 (`data_quality.회계분기_순번`) |
| 회계연도 끝 | **XBRL 12개월 값이 있는 날**이 곧 결산일 (150차-AL) |

**근거**: 이름은 보도자료 본문에서 글자로 뽑는데, 본문이 직전 분기·작년
같은 분기를 함께 말하면 엉뚱한 것을 집습니다. 실측 — 이름이 그 종목
안에서조차 어긋난 것이 **916칸 / 194종목**(2026-08-23). SLB 는 '20 Q4'
라는 이름이 붙은 행이 **4개**였습니다.

> ⚠️ **금지**: 인덱스로 "4칸 뒤"를 작년 같은 분기로 삼는 것.
> 분기가 하나만 빠져도 완전히 어긋납니다.

**회계분기 번호가 두 번 바뀐 내력** (달력에 붙이려던 두 번의 실패):

| 언제 | 어떻게 정했나 | 왜 버렸나 |
|---|---|---|
| ~183차-J | 기간종료일의 **월** | 13주 달력은 분기끝이 9/30↔10/1 을 오가 같은 분기가 해마다 다른 번호를 받음 |
| 183차-K | 가장 가까운 **달력 분기끝** | 12·12·12·16주 달력(COST·AZO)은 2월·5월 분기끝이 달력 분기끝의 **딱 중간**이라 네 분기가 세 칸에 뭉침 |
| **183차-X (지금)** | **그 종목 자기 분기끝 날짜 순서** | 달력을 아예 안 봄. 간격을 한 분기 길이(91.3일)로 나눠 반올림 — 12주(84일)도 16주(112일)도 1칸 |

지금 방식의 두 가지 주의점(둘 다 실측으로 배웠습니다):

- 며칠 차이로 **같은 분기가 두 줄** 있으면 **0칸**입니다(WAT 63행 중 20쌍).
  무조건 한 칸씩 밀면 그 뒤가 통째로 어긋납니다.
- 기준 날짜는 **칸이 바뀔 때만** 옮깁니다. 군더더기 줄을 징검다리로
  밟으면 한 분기를 잃습니다(NBIX 11-01 → 11-22 → 01-06).

**분기 이름이 어긋나면 버립니다** (183차-Y). 그 종목의 다수 규칙과
어긋난 이름은 고치지 않고 기간끝 날짜형("23/03")으로 바꿉니다 —
날짜형은 언제나 사실이고 틀린 분기 번호는 위험합니다. 전수 203칸·83종목.
어긋난 방향은 다음 분기 116칸(57%) · 직전 분기 62칸(31%)으로, 보도자료가
함께 말하는 **다음 분기 전망·작년 비교**를 파서가 문 것입니다.

---

## 2. 항목별 출처 우선순위 — 이 표가 전부다

높은 쪽이 이깁니다. 위 칸이 없을 때만 아래로 내려갑니다.

| 항목 | 1순위 | 2순위 | 3순위 | 없으면 |
|---|---|---|---|---|
| `revenue` | XBRL `Revenues` 계열 | 보도자료 합계 줄 | — | 없음 |
| `gaap_eps` | XBRL `EarningsPerShareDiluted` (3개월) | 보도자료 | — | 없음 |
| `op_income`(논갭) | 보도자료 논갭 영업이익 | 조정 EBITDA − 감가상각 | XBRL 영업이익 + 되돌림(근사) | 없음 |
| `adj_eps` | **보도자료만** | — | — | 없음 |
| `adjusted_ebitda` | **보도자료만** | — | — | 없음 |
| `gross_margin_pct` | XBRL 매출총이익 ÷ 매출 | 보도자료 | — | 없음 |
| 가이던스 | **회사가 직접 발표한 것만** | — | — | 없음 |

**조정 EPS·조정 EBITDA 에 XBRL 이 없는 이유**: 논갭 수치는 회계기준이
아니라 XBRL 태그가 없습니다. 그래서 이 둘은 **보도자료가 유일한 출처**이고,
회사가 안 내면 영원히 없음입니다 — 실측으로 조정 EPS 빈칸의 **31.4%**가
이 경우입니다(회사 미발표).

**가이던스는 회사 발표만**: 컨센서스·역산·근사 전부 금지 (헌법 1조).

### 2-b. 논갭 영업이익을 **버리는** 세 가지 자격 (고치지 않고 버린다)

보도자료의 논갭 영업이익 자리에 **분기 영업이익이 아닌 값**이 들어오는
일이 있습니다(연간값·누적값·단위 오독). 아래 셋 중 하나에 걸리면
그 칸을 **없음**으로 만듭니다. XBRL 값으로 바꿔 넣지는 않습니다 —
XBRL 은 GAAP, 보도자료는 논갭이라 한 열에 섞으면 분기마다 뜻이
달라집니다(주식보상비에서 이미 겪은 병).

| 자격 | 언제 버리나 | 회차 | 실물 |
|---|---|---|---|
| 매출이 XBRL 과 같음 | 마진이 이웃보다 20%p↑·2배↑ 이고 매출은 XBRL 과 2% 안쪽 | 183차-Q | ACMR 25 Q4 연간 143.0M |
| **XBRL 영업이익이 다른 말** | 위 이상 + XBRL 값으로 바꾸면 이상이 사라짐 + 두 값 차이 > 매출의 5% | 183차-AB | SEDG 23 Q4 +149.6M ↔ XBRL −202.7M |
| **산수 위반** | 영업이익 > 매출총이익 × 1.10 이고 **XBRL 은 산수를 지킴** | 183차-AC | HPE 25 Q2 2,500M ↔ 매출총이익 2,242M |

- 산수 자격은 **이웃을 보지 않습니다.** 이웃까지 함께 오염되면 이웃
  검사는 눈이 멉니다(KO 2020년 네 분기가 모두 어긋나 서로를 못 가림).
- 여유 10% 는 **지분법이익·기타영업수익**을 영업이익에 넣는 회사 때문
  입니다(ALB 23 Q1 3.3% 초과). 0.5%~10% 사이는 전수 8칸뿐입니다.
- XBRL 도 함께 산수를 어기면 **매출총이익률 쪽**이 의심이므로 손대지
  않습니다(전수 98칸 — 보험사 GL 처럼 '매출총이익'이 제 뜻대로 안 쓰이는
  회사).
### 2-c. 매출은 아직 **버리지 않고 세기만** 합니다 (183차-AR)

논갭 영업이익에는 위 세 자격이 있지만 **매출에는 없습니다.** 매출 쪽
검사는 "가까운 분기의 1/100 미만"(150차-Y) **한 방향뿐**이라, 값이
**너무 큰** 쪽은 아무도 안 보고 있었습니다. 전수로 재니:

| 무엇 | 칸 | 종목 |
|---|---:|---:|
| 4분기(결산) 칸에 **연간 매출**이 앉은 것으로 보임 | **28** | **14** |
| 그중 결산일임이 식 밖에서 확인됨(`gaap_eps_annual_xbrl`) | 16 | 4 |

실물 — **GS 는 아홉 해 내리** 12-31 칸에 그 해 연간 순수익이 들어 있습니다
(2024-12-31 = 53,512백만). **ORCL 은 다섯 해 내리** 5-31(결산일)이 그렇고,
ROST 2024-01-30 = 20,400백만(FY2023 연간) · BA 2021-12-30 = 62,286백만.

> ✅ **조정 EPS 는 멀쩡합니다.** 같은 행의 EPS 는 분기값이 맞습니다
> (ORCL 1.16 · GS 11.95 · TSN 1.81). 그래서 주 잣대(TTM 조정 EPS
> 신기록)는 이 결함의 영향을 **받지 않습니다** — 오염된 것은 매출 열뿐
> 입니다. 매출을 쓰는 곳(성장 속도표·매출총이익률)이 영향을 받습니다.

**왜 아직 안 버리나**: 걸러내는 규칙을 세우려다 **오탐을 찾았기**
때문입니다 — ZG 2022-03-31 의 4,257백만은 **진짜**입니다(질로우가 집
재고를 털어낸 분기). XBRL 도 심판이 못 됩니다: 걸린 8칸 모두 XBRL 값이
보도자료와 **똑같아**(비 1.00) XBRL 쪽에도 같은 값이 들어 있습니다.
오탐을 가를 자를 찾은 뒤에 자격을 세웁니다. 그때까지는 화면이 **세어서
보여 주기만** 합니다(106차 규칙 — 값은 안 쓰고 세기만).

- ⚠️ **매출총이익률은 행마다 잣대가 다를 수 있습니다** (183차-AQ).
  수집기는 보도자료에서 읽으면 회사 **조정** 값을, 못 읽어 XBRL 로
  근사하면 **GAAP** 값을 담습니다. 어느 쪽인지는 `gm_is_gaap` 표시가
  말해 주는데, 이 표시가 스냅샷 허용 목록에 없어 **지금까지 버려지고
  있었습니다.** 조정 매출총이익률이 보통 더 높으므로 산수 규칙이 행마다
  다른 자로 재고 있었던 셈입니다. 표시는 183차-AQ 부터 실립니다 —
  **표시가 쌓인 뒤에** 잣대별로 나눠 다시 재고, 규칙을 고칠지 정합니다
  (지금 고치면 근거 없이 바꾸는 것입니다).

---

## 3. 단위를 정하는 자격 — 네 가지뿐

값에 단위가 안 붙어 있을 때, **아래 넷 중 하나로만** 단위를 정합니다.
넷 다 안 되면 **곱하지 않습니다**(= 그대로 두고 뒷단 검사가 버립니다).

| 순위 | 자격 | 근거 |
|---|---|---|
| ① | 숫자에 **낱말이 붙어 있음** (`$17.23 billion`) | 값이 스스로 말한다 |
| ② | **가까운 표 머리**(3,000자 안)의 단위 선언 | 그 표의 선언 |
| ③ | **먼 표 머리** — 단, 숫자가 **표의 칸**이고 배수가 **10억이 아닐** 때만 | 150차-AF |
| ④ | **표 아래 각주** — 숫자 뒤 2,000자 안, 위와 같은 두 조건 | 150차-AG |

### 3-b. 표 머리의 **기간 선언**도 같은 자로 읽는다 (183차-AW)

단위와 똑같이, **기간**도 가까운 표 머리가 말해 줍니다.

| 규칙 | 값 |
|---|---|
| 창 | 값에서 **3,000자** 앞까지 |
| 연간 전용 표면 | 그 값은 **분기값이 아니다** — 건너뛴다 |
| 합친 표(분기 머리와 연간 머리가 **300자 안**에 함께) | **손대지 않는다** |

**왜 필요한가**: "Full Year and Fourth Quarter …" 보도자료에는 표가 둘
들어갑니다. **GS 는 아홉 해 내리**, ORCL 은 다섯 해 내리 연간 표의 값이
4분기 매출 칸에 앉아 있었습니다(2025-12-31 = 58,283백만 = 연간 순수익).
기존 연간 가드는 **라벨 앞 같은 줄**만 봐서 2,138자 뒤의 표 머리에
닿지 않았습니다.

**왜 합친 표는 건드리면 안 되나**: 많은 회사가 한 표에 분기 열과 연간
열을 나란히 둡니다(ACLS `Total revenue 252,417 310,288 1,017,865
1,130,604`). 이름 뒤 **첫 숫자가 분기값**이라 이미 맞습니다.

> ⚠️ **이 검사는 `avoid_annual` 밖에 두어야 합니다.** 매출 탐색은 같은
> 글을 네 번 훑는데 **뒤 두 번은 연간 가드를 끄고** 다시 찾습니다.
> 가드 안에 두면 앞 두 번이 걸러도 뒤 두 번이 그대로 주워 옵니다 —
> 183차-AT 의 고침이 매출을 한 칸도 못 바꾼 까닭입니다.

**표의 칸이란**: 이름과 값 사이가 **공백 두 칸 이상** 벌어진 것.
서술문은 한 칸으로 붙습니다. `(in millions)` 는 **그 표**의 단위 선언이지
문장의 단위가 아닙니다 (실측 — 이 문지기가 없으면 서술문 숫자 202·3·28 이
백만 배로 부풀어 그럴듯한 헛값이 됩니다).

**멀리 있는 10억 선언은 안 씁니다**: 재무제표 표는 천·백만으로 적지
10억으로 적지 않습니다. 실측 — 창 밖 10억 선언 **6건이 6건 모두** 틀렸고
Citi 10.35조 · JPM 100조 같은 값을 만들었습니다.

**주당 금액에는 표 단위를 절대 곱하지 않습니다** — $1.00 이 $1,000 이 됩니다.

---

## 4. 이름을 고르는 자격 — 좁은 이름이 먼저

| 규칙 | 내용 |
|---|---|
| 좁은 이름 먼저 | `Total net revenues` → `Total revenues` → `Net revenues` → `Revenue` |
| 같은 줄 먼저 | 이름과 값은 **같은 줄**에 있다. 제목 줄만 값이 다음 줄에 있다 |
| 가까운 것 먼저 | 같은 이름이 두 번 나오면 **값이 가까이 붙은** 쪽 |
| 조각을 전체로 쓰지 않는다 | `Product revenue` 는 조각. **단, 전체가 그 문서에 있을 때만** 물린다 |
| 부문 줄보다 합계 줄 | 은행 부문 표는 거리가 전부 0 이라 합계 줄을 **이름으로** 먼저 묻는다 |

---

## 5. 짝짓기 규칙 — 발표 ↔ 분기

| 규칙 | 값 | 근거 |
|---|---|---|
| 창 | 분기끝 뒤 **0 ~ 120일** | 연말은 10-K 와 함께 늦게 나온다 |
| 발표일 상식 범위 | 분기끝 뒤 **10 ~ 70일** | 실측 분포 (0.5%=10일 · 99.5%=58일) |
| 가장 가까운 분기가 임자 | 더 가까운 분기가 있으면 가져가지 않는다 | 앞 분기가 뒷 분기 발표를 가로채는 사고 |
| **알맹이 우선** | 이익 숫자(조정EPS·논갭영업이익·조정EBITDA)를 실은 발표가 **거리와 무관하게** 이긴다 | 예비 매출 공지가 진짜 발표를 밀어낸 사고 |
| 승격 | 마지막 XBRL 분기보다 105일 넘게 뒤면 **새 분기 행**으로 | 과거 분기를 덮어쓰는 오염 방지 |

---

## 6. "없음"의 사유 — 다섯 갈래로 나눠 적는다

**이것이 이번 규격의 핵심입니다.** 지금까지 모든 없음이 똑같이 보여서
"왜 이렇게 비었나"를 판단할 수 없었습니다. 실측(2026-08-23 · 401종목 ·
15,074행 · 조정 EPS 기준):

| 사유 | 뜻 | 칸 | 고칠 수 있나 |
|---|---|---:|---|
| 있음 | — | 6,606 (43.8%) | — |
| `없음_회사미발표` | 발표는 붙었는데 회사가 그 항목을 안 냄 | 4,838 (32.1%) | **아니오** |
| `없음_짝없음_회사는냄` | 발표는 있었는데 **8-K 를 못 붙임**, 그 회사는 조정 EPS 를 냄 | **2,308 (15.3%)** | **예 ← 여기가 일감** |
| `없음_짝없음_회사도안냄` | 못 붙였지만 그 회사는 애초에 안 냄 | 1,010 (6.7%) | 붙여도 값 없음 |
| `없음_그시절없음` | 발표 기록 자체가 없음 (**상장 전**) | 233 (1.5%) | **아니오** |
| `없음_구멍메움` | 짝지을 발표가 없는 보충 행 | 79 (0.5%) | 부분적으로 |

> ⚠️ **맨 아래 칸의 이름을 정정합니다 (183차-AQ).** 이 칸은 지금까지
> 표시를 **읽은 것이 아니라 짐작한 것**이었습니다. 수집기는 구멍을
> 메운 행에 `구멍메움` 표시를 달고 있었는데 그 표시가 스냅샷 허용
> 목록(`measure_store.EPS_FIELDS`)에 없어 버려졌고, 화면은 대신
> "`press_matched` 가 비었으면 구멍메움"이라고 셌습니다.
>
> 실측(2026-09-18 · 66칸): **10칸은 발표일조차 없어 구멍메움도
> 승격도 아니었습니다.** 지금은 표시를 스냅샷까지 실어 **세 갈래**로
> 가릅니다 — `구멍메움`(짝 없는 8-K 로 빠진 분기를 새로 끼운 행) ·
> `승격`(뼈대에 없던 8-K 를 그대로 행으로 올린 행) · `표시없음`.
> 183차-AQ **이전에 만들어진 스냅샷 행에는 표시 자체가 없으므로**
> 전부 '표시없음'으로 모입니다. 다음 런부터 실제 값이 찍힙니다.

> ⚠️ **처음 센 숫자를 정정합니다.** 원본 스냅샷만 보고 세었을 때는
> "상장 전 3,324칸 · 못 가져옴 225칸"으로 나왔습니다. **틀렸습니다.**
> 정제 단계에서 **바깥 자(야후)의 발표일**이 채워지면(`_recover_announced`)
> "발표는 분명히 있었는데 우리가 8-K 를 못 붙인" 분기가 드러납니다.
> 정제 뒤의 자료로 세어야 옳고, 그 수는 **2,308칸**입니다.
> 계기는 재는 대상과 **같은 단계**를 봐야 합니다.

**크리도 실물**: 2021-01-31 · 2021-07-31 · 2021-10-31 은 **상장 전**이라
8-K 실적 발표가 **존재하지 않습니다**. XBRL 에는 있는데(상장 서류에 과거
재무가 실림) 조정 EPS 가 비는 이유가 이것입니다. 가져올 것이 없습니다.
다만 2022-04-30 은 다릅니다 — 야후는 2022-06-01 에 발표가 있었다고
말하는데 우리가 8-K 를 못 붙였습니다. **이런 것이 일감입니다.**

### 일감 2,308칸은 왜 생겼나 (2026-08-23 로봇 계기로 확인)

가장 많이 빈 종목들의 수집 기록입니다 — **전부 5차 확장으로 새로 들어온
종목의 첫 수집**이고, 캐시가 0 이라 공시를 32~63건밖에 못 훑었습니다:

    CE   캐시적중 0 · 새로받음 36    HBAN 0 · 60    ZTS  0 · 34
    BAH  0 · 33                   TDG  0 · 63    XYL  0 · 32
    DXCM 0 · 45                   WAT  0 · 46    LDOS 0 · 38

공시 30~60건은 최근 2~3년치입니다. 그래서 그 앞 분기는 8-K 가 아예
없습니다. **파서 문제가 아니라 훑은 깊이 문제**입니다.

→ **캐시가 차면 저절로 깊어집니다.** 다음 런부터 며칠 지켜보고, 그래도
안 줄면 그때 시간 예산을 손봅니다. (짐작으로 뼈대를 건드리지 않는다.)

→ 남는 것은 **형식 때문에 못 읽는 종목**입니다 — FITB 는 캐시가 83건이나
있는데 조정 EPS 를 1개밖에 못 읽었습니다(슬라이드형 문서). 이쪽은
파서의 일감입니다.

> **화면 규칙**: 빈칸을 그냥 비워 두지 말고 **사유를 함께 보입니다.**
> "없음"과 "우리가 못 가져옴"은 사용자에게 전혀 다른 뜻입니다 (정직화).

---

## 7. 이 규격을 어기면 어떻게 아는가

규격은 **시험으로 강제**합니다. 규격을 고치면 시험도 함께 고칩니다.

| 규격 | 지키는 시험 |
|---|---|
| 이름은 계산에 안 쓴다 | `test_quality.py` 의 회계분기 시험 |
| 회계분기 번호는 **종목 자기 순서** | `test_quality.py::test_열두주달력_회사도_네_분기로_갈린다` 외 6개 |
| 어긋난 분기 이름은 버린다 | `test_dataset.py::test_겹치지_않아도_어긋난_이름_하나면_버린다` 외 |
| 단위 자격 네 가지 | `test_parsing.py` 의 단위 시험 묶음 |
| 좁은 이름 먼저 | `test_parsing.py::test_segment_rows_do_not_beat_the_total_row…` |
| 알맹이 우선 짝짓기 | `test_eps_parse.py` 의 짝짓기 시험 |
| 논갭 영업이익 버리는 세 자격 | `test_dataset.py::test_연간_영업이익이…` · `test_XBRL_영업이익이_직접…` · `test_매출총이익을_넘는…` |
| 없음 사유 | `test_dataset.py` · `test_screen_verify.py::test_빈칸_사유를…` |
| **이 문서가 낡지 않는다** | `test_dataset.py::test_데이터규격이_지금_코드와_맞는다` |

---

## 8. 고치는 법

1. 이 문서를 **먼저** 고친다 (무엇을 왜 바꾸는지).
2. 전수로 잰다 — **얻는 것과 잃는 것을 함께** 센다.
3. 잃는 것이 진짜 값이면 **버린다**(150차-AK 에서 그렇게 했습니다).
4. 채택하면 시험과 돌연변이를 붙이고 `측정결과.md` 에 회차로 남긴다.
