# 이 저장소에서 일하는 방식

이 파일은 **저장소 주인이 정한 작업 규칙**입니다. 새 세션이 시작될 때마다
자동으로 읽히므로, 매번 다시 설명하지 않아도 됩니다.

> ⚠️ **지금 이 저장소는 v3 재건축 중입니다** (1~4단계 완료 — 헌법·
> 이 문서·뼈대 설계도·데이터 계층. 남은 단계는 `전략.md` 6장 로드맵 참조).
> v2 코드는 `v2-final` 브랜치에 봉인했고, 남아 있는 것은
> **수집 로봇·파서와 새로 쓴 v3 헌법(`전략.md`)** 입니다.
>
> **새 세션은 먼저 `전략.md`(헌법)를 읽으세요.** 질문·확정된 지식·
> 규칙·로드맵이 거기 있습니다. 새로 지을 모듈의 경계는 `설계도.md`,
> 실패의 내력과 남긴 이유가 더 필요하면 `인수인계.md`,
> 측정의 원기록은 `측정결과.md` 입니다.

---

## 1. 사용량 한도에 걸리면 — 멈추지 말고 기다렸다가 이어서 한다

**이것이 가장 중요한 규칙입니다.**

작업 도중 사용량 한도(usage limit)에 걸리면:

- **중단하지 않는다.** 하던 일을 포기하거나 "여기까지 하겠습니다"로 끝내지 않는다.
- **그 자리에서 대기(홀딩)한다.** 지금까지 한 일과 다음에 이어서 할 일을 남겨 둔다.
- **한도가 풀리면 곧바로 이어서 진행한다.** 사용자가 다시 지시하지 않아도 재개한다.
- 재개할 때 앞의 과정을 길게 되짚지 않는다. 하던 자리에서 그대로 이어간다.

작업이 여러 세션에 걸쳐도 괜찮습니다. **끝까지 마치는 것**이 우선입니다.

한도에 걸릴 것 같으면 미리 작업을 잘게 쪼개거나, 중간 결과를 커밋·푸시해 두어
다음 세션에서 이어받을 수 있게 합니다.

---

## 2. 사용자에 대해

- **한국어로** 답합니다.
- **개발자가 아닙니다.** 코드 용어를 그대로 쓰지 말고 무엇을 왜 하는지 풀어서 설명합니다.
  코드 주석도 한국어로, 초보자가 읽을 수 있게 씁니다.
- **휴대폰으로만** 씁니다. 화면 관련 작업은 항상 모바일 폭(412px)에서 실제로
  렌더링해 확인합니다. 글자 잘림·열 가림이 없어야 합니다.

---

## 3. 모델링 원칙

v3 헌법(`전략.md`)에서 옮겨 적은 요약입니다.
**이 요약과 전략.md가 다르게 읽히면 전략.md가 우선입니다.**

**핵심 질문** (전략.md 0장):

> "좋은 장세라는 조건 아래에서만, 이익의 첫 신기록을 깬 종목을
> 고르면 기준선을 이길 수 있는가?"

위층(장세 = 실적 신기록의 "폭") × 아래층(종목 = TTM 조정 EPS 신고점
**첫** 돌파)의 두 층 구조입니다. 종목 신호는 장세라는 조건 아래에서만
의미를 묻습니다.

**지킬 원칙 6개**:

1. **창작 금지.** 없는 값은 "없음"으로 둡니다 — "없음"은 안전하고
   "틀림"은 위험합니다. 검사에서 탈락한 값은 고치지 말고 버립니다.
   전망 입력은 회사가 직접 발표한 가이던스뿐입니다(컨센서스·역산·
   근사 금지). 값 검사는 나중이 아니라 **1단계**입니다
   (`data_quality.py` — 계산에 들어가기 전에).
2. **사전 등록.** 문턱·창·표적·채택 기준은 **데이터를 보기 전에**
   `측정결과.md`에 적고 머지합니다. 측정 기본형(고정): 창 60거래일 ·
   폭등 = SPY 대비 +20%p · 기준선 = 같은 표본의 모든 발표.
   채택 기준(고정): 신호 그룹 윌슨 95% 구간 **하한** > 기준선
   **상한** (완전 분리), n≥10.
3. **검증 실패 시 미반영.** 채택 기준을 못 넘은 신호는 판단·점수·
   추천 어디에도 쓰지 않습니다. 화면에는 사실만 표시하되, 그 상태의
   **과거 실측 폭등률·기준선·판정 상태**를 함께 적습니다(정직화).
   미채택 가설은 버리지 않고 관찰 목록에 두어 로봇이 표본을 쌓는
   대로 자동 재판정합니다.
4. **장세 전제.** 장세가 종목 선택을 지배합니다(연도별 기준선
   0%→45.6%). 어떤 측정이든 앞/뒤 **시간 분할**을 함께 보고하고,
   강세장 구간만의 우위는 믿지 않습니다.
5. **탐색 규율.** 탐색은 채택 근거가 아닙니다. 잰 것은 좋든 나쁘든
   전부 기록합니다. 탐색에서 나온 후보는 새 H번호로 사전 등록한 뒤
   **새 데이터로만** 판정합니다(탐색 표본 재사용 금지).
6. **신호보다 장치를 먼저 의심.** 결과가 이상하면 측정 장치부터
   감사합니다(8차 감사에서 실제 결함 2건이 나왔습니다).

---

## 4. 정직하게 말하기

- 못 한 것은 못 했다고 말합니다. 되는 것처럼 보이게 만들지 않습니다.
- 확인하지 못한 숫자는 "확인 못함"으로 표시합니다. 지어내지 않습니다.
- 검산이 맞지 않으면 억지로 맞추지 않고, 맞지 않는다고 기록합니다.
- 화면에 신호를 띄울 때는 **그 신호가 과거에 실제로 몇 % 맞았는지** 함께 보여 줍니다.
- 한계는 감추지 않고 코드 주석·문서·화면 설명 세 곳 모두에 적습니다.

실제 사고에서 배운 검증 규칙 (자세한 내력은 `인수인계.md`):

- **새 테스트는 일부러 깨뜨려 빨간 불을 본 뒤에만 믿는다.**
  (실행 스위치 뒤에 적혀 한 번도 돌지 않은 테스트 12개 사건)
- **새 코드 경로는 실제로 실행됨을 증명한다** — 로그나 테스트로.
  (실행 불가능한 위치에 있던 구제 코드가 루멘텀 전망을 무너뜨린 사건)
- **보고에 적는 숫자는 실행 출력에서 복사한다.** 기억으로 적지 않는다.
  (커밋 메시지에 테스트 개수를 310으로 적었으나 실제 301이었던 사건)

---

## 5. 다중 에이전트 워크플로는 쓰지 않는다

**허용 창만 계속 뜨고 실제로 실패했습니다.** 원문 130건 분류에 에이전트
13개를 띄웠는데 11개가 실패해 58만 토큰을 쓰고 결과가 0건이었습니다.
같은 일을 한 세션에서 직접 전수 처리했더니 더 정확했습니다
(조정 EPS 회수 6→13건).

데이터가 79종목 규모라 한 세션에서 결정적으로 처리하는 편이 빠르고,
무엇보다 **검증이 됩니다.**

---

## 6. 이 환경의 제약 (매번 다시 알아내지 않도록)

- **SEC 신원 이메일**: `config.py`의 `DEFAULT_SEC_IDENTITY`에 실제 이메일이 들어 있는 것은
  저장소 주인이 직접 선택한 것입니다. SEC는 이메일 없는 요청을 차단(403)하기 때문입니다.
  지우지 마세요.
- **차단된 사이트**: sec.gov · yahoo · nasdaq · stooq · macrotrends · businesswire ·
  stocktitan · investor.* · wikipedia 등이 조직 정책으로 막혀 있습니다(403).
  **재시도하지 마세요.** 대신 웹 검색 결과 요약을 쓰고 출처 2개로 교차 확인합니다.
- **WebFetch 는 사실상 전 도메인이 막혀 있습니다.** 위 목록뿐 아니라 회사 홈페이지·
  stockanalysis·fool·marketscreener·wsj 등도 전부 차단됩니다(에이전트 3개가 각각
  30여 개 호스트를 시도해 전부 실패). 자료 수집은 **WebSearch 요약만** 쓸 수 있다고
  전제하고 계획하세요.
- **검색 요약은 숫자를 틀립니다.** 실제로 확인된 오류: 다른 회사 수치를 섞어 답함,
  전년동기 열(column)을 이번 분기 값으로 제시함, 다른 회계연도 값을 반환함.
  그래서 **산술 검산이 유일하게 믿을 만한 검증 수단**입니다 —
  분기 합계 vs 연간 발표치, 계산값 vs 회사가 밝힌 비율 같은 식으로 교차 확인하세요.
  요약 출처를 여러 개 모아도 같은 보도자료를 재배포한 곳이면 독립 출처가 아닙니다.
- **주가 데이터**: 이 개발 환경에서는 받을 수 없습니다. 실데이터 검증은 실적(펀더멘털)
  중심으로 하고, 주가가 필요한 검증은 한계를 명시합니다.
- **실데이터는 수집 로봇(깃허브 액션)이 저장소로 커밋해 옵니다.**
  로봇은 깃허브 서버에서 돌아 SEC·주가 접속이 되고, `data/measure/` 아래로
  커밋합니다. 개발 환경은 그 파일을 읽어 측정·테스트에 씁니다.
  개발 환경에서 직접 받으려고 재시도하지 않습니다. 로봇의 실행 기록은
  깃허브 액션 로그로 직접 확인할 수 있습니다 (실행 증명 규칙).
- **테스트**: `python3 run_tests.py` 로 전부 실행합니다. 커밋 전에 반드시 통과시킵니다.
  파이프로 넘기지 말고 **종료코드를 직접 확인**하세요 (`python3 run_tests.py; echo $?`).
  고친 직후에는 `find . -name __pycache__ -exec rm -rf {} +` 로 옛 캐시를 지우고
  돌립니다 (같은 초에 두 번 고치면 옛 코드가 그대로 도는 사고가 있었습니다).
