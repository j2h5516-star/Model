# 이 저장소에서 일하는 방식

이 파일은 **저장소 주인이 정한 작업 규칙**입니다. 새 세션이 시작될 때마다
자동으로 읽히므로, 매번 다시 설명하지 않아도 됩니다.

---

## 1. 사용량 한도에 걸리면 — 멈추지 말고 기다렸다가 이어서 한다

**이것이 가장 중요한 규칙입니다.**

작업 도중 사용량 한도(usage limit)에 걸리면:

- **중단하지 않는다.** 하던 일을 포기하거나 "여기까지 하겠습니다"로 끝내지 않는다.
- **그 자리에서 대기(홀딩)한다.** 지금까지 한 일과 다음에 이어서 할 일을 남겨 둔다.
- **한도가 풀리면 곧바로 이어서 진행한다.** 사용자가 다시 지시하지 않아도 재개한다.
- 재개할 때 앞의 과정을 길게 되짚지 않는다. 하던 자리에서 그대로 이어간다.

작업이 여러 세션에 걸쳐도 괜찮습니다. **끝까지 마치는 것**이 우선입니다.

한도에 걸릴 것 같으면 미리 작업을 잘게 쪼개거나, 중간 결과를 커밋·푸시해 두어
다음 세션에서 이어받을 수 있게 합니다.

---

## 2. 사용자에 대해

- **한국어로** 답합니다.
- **개발자가 아닙니다.** 코드 용어를 그대로 쓰지 말고 무엇을 왜 하는지 풀어서 설명합니다.
  코드 주석도 한국어로, 초보자가 읽을 수 있게 씁니다.
- **휴대폰으로만** 씁니다. 화면 관련 작업은 항상 모바일 폭(412px)에서 실제로
  렌더링해 확인합니다. 글자 잘림·열 가림이 없어야 합니다.

---

## 3. 모델링 원칙 (사용자가 직접 정한 것)

### 이 대시보드가 하려는 것

> 과거부터 현재까지 논갭 영업이익 델타와, 현재부터 앞으로의 논갭 영업이익 델타 예측에
> 주가 추세까지 판단해서 최종 점수를 내는 모델

### 값 검사는 **모델링의 1단계**다

> "값이 이상하면 다 버린다"가 아니라, 모델을 짤 때 **최우선적으로 값이 이상한지 먼저
> 판단하고 적용한다."

수집한 숫자는 점수·전망 계산에 들어가기 **전에** 검사합니다(`data_quality.py`).
나중에 이상한 결과를 보고 되돌리는 방식은 쓰지 않습니다.

### 바꾸기 전에 반드시 백테스트한다

로직을 바꾸기로 마음먹었다면 **적용 전에 숫자로 확인**합니다.

- 기준선(아무 계산도 하지 않았을 때의 성적)을 반드시 함께 냅니다.
- 신뢰구간(윌슨)을 함께 냅니다. 적중률만 크게 말하지 않습니다.
- 종목별로 쪼개 봅니다. 한두 종목이 만든 결과인지 확인합니다.
- **표본 편향을 경계합니다.** 폭등한 종목만 모으면 기준선이 95%가 되어
  무엇을 해도 좋아 보입니다. 부진한 종목을 반드시 섞습니다.
- 신호와 채점 항목이 **겹치지 않는지** 봅니다. 겹치면 순환 논리가 되어
  성적이 부풀려집니다.

### 검증에 실패하면 반영하지 않는다

백테스트가 근거를 주지 않으면, 지시받은 변경이라도 **그대로 적용하지 않고
결과를 먼저 보고**합니다. 근거 없는 신호에 점수를 주지 않습니다.

이미 화면·문서에 적어 둔 설명이 검증에서 틀린 것으로 드러나면
**그 자리에서 정정하고, 무엇이 어떻게 틀렸는지 숫자와 함께 남깁니다.**

---

## 4. 정직하게 말하기

- 못 한 것은 못 했다고 말합니다. 되는 것처럼 보이게 만들지 않습니다.
- 확인하지 못한 숫자는 "확인 못함"으로 표시합니다. 지어내지 않습니다.
- 검산이 맞지 않으면 억지로 맞추지 않고, 맞지 않는다고 기록합니다.
- 화면에 신호를 띄울 때는 **그 신호가 과거에 실제로 몇 % 맞았는지** 함께 보여 줍니다.
- 한계는 감추지 않고 코드 주석·README·화면 설명 세 곳 모두에 적습니다.

---

## 5. 다중 에이전트 사용

정밀한 검증이나 자료 수집이 필요하면 **다중 에이전트를 적극적으로 씁니다.**
사용자가 이미 허가했습니다("너보다 더 정밀하다면 사용량 쓸 수 있어").

에이전트가 저장소 파일을 고쳤다면, 커밋하기 전에 **반드시 직접 읽고 검토**합니다.

---

## 6. 이 환경의 제약 (매번 다시 알아내지 않도록)

- **SEC 신원 이메일**: `config.py`의 `DEFAULT_SEC_IDENTITY`에 실제 이메일이 들어 있는 것은
  저장소 주인이 직접 선택한 것입니다. SEC는 이메일 없는 요청을 차단(403)하기 때문입니다.
  지우지 마세요.
- **차단된 사이트**: sec.gov · yahoo · nasdaq · stooq · macrotrends · businesswire ·
  stocktitan · investor.* · wikipedia 등이 조직 정책으로 막혀 있습니다(403).
  **재시도하지 마세요.** 대신 웹 검색 결과 요약을 쓰고 출처 2개로 교차 확인합니다.
- **WebFetch 는 사실상 전 도메인이 막혀 있습니다.** 위 목록뿐 아니라 회사 홈페이지·
  stockanalysis·fool·marketscreener·wsj 등도 전부 차단됩니다(에이전트 3개가 각각
  30여 개 호스트를 시도해 전부 실패). 자료 수집은 **WebSearch 요약만** 쓸 수 있다고
  전제하고 계획하세요.
- **검색 요약은 숫자를 틀립니다.** 실제로 확인된 오류: 다른 회사 수치를 섞어 답함,
  전년동기 열(column)을 이번 분기 값으로 제시함, 다른 회계연도 값을 반환함.
  그래서 **산술 검산이 유일하게 믿을 만한 검증 수단**입니다 —
  분기 합계 vs 연간 발표치, 계산값 vs 회사가 밝힌 비율 같은 식으로 교차 확인하세요.
  요약 출처를 여러 개 모아도 같은 보도자료를 재배포한 곳이면 독립 출처가 아닙니다.
- **주가 데이터**: 이 개발 환경에서는 받을 수 없습니다. 실데이터 검증은 실적(펀더멘털)
  중심으로 하고, 주가가 필요한 검증은 한계를 명시합니다.
- **테스트**: `python3 run_tests.py` 로 전부 실행합니다. 커밋 전에 반드시 통과시킵니다.
- **`config.py`에 항목을 추가하고 `app.py`가 그것을 쓰면** `CONFIG_VERSION`과
  `REQUIRED_CONFIG_VERSION`을 함께 1 올립니다.
