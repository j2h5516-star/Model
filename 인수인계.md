# 인수인계 — 이 저장소를 이어받는 세션이 읽는 문서

작성: 2026-08-13(v3 재건축 0단계 직후) · 갱신: 2026-08-22(150차-H).
**모든 숫자는 실행 출력에서 복사한 것입니다.**

> **먼저 알 것**: v3 재건축 0~6단계는 **전부 끝났고 지금은 운영
> 단계**입니다. 로봇이 매일 수집·판정하고 화면이 정직화 원칙대로
> 표시합니다. 지금 상태와 다음에 할 일은 **5장**에 있습니다.
>
> · **1장·3장은 지난 일의 기록**입니다(무엇을 왜 남겼나, v1·v2가
>   무엇에 실패했나). 지금 사실이 아니라 **그때의 사실**입니다.
> · **2장·5장이 지금 상태**입니다.
> · **4장은 사고에서 나온 규칙** — 여기가 가장 값집니다.
>
> 헌법은 `전략.md`, 모듈 경계는 `설계도.md`, 측정 원기록은
> `측정결과.md` 에 있습니다.

---

## 1. 저장소에 무엇을 남겼나 (2026-08-13 봉인 당시의 기록)

v2 코드 전체는 `v2-final` 브랜치(32da696)에 봉인했습니다.
필요하면 언제든 꺼내 볼 수 있지만, **되살려 쓰지는 않습니다.**

### 남긴 것과 그 이유

| 남긴 것 | 왜 |
|---|---|
| `data/` (지금 70MB) | 수집물 250종목·5년치 + 공시 원문 1,443건. 유니버스는 401종목이라 나머지는 다음 런에서 채워집니다. **다시 못 모읍니다** — 개발 환경은 SEC·주가가 차단돼 있어 로봇만 받아올 수 있습니다 |
| `측정결과.md` | 1~9차 측정·감사 기록. v3가 같은 실패를 반복하지 않게 하는 유일한 근거 |
| `collect_job.py` + `.github/workflows/collect.yml` | 수집 로봇. 재건축 중에도 매일 돌아 데이터를 쌓아야 합니다 |
| `config.py` | 종목 목록과 수집 설정. v2 점수·판정 상수 82개는 걷어냈습니다 (870줄 → 553줄) |
| `sec_fundamentals.py` | 8-K 보도자료 파서. **규칙 하나하나가 실제 사고의 흉터**입니다 (아래 4장) |
| `data_quality.py` | 값 검사. "이상한 값은 고치지 말고 없앤다"의 구현 |
| `forward_estimates.py` | 가이던스 파서 |
| `market_data.py` | 주가 수신 (야후) |
| `measure_store.py` | 수집 결과를 `snapshot.json` 으로 꾸리는 부품 |
| `tests/` (그때 6개 · 지금 20개 파일 580개) | 위 코드를 지키는 자물쇠. **실물 원문으로 만든 시험이 다수** |

> ⚠️ `measure_store.py` 는 원래 0단계 삭제 목록에 있었지만 **남겼습니다.**
> 수집 로봇이 파일을 쓰는 통로라서, 지우면 로봇이 그날로 멈춥니다.
> (4단계에서 다시 정하기로 했었고, **그대로 두기로 끝났습니다** —
> `dataset.py` 가 그 위에서 데이터 계층을 만듭니다.)

### 지운 것

`app.py`(계기판) · `measurement.py`(측정 장치) · `status.py`(델타 판정) ·
`breadth.py`(장세 게이지) · `exploration.py`(탐색 배터리) · `verdict.py`(자동 판정) ·
`전략.md`(v2 헌법) · `요약서.md` · `README.md`,
그리고 이들을 검사하던 시험 4개.

`collect_job.py` 안의 자동 판정 호출도 함께 뺐습니다 — 0단계 당시
로봇은 수집만 했습니다. (**지금은 다릅니다**: 5단계에서 판정을 다시
연결해, 로봇이 매일 수집하고 24개 가설을 판정해 `verdict.json` 을
커밋합니다. 5장을 보세요.)

---

## 2. 지금 데이터에 무엇이 들어 있나

> 아래는 **2026-08-22 수집** 기준입니다(실행 출력에서 복사). 이 표는
> 회차마다 바뀌므로, 숫자가 궁금하면 이 문서를 믿지 말고 직접 재세요:
> `python3 model_verify.py` · `data_health.report(snap["eps"], None)`.
> (원래 이 자리에 2026-08-12 의 79종목 표가 그대로 남아 있었습니다 —
> 150차-H 에 갱신.)

`data/measure/snapshot.json` (2026-08-22 12:23 수집):

| 항목 | 수 |
|---|---|
| 수집물 종목 | 250 (+ 기준지수 SPY) — **유니버스는 401**, 나머지는 다음 런 |
| 주가 | 251종목 · 2016-08-22 ~ 2026-08-21 · 2,514거래일 |
| 전체 분기 행 | 9,466 |
| 발표일이 있는 분기 | 9,337 |
| 매출이 있는 분기 | 9,156 |
| GAAP EPS 가 있는 분기 | 8,885 |
| 조정 EPS 가 있는 분기 | 4,405 |
| 조정 EBITDA 가 있는 분기 | 669 |
| 회사 가이던스 EPS 가 있는 분기 | 359 |

**잣대 사다리**(종목마다 하나, ≥8분기여야 정해짐):

| 잣대 | 종목 수 |
|---|---|
| 조정 EPS | 162 |
| GAAP EPS | 74 |
| 조정 EBITDA | 14 |
| 못 정함(측정 제외) | **0** |

- 리츠(PLD·SPG·PSA·DLR 등)는 조정 EPS 칸이 비어 **GAAP EPS 로 판정**됩니다.
  FFO 오염은 없지만(150차 ③ 감사), GAAP EPS 는 감가상각 때문에 리츠의
  실체를 낮춰 잡습니다 — **이 종목들의 신호는 다른 종목과 같은 뜻이
  아닙니다.**
- `data/measure/raw/` 에 원문 **1,443건**(70MB). 그중 `부탁_` 로 시작하는
  것이 56건 — 조사가 콕 집어 요청해 받아 온 것들입니다. 파서를 고칠 때
  가장 값진 자료입니다 — **지어낸 예제로 시험하지 마세요.**
- `data/measure/robot_log.json` 에 종목별 수집 성적과 `수집계기`(일꾼·
  벽시계·겹침배수·초당요청)가 있습니다. **짐작 전에 여기부터 보세요.**

---

## 3. v1·v2가 무엇에 실패했나 (숫자로 — 지난 일의 기록)

### v1 — 점수를 먼저 만들고 검증을 나중에 했다

100점 만점 점수제·신뢰도 등급·컨센서스·역산 전망을 먼저 만들었고,
나중에 측정해 보니 **근거가 없었습니다.** v1은 `v1-final` 브랜치에 봉인.

### v2 — 검증 장치는 훌륭했는데, 찾은 신호가 없었다

v2는 사전 등록·홀드아웃·윌슨 신뢰구간까지 갖췄습니다. **그 규율 자체는
v3가 물려받아야 할 자산입니다.** 문제는 결과였습니다.

측정한 신호 7개 → **채택 0개**:

| 신호 | 결과 |
|---|---|
| 조정 EPS 가속 → 폭등 (v1의 질문) | **기각** — 3회 측정 모두 기준선 이하 (19.4% vs 21.8%) |
| 델타 변동성(성장의 꾸준함) | 기각 — 우위 없음 |
| 연속 가속 횟수 | 기각 — 평평함 |
| 국면 신호 H4 | 기각 — 60% → 26.3%로 붕괴 |
| TTM 신고점 돌파 (H2) | 미채택 — 22.2% vs 기준선 18.9% |
| 신고점 **첫** 돌파 (H2b) | 미채택 — 32.6%, 표본 46건이라 우연과 구분 안 됨 |
| 실적 폭 게이지 (H5) | 미채택 — 20.2% vs 18.9%. 게다가 79종목으로 재니 문턱 20%가 **4년 내내 항상 ON** 이라 변별력 상실 |

### 가장 중요한 발견 — 종목이 아니라 장세가 지배한다

같은 규칙으로 잰 **연도별 기준선**:

| 연도 | 아무 발표나 샀을 때 폭등률 |
|---|---|
| 2021 | 0.0% |
| 2022 | 18.2% |
| 2023 | 15.6% |
| 2024 | 9.8% |
| 2025 | 22.2% |
| 2026 | 45.6% |

**어느 해에 샀느냐가 무엇을 골랐느냐보다 훨씬 크게 작용합니다.**
v3의 질문을 정할 때 이 사실을 정면으로 다루세요 — 이걸 무시하고
"어떤 종목이 오르나"만 물으면 v2와 같은 답(신호 없음)이 나옵니다.

### 측정 장치 자체의 결함도 있었다 (8·9차 감사)

- **신고점 망각**: 실적 이력이 끊기면 옛 정점을 잊고 낮은 값을 "신고점"으로
  판정 (실데이터 27건). 수정 후 재측정 — 결론은 안 바뀜.
- **짝짓기 사고**: 예비 매출 공지가 분기 종료일에 더 가깝다고 진짜 실적
  발표를 밀어내, 조정 EPS $1.07 이 통째로 버려짐 (CRDO 26Q3).
- **파서 누락**: 조정 EBITDA 를 뽑고도 분기 행에 복사하지 않아 버림.

→ **교훈: 신호를 의심하기 전에 측정 장치를 먼저 의심하라.**

---

## 4. 절대 반복하면 안 되는 것

실제로 터졌던 사고들입니다. 규칙은 전부 사고에서 나왔습니다.

1. **점수를 먼저 만들지 마라.** 측정으로 근거를 확인한 것만 화면에 올립니다.
2. **없는 숫자를 만들지 마라.** XBRL 근사·역산·추정 전망은 v1을 무너뜨렸습니다.
   검사에서 탈락한 값은 고치지 말고 **"없음"** 으로 둡니다.
3. **새 시험은 일부러 깨뜨려 빨간 불을 본 뒤에만 믿어라.**
   실행 스위치 뒤에 적혀 **한 번도 돌지 않은 시험 12개**가 있었습니다.
4. **새 코드가 실제로 실행되는지 증명하라.** 실행 불가능한 위치에 있던
   구제 코드가 루멘텀 전망을 무너뜨렸습니다.
5. **보고 숫자는 실행 출력에서 복사하라.** 기억으로 적었다가 커밋 메시지에
   테스트 개수를 310으로 적었으나 실제 301이었습니다.
6. **테스트는 종료코드를 직접 확인하라.** 파이프로 넘기면 실패가 가려집니다.
   고친 직후엔 `__pycache__` 를 지우고 돌리세요 — 같은 초에 두 번 고치면
   옛 코드가 그대로 도는 사고가 있었습니다.
7. **끊긴 이력을 이어붙이지 마라.** 몇 년 떨어진 분기를 이웃으로 만들면
   한 푼도 안 늘었는데 "가속"이 나옵니다.
8. **결과를 보고 규칙을 고치지 마라.** 문턱·창·표적은 데이터를 보기 **전에**
   `측정결과.md` 에 등록하고 머지합니다.
9. **표본 편향을 경계하라.** 폭등주만 모으면 기준선이 95%가 되어 뭘 해도
   좋아 보입니다. 부진주를 반드시 섞으세요 — 지금 유니버스(401종목)는
    섞여 있습니다. **종목 수를 이 문서에 박지 마세요**: 79 라고 적힌 것이
    401 이 되도록 아무도 못 고쳤습니다(150차-E·H).
10. **다중 에이전트는 이득이 분명할 때 쓴다** (주인 결정 2026-08-13에
    전면 금지 해제, 2026-08-23에 재확인). 과거 실패는 사실로 남깁니다 —
    원문 130건 분류에 에이전트 13개를 띄웠는데 11개가 실패해 58만 토큰을
    쓰고 결과가 0건이었고, 같은 일을 한 세션에서 전수 처리하니 더
    정확했습니다(조정 EPS 회수 6→13건).
    **그 실패에서 남길 것은 "쓰지 마라"가 아니라 "어디에 쓰느냐"입니다**:
    결정적 산수·파싱·측정은 한 세션 전수 처리가 기본(검증 가능성이 우선),
    병렬이 이득인 일(서로 독립인 조사 등)에는 쓰되 출력을 구조화 형식으로
    받아 **산술 검산으로 교차 확인**합니다. 자세한 실행 규칙은
    `CLAUDE.md` 5장.

운영 단계(134~150차)에서 새로 얻은 것들입니다:

11. **부르는 방법을 짐작하지 말고 호출부를 복사하라.** `judge.run` 의 반환
    모양을 잘못 알고 손으로 재현해 "가설 17개 빠짐"이라는 **가짜 경보**를
    냈습니다(146차). 150차에 `data_health.report(snap)` 로 또 같은 실수를
    했습니다 — 로봇은 `snap["eps"]` 를 넘깁니다. 로봇 경로를 검사할 때는
    `collect_job.py` 에서 **코드를 그대로 뽑아** 돌리세요.
12. **원인을 세 번째로 단정하지 마라.** 수집이 느린 이유를 142차에
    "파싱 병목", 145차에 "보이지 않는 빈 접속"이라 단정했다 둘 다
    실측으로 반증당했습니다. 지금은 **미규명으로 기록**하고 계기를 더
    답니다. 이상한 모양을 봤을 때 **내 계기부터 의심**하세요 — 150차-C 의
    "문턱 아래 7개에 10종목" 은 문턱 아래만 세어 분포가 잘린 탓이었습니다.
13. **고친 뒤에는 그 계산을 쓰는 곳을 전부 훑어라.** 150차-B 에서
    `recent_completion_rows` 를 고쳤을 때 **같은 함수를 부르던 곳**은
    자동으로 고쳐졌지만, **스스로 세던 곳**(`model_verify`)은 조용히 옛
    숫자에 남아 앱과 다른 수를 댔습니다. 둘을 가르는 것은 "같은 함수를
    부르는가" 하나입니다.
14. **시험 수가 줄었는데 초록이면 파일이 안 돈 것이다.** 150차-G 에서
    컨테이너가 턴 중간에 초기화돼 580개 대신 191개만 돌았는데 종료코드는
    0 이고 "모두 통과"였습니다. **돈 파일 수와 총 개수**를 함께 보세요
    (`grep -c "^▶"`).
15. **화면에 박는 숫자는 그때 데이터에서 계산하라.** 설명하려고 "TER 종가
    375.74 < 4주선 385.38" 을 화면에 넣었다가 되돌렸습니다 — 다음 주면
    거짓이 되는 숫자입니다.
16. **주석이 코드와 반대면 낡은 문구가 아니라 잘못된 지시다.** 워크플로에
    "일꾼 수는 3 그대로 둡니다"가 남아 있었는데 코드는 이미 6 이었습니다
    (150차-G). 다음 사람이 그 주석을 믿고 되돌립니다.
17. **한계는 코드 주석·문서·화면 세 곳 모두에 적어라 — 화면을 빼먹기 쉽다.**
    "채택까지 얼마나 남았나"(139·140차)가 계기판에만 있고 웹앱에는 한
    글자도 없었습니다. 주인은 휴대폰만 쓰므로 **정작 보는 화면**에 안
    닿고 있었습니다(150차-C). 두 "정배열"의 뜻이 다른 것도 39차부터
    주석에만 있었습니다(150차-D).
18. **건강검진의 "사라진 분기"는 손실 지표가 아니다.** 589건이 떠서
    놀랐지만 어제 파일과 직접 대조하니 발표일이 "없음 → 실제 날짜"로
    **채워진** 것이었습니다(150차). 놀라기 전에 `git show <옛커밋>:파일`
    로 맞대 보세요.
19. **입력 검사와 글자 찾기만 하는 시험은 초록불만 켜 준다.**
    `collect_one.py` 의 시험 5개가 전부 통과였는데, 그 기능은 **한 번도
    된 적이 없었습니다**(150차-S). 시험이 수집 함수를 한 번도 안
    불렀기 때문입니다 — 종목 코드 모양 검사, 코드에 글자가 있나,
    워크플로에 줄이 있나만 봤습니다. **그 기능의 한가운데 함수를
    실제로 부르지 않으면 시험 개수만 늘고 기능은 안 됩니다.**
20. **"주인이 눌러 봐야 한다"고 적기 전에, 내가 누를 수 있는지 보라.**
    깃허브 액션은 API 로 띄울 수 있습니다. 개발 환경에서 SEC 가 막힌
    것은 *개발 환경*의 제약이지 **깃허브 서버의 제약이 아닙니다.**
    150차-S 에서 직접 띄워 보고서야 결함 셋을 찾았습니다.
21. **파일을 만들었다고 저장소에 올라가는 것이 아니다.** `.gitignore`
    의 `data/*` 에 걸려 요청 수집물이 통째로 버려지고 있었습니다
    (150차-S). 새 폴더에 쓰기 시작하면 `git check-ignore <경로>` 로
    **깃에게 직접 물어보세요.**
22. **실행 스위치 뒤에 시험을 적으면 영원히 안 돈다 — 두 번 났다.**
    1차 12개(위 4장 첫머리), 2차는 `test_parsing.py` 의 **8개**
    (150차-V). 그중 둘은 예전부터 잠들어 있던 것이었습니다. 시험을
    붙였는데 **개수가 안 늘면** 이것부터 의심하세요. 지금은
    `test_실행스위치_뒤에_갇힌_시험이_없다` 가 모든 파일을 훑습니다.
23. **버리기 전에 "다른 자로 잴 수 있는가"를 묻는다.** 보도자료 표의
    단위를 못 찾아 영업이익이 1,000배 작게 들어오던 1,800칸을 계속
    버리고 있었습니다. XBRL 영업이익은 규정상 **절대 달러**라 자로
    쓸 수 있습니다(150차-V). 숫자를 만드는 것이 아니라 **같은 숫자를
    어느 단위로 읽을지**를 다른 자가 정하는 것입니다 — 그것도 **딱
    하나로 정해질 때만**. 자가 없으면 그대로 버립니다.
24. **급성장하는 회사는 "한 분기 ≈ 지난 네 분기 합"이 될 수 있다.**
    누적(YTD) 오염 검사가 그 모양을 잡는데, 1년에 2.5배 크는 회사는
    정상적으로 그렇게 됩니다(CRDO 26 Q2). 버려진 212칸 중 **79칸이
    오탐으로 보입니다**(150차-V). 문턱은 **데이터를 보고 고치지
    마세요** — 사전 등록한 뒤 새 데이터로 판정합니다(헌법 2조).
25. **계기는 재는 대상과 같은 길을 써야 한다.** 진단기가 `_fill_missing_q4`
    를 직접 불러, 실제 수집 경로가 채우기를 그만둬도 "채워졌다"고
    말했습니다(150차-X). **딴 길로 간 계기는 거짓말을 합니다.**
    150차-S(요청 수집기가 로봇과 다른 길로 갔던 것)와 같은 부류입니다.
26. **넓히는 고침에는 "너무 넓히면 깨지는" 시험을 함께 둔다.**
    구멍 메우기 문지기에 GAAP EPS 를 넣을 때, ① 안 넣으면 은행이
    못 들어오는 시험 ② 문지기를 없애면 예비 매출 공지가 들어오는
    시험을 **둘 다** 걸었습니다(150차-AB). 한쪽만 보면 과잉 확장을
    못 잡습니다.
27. **은행·보험은 논갭을 아예 발표하지 않는다.** 조정EPS·논갭 영업이익·
    조정EBITDA 셋만 "이익 숫자"로 인정하면 **GS 의 4분기가 아홉 해
    내리 사라집니다**(150차-AB). XBRL 에도 매출·영업이익이 없어
    뼈대가 GAAP EPS 하나로만 만들어집니다. 업종을 가리는 규칙을 쓸
    때는 **그 셋이 없는 업종**을 꼭 떠올리세요.
28. **돌연변이를 되돌릴 때 `git checkout` 을 쓰지 마라.** 커밋 안 한
    새 시험 6개가 통째로 날아갔습니다(150차-W). 미리 뜬 사본
    (`cp 파일 /tmp/…` → `cp /tmp/… 파일`)으로 되돌리세요.
29. **값을 못 고쳐도 화면이 말하게 하라.** GS 매출은 고칠 자가 없어
    미해결로 남겼지만, 조용히 두면 주인은 그 숫자를 믿습니다.
    `screen_verify` 가 매일 ⚠️ 로 세어 보고합니다(150차-AC).
30. **창·문턱을 시험할 때는 메꿈이 문턱을 넘는지 시험이 스스로 확인하게
    하라.** 먼 단위 선언 시험에 메꿈을 2,700자만 넣었더니 창(3,000자)
    안이라 **가까운 길로 통과**했습니다 — 초록 불인데 아무것도 재지
    않았습니다(150차-AF). 이제 시험 안에서 `assert 글자수 > 3000`
    으로 스스로 지킵니다.
31. **표의 단위 선언을 서술문 숫자에 곱하지 마라.** `(in millions)` 는
    그 **표**의 단위지 문장의 단위가 아닙니다. 창을 넓혀 멀리까지 보게
    했더니 서술문·슬라이드 숫자(202·3·28)가 백만 배로 부풀어 **그럴듯한
    헛값**이 됐습니다(150차-AF, 88차 ④ 재현). 이름과 값 사이가 공백
    **두 칸 이상**인 것만 표의 칸으로 봅니다.
32. **먼 곳의 "billion" 선언은 믿지 마라.** 재무제표 표는 천·백만으로
    적지 10억으로 적지 않습니다. 실측 — 창 밖의 10억 선언 6건이 6건
    모두 틀렸고, Citi 10.35**조** · JPM 100**조** 같은 값을 만들었습니다
    (150차-AF).
33. **"빠진 분기"는 분기가 없는 것이 아니라 발표일이 없는 것이다.**
    XBRL 뼈대에는 분기도 매출도 있는데 8-K 가 안 붙어 발표일이 비어
    있습니다(150차-AH, 실물 ABNB 2025년 네 분기). 측정은 발표일로만
    줄을 세우므로 그 분기가 통째로 빠진 것처럼 보입니다. 구멍을 볼 때는
    **행이 없는지 · 발표일만 없는지** 먼저 가르세요.
34. **계기 숫자를 읽을 때는 분모가 무엇인지 먼저 물어라.** "파싱 실패
    원문 1,013건"은 문제를 부풀려 말했습니다 — 그 안의 **40%는 애초에
    실적 발표가 아닌 공시**(배당·인사·합병)였고, 또 34%는 그 분기가
    이미 다른 경로로 채워져 있었습니다. 진짜로 비어 있고 살릴 수 있는
    것은 **60건**이었습니다(150차-AH).

---

## 5. 다음에 할 일

> ⚠️ **이 문서를 쓸 당시(2026-08-13)의 "v3 재건축 0~6단계"는 전부
> 끝났습니다.** 여기 재건축 표가 "1단계 ▶ 다음"으로 남아 있어 새 세션을
> 이미 끝난 일로 보내고 있었습니다(150차-H에 수리). 아래가 지금 상태입니다.

### 지금은 운영 단계입니다 (2026-08-22 기준)

로봇이 매일 수집·자동 판정하고(`verdict.json`), 계기판(`app.py`)과
웹앱(`docs/`)이 정직화 원칙대로 표시합니다. 사람이 할 일은 **관찰 ·
장치 감사 · 새 가설의 사전 등록**뿐입니다.

| 항목 | 지금 |
|---|---|
| 유니버스 | **401종목** (5차 확장, 149차 — 6차는 사전 등록만, 150차-AR) |
| 등록 가설 | **24개** (H2~H26) |
| 채택된 신호 | **0개** — 화면은 "없음"이라고 말합니다 |
| 시험 | **699개** (150차-AT) |
| 주 잣대 채움률 | 조정 EPS **46.0%** (08-24 실측) — 여기가 지금 가장 큰 구멍 |

### 다음 회차에 이어서 할 것 (2026-08-25 09:30 UTC 기준)

> **가장 먼저: 오늘(08-25) 런의 효과를 세세요.** 이 런이 150차-AP·AS·AT
> 세 차례 파서 고침의 **첫 전체 시험**입니다. 커밋 b57d65e 가 09:15 UTC 에
> 들어갔고 예약은 09:30 이니, 런의 head_sha 가 b57d65e 이후인지부터
> 확인하세요(운영 규칙 ①).

**가. 08-25 런 검증 — 늘어난 것과 함께 "늘지 않았는지"도 셀 것**

견줄 08-24 실측 (401종목 · 15,088행):

    매출 14,631(97.0%) · GAAP EPS 14,282(94.7%) · **조정 EPS 6,948(46.0%)**
    조정 EBITDA 2,287(15.2%) · 발표일 없음 3,549행

⚠️ **이번에는 늘어난 것보다 "안 깨졌는지"가 더 중요합니다.** 오늘 잰 것은
전부 **저장된 실패 원문 2,064건**인데, 그 파일들은 애초에 파싱 실패로
저장된 것이라 **지금 잘 읽히는 보도자료는 표본에 없습니다.** 그런데
전망 창 좁히기(AT)·되돌아보기 창(AS)은 **모든 문서에 영향**을 줍니다.

    세어야 할 것: screen_verify 의 "이웃 튐" · "야후와 갈린 칸" ·
                 "버려진 영업이익·EBITDA" · "빠진 분기" 가 **늘지 않았는가**
    늘었으면 원인을 찾아 되돌립니다 (틀림은 없음보다 위험).

**나. 남은 빈칸 32건** — 이제 **얇게 흩어져** 있습니다:
EL 5 · EOG 3 · SMCI 3 · EW 2 · HPE 2 · IPGP 2 · MET 2 · PEP 2 · VRT 2 ·
ZBH 2, 나머지는 1건씩. **한 꼴을 고쳐 열 건이 풀리던 구간은 끝났습니다.**
할 때는 반드시 원문 2,064건 전수 대조로 손해를 함께 세세요
(`/tmp/.../parse_all.py` 방식 — 150차-AP 주석 참조).

⚠️ 일감을 셀 때 **"없음"의 두 갈래를 섞지 마세요**: "회사가 안 낸 것"과
"우리가 못 읽는 것"은 다른 일입니다. 값 없이 정의만 적는 문단
(STRL "Non-GAAP measures may include adjusted EPS…")을 일감으로 세면
숫자가 두 배로 부풀어 보입니다(150차-AT 정정).

**다. 6차 확장은 사전 등록만 해 두었습니다** (150차-AR → **조건은
150차-AY 에서 바뀌었습니다**). 조건: **일감(못 붙였고 그 회사는 냄)이
1,000칸 아래로** 내려오면 **+50종목**부터. 지금 2,134칸.
⚠️ 옛 조건이던 "채움률 80%" 는 **도달할 수 없는 숫자**여서 폐기했습니다 —
일감을 하나도 남김없이 다 풀어도 64.1%, 낙관적으로 봐도 72.9% 가 천장입니다
(조정 EPS 는 논갭이라 회사가 안 내면 어디에도 없습니다).
계산 근거 — 종목 +100 은 표본 1.25배(첫날 벽시계 304분, 한계 350)인데
빈칸 46%→80% 은 1.74배이고 수집 시간이 안 늘어납니다.
**정직하게: 둘을 다 해도(2.17배) 지금 가설은 채택되지 않습니다.**
가장 가까운 H9 조차 6.8배가 필요합니다 — 필요한 것은 표본이 아니라
**더 나은 가설**입니다.

**라. 앱에서 온 수집 요청** (150차-AQ). 없는 종목을 검색하면 요청장이
열리고 로봇이 받아 옵니다. **아직 실물로 한 번도 안 돌아 봤습니다** —
주인이 처음 쓸 때 `collect_request.yml` 이 실제로 깨어나는지,
이슈 권한(`issues: write`)이 실제로 통하는지 확인하세요.

**마. 2016~2018 옛 분기의 발표일** (150차-AH). 캐시가 차면 나아지는
종류라 며칠 더 지켜봅니다.

**바. 누적(YTD) 오탐** (150차-V). 고칠 규칙은 사전 등록 뒤 판정합니다.


### (옛 목록 — 위로 옮겨졌습니다)

1. **오늘 정기 런의 효과 세기** — 150차-V·W·Y·AB 는 전부 보도자료를
   다시 읽어야 효과가 납니다. 09:30 UTC 런이 끝나면(13:30~15:30 UTC)
   **몇 칸이 실제로 살아났는지 세어** 측정결과.md 에 적으세요.
   견줄 어제 숫자: 버려진 영업이익·EBITDA **1,800칸** · 빠진 분기
   **170건** · 어긋난 분기 이름 **652칸**.
2. ~~자기 단위를 말하는 값을 앞세우기~~ → **150차-AF 에서 다른 방식으로
   해결했습니다.** 산문을 앞세우는 안은 전수로 재 보니 442건/141종목이
   바뀌면서 **멀쩡한 표 값이 서술문에 밀려 깨졌습니다**(AAL 9,420 → 201,
   ABNB 37억 → 4.59억). 대신 ① 괄호 없는 단위 선언(`$ in millions`·
   줄머리 `In millions`)을 알아보게 하고 ② 부문 줄 대신 합계 줄
   (`Total net revenues/sales`)을 먼저 묻고 ③ 창 밖 단위 선언을
   **표의 칸 + 10억 아님** 조건으로만 쓰게 했습니다. GS 매출이
   드디어 172.27억으로 들어옵니다.
   ⚠️ **효과는 다음 수집 런부터 납니다** — 보도자료를 다시 읽어야
   반영되므로, 런이 끝나면 GS·AMZN·MCK·MET·LLY·MCD·CMCSA·NFLX 의
   매출이 실제로 제자리인지 세어 보세요.
3. **누적(YTD) 오탐 79칸** (150차-V). 지금 검사는 "직전 4분기 합과
   비슷하면 누적"인데 급성장 회사가 정상적으로 그 모양이 됩니다.
   고칠 규칙은 **사전 등록한 뒤 새 데이터로만** 판정합니다(헌법 2조).
4. **주 잣대(조정 EPS)에 연간값이 남아 있다** (150차-AD, **미해결**).
   실물 SHW: `24 Q1 2.17 · Q2 3.70 · Q3 3.37 · **Q4 11.33**` — 네 해
   내리 4분기 자리에 연간값. GAAP 쪽은 검사가 잡는데 조정 EPS 만
   빠져나갑니다. 원인은 확정됐습니다 — 검사가 쓰는 "직전 4분기 합"이
   **이미 오염돼** 있어(작년 연간값 포함) 첫 번째를 놓치면 영원히
   못 잡습니다.
   ⚠️ **"4 × 최근 중앙값"으로 바꾸는 안은 측정해 보고 물렀습니다** —
   AMD 의 평범한 분기 EPS 와 크루즈·정유의 진짜 계절성을 죽였습니다.
   다시 설계할 때는 **오염에 안 닿는 기준**(회계 달력 구조)을 쓰세요.
5. **`MAX_8K_SCAN = 200` 상한** — GS 처럼 공시가 잦은 회사는 옛 분기가
   잘립니다. 올리면 수집 시간이 늘어나므로 **런의 시간 예산을 보고**
   판단하세요(`robot_log.json` 의 벽시계_분 · 예산 190분 · 워크플로
   한도 300분).

### 지금 기다리는 것

- **H26 첫 판정** — 유니버스로 검증하는 가설(143차 등록). 창 125거래일
  첫 돌파를 **탐색에 쓰지 않은 새 151종목으로만** 판정합니다. 표본이
  쌓이려면 회차가 더 필요합니다(144차 표본 계획).
- **원문 대기** — YTD 누적 오염(ED·KEY) · 단위 혼선 · 잣대 구멍.
  부탁 목록 112건이 다음 런에서 원문을 담아 옵니다(150차).

### 미규명으로 남겨 둔 것 (단정 금지)

- 수집 시간의 정체 — GS 가 접속 시도 0인데 1,331초. 142·145차에 두 번
  단정했다 반증당했으므로 **세 번째는 하지 않습니다**(4장 12번).
- 잣대 사다리 문턱(8) 바로 아래 7개에 10종목이 몰리는 모양(150차-C).
  잡음 범위를 벗어났다고 말할 수 없습니다. 다음 수집에서 다시 봅니다.

### 웹앱 주소 (2026-08-23 확인)

**https://j2h5516-star.github.io/Model/**

GitHub Pages 는 **이미 켜져 있습니다** — `main` 브랜치의 `/docs` 폴더에서
배포됩니다. 주인이 켜 두었고 화면에 "Your site is live at …" 이 떠
있는 것을 확인했습니다.

> ⚠️ **세션들이 "Pages 를 켜세요"를 여러 회차에 걸쳐 반복해 말했습니다 —
> 틀린 안내였습니다**(150차-R). 개발 환경은 `github.io` 가 조직 정책으로
> 차단(403)돼 있어 **세션이 사이트를 직접 열어볼 수 없습니다.** 못 여는
> 것을 "안 켜져 있다"로 넘겨짚지 마세요. 주인에게 물어보거나, 저장소
> Settings 화면을 주인이 보여 주기 전에는 **모른다고 하세요.**

경로는 전부 상대경로(`data/app.json` · `./index.html`)라 `/Model/`
하위에서도 정상입니다 — 절대경로를 새로 넣지 마세요.

### 사람(주인)이 해야 할 것

**지금은 없습니다.** 세션이 깃허브 액션을 직접 띄울 수 있습니다
(깃허브 MCP `actions_run_trigger`). 개발 환경에서 SEC 가 막힌 것은
*개발 환경*의 제약일 뿐, **깃허브 서버는 안 막혀 있습니다** — 워크플로는
거기서 돕니다. 150차-S 에서 `collect-one` 을 직접 띄워 결함 셋을
찾았습니다.

> ⚠️ **"주인이 눌러 봐야 한다"고 적기 전에 내가 누를 수 있는지부터
> 확인하세요.** 못 하는 일과 남에게 시킬 일은 다릅니다.

주인이 **원하면** 할 수 있는 것 (필수 아님):

- **`SEC_IDENTITY` 시크릿** — 지금은 비어 있고 `config.py` 기본값으로
  잘 돕니다(NVDA 39건 수집 확인). 이메일을 바꾸고 싶을 때만
  Settings → Secrets → Actions 에 넣으면 됩니다.
- **`collect-one` 직접 쓰기** — Actions → collect-one → Run workflow →
  종목 코드 → Run. 한 종목이면 1분 남짓입니다.

---

## 6. 로봇이 도는지 확인하는 법

- 워크플로: `.github/workflows/collect.yml` (매일 09:30 UTC + 수동 실행)
- 결과: `data/measure/` 아래로 로봇이 직접 커밋 (`자동 수집 — 날짜`)
- 로봇이 반쯤 실패한 날은 종료코드 1로 끝나 **그날 커밋을 막습니다**
  (깨진 데이터로 덮어쓰지 않기)
- 실행 기록은 깃허브 액션 로그와 `data/measure/robot_log.json` 양쪽에 남습니다
