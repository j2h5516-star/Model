"""
config.py — 대시보드 전체 설정값 모음
====================================

종목 목록, 점수 배점, 판정 기준선 등 "바꿀 만한 숫자"는 전부 여기에 모아 두었습니다.
다른 파일을 건드리지 않고 이 파일만 고치면 동작을 조절할 수 있습니다.
"""

from __future__ import annotations

import os

# 설정 파일의 판 번호.
# 코드를 업데이트했는데 앱이 옛 설정 파일을 메모리에 붙들고 있으면
# "그런 항목이 없다(AttributeError)"며 앱이 죽습니다.
# app.py가 이 번호를 확인해, 그런 경우 빨간 오류 대신 재시작 안내를 보여줍니다.
# ⚠️ config에 새 항목을 추가하고 app.py가 그것을 쓰기 시작하면 이 번호를 1 올리세요.
CONFIG_VERSION = 4

# ---------------------------------------------------------------------------
# 분석 대상
# ---------------------------------------------------------------------------

# 분석할 종목 (티커 = 미국 주식 종목 코드)
TICKERS: list[str] = [
    "CRDO",  # Credo Technology — 고속 인터커넥트
    "CLS",   # Celestica — 전자제품 위탁생산
    "MU",    # Micron Technology — 메모리 반도체
    "SNDK",  # SanDisk — 낸드 플래시
    "LITE",  # Lumentum — 광통신 부품
    "STRL",  # Sterling Infrastructure — 인프라 건설
    "MXL",   # MaxLinear — 통신 반도체
    "COHR",  # Coherent — 광학·레이저
    "TTMI",  # TTM Technologies — 인쇄회로기판
    "ICHR",  # Ichor Holdings — 반도체 장비 부품
    "AMD",   # Advanced Micro Devices — CPU/GPU
    "PLTR",  # Palantir — 데이터 분석 소프트웨어
    "ZETA",  # Zeta Global — 마케팅 소프트웨어
]

# 상대강도(RS) 비교 기준이 되는 지수 ETF
BENCHMARK = "SPY"  # S&P 500 지수를 따라가는 ETF

# 실적을 "수집"하는 시작일 — 3년치를 모읍니다.
# 가속/감속 판정을 안정적으로 하려면 표본이 많아야 하기 때문입니다.
#   · QoQ 증가율 2개(최소 조건)  → 3분기
#   · 1년 전 대비(YoY) 비교        → 5분기
#   · 증가율 추세(회귀 기울기)     → 5~6분기
#   · 3년(12분기)이면 위 전부 가능하고 계절성도 2번 관찰됩니다
HISTORY_START_DATE = "2023-01-01"

# 화면에 "기본으로" 보여줄 시작일 (그 이전 자료는 '전체 이력 보기'에서 확인)
DISPLAY_START_DATE = "2025-01-01"

# 옛 이름 호환용 (외부에서 참조하던 코드가 깨지지 않도록)
EARNINGS_START_DATE = HISTORY_START_DATE

# 한 종목당 살펴볼 8-K 공시 최대 건수.
# 8-K는 실적 외에도 임원 변경·계약 체결 등으로 자주 올라와서, 전부 내려받으면
# 매우 느립니다. 실적발표는 분기 1회이므로 이 정도면 3년치를 충분히 덮습니다.
# (실전에서 45건으로는 3년치를 못 덮는 종목이 있어 60건으로 올렸습니다)
MAX_8K_SCAN = 60

# 실적 숫자를 이만큼 확보하면 더 스캔하지 않고 멈춥니다 (3년 × 4분기 + 여유 2)
EARLY_STOP_PARSED = 14

# 분기 종료일과 8-K 제출일이 이 일수 안이면 같은 분기의 발표로 짝짓습니다.
# 실적발표는 보통 분기 종료 후 2~8주이지만, 회계연도 마지막 분기는
# 10-K와 함께 늦게 나오는 회사가 있어 넉넉히 잡습니다.
PAIRING_WINDOW_DAYS = 120

# 대시보드에 담을 수 있는 최대 종목 수.
# 너무 많으면 수집 시간이 길어지고 야후가 요청을 거부할 수 있습니다.
MAX_TICKERS = 30

# SEC 전자공시(EDGAR)는 요청자 신원을 헤더에 담아 보내도록 요구합니다.
#
# 저장소가 공개되어 있으므로 개인 이메일을 코드에 직접 적지 않습니다.
# 대신 아래 순서로 찾습니다:
#   ① 환경변수 SEC_IDENTITY (Streamlit Cloud의 Secrets에 넣으면 비공개로 유지됩니다)
#   ② 없으면 아래 기본값 (프로젝트 주소를 연락처로 사용)
#
# 💡 Streamlit Cloud에서 본인 이메일을 비공개로 설정하는 방법:
#    앱 화면 오른쪽 아래 ⋮ → Settings → Secrets 에 아래 한 줄을 넣고 저장
#        SEC_IDENTITY = "이름 본인이메일@example.com"
#    Secrets는 저장소에 올라가지 않아 다른 사람이 볼 수 없습니다.
DEFAULT_SEC_IDENTITY = "Trend Dashboard (github.com/j2h5516-star/Model)"


def get_sec_identity() -> str:
    """SEC에 보낼 요청자 신원을 돌려줍니다 (환경변수 우선)."""
    return os.environ.get("SEC_IDENTITY", "").strip() or DEFAULT_SEC_IDENTITY


# 예전 이름 호환용 (다른 파일에서 참조하던 코드가 깨지지 않도록)
SEC_IDENTITY = get_sec_identity()

# 내려받은 SEC 파일을 저장해 둘 폴더 (재실행 시 다시 받지 않음)
CACHE_DIR = "data"

# 캐시 유효 기간 (일). 이 기간이 지나면 새로 받아옵니다.
CACHE_TTL_DAYS = 1


# ---------------------------------------------------------------------------
# 주가 추세 설정 (v1에서 이어받은 값)
# ---------------------------------------------------------------------------

MA_WINDOWS = [4, 13, 26, 52]      # 주봉 이동평균선 기간 (주)
NEUTRAL_BAND_PCT = 3.0            # 26주선 ±3% 이내 = 중립
SLOPE_LOOKBACK_WEEKS = 4          # 26주선 기울기: 4주 전과 비교
SLOPE_FLAT_BAND_PCT = 0.3         # ±0.3% 이내 변화는 "횡보"
RS_LOOKBACK_WEEKS = 13            # 상대강도: 최근 13주 수익률 비교

# 추세 5단계 이름
S_FULL_UP = "완전 정배열"
S_SEMI_UP = "준정배열"
S_NEUTRAL = "중립"
S_DAMAGED = "추세 훼손"
S_FULL_DOWN = "완전 역배열"
S_UNKNOWN = "판정 불가"

# 추세 단계 번호 (1이 가장 강함 ~ 5가 가장 약함). 매트릭스 판정에 사용
TREND_STAGE = {
    S_FULL_UP: 1,
    S_SEMI_UP: 2,
    S_NEUTRAL: 3,
    S_DAMAGED: 4,
    S_FULL_DOWN: 5,
    S_UNKNOWN: 6,   # 판정 불가는 별도 취급
}


# ---------------------------------------------------------------------------
# 펀더멘털 점수 배점 (합계 100점)
# ---------------------------------------------------------------------------

W_DELTA_ACCEL = 40      # 델타 가속 — 이익 증가 속도가 빨라지는가
W_GM_DRIVER = 25        # GM% 드라이버 — 마진 개선이 어떤 성격인가
W_REVENUE_QUALITY = 20  # 매출 성장의 질
W_FORWARD = 15          # 포워드 신호 — 다음 분기 전망

# "저기저(저가 기저) 함정" 방지:
# 최근 분기 논갭 영업이익이 이 금액 미만이면 델타 가속 점수를 절반까지만 인정
LOW_BASE_THRESHOLD_USD = 50_000_000   # $50M
LOW_BASE_CAP_RATIO = 0.5              # 상한 50%

# --- 가속/감속 판정 방식 ---
# 직전 분기 하나와만 비교하면 한 분기만 튀어도 판정이 뒤집힙니다.
# 그래서 두 가지 신호를 함께 봅니다:
#   ① 단기 신호 = 최근 QoQ 증가율 − 직전 QoQ 증가율   (민감함)
#   ② 추세 신호 = 최근 여러 QoQ 증가율의 회귀 기울기   (안정적)
# 두 신호가 같은 방향이면 가속/감속, 엇갈리면 "혼조"로 표시합니다.
DELTA_THRESHOLD_PP = 3.0        # ±3%p 를 넘어야 방향이 있다고 봅니다
DELTA_TREND_WINDOW = 4          # 회귀 기울기를 낼 때 사용할 QoQ 개수
DELTA_TREND_THRESHOLD = 1.5     # 회귀 기울기가 이 값을 넘어야 방향이 있다고 봅니다

# 델타 방향 이름
D_ACCEL = "가속"
D_STEADY = "유지"
D_DECEL = "감속"
D_MIXED = "혼조"        # 두 신호가 엇갈림 — 억지로 한쪽으로 밀지 않습니다
D_UNKNOWN = "판단불가"

# 방향별로 배점에 곱할 비율
DELTA_RATIO = {
    D_ACCEL: 1.0,
    D_STEADY: 0.6,
    D_MIXED: 0.45,
    D_DECEL: 0.25,
    D_UNKNOWN: 0.4,
}

# --- 델타 예측 (다음 분기 전망치로 앞으로의 방향을 미리 봅니다) ---
F_ACCEL_KEEP = "가속 지속 ↗↗"
F_ACCEL_SLOW = "가속 둔화 ↗↘"
F_DECEL_KEEP = "감속 지속 ↘↘"
F_REBOUND = "반등 ↘↗"
F_FLAT = "비슷 →"
F_NONE = "-"

# --- 델타가속예측 (다음 분기 + 다다음 분기, 2분기 경로) ---
# 가이던스(다음 분기)와 월가 컨센서스(다다음 분기)로 증가율 경로를 미리 그려서
# "가속이 언제까지 이어질 것 같은지"를 판정합니다.
F2_ACCEL_KEEP = "가속 지속 ↗↗"      # 두 분기 모두 증가율이 계속 올라감
F2_ACCEL_SLOW = "가속 후 둔화 ↗↘"   # 다음 분기는 가속, 그 뒤 꺾임 — 정점 근처 신호
F2_DECEL_KEEP = "둔화 지속 ↘↘"      # 두 분기 모두 증가율이 계속 내려감
F2_REBOUND = "둔화 후 반등 ↘↗"      # 다음 분기 꺾였다가 그 뒤 회복 — 바닥 신호
F2_FLAT = "완만 →"                   # 큰 변화 없음
F2_NONE = "-"

# --- 리비전 속도 (애널리스트 추정치가 30일간 몇 % 움직였나) ---
# 상향/하향 "건수"보다 추정치 자체의 변화율이 더 정밀한 신호입니다.
# ±이 값(%)에서 포워드 리비전 점수가 최저/최고가 됩니다.
REVISION_VELOCITY_FULL_PCT = 5.0

# GM% 드라이버 구간별 점수 (최근 분기 GM% − 직전 4개 분기 평균 GM%, 단위 %p)
GM_VOLUME_BAND = 1.0    # ±1%p 이내 = 물량형 (가장 건강)
GM_MIX_BAND = 3.0       # +1 ~ +3%p = 믹스형
GM_SCORE_VOLUME = 25    # 물량형 만점
GM_SCORE_MIX = 15       # 믹스형
GM_SCORE_PRICE = 5      # +3%p 초과 = 가격형 (지속성 낮음)
GM_SCORE_DECLINE = 8    # −1%p 넘는 하락


# ---------------------------------------------------------------------------
# 기술 점수 배점 (합계 100점)
# ---------------------------------------------------------------------------

W_TREND = 50        # 추세 5단계
W_SLOPE = 10        # 26주선 기울기
W_DISPARITY = 15    # 이격도
W_RS = 25           # 상대강도

# 추세 단계별 점수 (50점 만점을 5단계로 균등 배분)
TREND_SCORES = {
    S_FULL_UP: 50.0,
    S_SEMI_UP: 37.5,
    S_NEUTRAL: 25.0,
    S_DAMAGED: 12.5,
    S_FULL_DOWN: 0.0,
    S_UNKNOWN: 0.0,
}

# 26주선 기울기별 점수
SLOPE_SCORES = {"상승": 10.0, "횡보": 5.0, "하락": 0.0, "-": 0.0}

# 이격도 점수 기준
DISPARITY_OVERHEAT_PCT = 40.0   # +40% 초과 = 과열
DISPARITY_SCORE_NORMAL = 15.0   # 0 ~ +40% = 정상
DISPARITY_SCORE_OVERHEAT = 5.0  # 과열
DISPARITY_SCORE_NEGATIVE = 0.0  # 26주선 아래

# 상대강도(RS) 구간별 점수 — SPY 대비 13주 초과수익률(%p) 기준
# (아래에서부터 순서대로 검사해 처음 해당하는 구간의 점수를 줍니다)
RS_SCORE_BANDS = [
    (20.0, 25.0),   # +20%p 초과 → 25점 (시장을 크게 이김)
    (10.0, 20.0),   # +10 ~ +20%p → 20점
    (0.0, 14.0),    # 0 ~ +10%p → 14점 (시장보다 약간 나음)
    (-10.0, 7.0),   # -10 ~ 0%p → 7점
    (float("-inf"), 0.0),  # -10%p 미만 → 0점
]


# ---------------------------------------------------------------------------
# 최종 점수 및 매트릭스 판정
# ---------------------------------------------------------------------------

WEIGHT_FUNDAMENTAL = 0.6   # 최종점수 = 펀더 × 0.6 + 기술 × 0.4
WEIGHT_TECHNICAL = 0.4

FUND_STRONG = 70.0   # 펀더멘털 강함 기준선
FUND_WEAK = 40.0     # 펀더멘털 약함 기준선

# 판정 이름
V_BUY = "매수 후보"
V_WARN = "⚠️ 경고"
V_WATCH = "관찰"
V_MOMENTUM = "펀더 없는 모멘텀"
V_EXCLUDE = "제외"

# 판정별 색상 (다크 테마 기준)
VERDICT_COLORS = {
    V_BUY: "#22c55e",       # 초록
    V_WARN: "#f59e0b",      # 주황
    V_WATCH: "#3b82f6",     # 파랑
    V_MOMENTUM: "#a78bfa",  # 보라
    V_EXCLUDE: "#6b7280",   # 회색
}

# 데이터 출처 배지 이름
SRC_DIRECT = "직접공시"   # 보도자료에 논갭 수치가 그대로 있음
SRC_DERIVED = "역산"      # 매출 × GM% − opex 로 계산
SRC_APPROX = "근사치"     # XBRL GAAP 값 + 주식보상비 등으로 근사
SRC_GUIDANCE = "가이던스"  # 회사가 제시한 다음 분기 전망
SRC_ESTIMATE = "추정"     # 컨센서스 매출 × 과거 평균 마진
SRC_NONE = "없음"         # 자료를 구하지 못함


# ---------------------------------------------------------------------------
# 데이터 신뢰도 등급 (6단계)
# ---------------------------------------------------------------------------
# 숫자를 "어떻게 구했는가"에 따라 얼마나 믿을 만한지를 백분율로 매깁니다.
# ⚠️ 이 퍼센티지는 자료의 성격을 고려해 정한 값이며 통계로 검증된 수치가 아닙니다.
#    필요하면 이 표의 숫자만 바꾸면 화면 전체에 반영됩니다.
CONFIDENCE_TIERS = [
    # (순위, 종류, 신뢰도%, 한 줄 설명, 계산 방법)
    (1, SRC_DIRECT, 95, "회사 보도자료의 논갭 영업이익 원문 그대로",
     "보도자료의 `Non-GAAP operating income` 항목을 그대로 사용"),
    (2, SRC_GUIDANCE, 85, "회사가 직접 발표한 다음 분기 전망",
     "가이던스 매출(범위면 중간값) × 영업마진% 또는 매출 × GM% − 영업비용"),
    (3, SRC_DERIVED, 75, "공시된 매출·마진·비용으로 계산 (입력값은 모두 공시 원문)",
     "매출 × non-GAAP GM% − non-GAAP 영업비용"),
    (4, SRC_APPROX, 55, "XBRL 회계데이터로 만든 근사치 (공식 논갭과 다를 수 있음)",
     "GAAP 영업이익 + 주식보상비 + 무형자산상각"),
    (5, SRC_ESTIMATE, 40, "애널리스트 매출 전망에 과거 마진을 곱한 통계적 추정",
     "다음 분기 매출 컨센서스 × 최근 4개 분기 평균 non-GAAP 영업마진"),
    (6, SRC_NONE, 0, "자료를 구하지 못함", "-"),
]

# 종류 → 신뢰도% 로 바로 찾을 수 있게 만든 표
CONFIDENCE_PCT = {tier[1]: tier[2] for tier in CONFIDENCE_TIERS}
CONFIDENCE_RANK = {tier[1]: tier[0] for tier in CONFIDENCE_TIERS}

# 종합 신뢰도 = 실적 신뢰도 × 0.7 + 전망 신뢰도 × 0.3
CONF_WEIGHT_ACTUAL = 0.7
CONF_WEIGHT_FORWARD = 0.3

# 실적 신뢰도를 낼 때 최근 분기에 더 큰 가중치를 줍니다 (최근일수록 중요)
CONF_RECENCY_DECAY = 0.75   # 한 분기 과거로 갈 때마다 가중치 × 0.75

# 화면 표시용 신뢰도 색상 경계
CONF_HIGH = 80.0    # 이 이상이면 초록
CONF_MID = 55.0     # 이 이상이면 노랑, 미만이면 회색
