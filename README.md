# 📊 주식 추세추종 대시보드 v2

미국 주식 13개 종목의 **실적(SEC 공시)** 과 **주가 추세**를 자동으로 모아
종목별 종합 점수(100점)를 매기고 5가지로 판정해 주는 웹 대시보드입니다.

**무료 데이터만 사용하고, 손으로 입력할 값은 하나도 없습니다.**

**대상 종목**: CRDO, CLS, MU, SNDK, LITE, STRL, MXL, COHR, TTMI, ICHR, AMD, PLTR, ZETA
**비교 지수**: SPY (S&P 500 ETF)

---

## 📱 모바일에서 사용하기 (권장)

스마트폰에는 파이썬을 설치할 수 없지만, **무료 클라우드에 한 번만 올려두면**
그 뒤로는 폰 브라우저에서 주소만 열면 됩니다. 아래 과정은 전부 **폰에서만** 가능합니다.

### 최초 1회: Streamlit Community Cloud에 올리기 (약 5분, 무료)

1. 폰 브라우저에서 **[share.streamlit.io](https://share.streamlit.io)** 접속
2. **Continue with GitHub** 로 로그인 (권한 창이 나오면 **Authorize**)
3. **Create app** 버튼 클릭
4. 아래처럼 입력:
   - **Repository**: `j2h5516-star/Model`
   - **Branch**: `main`
   - **Main file path**: `app.py`
5. **Deploy** 를 누르고 기다립니다
6. 주소창의 `https://○○○.streamlit.app` 이 내 대시보드 주소입니다

> ⚠️ 저장소가 **비공개(private)** 면 Streamlit이 "This repository does not exist"
> 라고 표시합니다. GitHub → Settings → 맨 아래 Danger Zone →
> **Change visibility → Change to public** 으로 바꾸면 해결됩니다.

### 홈 화면에 추가하면 앱처럼 쓸 수 있어요

- **아이폰(Safari)**: 하단 공유 버튼(⬆️) → **홈 화면에 추가**
- **안드로이드(Chrome)**: 오른쪽 위 ⋮ → **홈 화면에 추가**

### 알아두면 좋은 점

- **첫 실행은 1~3분 걸립니다.** SEC에서 13개 종목의 실적 공시를 모두 읽어오기 때문입니다.
  이후에는 저장된 데이터를 재사용해 훨씬 빠릅니다.
- 며칠간 접속이 없으면 앱이 잠자기 모드가 됩니다.
  "get this app back up" 버튼을 누르고 1분 기다리면 깨어납니다.
- 최신 데이터로 다시 계산하려면 왼쪽 위 **☰ 메뉴** → **🔄 데이터 새로고침**
- 표는 **좌우로 밀어서(스와이프)** 나머지 열을 볼 수 있습니다.

---

## 💻 컴퓨터에서 실행하기 (선택 사항)

### 1단계. 파이썬 설치 (이미 있다면 건너뛰기)

[python.org/downloads](https://www.python.org/downloads/) 에서 내려받아 설치합니다.
⚠️ Windows는 설치 첫 화면에서 **"Add Python to PATH"** 에 꼭 체크하세요.

### 2단계. 터미널 열기

- **Windows**: `윈도우 키 + R` → `cmd` → 엔터
- **Mac**: `Command + 스페이스` → `터미널` → 엔터

### 3단계. 프로젝트 폴더로 이동

```bash
cd 내려받은폴더경로
```

### 4단계. 필요한 프로그램 설치 (최초 1회)

```bash
pip install -r requirements.txt      # Windows
pip3 install -r requirements.txt     # Mac
```

### 5단계. 실행

```bash
streamlit run app.py
```

> 명령을 못 찾는다고 나오면 — Windows: `python -m streamlit run app.py`,
> Mac: `python3 -m streamlit run app.py`
>
> 처음 실행 시 터미널에 `Email:` 이라고 물어보면 **엔터만** 누르면 됩니다.

종료하려면 터미널에서 `Ctrl + C` 를 누릅니다.

---

## 🖥️ 화면 구성

| 영역 | 설명 |
|---|---|
| **종목 관리 (메인 화면 맨 위)** | 티커를 입력해 **직접 추가/삭제**. 사이드바로 들어갈 필요 없이 순위 표 위에서 바로. 목록은 주소(URL)·서버에 저장돼 다음에 열어도 유지됩니다 |
| **종목 카드** | 최종점수 + 판정·출처 배지 + **데이터 신뢰도 게이지** |
| **순위 표** | 종목·**주가($)**·최종점수·펀더·기술·신뢰도·추세·마진 성격·델타방향·**국면**·델타예측·**델타가속예측**·상대강도·판정. **행을 누르면** 아래 상세로 이동 |
| **4사분면 매트릭스** | 점 안에 **순위 번호**, 종목명은 아래 범례 (종목이 많아도 글자가 겹치지 않습니다) |
| **종목 상세** | **8개 버튼**을 눌러 계산 과정 확인 + 분기별 이익 막대·증가율 선, 마진 추이, 원본 숫자 표 |
| **신뢰도 등급표** | 6단계 등급과 각각의 계산 방법 |
| **수집 진단** | SEC에서 자료를 어디까지 가져왔는지 단계별 건수 |
| **지표 해석 가이드** | 용어 설명과 점수 산식 |

### 📌 종목 직접 추가하기

**메인 화면 맨 위 `📌 종목 관리`** 에서 티커(예: `NVDA`)를 입력하고 **➕ 추가**를 누르면
즉시 비교 분석에 들어갑니다. 실존하지 않는 티커는 자동으로 걸러집니다.
아래 알약 모양 종목 이름을 누르면 삭제됩니다.

> 💡 종목을 바꾸면 **주소(URL)** 가 함께 바뀝니다. 그 주소를 홈 화면에 추가해두면
> 다음에 열어도 그대로 유지되고, 링크로 다른 기기에 공유할 수도 있습니다.

> ⚡ 종목별로 따로 저장하기 때문에, 하나를 추가해도 **새 종목만** 받아옵니다(수 초).
> 야후가 일시적으로 거부하면 자동으로 한 번 더 시도하고, 추가 직후 그 종목의
> 수집 결과(분기 몇 개, 신뢰도 몇 %)를 바로 보여줍니다.

### 💾 종목 목록을 영구 저장하기

메인 화면 종목 관리 칸의 **💾 이 목록 저장** 버튼을 누르면 서버와 주소(URL)에 동시에 저장되어,
앱을 다시 켜도 그 목록으로 시작합니다.

- **기본 저장**: 별도 설정 없이 동작합니다. 단, 코드를 새로 올리면(재배포) 초기화될 수 있습니다.
- **완전 영구 저장** (선택): GitHub 토큰을 넣으면 저장 버튼이 목록을 저장소에 직접
  커밋해 어떤 경우에도 유지됩니다.
  1. GitHub → Settings → Developer settings → **Fine-grained tokens** → Generate
     (이 저장소만, Contents: Read and write 권한)
  2. Streamlit 앱 → ⋮ → Settings → **Secrets** 에 한 줄 추가:
     `GITHUB_TOKEN = "발급받은토큰"`
  3. 이후 저장 버튼을 누르면 "GitHub 저장소에 저장했습니다"가 표시됩니다

종목은 최대 **30개**까지 담을 수 있습니다 (수집 시간과 야후 요청 제한 때문입니다).

### 🔎 "이 숫자 어떻게 나온 거야?" — 눌러서 확인하기

종목 상세 화면의 버튼을 누르면 각 항목이 **어떻게 계산됐는지** 볼 수 있습니다.

| 버튼 | 눌렀을 때 보이는 것 |
|---|---|
| 📄 **출처** | 직접공시/역산/근사치 중 어떤 방법인지, 실제 계산식, **SEC 원문 공시 링크** |
| 🔮 **전망** | 가이던스인지 추정인지, 계산에 쓴 매출·마진 숫자, 자료 출처 |
| 🏷️ **판정** | 적용된 규칙, 펀더·추세 실제 값, 판정 5종 규칙표(현재 판정 강조) |
| 📊 **GM% 드라이버** | 최근 GM% vs 직전 4분기 평균, 구간별 점수표, **분기별 마진 이력** |
| ⚡ **델타** | 분기별 **1년치(TTM) 이익과 증가율** 이력, **두 신호(단기·추세)** 계산 과정, 그 방향을 과거에 실제로 몇 % 맞혔는지, **다음 분기 예측** |
| 🔄 **국면** | 지금 1년치 이익 vs 과거 최고, 사이클 어디에 서 있는지, 국면별 검증 결과 |
| 📈 **기술 점수** | 추세·기울기·이격도·상대강도 4개 항목의 배점 내역 |
| 🎯 **신뢰도** | 분기별 출처와 각 신뢰도, 가중평균 계산 과정 |

> 참고: v2에서는 캔들차트와 거래량 차트를 뺐습니다. 추세는 5단계 판정과
> 기술 점수로 요약해서 보여줍니다.

---

## 🧮 점수는 이렇게 계산됩니다

### 펀더멘털 100점 (최종점수의 60%)

| 항목 | 배점 | 무엇을 보나 |
|---|---|---|
| **델타 가속** | 30 | 이익이 "얼마나" 늘었나가 아니라 **늘어나는 속도가 빨라지는가** |
| **국면** | 10 | 이익 사이클에서 **지금 어디에 서 있는가** (바닥 통과 / 신고점 / 고점 이탈) |
| **GM% 드라이버** | 25 | 마진 개선이 물량형/믹스형/가격형 중 어느 것인가 |
| **매출 성장의 질** | 20 | 매출이 크게, 그리고 가속하며 크는가 |
| **포워드 신호** | 15 | 다음 분기 전망이 이번 분기보다 좋은가 + 애널리스트 상향/하향 |

**GM% 드라이버 구간** (최근 분기 GM% − 직전 4개 분기 평균)

| 변화 | 성격 | 점수 | 뜻 |
|---|---|---|---|
| ±1%p 이내 | 물량형 | 25 | 제품이 잘 팔려 규모가 커진 것 — **가장 건강** |
| +1 ~ +3%p | 믹스형 | 15 | 수익성 좋은 제품 비중이 늘어난 것 |
| +3%p 초과 | 가격형 | 5 | 가격 인상 효과 — 오래 못 갈 수 있음 |
| −1%p 초과 하락 | 마진 악화 | 8 | 수익성이 나빠지는 중 |

> 💡 **저기저(低基底) 함정 방지**: 최근 분기 논갭 영업이익이 **$50M 미만**이면
> 델타 가속 점수를 **절반(15점)까지만** 인정합니다.
> 비교 대상이 작으면 증가율이 실제보다 커 보이는 착시가 생기기 때문입니다.

**가속/감속은 '1년치 이익'을 기준으로 봅니다**

최근 4개 분기를 더한 이익(TTM)의 증가율이 어떻게 변하는지를 봅니다.
분기 하나만 놓고 비교하지 않는 이유는 **실제로 재 봤더니 그게 더 정확했기 때문**입니다.

20종목(잘 큰 종목 12 + 부진한 종목 7) 132개 시점 백테스트:

| 무엇을 기준으로 보나 | 방향 적중 | "가속"이라 했을 때 실제로 맞은 비율 |
|---|---|---|
| 분기 단위 (구버전) | 55.0% | 81.6% |
| **1년치(TTM) 기준 (현재)** | **65.8%** | **91.4%** |

1년치로 더하면 계절 장사의 오르내림도 저절로 상쇄되므로, 계절 종목을 따로
다룰 필요가 없어집니다.

| 신호 | 계산 | 성격 |
|---|---|---|
| ① 단기 | 최근 1년치 증가율 − 직전 1년치 증가율 (±3%p 기준) | 민감함 |
| ② 추세 | 최근 4개 증가율의 **회귀 기울기** (±1.5 기준) | 안정적 |

- 두 신호가 **같은 방향** → `가속` 또는 `감속` (확신 높음)
- 한쪽만 방향을 보임 → `약한 가속` / `약한 감속`
- 두 신호가 **서로 반대** → `혼조` — 억지로 한쪽으로 정하지 않고 솔직히 표시합니다
- 분기가 6개 미만이면 옛 방식(분기 단위)으로 돌아가되, 화면에 그렇다고 밝힙니다

> ⚠️ **"감속"은 믿을 것이 못 됩니다.** 검증에서 감속이라 판정한 30회 중
> 실제로 이후 이익이 줄어든 것은 12회(40%)뿐이었습니다. 반대로 "가속"은
> 91%가 맞았습니다. 화면의 계산 근거에도 이 수치를 함께 띄웁니다.

**국면 — 이익 사이클에서 지금 어디에 서 있는가** (10점)

델타는 *얼마나 빨라지는가*를 봅니다. 그런데 델타만으로는 **바닥에서 돌아서는
순간**을 구조적으로 볼 수 없습니다. 직전이 적자면 증가율을 계산할 수 없기 때문입니다.

실제로 놓쳤습니다 — 2025년 반도체 슈퍼사이클에서 마이크론의 이익 폭증은
`FY25 Q1`에 시작됐는데, 델타가 처음 "가속"이라고 말한 것은 **4분기 뒤인 `FY25 Q4`** 였습니다.
국면 신호는 `FY25 Q1`, 바로 그 분기에 켜졌습니다.

| 국면 | 조건 (1년치 이익 기준) | 검증 결과 (2분기 뒤 이익 증가) |
|---|---|---|
| `턴어라운드 진입` | 적자 → 흑자 | 4회 · **100%** |
| `3년내 최고` | 수집 기간 안의 최고를 넘어섬 | 67회 · **95.5%** (크게 오른 구간의 83%를 잡음) |
| `적자 지속` | 1년치가 아직 적자 | 19회 · 57.9% (크게 오른 경우 **0/19**) |
| `고점 이탈` | 그 최고의 80% 아래 | 28회 · 42.9% |
| `중간 자리` | 그 사이 | 14회 · **35.7%** (크게 오른 경우 **0/14**) |

전체 평균은 72.7%입니다.

> ⚠️ **'역대 최고'가 아니라 '3년내 최고'입니다.** 이 대시보드는 3년치만 모으므로,
> 예전에 훨씬 잘 벌다가 무너진 회사가 조금 회복해도 이 표시가 뜹니다(예: 인텔).
> 이름에 '3년내'를 박아 둔 것은 그 오해를 막기 위해서입니다.

> ⚠️ **이 신호가 하지 못하는 일**: 국면은 *종목끼리 줄 세우는* 신호이지
> *한 종목을 언제 사야 하는지* 알려주는 신호가 아닙니다. 검증 표본에서 잘 크는
> 종목은 거의 항상 신호가 있었고 부진한 종목은 거의 항상 없었기 때문에,
> **한 종목 안에서의 타이밍 효과는 재지 못했습니다.** 그대로 밝혀 둡니다.

**델타 예측** (다음 분기 전망치로 앞으로의 방향을 미리 봅니다)

| 표시 | 뜻 | 실제로 정점이었던 비율 (20종목 142개 시점) |
|---|---|---|
| `가속 지속 ↗↗` | 탄력이 더 붙는 중 | **0.0%** (0/23) — "여기는 정점이 아니다"를 확실히 말해 줍니다 |
| `가속 둔화 ↗↘` | 증가 속도가 낮아지는 중 | 16.7% (4/24) — **기준선 21.1%보다 낮습니다** |
| `감속 지속 ↘↘` | 계속 느려짐 | **66.7%** (4/6) · 2분기 뒤 이익 감소 83.3% |
| `반등 ↘↗` | 바닥 통과 가능성 | — |

> ⚠️ **정정합니다.** 이전 판에서는 `가속 둔화 ↗↘` 를 **"정점 근처 가능성"** 이라고
> 적었습니다. 실제로 재 보니 사실이 아니었습니다. 이 신호가 뜬 24번 중 정말로
> 정점이었던 것은 16.7% 로, 아무 때나 찍는 것(21.1%)보다 오히려 낮습니다.
> 문턱을 3·5·10·15·20·30·40·50%p 로 바꿔 가며 전부 시험했지만 어느 값에서도
> 기준선을 유의하게 넘지 못했습니다. 오히려 둔화 폭이 20%p 를 넘는 '큰 둔화'는
> 정점 적중이 0% 였습니다 — 아주 높은 증가율에서 조금 낮은 증가율로 내려온 것은
> 정점이 아니라 그냥 성장이기 때문입니다(마이크론 +106% → +62%).
>
> 그래서 이 신호는 **점수에 반영하지 않습니다.** 화면에는 그대로 보여 주되
> 정점 신호라고 말하지 않습니다.

**진짜 정점 신호는 따로 있습니다** (같은 표본, 정점 기준선 21.1%)

| 신호 | 횟수 | 정점 적중 | 95% 구간 | 2분기 뒤 이익 감소 |
|---|---|---|---|---|
| `감속 지속 ↘↘` | 6 | **66.7%** | 30.0~90.3 ✅ | **83.3%** |
| 델타 `감속` | 47 | 40.4% | 27.6~54.7 ✅ | 48.8% |
| 국면 `고점 이탈` | 36 | 36.1% | 22.5~52.4 ✅ | 61.3% |

이 중 `감속 지속 ↘↘` 과 `가속 지속 ↗↗` 만 델타 점수에 배수로 반영합니다
(각각 ×0.70, ×1.10). 표본이 크지 않아 배수는 보수적으로 잡았습니다.

**델타가속예측** (2분기 경로 — 다음 분기는 가이던스, 다다음 분기는 월가 컨센서스)

증가율이 두 분기에 걸쳐 어떤 길을 갈지 봅니다. 상세 차트에서 주황 **점선**으로 그려집니다.

| 표시 | 경로 | 뜻 |
|---|---|---|
| `가속 지속 ↗↗` | 두 분기 모두 증가율 상승 | 성장 탄력이 이어짐 |
| `가속 후 둔화 ↗↘` | 다음 분기 가속 후 꺾임 | 증가 속도가 낮아지는 경로 (정점 신호로 검증되지 않았습니다) |
| `둔화 지속 ↘↘` | 두 분기 모두 하락 | 사이클 후반 |
| `둔화 후 반등 ↘↗` | 꺾였다가 회복 | **바닥 통과 신호** |

> 월가 컨센서스는 야후 파이낸스가 무료로 제공하는 **LSEG 집계 애널리스트 추정치**입니다.
> 참여 애널리스트 수와 추정치의 30일 변화 속도(리비전 속도)도 함께 수집해
> 전망 팝오버에 표시하고 포워드 점수에 반영합니다.

### 기술 100점 (최종점수의 40%)

| 항목 | 배점 | 기준 |
|---|---|---|
| **추세 5단계** | 50 | 완전 정배열 50 / 준정배열 37.5 / 중립 25 / 추세 훼손 12.5 / 완전 역배열 0 |
| **26주선 기울기** | 10 | 상승 10 / 횡보 5 / 하락 0 |
| **이격도** | 15 | 0~+40% 정상 15 / +40% 초과(과열) 5 / 음수 0 |
| **상대강도(RS)** | 25 | SPY 대비 13주 초과수익 +20%p↑ 25 / +10~20 20 / 0~10 14 / −10~0 7 / −10%p↓ 0 |

**추세 5단계 판정 기준** (주봉 4/13/26/52주 이동평균선)

| 단계 | 상태 | 조건 |
|---|---|---|
| 1 | 완전 정배열 | 주가 > 4주 > 13주 > 26주 > 52주 |
| 2 | 준정배열 | 4주선이 13주선 아래지만 주가 > 26주 > 52주 |
| 3 | 중립 | 주가가 26주선 ±3% 이내 |
| 4 | 추세 훼손 | 주가 < 26주선 또는 13주선이 26주선 하향돌파 |
| 5 | 완전 역배열 | 4주 < 13주 < 26주 < 52주 |

### 최종점수 = 펀더멘털 × 0.6 + 기술 × 0.4

### 판정 5종 (매트릭스)

| 판정 | 조건 | 뜻 |
|---|---|---|
| 🟢 **매수 후보** | 펀더 70+ & 추세 1~2단계 | 실적·주가 모두 양호 (RS 높은 순 정렬) |
| 🟧 **⚠️ 경고** | 펀더 70+ & 추세 4~5단계 | 실적은 좋은데 주가가 꺾임 — **정점 가능성** |
| 🔵 **관찰** | 펀더 40~70 & 추세 1~2단계 | 추세는 좋으나 실적 확신 부족 |
| 🟣 **펀더 없는 모멘텀** | 펀더 40 미만 & 추세 양호 | 실적 근거 없이 주가만 오르는 중 |
| ⬜ **제외** | 그 외 | 조건 미달 |

---

## 🔍 데이터를 모으는 방법 (전부 자동)

### 1) 실적 — 미국 SEC 전자공시(EDGAR)

`edgartools` 라이브러리로 각 종목의 **8-K(Item 2.02 = 실적발표)** 공시에 첨부된
보도자료를 자동으로 읽어, 아래 순서로 논갭 영업이익을 확보합니다.

| 순서 | 방법 | 화면 배지 |
|---|---|---|
| ① | 보도자료의 GAAP→non-GAAP 조정표에서 **non-GAAP operating income** 을 직접 추출 (`adjusted operating income` 등 표기 변형도 인식) | `직접공시` |
| ② | 직접 공시가 없으면 **매출 × non-GAAP GM% − non-GAAP 영업비용** 으로 역산 | `역산` |
| ③ | 그래도 실패하면 SEC **XBRL**로 `GAAP 영업이익 + 주식보상비 + 무형자산상각`, GM%는 GAAP 값 사용 | `근사치` |

**수집 구조** — "둘 중 하나"가 아니라 **합칩니다**

```
① XBRL로 전체 분기 뼈대를 만든다  (빠진 4분기도 계산해 채움)
② 8-K 보도자료에서 논갭 수치를 뽑는다
③ 성공한 분기만 ①에 덮어쓴다 → 분기마다 개별 출처 배지
```

이 구조 덕분에 8-K가 **일부만 성공해도** 그 분기는 `직접공시`로 올라가고,
나머지 분기는 `근사치`로 유지됩니다. (예전에는 하나라도 실패하면 전부 근사치였습니다)

> 🔧 **빠진 4분기 채우기**: 회사는 1~3분기만 10-Q에 3개월 단위로 신고하고,
> **4분기는 10-K에 연간으로만** 신고합니다. 그래서 3개월짜리만 모으면 매년 4분기가
> 통째로 빠집니다. `Q4 = 연간 − (Q1+Q2+Q3)` 로 계산해 채워, 표본이 **연 3개 → 4개**가 됩니다.

- 수집 기간: **3년(12분기)** — 가속/감속 판정을 안정적으로 하려면 표본이 필요합니다.
  화면은 기본적으로 2025년부터 보여주고, 사이드바 **전체 이력 보기**로 3년치를 볼 수 있습니다
- 정렬은 **8-K 제출일(발표일) 기준**
- SEC 요청 시 신원 헤더를 설정하고 요청 제한을 지킵니다
- 받은 데이터는 `data/` 폴더에 저장해 **재실행 시 다시 내려받지 않습니다**

> ⚠️ **SEC 요청자 신원에는 이메일이 반드시 들어가야 합니다**
>
> SEC는 연락 가능한 이메일이 없는 요청을 자동화 도구로 보고 차단(403)합니다.
> 그러면 **모든 종목의 실적이 비어 버립니다**. 기본값에 이메일이 들어 있어 그대로도
> 동작하지만, 다른 주소를 쓰고 싶다면 **비공개**로 설정할 수 있습니다:
>
> Streamlit 앱 화면 오른쪽 아래 **⋮ → Settings → Secrets** 에 아래 한 줄을 넣고 저장
> ```
> SEC_IDENTITY = "홍길동 mymail@example.com"
> ```
> Secrets는 저장소에 올라가지 않아 다른 사람이 볼 수 없습니다.

### 2) 다음 분기 전망 (포워드)

| 순서 | 방법 | 화면 배지 |
|---|---|---|
| ① | 보도자료의 **다음 분기 가이던스**(매출·GM%·영업비용 또는 영업마진)로 계산. 범위로 제시되면 중간값 사용 | `가이던스` |
| ② | 가이던스가 없으면 **yfinance 다음 분기 매출 컨센서스 × 최근 4개 분기 평균 논갭 영업마진** | `추정` |

**리비전 방향**: yfinance의 EPS 추정치 최근 30일 **상향/하향 건수**를 비교해
`+1`(상향) / `0`(중립) / `−1`(하향)을 자동 산출합니다.

### 3) 주가 — 야후 파이낸스

주봉으로 변환 후 4/13/26/52주 이동평균선, 추세 5단계, 26주선 기울기,
이격도(%), SPY 대비 13주 상대강도(RS)를 계산합니다.

---

### 4) 데이터 신뢰도 (6단계)

숫자를 **어떻게 구했는지**에 따라 신뢰도를 매겨 화면에 표시합니다.

| 순위 | 종류 | 신뢰도 | 무엇인가 |
|---|---|---|---|
| 1 | `직접공시` | **95%** | 회사 보도자료의 논갭 영업이익 원문 그대로 |
| 2 | `가이던스` | **85%** | 회사가 직접 발표한 다음 분기 전망 |
| 3 | `역산` | **75%** | 공시된 매출 × 마진 − 비용으로 계산 |
| 4 | `근사치` | **55%** | XBRL 회계데이터로 만든 근사치 |
| 5 | `추정` | **40%** | 애널리스트 매출 전망 × 과거 평균 마진 |
| 6 | `없음` | **0%** | 자료를 구하지 못함 |

**종합 신뢰도 = 실적 신뢰도 × 0.7 + 전망 신뢰도 × 0.3**
(실적 신뢰도는 분기별 출처의 가중평균, 최근 분기일수록 큰 가중치)

> **신뢰도는 점수에 반영하지 않습니다.** 순위는 그대로 두고 참고용으로만 표시합니다.
> 퍼센티지는 자료의 성격을 고려해 정한 값이며 통계로 검증된 수치가 아닙니다
> (`config.py`의 `CONFIDENCE_TIERS`에서 조정 가능).

---

## ⚠️ 데이터의 한계 (꼭 읽어 주세요)

- **포워드는 진짜 컨센서스가 아닙니다.** 무료 데이터로는 월가 컨센서스를 구할 수 없습니다.
  `가이던스` 배지는 회사 발표에 근거하지만, `추정` 배지는 **통계적 추정치**입니다.
  실제 결과와 다를 수 있습니다.
- **`근사치` 배지가 붙은 실적**은 회사가 발표하는 공식 논갭 수치와 차이가 날 수 있습니다.
  XBRL의 GAAP 값에 주식보상비·무형자산상각만 되돌린 값이기 때문입니다.
- **보도자료 형식은 회사마다 다릅니다.** 파싱이 완벽하지 않을 수 있으니,
  중요한 판단 전에는 화면의 **원본 숫자 표**와 실제 공시를 확인하세요.
- **주가는 15분 이상 지연**될 수 있습니다.
- SEC 서버가 느릴 때를 대비해 실적 수집에 **시간 제한**을 두었습니다.
  제한을 넘긴 종목은 "실적 데이터를 찾지 못함"으로 표시되고 주가 지표만 반영됩니다.

---

## 📁 파일 구성

| 파일 | 역할 |
|---|---|
| `app.py` | 대시보드 화면 (Streamlit) |
| `explain.py` | "이 숫자가 어떻게 나왔는지" 설명문 생성 (눌렀을 때 나오는 내용) |
| `config.py` | 종목 목록·배점·기준선 등 **바꿀 만한 설정값 모음** |
| `market_data.py` | 주가 수집·주봉 변환·추세 5단계·상대강도 |
| `sec_fundamentals.py` | SEC 8-K 실적 보도자료 수집·논갭 수치 추출·XBRL 근사 |
| `forward_estimates.py` | 가이던스 파싱·컨센서스 대체 추정·리비전 방향 |
| `scoring.py` | 펀더멘털/기술 점수 계산·매트릭스 판정 |
| `pipeline.py` | 전체 자동 실행 및 순위표 생성 |
| `run_tests.py` | 자동 검증 한 번에 실행 |
| `tests/` | 가상 데이터 기반 자동 검증 (121개) |

### 설정을 바꾸고 싶다면

`config.py` 파일 하나만 고치면 됩니다.

```python
TICKERS = ["CRDO", "CLS", ...]        # 종목 추가·삭제
LOW_BASE_THRESHOLD_USD = 50_000_000   # 저기저 함정 기준 금액
FUND_STRONG = 70.0                    # 매수 후보 펀더멘털 기준선
```

### 코드가 잘 도는지 확인하려면

```bash
python run_tests.py      # Windows
python3 run_tests.py     # Mac
```

인터넷 없이 가상 데이터로 121개 항목을 검증합니다.

---

## ❓ 문제 해결 (FAQ)

**Q. "This repository does not exist" 라고 나와요.**
→ 저장소가 비공개입니다. GitHub Settings → Danger Zone → Change visibility → public 으로 변경하세요.

**Q. 빨간 오류 화면이 뜨거나 "앱을 다시 시작해 주세요" 안내가 나와요.**
→ 코드가 업데이트됐는데 서버가 옛 파일을 붙들고 있는 상태입니다. 화면 **오른쪽 아래 `Manage app`**
→ 오른쪽 위 **⋮** → **`Reboot app`** 을 누르고 1~2분 기다리면 해결됩니다.
(코드를 새로 올린 직후에 가끔 생깁니다)

**Q. 첫 화면이 너무 오래 걸려요.**
→ 정상입니다. SEC에서 13개 종목의 공시를 모두 읽기 때문에 첫 실행은 1~3분 걸립니다.

**Q. 특정 종목만 "실적 데이터를 찾지 못했습니다" 라고 나와요.**
→ 보도자료 형식이 특이하거나 SEC 응답이 느렸을 수 있습니다. **🔄 데이터 새로고침**을 눌러 보세요.
그래도 안 되면 그 종목은 주가 지표(기술 점수)만 반영됩니다.

**Q. 점수가 갑자기 바뀌었어요.**
→ 새 분기 실적이 발표되면 델타 가속·GM% 드라이버 값이 바뀌어 점수가 크게 움직일 수 있습니다.

**Q. 종목을 추가하고 싶어요.**
→ `config.py` 의 `TICKERS` 목록에 티커를 넣고 저장한 뒤, GitHub에 올리면 앱이 자동으로 갱신됩니다.

---

⚠️ **면책**: 본 대시보드는 공개 데이터를 자동 집계한 **참고 자료이며 투자 조언이 아닙니다.**
포워드 수치는 회사 가이던스 또는 통계적 추정치이며 실제 결과와 다를 수 있습니다.
데이터 출처: SEC EDGAR, Yahoo Finance. 투자의 최종 판단과 책임은 이용자 본인에게 있습니다.
