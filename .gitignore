# 파이썬이 자동으로 만드는 캐시 파일 (저장소에 올릴 필요 없음)
__pycache__/
*.pyc

# SEC에서 내려받은 하루짜리 캐시는 무시하되, 측정용 실데이터는 커밋합니다.
# (data/measure 는 수집 로봇이 매일 커밋하는 저장 창고 — 전략.md v2 4장)
data/*
!data/measure/

# 가상환경 폴더
venv/
.venv/

# 운영체제가 만드는 파일
.DS_Store
Thumbs.db
