# 전략.md — v3 헌법

작성: 2026-08-13, v3 재건축 1단계.
이 문서는 이 프로젝트의 **헌법**입니다. 코드보다 위에 있고,
코드가 이 문서와 다르게 움직이면 고칠 쪽은 코드입니다.

모든 숫자는 `측정결과.md`(1~9차)의 실행 출력에서 복사했습니다.
근거가 필요하면 그 문서의 해당 차수를 보면 됩니다.

---

## 0장 — 이 프로젝트가 답하려는 질문

> **"좋은 장세라는 조건 아래에서만, 이익의 첫 신기록을 깬 종목을
> 고르면 기준선을 이길 수 있는가?"**

질문은 두 층으로 되어 있습니다:

- **위층 (때 · 장세)** — 지금이 폭등이 잘 나오는 장세인가?
  실적 신기록이 무리 지어 나오는 "폭"으로 이것을 먼저 가립니다.
- **아래층 (종목 · 첫 새 정보)** — 그 장세 안에서, 이익의
  **첫** 신기록(TTM 조정 EPS 신고점 첫 돌파)을 깬 종목에
  우위가 있는가?

두 층은 따로도 재고, 겹쳐서도 잽니다. 어느 층이든 측정을
통과하지 못하면 그 층은 화면과 판단에 반영하지 않습니다.

### 왜 이 질문인가

v2의 질문("발표 시점의 이익 상태와 앞으로의 이익 전망으로 폭등을
포착할 수 있나")은 네 가지 이유로 폐기했습니다:

1. **이미 답이 나왔습니다 — "아니오".** 서로 다른 표본 세 번
   (사건 98 → 139 → 380건)에서 "가속 확인 후 매수"는 매번
   기준선 이하였습니다 (마지막 19.4% vs 21.8%). 신호 후보 7개를
   쟀는데 채택 0개.
2. **전제가 틀렸습니다.** 추세와 실적은 같이 움직이지만, 발표에서
   그걸 확인한 시점엔 주가가 이미 달린 뒤였습니다 (정상 장세
   구간의 가속 확인 후 매수 = 16건 중 1건, 6.2%).
3. **지배 변수(장세)가 질문에 없었습니다.** 연도별 기준선이
   0%(2021) → 45.6%(2026). 어느 해였느냐가 무엇을 골랐느냐보다
   훨씬 큽니다.
4. **"전망" 재료가 사실상 없습니다.** 회사가 직접 준 분기 EPS
   가이던스는 1,359분기 중 43건뿐. 컨센서스는 창작 금지 원칙으로
   이미 제거했습니다.

새 질문은 그 최대 발견 — **장세가 지배한다** — 를 구조 안에 정면으로
넣습니다. 종목 신호는 장세라는 조건 아래에서만 의미를 묻습니다.

### 답의 세 가지 가능성 (전부 유효한 결과)

- **예** — 두 층이 겹칠 때 기준선과 신뢰구간이 분리된다 → 채택.
- **아니오** — 분리에 실패한다 → 미채택으로 기록하고, 화면은
  사실만 보여 준다. 이것도 성과입니다.
- **판정 불가** — 표본이 모자란다 → 로봇이 데이터를 쌓는 동안
  기다린다. 억지로 결론 내지 않는다.

---

## 1장 — 확정된 지식 (측정으로 이미 판명난 것)

v3는 여기서부터 출발합니다. 이 목록과 어긋나는 가정은
설계에 쓰지 않습니다.

### 기각·판명된 것

1. **"조정 EPS 가속 확인 후 매수"는 신호가 아니다 — 기각 확정.**
   독립 표본 3회 연속 기준선 이하 (34.8% vs 43.9% / 25.0% vs
   34.5% / 19.4% vs 21.8%). 가속폭의 단조성도 3회 연속 없음.
2. **장세가 종목 선택을 지배한다.** 같은 규칙의 연도별 기준선:
   2021년 0.0% → 2022년 18.2% → 2023년 15.6% → 2024년 9.8% →
   2025년 22.2% → 2026년 45.6%.
3. **강세장에서 좋아 보이던 신호는 5년 전체에서 무너졌다.**
   신고점 돌파 50.0%→25.4%, 추세×신고점 60.0%→26.3% (3차→4차).
   시간 분할 없이 잰 우위는 믿지 않는다.
4. **발표일 자체는 특별한 진입일이 아니다.** 아무 날이나 사기
   25.7% > 모든 발표일 사기 21.8% (4차, 29종목 5년).
5. **추세와 펀더멘털은 동행한다 — 그러나 확인하면 늦다.**
   상승추세 구간의 QoQ 상승 비율 55.7% vs 비상승 36.7%,
   분기 YoY 중앙값 +46.3% vs 0.0%. 동행은 사실이지만 발표
   시점에 그걸 확인하고 사면 우위가 없었다 (4번의 측정 일관).
6. **"성장의 꾸준함"(델타 변동성)은 우위 없음. 연속 가속 횟수도
   평평 (0회 22.0% vs 2회+ 20.8%).**

### 유망하나 미확정인 것 (v3가 이어서 잰다)

7. **실적 폭 게이지 (위층 후보).** 29종목 게이지에서 ON 30.7%
   vs OFF 12.3% — 이 프로젝트에서 유일하게 신뢰구간이 분리된
   관측. 실적 폭이 주가 폭보다 먼저 움직인 실제 사례 2건
   (2024 봄 붕괴 예고 · 2025 봄 폭발 예고). **단,** 문턱 20%는
   79종목 유니버스에선 항상 ON이라 변별력을 잃었다 — 문턱은
   유니버스가 바뀌면 이식되지 않는다. 재보정안은 새 사전 등록을
   거쳐 시간 방향 데이터로만 판정한다.
8. **신고점 첫 돌파 = H2b (아래층 후보).** 세 표본 모두 기준선
   위 (35.0% → 27.1% → 32.6%), 마지막엔 구간 간격이 하한 20.9
   vs 기준선 상한 22.1까지 좁혀짐. 아직 미채택. 강건성 감사에서
   창 80일·문턱 25%p로 바꾸면 우위가 뒤집혔다 — 조건에 민감.
9. **주가 폭(시장 자신의 추세 폭)은 아무것도 못 가린다**
   (ON 24.8% vs OFF 26.5%). 정보는 실적 신기록의 폭에 있었다.

### 데이터의 형편 (설계가 전제해야 할 현실)

10. 79종목 · 5년 · 발표 있는 분기 1,359 · 조정 EPS 1,160.
    조정 EPS 미발표 종목 6개 (ZETA·APP·TXN·ROKU·TSLA·FSLR) —
    이 중 3개는 조정 EBITDA로 대신 볼 수 있다 (표시용).
11. 분기 EPS 가이던스는 희소하다 (43건). 가이던스 기반 가설(H3)은
    표본 부족으로 판정 불가였다.
12. 2021~2022 약세장이 표본에 있으나 사이클 전환은 사실상 2번뿐.
    같은 주에 발표가 몰려(최대 28건) 사건이 완전 독립이 아니므로
    윌슨 구간은 실제보다 좁게 나올 수 있다.

---

## 2장 — 절대 규칙

### 제1조 — 창작 금지

- **숫자가 없으면 만들지 않는다.** XBRL 근사·역산·추정 전망·컨센서스
  전부 금지. 없는 값은 **"없음"** 으로 둔다.
- 검사에서 탈락한 값은 고치지 않고 버린다. "없음"은 안전하고
  "틀림"은 위험하다 (TSLA 열 순서 사건이 증거).
- 전망 입력은 **회사가 직접 발표한 가이던스뿐**이다.
- 값 검사(`data_quality.py`)는 나중이 아니라 **1단계**다 —
  수집한 숫자는 계산에 들어가기 전에 검사한다.

### 제2조 — 사전 등록

- 문턱·창·표적·채택 기준은 **데이터를 보기 전에** `측정결과.md`에
  적고 머지한다. 결과를 보고 규칙을 고치지 않는다.
- **채택 기준 (고정)**: 신호 그룹의 윌슨 95% 구간 **하한**이
  같은 표본 기준선(모든 발표)의 윌슨 **상한**보다 높아야 한다 —
  즉 신뢰구간 완전 분리. n≥10.
- **측정 기본형 (고정)**: 창 60거래일 · 폭등 = SPY 대비 +20%p ·
  기준선② = 같은 표본의 모든 발표 사건.
- 탐색 표본은 판정에 재사용하지 않는다. 새 데이터란
  ① 시간 방향(앞으로 쌓이는 발표) ② 종목 방향(신호가 본 적 없는
  종목의 과거) 두 가지다. ②를 쓸 때는 "같은 장세를 공유한다"는
  한계를 반드시 병기한다.

### 제3조 — 검증 실패 시 미반영, 그리고 현실적인 대책

미채택이라고 프로젝트가 멈추는 게 아닙니다. 실패했을 때 하는
일이 정해져 있습니다:

- **미반영**: 채택 기준을 못 넘은 신호는 화면의 판단 요소·점수·
  추천 어디에도 반영하지 않는다. "채택된 신호 없음"을 그대로
  표시한다.
- **정직화 표시**: 델타·신고점·게이지 같은 값은 **사실**로만
  보여 주고, 옆에 그 상태의 **과거 실측 폭등률과 기준선**을 함께
  적는다 (예: "이 상태의 과거 폭등률 32.6%, 기준선 18.9%,
  미채택"). "가속 = 매수"처럼 읽히는 요소는 두지 않는다.
- **자동 축적·재판정**: 로봇이 매일 수집하고, 판정은 사람 손이
  아니라 자동으로 다시 계산된다. 미채택 가설은 버리는 게 아니라
  표본이 쌓일 때까지 **관찰 목록**에 둔다 (H2b가 그 예 —
  구간 간격이 좁혀지는 중).
- **장치 먼저 의심**: 신호가 이상하면 신호를 의심하기 전에
  측정 장치를 감사한다 (8차 감사에서 결함 2건이 실제로 나왔고,
  수정 후에도 결론은 불변임을 확인했다).
- **표본이 원천적으로 부족한 가설**(H3처럼)은 "판정 불가"로
  기록하고 데이터 형편이 바뀔 때까지 접어 둔다.

---

## 3장 — 사고 백서 (v1·v2에서 실제로 터진 사고와 그때 만든 규칙)

규칙은 전부 실제 사고에서 나왔습니다. 사고를 잊으면 규칙도 잊힙니다.

| # | 사고 (실제로 있었던 일) | 만들어진 규칙 |
|---|---|---|
| 1 | v1: 100점 점수제·신뢰도 등급·역산 전망을 먼저 만들고 나중에 측정하니 근거가 없었다 | 점수를 먼저 만들지 않는다. 측정으로 확인된 것만 화면에 올린다 |
| 2 | 실행 스위치 뒤에 적혀 **한 번도 돌지 않은 테스트 12개** | 새 테스트는 일부러 깨뜨려 빨간 불을 본 뒤에만 믿는다 |
| 3 | 실행 불가능한 위치의 구제 코드가 루멘텀 전망을 무너뜨림 | 새 코드 경로는 로그나 테스트로 실행됨을 증명한다 |
| 4 | 커밋 메시지에 테스트 개수를 310으로 적었으나 실제 301 | 보고 숫자는 실행 출력에서 복사한다. 기억으로 적지 않는다 |
| 5 | 같은 초에 두 번 고치니 옛 `__pycache__` 코드가 그대로 돌았다 | 고친 직후 캐시를 지우고, 종료코드를 직접 확인한다 |
| 6 | **신고점 망각**: 이력이 끊기면 옛 정점을 잊고 낮은 값을 신고점으로 판정 (실데이터 27건) | 문서의 정의와 구현이 같은지 실데이터로 대조한다. 회귀 시험을 남긴다 |
| 7 | **가짜 TTM**: 결측 분기를 건너뛰어 4분기 합을 이어붙임 | 몇 년 떨어진 분기를 이웃으로 만들지 않는다. 진짜 4분기 합만 쓴다 |
| 8 | **짝짓기 사고**: 예비 매출 공지가 실적 발표를 밀어내 조정 EPS $1.07이 통째로 버려짐 (CRDO 26Q3) | 이익 숫자를 실은 발표가 항상 이긴다 |
| 9 | TSLA 표가 과거→현재 열 순서라 1년 전 값을 최신으로 수집 | 확신 없는 자동 추출은 차단한다. "틀림"은 "없음"보다 위험하다 |
| 10 | 13종목 전부 관심 성장주라 "아무 날이나 사도" 33% — 뭘 해도 좋아 보였다 | 부진·횡보 종목을 반드시 섞는다 (지금 79종목은 섞여 있다) |
| 11 | 강세장 구간만 보고 신고점 50%·추세×신고점 60%라 믿었다가 5년 전체에서 25~26%로 붕괴 | 앞/뒤 시간 분할을 항상 함께 보고한다 |
| 12 | 실적 폭 문턱 20%가 79종목에선 **4년 내내 항상 ON** — 변별력 상실 | 문턱은 유니버스에 종속된다. 재보정은 사후 조정이 아니라 새 사전 등록으로만 한다 |
| 13 | 다중 에이전트 13개 중 11개 실패 — 58만 토큰 쓰고 결과 0건 | 다중 에이전트 워크플로를 쓰지 않는다. 한 세션 전수 처리가 더 정확했다 |
| 14 | 조정 EBITDA를 뽑고도 분기 행에 복사하지 않아 버림 (3번 유형의 재발) | 파서의 출력이 저장까지 도달하는지 배관 전체를 시험한다 |

---

## 4장 — 구조 (어떤 몸이 무슨 일을 하는가)

원칙: **수집과 판단을 분리한다.** 로봇은 수집만 하고,
판단(측정·판정)은 등록된 규칙대로 자동 계산되며, 화면은
그 결과를 그대로 보여 줄 뿐이다.

### 지금 있는 몸 (v2에서 검증되어 살아남은 것)

| 몸 | 하는 일 |
|---|---|
| 수집 로봇 (`collect_job.py` + 깃허브 액션) | 매일 09:30 UTC, 깃허브 서버에서 SEC·주가를 받아 `data/measure/`로 커밋. 반쯤 실패한 날은 커밋을 막는다 |
| 보도자료 파서 (`sec_fundamentals.py`) | 8-K에서 조정 EPS·조정 EBITDA 추출. 규칙 하나하나가 사고 백서의 흉터 |
| 가이던스 파서 (`forward_estimates.py`) | 회사가 직접 준 분기 가이던스만 추출 |
| 값 검사 (`data_quality.py`) | 이상한 값은 고치지 않고 없앤다. 계산 전에 실행 |
| 주가 수신 (`market_data.py`) | 일봉 수집 (로봇 환경에서만 동작) |
| 저장 (`measure_store.py`) | 수집 결과를 `snapshot.json`으로 꾸림. ⚠️ 4단계에서 운명 재결정 |
| 시험 (`tests/` + `run_tests.py`) | 실물 원문 기반 시험 다수. 커밋 전 필수 통과 |

### v3에서 새로 지을 몸 (로드맵 3~6단계)

| 몸 | 하는 일 | 원칙 |
|---|---|---|
| 데이터 계층 | snapshot을 측정이 쓰기 좋은 형태로 | 값 검사 통과분만 통행 |
| 측정 장치 | 0장의 질문을 층별로 잰다 | 사전 등록 후에만 작성 |
| 자동 판정 | 등록된 채택 기준으로 재판정·기록 | 사람 개입 없음 |
| 계기판 | 사실 + 과거 실측 적중률 + 판정 상태 | 정직화 (2장 제3조). 모바일 412px 실렌더 확인 |

---

## 5장 — 탐색 규율

새 아이디어를 찾는 일(탐색)과 그 아이디어를 믿는 일(판정)은
철저히 분리합니다.

1. **탐색은 채택 근거가 아니다.** 요인 여러 개를 동시에 재면
   우연한 승자가 반드시 나온다 (실제로 8개를 재니 그랬다).
2. **잰 것은 좋든 나쁘든 전부 기록한다.** 좋은 것만 골라 적으면
   서랍 속 실패가 사라져 우연이 실력처럼 보인다.
3. **시간 분할은 필수다.** 앞시기/뒤시기 모두에서 우세해야
   후보 자격이 있다 (뒤시기는 기준선 자체가 ~50%인 초강세장이라
   시기 구분 없이 보면 모든 요인이 좋아 보인다 — 실측).
4. **탐색에서 나온 후보는 새 H번호를 붙여 사전 등록한 뒤,
   새 데이터로만 판정한다.** 탐색에 쓴 표본은 판정에 재사용 금지.
5. **역방향 탐색도 허용된다** — "폭등한 사례에서 무엇이 먼저
   움직였나"를 거꾸로 해부하는 것. 단 오른 구간만 보지 말고
   비상승 구간과 나란히 본다 (3차 측정이 그 모범).
6. **군집 효과를 명시한다.** 같은 시기의 발표들은 같은 장세를
   공유하므로 통계 구간은 실제보다 좁게 나온다.
7. 탐색 결과를 화면에 올릴 때는 "탐색치"라고 명시하고 판정
   상태(미채택)를 함께 적는다.

---

## 6장 — 로드맵

### 재건축 단계 (인수인계에서 이어받음)

| 단계 | 할 일 | 상태 |
|---|---|---|
| 0 | 봉인·청소·인수인계 | ✅ 완료 |
| 1 | 전략.md v3 — 이 문서. 질문 확정 | ✅ 이 문서 |
| 2 | CLAUDE.md 3장(모델링 원칙)을 이 헌법에서 옮겨 채움 | 다음 |
| 3 | 뼈대 설계도 — 코드 쓰기 전에 모듈 경계 | |
| 4 | 데이터 계층 — 한 번에 파일 하나씩. `measure_store.py` 운명 결정 | |
| 5 | 측정 장치 + **사전 등록이 먼저** | |
| 6 | 계기판 — 살아남은 것만, 정직화 원칙대로 | |

### 측정 로드맵 (0장의 질문을 층별로)

1. **위층 (장세)**: 실적 폭 게이지의 문턱 재보정안을 **새로 사전
   등록** (고정 20% 대신 유니버스에 이식 가능한 정의 — 예:
   백분위 기준. 구체 문턱은 5단계 등록 때 확정). 판정은 시간
   방향 데이터로만.
2. **아래층 (종목)**: H2b(신고점 첫 돌파)를 등록 그대로 계속
   관찰. 로봇이 표본을 쌓고 자동 재판정 (9차 기준 하한 20.9 vs
   기준선 상한 22.1 — 간격 좁혀지는 중).
3. **결합 (때 × 종목)**: 위층 게이지가 재등록된 뒤, [위층 ON ×
   H2b]를 새 H번호로 사전 등록해 판정. 표본이 작을 것이 예상되므로
   (과거 유사 조합 n=15~19) 판정을 서두르지 않는다.
4. **계속 유지**: 로봇 매일 수집 · 분기마다 자동 재판정 ·
   장치 감사는 신호 의심보다 먼저.

### 이 문서를 고치는 법

- 0~2장(질문·확정 지식·절대 규칙)의 수정은 **측정 근거**가 있을
  때만 한다. 근거는 `측정결과.md`의 차수를 인용한다.
- 3장(사고 백서)은 사고가 날 때마다 **추가만** 한다. 지우지 않는다.
