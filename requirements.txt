# 이 파일은 프로그램 실행에 필요한 라이브러리 목록입니다.
# 터미널에서 `pip install -r requirements.txt` 를 실행하면 자동으로 설치됩니다.
#
pandas>=2.2         # 표(데이터) 계산 도구
numpy>=1.26         # 숫자 계산 도구
yfinance>=0.2.54    # 야후 파이낸스에서 주가를 받아오는 도구
edgartools>=5.0     # 미국 SEC 전자공시(8-K 실적발표)를 받아오는 도구
streamlit>=1.60     # 계기판 화면 (v3 6단계에서 재도입)
