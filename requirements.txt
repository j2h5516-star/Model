# 이 파일은 앱 실행에 필요한 프로그램(라이브러리) 목록입니다.
# 터미널에서 `pip install -r requirements.txt` 를 실행하면 자동으로 설치됩니다.

yfinance>=0.2.54    # 야후 파이낸스에서 주가 데이터를 받아오는 도구
streamlit>=1.49     # 웹 대시보드를 만들어 주는 도구
plotly>=5.24        # 캔들차트 등 그래프를 그리는 도구
pandas>=2.2         # 표(데이터) 계산 도구
